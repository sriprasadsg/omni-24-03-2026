---
phase: 50-native-security-scanning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/agent_security_feed_service.py
  - backend/agent_security_feed_endpoints.py
  - backend/router_registry.py
  - backend/tests/test_agent_security_feed.py
autonomous: true
requirements: [NSCAN-02, NSCAN-03]
must_haves:
  truths:
    - "The backend builds a SQLite feed bundle with hash_sigs, yara_rules, url_feed, ip_feed, and a manifest(version, created_at)"
    - "The bundle is signed with an ed25519 private key (detached signature over the file bytes); the private key never leaves the server and is not committed"
    - "GET /api/agents/security/feed-bundle returns the bundle bytes + signature + version; ?have=<version> yields a no-op when the agent is already current"
    - "The matching ed25519 public key is retrievable for embedding in the agent (constant/export), and the bundle includes an EICAR test signature so scans can be verified without real malware"
  artifacts:
    - path: "backend/agent_security_feed_service.py"
      provides: "build + ed25519-sign the SQLite feed bundle; load/generate the signing keypair (private persisted server-side, not committed)"
      contains: "ed25519"
      min_lines: 60
    - path: "backend/agent_security_feed_endpoints.py"
      provides: "GET /api/agents/security/feed-bundle (versioned, signed)"
      contains: "feed-bundle"
      min_lines: 25
    - path: "backend/tests/test_agent_security_feed.py"
      provides: "hermetic tests: bundle build, signature verifies with the public key, version no-op, EICAR row present"
      contains: "ed25519"
      min_lines: 50
  key_links:
    - from: "backend/router_registry.py"
      to: "backend/agent_security_feed_endpoints.py"
      via: "_load(app, 'agent_security_feed_endpoints', 'router')"
      pattern: "agent_security_feed_endpoints"
  prohibitions:
    - "MUST NOT commit the ed25519 private signing key (persist to a gitignored data path or env; tests may generate an ephemeral key)"
    - "MUST NOT make any external network call to build the bundle (feed source is vendored/local)"
    - "MUST NOT ship real live malware — use the standard EICAR test string/hash only"
---

<objective>
Deliver the signed-feed-bundle foundation (reused by Phase 51/52): a backend service that assembles a SQLite feed DB (hash signatures, YARA rules, URL/IP feeds + manifest) and signs it with ed25519, plus a versioned `GET /api/agents/security/feed-bundle` endpoint. The agent (50-02) fetches + verifies this offline.

Purpose: D-03 locks a signed, self-contained feed bundle as the milestone's air-gapped update mechanism. NSCAN-02's "no live lookup" requires the agent to carry the feed locally; this plan produces and serves it.
Output: feed-bundle build/sign service + endpoint + hermetic tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@.planning/phases/50-native-security-scanning/50-RESEARCH.md
@.planning/phases/50-native-security-scanning/50-CONTEXT.md

# Read-first anchors:
@backend/agent_fleet_geo_endpoints.py    # router/registry + tenant patterns (recent clean example)
@backend/router_registry.py               # _load registration seam
@backend/agent_security_endpoints.py      # existing security surface + alert shape (do not modify here)
</execution_context>

<artifacts>
- NEW `backend/agent_security_feed_service.py` — build + ed25519-sign the SQLite bundle; keypair load/generate.
- NEW `backend/agent_security_feed_endpoints.py` — `GET /api/agents/security/feed-bundle` (versioned).
- EDIT `backend/router_registry.py` — register the router.
- NEW `backend/tests/test_agent_security_feed.py` — hermetic build/sign/version/EICAR tests.
</artifacts>

<tasks>

<task type="tdd">
  <name>Task 1: failing tests for bundle build + signature + versioning</name>
  <files>backend/tests/test_agent_security_feed.py</files>
  <read_first>backend/tests/test_agent_fleet_geo.py for the hermetic FastAPI harness (dependency_overrides, TestClient).</read_first>
  <action>Write hermetic tests importing `agent_security_feed_service`: (a) `build_bundle()` returns bytes for a valid SQLite DB containing tables hash_sigs/yara_rules/url_feed/ip_feed/manifest and at least the EICAR test hash row; (b) `sign_bundle(bytes)` produces a detached signature that verifies against `get_public_key()` (ed25519) and fails on a tampered byte; (c) the endpoint `GET /api/agents/security/feed-bundle` returns 200 with the bundle + a version, and with `?have=<current_version>` returns a no-op (e.g. 304 or `{updated: false}`); (d) the persisted private key is never returned by any endpoint. Use an ephemeral generated keypair in the test (monkeypatch the key path).</action>
  <verify>
    <automated>backend/venv/bin/python -m pytest backend/tests/test_agent_security_feed.py -q 2>&1 | tail -12</automated>
  </verify>
  <done>Tests exist and FAIL (service/endpoint not implemented).</done>
</task>

<task type="auto">
  <name>Task 2: feed-bundle build/sign service + keypair management</name>
  <files>backend/agent_security_feed_service.py</files>
  <action>Implement `agent_security_feed_service.py`: `build_bundle() -> bytes` creates an in-memory/temp SQLite DB with tables `hash_sigs(sha256, md5, verdict, name)`, `yara_rules(name, severity, source)`, `url_feed(pattern, kind, verdict)`, `ip_feed(cidr, verdict)`, `manifest(version, created_at)`, seeded from a small **vendored** local source (include the EICAR test file's SHA256 in hash_sigs and one sample YARA rule + a couple of documented sample C2 IP CIDRs / bad-URL patterns — clearly marked sample data, no live fetch). `bundle_version()` derives a stable version (e.g. sha256 of the bytes or manifest date+counter). ed25519 keypair: `_load_or_create_keypair()` reads the private key from a gitignored data path (or env `AGENT_FEED_SIGNING_KEY`); generates + persists if absent; `get_public_key() -> bytes` returns the public key (for the agent to embed). `sign_bundle(data) -> bytes` returns a detached ed25519 signature. Use the existing `cryptography` library (Ed25519PrivateKey). Add the key path to `.gitignore` if not already covered.</action>
  <verify>
    <automated>backend/venv/bin/python -c "import agent_security_feed_service as s; b=s.build_bundle(); sig=s.sign_bundle(b); print('bundle bytes', len(b), 'sig', len(sig), 'ver', s.bundle_version())" 2>&1 | tail -3</automated>
  </verify>
  <done>Bundle builds, signs, and versions; keypair persists outside git.</done>
</task>

<task type="auto">
  <name>Task 3: feed-bundle endpoint + registration</name>
  <files>backend/agent_security_feed_endpoints.py, backend/router_registry.py</files>
  <read_first>backend/agent_fleet_geo_endpoints.py for the router/dep shape; router_registry.py for the _load line.</read_first>
  <action>Create `agent_security_feed_endpoints.py`: `router = APIRouter(prefix="/api/agents/security", tags=["Agent Security Feed"])`. `GET /feed-bundle` (agent-authenticated via `Depends(verify_agent_key)` — the same agent-key dep the agent uses on `agent_tasks_endpoints` `POST /instructions/result`; NOT `get_current_user`, since the agent calls this) returns the current bundle bytes + `X-Feed-Version` header + detached signature (as a header or a multipart/JSON envelope — pick one and document it); when `?have=<version>` equals the current version, return a no-op (304 or `{updated: false}`) without the body. Then register `_load(app, "agent_security_feed_endpoints", "router")` in router_registry.py.</action>
  <verify>
    <automated>backend/venv/bin/python -m pytest backend/tests/test_agent_security_feed.py -q 2>&1 | tail -8</automated>
  </verify>
  <done>All Task 1 tests pass; endpoint registered and serves the signed bundle + version no-op.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-50-01 | Tampering | feed bundle in transit / at rest on agent | mitigate | Detached ed25519 signature; agent verifies before load (50-02). Private key server-side only, gitignored |
| T-50-02 | Info Disclosure | signing private key leak | mitigate | Never committed, never returned by any endpoint (test d); persisted to gitignored path/env |
| T-50-03 | Tampering | shipping real malware in the bundle | avoid | EICAR test artifact only; sample feed data documented |
| T-50-SC | Tampering | package installs | accept | Uses existing `cryptography`; no new pip dep |
</threat_model>

<verification>
- `pytest backend/tests/test_agent_security_feed.py` green (build, sign/verify, tamper-fail, version no-op, EICAR present, key never exposed).
- Bundle builds offline; private key not in git (`git check-ignore` the key path).
</verification>

<success_criteria>
A signed SQLite feed bundle (hash sigs incl. EICAR, YARA rules, URL/IP feeds, manifest) is built + ed25519-signed server-side and served versioned at `GET /api/agents/security/feed-bundle` with a current-version no-op; the public key is retrievable for the agent; the private key never leaves the server.
</success_criteria>

<output>
Create `.planning/phases/50-native-security-scanning/50-01-SUMMARY.md` when done.
</output>
