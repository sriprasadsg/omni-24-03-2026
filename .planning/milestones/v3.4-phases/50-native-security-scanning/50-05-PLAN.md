---
phase: 50-native-security-scanning
plan: 05
type: execute
wave: 3
depends_on: ["50-02", "50-03", "50-04"]
files_modified:
  - agent-install/omni-agent-rs/src/instructions.rs
  - agent-install/omni-agent-rs/src/capabilities/feed_bundle.rs
autonomous: true
requirements: [NSCAN-03]
must_haves:
  truths:
    - "The agent builds on linux and cargo-checks for x86_64-pc-windows-gnu with the crates already added by 50-02 (ed25519-dalek) and 50-03 (yara-x) — this plan adds NO new crates"
    - "instructions.rs dispatches scan_file/scan_url/scan_hash/scan_ip commands to capabilities::security_scan and POSTs the verdict to /api/agents/{id}/security/scan-result"
    - "A feed_bundle::update refresh runs (on startup and/or periodically) so the agent has a verified feed before scanning"
    - "The agent binary is rebuilt (stale-cache invalidated) and contains the new scan commands"
  artifacts:
    - path: "agent-install/omni-agent-rs/Cargo.toml"
      provides: "yara-x + ed25519-dalek deps"
      contains: "yara-x"
    - path: "agent-install/omni-agent-rs/src/instructions.rs"
      provides: "scan_file/scan_url/scan_hash/scan_ip dispatch arms + verdict POST"
      contains: "scan_file"
  key_links:
    - from: "agent-install/omni-agent-rs/src/instructions.rs"
      to: "agent-install/omni-agent-rs/src/capabilities/security_scan.rs"
      via: "capabilities::security_scan::scan_* then POST to /security/scan-result"
      pattern: "security_scan"
  prohibitions:
    - "MUST NOT enable a yara-x feature that breaks the x86_64-pc-windows-gnu cross build"
    - "MUST NOT hard-fail the dispatcher when the feed is degraded — post the degraded verdict"
---

<objective>
Wire NSCAN-03 end-to-end: add the `yara-x` + `ed25519-dalek` Cargo deps, dispatch the `scan_file`/`scan_url`/`scan_hash`/`scan_ip` commands through `instructions.rs` to `capabilities::security_scan`, POST each verdict to the 50-04 endpoint, and ensure the feed is refreshed before scanning — mirroring the existing `network_scan` arm.

Purpose: closes the loop — operator/event triggers a scan command, the agent runs the native engine and reports the verdict. This is the single Cargo edit for the phase (unblocks 50-02/50-03 builds too).
Output: Cargo deps + dispatch arms + feed refresh.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@.planning/phases/50-native-security-scanning/50-RESEARCH.md
@.planning/phases/50-native-security-scanning/50-CONTEXT.md

# Read-first anchors:
@agent-install/omni-agent-rs/src/instructions.rs   # the "network_scan" arm: capability -> POST -> status json (clone it)
@agent-install/omni-agent-rs/src/capabilities/security_scan.rs   # scan_* fns
@agent-install/omni-agent-rs/src/capabilities/feed_bundle.rs     # update() refresh + embedded public key
@backend/agent_security_feed_service.py            # the real ed25519 PUBLIC key to embed in feed_bundle.rs
</execution_context>

<artifacts>
- EDIT `agent-install/omni-agent-rs/src/instructions.rs` — four dispatch arms + verdict POST + feed refresh call.
- EDIT `agent-install/omni-agent-rs/src/capabilities/feed_bundle.rs` — replace the placeholder `FEED_PUBLIC_KEY` with the real 50-01 public key.
- (Crates: `yara-x` + `ed25519-dalek` are already in Cargo.toml from 50-03/50-02 — this plan adds none.)
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: full-agent build gate (linux + windows-gnu)</name>
  <files>agent-install/omni-agent-rs/src/instructions.rs</files>
  <action>`yara-x` (50-03) and `ed25519-dalek` (50-02) are already in Cargo.toml — this plan adds no crates. Run `cargo build` (linux) and `cargo check --target x86_64-pc-windows-gnu` to confirm the whole agent (all scan modules + the new dispatch) still builds on both targets before wiring dispatch. If windows-gnu regresses, resolve before proceeding.</action>
  <verify>
    <automated>cd agent-install/omni-agent-rs && cargo build 2>&1 | tail -6 && cargo check --target x86_64-pc-windows-gnu 2>&1 | tail -8</automated>
  </verify>
  <done>Both linux build + windows-gnu check pass with yara-x + ed25519-dalek.</done>
</task>

<task type="auto">
  <name>Task 2: embed the real feed public key</name>
  <files>agent-install/omni-agent-rs/src/capabilities/feed_bundle.rs</files>
  <read_first>backend/agent_security_feed_service.py get_public_key() — obtain the 32-byte ed25519 public key.</read_first>
  <action>Replace the placeholder `FEED_PUBLIC_KEY` const in feed_bundle.rs with the real public-key bytes from the backend signing keypair (obtain via `backend/venv/bin/python -c "import agent_security_feed_service as s; print(list(s.get_public_key()))"` or the documented export). Document where the key came from so it can be rotated.</action>
  <verify>
    <automated>cd agent-install/omni-agent-rs && cargo build 2>&1 | tail -4</automated>
  </verify>
  <done>Real public key embedded; agent builds.</done>
</task>

<task type="auto">
  <name>Task 3: dispatch arms + verdict POST + feed refresh</name>
  <files>agent-install/omni-agent-rs/src/instructions.rs</files>
  <read_first>the "network_scan" arm — clone its structure (capability call → POST results → status json).</read_first>
  <action>Add four `match` arms — `"scan_file"`, `"scan_url"`, `"scan_hash"`, `"scan_ip"` — each reading its parameter (path/url/hash/ip) from `item.parameters`, calling the matching `capabilities::security_scan::scan_*`, POSTing the verdict to `{api_base}/api/agents/{host}/security/scan-result` with bearer auth (mirror the network_scan POST), and returning the verdict as the arm's status JSON. Ensure a feed refresh (`capabilities::feed_bundle::update(cfg)`) runs before scanning — call it at agent startup and/or lazily before the first scan; a degraded feed must still let the scan run and POST a degraded verdict (never hard-fail the dispatcher). Trigger a rebuild (new .rs mtime satisfies agent_rust_builder's cache check).</action>
  <verify>
    <automated>cd agent-install/omni-agent-rs && cargo build 2>&1 | tail -5 && cargo test 2>&1 | tail -8</automated>
  </verify>
  <done>Four scan commands dispatch to the engine and POST verdicts; feed refresh wired; agent builds + tests green.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-50-10 | Elevation of Privilege | scan command triggers arbitrary action | mitigate | Commands are constrained to scan_* → read-only scan + POST; no exec; operator-role gating lives on the backend trigger (Phase 54) |
| T-50-11 | Tampering | windows cross-build broken by yara-x feature | mitigate | Task 1 windows-gnu gate; feature paring / documented fallback |
| T-50-SC | Tampering | Cargo deps (yara-x, ed25519-dalek) | accept | Reputable crates (VirusTotal BSD / dalek); pinned versions; pure Rust |
</threat_model>

<verification>
- `cargo build` (linux) + `cargo check --target x86_64-pc-windows-gnu` clean with yara-x + ed25519-dalek; `cargo test` green.
- The four scan commands appear in instructions.rs and POST to `/security/scan-result`.
- End-to-end (manual, deferred to UAT): a `scan_file` command on the EICAR file yields a Malicious verdict ingested by 50-04 and a critical native alert.

<human-check>Trigger a scan_file on the EICAR test file via the operator/command path and confirm the backend receives a Malicious native verdict + raises a critical alert; confirm scan_url/scan_ip/scan_hash return feed-based verdicts offline.</human-check>
</verification>

<success_criteria>
The agent builds (linux + windows-gnu) with yara-x + ed25519-dalek and dispatches scan_file/scan_url/scan_hash/scan_ip through the native engine, refreshing the verified feed first and POSTing each verdict to the backend — closing NSCAN-03 end-to-end.
</success_criteria>

<output>
Create `.planning/phases/50-native-security-scanning/50-05-SUMMARY.md` when done.
</output>
