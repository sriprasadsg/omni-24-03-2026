---
phase: 64-key-rotation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/secret_manager_service.py
  - backend/config.py
  - backend/tests/test_secret_manager_service.py
  - agent-rust/src/instructions.rs
  - agent-rust/src/remediation/key_rotation.rs
  - agent-rust/src/remediation/mod.rs
  - backend/autonomous_remediation_service.py
  - backend/tests/test_key_rotation_tracer.py
autonomous: true
requirements: [AUTO-02.1]
user_setup:
  - service: HashiCorp Vault
    why: "Secure secret management for key rotation"
    env_vars:
      - name: VAULT_ADDR
        source: "HashiCorp Vault endpoint URL (e.g., http://127.0.0.1:8200)"
      - name: VAULT_TOKEN
        source: "HashiCorp Vault authentication token (or configure other auth method)"
---

<objective>
Implement initial HashiCorp Vault integration and define the agent's `rotate_key` instruction.
This plan establishes the end-to-end communication path for secure key rotation, proving the architecture from backend dispatch to agent mock execution.

Purpose: To lay the secure foundation for autonomous key rotation by integrating with a secret management solution and verifying the agent's ability to receive and acknowledge key rotation commands.
Output:
- A functional backend client for HashiCorp Vault.
- An agent instruction for `RotateKey`.
- A verified end-to-end communication path for `RotateKey` instructions.
</objective>

<execution_context>
@/home/user/enterprise-omni-agent-ai-platform/.claude/gsd-core/workflows/execute-plan.md
@/home/user/enterprise-omni-agent-ai-platform/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v3.4-phases/64/RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backend HashiCorp Vault Client & Configuration</name>
  <files>
    backend/secret_manager_service.py
    backend/config.py
    backend/tests/test_secret_manager_service.py
  </files>
  <behavior>
    - Test 1: Vault client successfully connects to VAULT_ADDR.
    - Test 2: Vault client can read a mock secret from a specified path.
    - Test 3: Handles connection errors gracefully.
  </behavior>
  <action>
    Create `backend/secret_manager_service.py`. Implement a Python client for HashiCorp Vault using the `hvac` library.
    Define a `VaultService` class with methods for `connect()` and `read_secret(path: str)`.
    In `backend/config.py`, add `VAULT_ADDR` and `VAULT_TOKEN` environment variables, ensuring they are loaded securely.
    Create `backend/tests/test_secret_manager_service.py` with unit tests to verify client connectivity and mock secret retrieval.
    Ensure that `hvac` is added to `requirements.txt` if not already present, or suggest `pip install hvac`.
  </action>
  <verify>
    <automated>
      pip install hvac # if not already present
      pytest backend/tests/test_secret_manager_service.py
    </automated>
  </verify>
  <done>
    The `VaultService` can successfully connect to the configured Vault address and retrieve secrets using the provided token. Unit tests confirm this behavior.
  </done>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: Agent `rotate_key` Instruction & End-to-End Tracer</name>
  <files>
    agent-rust/src/instructions.rs
    agent-rust/src/remediation/key_rotation.rs
    agent-rust/src/remediation/mod.rs
    backend/autonomous_remediation_service.py
    backend/tests/test_key_rotation_tracer.py
  </files>
  <behavior>
    - Test 1: Agent compiles after adding `RotateKey` instruction.
    - Test 2: Backend can dispatch `RotateKey` instruction.
    - Test 3: Agent receives `RotateKey` instruction and logs parameters.
    - Test 4: Agent returns `ActionCompleted` response after mock execution.
    - Test 5: End-to-end test verifies dispatch, agent log, and response.
  </behavior>
  <action>
    1.  **Agent Instruction:** In `agent-rust/src/instructions.rs`, add a new variant to the `AgentInstruction` enum: `RotateKey { key_id: String, vault_path: String }`.
    2.  **Agent Handler:** Create a new file `agent-rust/src/remediation/key_rotation.rs`. Implement a function `handle_rotate_key(key_id: String, vault_path: String) -> AgentResponse` that logs `INFO!` messages for the received `key_id` and `vault_path`, then returns `AgentResponse::ActionCompleted`.
    3.  **Agent Dispatch Integration:** In `agent-rust/src/remediation/mod.rs`, integrate `handle_rotate_key` into the main `handle_instruction` match arm for `AgentInstruction::RotateKey`.
    4.  **Backend Dispatch (Tracer):** For this tracer, add a temporary method in `backend/autonomous_remediation_service.py` (e.g., `_dispatch_rotate_key_for_test`) that constructs and dispatches a `RotateKey` instruction. This will be removed or integrated properly in subsequent plans.
    5.  **End-to-End Test:** Create `backend/tests/test_key_rotation_tracer.py`. This test should:
        *   Mock the agent communication layer.
        *   Call the temporary backend dispatch method.
        *   Assert that the mock agent received the correct `RotateKey` instruction.
        *   Simulate the agent's log output.
        *   Assert that the backend receives `AgentResponse::ActionCompleted`.
    Ensure `cargo check` passes in `agent-rust`.
  </action>
  <verify>
    <automated>
      cd agent-rust && cargo check && cd ..
      pytest backend/tests/test_key_rotation_tracer.py
    </automated>
  </verify>
  <done>
    The agent instruction set includes `RotateKey`, the agent can receive and log its parameters, and the backend can dispatch this instruction and verify the agent's mock success response in an end-to-end test.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary       | Description                         |
|----------------|-------------------------------------|
| Backend ↔ Vault | Secrets in transit, authentication. |
| Backend ↔ Agent | Instruction integrity, parameter tampering. |

## STRIDE Threat Register

| Threat ID | Category             | Component                      | Severity | Disposition | Mitigation Plan                                                                       |
|-----------|----------------------|--------------------------------|----------|-------------|---------------------------------------------------------------------------------------|
| T-64-01   | Tampering            | Vault connection / token       | high     | mitigate    | Use TLS for Vault connection; restrict Vault token permissions to least privilege.    |
| T-64-02   | Information Disclosure | Vault token in config          | high     | mitigate    | Use environment variables, never hardcode; ensure proper file permissions for config. |
| T-64-03   | Tampering            | Agent `RotateKey` parameters   | medium   | mitigate    | Validate `key_id` and `vault_path` on backend before dispatch; agent to log received parameters for audit. |
| T-64-SC   | Tampering            | npm/pip/cargo installs         | high     | mitigate    | slopcheck + blocking human checkpoint for `hvac` Python library.                    |
</threat_model>

<verification>
Overall: HashiCorp Vault is integrated, and the agent can receive and acknowledge key rotation instructions.
</verification>

<success_criteria>
- `backend/secret_manager_service.py` securely connects to HashiCorp Vault.
- `agent-rust/src/instructions.rs` contains the `RotateKey` instruction.
- The end-to-end tracer test `backend/tests/test_key_rotation_tracer.py` passes.
</success_criteria>

<output>
Create `.planning/milestones/v3.4-phases/64/64-01-SUMMARY.md` when done
</output>