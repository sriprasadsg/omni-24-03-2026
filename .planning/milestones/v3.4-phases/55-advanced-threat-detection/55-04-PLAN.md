---
phase: 55-advanced-threat-detection
plan: 04
type: execute
wave: 3
depends_on: ["55-01", "55-03"]
files_modified:
  - backend/soc_integration_service.py
  - backend/siem_engine.py
  - backend/ueba_service.py
  - backend/remediation_audit_service.py
  - backend/tests/test_soc_integration.py
autonomous: true
requirements: [COMM-01]
user_setup: []

must_haves:
  truths:
    - "Correlation case creation, UEBA anomaly persistence, and remediation stage transitions each push an OCSF-formatted payload to subscribed external SIEM/syslog webhooks, outbound-only (COMM-01, D-03)."
    - "Every outbound OCSF payload uses class_uid=2004, category_uid=2 (Detection Finding), matching ocsf_endpoints.py's existing convention across the codebase's two OCSF surfaces."
    - "Every push is dispatched fire-and-forget via asyncio.create_task — never awaited inline in the correlation/anomaly/remediation code path (Pitfall 3)."
    - "A webhook delivery failure (down/slow SIEM) does not raise or propagate into the calling correlation/anomaly/remediation pipeline (Pitfall 3)."
    - "Outbound delivery reuses webhook_service.trigger_webhook() unchanged — the existing _is_safe_webhook_url SSRF blocklist and per-webhook HMAC-SHA256 signing are not bypassed or re-implemented (ASVS V6/V13, Don't-Hand-Roll)."
  artifacts:
    - "backend/soc_integration_service.py — new module: push_ocsf_event(event_type, source_doc) OCSF builder + fire-and-forget dispatch wrapper"
    - "backend/siem_engine.py — fire-and-forget OCSF push added to _trigger_alert after security_cases insert"
    - "backend/ueba_service.py — fire-and-forget OCSF push added to _persist_alert after security_alerts insert"
    - "backend/remediation_audit_service.py — fire-and-forget OCSF push added to write_audit after remediation_audit insert"
    - "backend/tests/test_soc_integration.py — new test file (OCSF shape, fire-and-forget, delivery-failure-non-fatal)"
  key_links:
    - "push_ocsf_event -> asyncio.create_task(WebhookService().trigger_webhook(event_type, ocsf_payload)) mirrors notification_manager.send_notification's fire-and-forget wrap exactly."
    - "trigger_webhook's db.webhooks.find({status:Active, events:event_type}) subscription lookup is the unchanged delivery mechanism; operators subscribe via the existing POST /api/webhooks events[] array."
  prohibitions:
    - statement: "MUST NOT build inbound SOC alert ingestion this phase — outbound push only (D-03)."
      status: flagged
    - statement: "MUST NOT await trigger_webhook() inline in any correlation/anomaly/remediation hot path — wrap in asyncio.create_task (Pitfall 3)."
      status: flagged
    - statement: "MUST NOT re-implement webhook delivery, SSRF URL validation, or HMAC signing — reuse webhook_service.trigger_webhook() unchanged (Don't-Hand-Roll)."
      status: flagged
    - statement: "MUST NOT invent a second OCSF schema shape — reuse the class_uid=2004/category_uid=2 convention and the _to_epoch/severity_map helpers from ocsf_endpoints.py."
      status: flagged
  flagged_assumptions:
    - "COMM-01 was returned 'unclassified/unresolved' by the deterministic edge-probe; acceptance criteria are authored from RESEARCH Pattern 3 + Test Map, not probe-derived."
    - "RESEARCH Assumption A3: the new event_type strings (threat.correlation, ueba.anomaly, remediation.event) are illustrative free-form subscription strings, not an existing codebase convention — any consistent naming works; no code depends on a specific string."
---

<objective>
COMM-01 (SOC integration): push OCSF-formatted alerts/findings/remediation events to an
external SIEM/syslog target through the EXISTING webhook infrastructure, outbound-only,
fire-and-forget, at the three pipeline points D-03 identifies (D-03). No new external API,
no inbound surface, no new delivery/SSRF/HMAC code.

Purpose: Close the loop — the correlation (55-01) and containment (55-03) events this phase
produces become externally visible to a SOC/SIEM without ever blocking or endangering the
pipelines that emit them.

Output: soc_integration_service.py (OCSF builder + fire-and-forget wrapper) and three
one-line push call sites, plus a dedicated test file.

## Artifacts this phase produces (Plan 55-04)
- backend/soc_integration_service.py — new module with push_ocsf_event(event_type, source_doc)
- three new fire-and-forget push call sites in siem_engine._trigger_alert, ueba_service._persist_alert, remediation_audit_service.write_audit
- backend/tests/test_soc_integration.py — new test file
- new webhook event_type strings: "threat.correlation", "ueba.anomaly", "remediation.event"

## Flagged assumptions (surfaced, not dropped)
- See must_haves.flagged_assumptions — COMM-01 probe abstention, A3 event-type naming.
</objective>

<execution_context>
@/home/user/enterprise-omni-agent-ai-platform/.claude/gsd-core/workflows/execute-plan.md
@/home/user/enterprise-omni-agent-ai-platform/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-advanced-threat-detection/55-CONTEXT.md
@.planning/phases/55-advanced-threat-detection/55-RESEARCH.md
@.planning/phases/55-advanced-threat-detection/55-PATTERNS.md

@backend/ocsf_endpoints.py
@backend/webhook_service.py
@backend/notification_manager.py
@backend/remediation_audit_service.py
</context>

<tasks>

<task type="tracer">
  <name>Task 1: soc_integration_service + first end-to-end OCSF push (correlation case)</name>
  <files>backend/soc_integration_service.py, backend/siem_engine.py, backend/tests/test_soc_integration.py</files>
  <read_first>
    - backend/ocsf_endpoints.py (the OCSF shape to reuse verbatim: class_uid=2004/category_uid=2/type_uid=200401 dict lines 34-44, the _to_epoch helper lines 14-19, the severity_map dict line 30)
    - backend/notification_manager.py lines 11-21 (the asyncio.create_task(self.webhook_service.trigger_webhook(...)) fire-and-forget precedent to mirror)
    - backend/webhook_service.py lines 55-104 (trigger_webhook signature (event_type, payload) + _is_safe_webhook_url + HMAC signing — consumed UNCHANGED)
    - backend/siem_engine.py lines 92-131 (_trigger_alert — the security_cases insert + existing non-fatal try/except email-dispatch pattern to place the push beside)
  </read_first>
  <action>
    Create backend/soc_integration_service.py with push_ocsf_event(event_type: str, source_doc: dict).
    It builds an OCSF Detection Finding payload reusing the EXACT class_uid=2004, category_uid=2,
    type_uid=200401, severity_id (via a severity_map identical to ocsf_endpoints.py), finding
    {uid,title}, time (via a _to_epoch helper identical to ocsf_endpoints.py — import/reuse rather
    than re-derive), metadata {version:1.0.0, product:{name:"OmniAgent Platform"}} shape, mapping
    from the passed source_doc's id/title/severity/timestamp fields. It then dispatches
    asyncio.create_task(WebhookService().trigger_webhook(event_type, ocsf_payload)) — fire-and-forget,
    never awaited — and wraps construction/dispatch in a try/except that logs and swallows so a
    caller can never be affected. For the tracer, wire ONE end-to-end push: in
    siem_engine._trigger_alert, AFTER the db.security_cases.insert_one(case_doc), call
    push_ocsf_event("threat.correlation", case_doc) inside the existing non-fatal try/except style
    already used there. Do not await it.
    In test_soc_integration.py: (a) assert push_ocsf_event builds a payload with class_uid==2004 and
    category_uid==2 from a sample case doc; (b) patch WebhookService.trigger_webhook and assert it is
    scheduled via create_task (not awaited inline — the caller returns first); (c) assert
    _trigger_alert still completes/inserts the case when push is triggered.
  </action>
  <verify>
    <automated>backend/venv/bin/python -m pytest backend/tests/test_soc_integration.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - backend/soc_integration_service.py exists and defines `def push_ocsf_event(` (or async) building a dict with `"class_uid": 2004` and `"category_uid": 2`
    - push_ocsf_event dispatches via `asyncio.create_task(` and calls `trigger_webhook(` (no inline `await` of trigger_webhook in the builder)
    - siem_engine._trigger_alert calls `push_ocsf_event("threat.correlation"` after the security_cases insert
    - test asserts the OCSF class_uid/category_uid and that dispatch is non-blocking
    - `wc -l backend/soc_integration_service.py` reports < 500
  </acceptance_criteria>
  <done>A correlation case creation emits one OCSF (class_uid=2004) payload to subscribed webhooks fire-and-forget, reusing trigger_webhook unchanged — proven end-to-end by a green test.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the remaining two push points + delivery-failure-non-fatal proof (COMM-01)</name>
  <files>backend/ueba_service.py, backend/remediation_audit_service.py, backend/tests/test_soc_integration.py</files>
  <read_first>
    - backend/ueba_service.py lines 57-102 (_persist_alert — the security_alerts insert + its existing non-fatal streaming/blockchain try/except blocks to place the push beside)
    - backend/remediation_audit_service.py lines 14-22 (write_audit — the single funnel every remediation stage transition flows through; push after the insert_one)
    - backend/soc_integration_service.py (push_ocsf_event from Task 1)
  </read_first>
  <action>
    Add the remaining two fire-and-forget push call sites, each after its existing insert and inside
    a non-fatal try/except so delivery never affects the caller: (1) in ueba_service._persist_alert,
    after db.security_alerts.insert_one(alert), call
    asyncio.create_task-style push_ocsf_event("ueba.anomaly", alert); (2) in
    remediation_audit_service.write_audit, after db.remediation_audit.insert_one(doc), call
    push_ocsf_event("remediation.event", doc) (this single funnel covers ALL remediation stage
    transitions — selected/pending_approval/dispatched/verified/override). Do not await any of them.
    In test_soc_integration.py add: (a) a delivery-failure test — patch WebhookService.trigger_webhook
    (or httpx post) to raise, and assert neither _persist_alert nor write_audit propagates the
    exception (each still completes its insert); (b) assert both new call sites pass the correct
    event_type strings ("ueba.anomaly", "remediation.event").
  </action>
  <verify>
    <automated>backend/venv/bin/python -m pytest backend/tests/test_soc_integration.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - ueba_service._persist_alert calls `push_ocsf_event("ueba.anomaly"` after the security_alerts insert
    - remediation_audit_service.write_audit calls `push_ocsf_event("remediation.event"` after the remediation_audit insert
    - a test patches trigger_webhook (or httpx post) to raise and asserts _persist_alert and write_audit both still complete their insert without propagating the exception
    - `grep -c 'push_ocsf_event(' backend/ueba_service.py backend/remediation_audit_service.py backend/siem_engine.py` shows the three call sites total >= 3
  </acceptance_criteria>
  <done>All three D-03 pipeline points (correlation, anomaly, remediation) push OCSF events fire-and-forget; a failing SIEM endpoint provably never breaks the emitting pipeline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend push call site -> external SIEM/syslog HTTP endpoint | outbound only; URL is operator-configured and must pass the existing SSRF blocklist |
| pipeline hot path -> webhook delivery | delivery latency/failure must not propagate back into correlation/anomaly/remediation code |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-55-10 | Tampering / Elevation of Privilege | SSRF via an attacker-controlled SIEM webhook URL | high | mitigate | Reuse webhook_service.trigger_webhook unchanged — the existing _is_safe_webhook_url blocklist (private/loopback/link-local/metadata CIDRs) is not bypassed; no new URL handling added. |
| T-55-11 | Availability | slow/down SIEM endpoint stalling the emitting pipeline | high | mitigate | Every push is asyncio.create_task fire-and-forget and wrapped in a swallow-and-log try/except; delivery-failure test asserts no propagation. |
| T-55-12 | Information Disclosure | OCSF payload leaking cross-tenant data to a SIEM | medium | mitigate | trigger_webhook's subscription lookup is tenant-scoped via the existing webhooks collection; payloads are built from the same tenant-scoped source docs the pipeline already produced. |
| T-55-13 | Spoofing/Tampering (in transit) | forged SIEM payload | low | accept/mitigate | Existing per-webhook HMAC-SHA256 signing (X-Webhook-Signature) reused unchanged; receiver verifies. No new crypto. |
| T-55-SC | Tampering | npm/pip installs | low | accept | No new packages this phase (RESEARCH Package Legitimacy Audit: not applicable); nothing to slopcheck. |
</threat_model>

<verification>
- `backend/venv/bin/python -m pytest backend/tests/test_soc_integration.py -x -q` green
- `grep -v '^#' backend/soc_integration_service.py | grep -c 'await .*trigger_webhook('` returns 0 (never awaited inline)
- Per-wave full suite: `cd backend && venv/bin/python -m pytest -q` (no regression)
</verification>

<success_criteria>
Correlation, anomaly, and remediation events push OCSF (class_uid=2004) payloads to subscribed
external SIEM webhooks, outbound-only, fire-and-forget, reusing the existing SSRF-safe HMAC-signed
delivery — and a failing SIEM endpoint provably never breaks the pipeline that emitted the event.
</success_criteria>

<output>
Create `.planning/phases/55-advanced-threat-detection/55-04-SUMMARY.md` when done
</output>
