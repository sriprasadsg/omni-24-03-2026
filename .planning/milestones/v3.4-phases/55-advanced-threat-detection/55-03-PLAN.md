---
phase: 55-advanced-threat-detection
plan: 03
type: execute
wave: 2
depends_on: ["55-02"]
files_modified:
  - backend/ueba_service.py
  - backend/tests/test_ueba_remediation_trigger.py
autonomous: false
requirements: [AUT-03]
user_setup: []

must_haves:
  truths:
    - "When a UEBA event is anomalous and carries the shadow_ai_detected rule with a real agent_id, a RemediationFinding(finding_type='anomaly', details={'anomaly_rule':'shadow_ai_detected', ...}) is built and remediate() is invoked — the FIRST production caller of the Phase 53 engine (AUT-03, D-02)."
    - "The UEBA -> remediate() call is dispatched fire-and-forget (background_tasks.add_task / asyncio.create_task), never awaited inline in the request/response cycle (RESEARCH anti-pattern: _dispatch_and_verify can block up to ~120s)."
    - "ResponseOrchestrator.is_duplicate_task(...) is called BEFORE remediate() so a repeated anomaly does not trigger repeated destructive dispatch (Pitfall 2 / threat table)."
    - "The anomaly-triggered containment goes through the IDENTICAL default-on approval gate + dry-run + DB-lease + audit path as Phase 53 — no confidence/severity-based bypass (D-04)."
    - "RemediationQueueTab.tsx renders the new finding_type 'anomaly' as a plain interpolated string (item.findingType, line 73) with no code change — a new value displays correctly (UI-SPEC covered)."
    - "RemediationQueueTab.tsx renders the resulting status (including the no_playbook terminal outcome) as plain text (item.status, lines 76/99) with no enum-map miss (UI-SPEC covered)."
    - "RemediationQueueTab.tsx agent identifier falls back to 'unknown' (item.agentId || 'unknown', line 76) for anomaly items lacking an agent_id — existing fallback, no new gap (UI-SPEC covered)."
  artifacts:
    - "backend/ueba_service.py — new post-analysis containment call site building RemediationFinding + calling remediate() via background dispatch"
    - "backend/tests/test_ueba_remediation_trigger.py — new test file (dedup-before-dispatch, fire-and-forget, approval-gate-preserving, no-agent -> no dispatch)"
  key_links:
    - "ueba_service analyze path -> RemediationFinding(finding_type='anomaly') -> remediate() -> select_playbook() anomaly branch (55-02) -> kill_process -> _dispatch_and_verify approval gate (D-04)."
    - "is_duplicate_task(agent_id=finding.agent_id, action='remediate_anomaly', tenant_id=..., alert_type='anomaly') mirrors the existing scan_for_remediable_findings/run_cycle dedup call shape."
  prohibitions:
    - statement: "MUST NOT await remediate() inline in a UEBA request handler — dispatch fire-and-forget only (fire-and-forget pipeline safety)."
      status: flagged
    - statement: "MUST NOT bypass Phase 53's approval gate for anomaly-triggered containment regardless of anomaly confidence/severity (D-04)."
      status: flagged
    - statement: "MUST NOT modify or gate the pre-existing UEBA auto-ban block (ip_ban_service.ban_ip at score>=80, lines ~230-250) — it is out of this phase's scope and deliberately unchanged (Pitfall 4)."
      status: flagged
    - statement: "MUST NOT build a second/parallel dispatch engine — route exclusively through the existing autonomous_remediation_service.remediate() (D-02)."
      status: flagged
  flagged_assumptions:
    - "RESEARCH Assumption A1: ueba_service.py's per-event rule engine (not ueba_engine.py's batch ML score) is the containment trigger — the only per-event 'moment' that fits 'real-time predictive'. Confirmed at the checkpoint:decision below."
    - "RESEARCH Pitfall 1: remediate() has ZERO production callers today; this call site is the first. The pre-existing nscan/vuln/fim dead-call-site gap in run_cycle()/generate_plan() is NOT fixed by this phase (out of scope fences)."
    - "RESEARCH Pitfall 4: the pre-existing UEBA auto-ban already bypasses an approval gate for brute_force/known_malicious_ip; this is a known, deliberately-unaddressed, out-of-file-scope behavior, NOT a D-04 gap this phase introduces."
---

<objective>
AUT-03 (predictive containment — trigger half): wire the FIRST production call site
of the Phase 53 remediate() engine off the per-event UEBA rule results, so an anomalous
shadow_ai_detected event routes through the existing approval-gated containment machinery
(D-02/D-04). This is the phase's containment spine and its single highest-risk wiring.

Purpose: RESEARCH Pitfall 1 — remediate() is fully built and tested but has never been
called in production. This plan makes UEBA its first caller, event-driven and fire-and-forget,
deduped, and approval-gated. Because it introduces the first live path from an automated
signal to a destructive-on-a-live-agent action, the consequential scope decision (which
anomaly types get containment, Assumption A2) is surfaced to the user before the trigger is
wired.

Output: the UEBA containment call site + its dedicated test file.

## Artifacts this phase produces (Plan 55-03)
- new UEBA post-analysis containment call site (backend/ueba_service.py) — builds RemediationFinding, calls is_duplicate_task then remediate() via background dispatch
- backend/tests/test_ueba_remediation_trigger.py — new test file
- new finding value flowing to UI: finding_type "anomaly" and the no_playbook status (rendered unchanged by RemediationQueueTab.tsx per UI-SPEC — no frontend file in this plan)

## Flagged assumptions (surfaced, not dropped)
- See must_haves.flagged_assumptions — A1 trigger choice, Pitfall 1 first-caller, Pitfall 4 pre-existing auto-ban.
</objective>

<execution_context>
@/home/user/enterprise-omni-agent-ai-platform/.claude/gsd-core/workflows/execute-plan.md
@/home/user/enterprise-omni-agent-ai-platform/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-advanced-threat-detection/55-CONTEXT.md
@.planning/phases/55-advanced-threat-detection/55-RESEARCH.md
@.planning/phases/55-advanced-threat-detection/55-PATTERNS.md
@.planning/phases/55-advanced-threat-detection/55-UI-SPEC.md

@backend/ueba_service.py
@backend/autonomous_remediation_service.py
@backend/response_orchestrator.py
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <decision>Which UEBA anomaly types trigger automated (approval-gated) containment this phase?</decision>
  <context>
    RESEARCH Assumption A2 (the single most consequential judgment call in this phase): only
    ShadowAIEvent carries a real agent_id, so only shadow_ai_detected has a clean fit to an
    existing agent-dispatched playbook (kill_process). The other 9 UEBA rules (brute_force,
    impossible_travel, known_malicious_ip, mass_download, lateral_movement, dormant_account,
    off_hours_login, new_country, after_hours_data_access) are user/IP-scoped with NO agent_id and
    NO user->agent mapping anywhere in the codebase. This is also a reversibility gate: this call
    site is the first live path from an automated signal to a destructive-on-a-live-agent action
    (kill_process), so the scope must be confirmed before it is wired.
  </context>
  <options>
    <option id="option-a">
      <name>Recommended: only shadow_ai_detected dispatches; all other anomalies are recorded + correlated + SIEM-pushed but resolve to no_playbook</name>
      <pros>Matches the codebase's actual shape (RESEARCH Pattern 2); no fictitious user->agent resolution; every containment stays inside the existing tested approval-gated engine (D-02/D-04); no second dispatch engine.</pros>
      <cons>User/IP-scoped anomalies get no automated containment action this phase (they are still recorded, correlated, and pushed to SIEM).</cons>
    </option>
    <option id="option-b">
      <name>Broader containment coverage for user/IP-scoped anomalies (defer to a follow-up phase)</name>
      <pros>Wider automated response surface.</pros>
      <cons>Requires a new non-agent-dispatch action type / synthetic lease keying — risks becoming the 'second dispatch engine' the scope fences forbid; out of this phase's locked scope; needs its own research + discuss-phase.</cons>
    </option>
  </options>
  <resume-signal>Select: option-a (proceed as planned) or option-b (stop and re-scope as a follow-up phase)</resume-signal>
</task>

<task type="tracer">
  <name>Task 2: End-to-end UEBA anomaly -> remediate() containment — one path only (first production caller)</name>
  <files>backend/ueba_service.py, backend/tests/test_ueba_remediation_trigger.py</files>
  <read_first>
    - backend/ueba_service.py (the file being modified: report_shadow_ai lines ~313-324 and analyze_login_behavior lines ~421-440 for the background_tasks.add_task convention; ShadowAIEvent dataclass lines ~48-53 for agent_id/process/remote_host; the analyze_login result dict keys risk_score/triggered_rules/is_anomalous lines ~252-263; the pre-existing _AUTO_BAN_RULES block lines ~230-250 that MUST stay untouched)
    - backend/autonomous_remediation_service.py lines 42-53 (RemediationFinding dataclass), lines 548-599 (remediate() signature + no_playbook/pending_approval outcomes), lines 505-518 (the is_duplicate_task call shape to mirror)
    - backend/response_orchestrator.py lines 231-250 (is_duplicate_task signature: agent_id, action, dedup_window_minutes, tenant_id, alert_type)
    - backend/remediation_playbook_service.py (the 55-02 anomaly branch this path depends on)
  </read_first>
  <action>
    Per the checkpoint decision (Option A), add ONE new containment call site in ueba_service.py
    triggered where a shadow_ai_detected anomaly is produced (the report_shadow_ai path and/or the
    analyze_event shadow-AI branch that appends "shadow_ai_detected" to triggered_rules). When the
    result is anomalous AND "shadow_ai_detected" is in triggered_rules AND a real agent_id is
    present: (1) resolve tenant_id for the event; (2) call ResponseOrchestrator().is_duplicate_task(
    agent_id=<event agent_id>, action="remediate_anomaly", dedup_window_minutes=5,
    tenant_id=<tenant>, alert_type="anomaly") and skip if duplicate; (3) build
    RemediationFinding(finding_id=<stable id>, finding_type="anomaly", severity=<mapped from
    risk_score>, tenant_id=<tenant>, agent_id=<event agent_id>, resource_id=<event.process or
    event.remote_host>, details={"anomaly_rule":"shadow_ai_detected", ...}); (4) dispatch
    remediate(finding) fire-and-forget via background_tasks.add_task (in a request handler) or
    asyncio.create_task (outside one) — NEVER await it inline. Do not touch the pre-existing
    _AUTO_BAN_RULES block. Wire ONLY this single shadow_ai path end-to-end for the tracer.
    In test_ueba_remediation_trigger.py: assert (a) an anomalous shadow_ai_detected event with an
    agent_id calls remediate() exactly once (patch autonomous_remediation_service.remediate); (b)
    is_duplicate_task is called before remediate and a duplicate short-circuits remediate; (c) an
    anomaly with no agent_id / a non-shadow-AI rule does NOT call remediate; (d) remediate is
    dispatched via the background/create_task path, not awaited inline (assert the handler returns
    before remediate resolves). Clone the fixture style from test_remediation_guards.py.
  </action>
  <verify>
    <automated>backend/venv/bin/python -m pytest backend/tests/test_ueba_remediation_trigger.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - ueba_service.py builds `RemediationFinding(` with `finding_type="anomaly"` and `details=` containing `"anomaly_rule"`
    - `is_duplicate_task(` is called before the `remediate(` dispatch in ueba_service.py
    - the remediate dispatch uses `background_tasks.add_task(` or `asyncio.create_task(` (no inline `await ...remediate(`)
    - test proves: shadow_ai+agent -> remediate called once; duplicate -> skipped; no-agent/other-rule -> not called; dispatch is non-blocking
    - `grep -n "_AUTO_BAN_RULES" backend/ueba_service.py` shows the pre-existing block still present and unmodified (line count of that block unchanged)
  </acceptance_criteria>
  <precondition>Plan 55-02's select_playbook() anomaly branch is merged (this path depends on it to reach kill_process rather than no_playbook).</precondition>
  <reversibility rating="costly">First live automated path to a destructive-on-a-live-agent action (kill_process); the code is revertible, but the capability is destructive, so the scope is gated by the preceding checkpoint:decision and every dispatch stays behind Phase 53's human approval gate (D-04).</reversibility>
  <done>An anomalous shadow_ai_detected event with an agent_id triggers exactly one deduped, fire-and-forget, approval-gated remediate() call; non-dispatchable anomalies do not; the pre-existing auto-ban is untouched — all proven by tests.</done>
</task>

<task type="auto">
  <name>Task 3: Approval-gate regression + no-bypass assertion (AUT-03, D-04)</name>
  <files>backend/tests/test_ueba_remediation_trigger.py</files>
  <read_first>
    - backend/tests/test_remediation_guards.py (existing approval-gate/destructive-step test patterns to mirror — re-run as the integration regression per VALIDATION Test Map)
    - backend/autonomous_remediation_service.py lines 579-599 (is_destructive -> pending_approval branch — the gate that must NOT be bypassed)
  </read_first>
  <action>
    Add a no-bypass test to test_ueba_remediation_trigger.py proving the anomaly path does not
    introduce a severity/confidence shortcut around the approval gate: with the real
    select_playbook anomaly branch + a destructive kill_process playbook, assert an anomaly-triggered
    remediate() lands in pending_approval (not auto-dispatched) exactly like any other destructive
    finding — even at max risk_score. Confirm no code path keys dispatch on the finding's
    confidence/severity. Reference D-04 in the test docstring. Keep test_remediation_guards.py as the
    unchanged integration regression (re-run, do not modify).
  </action>
  <verify>
    <automated>backend/venv/bin/python -m pytest backend/tests/test_ueba_remediation_trigger.py backend/tests/test_remediation_guards.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - a test asserts an anomaly-triggered destructive remediation returns status `pending_approval` (not `dispatching`/`queued`) even at max risk_score
    - the test docstring references D-04 (no confidence/severity bypass)
    - `pytest backend/tests/test_remediation_guards.py -x` still exits 0 (no regression to the existing gate)
  </acceptance_criteria>
  <done>The anomaly containment path is proven to honor the identical approval gate as Phase 53 with no confidence-based bypass; the existing guard suite still passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UEBA event -> RemediationFinding -> remediate() | an automated signal crosses into a destructive-action pipeline; must be deduped, tenant-validated, and approval-gated |
| anomaly finding -> _dispatch_and_verify approval gate | the human gate that must never be bypassed on urgency/confidence grounds |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-55-06 | Elevation of Privilege | anomaly-triggered containment bypassing the approval gate via a high confidence score | high | mitigate | is_destructive is computed from the PLAYBOOK's steps, not the finding's confidence/severity; the new branch adds no shortcut (D-04). Task 3 asserts pending_approval even at max risk_score. |
| T-55-07 | Denial of Service (self-inflicted) | replayed/duplicate anomaly causing repeated destructive dispatch | high | mitigate | is_duplicate_task(...) called before remediate() with the per-agent+incident key; duplicate short-circuits. |
| T-55-08 | Availability | inline-awaited remediate() (up to ~120s) stalling the UEBA request path for all tenants | medium | mitigate | Dispatch fire-and-forget via background_tasks.add_task / asyncio.create_task; test asserts non-blocking return. |
| T-55-09 | Input Validation | RemediationFinding built with a missing tenant_id/agent_id | medium | mitigate | Build the finding only when a real agent_id + resolved tenant_id are present; otherwise no dispatch (fail-closed). |
</threat_model>

<verification>
- `backend/venv/bin/python -m pytest backend/tests/test_ueba_remediation_trigger.py backend/tests/test_remediation_guards.py -x -q` green
- `grep -v '^#' backend/ueba_service.py | grep -c 'await .*remediate('` returns 0 (no inline-awaited remediate in a hot path)
- Per-wave full suite: `cd backend && venv/bin/python -m pytest -q` (no regression)
</verification>

<success_criteria>
UEBA becomes remediate()'s first production caller: a shadow_ai_detected anomaly with an agent_id
triggers exactly one deduped, fire-and-forget, approval-gated containment; non-dispatchable
anomalies resolve to no_playbook; the approval gate is provably un-bypassable; the pre-existing
auto-ban is untouched.
</success_criteria>

<output>
Create `.planning/phases/55-advanced-threat-detection/55-03-SUMMARY.md` when done
</output>
