---
phase: 55-advanced-threat-detection
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/remediation_playbook_service.py
  - backend/tests/test_remediation_playbook.py
autonomous: true
requirements: [AUT-03]
user_setup: []

must_haves:
  truths:
    - "select_playbook() gains an anomaly branch: finding_type=='anomaly' with details.anomaly_rule=='shadow_ai_detected' AND a real agent_id returns the existing kill_process playbook; any other anomaly (no agent_id / other rule) returns None -> an honest no_playbook outcome, still recorded (AUT-03, D-02)."
    - "The anomaly branch is a pure deterministic if/elif lookup with no LLM anywhere — it stays inside the existing dispatcher's shape and reuses the _finding_attr accessor (D-02 deterministic-only, inherits Phase 53 D-02)."
    - "No new ACTION_MAP entry is introduced — the anomaly branch maps only onto the already-vendored kill_process action (no new agent-dispatch command surface)."
  artifacts:
    - "backend/remediation_playbook_service.py — new `if finding_type == 'anomaly':` branch in select_playbook()"
    - "backend/tests/test_remediation_playbook.py — new -k anomaly cases (shadow_ai+agent -> kill_process; no-agent -> None; other rule -> None)"
  key_links:
    - "select_playbook() is called by autonomous_remediation_service.remediate() (line ~562); returning the existing kill_process playbook routes the anomaly through the UNCHANGED _dispatch_and_verify approval/lease/audit machinery (D-04)."
    - "returning None yields remediate()'s existing {'status':'no_playbook'} terminal state — a normal, already-handled outcome, not an error."
  prohibitions:
    - statement: "MUST NOT invent a new ACTION_MAP entry (e.g. a synchronous ip_ban-backed action) for user/IP-scoped anomalies this phase — that risks becoming the 'second dispatch engine' the scope fence forbids (RESEARCH Pattern 2 step 3)."
      status: flagged
    - statement: "MUST NOT put an LLM in the selection path — select_playbook stays a pure deterministic lookup (D-02, module docstring)."
      status: flagged
    - statement: "MUST NOT silently expand anomaly containment coverage beyond shadow_ai_detected; broader user/IP-scoped containment is a follow-up phase requiring user confirmation, not a silent addition (RESEARCH Assumption A2)."
      status: flagged
  flagged_assumptions:
    - "AUT-03 was returned 'unclassified/unresolved' by the deterministic edge-probe; acceptance criteria are authored from RESEARCH Pattern 2 + Test Map, not probe-derived."
    - "RESEARCH Assumption A2 (the single most consequential judgment call): only shadow_ai_detected anomalies reach a dispatchable playbook this phase; the other 9 UEBA rule types resolve to no_playbook. This scope choice is confirmed at the checkpoint:decision in Plan 55-03 before the trigger is wired."
    - "RESEARCH Open Question 1: kill_process.yaml is reused verbatim for the anomaly path rather than adding a near-duplicate shadow_ai_kill.yaml — a low-risk audit-trail-clarity tradeoff, not architectural."
---

<objective>
AUT-03 (predictive containment — mapping half): teach the deterministic playbook
dispatcher which playbook an anomaly finding maps to, WITHOUT adding any new action
type or a second dispatch mechanism (D-02). This is the pure-function foundation the
Plan 55-03 UEBA trigger builds on.

Purpose: The concrete resolution of CONTEXT.md's flagged open question (anomaly ->
playbook). Grounded in RESEARCH Pattern 2: only shadow_ai_detected (the one UEBA
signal carrying a real agent_id) has a clean existing-playbook fit (kill_process);
everything else honestly returns no_playbook.

Output: a new anomaly branch in select_playbook() + its test cases.

## Artifacts this phase produces (Plan 55-02)
- new `finding_type == "anomaly"` branch in select_playbook() (backend/remediation_playbook_service.py)
- new anomaly test cases in backend/tests/test_remediation_playbook.py
- new finding_type VALUE "anomaly" (consumed by remediate() unchanged; the RemediationFinding dataclass itself is unchanged)

## Flagged assumptions (surfaced, not dropped)
- See must_haves.flagged_assumptions — AUT-03 probe abstention, Assumption A2 scope, Open-Question-1 playbook reuse.
</objective>

<execution_context>
@/home/user/enterprise-omni-agent-ai-platform/.claude/gsd-core/workflows/execute-plan.md
@/home/user/enterprise-omni-agent-ai-platform/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-advanced-threat-detection/55-CONTEXT.md
@.planning/phases/55-advanced-threat-detection/55-RESEARCH.md
@.planning/phases/55-advanced-threat-detection/55-PATTERNS.md

@backend/remediation_playbook_service.py
@backend/tests/test_remediation_playbook.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the anomaly branch to select_playbook() (AUT-03)</name>
  <files>backend/remediation_playbook_service.py, backend/tests/test_remediation_playbook.py</files>
  <read_first>
    - backend/remediation_playbook_service.py (the function being extended: existing fim/nscan/vuln branches lines ~108-123, the _finding_attr(finding, name, default) accessor lines ~85-88, the ACTION_MAP allowlist lines ~25-33 — confirm kill_process is present and no new entry is needed)
    - backend/tests/test_remediation_playbook.py (existing test/fixture style to extend)
    - backend/autonomous_remediation_service.py lines 42-53 (RemediationFinding dataclass — confirm agent_id/details fields the branch reads)
  </read_first>
  <behavior>
    - Test: select_playbook(finding=anomaly, details={"anomaly_rule":"shadow_ai_detected"}, agent_id="agent-1") returns the playbook named "kill_process".
    - Test: select_playbook(finding=anomaly, details={"anomaly_rule":"shadow_ai_detected"}, agent_id=None) returns None (no agent to dispatch to).
    - Test: select_playbook(finding=anomaly, details={"anomaly_rule":"brute_force"}, agent_id=None) returns None.
    - Test: an unknown finding_type still returns None (no regression to existing fim/nscan/vuln branches).
  </behavior>
  <action>
    Add a new branch to select_playbook(), placed alongside the existing fim/nscan/vuln
    branches and BEFORE the final `return None`: when finding_type == "anomaly", if
    _finding_attr(finding, "details", {}) has anomaly_rule == "shadow_ai_detected" AND
    _finding_attr(finding, "agent_id") is truthy, return by_name.get("kill_process"); otherwise
    return None. Reuse the existing _finding_attr accessor — do not reimplement dict-or-dataclass
    access. Do NOT add anything to ACTION_MAP and do NOT add a new YAML playbook (reuse the
    already-vendored kill_process — RESEARCH Open Question 1). Extend test_remediation_playbook.py
    with the four -k anomaly cases in <behavior>, cloning the existing _finding(...) fixture
    (finding_type override + details override).
  </action>
  <verify>
    <automated>backend/venv/bin/python -m pytest backend/tests/test_remediation_playbook.py -k "anomaly or select" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - remediation_playbook_service.py contains `if finding_type == "anomaly":` and references `by_name.get("kill_process")`
    - the branch calls `_finding_attr(` for both details and agent_id (accessor reused, not reimplemented)
    - `grep -c '"anomaly"' backend/remediation_playbook_service.py` >= 1 and ACTION_MAP is unchanged (still exactly 7 entries; `grep -c '":' `-style count of the ACTION_MAP block unchanged — verify no new key added)
    - `pytest backend/tests/test_remediation_playbook.py -k anomaly` exits 0 with the 4 cases (shadow_ai+agent -> kill_process, shadow_ai+no-agent -> None, other-rule -> None, unknown-type -> None)
  </acceptance_criteria>
  <done>select_playbook() deterministically maps shadow_ai_detected+agent_id to kill_process and every other anomaly to no_playbook, proven by tests, with zero new action types.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anomaly finding -> select_playbook() | finding attributes (anomaly_rule, agent_id) decide whether a destructive playbook is selected |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-55-04 | Elevation of Privilege | select_playbook anomaly branch selecting a destructive action without an agent | high | mitigate | Branch requires a truthy agent_id before returning kill_process; no agent -> None -> no dispatch. Approval gate (destructive step) still applies downstream via remediate()/_dispatch_and_verify (D-04) — the branch never bypasses it. |
| T-55-05 | Tampering | crafted anomaly_rule string forcing an unintended playbook | low | mitigate | Only the exact literal shadow_ai_detected maps to a playbook; all other values fall through to None. |
</threat_model>

<verification>
- `backend/venv/bin/python -m pytest backend/tests/test_remediation_playbook.py -k "anomaly or select" -x -q` green
- Per-wave full suite: `cd backend && venv/bin/python -m pytest -q` (no regression to existing playbook tests)
</verification>

<success_criteria>
The deterministic dispatcher recognizes the new anomaly finding_type, maps only the
agent-scoped shadow_ai_detected case to the existing kill_process playbook, and honestly
returns no_playbook for everything else — no new action surface, no LLM, tests green.
</success_criteria>

<output>
Create `.planning/phases/55-advanced-threat-detection/55-02-SUMMARY.md` when done
</output>
