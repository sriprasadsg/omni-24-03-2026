---
phase: 52-file-integrity-monitoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/agent_security_endpoints.py
  - backend/tests/test_fim_events_rich.py
autonomous: true
requirements: [FIM-02]
must_haves:
  truths:
    - "POST /fim-events accepts and persists rich change events: change_type, hash_before, hash_after, process, user, ts, per path"
    - "The existing VirusTotal hash enrichment + malware-alert-on-malicious behavior is preserved"
    - "The existing GET /fim-events list contract is unchanged (additive fields only)"
    - "Ingestion is tenant-scoped and does not access db._db"
  artifacts:
    - path: "backend/agent_security_endpoints.py"
      provides: "extended fim-events ingestion (change_type/before-after/process/user)"
      contains: "change_type"
    - path: "backend/tests/test_fim_events_rich.py"
      provides: "tests: rich fields persisted, VT path preserved, list contract intact"
      contains: "change_type"
      min_lines: 45
  key_links:
    - from: "backend/agent_security_endpoints.py"
      to: "backend/tests/test_fim_events_rich.py"
      via: "ingest_fim_event persists the rich event fields"
      pattern: "change_type"
  prohibitions:
    - "MUST NOT remove or break the existing VirusTotal enrichment / malware-alert path"
    - "MUST NOT change the existing GET /fim-events response shape (additive only)"
    - "MUST NOT access db._db — use the wrapped db"
---

<objective>
Extend the existing `POST /fim-events` ingestion to accept and persist the rich FIM event shape (change_type, before/after hash, process, user) the agent will send, while preserving the current VirusTotal enrichment and list path.

Purpose: FIM-02 — events carry before/after hash + process + user. D-04, additive.
Output: extended ingestion + tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@.planning/phases/52-file-integrity-monitoring/52-RESEARCH.md
@.planning/phases/52-file-integrity-monitoring/52-CONTEXT.md

# Read-first anchors:
@backend/agent_security_endpoints.py   # ingest_fim_event (~line 35) + _raise_malware_alert + VT enrichment — extend, don't break
@backend/tests/test_agent_fleet_geo.py # hermetic harness
</execution_context>

<artifacts>
- EDIT `backend/agent_security_endpoints.py` — accept/persist rich fields in `ingest_fim_event`.
- NEW `backend/tests/test_fim_events_rich.py` — rich-field persistence + VT-preserved + list-intact tests.
</artifacts>

<tasks>

<task type="tdd">
  <name>Task 1: failing tests for rich fim-events ingestion</name>
  <files>backend/tests/test_fim_events_rich.py</files>
  <action>Tests: (a) POST `{changes: [{path, change_type: "modify", hash_before, hash_after, process: {name, pid}, user, ts}]}` → the persisted `fim_events` doc carries all these fields + tenantId + agentId; (b) VT enrichment still runs on `hash_after` and a malicious verdict still raises the alert (mock `enrich_file_hashes`); (c) a legacy-shape event `{changes: [{path, new_hash}]}` still ingests (backward-compatible); (d) tenant scoping. Mock get_database + virustotal_client per the existing test patterns.</action>
  <verify>
    <automated>backend/venv/bin/python -m pytest backend/tests/test_fim_events_rich.py -q 2>&1 | tail -10</automated>
  </verify>
  <done>Tests exist and FAIL (rich fields not persisted yet).</done>
</task>

<task type="auto">
  <name>Task 2: extend ingest_fim_event with the rich fields</name>
  <files>backend/agent_security_endpoints.py</files>
  <read_first>ingest_fim_event current body + the doc it inserts + the VT enrichment block.</read_first>
  <action>In `ingest_fim_event`, read the new optional per-change fields (`change_type`, `hash_before`, `hash_after`, `process`, `user`, `ts`) alongside the existing `new_hash`/`hash`, and persist them on the `fim_events` doc (default missing ones to None; treat `hash_after` as the primary hash for VT enrichment, falling back to `new_hash`/`hash` for backward compat). Keep the VT enrichment + `_raise_malware_alert` path and the tenant scoping exactly as-is. Do not touch `GET /fim-events`.</action>
  <verify>
    <automated>backend/venv/bin/python -m pytest backend/tests/test_fim_events_rich.py backend/tests/ -k "fim" -q 2>&1 | tail -8</automated>
  </verify>
  <done>Rich fields persisted; VT + alert + legacy shape + list all still work.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-52-01 | Info Disclosure | cross-tenant fim event leak | mitigate | Existing tenant scoping preserved; wrapped db |
| T-52-02 | Tampering | forged fim events | accept | Agent-authenticated route; audit-only ingestion |
| T-52-SC | Tampering | package installs | accept | No new pip dep |
</threat_model>

<verification>
- `pytest test_fim_events_rich.py` + `-k fim` green (rich fields, VT preserved, legacy shape, list intact, tenant).
- `GET /fim-events` response shape unchanged.
</verification>

<success_criteria>
`POST /fim-events` persists the rich FIM event shape (change_type/before-after/process/user) tenant-scoped, with the existing VirusTotal enrichment, malware alert, backward-compatible legacy events, and list contract all intact.
</success_criteria>

<output>
Create `.planning/phases/52-file-integrity-monitoring/52-01-SUMMARY.md` when done.
</output>
