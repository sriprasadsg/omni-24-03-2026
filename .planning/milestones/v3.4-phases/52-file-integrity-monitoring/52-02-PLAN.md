---
phase: 52-file-integrity-monitoring
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - agent-install/omni-agent-rs/Cargo.toml
  - agent-install/omni-agent-rs/src/capabilities/fim.rs
autonomous: true
requirements: [FIM-01, FIM-02]
must_haves:
  truths:
    - "The agent watches configured paths via the notify crate (event-driven), emitting create/modify/delete/rename change events (not poll-and-hash)"
    - "Each event carries path, change_type, hash_before, hash_after, best-effort process + user, and a timestamp"
    - "Change events are written to a local sqlite fim_queue table (rowid-ordered) for the remediation engine to drain"
    - "The watcher failing to start degrades gracefully (logs + continues); the agent never crashes; builds linux + windows-gnu"
  artifacts:
    - path: "agent-install/omni-agent-rs/Cargo.toml"
      provides: "notify crate dependency"
      contains: "notify"
    - path: "agent-install/omni-agent-rs/src/capabilities/fim.rs"
      provides: "notify watcher + event assembly + local sqlite fim_queue writer; collect() reports status"
      contains: "notify"
  key_links:
    - from: "agent-install/omni-agent-rs/src/capabilities/fim.rs"
      to: "fim_queue (local sqlite)"
      via: "enqueue change events for Phase 53 drain + 52-04 backend post"
      pattern: "fim_queue"
  prohibitions:
    - "MUST NOT poll-and-hash whole trees for change detection (event-driven via notify)"
    - "MUST NOT panic/crash if the watcher can't start — degrade gracefully"
    - "MUST NOT introduce a C dependency (notify is pure Rust); windows-gnu must still check"
---

<objective>
Replace the poll-and-hash FIM stub with an event-driven `notify` watcher that emits rich change events into a local sqlite `fim_queue`. The watcher lifecycle wiring into `agent_loop` + the backend drain land in 52-04; this plan builds the watcher + queue + event assembly.

Purpose: FIM-01/02 core. D-01/D-02.
Output: `notify` dep + rewritten `fim.rs` (watcher, event assembly, queue writer).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@.planning/phases/52-file-integrity-monitoring/52-RESEARCH.md
@.planning/phases/52-file-integrity-monitoring/52-CONTEXT.md

# Read-first anchors:
@agent-install/omni-agent-rs/src/capabilities/fim.rs   # current poll-and-hash to replace
@agent-install/omni-agent-rs/src/capabilities/feed_bundle.rs  # rusqlite open/write pattern to mirror for the queue
@agent-install/omni-agent-rs/Cargo.toml                # add notify
</execution_context>

<artifacts>
- EDIT `agent-install/omni-agent-rs/Cargo.toml` — add `notify`.
- EDIT `agent-install/omni-agent-rs/src/capabilities/fim.rs` — watcher + event assembly + fim_queue writer; `collect()` → status.
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: notify dep + windows cross-check</name>
  <files>agent-install/omni-agent-rs/Cargo.toml</files>
  <action>Add `notify = "<current 6.x/8.x>"` to `[dependencies]`. Run `cargo build` (linux) and `cargo check --target x86_64-pc-windows-gnu`. Confirm both pass (notify is pure Rust; pare features if any pull a C dep).</action>
  <verify>
    <automated>cd agent-install/omni-agent-rs && cargo build 2>&1 | tail -5 && cargo check --target x86_64-pc-windows-gnu 2>&1 | tail -6</automated>
  </verify>
  <done>notify builds on linux + windows-gnu.</done>
</task>

<task type="auto">
  <name>Task 2: event-driven watcher + fim_queue writer + event assembly</name>
  <files>agent-install/omni-agent-rs/src/capabilities/fim.rs</files>
  <read_first>feed_bundle.rs for the rusqlite open/write pattern; the current fim.rs hash_file helper (reuse for hashing).</read_first>
  <action>Rewrite fim.rs: `pub fn start_watcher(paths: Vec<String>, stop: <shutdown handle>)` creates a `notify::RecommendedWatcher` (recursive) over the given paths and, on each event, maps `EventKind`→`change_type` (create/modify/delete/rename; permission→modify best-effort), computes `hash_after` (reuse `hash_file`; deletes → None), resolves `hash_before` from a last-known map/baseline (None if unknown), gathers best-effort `process` (OS context; `unknown` where unavailable) + `user` (current process user), assembles `{path, change_type, hash_before, hash_after, process, user, ts}`, and calls `enqueue(event)`. `fn enqueue(ev)` opens/creates the local sqlite `fim_queue(rowid INTEGER PRIMARY KEY AUTOINCREMENT, path, change_type, hash_before, hash_after, process_json, user, ts, posted INTEGER DEFAULT 0)` and inserts the row. `Capability::collect()` now returns a status summary (`{watching: true/false, watched_paths: n, queued: n, last_event_ts}`) instead of poll-hashing. Any watcher/enqueue failure logs + returns (no panic). Keep `start_watcher` callable but do NOT auto-start it here (52-04 wires it into agent_loop).</action>
  <verify>
    <automated>cd agent-install/omni-agent-rs && cargo build 2>&1 | tail -6</automated>
  </verify>
  <done>Watcher + queue writer + event assembly compile; collect() reports status; no panics on failure.</done>
</task>

<task type="auto">
  <name>Task 3: unit tests — event mapping + enqueue</name>
  <files>agent-install/omni-agent-rs/src/capabilities/fim.rs</files>
  <action>Add `#[cfg(test)] mod tests`: `EventKind` → `change_type` mapping cases; `enqueue` into an in-memory sqlite inserts a rowid-ordered row with the right columns; a delete event yields `hash_after: None`; a modify yields a computed `hash_after`. Keep hermetic (temp files / in-memory db; do not assert on real inotify timing).</action>
  <verify>
    <automated>cd agent-install/omni-agent-rs && cargo test fim 2>&1 | tail -12</automated>
  </verify>
  <done>Event-mapping + enqueue unit tests pass.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-52-03 | DoS | recursive watch on huge trees | mitigate | Bounded watched paths (52-04 config); cap depth/count |
| T-52-04 | DoS | watcher panic takes down agent | mitigate | All failures log + return; no panic; heartbeat loop independent |
| T-52-SC | Tampering | notify crate | accept | Reputable pure-Rust crate; pinned; windows-gnu checked |
</threat_model>

<verification>
- `cargo build` (linux) + `cargo check --target x86_64-pc-windows-gnu` clean; `cargo test fim` green.
- Watcher is event-driven (notify), events assembled with the required fields, written to fim_queue; graceful on failure.
</verification>

<success_criteria>
An event-driven `notify` FIM watcher emits rich create/modify/delete/rename change events (path/change_type/before-after hash/process/user/ts) into a local sqlite `fim_queue`, degrading gracefully, building on linux + windows-gnu.
</success_criteria>

<output>
Create `.planning/phases/52-file-integrity-monitoring/52-02-SUMMARY.md` when done.
</output>
