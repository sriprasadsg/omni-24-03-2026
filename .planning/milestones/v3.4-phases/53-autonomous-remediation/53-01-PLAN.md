---
phase: 53-autonomous-remediation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/remediation_playbook_service.py
  - backend/remediation_playbook_endpoints.py
  - backend/playbooks/
  - backend/router_registry.py
  - backend/tests/test_remediation_playbook.py
autonomous: true
requirements: [AUTO-02]
must_haves:
  truths:
    - "Deterministic YAML playbooks {name, finding_class, steps:[{action, params, destructive}], rollback:[...]} are loaded and validated"
    - "select_playbook(finding) maps a finding's class to the correct playbook"
    - "Vendored default playbooks exist for patch_package, kill_process, restore_file, block_ip, disable_service (rotate_key deferred)"
    - "Playbooks are materialized into a dedicated remediation_playbooks collection with its own CRUD (NOT the LLM enhanced_playbook store)"
    - "A fixed ACTION_MAP resolves each action to an agent command with NO LLM in the path"
  artifacts:
    - path: "backend/remediation_playbook_service.py"
      provides: "YAML playbook load/validate + finding_class->playbook selection + deterministic ACTION_MAP"
      contains: "select_playbook"
      min_lines: 60
    - path: "backend/tests/test_remediation_playbook.py"
      provides: "tests: load/validate, selection by finding_class, destructive flag, action map, invalid playbook rejected"
      contains: "select_playbook"
      min_lines: 45
  key_links:
    - from: "backend/remediation_playbook_service.py"
      to: "backend/playbooks/"
      via: "loads vendored default YAML playbooks"
      pattern: "playbooks"
  prohibitions:
    - "MUST NOT call an LLM to select or expand a playbook at run time (deterministic only)"
    - "MUST NOT accept a playbook with an unknown action (validate against ACTION_MAP)"
---

<objective>
Build the deterministic YAML playbook layer: load/validate playbooks, select one per finding class, and map actions to agent commands — the substrate the engine (53-03) executes. Operator-extensible via the existing playbook store.

Purpose: AUTO-02. D-02 — YAML-defined, deterministic, no LLM in the execution path.
Output: `remediation_playbook_service.py` + vendored default playbooks + tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@.planning/phases/53-autonomous-remediation/53-RESEARCH.md
@.planning/phases/53-autonomous-remediation/53-CONTEXT.md

# Read-first anchors:
@backend/autonomous_remediation_service.py   # RemediationFinding fields (finding_type/severity/agent_id) selection keys off
@backend/enhanced_playbook_endpoints.py      # existing playbook store/CRUD to reuse for operator edits
@backend/ai_playbook_service.py              # existing playbook step shape (align, but deterministic)
</execution_context>

<artifacts>
- NEW `backend/remediation_playbook_service.py` — load/validate/select + ACTION_MAP.
- NEW `backend/playbooks/*.yaml` — vendored default playbooks (6 actions).
- NEW `backend/tests/test_remediation_playbook.py` — load/select/validate tests.
</artifacts>

<tasks>

<task type="tdd">
  <name>Task 1: failing tests for playbook load/select/validate</name>
  <files>backend/tests/test_remediation_playbook.py</files>
  <action>Tests: (a) `load_playbooks()` reads the vendored YAML dir → ≥6 playbooks each with name/finding_class/steps; (b) `select_playbook(finding)` returns the patch_package playbook for a vuln finding, kill_process for a malicious-process nscan finding, restore_file for a FIM drift finding (map finding_class→playbook); (c) a step with a `destructive: true` flag is preserved; (d) `ACTION_MAP` resolves every action used by the defaults to an agent command string; (e) a playbook with an unknown action is rejected by `validate()`.</action>
  <verify>
    <automated>backend/venv/bin/python -m pytest backend/tests/test_remediation_playbook.py -q 2>&1 | tail -10</automated>
  </verify>
  <done>Tests exist and FAIL (service/playbooks absent).</done>
</task>

<task type="auto">
  <name>Task 2: playbook service + vendored default playbooks</name>
  <files>backend/remediation_playbook_service.py, backend/playbooks/</files>
  <action>Create `backend/playbooks/` with 5 YAML files (patch_package, kill_process, restore_file, block_ip, disable_service), each `{name, finding_class, match, steps:[{action, params, destructive}], rollback:[{action, params}]}` — destructive steps flagged; each with a sane rollback where reversible (block_ip → unblock_ip; disable_service → enable_service). Irreversible actions (kill_process, patch_package) carry NO rollback (their failure escalates to a human alert in 53-04, not an undo). Create `remediation_playbook_service.py`: `ACTION_MAP` (action→agent command: patch_package→"upgrade_software", kill_process→"kill_process", restore_file→"restore_file", block_ip→"block_ip", disable_service→"disable_service" — rotate_key deferred, not in the map); `load_playbooks()` (parse + `validate()` each, reject unknown actions); `select_playbook(finding)` (finding_class match, deterministic tie-break). On first load, seed the vendored defaults into a dedicated **`remediation_playbooks` collection**; `load_playbooks()` reads defaults + operator-authored rows from that collection. Then create `remediation_playbook_endpoints.py` — tenant-scoped CRUD (GET list / GET one / POST / PUT / DELETE) over `remediation_playbooks`, each POST/PUT `validate()`-gated — and register it via `_load(app, "remediation_playbook_endpoints", "router")`. This is the store Phase 54's Playbooks tab targets (NOT the LLM `enhanced_playbook_endpoints`). No LLM anywhere.</action>
  <verify>
    <automated>backend/venv/bin/python -m pytest backend/tests/test_remediation_playbook.py -q 2>&1 | tail -8</automated>
  </verify>
  <done>Playbooks load/validate/select; ACTION_MAP complete; defaults present.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-53-01 | Tampering | malicious playbook triggers arbitrary action | mitigate | validate() rejects unknown actions; ACTION_MAP is a fixed allowlist; destructive flag gates approval (53-04) |
| T-53-02 | EoP | LLM-injected steps | avoid | No LLM in selection/execution (deterministic) |
| T-53-SC | Tampering | package installs | accept | YAML via existing PyYAML; no new dep |
</threat_model>

<verification>
- `pytest test_remediation_playbook.py` green (load, select by class, destructive flag, action map, invalid rejected).
- 6 default playbooks present + valid; no LLM in the path.
</verification>

<success_criteria>
Deterministic YAML playbooks (finding_class→steps, destructive-flagged, with rollback) load/validate/select against a fixed action allowlist, with vendored defaults for all six actions and operator-extensibility via the existing store — no LLM at run time.
</success_criteria>

<output>
Create `.planning/phases/53-autonomous-remediation/53-01-SUMMARY.md` when done.
</output>
