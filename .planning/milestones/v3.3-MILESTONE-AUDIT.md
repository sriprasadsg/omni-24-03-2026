---
milestone: "3.3"
milestone_name: "Agent Geo & Fleet Observability"
audited: 2026-07-30T16:35:00Z
status: passed
auditor: Claude (inline — gsd-core workflow/agents absent, run against .planning conventions)
scores:
  requirements: 11/11 implemented (10 formally verified + GMAP set verified this audit)
  phases: 4/4   # 46-49 executed & verified
  integration: pass
  flows: pass
gaps:
  requirements: []
  integration: []
  flows: []
resolved_this_audit:
  - "49-VERIFICATION.md was missing (46-48 had one) — created 2026-07-30 from code + live services + shipped-bundle + test evidence (status: passed, 12/12 must-haves). Artifact gap closed."
tech_debt:
  - phase: 49-fleet-geo-map
    items:
      - "No 49-VALIDATION.md (Nyquist) — same small-artifact gap v3.2's Phase 45 carried; 49-VERIFICATION.md present and passed. Coverage otherwise proven by 16 unit tests + 7 live CLI UAT assertions."
      - "Backdrop ships the graticule fallback (locked D-01), not a vendored land outline — a public-domain equirectangular land <path> can be prepended to worldMapAsset.ts later, no other change."
      - "On-screen pixel paint not eyeballed (browser tools declined) — verified by proxy (bundle carries the backdrop; projection/clustering correct against live data)."
  - phase: 48-fleet-observability-uptime-rollups
    items:
      - "2 human-check UAT items still [pending] in 48-UAT.md: Metrics-tab chart render + Fleet-Observability non-admin gate. Wired + unit-tested; deferred visual/live confirmation only."
  - milestone: tooling
    items:
      - "REQUIREMENTS.md does not track the v3.3 IDs (GAUD/GSEC/FOBS/GMAP) — same known ROADMAP-vs-REQUIREMENTS sync gap noted in the v3.2 audit. The ROADMAP per-phase [x] checklist + phase artifacts are authoritative."
---

# v3.3 Milestone Audit — Agent Geo & Fleet Observability

**Verdict: PASSED.** All 11 requirements implemented across phases 46–49; every phase has a passing VERIFICATION.md (49's created during this audit); cross-phase integration confirmed live.

## Requirements coverage (11/11)

| Req | Phase | Status | Evidence |
|-----|-------|--------|----------|
| GAUD-01 append-only location history | 46 | ✓ | 46-VERIFICATION passed |
| GAUD-02 per-agent location timeline | 46 | ✓ | 46-VERIFICATION passed |
| GSEC-01 heuristic VPN/hosting flag | 47 | ✓ | 47-VERIFICATION passed |
| GSEC-02 agent-scoped impossible-travel | 47 | ✓ | 47-VERIFICATION passed |
| GSEC-03 alert-only geo-fence | 47 | ✓ | 47-VERIFICATION passed |
| FOBS-01 metrics-history charts | 48 | ✓ | 48-VERIFICATION (10/12; human-pending render) |
| FOBS-02 uptime timeline + % | 48 | ✓ | 48-VERIFICATION |
| FOBS-03 offline + version-drift view | 48 | ✓ | 48-VERIFICATION (human-pending gate) |
| GMAP-01 air-gapped fleet map | 49 | ✓ | 49-VERIFICATION (this audit) |
| GMAP-02 clustering + tenant/status filters | 49 | ✓ | 49-VERIFICATION |
| GMAP-03 marker drill-down | 49 | ✓ | 49-VERIFICATION |

No orphaned requirements. No unmapped phases.

## Cross-phase integration (the milestone's whole point: the map reads everything upstream)

- **49 → 46 (geo data):** FleetGeoMap markers/drill-down read `agent.geo{lat,lon,city,country}` persisted by `geoip_service.lookup(public_ip)` in the heartbeat + registration handlers. Confirmed live. Agents with no resolvable public IP → `geo=null` → surfaced as "Unlocated (N)" (D-07), not dropped.
- **49 → 48 (status):** map status filter + marker color render the server-reported `status` maintained by `monitor_agent_status()` (observed flipping stale agents Online→Offline during UAT). No client recomputation.
- **49 → 47 (flags):** GMAP-03's required drill-down fields (identity, LAN/public IP, location, status) are surfaced; 47's `vpn_heuristic` flag is available on `GeoLocation` but not shown in the geo-map drill-down — outside GMAP-03's required set, noted as an optional future enhancement, not a gap.
- **RBAC:** `GET /api/fleet/geo` scopes cross-tenant (super-admin) vs own-tenant via wrapped `get_database()` + `is_super_admin`; nav gated `manage:agents`. Hermetic + bundle + live confirmed.

## End-to-end flow

Heartbeat (geoip enrichment, 46) → agent doc `geo` persisted → `GET /api/fleet/geo` cross-tenant projection (49-01) → `FleetGeoMap` projects + clusters + filters + drills down (49-04) → gated nav (49-05). Walked live: 6 seeded agents (5 located + 1 unlocated) rendered the correct projection/cluster/filter/drill-down results via the real logic modules.

## Recommendation

Milestone is complete and internally consistent. Before `/gsd-complete-milestone` archives v3.3, optionally: (a) clear the 2 phase-48 human UAT items + the phase-49 on-screen render in a browser, (b) add 49-VALIDATION.md if Nyquist parity with 46–48 is wanted. Neither blocks archival. On archive, the held v4.0 ITAM `MILESTONE-CONTEXT.md` becomes the next cycle.
