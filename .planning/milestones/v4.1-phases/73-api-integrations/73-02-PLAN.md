---
phase: 73-api-integrations
plan: 02
type: execute
wave: 2
depends_on: ["73-01"]
files_modified:
  - backend/itam_lifecycle_endpoints.py
  - backend/itam_consumable_service.py
  - backend/itam_asset_request_service.py
  - backend/tests/test_itam_webhook_events.py
autonomous: true
requirements: [ITAM-API-02]
user_setup: []

must_haves:
  truths:
    - "Checking an asset back in dispatches asset.checked_in with a before/after diff of the same lifecycle keys the check-out event carries (D-05, D-06, D-07)."
    - "Checking out a consumable dispatches consumable.low_stock only when the post-decrement availableQuantity is at or below that consumable's own reorderThreshold, or at or below the shared default when the consumable has no threshold configured — the identical rule the low-stock report already applies, so the event and the report can never disagree (D-05)."
    - "Approving an asset request dispatches asset.request_approved and rejecting one dispatches asset.request_denied, each carrying the flat request record (D-05, D-06)."
    - "No webhook dispatch at any of these mutation points is awaited inline: a slow or unresponsive subscriber can never stall the check-in, consumable-checkout, or approval HTTP response (RESEARCH Pitfall 8)."
    - "Every webhook dispatch added here names its event type through the shared constants module rather than a literal string, so a typo cannot produce an event nothing is subscribed to (RESEARCH Security Domain V5)."
    - "A dispatch failure inside any of these call sites never fails, rolls back, or alters the outcome of the mutation the user actually requested."
  artifacts:
    - backend/tests/test_itam_webhook_events.py
  key_links:
    - "itam_lifecycle_endpoints.checkin_asset -> webhook_service.WebhookService.trigger_webhook (asset.checked_in)"
    - "itam_consumable_service.ConsumableService.checkout_consumable -> webhook_service.WebhookService.trigger_webhook (consumable.low_stock)"
    - "itam_asset_request_service.approve_asset_request / reject_asset_request -> webhook_service.WebhookService.trigger_webhook (asset.request_approved / asset.request_denied)"
    - "itam_consumable_service -> itam_reporting_prebuilt.DEFAULT_LOW_STOCK_QUANTITY (single source of the low-stock threshold fallback)"
  prohibitions:
    - "No webhook payload, ticket body, or log line emitted from these call sites may contain a Jira/ServiceNow credential, an API key or key hash, or any document belonging to a tenant other than the one the mutation occurred in."
---

<objective>
Wire the five request-scoped ITAM webhook events (D-05 categories 1, 3 and the mutation-triggered half of category 4) at their real mutation points, inline, with no new event-dispatch abstraction layer (D-07).

Plan 73-01's tracer already proved the dispatch spine on `asset.checked_out`. This plan expands horizontally across the remaining request-scoped events, reusing that exact call shape.

Purpose: ITAM-API-02's user-visible promise is that ITAM actions push events out. The four background-window events are handled by plans 73-03 and 73-05; everything a user directly triggers is handled here.
Output: `asset.checked_in`, `consumable.low_stock`, `asset.request_approved`, and `asset.request_denied` firing from their true mutation points, plus the ITAM-API-02 request-scoped test coverage.
</objective>

<execution_context>
@/home/user/enterprise-omni-agent-ai-platform/.claude/gsd-core/workflows/execute-plan.md
@/home/user/enterprise-omni-agent-ai-platform/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73-api-integrations/73-CONTEXT.md
@.planning/phases/73-api-integrations/73-RESEARCH.md
@.planning/phases/73-api-integrations/73-PATTERNS.md
@.planning/phases/73-api-integrations/73-01-SUMMARY.md
</context>

## Artifacts this phase produces

Created by THIS plan:

| Symbol | Kind | Location |
|--------|------|----------|
| `asset.checked_in` dispatch call site | inline call | `backend/itam_lifecycle_endpoints.py::checkin_asset` |
| `consumable.low_stock` dispatch call site + `_is_low_stock` local threshold helper | inline call + module helper (new) | `backend/itam_consumable_service.py` |
| `asset.request_approved` / `asset.request_denied` dispatch call sites | inline calls | `backend/itam_asset_request_service.py` |
| `backend/tests/test_itam_webhook_events.py` | test module (new) | `backend/tests/` |

Consumed from plan 73-01 (do not redefine): `backend/itam_webhook_events.py`'s eight `EVENT_*` constants and `ITAM_WEBHOOK_EVENT_TYPES`; the module-level `_webhook_service` instance in `itam_lifecycle_endpoints.py`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: asset.checked_in dispatch, mirrored from the proven check-out slice</name>
  <files>backend/itam_lifecycle_endpoints.py, backend/tests/test_itam_webhook_events.py</files>
  <read_first>
    - backend/itam_lifecycle_endpoints.py — the `checkout_asset` call site as committed by plan 73-01 (this is the shape to mirror), then `checkin_asset` at line 212 through its `invalidate_cache("assets:*")` at line 307, noting its own `pre_image` and reconstructed post-state values
    - backend/itam_webhook_events.py — the constant names
    - .planning/phases/73-api-integrations/73-PATTERNS.md — "backend/itam_lifecycle_endpoints.py — checkout_asset/checkin_asset"
  </read_first>
  <behavior>
    - A successful check-in dispatches exactly one event whose type is `EVENT_ASSET_CHECKED_IN`.
    - The dispatched payload contains `assetId`, a `before` mapping and an `after` mapping, and the full post-state asset document.
    - `before.assignedToId` is the value the asset carried before check-in; `after.assignedToId` reflects the cleared assignment.
    - A check-in that fails its guard (asset not found, or not currently checked out) dispatches nothing.
    - The dispatch is scheduled, not awaited: the handler returns without waiting on subscriber I/O.
  </behavior>
  <action>
Create `backend/tests/test_itam_webhook_events.py` first and write the failing tests for the behaviors above, selectable by `-k lifecycle` per the validation contract. Then implement.

In `backend/itam_lifecycle_endpoints.py`, inside `checkin_asset`, immediately after its existing `invalidate_cache("assets:*")` call and before the `log_itam_action` call, schedule `_webhook_service.trigger_webhook(EVENT_ASSET_CHECKED_IN, payload)` through `asyncio.create_task` — reusing the module-level `_webhook_service` and the `asyncio` import plan 73-01 already added, and importing `EVENT_ASSET_CHECKED_IN` alongside the existing `EVENT_ASSET_CHECKED_OUT` import. Do not add a second `WebhookService()` instance.

The payload mirrors the check-out payload key-for-key (D-06, no bespoke per-event schema): `assetId` from the route parameter, `before` built from the check-in handler's own pre-update document for keys `lifecycleStatus`/`assignedToType`/`assignedToId`, `after` built from its reconstructed post-state for keys `lifecycleStatus`/`assignedToType`/`assignedToId`/`checkedInAt`/`checkedInBy` (use whatever the handler actually names those fields — read them, do not assume), and `asset` set to the post-state document.

Place the call after the guard branches that return early, so a rejected check-in dispatches nothing. Do not wrap it in a try/except at the call site: `asyncio.create_task` does not raise for failures inside the coroutine, and `trigger_webhook` already contains its own error handling — adding a wrapper here would be dead code that suggests a guarantee it does not provide.
  </action>
  <verify>
    <automated>cd /home/user/enterprise-omni-agent-ai-platform && backend/venv/bin/python -m pytest backend/tests/test_itam_webhook_events.py -k lifecycle -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend/venv/bin/python -m pytest backend/tests/test_itam_webhook_events.py -k lifecycle -x -q` exits 0 with at least 4 tests passed.
    - `grep -c "EVENT_ASSET_CHECKED_IN" backend/itam_lifecycle_endpoints.py` returns at least 2 (import plus call site).
    - `grep -c "WebhookService()" backend/itam_lifecycle_endpoints.py` returns exactly 1 (no duplicate instance).
    - A test asserts the recorded payload's `before` and `after` `assignedToId` values differ.
    - `cd backend && venv/bin/python -c "import app"` exits 0.
  </acceptance_criteria>
  <done>Check-in fires `asset.checked_in` with a real before/after diff and does not block on subscriber delivery.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: consumable.low_stock dispatch using the report's own threshold rule</name>
  <files>backend/itam_consumable_service.py, backend/tests/test_itam_webhook_events.py</files>
  <read_first>
    - backend/itam_consumable_service.py — `ConsumableService.checkout_consumable` (around line 88): the atomic `find_one_and_update` with `$inc` and `return_document=ReturnDocument.AFTER`, and the not-found / insufficient-quantity guard branch that follows
    - backend/itam_reporting_prebuilt.py lines 325-370 — `_build_low_stock_consumables_rows` and `DEFAULT_LOW_STOCK_QUANTITY` (line 37): note that a configured `reorderThreshold` is honoured even when it is zero (the check is an explicit not-None test, not a falsy-or fallback), and that "low" means at-or-below, never strictly-below
    - backend/itam_webhook_events.py — the constant names
    - .planning/phases/73-api-integrations/73-RESEARCH.md — "Code Examples / D-05/D-07: consumable.low_stock"
  </read_first>
  <behavior>
    - Post-decrement quantity strictly above the configured `reorderThreshold` → no event.
    - Post-decrement quantity exactly equal to the configured `reorderThreshold` → event fires.
    - Post-decrement quantity below the configured `reorderThreshold` → event fires.
    - Consumable with `reorderThreshold` explicitly set to 0 and post-decrement quantity of 0 → event fires (the configured zero is honoured, not treated as unset).
    - Consumable with no `reorderThreshold` field → the shared default is used as the threshold, with the same at-or-below rule.
    - A checkout that raises (insufficient quantity, or consumable not found) dispatches nothing.
  </behavior>
  <action>
Write the failing tests first, selectable by `-k low_stock` per the validation contract, then implement.

Add a small module-level helper `_is_low_stock(available, threshold)` in `backend/itam_consumable_service.py` expressing the exact rule the low-stock report uses: when `threshold` is not None, low means `available` is at or below `threshold`; when `threshold` is None, low means `available` is at or below the shared default. Resolve the shared default by importing `DEFAULT_LOW_STOCK_QUANTITY` from `itam_reporting_prebuilt` — never redefine the number here, so the event and the report can never drift. Perform that import inside the function body rather than at module top level: `itam_reporting_prebuilt` imports from the ITAM endpoint modules and a top-level import risks a circular import at app startup. Confirm the choice with a real `import app` run, not by reasoning.

In `checkout_consumable`, after the existing not-found / insufficient-quantity guard branch and before the `return Consumable(**consumable)`, evaluate `_is_low_stock(consumable.get("availableQuantity"), consumable.get("reorderThreshold"))` against the post-decrement document the `find_one_and_update` returned, and when it is low schedule `trigger_webhook(EVENT_CONSUMABLE_LOW_STOCK, payload)` through `asyncio.create_task`. Add a module-level `_webhook_service = WebhookService()` to this module rather than constructing one per call.

Payload (D-06 flat record): `consumableId` as the string form of the document's `_id`, `name`, `availableQuantity` (the post-decrement value), and `reorderThreshold` (the effective threshold actually applied, so a subscriber can tell whether it was configured or defaulted).

This call site runs inside a request handler, so ambient tenant context is already established — no `set_tenant_id` bracketing here (that applies only to the background sweeps in plans 73-03 and 73-05).
  </action>
  <verify>
    <automated>cd /home/user/enterprise-omni-agent-ai-platform && backend/venv/bin/python -m pytest backend/tests/test_itam_webhook_events.py -k low_stock -x -q</automated>
    <automated>cd /home/user/enterprise-omni-agent-ai-platform/backend && venv/bin/python -c "import app"</automated>
  </verify>
  <acceptance_criteria>
    - `backend/venv/bin/python -m pytest backend/tests/test_itam_webhook_events.py -k low_stock -x -q` exits 0 with at least 6 tests passed, covering the above/equal/below/zero-threshold/no-threshold/failed-checkout cases.
    - `grep -c "from itam_reporting_prebuilt import" backend/itam_consumable_service.py` returns at least 1 — the threshold constant arrives by import and is never restated locally.
    - `backend/venv/bin/python -c "import sys; sys.path.insert(0,'backend'); import itam_consumable_service as m, itam_reporting_prebuilt as r; assert getattr(m, 'DEFAULT_LOW_STOCK_QUANTITY', r.DEFAULT_LOW_STOCK_QUANTITY) == r.DEFAULT_LOW_STOCK_QUANTITY"` exits 0 (no divergent local value).
    - `cd backend && venv/bin/python -c "import app"` exits 0, proving no circular import was introduced.
    - `backend/venv/bin/python -m pytest backend/tests/test_itam_consumable.py -q` still exits 0 (no regression on the pre-existing consumable tests).
  </acceptance_criteria>
  <done>A consumable checkout that crosses the low-stock line fires `consumable.low_stock` using the same threshold rule the low-stock report applies, and one that does not cross it fires nothing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: asset.request_approved and asset.request_denied dispatch</name>
  <files>backend/itam_asset_request_service.py, backend/tests/test_itam_webhook_events.py</files>
  <read_first>
    - backend/itam_asset_request_service.py lines 96-163 — `approve_asset_request` and `reject_asset_request`: both guard on the request already being pending, both `find_one_and_update` returning the post-update document, and both then call `self.notification_service.send_asset_request_notification`
    - backend/itam_asset_request_endpoints.py lines 121-167 — the two routes that call them, and their `_require_asset_approver` gate (confirming a request context, and therefore ambient tenant context, exists)
    - backend/itam_models.py lines 566-602 — `AssetRequest` and `AssetRequestStatus`; note `model_config` uses `extra="ignore"`, which is why the webhook payload is built from the raw returned document rather than the validated model
    - backend/itam_webhook_events.py — the constant names
  </read_first>
  <behavior>
    - Approving a pending request dispatches exactly one event of type `EVENT_ASSET_REQUEST_APPROVED`.
    - Rejecting a pending request dispatches exactly one event of type `EVENT_ASSET_REQUEST_DENIED`.
    - Approving or rejecting a request that is not pending (already approved or rejected, or not found) dispatches nothing.
    - The payload carries the request's `id`, `item_description`, `quantity`, `status`, `requester_id`, `approver_id`, and the approval timestamp.
    - A dispatch failure does not prevent the existing notification from being sent, and does not change the value the service returns.
  </behavior>
  <action>
Write the failing tests first, selectable by `-k asset_request` per the validation contract, then implement.

Add a module-level `_webhook_service = WebhookService()` and an `asyncio` import to `backend/itam_asset_request_service.py`, and import `EVENT_ASSET_REQUEST_APPROVED` and `EVENT_ASSET_REQUEST_DENIED` from `itam_webhook_events`.

In `approve_asset_request`, inside the `if result:` branch — after the model validation, alongside the existing notification call — schedule `_webhook_service.trigger_webhook(EVENT_ASSET_REQUEST_APPROVED, payload)` through `asyncio.create_task`. Mirror it exactly in `reject_asset_request` with `EVENT_ASSET_REQUEST_DENIED`. Note the deliberate asymmetry between the internal status value (`rejected`) and the outward event name (`asset.request_denied`): the event name is fixed by D-05 and by the frontend picker, so it must not be "corrected" to match the enum.

Build the payload from the raw `result` document rather than the validated `AssetRequest` model, because that model is configured to drop unknown fields and would silently strip the ticket fields plan 73-04 adds to these documents. Serialize datetime values to ISO-8601 strings so the payload is JSON-encodable by the dispatcher. Include: `requestId` (the document's `id`), `item_description`, `quantity`, `status`, `requester_id`, `approver_id`, `approval_date`, `request_date`.

Place the dispatch so it cannot alter control flow: it must not precede the notification call in a way that would let an exception skip it, and the function must return exactly what it returned before.
  </action>
  <verify>
    <automated>cd /home/user/enterprise-omni-agent-ai-platform && backend/venv/bin/python -m pytest backend/tests/test_itam_webhook_events.py -k asset_request -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend/venv/bin/python -m pytest backend/tests/test_itam_webhook_events.py -k asset_request -x -q` exits 0 with at least 5 tests passed.
    - `grep -c "EVENT_ASSET_REQUEST_DENIED" backend/itam_asset_request_service.py` returns at least 2.
    - A test asserts that a non-pending request produces zero dispatches.
    - A test asserts the existing `send_asset_request_notification` call still occurs on both paths.
    - `backend/venv/bin/python -m pytest backend/tests/test_itam_webhook_events.py -q` exits 0 for the whole module.
    - `cd backend && venv/bin/python -c "import app"` exits 0.
  </acceptance_criteria>
  <done>Approval and rejection each fire their D-05 event with the flat request record, and neither the notification path nor the return value changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ITAM mutation handler → outbound webhook subscriber | Tenant business data crosses to a tenant-configured external URL |
| request task → `asyncio.create_task` dispatch task | Tenant context crosses a task boundary by contextvars snapshot |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-73-07 | Information Disclosure | webhook payloads from check-in, consumable checkout, approve/reject | high | mitigate | Each payload is an explicit key list built from the mutated document only; no credential, key hash, notification recipient list, or cross-tenant document is placed in it (Tasks 1-3) |
| T-73-08 | Information Disclosure | cross-tenant dispatch | high | mitigate | All five call sites run inside request handlers where the auth dependency already set tenant context; `asyncio.create_task` snapshots that context at creation, so a later reset cannot retarget an in-flight dispatch. No background-sweep dispatch is added by this plan (RESEARCH Pitfall 3) |
| T-73-09 | Denial of Service | inline dispatch stalling a mutation response | medium | mitigate | Every call site schedules rather than awaits; `trigger_webhook`'s sequential 10-second-per-subscriber loop can never enter the request's critical path (RESEARCH Pitfall 8) |
| T-73-10 | Tampering | event-type strings | low | mitigate | All call sites reference the shared constants module; a typo becomes an import error rather than a silently unmatched subscription |
| T-73-11 | Repudiation | dispatch failure invisible to operators | low | accept | `trigger_webhook` already records delivery attempts in `webhook_deliveries`, which the existing WebhookManagement deliveries view surfaces; no additional signalling is warranted for this phase's scope |
| T-73-SC | Tampering | npm/pip/cargo installs | high | accept | Zero new third-party packages — see plan 73-01's register entry and RESEARCH.md "Package Legitimacy Audit" |
</threat_model>

<verification>
- `backend/venv/bin/python -m pytest backend/tests/test_itam_webhook_events.py -q` is fully green.
- Pre-existing consumable, lifecycle, and asset-request test modules still pass unchanged.
- `cd backend && venv/bin/python -c "import app"` exits 0 (circular-import check for the deferred `itam_reporting_prebuilt` import).
</verification>

<success_criteria>
Five of the eight D-05 event types (`asset.checked_out` from plan 73-01, plus `asset.checked_in`, `consumable.low_stock`, `asset.request_approved`, `asset.request_denied` here) fire from real user-triggered ITAM mutations, are subscribable through the existing webhook subscription CRUD, and none of them can delay or break the mutation that produced them.
</success_criteria>

<output>
Create `.planning/phases/73-api-integrations/73-02-SUMMARY.md` when done. Record where the `DEFAULT_LOW_STOCK_QUANTITY` import was placed and why, and confirm the `import app` circular-import check result.
</output>
