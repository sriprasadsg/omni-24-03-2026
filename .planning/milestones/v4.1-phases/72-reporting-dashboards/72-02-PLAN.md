---
phase: 72-reporting-dashboards
plan: 02
type: execute
wave: 2
depends_on:
  - 72-01
files_modified:
  - backend/itam_reporting_prebuilt.py
  - backend/itam_models.py
  - backend/tests/test_itam_reporting_prebuilt.py
autonomous: true
requirements:
  - ITAM-REP-02
user_setup: []

must_haves:
  truths:
    - "All six pre-built reports from D-08 are runnable from the Reports tab: Asset value/depreciation summary, Check-out/check-in activity log, Warranty expiring, License seat utilization, Low-stock consumables, Overdue physical audits."
    - "Pre-built reports take no user-configurable parameters beyond pagination — each report's filter and sort are code-defined (D-09)."
    - "Each pre-built report has a report-appropriate default sort: Warranty Expiring ascending by expiry date, Asset value descending by value, Check-out/in log descending by event timestamp, License utilization descending by usage %, Low-stock consumables ascending by remaining quantity, Overdue audits ascending by days overdue."
    - "A pre-built report with zero matching rows (e.g. no assets with expiring warranty) shows the same 'No matching assets' empty state as the custom-builder preview (UI-SPEC element E4)."
    - "The Asset value report's book value for a given asset equals what GET /api/assets/{id}/book-value returns for that same asset — the report calls itam_finance_service.compute_book_value with the same inputs rather than re-deriving depreciation."
    - "The License seat utilization report's seatsAssigned/seatsAvailable match what the Licenses tab shows, because both run itam_license_endpoints._enrich_license_seats_and_expiry."
    - "The Overdue physical audits report returns the same asset set as the already-shipped GET /api/assets/reports/overdue-audit route, because it calls that route's own _overdue_query/_overdue_row helpers."
    - "A consumable carries an optional reorderThreshold (nullable, admin-settable); the Low-stock consumables report flags items where availableQuantity is at or below reorderThreshold when it is set, and falls back to a fixed availableQuantity at-or-below 5 heuristic when it is not — so the report works with no data migration (D-19)."
    - "Adding reorderThreshold does not break any existing consumable create/update/checkout request — the field is optional on ConsumableCreate, Consumable and ConsumableUpdate and defaults to null."
  artifacts:
    - backend/itam_reporting_prebuilt.py (six PREBUILT_REPORTS entries)
    - backend/tests/test_itam_reporting_prebuilt.py
    - backend/itam_models.py (reorderThreshold on the three consumable models)
  key_links:
    - "Each new report registers itself in the existing PREBUILT_REPORTS dict, so the Reports tab list and the export route pick it up with no endpoint change."
    - "Report builders import compute_book_value, _enrich_license_seats_and_expiry and _overdue_query/_overdue_row from the existing services rather than re-implementing those calculations."
  prohibitions:
    - requirement_id: ITAM-REP-02
      category: transparency
      status: resolved
      verification: test
      statement: "MUST NOT re-derive book value, warranty status, licence-seat utilisation or overdue-audit age with a second implementation inside the reporting module — a report figure that silently disagrees with the same figure on the Finance or Licences tab misleads the operator who trusts both."
---

<objective>
Complete D-08's six pre-built reports by adding the five the tracer did not cover, each reading data that already exists in the Phase 56-71 ITAM backend, and add the one data-model field the Low-stock report needs (D-19).

Purpose: the pre-built section of the Reports tab (ITAM-REP-02) is only real once all six cards run.
Output: five new PREBUILT_REPORTS entries, an optional reorderThreshold on the consumable models, and a test file covering all six reports.
</objective>

<execution_context>
@/home/user/enterprise-omni-agent-ai-platform/.claude/gsd-core/workflows/execute-plan.md
@/home/user/enterprise-omni-agent-ai-platform/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-reporting-dashboards/72-CONTEXT.md
@.planning/phases/72-reporting-dashboards/72-RESEARCH.md
@.planning/phases/72-reporting-dashboards/72-PATTERNS.md
@.planning/phases/72-reporting-dashboards/72-01-SUMMARY.md
</context>

<assumption_delta_decision>
Signal kind `chosen` (derived→chosen): the low-stock threshold. Before this phase there is no threshold field at all, so the only possible implementation is a derived heuristic; D-19 introduces a chosen per-consumable value. Decision: **add-alongside**, not promote. The chosen `reorderThreshold` takes precedence whenever it is set, and the derived heuristic remains as the fallback for the documents that predate the field — promoting the chosen value to the sole source would require backfilling every existing consumable, which D-19 explicitly rules out ("so the report works day one without forcing a data-migration/backfill step").
</assumption_delta_decision>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Asset value, check-out/in activity log, and overdue-audit reports</name>
  <read_first>
    backend/itam_reporting_prebuilt.py
    backend/itam_reporting_service.py
    backend/itam_finance_service.py
    backend/itam_finance_endpoints.py
    backend/itam_lifecycle_endpoints.py
    backend/itam_lifecycle_service.py
    backend/tests/itam_reporting_test_support.py
  </read_first>
  <files>
    backend/itam_reporting_prebuilt.py
    backend/tests/test_itam_reporting_prebuilt.py
  </files>
  <behavior>
    - `asset_value` returns one row per asset with a purchase record, sorted by book value descending.
    - An asset whose model carries no usefulLifeYears/salvageValueCents yields a row with an em-dash book value and the same reason string the book-value endpoint returns, never a fabricated number.
    - `asset_value` book values equal `compute_book_value(...)` for the same inputs.
    - `checkout_activity` returns assignment_history events newest-first across the whole tenant, including checkout, checkin and audit actions.
    - `overdue_audits` returns exactly the assets `_overdue_query` matches, sorted ascending by days overdue with unknown-basis rows last.
    - Each of the three reports returns zero rows and its declared column list (not an error) when nothing matches.
  </behavior>
  <action>
Add three entries to `PREBUILT_REPORTS` in `backend/itam_reporting_prebuilt.py`, each following the existing `warranty_expiring` entry's shape (`key`, `title`, `description`, `columns`, `defaultSort`, `builder`).

`asset_value` — title `Asset Value & Depreciation`. Query `db.assets` tenant-scoped for documents where `purchaseCostCents` and `purchaseDate` are both present, collect their `modelId` values, and issue ONE batched `db.asset_models.find({"id": {"$in": [...]}})` lookup rather than a per-asset query or an aggregation join. Resolve depreciation inputs exactly the way `itam_finance_endpoints.get_asset_book_value` does — `usefulLifeYears` and `salvageValueCents` come from the model document — then call `itam_finance_service.compute_book_value(purchase_date=..., purchase_cost_cents=..., useful_life_years=..., salvage_value_cents=..., now=...)`. When the model document is missing or either policy field is None, emit the row with an em dash for the value columns and the endpoint's own `REASON_NO_DEPRECIATION_POLICY` string in a `Reason` column; catch `ValueError` from `compute_book_value` and treat it the same way. Display headers: `Asset Tag`, `Name`, `Purchase Date`, `Purchase Cost`, `Book Value`, `Years Elapsed`, `Reason`. Money columns render as a decimal currency amount derived from the cents integer. Sort descending by computed book value, rows without a value last.

`checkout_activity` — title `Check-Out / Check-In Activity`. Read `db.assignment_history` tenant-scoped (this is a straight ledger read — the collection is written only by `itam_lifecycle_service.write_history`), sorted `ts` descending with `_id` descending as the tiebreak, mirroring `itam_lifecycle_service.list_history`'s stable ordering but fleet-wide instead of per-asset. Batch-resolve the referenced `assetId` values through one `db.assets.find({"id": {"$in": [...]}})` call to add the asset tag and name. Display headers: `When`, `Action`, `Asset Tag`, `Asset Name`, `Target Type`, `Target`, `Actor`, `Note`. Actions include the `checkout`, `checkin` and `audit` values `itam_lifecycle_endpoints` writes.

`overdue_audits` — title `Overdue Physical Audits`. Import `AUDIT_INTERVAL_DAYS`, `_audit_cutoff_iso`, `_overdue_query` and `_overdue_row` from `itam_lifecycle_endpoints` and use them unchanged; the only new work here is the display-row mapping and the export wrapper the existing JSON route lacks. Display headers: `Asset Tag`, `Name`, `Lifecycle Status`, `Last Audited`, `Age Basis`, `Days Overdue`, `Never Audited`. Sort ascending by `daysOverdue` with null-basis rows last, matching the existing route's own ordering intent.

Create `backend/tests/test_itam_reporting_prebuilt.py` importing the fixtures from `backend/tests/itam_reporting_test_support.py`, with one test per behavior listed above plus a test asserting the report's book value equals a direct `compute_book_value` call on the same seeded asset.
  </action>
  <verify>
    <automated>backend/venv/bin/python -m pytest backend/tests/test_itam_reporting_prebuilt.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `backend/venv/bin/python -m pytest backend/tests/test_itam_reporting_prebuilt.py -x` exits 0.
    - `backend/venv/bin/python -c "from itam_reporting_prebuilt import PREBUILT_REPORTS; print(sorted(PREBUILT_REPORTS))"` run from `backend/` includes `asset_value`, `checkout_activity`, `overdue_audits` and `warranty_expiring`.
    - A test asserts the `asset_value` row's book value is byte-equal to `compute_book_value(...)`'s `bookValueCents` for the same seeded inputs.
    - A test asserts `overdue_audits` returns the same asset ids as `_overdue_query` run directly against the seeded collection.
    - `grep -v '^\s*#' backend/itam_reporting_prebuilt.py | grep -c 'from itam_lifecycle_endpoints import'` returns 1.
  </acceptance_criteria>
  <done>Three more pre-built reports run end-to-end from the Reports tab and export through the existing CSV route, with figures that match the Finance and Lifecycle surfaces.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Licence utilisation and low-stock consumables reports, with the reorderThreshold field</name>
  <read_first>
    backend/itam_models.py
    backend/itam_reporting_prebuilt.py
    backend/itam_license_endpoints.py
    backend/itam_consumable_service.py
    backend/itam_consumable_endpoints.py
    backend/tests/test_itam_reporting_prebuilt.py
  </read_first>
  <files>
    backend/itam_models.py
    backend/itam_reporting_prebuilt.py
    backend/tests/test_itam_reporting_prebuilt.py
  </files>
  <behavior>
    - `ConsumableCreate`, `Consumable` and `ConsumableUpdate` each accept an optional integer `reorderThreshold` with a zero lower bound and a null default.
    - An existing consumable payload that omits reorderThreshold still validates (the field is optional, extra="forbid" is unchanged).
    - `low_stock_consumables` flags a consumable whose availableQuantity is less than or equal to its set reorderThreshold.
    - A consumable with no reorderThreshold is flagged only when availableQuantity is less than or equal to 5.
    - A consumable above both its threshold and the fallback is absent from the report.
    - `license_utilization` returns one row per licence with seatsAssigned/seatsAvailable/utilisation percent computed by `_enrich_license_seats_and_expiry`, sorted by utilisation descending.
    - A licence with a zero seatCount reports zero percent utilisation rather than raising a division error.
  </behavior>
  <action>
First the model change (D-19): add `reorderThreshold: Optional[int] = Field(None, ge=0)` to `ConsumableCreate` and to `ConsumableUpdate` in `backend/itam_models.py`, following the `Optional[...] = Field(None, ...)` convention already used in that file. `Consumable` inherits from `ConsumableCreate`, so it needs no separate declaration — confirm that by reading the class rather than assuming. Do not touch `itam_consumable_endpoints.py` or `itam_consumable_service.py`: the update route already passes a validated `ConsumableUpdate` straight through, so the new optional field flows without an endpoint change. Do not add a migration or a backfill — the field is nullable by design.

Then add two entries to `PREBUILT_REPORTS`.

`license_utilization` — title `License Seat Utilization`. Read `db.licenses` tenant-scoped, and for each licence obtain the assigned-seat count with `db.license_assignments.count_documents({"licenseId": lic["id"]})`, then call `itam_license_endpoints._enrich_license_seats_and_expiry(doc, seats_assigned, now)` — the same function `GET /api/itam/licenses` uses, so the report and the Licences tab cannot disagree. Compute the utilisation percentage as assigned over `seatCount`, guarding a zero `seatCount` to a zero percentage rather than a division error. Display headers: `License`, `Manufacturer`, `Seats Total`, `Seats Assigned`, `Seats Available`, `Utilization %`, `Expiry Date`, `Status`. Sort descending by utilisation percentage.

`low_stock_consumables` — title `Low-Stock Consumables`. Read `db.itam_consumables` tenant-scoped. Flag a consumable when `availableQuantity` is at or below its `reorderThreshold` where that field is set, otherwise when `availableQuantity` is at or below the module constant `DEFAULT_LOW_STOCK_QUANTITY = 5` (D-19's fallback). Display headers: `Consumable`, `Unit Type`, `Available`, `Initial`, `Reorder Threshold`, `Basis`, where `Basis` states whether the row was flagged by the configured threshold or by the default. Per RESEARCH.md Pitfall 3 consumable ids are raw ObjectId hex strings with no prefix, unlike every sibling entity — display `name` as the identifying column and do not join consumables to assets by id. Sort ascending by `availableQuantity`.

Extend `backend/tests/test_itam_reporting_prebuilt.py` with one test per behavior listed above, including a consumable payload round-trip through `ConsumableCreate` with the field absent and with it set.
  </action>
  <verify>
    <automated>backend/venv/bin/python -m pytest backend/tests/test_itam_reporting_prebuilt.py backend/tests/test_itam_license.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `backend/venv/bin/python -m pytest backend/tests/test_itam_reporting_prebuilt.py backend/tests/test_itam_license.py -x` exits 0.
    - `backend/venv/bin/python -c "from itam_models import ConsumableCreate, Consumable, ConsumableUpdate; print(ConsumableCreate.model_fields['reorderThreshold'].default, Consumable.model_fields['reorderThreshold'].default, ConsumableUpdate.model_fields['reorderThreshold'].default)"` run from `backend/` prints three `None` values.
    - `backend/venv/bin/python -c "from itam_reporting_prebuilt import PREBUILT_REPORTS; print(len(PREBUILT_REPORTS))"` run from `backend/` prints 6.
    - A test asserts a consumable with `availableQuantity` 6 and `reorderThreshold` 10 is flagged while a consumable with `availableQuantity` 6 and no threshold is not.
    - `backend/venv/bin/python -m pytest` reports no new failures relative to the pre-existing baseline.
  </acceptance_criteria>
  <done>All six D-08 pre-built reports are registered and tested, and an admin can set a per-consumable reorder threshold that the Low-Stock report honours.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → MongoDB | Six pre-built report builders read `assets`, `licenses`, `license_assignments`, `itam_consumables`, `assignment_history` and `asset_models` across a multi-tenant database. |
| Admin form → consumable document | The new `reorderThreshold` value is written from an admin-supplied payload. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-72-02a | Information Disclosure | Batched cross-collection lookups in `asset_value` and `checkout_activity` | high | mitigate | Every secondary read goes through the tenant-isolated `db.X.find({"id": {"$in": [...]}})` handle, which injects the tenant match; no aggregation join is used, so no sub-pipeline can escape tenant scoping (RESEARCH Pitfall 4). |
| T-72-02b | Tampering | `reorderThreshold` written from an admin payload | low | mitigate | Pydantic `Optional[int]` with `ge=0` on `ConsumableCreate`/`ConsumableUpdate`, and `extra="forbid"` remains in force so no adjacent field can be smuggled in. |
| T-72-02c | Information Disclosure | Licence rows exposing `productKey` | medium | mitigate | The `license_utilization` report's display headers are a fixed list that does not include the product key; the report emits only the declared columns, never the raw licence document. |
| T-72-SC | Tampering | npm/pip installs | low | accept | No package is installed by this plan. |
</threat_model>

<verification>
- `backend/venv/bin/python -m pytest backend/tests/test_itam_reporting*.py -x` passes.
- `backend/venv/bin/python -m pytest` shows no new failures against the pre-existing baseline.
- Running each of the six report keys through `POST /api/itam/reports/prebuilt/{key}/run` returns 200 with the declared columns.
</verification>

<success_criteria>
- `PREBUILT_REPORTS` contains exactly the six keys D-08 names.
- Report figures for book value, seat utilisation and overdue-audit age are produced by the existing service functions, proven by equality assertions in tests.
- `reorderThreshold` is optional everywhere it appears and no existing consumable test regresses.
</success_criteria>

## Artifacts this phase produces

New backend symbols: `PREBUILT_REPORTS` entries `asset_value`, `checkout_activity`, `overdue_audits`, `license_utilization`, `low_stock_consumables`; module constant `DEFAULT_LOW_STOCK_QUANTITY`; their builder callables.

New model fields: `ConsumableCreate.reorderThreshold`, `ConsumableUpdate.reorderThreshold` (inherited onto `Consumable`).

New files: `backend/tests/test_itam_reporting_prebuilt.py`.

<output>
Create `.planning/phases/72-reporting-dashboards/72-02-SUMMARY.md` when done
</output>
