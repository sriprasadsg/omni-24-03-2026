---
phase: 64-user-management
plan: "06"
type: execute
wave: 2
depends_on: ["64-01", "64-02"]
files_modified:
  - backend/mfa_service.py
  - backend/mfa_endpoints.py
  - backend/authentication_service.py
  - backend/tests/test_mfa.py
autonomous: true
requirements:
  - ITAM-USR-06
user_setup: []
must_haves:
  truths:
    - Users can enroll in 2FA (TOTP) via QR code
    - Users can verify TOTP codes to complete login (two-phase flow)
    - Backup codes generated, encrypted, and usable for recovery
    - MFA session tokens stored persistently (not in-memory) with TTL
    - TOTP secrets encrypted at rest (no base64 fallback in production)
    - Users can disable 2FA (with password confirmation)
  artifacts:
    - Fixed mfa_service.py (Pitfalls 1 & 2 resolved)
    - Extended mfa_endpoints.py for enroll/verify/disable/backup-codes
    - Test file covering enroll, verify, backup codes, disable, encryption, session persistence
  key_links:
    - User model: mfaEnabled, mfaSecretEncrypted, mfaBackupCodesEncrypted, mfaSessionTokens (TTL collection)
    - Two-phase login: create_mfa_session -> verify_mfa_token -> JWT
    - encryption_service used for secret/backup-code encryption (fail loudly if unavailable)
---

<objective>
Fix and complete existing mfa_service.py (260 lines) and mfa_endpoints.py to fully implement ITAM-USR-06: Two-Factor Authentication. Address known pitfalls: in-memory MFA session storage (Pitfall 1) and TOTP secret encryption fallback (Pitfall 2). Verify frontend wiring.

Purpose: Provide production-ready 2FA with TOTP, backup codes, secure encryption, and persistent session storage.

Output: Working 2FA with enroll/verify/disable/backup-codes, fixed pitfalls, persistent sessions, encryption health check, and test coverage.
</objective>

<execution_context>
@/home/user/enterprise-omni-agent-ai-platform/.claude/gsd-core/workflows/execute-plan.md
@/home/user/enterprise-omni-agent-ai-platform/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@/home/user/enterprise-omni-agent-ai-platform/.planning/milestones/v4.1-phases/64-user-management/RESEARCH.md
@/home/user/enterprise-omni-agent-ai-platform/backend/mfa_service.py
@/home/user/enterprise-omni-agent-ai-platform/backend/mfa_endpoints.py
@/home/user/enterprise-omni-agent-ai-platform/backend/authentication_service.py
@/home/user/enterprise-omni-agent-ai-platform/backend/encryption_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix MFA pitfalls — persistent sessions and encryption enforcement</name>
  <files>
    backend/mfa_service.py
    backend/tests/test_mfa.py
  </files>
  <action>
    Fix mfa_service.py per D-06 (2FA) and RESEARCH.md Pitfalls 1 & 2:
    1. Pitfall 1 (In-memory MFA sessions): Replace `_mfa_sessions: dict` with MongoDB TTL collection:
       - Create `mfa_sessions` collection with TTL index on `expires_at` (5 minutes)
       - `create_mfa_session(email)` -> insert document with session_token, email, created_at, expires_at
       - `verify_mfa_token(session_token, totp_code)` -> find_and_delete (single-use), verify TOTP, return result
       - `cleanup_expired_sessions()` -> handled by TTL index automatically
    2. Pitfall 2 (Encryption fallback): Remove base64 fallback in `_encrypt_secret()` / `_decrypt_secret()`:
       - Import `encryption_service` at module level (fail fast if unavailable)
       - Add startup health check: `verify_encryption_service()` -> raises if encryption_service not available
       - In production, this MUST pass; in dev, document requirement
    3. Ensure backup codes are encrypted with same encryption_service (AES-256)
    4. Add `disable_mfa(user_id, password)` -> verify password, clear MFA fields, revoke sessions
    5. Add `regenerate_backup_codes(user_id)` -> generate new codes, encrypt, store
    6. Preserve existing two-phase login flow: create_mfa_session -> verify_mfa_token
    7. Add `get_mfa_status(user_id)` -> {enabled, has_backup_codes, last_used}
    Create test_mfa.py covering: enroll, verify, backup codes, disable, regenerate, encryption, session TTL, health check.
  </action>
  <verify>
    <automated>pytest backend/tests/test_mfa.py -x -v</automated>
  </verify>
  <done>
    MFA sessions use MongoDB TTL (not in-memory); encryption enforced (no base64 fallback); health check works; all MFA flows tested
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend MFA endpoints and integrate with authentication flow</name>
  <files>
    backend/mfa_endpoints.py
    backend/authentication_service.py
    backend/tests/test_mfa.py
  </files>
  <action>
    Extend mfa_endpoints.py (user-scoped, gated by get_current_user). **Route names are frozen** — the five existing routes are already consumed by shipped frontend components (see <frontend_scope> below). Extend their payloads additively; do NOT rename them.
    1. POST /api/mfa/setup — start TOTP enrollment (EXISTING route, keep the name)
       * Returns: {secret, qr_code_base64, backup_codes[]}
       * Does NOT enable MFA yet (user must verify first code)
    2. POST /api/mfa/verify-setup — verify first TOTP code to enable MFA (EXISTING route, keep the name)
       * Body: {code}
       * On success: enable MFA, store encrypted secret/backup codes
    3. POST /api/mfa/disable — disable MFA (EXISTING route)
       * Body: {password}
       * Verify password, clear MFA fields
       * If the shipped UserProfilePage currently posts without a password field, add the password requirement server-side AND update the component's request body in the same task — this is a deliberate, breaking security tightening, not an accident.
    4. GET /api/mfa/status — {enabled, has_backup_codes, backup_codes_remaining, last_used} (EXISTING route; adding fields is safe, the consumer reads defensively)
    5. POST /api/mfa/backup-codes/regenerate — generate new backup codes (NEW route, no current consumer)
       * Body: {password}
       * Returns: {backup_codes[]}
    6. POST /api/mfa/verify — verify TOTP/backup code during login, two-phase (EXISTING route, keep the name)
       * Body: {session_token, code}
       * Returns: {success, token (JWT), mfa_verified: true}
    Integrate with authentication_service.py login flow:
    7. Modify login: if user has MFA enabled, return mfa_session_token (phase 1) instead of JWT
    8. Client calls /api/mfa/verify with session_token + code -> returns JWT (phase 2)
    9. Ensure backup codes work as alternative to TOTP in verify
    Add tests for all endpoints and login integration.
  </action>
  <verify>
    <automated>pytest backend/tests/test_mfa.py -x -v -k "endpoint or login"</automated>
    <automated>bash -c 'fail=0; for p in $(grep -rho "api/mfa/[a-z-]*" components/MFASetupWizard.tsx components/MFAVerifyModal.tsx components/UserProfilePage.tsx | sort -u); do r="${p#api/mfa}"; if grep -q "\"$r\"" backend/mfa_endpoints.py; then echo "OK   $p"; else echo "DEAD $p"; fail=1; fi; done; exit $fail'</automated>
  </verify>
  <done>
    MFA endpoints work; login flow integrates two-phase MFA; backup codes functional; tests pass. The route gate exits 0 — every MFA path the shipped components call still resolves to a route registered in mfa_endpoints.py. (This gate passes against the pre-change codebase; it exists to catch a rename regression, so a failure means the plan's frozen-route constraint was violated.)
  </done>
</task>

</tasks>

<frontend_scope>
## Frontend wiring (ROADMAP "UI hint: yes")

2FA UI already exists and is wired to the backend — this is the one requirement in Phase 64 whose frontend is genuinely complete. Verified consumers:

| Component | Lines | Calls |
|-----------|-------|-------|
| `components/MFASetupWizard.tsx` | 146 | `POST /api/mfa/setup`, `POST /api/mfa/verify-setup` |
| `components/MFAVerifyModal.tsx` | 121 | `POST /api/mfa/verify` |
| `components/UserProfilePage.tsx` | 317 | `GET /api/mfa/status`, `POST /api/mfa/disable` |

All three are mounted from `App.tsx` / `src/router/routes.tsx`. Backend counterparts exist today in `backend/mfa_endpoints.py` at `/setup`, `/verify-setup`, `/verify`, `/disable`, `/status`.

This resolves RESEARCH.md's "verify frontend wiring" note for ITAM-USR-06: the wiring is present, so **no new UI is needed** — the risk is the reverse, backend contract drift breaking shipped UI. Hence the frozen route names in Task 2 and the grep gate in its verify block.

**Deferred:** a backup-code regeneration affordance in `UserProfilePage.tsx` for the new `/api/mfa/backup-codes/regenerate` route. Backend-only here; UI deferred to the Phase 65 ITAM console work, which owns the frontend service layer.
</frontend_scope>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → MFA Endpoints | User manages own MFA; password required for disable/regenerate |
| MFA Service → MongoDB | Encrypted secrets/backup codes; TTL session collection |
| MFA Service → Encryption Service | AES-256 for secrets; fail loudly if unavailable |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-64-23 | Elevation of Privilege | MFA bypass via session | high | mitigate | Short TTL (5 min), single-use consumption, persistent MongoDB storage (Pitfall 1 fixed) |
| T-64-24 | Information Disclosure | TOTP secret exposure | critical | mitigate | AES-256 encryption at rest; no base64 fallback (Pitfall 2 fixed); plaintext only in QR at enrollment |
| T-64-25 | Elevation of Privilege | Backup code reuse | medium | mitigate | Single-use consumption; encrypted storage; regenerate invalidates old |
| T-64-26 | Tampering | MFA disable without auth | high | mitigate | Password confirmation required for disable/regenerate |
| T-64-27 | Denial of Service | MFA session exhaustion | low | mitigate | TTL auto-cleanup; per-user session limit |
| T-64-SC | Tampering | npm/pip/cargo installs | high | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] packages |

</threat_model>

<verification>
Run: pytest backend/tests/test_mfa.py -x -v
All tests pass including enroll, verify, backup codes, disable, encryption, session TTL, login integration
</verification>

<success_criteria>
2FA fully functional: TOTP enrollment/verification, backup codes, disable/regenerate, persistent sessions, enforced encryption, login integration.
</success_criteria>

<output>
Create .planning/milestones/v4.1-phases/64-user-management/64-06-SUMMARY.md when done
</output>