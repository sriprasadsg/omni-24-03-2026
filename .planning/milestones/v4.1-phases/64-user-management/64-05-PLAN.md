---
phase: 64-user-management
plan: "05"
type: execute
wave: 3
depends_on: ["64-01", "64-02"]
files_modified:
  - backend/api_key_auth.py
  - backend/api_key_endpoints.py
  - backend/tests/test_api_key.py
autonomous: true
requirements:
  - ITAM-USR-05
user_setup: []
must_haves:
  truths:
    - Users can create, list, revoke, and view API tokens with scopes
    - Tokens have expiration, rate limits, and scoped permissions
    - Token hash stored securely (bcrypt); plaintext shown only once at creation
    - Token authentication enforces scopes at endpoint level
  artifacts:
    - Extended api_key_auth.py with full token lifecycle
    - New api_key_endpoints.py for user-facing token management
    - Test file covering create/list/revoke/scope/expiration/rate-limit
  key_links:
    - User model: apiKeys array with keyHash, scopes, expiresAt, rateLimit, lastUsedAt
    - rbac_service.has_permission() extended to check token scopes
    - Authentication: X-API-Key header validated -> TokenData with token scopes
---

<objective>
Extend existing api_key_auth.py (32 lines, minimal stub) to fully implement ITAM-USR-05: API Access Token Management with full lifecycle, scoping, expiration, and rate limiting.

Purpose: Enable programmatic API access for integrations, automation, and CLI tools with granular permission control.

Output: Working API token management with create/list/revoke, scoped permissions, expiration, rate limits, secure storage, and test coverage.
</objective>

<execution_context>
@/home/user/enterprise-omni-agent-ai-platform/.claude/gsd-core/workflows/execute-plan.md
@/home/user/enterprise-omni-agent-ai-platform/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@/home/user/enterprise-omni-agent-ai-platform/.planning/milestones/v4.1-phases/64-user-management/RESEARCH.md
@/home/user/enterprise-omni-agent-ai-platform/backend/api_key_auth.py
@/home/user/enterprise-omni-agent-ai-platform/backend/rbac_service.py
@/home/user/enterprise-omni-agent-ai-platform/backend/user_endpoints.py
@/home/user/enterprise-omni-agent-ai-platform/backend/auth_types.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend api_key_auth.py with full token lifecycle and scoping</name>
  <files>
    backend/api_key_auth.py
    backend/tests/test_api_key.py
  </files>
  <action>
    Extend api_key_auth.py per D-05 (API Tokens) and RESEARCH.md Pitfall 5:
    1. APIKeyModel dataclass: id, name, keyHash (bcrypt), keyPrefix (first 8 chars for display), scopes (list[str]), expiresAt (datetime|null), rateLimit (requests/minute), createdAt, lastUsedAt, revokedAt, createdBy (userId)
    2. APIKeyService class:
       - create_key(user_id, name, scopes, expires_in_days, rate_limit) -> (api_key_model, plaintext_key)
         * Generate secure random key (secrets.token_urlsafe(32))
         * Hash with bcrypt (passlib)
         * Store prefix for display (first 8 chars)
         * Return plaintext ONCE only
       - list_keys(user_id) -> list[APIKeyModel] (exclude keyHash, include prefix)
       - revoke_key(user_id, key_id) -> bool
       - validate_key(api_key) -> APIKeyModel | None
         * Find by prefix, verify bcrypt, check not revoked, check not expired
         * Update lastUsedAt on success
    3. Scope system: predefined scopes matching ITAM permissions (read:assets, write:assets, read:licenses, write:licenses, manage:users, admin:itam, etc.)
    4. Scope validation: validate_key returns TokenData with token.scopes; rbac_service.has_permission() checks token scopes
    5. Rate limiting: track usage per key; enforce rateLimit (requests/minute) via middleware or dependency
    6. Expiration: support expires_in_days; auto-expire; notify before expiry (future)
    7. Follow Pitfall 5: design token scoping from day one; store scopes in token record; enforce at API gateway/endpoint
    Create test_api_key.py covering: create/list/revoke, scope enforcement, expiration, rate limiting, bcrypt hashing, prefix lookup.
  </action>
  <verify>
    <automated>pytest backend/tests/test_api_key.py -x -v</automated>
  </verify>
  <done>
    api_key_auth.py has full lifecycle; scopes enforced; expiration works; rate limiting works; bcrypt hashing; tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Create user-facing API token management endpoints</name>
  <files>
    backend/api_key_endpoints.py
    backend/tests/test_api_key.py
  </files>
  <action>
    Create api_key_endpoints.py (user-scoped, gated by get_current_user):
    1. POST /api/api-keys — create new token (body: name, scopes[], expires_in_days?, rate_limit?)
       * Returns: {id, name, key (plaintext, ONLY time), prefix, scopes, expires_at, rate_limit, created_at}
    2. GET /api/api-keys — list user's tokens (exclude keyHash, include prefix, last_used_at, revoked_at)
    3. DELETE /api/api-keys/{key_id} — revoke token
    4. GET /api/api-keys/scopes — list available scopes with descriptions (from rbac_service)
    Admin endpoints (gated by _require_itam_admin):
    5. GET /api/admin/api-keys — list all tokens across tenants (with user info)
    6. DELETE /api/admin/api-keys/{key_id} — admin revoke
    Integrate with authentication: X-API-Key header auth creates TokenData with token scopes for rbac_service.has_permission()
    Add tests for all endpoints.
  </action>
  <verify>
    <automated>pytest backend/tests/test_api_key.py -x -v -k "endpoint"</automated>
  </verify>
  <done>
    User and admin API key endpoints work; X-API-Key auth integrated with RBAC scopes; tests pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API Key Endpoints | User creates/manages own tokens; admin manages all |
| Client → API (X-API-Key) | Token in header; validated server-side; scopes enforced |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-64-19 | Information Disclosure | API key leakage | high | mitigate | Bcrypt hash storage; plaintext shown once; prefix for identification; key rotation support |
| T-64-20 | Elevation of Privilege | Scope escalation | high | mitigate | Scopes validated against rbac_service.default_roles; user cannot grant scopes they don't have |
| T-64-21 | Tampering | Token validation | medium | mitigate | Bcrypt verification; revoked/expired checks; lastUsedAt tracking |
| T-64-22 | Denial of Service | Rate limit bypass | medium | mitigate | Per-key rate limiting enforced at middleware/dependency level; admin override |
| T-64-SC | Tampering | npm/pip/cargo installs | high | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] packages |

</threat_model>

<verification>
Run: pytest backend/tests/test_api_key.py -x -v
All tests pass including lifecycle, scoping, expiration, rate limiting, bcrypt, endpoints
</verification>

<success_criteria>
API token management fully functional: create/list/revoke, scoped permissions, expiration, rate limits, secure storage, X-API-Key auth integrated with RBAC.
</success_criteria>

<output>
Create .planning/milestones/v4.1-phases/64-user-management/64-05-SUMMARY.md when done
</output>