---
phase: 65-core-data-audit-customization
plan: 04
type: execute
wave: 2
depends_on: ["65-01"]
files_modified:
  - prisma/schema.prisma
  - src/server/api/routers/itam/settings.ts
  - src/server/api/schemas/itam/settingsSchemas.ts
  - src/app/(dashboard)/settings/itam/page.tsx
  - src/components/itam/SettingsPanel.tsx
  - src/components/itam/CustomFieldsManager.tsx
autonomous: true
requirements: [ITAM-SET-01, ITAM-SET-02, ITAM-SET-03]

must_haves:
  truths:
    - Global ITAM settings can be viewed and modified via a settings page.
    - Custom fields can be managed (created, edited, deleted) from the UI.
    - Default asset fields can be configured.
  artifacts:
    - prisma/schema.prisma (with ITAMSettings model)
    - src/app/(dashboard)/settings/itam/page.tsx (UI page)
  key_links:
    - Settings page connects to API router for GET/PUT operations.
    - CustomFieldsManager component connects to customFields API from plan 65-01.
---

<objective>
Implement the Global ITAM Settings UI and backend, including custom field management.

Purpose: Provide a central place for administrators to configure ITAM behavior and manage custom fields.
Output: A settings page with tabs for General, Custom Fields, and Default Fields.
</objective>

<execution_context>
@/home/user/enterprise-omni-agent-ai-platform/.claude/gsd-core/workflows/execute-plan.md
@/home/user/enterprise-omni-agent-ai-platform/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend for ITAM Settings</name>
  <files>
    prisma/schema.prisma
    src/server/api/routers/itam/settings.ts
    src/server/api/schemas/itam/settingsSchemas.ts
  </files>
  <action>
    Add an `ITAMSettings` model to `prisma/schema.prisma` (singleton or key-value). Fields: `id`, `key`, `value` (JSONB).
    Create `src/server/api/schemas/itam/settingsSchemas.ts` with Zod schemas for settings keys (e.g., `defaultAssetFields`, `notificationsEnabled`).
    Create `src/server/api/routers/itam/settings.ts` with GET (all settings) and PUT (update setting) endpoints.
  </action>
  <verify>
    <automated>
      gsd_run query prisma.diff 'model ITAMSettings' && \
      grep -q "getItamSettings" src/server/api/routers/itam/settings.ts
    </automated>
  </verify>
  <done>
    `ITAMSettings` model and API endpoints for GET/PUT are implemented.
  </done>
</task>

<task type="auto">
  <name>Task 2: Settings Page UI and Custom Fields Manager</name>
  <files>
    src/app/(dashboard)/settings/itam/page.tsx
    src/components/itam/SettingsPanel.tsx
    src/components/itam/CustomFieldsManager.tsx
  </files>
  <action>
    Create `src/app/(dashboard)/settings/itam/page.tsx` as the main settings page.
    Use a tabbed layout: "General", "Custom Fields", "Default Fields".
    Create `src/components/itam/SettingsPanel.tsx` for the "General" tab to display and edit key-value settings.
    Create `src/components/itam/CustomFieldsManager.tsx` for the "Custom Fields" tab.
    This component should list existing custom fields (from plan 65-01 API) and provide a modal/form to create/edit/delete them.
  </action>
  <verify>
    <automated>
      grep -q "CustomFieldsManager" src/app/\(dashboard\)/settings/itam/page.tsx && \
      grep -q "SettingsPanel" src/app/\(dashboard\)/settings/itam/page.tsx
    </automated>
  </verify>
  <done>
    Settings page with tabs is created. General tab works. Custom Fields tab uses the manager component.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Admin user modifying global ITAM configuration. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-65-07 | Tampering | Settings API | high | mitigate | RBAC: Only users with 'ADMIN' or 'ITAM_ADMIN' role can modify settings. |
| T-65-08 | Elevation of Privilege | Custom Fields Manager | medium | mitigate | Ensure the component does not bypass API validation. |
| T-65-SC | Tampering | npm/pip/cargo installs | high | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
Verify that the settings page loads and displays existing settings. Verify that the Custom Fields tab can create, edit, and delete custom field definitions via the API.
</verification>

<success_criteria>
- Settings page is accessible and functional.
- Custom fields can be fully managed from the UI.
</success_criteria>

<output>
Create `.planning/phases/65-core-data-audit-customization/65-04-SUMMARY.md` when done
</output>