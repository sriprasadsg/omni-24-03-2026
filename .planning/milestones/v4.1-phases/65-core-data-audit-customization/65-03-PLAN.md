---
phase: 65-core-data-audit-customization
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/server/api/routers/itam/csvImport.ts
  - src/server/api/routers/itam/csvExport.ts
  - src/server/api/schemas/itam/csvSchemas.ts
  - src/lib/csv.ts
autonomous: true
requirements: [ITAM-DAT-03]

must_haves:
  truths:
    - Users can import assets from a CSV file.
    - Users can export assets to a CSV file.
  artifacts:
    - src/server/api/routers/itam/csvImport.ts
    - src/server/api/routers/itam/csvExport.ts
  key_links:
    - Import route handles file uploads and maps CSV columns to asset fields.
    - Export route streams or returns CSV with all asset fields.
---

<objective>
Implement CSV import/export functionality for ITAM assets.

Purpose: Enables bulk data transfer to and from the ITAM system.
Output: API endpoints for importing and exporting assets in CSV format.
</objective>

<execution_context>
@/home/user/enterprise-omni-agent-ai-platform/.claude/gsd-core/workflows/execute-plan.md
@/home/user/enterprise-omni-agent-ai-platform/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CSV Export API</name>
  <files>
    src/server/api/routers/itam/csvExport.ts
    src/server/api/schemas/itam/csvSchemas.ts
  </files>
  <action>
    Create `src/server/api/routers/itam/csvExport.ts` with an endpoint to export assets to CSV.
    Use a library like `csv-stringify` for generation or a simple string-based approach.
    Include a header row for asset fields. Include the option to filter by asset type.
  </action>
  <verify>
    <automated>grep -q "exportAssetsCsv" src/server/api/routers/itam/csvExport.ts</automated>
  </verify>
  <done>
    CSV export endpoint for assets is implemented.
  </done>
</task>

<task type="auto">
  <name>Task 2: CSV Import API</name>
  <files>
    src/server/api/routers/itam/csvImport.ts
    src/lib/csv.ts
  </files>
  <action>
    Create `src/lib/csv.ts` for parsing CSV input (using `csv-parse` or similar).
    Create `src/server/api/routers/itam/csvImport.ts` with an endpoint that accepts a CSV file, parses it, and creates assets.
    For each row, validate (with Zod) and create an asset. Return a summary of successful/unsuccessful imports.
  </action>
  <verify>
    <automated>
      grep -q "importAssetsCsv" src/server/api/routers/itam/csvImport.ts && \
      grep -q "parseCsv" src/lib/csv.ts
    </automated>
  </verify>
  <done>
    CSV import endpoint for assets is implemented, with row-level validation.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Untrusted CSV file upload containing arbitrary asset data and potentially malicious content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-65-05 | Tampering | CSV import | high | mitigate | Validate every row with Zod schemas. Reject rows with invalid data; prevent CSV formula injection by escaping cells starting with '=', '+', '-', '@'. |
| T-65-06 | Denial of Service | CSV import | medium | mitigate | Limit CSV upload size (e.g., 5MB) and row count. |
| T-65-SC | Tampering | npm/pip/cargo installs | high | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
Verify that the CSV export endpoint returns a well-formed CSV with asset data. Verify that the CSV import endpoint creates assets from valid rows and rejects invalid rows.
</verification>

<success_criteria>
- Export endpoint returns CSV data.
- Import endpoint creates assets from CSV and returns a summary.
</success_criteria>

<output>
Create `.planning/phases/65-core-data-audit-customization/65-03-SUMMARY.md` when done
</output>