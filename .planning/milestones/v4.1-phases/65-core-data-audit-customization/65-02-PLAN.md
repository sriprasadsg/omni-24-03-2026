---
phase: 65-core-data-audit-customization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/server/api/routers/itam/auditLog.ts
  - src/server/api/schemas/itam/auditLogSchemas.ts
  - src/server/db/migrations/*
autonomous: true
requirements: [ITAM-DAT-02]

must_haves:
  truths:
    - An audit trail of changes to assets is recorded.
    - Audit logs are accessible via an API.
  artifacts:
    - prisma/schema.prisma (with AuditLog model)
    - src/server/api/routers/itam/auditLog.ts
  key_links:
    - AuditLog model is correctly structured to capture relevant change data.
    - A service or hook exists to generate audit log entries.
---

<objective>
Implement the backend for an audit log to track changes to ITAM assets.

Purpose: Provides a complete history of changes for security, compliance, and debugging.
Output: A database schema and API for recording and retrieving audit log entries.
</objective>

<execution_context>
@/home/user/enterprise-omni-agent-ai-platform/.claude/gsd-core/workflows/execute-plan.md
@/home/user/enterprise-omni-agent-ai-platform/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Database Schema for Audit Log</name>
  <files>prisma/schema.prisma</files>
  <action>
    Add an `AuditLog` model to `prisma/schema.prisma`.
    It should include `id`, `entityType` (e.g., 'ASSET'), `entityId`, `action` (e.g., 'CREATE', 'UPDATE', 'DELETE'), `details` (JSONB to store old and new values), `actorId` (linking to the User model), and `timestamp`.
  </action>
  <verify>
    <automated>gsd_run query prisma.diff 'model AuditLog'</automated>
  </verify>
  <done>
    `AuditLog` model is defined in `prisma/schema.prisma`.
  </done>
</task>

<task type="auto">
  <name>Task 2: API and Service for Audit Log</name>
  <files>
    src/server/api/routers/itam/auditLog.ts
    src/server/api/schemas/itam/auditLogSchemas.ts
  </files>
  <action>
    Create `src/server/api/schemas/itam/auditLogSchemas.ts` with Zod schemas for audit log queries.
    Create `src/server/api/routers/itam/auditLog.ts` with an endpoint to retrieve audit logs for a specific entity (e.g., an asset) with pagination.
    This plan does not include the logic for creating log entries; it only provides the API to query them. That logic will be added to the relevant mutation endpoints in other plans.
  </action>
  <verify>
    <automated>grep -q "getAuditLogs" src/server/api/routers/itam/auditLog.ts</automated>
  </verify>
  <done>
    An API endpoint to query audit logs is implemented.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | User querying for audit log data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-65-03 | Information Disclosure | Audit log API | high | mitigate | Use RBAC to ensure users can only view logs for entities they are authorized to see. |
| T-65-04 | Tampering | Audit log entries | critical | mitigate | Make the audit log append-only. Do not expose 'update' or 'delete' API endpoints for audit logs. Database-level permissions could also be used. |
| T-65-SC | Tampering | npm/pip/cargo installs | high | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
Verify that the audit log API can be queried to return (initially empty) logs for a given entity.
</verification>

<success_criteria>
- A new `AuditLog` model exists in `prisma/schema.prisma`.
- An API endpoint exists to query and retrieve audit logs.
</success_criteria>

<output>
Create `.planning/phases/65-core-data-audit-customization/65-02-SUMMARY.md` when done
</output>
