# Plan 60-01: Software License CRUD

**Goal:** Implement software license creation, assignment, and expiry tracking.

**Tasks:**
- Create `LicenseCreate`/`LicenseUpdate` Pydantic models.
- Implement `/api/licenses` CRUD endpoints in `license_endpoints.py`.
- Implement license seat-tracking logic in `license_service.py` (assign/reclaim).
- Add tests in `tests/test_license.py`.
