# Plan 60-03: Component Attachment

**Goal:** Implement component attachment logic to parent assets.

**Tasks:**
- Add `Component` model referencing `parentAssetId`.
- Implement endpoints to attach/detach components in `component_endpoints.py`.
- Update `AssetDetail` schema to list attached components.
- Add tests in `tests/test_components.py`.
