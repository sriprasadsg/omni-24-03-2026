# Plan 60-02: Consumables Quantity-Aware Checkout

**Goal:** Implement accessories/consumables with quantity-aware checkout logic.

**Tasks:**
- Add `ConsumableCreate` model with `quantity` field.
- Implement `/api/consumables` endpoints in `consumable_endpoints.py`.
- Logic: decrement available quantity on checkout, block if requested > available.
- Add tests in `tests/test_consumables.py`.
