---
phase: 47-agent-scoped-geo-security-detectors
plan: 03
type: execute
wave: 2
depends_on: ["47-01", "47-02"]
files_modified:
  - backend/agent_heartbeat_endpoints.py
  - backend/tests/test_agent_heartbeat_geo_security.py
autonomous: true
requirements: [GSEC-02, GSEC-03]
must_haves:
  truths:
    - "On a heartbeat, run_geo_security_detectors is invoked after record_location_change with the RAW existing_agent + newly-resolved geo"
    - "Each returned alert payload is passed to persist_security_alert (the reused fan-out, not a parallel channel)"
    - "When detectors return no payloads, no alert is persisted"
    - "A detector exception is caught and logged; it never fails the heartbeat response (D-04 alert-only)"
    - "agent_heartbeat_endpoints.py stays under 500 lines"
  artifacts:
    - path: "backend/agent_heartbeat_endpoints.py"
      provides: "Toggle-gated geo-security detector call-through in the heartbeat handler"
      contains: "run_geo_security_detectors"
    - path: "backend/tests/test_agent_heartbeat_geo_security.py"
      provides: "Wiring test proving persist_security_alert fires from returned payloads"
  key_links:
    - from: "backend/agent_heartbeat_endpoints.py"
      to: "backend/geo_security_service.py"
      via: "run_geo_security_detectors called after record_location_change"
      pattern: "run_geo_security_detectors"
    - from: "backend/agent_heartbeat_endpoints.py"
      to: "backend/ueba_service.py"
      via: "persist_security_alert(db, **payload) for each returned alert"
      pattern: "persist_security_alert"
  prohibitions:
    - "MUST NOT write directly to db.security_alerts — only via persist_security_alert"
    - "MUST NOT block/quarantine/reject the connection on a violation (D-04 alert-only)"
    - "MUST NOT use existing_agent.locationConfirmed as the previous check-in"
    - "MUST NOT push agent_heartbeat_endpoints.py to or past 500 lines"
---

<objective>
Wire the geo-security detectors into the live heartbeat path. After Phase 46's `record_location_change` call in `agent_heartbeat_endpoints.py`, invoke `run_geo_security_detectors(...)` (from Plan 47-02) against the raw previous-beat state and the newly resolved geo, then fan each returned alert payload out through `persist_security_alert` (fixed in Plan 47-01). Alert-only: no connection side effects (D-04).

Purpose: This is the single integration point that turns the pure detector core into real GSEC-02/GSEC-03 alerts reaching the existing notification fan-out.
Output: A toggle-gated, exception-safe detector call-through in the heartbeat handler plus a wiring regression test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-agent-scoped-geo-security-detectors/47-RESEARCH.md
@.planning/phases/47-agent-scoped-geo-security-detectors/47-02-SUMMARY.md
@backend/agent_heartbeat_endpoints.py
</context>

<artifacts_produced>
- New import + call-through in `backend/agent_heartbeat_endpoints.py`: `from geo_security_service import run_geo_security_detectors` and `from ueba_service import persist_security_alert`, invoked once after `record_location_change`.
- `backend/tests/test_agent_heartbeat_geo_security.py` — new wiring test module.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Write failing heartbeat-wiring test</name>
  <files>backend/tests/test_agent_heartbeat_geo_security.py</files>
  <read_first>
    - backend/tests/test_rust_heartbeat_parity.py (existing TestClient + FastAPI dependency-override / AsyncMock db pattern for the heartbeat route — clone its setup).
    - backend/agent_heartbeat_endpoints.py lines 43-165 (handler signature, `verify_agent_key` dependency, `existing_agent` fetch, the `record_location_change` block to insert after).
  </read_first>
  <action>
    Create backend/tests/test_agent_heartbeat_geo_security.py. Drive the heartbeat route (or the handler) with `verify_agent_key`/`get_database` overridden to an AsyncMock db and an `existing_agent` carrying a prior `geo`/`lastSeen`. Monkeypatch `geo_security_service.run_geo_security_detectors` to return (a) one alert payload, and (b) an empty list, in two separate tests; monkeypatch `agent_heartbeat_endpoints.persist_security_alert` (or the ueba symbol it imports) with an AsyncMock. Assert: when a payload is returned, persist_security_alert is awaited once with those kwargs; when [] is returned, it is not called. Add a third test where run_geo_security_detectors raises — assert the heartbeat still returns success and persist is not called. These MUST fail before Task 2 (wiring absent) — RED.
  </action>
  <verify>
    <automated>backend/venv/bin/python -m pytest backend/tests/test_agent_heartbeat_geo_security.py -q</automated>
  </verify>
  <acceptance_criteria>
    Test file exists and fails before Task 2 because the handler does not yet call run_geo_security_detectors.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wire the toggle-gated detector call-through after record_location_change</name>
  <files>backend/agent_heartbeat_endpoints.py</files>
  <read_first>
    - backend/agent_heartbeat_endpoints.py lines 125-165 (the enrichment + `record_location_change` block — insert the new call-through immediately after it, inside the same `if public_ip:` region so it only runs when geo was resolved).
    - backend/agent_heartbeat_endpoints.py lines 349-360 (existing `persist_security_alert` call-site shape to mirror kwargs).
  </read_first>
  <action>
    Add module imports `from geo_security_service import run_geo_security_detectors` and `from ueba_service import persist_security_alert` (top of file with the other imports). After the `record_location_change` try/except block, add a try/except-wrapped call-through: compute `now = datetime.now(timezone.utc)`, call `alerts = await run_geo_security_detectors(db, existing_agent, agent_id, _hb_tenant_id, geo, now)`, then `for a in alerts: await persist_security_alert(db, **a)`. Wrap in try/except logging a warning on failure (mirror the record_location_change guard) so a detector error never breaks the heartbeat (D-04 alert-only — do not reject/quarantine). Use `existing_agent` (the PRE-update doc fetched at line 63) as the previous state — NOT locationConfirmed. Keep the handler minimal (all logic is in geo_security_service); confirm `wc -l` stays under 500. If adding the imports/call would breach 500, extract the ~4-line call-through into a one-line helper in geo_security_service.py and call that instead.
  </action>
  <verify>
    <automated>backend/venv/bin/python -m pytest backend/tests/test_agent_heartbeat_geo_security.py backend/tests/test_geo_security_service.py -q</automated>
  </verify>
  <acceptance_criteria>
    Wiring tests pass; persist_security_alert fires only when payloads are returned; heartbeat survives a detector exception; `wc -l backend/agent_heartbeat_endpoints.py` < 500.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent -> heartbeat handler | Authenticated via verify_agent_key; agent self-reports publicIp/meta |
| handler -> alert fan-out | Detector payloads cross into db.security_alerts + streaming + blockchain audit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-03-R | Repudiation | detector call-through | mitigate | Reuse persist_security_alert exclusively (single audited fan-out); wiring test asserts it actually fires |
| T-47-03-D | Denial of Service | heartbeat hot path | mitigate | Detector logic is pure/cheap; try/except means a detector fault degrades to a logged warning, never a failed heartbeat or connection change (D-04) |
| T-47-03-E | Elevation of Privilege | connection handling | mitigate | Alert-only by construction — the call-through performs no reject/quarantine/block; verified by prohibition + code review |
| T-47-03-SC | Tampering | package installs | accept | No new packages introduced |
</threat_model>

<verification>
- `backend/venv/bin/python -m pytest backend/tests/test_agent_heartbeat_geo_security.py backend/tests/test_geo_security_service.py -q` green.
- `grep -c "db.security_alerts.insert" backend/agent_heartbeat_endpoints.py` unchanged (no new direct alert writes; fan-out only via persist_security_alert).
- `wc -l backend/agent_heartbeat_endpoints.py` < 500.
- Full suite: no new failures beyond baseline (1343 pass / 3 pre-existing).
</verification>

<success_criteria>
A heartbeat that trips impossible-travel or geo-fence raises an alert through the existing fan-out; a clean heartbeat raises none; a detector error never disrupts the agent's connection.
</success_criteria>

<output>
Create `.planning/phases/47-agent-scoped-geo-security-detectors/47-03-SUMMARY.md` when done.
</output>
