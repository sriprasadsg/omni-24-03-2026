---
phase: 47-agent-scoped-geo-security-detectors
plan: 06
type: execute
wave: 3
depends_on: ["47-04"]
files_modified:
  - services/apiService.ts
  - components/SecuritySettingsDashboard.tsx
  - App.tsx
  - components/Sidebar.tsx
autonomous: true
requirements: [GSEC-03]
must_haves:
  truths:
    - "apiService exposes getGeoSecuritySettings and setGeoSecuritySettings hitting /settings/geo-security"
    - "A new admin Security settings panel lets an admin toggle both detectors and edit the allowed-country-code allowlist"
    - "The panel loads current settings on mount and persists changes via PATCH"
    - "The panel is reachable via a Sidebar nav entry and an App.tsx view case"
  artifacts:
    - path: "services/apiService.ts"
      provides: "getGeoSecuritySettings / setGeoSecuritySettings clients"
      contains: "settings/geo-security"
    - path: "components/SecuritySettingsDashboard.tsx"
      provides: "Admin geo-security settings panel (toggles + allowlist editor)"
    - path: "App.tsx"
      provides: "Lazy import + view case for the panel"
    - path: "components/Sidebar.tsx"
      provides: "Nav entry routing to the panel"
  key_links:
    - from: "components/SecuritySettingsDashboard.tsx"
      to: "services/apiService.ts"
      via: "getGeoSecuritySettings / setGeoSecuritySettings"
      pattern: "GeoSecuritySettings"
    - from: "services/apiService.ts"
      to: "backend/geo_security_endpoints.py"
      via: "GET/PATCH /api/settings/geo-security"
      pattern: "settings/geo-security"
  prohibitions:
    - "MUST NOT put geo-security config in the Phase 46 PrivacyDashboard — a distinct Security settings surface per D-06"
    - "MUST NOT let non-admins reach the mutation (nav permission 'manage:settings' + backend admin gate)"
    - "MUST NOT push any edited file to or past 500 lines (extract a subcomponent if needed)"
---

<objective>
Build the admin-gated Security settings panel (D-06) that drives the Plan 47-04 config endpoints: toggle impossible-travel and geo-fence detection and manage the per-tenant allowed-country-code allowlist. Kept distinct from Phase 46's PrivacyDashboard (security config separate from privacy).

Purpose: Give tenant admins the UI to configure GSEC-03 allowed regions and turn each detector on/off, completing the phase's user-facing config surface.
Output: `apiService` client functions, a new `SecuritySettingsDashboard.tsx`, and its registration in `App.tsx` + `Sidebar.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-agent-scoped-geo-security-detectors/47-RESEARCH.md
@.planning/phases/47-agent-scoped-geo-security-detectors/47-04-SUMMARY.md
@components/PrivacyDashboard.tsx
@services/apiService.ts
</context>

<artifacts_produced>
- `services/apiService.ts`: `GeoSecuritySettings` type + `getGeoSecuritySettings()` and `setGeoSecuritySettings(settings)` (clone of getAgentLocationTracking/setAgentLocationTracking, path `/settings/geo-security`).
- `components/SecuritySettingsDashboard.tsx`: new admin panel (two toggles + allowlist add/remove editor).
- `App.tsx`: `const SecuritySettingsDashboard = lazy(...)` + `case 'geoSecurity':` render.
- `components/Sidebar.tsx`: nav item `{ view: 'geoSecurity', label: 'Geo Security', permission: 'manage:settings' }`.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Add getGeoSecuritySettings / setGeoSecuritySettings API clients</name>
  <files>services/apiService.ts</files>
  <read_first>
    - services/apiService.ts lines 4633-4660 (getAgentLocationTracking / setAgentLocationTracking — exact clone target for shape, authFetch usage, and error handling).
    - 47-04-SUMMARY.md for the settings payload keys (impossible_travel_enabled, geo_fence_enabled, allowed_country_codes).
  </read_first>
  <action>
    Add a `GeoSecuritySettings` interface (impossible_travel_enabled: boolean; geo_fence_enabled: boolean; allowed_country_codes: string[]) and two functions cloning getAgentLocationTracking/setAgentLocationTracking: `getGeoSecuritySettings()` (GET `/settings/geo-security`, returns resolved settings, graceful fallback to defaults) and `setGeoSecuritySettings(settings: Partial<GeoSecuritySettings>)` (PATCH with JSON body, throws on non-ok mirroring the existing error pattern). Place them adjacent to the location-tracking helpers.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    tsc passes; both functions target `/settings/geo-security` and are exported.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Build the SecuritySettingsDashboard panel</name>
  <files>components/SecuritySettingsDashboard.tsx</files>
  <read_first>
    - components/PrivacyDashboard.tsx lines 1-100 (toggle-panel + section layout to clone; use its styling conventions).
    - services/apiService.ts (the new getGeoSecuritySettings/setGeoSecuritySettings from Task 1).
  </read_first>
  <action>
    Create components/SecuritySettingsDashboard.tsx cloning PrivacyDashboard's toggle-section layout. On mount call getGeoSecuritySettings and populate state. Render: a toggle for impossible_travel_enabled, a toggle for geo_fence_enabled, and an allowed-country-codes editor (input to add an ISO alpha-2 code, uppercased on entry, plus a removable chip list). Persist changes via setGeoSecuritySettings (on toggle change and on add/remove). Show a success/error indication consistent with existing dashboards (toast if the project's toast util is used elsewhere in this component's siblings). Keep the file under 500 lines — if the allowlist editor grows large, extract it to a sibling subcomponent file. Label the geo-fence section clearly as alert-only (no blocking) per D-04.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    Build clean; panel loads settings, toggles persist, allowlist add/remove persists; file < 500 lines.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Register the panel in App.tsx and Sidebar.tsx</name>
  <files>App.tsx, components/Sidebar.tsx</files>
  <read_first>
    - App.tsx line 164 (`const PrivacyDashboard = lazy(...)`) and line 1909 (`case 'privacy': return <ErrorBoundary ...><PrivacyDashboard /></ErrorBoundary>;`) — clone both patterns.
    - components/Sidebar.tsx lines 364-365 and 406-411 (nav-item shape with view/label/icon/permission; place the new item in a Settings/Security group with `permission: 'manage:settings'`).
  </read_first>
  <action>
    In App.tsx add `const SecuritySettingsDashboard = lazy(() => import('./components/SecuritySettingsDashboard').then(m => ({ default: m.SecuritySettingsDashboard })));` (match the module's actual export style) and a `case 'geoSecurity': return <ErrorBoundary name="SecuritySettingsDashboard"><SecuritySettingsDashboard /></ErrorBoundary>;`. In Sidebar.tsx add a nav item `{ view: 'geoSecurity', label: 'Geo Security', icon: <ShieldLockIcon size={20} />, permission: 'manage:settings' }` in the appropriate settings/security group. Ensure the `view` string matches the App.tsx case exactly.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    Build clean; the panel is reachable via the new nav entry; `grep -c "geoSecurity" App.tsx components/Sidebar.tsx` shows the wiring in both files.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser -> settings panel -> API | Admin edits per-tenant detector config |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-06-E | Elevation of Privilege | Security settings nav/mutation | mitigate | Nav gated by `manage:settings` permission; backend PATCH enforces `_require_admin` (defense in depth — client gating is not the control) |
| T-47-06-T | Tampering | allowlist input | mitigate | Codes uppercased/normalized client-side; backend re-validates ISO 3166 alpha-2 (Plan 47-04) as the authoritative check |
| T-47-06-SC | Tampering | package installs | accept | No new npm packages (existing React/Tailwind/icons only) |
</threat_model>

<verification>
- `npx tsc --noEmit && npm run build` clean.
- `grep -c "geoSecurity" App.tsx components/Sidebar.tsx` confirms registration in both.
- Manual UAT (per 47-VALIDATION.md Manual-Only gate): open the Security settings panel as an admin, toggle a detector, add/remove a country, confirm persistence and tenant isolation. Surfaced at `/gsd-verify-work`.
</verification>

<success_criteria>
A tenant admin can reach a dedicated Security settings panel and configure the detectors + allowed-region allowlist, persisted through the admin-gated endpoints — satisfying D-06 and the config half of GSEC-03.
</success_criteria>

<output>
Create `.planning/phases/47-agent-scoped-geo-security-detectors/47-06-SUMMARY.md` when done.
</output>
