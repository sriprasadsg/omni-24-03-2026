# Deferred Items — Phase 47

## 1. `backend/ueba_service.py` exceeds CLAUDE.md's 500-line cap (pre-existing)

- **Status:** resolved
- **Resolved note:** 2026-08-26 — split per this item's own recommendation: `_persist_alert`/`persist_security_alert` moved to `backend/ueba_alert_persistence_service.py` (mirrors `agent_heartbeat_alerts_service.py`), and the rule-based analysis engine (`_RULES`, `LoginEvent`/`DataAccessEvent`/`ShadowAIEvent`, `_parse_dt`/`_haversine_km`, `analyze_login`/`analyze_data_access`) moved to `backend/ueba_analysis.py` — `ueba_service.py` re-exports both via plain module-level imports so every existing external import (`from ueba_service import persist_security_alert`/`_haversine_km`/`analyze_login, LoginEvent`) and module-qualified test reference keeps resolving to the same objects unchanged. `ueba_service.py` is now 412 lines (was 690). One test (`test_auto_ban_block_still_present_and_unmodified`) did raw source-inspection on `ueba_service` specifically; updated to inspect `ueba_analysis` (where the code now actually lives) rather than weakened. Full backend suite: 2474 passed / 34 skipped / 4 failed (all 4 pre-existing/unrelated, `test_distributed_lock.py`). Also discovered but NOT touched (unrelated, out of scope): `ueba_endpoints.py`/`ueba_engine.py` are a separate, apparently-dead legacy UEBA implementation registered at the *same* `/api/ueba` prefix with colliding routes (`GET /risk-scores`, `GET /alerts`) — a pre-existing routing collision, flagged for a future cleanup pass.
- **Found during:** 47-01, Task 2.
- **Detail:** `ueba_service.py` was already 572 lines before this plan touched it (confirmed in 47-RESEARCH.md's Sources section: "read in full (572 lines)"). Plan 47-01's minimal, non-breaking fix (`persist_security_alert = _persist_alert` plus an explanatory comment) added 12 lines, bringing it to 584.
- **Why not fixed here:** 47-01's `<prohibitions>` explicitly scope this plan to the alias only ("MUST NOT rename `_persist_alert`... MUST NOT write a second/parallel alert-insert path"). Splitting `ueba_service.py` to get under the 500-line cap is an unrelated structural refactor, out of scope for a Wave-0 prerequisite bugfix plan, and risks destabilizing the five now-reactivated alert call sites this plan intentionally leaves untouched.
- **Recommendation:** If a future phase touches `ueba_service.py` again, consider splitting the alert-persistence helper (`_persist_alert`/`persist_security_alert`) into a small `alert_persistence_service.py` sibling module, mirroring the `agent_heartbeat_alerts_service.py` split Phase 46 already did for a similar reason.
