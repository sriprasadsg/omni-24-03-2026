---
phase: 47-agent-scoped-geo-security-detectors
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - types.ts
  - components/AgentList.tsx
autonomous: true
requirements: [GSEC-01]
must_haves:
  truths:
    - "The GeoLocation TS interface exposes vpn_heuristic?: boolean and asn (optional) fields"
    - "AgentList.tsx renders an amber 'likely VPN/hosting' badge in the agent-card Location row when agent.geo.vpn_heuristic === true"
    - "The badge is absent when vpn_heuristic is false or undefined"
    - "The label reads 'likely VPN/hosting' (heuristic) and never 'detected' or an authoritative claim"
  artifacts:
    - path: "types.ts"
      provides: "GeoLocation.vpn_heuristic + asn fields"
      contains: "vpn_heuristic"
    - path: "components/AgentList.tsx"
      provides: "Amber heuristic VPN/hosting badge on the agent card"
      contains: "likely VPN/hosting"
  key_links:
    - from: "components/AgentList.tsx"
      to: "types.ts"
      via: "reads agent.geo.vpn_heuristic (typed by GeoLocation)"
      pattern: "vpn_heuristic"
  prohibitions:
    - "MUST NOT label the flag 'detected' or as authoritative — heuristic wording only (GSEC-01)"
    - "MUST NOT render the badge when vpn_heuristic is false/absent (=== true only, matching the 3-valued flag)"
    - "MUST NOT add a new backend call — read the already-computed agent.geo.vpn_heuristic"
---

<objective>
Surface the heuristic VPN/hosting flag (GSEC-01) on the live agent card. Phase 46 already computes and stores `agent.geo.vpn_heuristic`, but only `AgentLocationHistory.tsx` renders it; the `GeoLocation` TS interface and the `AgentList.tsx` agent card do not yet expose or show it. This plan closes that gap by cloning the existing amber badge, always labeled as a heuristic.

Purpose: Make the "likely VPN/hosting" signal visible where admins actually look (the agent card), so the D-02 impossible-travel suppression is explainable and never implies an authoritative "detected" claim.
Output: `GeoLocation` interface extended with `vpn_heuristic?`/`asn?`; the amber badge rendered on the agent-card Location row.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-agent-scoped-geo-security-detectors/47-RESEARCH.md
@components/AgentLocationHistory.tsx
@components/AgentList.tsx
@types.ts
</context>

<artifacts_produced>
- `types.ts`: `GeoLocation` interface gains `vpn_heuristic?: boolean` and `asn?` (shape matching what `agent_asn_service.lookup()` stores — e.g. `{ number?: number | string; org?: string }`, optional).
- `components/AgentList.tsx`: amber heuristic badge in the Location row (lines ~221-229 region).
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Extend the GeoLocation interface</name>
  <files>types.ts</files>
  <read_first>
    - types.ts lines 686-700 (the `Agent.geo?: GeoLocation` field and the `GeoLocation` interface body — add the new optional fields without disturbing existing ones).
    - components/AgentLocationHistory.tsx around the badge (how vpn_heuristic is typed/consumed there) for field-name consistency.
  </read_first>
  <action>
    Add `vpn_heuristic?: boolean;` and an optional `asn?` field to the `GeoLocation` interface in types.ts. Match the field names already produced by the backend (`vpn_heuristic`, `asn`) so the card reads them directly. Keep both optional (the flag is 3-valued: true/false/absent). Do not change unrelated fields.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    tsc passes; `agent.geo?.vpn_heuristic` type-checks without `any`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Render the amber 'likely VPN/hosting' heuristic badge on the agent card</name>
  <files>components/AgentList.tsx</files>
  <read_first>
    - components/AgentLocationHistory.tsx lines 132-137 (exact amber badge markup + WifiIcon to clone; note the `=== true` guard and the heuristic wording).
    - components/AgentList.tsx lines 221-229 (the Location row where `formatGeo(agent.geo)` and the country flag render — add the badge adjacent to the location text).
  </read_first>
  <action>
    In the AgentList.tsx agent-card Location row, render the identical amber badge from AgentLocationHistory.tsx (same Tailwind classes: `bg-amber-100 text-amber-800 dark:bg-amber-900/50 dark:text-amber-300` pill, WifiIcon, uppercase tracking) guarded by `agent.geo?.vpn_heuristic === true`. The label text stays exactly the heuristic wording used in the source component and must never present the flag as an authoritative "detected" result (GSEC-01). Import WifiIcon the same way AgentLocationHistory does. Do not add any new API call — the flag is already on `agent.geo`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    Build clean; the badge renders only when vpn_heuristic === true; wording is heuristic ("likely VPN/hosting"), never "detected". Confirm with `grep -c "likely VPN/hosting" components/AgentList.tsx` >= 1 and `grep -c "detected" components/AgentList.tsx` shows no new authoritative-claim string added for this flag.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend geo -> UI | Read-only render of an already-computed heuristic flag |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-05-I | Information Disclosure / misrepresentation | VPN badge label | mitigate | Label is explicitly heuristic ("likely VPN/hosting"), never "detected" — avoids implying authoritative classification the free X4BNet heuristic can't support (GSEC-01, legal/UX risk) |
| T-47-05-SC | Tampering | package installs | accept | No new npm packages (WifiIcon + Tailwind already present) |
</threat_model>

<verification>
- `npx tsc --noEmit && npm run build` clean.
- Manual UAT (per 47-VALIDATION.md Manual-Only gate): load an agent card whose geo has vpn_heuristic true and confirm the amber "likely VPN/hosting" badge renders; confirm no badge for a non-VPN agent. Surfaced at `/gsd-verify-work`.
</verification>

<success_criteria>
The heuristic VPN/hosting flag is visible on the agent card, correctly typed, rendered only when true, and worded as a heuristic — satisfying GSEC-01.
</success_criteria>

<output>
Create `.planning/phases/47-agent-scoped-geo-security-detectors/47-05-SUMMARY.md` when done.
</output>
