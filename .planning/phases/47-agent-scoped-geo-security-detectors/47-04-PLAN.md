---
phase: 47-agent-scoped-geo-security-detectors
plan: 04
type: execute
wave: 2
depends_on: ["47-02"]
files_modified:
  - backend/geo_security_endpoints.py
  - backend/router_registry.py
  - backend/tests/test_geo_security_endpoints.py
autonomous: true
requirements: [GSEC-02, GSEC-03]
must_haves:
  truths:
    - "GET /api/settings/geo-security returns the tenant-resolved settings (impossible_travel_enabled, geo_fence_enabled, allowed_country_codes)"
    - "PATCH /api/settings/geo-security persists the per-tenant system_settings doc (type geo_security_detectors)"
    - "A non-admin PATCH returns 403 (admin-gated per D-06)"
    - "allowed_country_codes is validated as a list of uppercase ISO 3166 alpha-2 codes; malformed input is rejected"
    - "The new router is registered so the routes resolve in the running app"
  artifacts:
    - path: "backend/geo_security_endpoints.py"
      provides: "Admin-gated GET/PATCH /api/settings/geo-security"
      exports: ["router"]
    - path: "backend/tests/test_geo_security_endpoints.py"
      provides: "Admin-gate, validation, and tenant-scoping tests"
  key_links:
    - from: "backend/geo_security_endpoints.py"
      to: "backend/geo_security_service.py"
      via: "get_geo_security_settings for the GET resolution"
      pattern: "get_geo_security_settings"
    - from: "backend/router_registry.py"
      to: "backend/geo_security_endpoints.py"
      via: "_load(app, \"geo_security_endpoints\", \"router\")"
      pattern: "geo_security_endpoints"
  prohibitions:
    - "MUST NOT fold these routes into agent_location_history_endpoints.py — separate settings surface per D-06"
    - "MUST NOT accept unvalidated country codes (reject non-2-letter / non-uppercase-normalizable input at the boundary)"
    - "MUST NOT allow non-admin mutation"
---

<objective>
Add the admin-gated per-tenant config surface for the geo-security detectors (D-06): GET/PATCH `/api/settings/geo-security` storing a `system_settings` doc of `type: "geo_security_detectors"`, cloning the proven `agent_location_history_endpoints.py` toggle pattern. This is the backend the Plan 47-06 Security settings panel calls.

Purpose: Let a tenant admin toggle the two detectors and manage the country-code allowlist that GSEC-03 checks against, using the established tenant->global->default `system_settings` convention.
Output: `geo_security_endpoints.py` (registered in `router_registry.py`) plus endpoint tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-agent-scoped-geo-security-detectors/47-RESEARCH.md
@.planning/phases/47-agent-scoped-geo-security-detectors/47-02-SUMMARY.md
@backend/agent_location_history_endpoints.py
</context>

<artifacts_produced>
New symbols in `backend/geo_security_endpoints.py`:
- `router` (APIRouter, no prefix — full paths on each route).
- `_SETTINGS_ADMIN_ROLES`, `_require_admin` (cloned from agent_location_history_endpoints.py).
- `GeoSecuritySettingsUpdate` Pydantic body (impossible_travel_enabled: bool | None, geo_fence_enabled: bool | None, allowed_country_codes: list[constr uppercase 2-letter] | None).
- `GET /api/settings/geo-security`, `PATCH /api/settings/geo-security`.
- New `_load(app, "geo_security_endpoints", "router")` line in `router_registry.py`.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Write failing endpoint tests (admin gate, validation, tenant scope)</name>
  <files>backend/tests/test_geo_security_endpoints.py</files>
  <read_first>
    - backend/agent_location_history_endpoints.py lines 26-129 (the admin-role set, `_require_admin`, and the GET/PATCH toggle handlers + system_settings upsert to clone).
    - backend/tests/test_geo_security_service.py (from 47-02) for the settings-doc shape and defaults.
  </read_first>
  <action>
    Create backend/tests/test_geo_security_endpoints.py using the project's endpoint-test convention (TestClient + dependency_overrides for get_current_user and get_database with an AsyncMock db). Tests: admin_gate (non-admin PATCH -> 403); get_returns_defaults (GET with no stored doc returns the hardcoded defaults); patch_persists (PATCH by an admin upserts a system_settings doc with type "geo_security_detectors" scoped to the tenant); validation_rejects_bad_country (PATCH with allowed_country_codes ["USA","x"] -> 422/400); validation_normalizes_case (["us","gb"] accepted, stored uppercase). RED before Task 2.
  </action>
  <verify>
    <automated>backend/venv/bin/python -m pytest backend/tests/test_geo_security_endpoints.py -q</automated>
  </verify>
  <acceptance_criteria>
    Test file exists and fails before Task 2 (module/route absent).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement geo_security_endpoints.py and register the router</name>
  <files>backend/geo_security_endpoints.py, backend/router_registry.py</files>
  <read_first>
    - backend/agent_location_history_endpoints.py in full (exact clone target: imports, `_SETTINGS_ADMIN_ROLES`, `_require_admin`, GET/PATCH handlers, the `raw = db._db if hasattr(db,"_db") else db` unwrap and the tenant vs global `system_settings.update_one` upsert branches).
    - backend/router_registry.py lines 168-175 (register the new `_load` line right after `agent_location_history_endpoints`).
  </read_first>
  <action>
    Create backend/geo_security_endpoints.py cloning the toggle endpoint pair. Define `GeoSecuritySettingsUpdate(BaseModel)` with optional `impossible_travel_enabled: bool`, `geo_fence_enabled: bool`, and `allowed_country_codes: list[str]` where each code is validated to ISO 3166 alpha-2 (uppercase 2-letter; use a Pydantic field/validator, e.g. constrained pattern `^[A-Z]{2}$` after uppercasing) — reject malformed codes at the boundary (V5 input validation). GET calls `get_geo_security_settings(db, tenant_id)` (from 47-02) and returns the resolved dict. PATCH calls `_require_admin(current_user)` then upserts the `type: "geo_security_detectors"` system_settings doc for the tenant (mirror the tenant vs global `$exists` branches), setting only the provided fields. Use the raw db-handle unwrap so `system_settings` (not exempt) isn't double tenant-injected inconsistently — follow the location-history file's exact handling. Add `_load(app, "geo_security_endpoints", "router")` to router_registry.py after the location-history line.
  </action>
  <verify>
    <automated>backend/venv/bin/python -m pytest backend/tests/test_geo_security_endpoints.py -q</automated>
  </verify>
  <acceptance_criteria>
    All endpoint tests pass; non-admin PATCH is 403; malformed country codes rejected; router registered (routes resolve).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser -> settings API | Authenticated admin mutates a per-tenant config doc |
| API -> system_settings | Tenant-scoped config storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-04-E | Elevation of Privilege | PATCH /api/settings/geo-security | mitigate | Reuse `_require_admin`/`_SETTINGS_ADMIN_ROLES` verbatim (V4 access control); non-admin -> 403, asserted by test |
| T-47-04-T | Tampering | allowed_country_codes input | mitigate | Pydantic validation to ISO 3166 alpha-2 at the boundary; reject garbage that would silently never match a real country_code (V5) |
| T-47-04-I | Information Disclosure | cross-tenant config read/write | mitigate | Tenant-scoped system_settings resolution + TenantIsolatedCollection auto-injection (proven for system_settings in Phase 46) |
| T-47-04-SC | Tampering | package installs | accept | No new packages introduced |
</threat_model>

<verification>
- `backend/venv/bin/python -m pytest backend/tests/test_geo_security_endpoints.py -q` green.
- `grep -n "geo_security_endpoints" backend/router_registry.py` shows the registration line.
- `wc -l backend/geo_security_endpoints.py` < 500.
- Full suite: no new failures beyond baseline.
</verification>

<success_criteria>
A tenant admin can read and update the detector toggles and country-code allowlist via admin-gated, input-validated, tenant-scoped endpoints; non-admins are refused.
</success_criteria>

<output>
Create `.planning/phases/47-agent-scoped-geo-security-detectors/47-04-SUMMARY.md` when done.
</output>
