---
phase: 47-agent-scoped-geo-security-detectors
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/geo_security_service.py
  - backend/tests/test_geo_security_service.py
autonomous: true
requirements: [GSEC-02, GSEC-03]
must_haves:
  truths:
    - "Impossible-travel fires (True) when haversine distance / elapsed hours > 1000 km/h between two raw consecutive check-ins (D-01)"
    - "Impossible-travel is fully suppressed when either endpoint's vpn_heuristic is True (D-02)"
    - "Impossible-travel is skipped unless >= 15 minutes elapsed between the two check-ins (D-08)"
    - "Impossible-travel does NOT fire on an agent's first-ever check-in (no prior geo)"
    - "Geo-fence fires when the check-in country_code is not in the tenant allowlist; does not fire when it is (D-03)"
    - "Config resolves tenant doc -> global doc -> hardcoded default; geo_fence disabled by default"
    - "Dedup fires exactly one alert on the clean->violating transition and re-fires after the 6h cooldown while still violating (D-05, D-07)"
    - "run_geo_security_detectors returns a list of alert payloads to persist (empty when clean/suppressed/toggled-off)"
  artifacts:
    - path: "backend/geo_security_service.py"
      provides: "evaluate_impossible_travel, evaluate_geo_fence, get_geo_security_settings, dedup_and_maybe_alert, run_geo_security_detectors"
      min_lines: 90
    - path: "backend/tests/test_geo_security_service.py"
      provides: "Hermetic unit tests for all detector + dedup + config behaviors"
  key_links:
    - from: "backend/geo_security_service.py"
      to: "backend/ueba_service.py"
      via: "from ueba_service import _haversine_km (reuse, never re-copy the formula)"
      pattern: "_haversine_km"
  prohibitions:
    - "MUST NOT re-copy the haversine formula — import _haversine_km from ueba_service"
    - "MUST NOT import persist_security_alert here — run_geo_security_detectors RETURNS alert payloads; the heartbeat wiring (47-03) fires them"
    - "MUST NOT compare against locationConfirmed/locationPending — use the RAW existing_agent.geo + existing_agent.lastSeen"
    - "MUST NOT treat vpn_heuristic None/absent as False for suppression — only `is True` suppresses"
    - "MUST NOT make impossible-travel threshold or the 15-min floor tenant-configurable (fixed for v3.3)"
---

<objective>
Build the pure detector + dedup + config core for agent-scoped impossible-travel (GSEC-02) and country-code geo-fence (GSEC-03) as a standalone `geo_security_service.py`, keeping the heartbeat handler change (Plan 47-03) to a single call-through. The only genuinely new logic in this phase (~15 lines of boolean checks) lives here; everything else is disciplined reuse.

Purpose: Isolate the detector logic in a testable sibling module (RESEARCH Recommended Project Structure) so `agent_heartbeat_endpoints.py` stays under the 500-line cap and the logic is unit-tested hermetically without a live DB or GeoLite2 `.mmdb`.
Output: `geo_security_service.py` exposing `evaluate_impossible_travel`, `evaluate_geo_fence`, `get_geo_security_settings`, `dedup_and_maybe_alert`, and the `run_geo_security_detectors` orchestrator that returns alert payloads.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-agent-scoped-geo-security-detectors/47-RESEARCH.md
@.planning/phases/47-agent-scoped-geo-security-detectors/47-CONTEXT.md
@backend/ueba_service.py
@backend/agent_location_history_service.py
</context>

<artifacts_produced>
New symbols in `backend/geo_security_service.py`:
- `MAX_SPEED_KMH = 1000` (D-01), `MIN_ELAPSED_MINUTES = 15` (D-08), `COOLDOWN_WINDOW = timedelta(hours=6)` (D-05).
- `evaluate_impossible_travel(prev_geo, prev_last_seen_iso, curr_geo, now, prev_vpn, curr_vpn) -> bool`
- `evaluate_geo_fence(country_code, allowlist) -> bool`
- `get_geo_security_settings(db, tenant_id) -> dict` (keys: impossible_travel_enabled, geo_fence_enabled, allowed_country_codes)
- `dedup_and_maybe_alert(raw_db, agent_id, existing_agent, violation_type, violating) -> bool`
- `run_geo_security_detectors(db, existing_agent, agent_id, tenant_id, curr_geo, now) -> list[dict]`
- Shadow field written on the `agents` doc: `geoSecurityState.<violation_type>.{violating,lastAlertedAt}`.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing detector + dedup + config tests</name>
  <files>backend/tests/test_geo_security_service.py</files>
  <read_first>
    - backend/tests/test_agent_location_history.py and backend/tests/test_agent_asn_service.py (hermetic `AsyncMock`/`monkeypatch` conventions; GeoLite2 `.mmdb` is NOT bundled so all geo is mocked — RESEARCH Pitfall 6).
    - 47-RESEARCH.md Pattern 1/2/3 (concrete function shapes) and the Phase Requirements -> Test Map (`-k` marker names to reuse).
  </read_first>
  <behavior>
    - impossible_travel_positive: two geos ~5000 km apart, 1h apart, no vpn -> True.
    - impossible_travel_below_speed: same distance over 24h -> False.
    - vpn_suppression: distance/time exceeds threshold but prev_vpn is True (and separately curr_vpn is True) -> False (D-02).
    - vpn_none_handling: vpn absent/None on either side does NOT suppress; a real over-speed jump still returns True (D-02 uses `is True` only).
    - first_checkin: prev_geo is None (or missing latitude) -> False.
    - elapsed_floor: distance exceeds threshold but only 10 minutes elapsed -> False (D-08); 20 minutes with a genuinely impossible speed -> True.
    - clock_skew: prev_last_seen in the future / elapsed <= 0 -> False.
    - geo_fence_violation: country_code "RU" not in ["US","GB"] -> True; case-insensitive normalization ("us" matches "US").
    - geo_fence_clean: country_code in allowlist -> False; empty/None country_code -> False.
    - config_resolution: tenant doc overrides global overrides default; default has geo_fence_enabled False and allowed_country_codes [].
    - dedup_cooldown: first violating call returns True (writes state); immediate repeat returns False; a repeat with lastAlertedAt older than 6h returns True again (D-07 re-fire); a clean call after violating clears state and returns False.
  </behavior>
  <action>
    Create backend/tests/test_geo_security_service.py with hermetic tests covering every behavior above. Mock the db with a hand-rolled AsyncMock (for `get_geo_security_settings` stub `raw.system_settings.find_one`; for `dedup_and_maybe_alert` stub `raw.agents.update_one` and pass an in-memory `existing_agent` dict carrying `geoSecurityState`). Do NOT call real geoip/asn. These tests MUST fail at collection/import (module does not exist yet) — RED.
  </action>
  <verify>
    <automated>backend/venv/bin/python -m pytest backend/tests/test_geo_security_service.py -q</automated>
  </verify>
  <acceptance_criteria>
    Test file exists; before Task 2/3 it fails on ModuleNotFoundError for geo_security_service. Confirms the tests exercise the real target.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement evaluate_impossible_travel + evaluate_geo_fence + get_geo_security_settings</name>
  <files>backend/geo_security_service.py</files>
  <read_first>
    - backend/ueba_service.py lines 109-116 (`_haversine_km` — import it, do not re-copy).
    - backend/agent_location_history_service.py `get_track_agent_location` (the tenant->global->default resolution shape to clone; note the `db._db if hasattr(db,"_db")` raw-handle unwrap).
    - 47-RESEARCH.md Pattern 1 and Pattern 2 (exact guard order and constants).
  </read_first>
  <action>
    Create backend/geo_security_service.py. Define `MAX_SPEED_KMH = 1000` (D-01), `MIN_ELAPSED_MINUTES = 15` (D-08). Implement `evaluate_impossible_travel(prev_geo, prev_last_seen_iso, curr_geo, now, prev_vpn, curr_vpn)`: guard-return False when prev_geo/curr_geo missing or lat missing (first check-in, Pitfall 3); return False when `prev_vpn is True or curr_vpn is True` (D-02, use `is True` only — Pitfall 5); parse prev_last_seen ISO (handle trailing Z), compute elapsed; return False when elapsed <= 0 (clock skew) or elapsed < 15 minutes (D-08 floor); compute `_haversine_km` distance and return `(distance_km / elapsed_hours) > MAX_SPEED_KMH`. Implement `evaluate_geo_fence(country_code, allowlist)`: return False when country_code falsy or allowlist empty; normalize both to uppercase; return `country_code not in allowlist`. Implement `get_geo_security_settings(db, tenant_id)` cloning the 3-step tenant->global->default resolution against `system_settings` docs of `type: "geo_security_detectors"`, defaults `{impossible_travel_enabled: True, geo_fence_enabled: False, allowed_country_codes: []}` (geo_fence off by default so an empty allowlist never alerts on every check-in). Import `_haversine_km` from ueba_service; do NOT import persist_security_alert.
  </action>
  <verify>
    <automated>backend/venv/bin/python -m pytest backend/tests/test_geo_security_service.py -k "impossible_travel or vpn or first_checkin or elapsed or clock or geo_fence or config_resolution" -q</automated>
  </verify>
  <acceptance_criteria>
    All impossible-travel, geo-fence, and config-resolution tests pass. Threshold/floor are module constants, not tenant config.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement dedup_and_maybe_alert + run_geo_security_detectors orchestrator</name>
  <files>backend/geo_security_service.py</files>
  <read_first>
    - 47-RESEARCH.md Pattern 3 (state-transition + cooldown shadow field; the Anti-Pattern note against porting Phase 46's 4-branch NAT-flip machine).
    - 47-CONTEXT.md D-05 and D-07 (cooldown re-fires — do not go permanently silent).
  </read_first>
  <behavior>
    - dedup_and_maybe_alert returns True on clean->violating transition and persists `geoSecurityState.<type>.{violating:True,lastAlertedAt:now}`.
    - Returns False on a repeat within 6h; returns True again once lastAlertedAt is older than COOLDOWN_WINDOW (D-07), resetting lastAlertedAt.
    - On a non-violating call after a violating state, clears `violating` to False and returns False.
    - run_geo_security_detectors returns [] when both toggles off; returns one alert payload dict per firing detector, each carrying alert_type/severity/title/description/metadata.
  </behavior>
  <action>
    Add `COOLDOWN_WINDOW = timedelta(hours=6)` (D-05) and implement `dedup_and_maybe_alert(raw_db, agent_id, existing_agent, violation_type, violating)` per Pattern 3 (read existing `geoSecurityState.<violation_type>` off the already-fetched `existing_agent`; write via `raw_db.agents.update_one` — zero extra reads). Then implement `run_geo_security_detectors(db, existing_agent, agent_id, tenant_id, curr_geo, now) -> list[dict]`: unwrap the raw db handle (`db._db if hasattr(db,"_db") else db`) for settings + shadow writes; call `get_geo_security_settings`; when impossible_travel_enabled, read the RAW prior beat from `existing_agent.get("geo")` + `existing_agent.get("lastSeen")` (NEVER locationConfirmed — Pitfall 2) plus prev/curr `vpn_heuristic`, evaluate, dedup, and append an `impossible_travel`/severity `high` payload when it should fire; when geo_fence_enabled, evaluate `curr_geo.country_code` against `allowed_country_codes`, dedup, append a `geo_fence_violation`/severity `medium` payload when it should fire. Return the collected payloads. The function performs NO connection side effects (D-04 alert-only) and does NOT call persist_security_alert. Keep the whole module under 500 lines.
  </action>
  <verify>
    <automated>backend/venv/bin/python -m pytest backend/tests/test_geo_security_service.py -q</automated>
  </verify>
  <acceptance_criteria>
    Full test file green, including dedup_cooldown (transition + re-fire + clear). run_geo_security_detectors returns alert payloads with no DB alert writes and no connection mutation.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent-reported publicIp -> detector | Agent self-reports publicIp (pre-existing, unverified trust boundary); geo is derived from it |
| detector -> agents doc shadow state | Detector writes geoSecurityState on the agents doc |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-02-D | Denial of Service | evaluate_impossible_travel | mitigate | D-02 VPN suppression + D-08 15-min elapsed floor kill the dominant false-positive classes (corporate VPN + CGNAT/mobile GeoIP jitter) so the fan-out isn't flooded; dedup+cooldown bounds alert volume to state transitions |
| T-47-02-I | Information Disclosure | get_geo_security_settings / shadow writes | mitigate | Reads/writes go through the same tenant-scoped db handle; settings resolution keys on tenantId; no cross-tenant read path introduced |
| T-47-02-S | Spoofing | agent-reported publicIp | accept | Pre-existing trust boundary (agent self-reports IP); unchanged by this phase; documented as a residual limitation of IP-self-reported geo (RESEARCH Security Domain) |
| T-47-02-SC | Tampering | package installs | accept | No new packages (stdlib math/datetime + existing _haversine_km) |
</threat_model>

<verification>
- `backend/venv/bin/python -m pytest backend/tests/test_geo_security_service.py -q` fully green.
- `grep -n "_haversine_km" backend/geo_security_service.py` shows an import from ueba_service (no re-copied formula body).
- `grep -c "persist_security_alert" backend/geo_security_service.py` returns 0 (fan-out is fired by the wiring, not here).
- Module under 500 lines (`wc -l backend/geo_security_service.py`).
</verification>

<success_criteria>
The detector core computes impossible-travel (D-01/D-02/D-08), geo-fence (D-03), dedup/cooldown (D-05/D-07), and per-tenant config resolution correctly and hermetically, exposing a `run_geo_security_detectors` orchestrator that returns alert payloads without touching the alert store or the connection.
</success_criteria>

<output>
Create `.planning/phases/47-agent-scoped-geo-security-detectors/47-02-SUMMARY.md` when done.
</output>
