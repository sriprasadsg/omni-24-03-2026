---
phase: 04-remediation-workflow
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/tests/test_remediation_workflow.py
  - agent-rust/src/poll.rs
  - backend/websocket_manager.py
autonomous: true
requirements: [REM-01, REM-02, REM-03, REM-04]

must_haves:
  truths:
    - "A failing pytest suite exists asserting REM-01..REM-04 behavior before any feature code is written"
    - "A Rust agent receiving a 'Run Compliance Scan' instruction routes it to the compliance check capability"
    - "websocket_manager exposes broadcast_remediation_update(tenant_id, payload) that emits a 'remediation_update' Socket.IO event"
  artifacts:
    - path: "backend/tests/test_remediation_workflow.py"
      provides: "Failing test scaffold for REM-01 through REM-04"
      contains: "def test_"
    - path: "backend/websocket_manager.py"
      provides: "broadcast_remediation_update broadcaster"
      contains: "broadcast_remediation_update"
    - path: "agent-rust/src/poll.rs"
      provides: "Rust dispatch match arm accepting 'Run Compliance Scan'"
      contains: "Run Compliance Scan"
  key_links:
    - from: "agent-rust/src/poll.rs"
      to: "caps::run_compliance_check"
      via: "dispatch_instruction match arm"
      pattern: "Run Compliance Scan.*=>"
    - from: "backend/websocket_manager.py broadcast_remediation_update"
      to: "sio.emit('remediation_update')"
      via: "per-sid emit loop over connected_clients[tenant_id]"
      pattern: "sio.emit\\(['\"]remediation_update"
---

<objective>
Lay the Wave 0 foundation for the remediation workflow: a failing test suite that pins REM-01..REM-04 behavior, the Rust agent instruction-string fix so re-scan dispatch reaches Rust agents, and the WebSocket broadcaster that Wave 1/2 will call to push live status updates.

Purpose: Establish the Nyquist test floor and patch two shared-infrastructure seams (Rust dispatch match arm, websocket_manager broadcaster) before any CRUD code is written. These are low-context, high-leverage changes that everything downstream depends on.

Output: backend/tests/test_remediation_workflow.py (failing scaffold), patched agent-rust/src/poll.rs, broadcast_remediation_update() added to backend/websocket_manager.py.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-remediation-workflow/04-RESEARCH.md
@.planning/phases/04-remediation-workflow/04-PATTERNS.md
@backend/websocket_manager.py
@agent-rust/src/poll.rs
@backend/tests/test_rust_heartbeat_parity.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create failing test scaffold for REM-01..REM-04</name>
  <files>backend/tests/test_remediation_workflow.py</files>
  <action>
Create the test file covering REM-01 through REM-04 (per REM-01..REM-04). Follow the AsyncMock pattern in backend/tests/test_rust_heartbeat_parity.py (lines 60-69): build a fake Motor `db` whose `compliance_remediation_tasks`, `assets`, `agents`, and `agent_instructions` collections are MagicMock with AsyncMock methods (`insert_one`, `find`, `find_one`, `update_one`). Use `asyncio.run(...)` to drive async calls — pytest-asyncio is NOT installed (STATE.md decision, 02-01). Do NOT add pytest markers requiring plugins.

Write these tests, all expected to FAIL now because the service/endpoints modules do not exist yet:
- `test_rem01_create_task_persists`: imports `compliance_remediation_service` (module not yet created), calls `create_task(db, {title, control_id, asset_id, framework_id, priority, due_date, description}, tenant_filter={'tenantId': 't1'}, created_by='u@e')`, asserts the inserted doc has id, status=='open', tenantId=='t1', title set.
- `test_rem02_list_tasks_filters_by_status`: calls `list_tasks(db, tenant_filter, status='open')`, asserts the Motor query passed to `db.compliance_remediation_tasks.find(...)` includes `status: 'open'` and the tenant filter.
- `test_rem03_resolve_dispatches_rescan`: calls `update_task(db, task_id, {'status':'resolved'}, tenant_filter)` where the stored task has `asset_id` resolving (via `db.assets.find_one`) to an `agentId`; asserts `db.agent_instructions.insert_one` was awaited with a doc whose `instruction == 'Run Compliance Scan'` and `control_id` matches the task.
- `test_rem04_broadcast_remediation_update_emits_event`: imports `broadcast_remediation_update` from `websocket_manager`, monkeypatches `websocket_manager.connected_clients` to `{'t1': {'sid1'}}` and `websocket_manager.sio.emit` to an AsyncMock, calls `broadcast_remediation_update('t1', {'task_id':'x','status':'resolved'})`, asserts `sio.emit` awaited with first arg `'remediation_update'`.

Guard imports of not-yet-existing modules with a module-level `pytest.importorskip` is NOT allowed here — instead use a direct `import compliance_remediation_service` at call time inside each test so the test fails loudly (ImportError → test failure) until Wave 1 creates it. Add a top-of-file comment block listing the four REM IDs and that this scaffold is RED until 04-01/04-02 land.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_remediation_workflow.py -q 2>&1 | grep -Ev '^#' | grep -Eq '(failed|error)' && echo "RED-as-expected: scaffold fails before impl"</automated>
  </verify>
  <done>test_remediation_workflow.py exists with 4 tests named for REM-01..REM-04; running it reports failures/errors (RED) because compliance_remediation_service does not yet exist; the broadcast test (REM-04) passes only after Task 3 lands broadcast_remediation_update.</done>
</task>

<task type="auto">
  <name>Task 2: Patch Rust agent to accept "Run Compliance Scan" instruction string</name>
  <files>agent-rust/src/poll.rs</files>
  <action>
In `dispatch_instruction` (agent-rust/src/poll.rs), the compliance match arm at line 260 currently reads:
`"run_compliance_check" | "Run Compliance Check" | "remediate_compliance" => caps::run_compliance_check().await,`

Add `"Run Compliance Scan"` as an additional alternative on that same match arm so the Python-dispatched instruction string (`"Run Compliance Scan"`, used by compliance_scans_endpoints.py and the Wave 1 dispatch) routes to `caps::run_compliance_check()` on Rust agents. The resolved arm pattern must list `"Run Compliance Scan"` alongside the existing three alternatives. Do not change the body or any other arm. This closes the Pitfall 1 / Open Question 1 string-mismatch gap from 04-RESEARCH.md (REM-03).

Do NOT attempt to compile the Rust agent (no cargo toolchain assumed in CI); verification is a source-level grep that the new alternative is present on the dispatch arm.
  </action>
  <verify>
    <automated>grep -n '"Run Compliance Scan"' agent-rust/src/poll.rs | grep -q '=>' || grep -B1 'run_compliance_check().await' agent-rust/src/poll.rs | grep -q 'Run Compliance Scan'</automated>
  </verify>
  <done>poll.rs dispatch_instruction has a compliance match arm that includes the literal "Run Compliance Scan" routing to caps::run_compliance_check(); the existing "run_compliance_check" / "Run Compliance Check" / "remediate_compliance" alternatives are preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Add broadcast_remediation_update to websocket_manager</name>
  <files>backend/websocket_manager.py</files>
  <action>
Add an async function `broadcast_remediation_update(tenant_id: str, payload: dict) -> None` to backend/websocket_manager.py, modeled exactly on `broadcast_compliance_alert` (lines 287-295) and `broadcast_mitre_heatmap` (lines 306-316). Place it immediately after `broadcast_compliance_alert`. Behavior (per REM-04):
- Return early if `tenant_id not in connected_clients`.
- Set `payload['timestamp'] = payload.get('timestamp', datetime.now(timezone.utc).isoformat())` (datetime/timezone already imported at line 5).
- Iterate `for sid in list(connected_clients[tenant_id]):` and `await sio.emit('remediation_update', payload, room=sid)`.
- Add a `logger.debug(...)` line consistent with the other broadcasters.

Use `list(connected_clients[tenant_id])` (snapshot copy) to avoid mutation-during-iteration, matching broadcast_mitre_heatmap. Do not modify `send_to_agent` (lines 353-365) — Wave 1 imports it as-is.
  </action>
  <verify>
    <automated>cd backend && python -c "import websocket_manager as w; import inspect; assert hasattr(w,'broadcast_remediation_update'); src=inspect.getsource(w.broadcast_remediation_update); assert 'remediation_update' in src and 'connected_clients' in src; print('OK')"</automated>
  </verify>
  <done>websocket_manager.broadcast_remediation_update exists, guards on connected_clients membership, stamps a timestamp, and emits the 'remediation_update' event per connected sid; importable without side effects; the REM-04 broadcast test from Task 1 now passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend → connected browser clients | broadcast_remediation_update emits to all sids of a tenant; payload must not leak cross-tenant data |
| Rust agent ← backend instruction queue | agent acts on instruction strings; an injected/unexpected instruction string could trigger an unintended scan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | broadcast_remediation_update | mitigate | Emit only to `connected_clients[tenant_id]` sids — never iterate all tenants; Wave 1 passes the task's own tenantId as tenant_id |
| T-04-02 | Tampering | poll.rs dispatch match arm | accept | New arm only routes the fixed literal "Run Compliance Scan" to the existing read-only compliance scan capability; no new privileged action introduced |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan; no audit table needed (04-RESEARCH.md Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `cd backend && python -m pytest tests/test_remediation_workflow.py -q` runs and reports RED (failures/errors) for REM-01..REM-03 and GREEN for the REM-04 broadcast test after Task 3.
- `grep '"Run Compliance Scan"' agent-rust/src/poll.rs` shows the literal on the compliance dispatch arm.
- `python -c "import websocket_manager"` imports cleanly with broadcast_remediation_update present.
</verification>

<success_criteria>
- Failing test scaffold exists for all four REM requirements (Nyquist floor established).
- Rust agents will execute compliance re-scans dispatched as "Run Compliance Scan".
- The broadcaster Wave 1/2 depend on exists and emits 'remediation_update'.
</success_criteria>

<output>
Create `.planning/phases/04-remediation-workflow/04-00-SUMMARY.md` when done.
</output>
