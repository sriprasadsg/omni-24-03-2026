---
phase: 04-remediation-workflow
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - components/RemediationTaskModal.tsx
  - components/RemediationDashboard.tsx
  - components/Sidebar.tsx
  - App.tsx
autonomous: true
requirements: [REM-01, REM-02, REM-03, REM-04]

must_haves:
  truths:
    - "A user can open a Create Task modal, fill title/assignee/due date/description, and create a remediation task"
    - "Remediation tasks render in a list filterable by status Open / In Progress / Resolved"
    - "Marking a task Resolved calls the backend and triggers the re-scan dispatch"
    - "The dashboard updates a task's status live on a remediation_update WebSocket event without manual refresh"
    - "A Remediation nav item under Governance & Compliance routes to the dashboard"
  artifacts:
    - path: "components/RemediationTaskModal.tsx"
      provides: "Create/edit remediation task form modal"
      contains: "RemediationTaskModal"
      max_lines: 500
    - path: "components/RemediationDashboard.tsx"
      provides: "Task list + status filter + create button + WS subscription"
      contains: "RemediationDashboard"
      max_lines: 500
    - path: "components/Sidebar.tsx"
      provides: "Remediation nav item under Governance & Compliance"
      contains: "remediationWorkflow"
  key_links:
    - from: "components/RemediationDashboard.tsx"
      to: "services/apiService.ts getRemediationTasks/updateRemediationTask"
      via: "fetch on mount + status filter; PATCH on resolve"
      pattern: "getRemediationTasks|updateRemediationTask"
    - from: "components/RemediationDashboard.tsx"
      to: "socketService 'remediation_update'"
      via: "socketService.on/off in useEffect with teardown"
      pattern: "remediation_update"
    - from: "components/RemediationTaskModal.tsx"
      to: "services/apiService.ts createRemediationTask/suggestRemediation"
      via: "form submit + AI-suggest button"
      pattern: "createRemediationTask|suggestRemediation"
    - from: "components/Sidebar.tsx"
      to: "App.tsx view router"
      via: "view: 'remediationWorkflow' nav item -> RemediationDashboard render"
      pattern: "remediationWorkflow"
---

<objective>
Build the frontend for the compliance remediation workflow: a create/edit modal, a filterable task dashboard with live WebSocket status updates, a sidebar nav item, and the App.tsx view route. Consumes the RemediationTask type and apiService helpers shipped in 04-01.

Purpose: Deliver REM-01 (create task UI), REM-02 (filterable list), the user-facing half of REM-03 (Resolve button calling PATCH), and REM-04 (live status patch on remediation_update). Splitting the modal into its own file keeps RemediationDashboard.tsx under the 500-line CLAUDE.md limit (Pitfall 4).

Output: components/RemediationTaskModal.tsx, components/RemediationDashboard.tsx, a Sidebar.tsx nav item, and an App.tsx route wiring 'remediationWorkflow' to the dashboard.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-remediation-workflow/04-PATTERNS.md
@components/SiemRulesDashboard.tsx
@components/AgentDetailModal.tsx
@components/Sidebar.tsx
@services/apiService.ts
@services/socketService.ts
@types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: RemediationTaskModal create/edit form (REM-01)</name>
  <files>components/RemediationTaskModal.tsx</files>
  <action>
Create components/RemediationTaskModal.tsx following the inline-form analog in components/SiemRulesDashboard.tsx (lines 157-237) and the modal-props pattern in components/AgentDetailModal.tsx (lines 17-29). Props: `{ isOpen: boolean; onClose: () => void; task: RemediationTask | null; controlId?: string; assetId?: string; frameworkId?: string; onRefresh: () => void }`. First render line: `if (!isOpen) return null;` (04-PATTERNS.md gotcha).

Controlled inputs (04-PATTERNS.md lines 303-318) for: title (text, required), description (textarea), assignee (text — accepts agent id or user id; label "Assignee"), assignee_type (select: Agent | User), due_date (date input), priority (select low/medium/high/critical). Show `control_id` read-only from the `controlId`/`task.control_id` prop (REM-01: control linkage shown, not editable). Initialize state from `task` when editing, empty when creating.

AI assist (REM-01, AI-SPEC.md): add a "Suggest steps" button that calls `api.suggestRemediation(task.id)` when editing an existing task (id required to call the suggest endpoint) and populates the description textarea with the returned `suggestion` text. If creating a brand-new task (no id yet), disable the button with a tooltip "Save the task first to get AI suggestions" — the suggest endpoint operates on a persisted task. Guard the call in try/catch; show a small inline spinner/disabled state while awaiting.

Save handler (04-PATTERNS.md lines 321-335): on save, if `task` exists call `api.updateRemediationTask(task.id, {...})`, else `api.createRemediationTask({ title, control_id, asset_id, framework_id, assignee, due_date, description, priority })`; then `onClose(); onRefresh();` in a try/catch logging errors. Cancel + Save buttons per 04-PATTERNS.md lines 339-353. Use the existing dark-mode Tailwind classes from the analog. Keep under 500 lines.
  </action>
  <verify>
    <automated>cd /home/user/enterprise-omni-agent-ai-platform && grep -q 'RemediationTaskModal' components/RemediationTaskModal.tsx && grep -q 'createRemediationTask' components/RemediationTaskModal.tsx && grep -q 'if (!isOpen) return null' components/RemediationTaskModal.tsx && test "$(wc -l < components/RemediationTaskModal.tsx)" -lt 500 && echo "modal OK"</automated>
    <human-check>Open the modal from the dashboard: title/assignee/assignee_type/due_date/priority fields render; control_id shows read-only; Save creates a task; "Suggest steps" populates the description from AI when editing.</human-check>
  </verify>
  <done>RemediationTaskModal renders a controlled create/edit form, guards on isOpen, shows control_id read-only, calls createRemediationTask/updateRemediationTask on save and suggestRemediation for AI description; under 500 lines.</done>
</task>

<task type="auto">
  <name>Task 2: RemediationDashboard list + filter + live updates (REM-02, REM-03, REM-04)</name>
  <files>components/RemediationDashboard.tsx</files>
  <action>
Create components/RemediationDashboard.tsx following components/SiemRulesDashboard.tsx (lines 1-346, exact role/data-flow analog). State (04-PATTERNS.md lines 246-251): `tasks: RemediationTask[]`, `loading`, `filterStatus` (default 'all'), `isCreating`, `editingTask`.

Fetch (04-PATTERNS.md lines 254-268): `fetchTasks = useCallback(async () => { ... api.getRemediationTasks(filterStatus==='all'?undefined:filterStatus) ... })` and `useEffect(() => { fetchTasks(); }, [fetchTasks])`. Re-fetch when filterStatus changes (include it in the useCallback deps).

Status filter bar (REM-02, 04-RESEARCH.md lines 499-521): chips for ['all','open','in_progress','resolved'] mapping to the REM-02 labels All / Open / In Progress / Resolved; active chip highlighted. Filtering may be server-side (status query param) and/or client-side `tasks.filter`.

Task list: render each task with title, control_id, assignee, due_date, and a status badge using the STATUS_COLORS map (04-PATTERNS.md lines 286-291) with `STATUS_COLORS[task.status] ?? STATUS_COLORS.open` fallback. Each row: an Edit button (sets editingTask + opens modal) and, for non-resolved tasks, a "Mark Resolved" button (REM-03) that calls `api.updateRemediationTask(task.id, { status: 'resolved' })` then refetches. A "Create Task" button (top-right) opens the modal in create mode (REM-01).

WebSocket live update (REM-04, 04-PATTERNS.md lines 271-282): `useEffect` registering `socketService.on('remediation_update', onUpdate)` where `onUpdate(data)` patches the matching task in place: `setTasks(prev => prev.map(t => t.id === data.task_id ? { ...t, ...data } : t))`; return `() => socketService.off('remediation_update', onUpdate)` for teardown. Import socketService from services/socketService.ts.

Render `<RemediationTaskModal isOpen={isCreating || !!editingTask} task={editingTask} onClose={() => { setIsCreating(false); setEditingTask(null); }} onRefresh={fetchTasks} />`. Use existing dark-mode Tailwind classes. Keep under 500 lines (delegate all form state to the modal — Pitfall 4).
  </action>
  <verify>
    <automated>cd /home/user/enterprise-omni-agent-ai-platform && grep -q 'RemediationDashboard' components/RemediationDashboard.tsx && grep -q 'getRemediationTasks' components/RemediationDashboard.tsx && grep -q "socketService.on('remediation_update'" components/RemediationDashboard.tsx && grep -q "socketService.off('remediation_update'" components/RemediationDashboard.tsx && grep -q "status: 'resolved'" components/RemediationDashboard.tsx && test "$(wc -l < components/RemediationDashboard.tsx)" -lt 500 && echo "dashboard OK"</automated>
    <human-check>Navigate to Remediation: tasks list renders; status chips filter Open/In Progress/Resolved; Create Task opens the modal; Mark Resolved updates a task; a backend remediation_update event flips a task's status without a manual refresh.</human-check>
  </verify>
  <done>RemediationDashboard lists tasks, filters by status, opens the create/edit modal, marks tasks resolved via updateRemediationTask, and patches task status live on remediation_update with proper socket teardown; under 500 lines.</done>
</task>

<task type="auto">
  <name>Task 3: Sidebar nav item + App.tsx view route (REM-02)</name>
  <files>components/Sidebar.tsx, App.tsx</files>
  <action>
In components/Sidebar.tsx: add a nav item to the "Governance & Compliance" group (title at line 341), immediately after the `complianceEvidence` item (line 344): `{ view: 'remediationWorkflow', label: 'Remediation', icon: <ShieldAlertIcon size={20} />, permission: 'view:compliance' }`. Reuse an already-imported icon (ShieldAlertIcon is used elsewhere in this file for patching items; confirm it is imported, otherwise reuse ShieldCheckIcon which is already imported for complianceEvidence). Match the exact object shape of the sibling items in that group. Use the same `permission: 'view:compliance'` as complianceEvidence so RBAC visibility matches the compliance domain.

In App.tsx: wire the `'remediationWorkflow'` AppView to render `<RemediationDashboard />`. Find the existing view switch/router that renders other AppView components (e.g. where `complianceEvidence` maps to its component) and add the corresponding case/branch importing RemediationDashboard from './components/RemediationDashboard'. Match the existing render pattern exactly (conditional render or switch case, whichever App.tsx uses). Pass any props the sibling dashboards receive (e.g. tenant/user context) if the pattern requires them; otherwise render with no props.
  </action>
  <verify>
    <automated>cd /home/user/enterprise-omni-agent-ai-platform && grep -q "view: 'remediationWorkflow'" components/Sidebar.tsx && grep -q 'RemediationDashboard' App.tsx && grep -q "remediationWorkflow" App.tsx && echo "nav+route OK"</automated>
    <human-check>The "Remediation" item appears under Governance & Compliance in the sidebar and clicking it renders the RemediationDashboard.</human-check>
  </verify>
  <done>Sidebar shows a Remediation item under Governance & Compliance scoped to view:compliance; App.tsx routes the 'remediationWorkflow' view to RemediationDashboard; navigation works end to end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser to API | dashboard/modal send user input to /api/compliance-remediation; backend (04-01) enforces tenant scoping and validation |
| Socket.IO event to browser | remediation_update payloads patch local task state; only the tenant's own events reach this client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Information Disclosure | RemediationDashboard fetch | accept | Tenant scoping enforced server-side by _tenant_filter (04-01 T-04-03); the client renders only what the API returns for its tenant |
| T-04-09 | Tampering | remediation_update WS handler | mitigate | onUpdate only patches a task whose id already exists in local state (`t.id === data.task_id`); unknown ids are ignored, so a stray event cannot inject a new task |
| T-04-10 | Spoofing | Mark Resolved action | accept | Resolve goes through authFetch (JWT-authenticated) to the tenant-scoped PATCH; client cannot resolve another tenant's task |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase; no new npm deps (constraint); 04-RESEARCH.md Package Legitimacy Audit: N/A |
</threat_model>

<verification>
- `grep` confirms RemediationTaskModal and RemediationDashboard exist, both under 500 lines, wired to the 04-01 apiService helpers.
- Dashboard registers and tears down the `remediation_update` socket handler (REM-04).
- Sidebar has the remediationWorkflow item; App.tsx routes it to RemediationDashboard.
- End-of-phase human-check (human_verify_mode=end-of-phase): create a task, filter the list, mark resolved, observe live status update.
</verification>

<success_criteria>
- REM-01: user creates a remediation task via the modal.
- REM-02: tasks list filters by Open / In Progress / Resolved.
- REM-03 (UI): Mark Resolved triggers the backend dispatch.
- REM-04: task status updates live on remediation_update without manual refresh.
- RemediationDashboard.tsx and RemediationTaskModal.tsx both under 500 lines.
</success_criteria>

<output>
Create `.planning/phases/04-remediation-workflow/04-02-SUMMARY.md` when done.
</output>
