---
phase: 32-cloud-and-saas-provider-expansion
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/attack_path_service.py
  - backend/attack_path_endpoints.py
  - backend/tests/test_attack_path.py
  - types.ts
  - components/AttackPathDashboard.tsx
autonomous: true
requirements: [PROV-04]
user_setup: []
must_haves:
  truths:
    - "GET /api/security/attack-paths returns real correlated paths (from AttackPathService.get_attack_paths) when db.assets/db.vulnerabilities have data — NOT the immediate demo-seed the endpoint returns today (PROV-04)"
    - "The endpoint's duplicate _seed_paths() logic is deleted; the handler calls attack_path_service.get_attack_path_service(db).get_attack_paths(tenant_id) instead (PROV-04)"
    - "Both the real-correlation path dict and the _seed_demo_paths() dict carry an explicit simulated boolean (false for real, true for demo) so the fallback is unambiguously labeled (PROV-04, T-32-06)"
    - "The AttackPathEdge frontend contract matches the backend's actual emitted fields (source/target/vulnerability); AttackPathDashboard renders the edge label via e.source/e.vulnerability so edge labels finally appear (Pitfall 3)"
    - "AttackPathDashboard shows a prominent SIMULATED badge (cloning IacContainerDashboard's convention) whenever displayPath.simulated is true"
  artifacts:
    - "backend/attack_path_service.py with simulated on both real and demo path dicts"
    - "backend/attack_path_endpoints.py rewired to the service, _seed_paths() removed"
    - "types.ts with corrected AttackPathEdge (source/target/vulnerability) and AttackPath.simulated?"
    - "components/AttackPathDashboard.tsx with the fixed edge lookup + SIMULATED badge"
    - "backend/tests/test_attack_path.py (new — closes a pre-existing zero-coverage gap)"
  key_links:
    - "GET /api/security/attack-paths -> get_attack_path_service(db).get_attack_paths(tenant_id) -> real correlation (assets x vulnerabilities/cspm_findings) OR _seed_demo_paths() fallback, both tagged simulated"
    - "backend edge dict {source,target,vulnerability} -> types.ts AttackPathEdge -> AttackPathDashboard edge-label lookup e.source===node.id ? e.vulnerability"
---

<objective>
Deliver PROV-04: attack-path visualization must prefer real findings and clearly label the demo fallback. This is a dead-code re-wiring, not new logic. AttackPathService.get_attack_paths() already implements the full "prefer real correlation, fall back to demo seed only when truly empty" behavior but has zero call sites — the registered endpoint instead runs a thinner inline handler that returns hardcoded demo scenarios immediately. Delete the endpoint's duplicate _seed_paths() and call the real service. Add an explicit simulated flag to both the real and demo path dicts. Fix the independent pre-existing edge-field mismatch (frontend expects from/to/label, backend emits source/target/vulnerability at every call site) by correcting types.ts + AttackPathDashboard.tsx to the backend's naming, so edge labels render for the first time. Add the SIMULATED badge cloning IacContainerDashboard's convention.

Purpose: Make the attack-path dashboard show genuine asset x vulnerability correlations when data exists, honestly labeled, with visible edge labels.
Output: rewired attack_path_endpoints.py, simulated-tagged attack_path_service.py, corrected types.ts + AttackPathDashboard.tsx, and the first test file for this feature.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-cloud-and-saas-provider-expansion/32-RESEARCH.md
@.planning/phases/32-cloud-and-saas-provider-expansion/32-PATTERNS.md
@.planning/phases/32-cloud-and-saas-provider-expansion/32-VALIDATION.md
@backend/attack_path_service.py
@backend/attack_path_endpoints.py
@components/AttackPathDashboard.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewire endpoint to the real service + simulated flag on both path dicts + tests</name>
  <files>backend/attack_path_endpoints.py, backend/attack_path_service.py, backend/tests/test_attack_path.py</files>
  <read_first>
    - backend/attack_path_endpoints.py (full — the current get_attack_paths handler lines 66-86 that returns _seed_paths() immediately, the _seed_paths() duplicate function lines 18-63 to delete, the _tenant helper, and the auth dependency `from authentication_service import get_current_user`)
    - backend/attack_path_service.py (full — get_attack_paths() at lines 20-142 with the real-correlation branch and the _seed_demo_paths() fallback at lines 145+, the two path-dict construction sites where simulated must be added, and get_attack_path_service(db) factory at line 201)
    - 32-PATTERNS.md sections "attack_path_endpoints.py + attack_path_service.py (MODIFIED)" (the replacement handler + the exact simulated-flag insertion points) and "backend/tests/test_cloud_integrations.py, test_attack_path.py (NEW)" (the TestClient harness + the three assertions to make)
    - 32-RESEARCH.md Architecture Patterns — Pattern 4 + Security Domain threat T-32-06
  </read_first>
  <behavior>
    - GET attack-paths with db.attack_paths empty but db.assets + db.vulnerabilities populated -> real-correlation branch runs, returns paths with simulated == False.
    - GET attack-paths with assets/vulns/cspm_findings all empty -> _seed_demo_paths() runs, returns paths with simulated == True on every dict.
    - Every returned edge dict has keys source/target/vulnerability.
  </behavior>
  <action>
    In backend/attack_path_service.py: add `"simulated": False` to the path dict built in the real-correlation branch (around lines 107-120) and `"simulated": True` to each path dict appended in _seed_demo_paths() (around lines 182-195). Change nothing else in the correlation logic. In backend/attack_path_endpoints.py: delete the entire duplicate _seed_paths() function (lines 18-63) and replace the get_attack_paths handler body so that, after resolving tenant_id (keep the 403 guard when tenant is missing), it calls `service = get_attack_path_service(db)` then `paths = await service.get_attack_paths(tenant_id)` and returns paths. Add `from attack_path_service import get_attack_path_service` at the top. Do not leave any reference to the deleted _seed_paths.

    Create backend/tests/test_attack_path.py cloning the _col/_db/_user/_app TestClient harness from tests/test_automation_and_baa.py (override authentication_service.get_current_user, which is the symbol this router imports). Mock db.attack_paths, db.assets, db.vulnerabilities, db.cspm_findings and assert: (a) with attack_paths empty and assets+vulns present, the response paths carry simulated == False (real-correlation branch — test_attack_paths_prefers_real_correlation); (b) with everything empty, the response paths carry simulated == True (demo fallback); (c) every edge dict uses source/target/vulnerability keys (test_edge_field_contract). This test file is the Wave-0 scaffold.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_attack_path.py -x</automated>
  </verify>
  <acceptance_criteria>
    - _seed_paths() is gone from attack_path_endpoints.py; the handler calls get_attack_path_service(db).get_attack_paths(tenant_id).
    - Real correlation runs when data exists (simulated False); demo seed only when empty (simulated True on every dict).
    - Edge dicts use source/target/vulnerability; all three tests pass.
  </acceptance_criteria>
  <done>Endpoint calls the real service, both path dicts carry simulated, and the three backend tests are green.</done>
</task>

<task type="auto">
  <name>Task 2: Fix AttackPathEdge contract + edge-label render + SIMULATED badge</name>
  <files>types.ts, components/AttackPathDashboard.tsx</files>
  <read_first>
    - types.ts lines ~1273-1277 (AttackPathEdge interface: currently from/to/label) and the AttackPath interface nearby
    - components/AttackPathDashboard.tsx (full — the broken edge-label lookup at line 82 using e.from/e.label, the "High Priority Scenario" badge block at lines 57-62 to place the new badge next to, and the icon import at line 3)
    - components/IacContainerDashboard.tsx lines 367-371 (the SIMULATED badge markup + AlertTriangle usage) and line 37 (simulated?: boolean on its result type) — the convention to clone
    - 32-PATTERNS.md sections "components/AttackPathDashboard.tsx (MODIFIED)" and the AttackPathEdge decision (keep backend naming, fix frontend + types.ts) + 32-RESEARCH.md Open Questions #3 (final grep for other consumers)
  </read_first>
  <action>
    In types.ts: change AttackPathEdge from {from,to,label} to {source: string; target: string; vulnerability: string} to match the fields the backend actually emits at every call site, and add `simulated?: boolean` to the AttackPath interface. In components/AttackPathDashboard.tsx: fix the edge-label lookup (currently `edges.find(e => e.from === node.id)?.label`, which always returns undefined) to `edges.find(e => e.source === node.id)?.vulnerability` so edge labels finally render (Pitfall 3). Add a SIMULATED badge cloning IacContainerDashboard.tsx's markup, rendered only when displayPath.simulated is true, placed as a sibling of the existing "High Priority Scenario" badge block; adapt the copy to attack-path context (e.g. "SIMULATED — demo scenario, no real assets/vulnerabilities found"). Import AlertTriangle from the same icon source IacContainerDashboard uses (confirm it exists in ./icons or lucide-react as that file imports it). Do not change any other edge consumer — run the final grep in verify to confirm none exist.
  </action>
  <verify>
    <automated>cd /home/user/enterprise-omni-agent-ai-platform && npx tsc --noEmit 2>&1 | grep -i "attackpath" || echo "no attackpath type errors"</automated>
    <human-check>Open the Attack Path dashboard: with empty assets/vulns confirm the SIMULATED badge renders; seed an asset + open vulnerability and confirm a real (unbadged) path renders WITH visible edge labels between nodes (deferred to /gsd-verify-work per 32-VALIDATION Manual-Only).</human-check>
  </verify>
  <acceptance_criteria>
    - AttackPathEdge in types.ts is {source, target, vulnerability}; AttackPath has simulated?: boolean.
    - AttackPathDashboard edge lookup uses e.source/e.vulnerability and renders edge labels.
    - The SIMULATED badge renders only when displayPath.simulated is true, cloning IacContainerDashboard's convention.
    - tsc --noEmit reports no AttackPath-related type errors; grep confirms AttackPathDashboard.tsx is the only edge-field consumer.
  </acceptance_criteria>
  <done>The frontend contract matches the backend, edge labels render, and the SIMULATED badge appears for demo paths — with no other stray AttackPathEdge consumers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| attack_path service → dashboard | path.simulated must faithfully signal demo vs real; the UI trusts this flag |
| client → GET /api/security/attack-paths | tenant context resolved before any query (existing 403 guard preserved) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-06 | Spoofing (of finding authenticity) | attack-path demo-seed presented as real | high | mitigate | Explicit simulated:true on every demo-seed path dict + prominent SIMULATED badge in the dashboard (Phase 25 CHK-03 precedent); real correlation returns simulated:false; backend test asserts both branches |
| T-32-EDGE | Tampering (data integrity of displayed graph) | AttackPathEdge field-name mismatch hiding edge labels | medium | mitigate | Align types.ts + dashboard to the backend's source/target/vulnerability contract; test_edge_field_contract locks the field names; grep confirms single consumer |
| T-32-AC4 | Access Control (V4) | tenant scoping of get_attack_paths | medium | mitigate | Preserve the endpoint's tenant-context 403 guard; AttackPathService queries are tenant-scoped |
</threat_model>

<artifacts>
## Artifacts this phase produces (Plan 32-04)

Modified backend:
- `backend/attack_path_service.py` — `simulated: False` on the real-correlation path dict, `simulated: True` on every `_seed_demo_paths()` dict
- `backend/attack_path_endpoints.py` — `_seed_paths()` deleted; handler now calls `get_attack_path_service(db).get_attack_paths(tenant_id)`

Modified frontend:
- `types.ts` — `AttackPathEdge` corrected to `{source, target, vulnerability}`; `AttackPath.simulated?: boolean` added
- `components/AttackPathDashboard.tsx` — edge-label lookup fixed to `e.source`/`e.vulnerability`; SIMULATED badge added

New test:
- `backend/tests/test_attack_path.py` — `test_attack_paths_prefers_real_correlation`, demo-fallback simulated test, `test_edge_field_contract`
</artifacts>

<verification>
- `cd backend && python -m pytest tests/test_attack_path.py -x`
- `npx tsc --noEmit` — no AttackPath type errors
- `cd backend && python -m pytest tests/ -q` — no regression
- Manual: SIMULATED badge + edge labels verified at /gsd-verify-work (32-VALIDATION Manual-Only)
</verification>

<success_criteria>
- The registered endpoint returns real correlated paths when data exists, demo only when empty.
- Demo paths are unambiguously labeled (simulated flag + UI badge).
- Edge labels render for the first time via the corrected field contract.
</success_criteria>

<output>
Create `.planning/phases/32-cloud-and-saas-provider-expansion/32-04-SUMMARY.md` when done.
</output>
