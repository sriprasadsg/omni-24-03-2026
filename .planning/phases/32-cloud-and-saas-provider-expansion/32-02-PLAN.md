---
phase: 32-cloud-and-saas-provider-expansion
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/cloud_checks_m365.py
  - backend/cloud_checks_mongodb_atlas.py
  - backend/cloud_checks_service.py
  - backend/cloud_account_endpoints.py
  - backend/cloud_checks_endpoints.py
  - backend/mcp_server_endpoints.py
  - backend/tests/test_cloud_checks_expansion.py
autonomous: true
requirements: [PROV-02, PROV-01]
user_setup: []
must_haves:
  truths:
    - "run_checks() evaluates microsoft365 and mongodb_atlas checks unmodified once their catalogs exist and their provider strings are in RUNNABLE_PROVIDERS (PROV-02)"
    - "All four CSPM gates accept microsoft365 and mongodb_atlas: _VALID_PROVIDERS (registration), cloud_checks_endpoints.py run tuple, mcp_server_endpoints.py tuples, and RUNNABLE_PROVIDERS — no gate accepts a provider another rejects (PROV-02, Phase 25 lockstep)"
    - "The three CSPM registration/validation gates (_VALID_PROVIDERS, cloud_checks_endpoints.py, mcp_server_endpoints.py) also accept oci, alibaba, cloudflare so AddCloudAccountModal submissions for those three stop returning 400 (PROV-01 lockstep fix)"
    - "run_checks() results carry an additive simulated flag: true when db.cloud_findings had no entries for the account (catalog-only evaluation), false when real findings were present — distinguishing real-findings evaluations from empty-collection ones (PROV-02, Phase 25 simulated-flag convention)"
    - "Existing catalog-only providers (aws/azure/gcp/kubernetes/digitalocean) keep identical PASS/FAIL behavior and ran counts — the simulated field is purely additive, no fail-closed change, no regression"
  artifacts:
    - "backend/cloud_checks_m365.py exposing M365_CHECKS: List[Dict] with provider == microsoft365"
    - "backend/cloud_checks_mongodb_atlas.py exposing MONGODB_ATLAS_CHECKS: List[Dict] with provider == mongodb_atlas"
    - "backend/cloud_checks_service.py importing both catalogs, appending to CLOUD_CHECKS, adding microsoft365/mongodb_atlas to RUNNABLE_PROVIDERS, and setting the simulated provenance flag in run_checks()"
    - "widened gates in cloud_account_endpoints.py, cloud_checks_endpoints.py, mcp_server_endpoints.py"
    - "extended backend/tests/test_cloud_checks_expansion.py with M365/Atlas eval, denominator, gate-parity, and provenance/no-regression cases"
  key_links:
    - "cloud_checks_service.py imports M365_CHECKS + MONGODB_ATLAS_CHECKS -> CLOUD_CHECKS concat -> run_checks() filters by check[provider] == provider"
    - "RUNNABLE_PROVIDERS + _RUNNABLE_CHECKS_COUNT stay in sync (new checks all belong to runnable providers)"
    - "run_checks() -> len(findings_raw) == 0 ? simulated=true : simulated=false -> cloud_check_results doc"
---

<objective>
Deliver PROV-02's "scanned providers" catalog half and the shared provider-gate lockstep. Add M365 and MongoDB Atlas check catalogs as new per-provider files (the established cloud_checks_aws.py / cloud_checks_k8s.py split pattern), wire them into cloud_checks_service.py's CLOUD_CHECKS concatenation and RUNNABLE_PROVIDERS, and widen all four CSPM provider gates in one lockstep edit. run_checks() itself needs zero logic changes to evaluate the two new providers (it filters CLOUD_CHECKS by provider) — but this plan adds one additive change: a simulated provenance flag on results so evaluations against real ingested findings (written by Plan 32-05) become distinguishable from catalog-only evaluations against the currently-empty cloud_findings collection.

This plan also folds in PROV-01's small lockstep fix: the same three registration/validation gates accept oci/alibaba/cloudflare so the AddCloudAccountModal submit stops 400ing — done here because these gate files are shared and must be widened once, not twice.

Purpose: Make M365 and MongoDB Atlas first-class runnable CSPM providers and set up the provenance flag Plan 32-05 flips to false when it writes real findings.
Output: two catalog modules, the widened cloud_checks_service.py, the four-gate lockstep, and the extended expansion test suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-cloud-and-saas-provider-expansion/32-RESEARCH.md
@.planning/phases/32-cloud-and-saas-provider-expansion/32-PATTERNS.md
@.planning/phases/32-cloud-and-saas-provider-expansion/32-VALIDATION.md
@backend/cloud_checks_service.py
@backend/cloud_account_endpoints.py
@backend/tests/test_cloud_checks_expansion.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: M365 + MongoDB Atlas catalogs, wire into CLOUD_CHECKS + RUNNABLE_PROVIDERS</name>
  <files>backend/cloud_checks_m365.py, backend/cloud_checks_mongodb_atlas.py, backend/cloud_checks_service.py, backend/tests/test_cloud_checks_expansion.py</files>
  <read_first>
    - backend/cloud_checks_service.py (full — the import block lines 9-29, the CLOUD_CHECKS concatenation, the RUNNABLE_PROVIDERS tuple + _RUNNABLE_CHECKS_COUNT at lines 31-36, and the run_checks() body lines 63-110 that reads check[id]/[name]/[service]/[severity]/[frameworks]/[remediation]/[provider])
    - 32-PATTERNS.md sections "cloud_checks_m365.py, cloud_checks_mongodb_atlas.py" (the exact per-check dict shape) and "cloud_checks_service.py (MODIFIED)" (import + concat + RUNNABLE_PROVIDERS widening points)
    - 32-RESEARCH.md Architecture Patterns — Pattern 2 (recommended M365 and MongoDB Atlas check topics)
    - backend/tests/test_cloud_checks_expansion.py (full — the _chain/_mkdb helper convention and the test_run_checks_evaluates_kubernetes / test_coverage_denominator_includes_new_providers shapes to clone)
  </read_first>
  <behavior>
    - run_checks("acct","microsoft365","t") with empty cloud_findings returns ran == len(M365_CHECKS), no error.
    - run_checks("acct","mongodb_atlas","t") returns ran == len(MONGODB_ATLAS_CHECKS), no error.
    - _RUNNABLE_CHECKS_COUNT == len(CLOUD_CHECKS) still holds after the two providers are added.
    - "microsoft365" and "mongodb_atlas" are both in RUNNABLE_PROVIDERS.
  </behavior>
  <action>
    Create cloud_checks_m365.py exposing M365_CHECKS: List[Dict[str, Any]] and cloud_checks_mongodb_atlas.py exposing MONGODB_ATLAS_CHECKS: List[Dict[str, Any]]. Every entry must be a dict with all seven load-bearing keys read by run_checks(): id, name, description, provider, service, severity, frameworks (list), remediation. provider must be exactly "microsoft365" for M365 and "mongodb_atlas" for Atlas. Populate M365 checks from RESEARCH Pattern 2's recommended topics (MFA enforcement for admins, Conditional Access coverage, mailbox audit logging, external sharing restrictions, legacy-auth blocked, Secure Score baseline). Populate Atlas checks from RESEARCH Pattern 2's topics (no 0.0.0.0/0 network access entry, encryption-at-rest enabled, database auditing enabled, IP access list / VPC peering, least-privilege roles, backup/PITR). Give each a stable id prefix (e.g. m365-* / atlas-*), a severity, and framework tags matching the style of the existing catalogs.

    In cloud_checks_service.py: add `from cloud_checks_m365 import M365_CHECKS` and `from cloud_checks_mongodb_atlas import MONGODB_ATLAS_CHECKS`, append both to the CLOUD_CHECKS concatenation, and add "microsoft365" and "mongodb_atlas" to the RUNNABLE_PROVIDERS tuple (the _RUNNABLE_CHECKS_COUNT comprehension recomputes automatically). Make NO change to run_checks()'s evaluation logic in this task — it is provider-agnostic and works unmodified once the catalogs exist and the provider strings are registered.

    Extend test_cloud_checks_expansion.py: add test_run_checks_evaluates_microsoft365 and test_run_checks_evaluates_mongodb_atlas mirroring test_run_checks_evaluates_kubernetes (assert no error and ran == the respective catalog length), and extend test_coverage_denominator_includes_new_providers to also assert "microsoft365" in RUNNABLE_PROVIDERS, "mongodb_atlas" in RUNNABLE_PROVIDERS, and the denominator invariant still holds.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_cloud_checks_expansion.py -k "microsoft365 or mongodb_atlas or denominator" -x</automated>
  </verify>
  <acceptance_criteria>
    - Both catalog files exist with correctly-shaped dicts and the right provider strings.
    - CLOUD_CHECKS includes both new lists; RUNNABLE_PROVIDERS includes both new providers.
    - New eval tests and the extended denominator test pass.
  </acceptance_criteria>
  <done>M365 and MongoDB Atlas are runnable providers with catalogs, and the expansion tests for them are green.</done>
</task>

<task type="auto">
  <name>Task 2: Four-gate lockstep widening (M365/Atlas + oci/alibaba/cloudflare)</name>
  <files>backend/cloud_account_endpoints.py, backend/cloud_checks_endpoints.py, backend/mcp_server_endpoints.py, backend/tests/test_cloud_checks_expansion.py</files>
  <read_first>
    - backend/cloud_account_endpoints.py line 13 (_VALID_PROVIDERS set) and register_account()'s provider check
    - backend/cloud_checks_endpoints.py lines ~72-74 (the inline provider tuple in the /run route + its 400 message)
    - backend/mcp_server_endpoints.py lines ~24 and ~78 (the doc-string provider list and the validation tuple)
    - 32-PATTERNS.md "Provider-registration gate widening" shared pattern (the four-gate lockstep and the grep-before-done note)
    - 32-RESEARCH.md Open Questions #1 (resolved: PROV-01's _VALID_PROVIDERS 400-fix folded in via Phase 25 lockstep)
  </read_first>
  <action>
    Widen the CSPM provider gates in one lockstep edit. Add "microsoft365" and "mongodb_atlas" to: _VALID_PROVIDERS (cloud_account_endpoints.py line 13), the inline tuple in cloud_checks_endpoints.py's /run route (and update its 400 detail message accordingly), and both provider lists/tuples in mcp_server_endpoints.py (the doc-string at line 24 and the validation tuple at line 78). RUNNABLE_PROVIDERS in cloud_checks_service.py already received microsoft365/mongodb_atlas in Task 1, completing the fourth gate.

    Separately, add "oci", "alibaba", and "cloudflare" to the SAME three registration/validation gates (_VALID_PROVIDERS, cloud_checks_endpoints.py tuple, mcp_server_endpoints.py tuples) per PROV-01's lockstep fix, so AddCloudAccountModal submissions for those three stop 400ing. Do NOT add oci/alibaba/cloudflare to RUNNABLE_PROVIDERS — they are SIEM-ingest providers (handled by Plan 32-01's cloud_integrations subsystem) with no CSPM check catalog; run_checks correctly rejects them via the RUNNABLE_PROVIDERS message if a run is attempted, while registration now succeeds. Keep the string literals identical across every gate to avoid a half-registration.

    Extend test_cloud_checks_expansion.py with gate-parity assertions: import the gate collections and assert microsoft365 and mongodb_atlas appear in all four gates, and that oci/alibaba/cloudflare appear in the three registration/validation gates. Where a gate lives in an endpoint module that pulls heavy imports at load, assert against the module-level tuple/set directly (import the symbol) rather than spinning up a TestClient.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_cloud_checks_expansion.py -k "gate or provider" -x</automated>
  </verify>
  <acceptance_criteria>
    - microsoft365 and mongodb_atlas present in all four gates.
    - oci/alibaba/cloudflare present in the three registration/validation gates (not RUNNABLE_PROVIDERS).
    - Gate-parity tests pass; no gate accepts a provider another rejects (within the documented asymmetry for the catalog-less SIEM providers).
  </acceptance_criteria>
  <done>All gate widenings committed in lockstep with the same provider string literals, verified by test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Additive simulated provenance flag in run_checks() (no regression)</name>
  <files>backend/cloud_checks_service.py, backend/tests/test_cloud_checks_expansion.py</files>
  <read_first>
    - backend/cloud_checks_service.py run_checks() lines 63-110 (where findings_raw is fetched and the per-check doc is built + upserted into cloud_check_results)
    - 32-RESEARCH.md Open Questions #2 (resolved: real-findings ingestion; catalog-only evaluations against an empty findings set are the "simulated"-equivalent case) and Security Domain threat T-32-04
    - STATE.md decision "25-03: simulated field added purely additively (no fail-closed change)" — the precedent to match
  </read_first>
  <behavior>
    - run_checks with empty cloud_findings -> every result doc has simulated == True; PASS/FAIL and ran count unchanged from today.
    - run_checks with non-empty significant cloud_findings -> result docs have simulated == False.
    - kubernetes/digitalocean eval tests (existing) still pass with identical ran counts (no regression).
  </behavior>
  <action>
    In run_checks(), after fetching findings_raw, compute a provenance boolean once: `has_real_findings = len(findings_raw) > 0` (findings actually present in cloud_findings for this account/tenant). Add `"simulated": not has_real_findings` to the per-check doc dict that gets upserted into cloud_check_results. This is purely additive — do NOT alter the existing in_findings / result (PASS/FAIL) computation or the upsert key. Every existing provider (aws/azure/gcp/kubernetes/digitalocean) continues to evaluate exactly as before; they simply gain simulated=true today because cloud_findings is empty for them (matching the pre-existing always-PASS behavior, now labeled). When Plan 32-05 writes real M365/Atlas findings, those evaluations flip to simulated=false automatically.

    Extend test_cloud_checks_expansion.py: add a provenance test asserting simulated==True on results when the mocked cloud_findings.find returns [] and simulated==False when it returns a non-empty list of significant findings. Add a no-regression assertion confirming the kubernetes eval still returns the same ran count and PASS results as before (the simulated field is additive only).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_cloud_checks_expansion.py -x</automated>
  </verify>
  <acceptance_criteria>
    - cloud_check_results docs carry simulated (true when no real findings, false when present).
    - No change to PASS/FAIL logic, ran counts, or upsert keys for existing providers.
    - Provenance + no-regression tests pass; full expansion suite green.
  </acceptance_criteria>
  <done>The additive simulated flag is written by run_checks() and verified in both branches with no regression to existing providers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → cloud-accounts / cloud-checks / mcp API | provider string crosses here; must be allowlist-validated at every gate |
| run_checks() → cloud_findings | findings presence determines the simulated provenance label consumers trust |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-03 | Tampering | four CSPM provider gates | medium | mitigate | Widen all gates in one lockstep edit with identical string literals; gate-parity test asserts no gate accepts a provider another rejects |
| T-32-04 | Spoofing (of finding authenticity) | run_checks() result provenance | medium | mitigate | Additive simulated=true when cloud_findings empty, false when real findings present; test asserts both branches, so catalog-only PASSes are never presented as real-scan results |
| T-32-IV | Input Validation (V5) | register_account / run route provider field | low | mitigate | New provider strings validated by the same allowlist gates as existing providers; no bypass introduced |
</threat_model>

<artifacts>
## Artifacts this phase produces (Plan 32-02)

New catalog modules:
- `backend/cloud_checks_m365.py` — `M365_CHECKS: List[Dict]` (provider == microsoft365)
- `backend/cloud_checks_mongodb_atlas.py` — `MONGODB_ATLAS_CHECKS: List[Dict]` (provider == mongodb_atlas)

Modified:
- `backend/cloud_checks_service.py` — catalog imports + CLOUD_CHECKS concat + RUNNABLE_PROVIDERS (microsoft365, mongodb_atlas) + additive `simulated` flag in run_checks()
- `backend/cloud_account_endpoints.py` — `_VALID_PROVIDERS` += microsoft365, mongodb_atlas, oci, alibaba, cloudflare
- `backend/cloud_checks_endpoints.py` — /run provider tuple widened (same five) + 400 message
- `backend/mcp_server_endpoints.py` — provider doc-string + validation tuple widened
- `backend/tests/test_cloud_checks_expansion.py` — M365/Atlas eval, denominator, gate-parity, provenance, no-regression cases

New field emitted: `simulated: bool` on `cloud_check_results` docs
Collection read: `cloud_findings` (provenance source; written by Plan 32-05)
</artifacts>

<verification>
- `cd backend && python -m pytest tests/test_cloud_checks_expansion.py -x` — full expansion suite green
- `cd backend && python -m pytest tests/ -q` — no regression
</verification>

<success_criteria>
- M365 and MongoDB Atlas are runnable CSPM providers with catalogs and pass through all four gates.
- oci/alibaba/cloudflare no longer 400 on AddCloudAccountModal submit.
- run_checks() results carry an additive simulated provenance flag; existing providers unchanged.
</success_criteria>

<output>
Create `.planning/phases/32-cloud-and-saas-provider-expansion/32-02-SUMMARY.md` when done.
</output>
