---
phase: 32-cloud-and-saas-provider-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/requirements.txt
  - backend/oci_ingest.py
  - backend/alibaba_ingest.py
  - backend/cloudflare_ingest.py
  - backend/cloud_integrations_endpoints.py
  - backend/tests/test_cloud_integrations.py
autonomous: false
requirements: [PROV-01]
user_setup: []
must_haves:
  truths:
    - "test_integration() for oci_cloud_guard / alibaba_sas / cloudflare_zero_trust calls a real (mocked-in-test) SDK/HTTP client and ingests findings into security_events, returning a non-zero count when the mocked client returns alerts (PROV-01)"
    - "trigger_cloud_discovery() dispatches the same three new providers in lockstep with test_integration() — no provider pollable via one path but silently no-op via the other (PROV-01)"
    - "A malformed/exception-raising provider response never propagates past the poll function — it is swallowed and 0 is returned, exactly like azure_defender_ingest.py (T-32-02)"
    - "Every new provider secret field (oci_private_key, access_key_secret, cf_api_token) is a member of BOTH _SECRET_FIELDS and _mask_secrets()'s inline secret_keys set, so credentials are Fernet-encrypted at rest and masked in list_integrations() output (T-32-01)"
    - "The three new SUS-flagged packages are human-verified on PyPI before being declared in requirements.txt (T-32-SC)"
  artifacts:
    - "backend/oci_ingest.py with poll_oci_cloud_guard_problems(config, omni_tenant_id) -> int and an _OCI_SDK_AVAILABLE ImportError guard"
    - "backend/alibaba_ingest.py with poll_alibaba_sas_alerts(config, omni_tenant_id) -> int"
    - "backend/cloudflare_ingest.py with poll_cloudflare_zero_trust_events(config, omni_tenant_id) -> int"
    - "backend/cloud_integrations_endpoints.py with three new elif branches in BOTH dispatch blocks and the three secret fields added to both secret sets"
    - "backend/tests/test_cloud_integrations.py (new file — closes a pre-existing zero-coverage gap for this 481-line endpoint file)"
    - "backend/requirements.txt with oci, aliyun-python-sdk-core-v3, cloudflare, and an explicit msal line"
  key_links:
    - "POST /api/cloud-integrations/{id}/test -> test_integration() elif provider dispatch -> poll_<provider>_*() -> set_tenant_id() + db.security_events.insert_many()"
    - "POST /api/cloud-integrations/discover -> trigger_cloud_discovery() second dispatch block -> same poll_<provider>_*() functions"
    - "_encrypt_secrets()/_mask_secrets() -> _SECRET_FIELDS + secret_keys (both sets) -> oci_private_key/access_key_secret/cf_api_token"
---

<objective>
Close PROV-01: OCI Cloud Guard, Alibaba Security Center (SAS), and Cloudflare Zero Trust integrations currently store connection config but their test_integration() branch falls through to count=0 with no real call. Add three real-poll ingest modules that mirror azure_defender_ingest.py exactly (SDK-availability guard, required-field gate, try/except-return-0 never-raise contract, set_tenant_id() + db.security_events.insert_many()), wire them into BOTH dispatch blocks of cloud_integrations_endpoints.py, and close the credential-leak gap by registering every new secret field in both secret sets.

Purpose: Deliver the genuinely-new external-API integration half of this phase (the other three requirements are largely reuse/re-wiring). This is the only PROV requirement introducing new SDK dependencies, so it carries the package-legitimacy checkpoint and the credential-encryption threat mitigation.
Output: oci_ingest.py, alibaba_ingest.py, cloudflare_ingest.py, the widened cloud_integrations_endpoints.py dispatch + secret sets, the msal/oci/aliyun/cloudflare requirements.txt lines, and the first-ever test file for cloud_integrations_endpoints.py.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-cloud-and-saas-provider-expansion/32-RESEARCH.md
@.planning/phases/32-cloud-and-saas-provider-expansion/32-PATTERNS.md
@.planning/phases/32-cloud-and-saas-provider-expansion/32-VALIDATION.md
@backend/azure_defender_ingest.py
@backend/gcp_scc_ingest.py
@backend/cloud_integrations_endpoints.py
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy checkpoint + declare new dependencies</name>
  <what-built>
    RESEARCH.md's Package Legitimacy Audit flagged oci, aliyun-python-sdk-core-v3, and cloudflare as [SUS] (false-positive "too-new"/"unknown-downloads" seam signals — all three are long-lived vendor-owned SDKs cross-checked against the live PyPI registry). Per the audit disposition, these must get a human glance before landing in requirements.txt. msal is already an OK-verdict transitive dependency being promoted to an explicit line (no verification needed for it). Threat T-32-SC.
  </what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/oci/ — confirm it is Oracle's official OCI Python SDK (source repo github.com/oracle/oci-python-sdk), long version history.
    2. Open https://pypi.org/project/aliyun-python-sdk-core-v3/ — confirm it is Alibaba's official core SDK (github.com/aliyun/aliyun-openapi-python-sdk).
    3. Open https://pypi.org/project/cloudflare/ — confirm it is Cloudflare's official Python client (github.com/cloudflare/cloudflare-python).
    4. If all three are the legitimate vendor packages, approve. If any looks like a typosquat or an unfamiliar publisher, reject and stop.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed, or name the package to reject.</resume-signal>
  <action>
    After approval, append four lines to backend/requirements.txt (per PROV-01 for the three SDKs; the msal line is added here to keep requirements.txt single-owner, per PROV-02's M365 Graph auth need): oci, aliyun-python-sdk-core-v3, cloudflare, and msal. Pin each to the version RESEARCH.md recorded (oci, aliyun-python-sdk-core-v3, cloudflare, msal) but re-verify current latest with `pip index versions <pkg>` first since the research snapshot may be days stale. Do NOT run a global pip install as part of the commit — the ingest modules use ImportError guards and the tests mock the SDK, so nothing in this plan requires the packages to be physically installed to pass.
  </action>
  <verify>
    <automated>grep -Eq '^(oci|aliyun-python-sdk-core-v3|cloudflare|msal)\b' backend/requirements.txt && grep -c -E '^(oci|aliyun-python-sdk-core-v3|cloudflare|msal)\b' backend/requirements.txt | grep -qx 4 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Human approved the three SUS packages against their PyPI pages before the requirements.txt edit.
    - requirements.txt declares all four packages (oci, aliyun-python-sdk-core-v3, cloudflare, msal) each on its own line, version-pinned.
  </acceptance_criteria>
  <done>All four dependency lines present in requirements.txt after an explicit human approval of the three SUS SDKs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Three real-poll ingest modules cloning azure_defender_ingest.py + test scaffold</name>
  <files>backend/oci_ingest.py, backend/alibaba_ingest.py, backend/cloudflare_ingest.py, backend/tests/test_cloud_integrations.py</files>
  <read_first>
    - backend/azure_defender_ingest.py (full — the verbatim structural template: imports, ImportError SDK guard, _make_*_client helper, severity-map dict, _parse_*_alert returning the security_events doc shape, and the poll_* control flow)
    - backend/gcp_scc_ingest.py (poll_gcp_chronicle raw-REST-call variant — the model for Cloudflare if raw httpx is chosen instead of the SDK, and for Alibaba's access-key auth shape)
    - 32-PATTERNS.md sections "backend/oci_ingest.py, alibaba_ingest.py, cloudflare_ingest.py" and "backend/tests/test_cloud_integrations.py" (the poll_* contract and the test-harness clone note)
    - 32-RESEARCH.md Code Examples (azure_defender poll reference) + Pitfall 4 (secret-field encryption) + Security Domain threat table
    - backend/cloud_integrations_endpoints.py lines 132-166 (the SUPPORTED_PROVIDERS required_fields for the three providers — the exact config keys each poll function reads)
  </read_first>
  <behavior>
    - poll_oci_cloud_guard_problems(config, omni_tenant_id): with SDK-available patched True and a mocked client returning 2 problems, ingests 2 docs into security_events and returns 2.
    - poll_alibaba_sas_alerts(config, omni_tenant_id): same shape, mocked client returns 3 alerts -> returns 3.
    - poll_cloudflare_zero_trust_events(config, omni_tenant_id): mocked client/httpx returns 1 event -> returns 1.
    - Missing required config fields (empty all([...]) gate) -> returns 0 with no API call.
    - Mocked client raising an exception -> returns 0, no exception propagates (T-32-02).
  </behavior>
  <action>
    Create the three ingest modules, each cloning azure_defender_ingest.py's shape exactly. Each module has: (a) the standard imports (asyncio, logging, uuid, datetime, typing) plus `from database import get_database` and `from tenant_context import set_tenant_id`; (b) a module-level try/except ImportError guard setting _OCI_SDK_AVAILABLE / _ALIBABA_SDK_AVAILABLE / _CLOUDFLARE_SDK_AVAILABLE (Cloudflare may instead use raw httpx per RESEARCH Alternatives-Considered — prefer the SDK for consistency, but if raw httpx is chosen, follow gcp_scc_ingest.py's poll_gcp_chronicle shape and skip the guard); (c) a client-builder helper; (d) a small severity-map dict translating each vendor's severity vocabulary into this codebase's Critical/High/Medium/Low scale via `.get(value, "Medium")`; (e) a `_parse_<provider>_alert()` returning the exact security_events document shape (id, tenant_id, timestamp, log_type, title, description, severity, status, raw_message, source — provider-specific extras additive); (f) the async `poll_<provider>_<thing>(config, omni_tenant_id) -> int` function reading config fields via config.get(), gating on `all([...required fields...])` before any API call, wrapping the whole API-call-parse-insert body in try/except Exception that logs and returns 0 (NEVER raises — per PROV-01 and threat T-32-02), and on success calling set_tenant_id(omni_tenant_id) then `await db.security_events.insert_many(events)` and returning len(events). Read each provider's required_fields from SUPPORTED_PROVIDERS to know the exact config keys: OCI reads oci_tenancy_ocid/oci_user_ocid/oci_private_key/oci_fingerprint/oci_region; Alibaba reads access_key_id/access_key_secret/region_id; Cloudflare reads cf_account_id/cf_api_token.

    Create backend/tests/test_cloud_integrations.py. Clone the _col/_db/_user/_app helper block from tests/test_automation_and_baa.py verbatim (this file also becomes the first dedicated test for cloud_integrations_endpoints.py). Note cloud_integrations_endpoints.py imports its auth dependency from auth_utils (`from auth_utils import get_current_user`), NOT authentication_service — override that exact symbol when testing the endpoint later. For THIS task, add module-level tests that call the three poll_* functions directly with the SDK-availability constant patched True and the client builder mocked (so no real SDK install is needed): assert the ingested count equals the mocked alert count, assert set_tenant_id + insert_many were invoked with the correct tenant, assert the missing-fields path returns 0 with no client call, and assert a client that raises returns 0 (T-32-02). This test file is the Wave-0 scaffold every later PROV-01 assertion runs against.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_cloud_integrations.py -k "oci or alibaba or cloudflare" -x</automated>
  </verify>
  <acceptance_criteria>
    - Each ingest module exposes its poll_* function with the (config, omni_tenant_id) -> int signature, an SDK-availability guard, a required-field all([...]) gate, and a try/except-return-0 body that never raises.
    - The happy-path test ingests the mocked alert count into security_events; the missing-fields and exception tests both return 0.
    - Tests pass WITHOUT the real SDKs installed (availability constant patched, client mocked).
  </acceptance_criteria>
  <done>Three ingest modules exist and test_cloud_integrations.py's poll-level tests pass with mocked SDK clients.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire both dispatch blocks + widen both secret sets in cloud_integrations_endpoints.py</name>
  <files>backend/cloud_integrations_endpoints.py, backend/tests/test_cloud_integrations.py</files>
  <read_first>
    - backend/cloud_integrations_endpoints.py (full — the _SECRET_FIELDS frozenset at line 19, _mask_secrets()'s inline secret_keys set at line 179, the test_integration() dispatch ladder at lines ~329-343, and the SECOND dispatch block in trigger_cloud_discovery() at lines ~377-388)
    - 32-PATTERNS.md section "backend/cloud_integrations_endpoints.py (MODIFIED)" (both widening points, the lockstep note)
    - 32-RESEARCH.md Security Domain V6 Cryptography row + Pitfall 4 (do not introduce a fourth encryption scheme)
  </read_first>
  <behavior>
    - POST test for oci_cloud_guard/alibaba_sas/cloudflare_zero_trust returns the ingested count from the (patched) poll function rather than count=0.
    - list_integrations() output masks oci_private_key/access_key_secret/cf_api_token — the raw secret value never appears in the response.
    - trigger_cloud_discovery() accumulates real_count from the three new providers.
  </behavior>
  <action>
    In cloud_integrations_endpoints.py: add oci_private_key, access_key_secret, and cf_api_token to BOTH _SECRET_FIELDS (line 19) and the duplicate inline secret_keys set inside _mask_secrets() (line 179) — these two sets currently mirror each other and BOTH must be edited in lockstep or the credential leaks unmasked (threat T-32-01). Do not create any new Fernet instance; reuse the existing INTEGRATION_ENCRYPTION_KEY machinery (Pitfall 4). In test_integration()'s dispatch ladder, add three elif branches for provider == "oci_cloud_guard", "alibaba_sas", and "cloudflare_zero_trust", each doing a lazy inline import (`from oci_ingest import poll_oci_cloud_guard_problems`, etc.) and setting count = await poll_<provider>_*(config, integ.get("tenant_id", "")), matching the exact style of the existing azure_defender/gcp_scc branches. Add the SAME three elif branches to the SECOND dispatch block in trigger_cloud_discovery() (real_count += await poll_...) — both blocks must be widened or the provider is pollable via /test but silently no-op via /discover.

    Extend test_cloud_integrations.py with endpoint-level tests using the cloned TestClient harness (override auth_utils.get_current_user): assert POST /{id}/test for each of the three providers returns the mocked ingested count (patch the poll_* functions to return a known int), and assert that a config carrying oci_private_key/access_key_secret/cf_api_token comes back masked from list_integrations() (the plaintext value is absent from the serialized response).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_cloud_integrations.py -x</automated>
  </verify>
  <acceptance_criteria>
    - Both dispatch blocks (test_integration and trigger_cloud_discovery) contain the three new provider branches.
    - The three new secret field names are present in both _SECRET_FIELDS and _mask_secrets()'s secret_keys set.
    - A masking test proves the raw secret values never appear in list_integrations() output.
  </acceptance_criteria>
  <done>Full test_cloud_integrations.py suite green; both dispatch blocks and both secret sets widened.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tenant admin → cloud_integrations API | Provider config incl. credentials crosses here; must be RBAC-scoped and encrypted at rest |
| backend → external provider API (OCI/Alibaba/Cloudflare) | Untrusted upstream response parsed and inserted into security_events |
| requirements.txt → developer environment | New third-party packages enter the dependency graph |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-01 | Information Disclosure | _SECRET_FIELDS / _mask_secrets() in cloud_integrations_endpoints.py | high | mitigate | Add oci_private_key/access_key_secret/cf_api_token to BOTH sets; masking test asserts plaintext never returned by list_integrations() |
| T-32-02 | Tampering / DoS | poll_*() parse-and-insert of upstream response | medium | mitigate | Wrap whole body in try/except Exception: log + return 0 (clone azure_defender_ingest.py); required-field gate before any call; test asserts a raising client returns 0 |
| T-32-SC | Tampering (supply chain) | oci / aliyun-python-sdk-core-v3 / cloudflare npm-equiv pip installs | high | mitigate | Task 1 blocking-human checkpoint verifies each package on pypi.org before it lands in requirements.txt; never auto-approvable |
</threat_model>

<artifacts>
## Artifacts this phase produces (Plan 32-01)

New modules:
- `backend/oci_ingest.py` — `poll_oci_cloud_guard_problems(config, omni_tenant_id) -> int`, `_OCI_SDK_AVAILABLE` guard, `_parse_oci_problem()` helper
- `backend/alibaba_ingest.py` — `poll_alibaba_sas_alerts(config, omni_tenant_id) -> int`, `_ALIBABA_SDK_AVAILABLE` guard
- `backend/cloudflare_ingest.py` — `poll_cloudflare_zero_trust_events(config, omni_tenant_id) -> int`, `_CLOUDFLARE_SDK_AVAILABLE` guard (or raw httpx variant)
- `backend/tests/test_cloud_integrations.py` — new dedicated test file (closes pre-existing zero-coverage gap)

Modified:
- `backend/cloud_integrations_endpoints.py` — three elif branches in `test_integration()` and `trigger_cloud_discovery()`; secret fields added to `_SECRET_FIELDS` + `_mask_secrets()`
- `backend/requirements.txt` — `oci`, `aliyun-python-sdk-core-v3`, `cloudflare`, `msal`

Collection written: `security_events` (existing sink, same as azure_defender_ingest.py)
</artifacts>

<verification>
- `cd backend && python -m pytest tests/test_cloud_integrations.py -x` — full new test file green
- `cd backend && python -m pytest tests/ -q` — no regression across the suite
- Package checkpoint approved before any requirements.txt credential-bearing dependency lands
</verification>

<success_criteria>
- OCI/Alibaba/Cloudflare test_integration() and trigger_cloud_discovery() both invoke real (mocked-in-test) poll functions and ingest into security_events.
- No poll function raises on a malformed response — all return 0.
- Every new secret field is encrypted at rest and masked in output.
- Package legitimacy checkpoint completed before dependencies declared.
</success_criteria>

<output>
Create `.planning/phases/32-cloud-and-saas-provider-expansion/32-01-SUMMARY.md` when done.
</output>
