---
phase: 49-fleet-geo-map
plan: 04
type: execute
wave: 2
depends_on: ["49-01", "49-02", "49-03"]
files_modified:
  - components/FleetGeoMap.tsx
  - utils/fleetClustering.ts
  - src/__tests__/fleetClustering.test.ts
requirements: [GMAP-01, GMAP-02, GMAP-03]
autonomous: true
must_haves:
  truths:
    - "FleetGeoMap renders the air-gapped world backdrop and plots located agents as status-colored markers via projectLatLon (GMAP-01)"
    - "Markers whose projected positions share a fixed pixel-grid cell collapse into one cluster marker with a count badge (GMAP-02)"
    - "A tenant filter (from response.tenants) and a status multi-toggle filter the plotted set before clustering (GMAP-02)"
    - "Clicking a single marker opens a drill-down panel showing hostname, LAN IP, public IP, resolved location, and status from the payload (GMAP-03)"
    - "Agents with geo=null render as an off-map 'Unlocated (N)' count, honoring active filters (D-07)"
    - "The component makes no external network/tile/CDN request — the map is fully self-contained"
  artifacts:
    - path: "components/FleetGeoMap.tsx"
      provides: "Fleet geo map component (basemap + markers + clustering + filters + drill-down)"
      contains: "projectLatLon"
      min_lines: 80
    - path: "utils/fleetClustering.ts"
      provides: "grid-bucket clustering over projected agent positions"
      contains: "clusterAgents"
      min_lines: 20
  key_links:
    - from: "components/FleetGeoMap.tsx"
      to: "services/apiService.ts"
      via: "fetchFleetGeo() on mount"
      pattern: "fetchFleetGeo"
    - from: "components/FleetGeoMap.tsx"
      to: "components/worldMapAsset.ts"
      via: "renders WORLD_BACKDROP_SVG in the projection viewBox"
      pattern: "WORLD_BACKDROP_SVG"
    - from: "components/FleetGeoMap.tsx"
      to: "utils/fleetClustering.ts"
      via: "clusterAgents(filtered, cell)"
      pattern: "clusterAgents"
  prohibitions:
    - "MUST NOT reference any http(s) URL, tile server, CDN, or remote image"
    - "MUST NOT recompute agent status/offline on the client — render the payload status"
    - "MUST NOT drop unlocated agents — show them as an Unlocated (N) count (D-07)"
    - "MUST NOT add a map/clustering npm dependency"
---

<objective>
Deliver the GMAP-01/02/03 surface: `FleetGeoMap.tsx` renders the air-gapped world backdrop (49-03) with fleet agents plotted as status-colored markers (49-03 projection over 49-02 data), collapses dense cells into counted clusters, filters by tenant + status, and drills a clicked marker into hostname / LAN IP / public IP / resolved location / status. Clustering math lives in `utils/fleetClustering.ts` to keep the component under 500 lines.

Purpose: this is the milestone's highest-visibility surface and the payload already carries every drill-down field (D-06), so the component is pure presentation over `/api/fleet/geo`.
Output: `FleetGeoMap.tsx` + `utils/fleetClustering.ts` + clustering unit tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@.planning/phases/49-fleet-geo-map/49-RESEARCH.md
@.planning/phases/49-fleet-geo-map/49-CONTEXT.md

# Read-first source anchors:
@components/FleetObservabilityDashboard.tsx   # 48-05 dashboard shell/styling + loading/empty/error states to mirror
@components/AgentList.tsx                      # the existing agent-status color convention (D-05) + geo label usage
@utils/geo.ts                                 # flagEmoji + formatGeo for the drill-down location label
@utils/worldMap.ts                            # projectLatLon, MAP_VIEW_W/H (49-03)
@components/worldMapAsset.ts                  # WORLD_BACKDROP_SVG, WORLD_VIEWBOX (49-03)
@services/apiService.ts                       # fetchFleetGeo (49-02)
</execution_context>

<artifacts>
## Artifacts This Plan Produces
- NEW `utils/fleetClustering.ts` — `clusterAgents(agents, cellPx)` grid-bucket clustering over projected positions.
- NEW `components/FleetGeoMap.tsx` — the map page (named export `FleetGeoMap`).
- NEW `src/__tests__/fleetClustering.test.ts` — clustering unit tests.
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: grid-bucket clustering util</name>
  <files>utils/fleetClustering.ts, src/__tests__/fleetClustering.test.ts</files>
  <read_first>utils/worldMap.ts for projectLatLon; types.ts FleetGeoAgent; an existing src/__tests__ file for vitest style.</read_first>
  <action>Create `utils/fleetClustering.ts` exporting `export interface GeoCluster { x: number; y: number; agents: FleetGeoAgent[]; count: number; worstStatus: AgentStatus; }` and `export function clusterAgents(agents: FleetGeoAgent[], cellPx: number, width: number, height: number): GeoCluster[]`. For each located agent (geo != null), project to px via `projectLatLon(lat, lon, width, height)`, bucket by `key = `${Math.floor(x/cellPx)}:${Math.floor(y/cellPx)}``, and emit one GeoCluster per non-empty bucket whose `x/y` is the mean of its members, `count = agents.length`, and `worstStatus` by precedence Quarantined > Error > Offline > Online. Deterministic ordering. Add `src/__tests__/fleetClustering.test.ts` (vitest, importing from `../../utils/fleetClustering`): two agents at nearly-identical lat/lon collapse to one cluster with count 2; two far-apart agents yield two clusters; `worstStatus` picks the worst; empty input → []; unlocated agents are ignored.</action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5 && npx vitest run src/__tests__/fleetClustering.test.ts 2>&1 | tail -12</automated>
  </verify>
  <done>Clustering compiles; tests pass (collapse, split, worst-status, empty, unlocated-ignored).</done>
</task>

<task type="auto">
  <name>Task 2: FleetGeoMap component — backdrop + markers + filters + clustering + drill-down</name>
  <files>components/FleetGeoMap.tsx</files>
  <read_first>components/FleetObservabilityDashboard.tsx (shell + loading/empty/error), components/AgentList.tsx (status color map, D-05), utils/geo.ts (formatGeo/flagEmoji).</read_first>
  <action>Create `components/FleetGeoMap.tsx` with named export `FleetGeoMap`. On mount call `fetchFleetGeo()` with loading / error / empty states cloned from FleetObservabilityDashboard. Render an `<svg viewBox={WORLD_VIEWBOX}>` (responsive, `width:100%`) with `WORLD_BACKDROP_SVG` injected as the first layer (via `dangerouslySetInnerHTML` on a `<g>` — the string is a bundled constant, not user input). Derive the status color from the SAME convention AgentList uses — its `statusInfo` map (Online/Offline/Error/Quarantined) is a local non-exported `const`, so either lift it into a shared `utils/agentStatus.ts` and import it in both, or replicate its exact color values verbatim; do NOT invent a new palette (D-05). State: `tenantFilter` (default 'all', options from `response.tenants`) and `statusFilter` (a Set of enabled statuses, all on by default). Compute `filtered = response.agents` minus tenant/status mismatches; split into located (geo != null) and unlocated; `clusters = clusterAgents(located, CELL, MAP_VIEW_W, MAP_VIEW_H)`. Plot each cluster: `count === 1` → a status-colored `<circle>` at (x,y); `count > 1` → a larger cluster `<circle>` in `worstStatus` color with a `<text>` count badge. Clicking a single-agent marker sets `selected` and opens a side/overlay drill-down panel showing `hostname`, LAN `ipAddress`→"LAN IP", `publicIp`→"Public IP", location `${flagEmoji(geo.country_code)} ${formatGeo(geo)}`, and a status badge (GMAP-03); clicking a multi-agent cluster expands a compact list of its agents to pick one. Show an "Unlocated (N)" chip (N = filtered unlocated count, D-07) and a small legend of the status colors. Keep the file under 500 lines — clustering is already extracted; extract a `<StatusBadge>`/`<DrillDownPanel>` helper within the file if needed but do not add new files beyond those declared.</action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5 && npm run build 2>&1 | tail -3 && grep -nE "http|tile|cdn|<img|fetch\('https|maps\." components/FleetGeoMap.tsx || echo "AIRGAP-OK"</automated>
    <human-check>Open the Fleet Geo Map. Confirm: the world backdrop renders with network throttled/offline (no tile requests in the Network tab); agents appear as status-colored markers positioned by location; dense areas collapse to counted clusters; the tenant dropdown and status toggles filter the map; clicking a marker shows hostname/LAN IP/public IP/location/status; unlocated agents show as an "Unlocated (N)" chip.</human-check>
  </verify>
  <done>Component typechecks + builds; renders backdrop, status markers, clusters, tenant/status filters, drill-down, unlocated count; no external network refs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → GET /api/fleet/geo | Authenticated; server enforces tenant/role (49-01) |
| bundled backdrop string → dangerouslySetInnerHTML | Trusted build-time constant (worldMapAsset.ts), never user/network data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-04 | Information Disclosure | outbound tile/CDN request | mitigate | Fully self-contained inline SVG; grep gate in Task 2 verify + prohibitions |
| T-49-05 | Tampering (XSS) | dangerouslySetInnerHTML on the backdrop | mitigate | Source is a bundled constant string (worldMapAsset.ts), not payload/user input; agent-derived text rendered as JSX text nodes, never as HTML |
| T-49-06 | Information Disclosure | client showing cross-tenant agents to a non-admin | mitigate | Server scopes the payload (49-01); nav gated by manage:agents (49-05); client only filters what the server already authorized |
| T-49-SC | Tampering | package installs | accept | No new packages this plan |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; `npm run build` succeeds; clustering tests pass.
- `grep` for external map references in FleetGeoMap.tsx returns nothing.
- Manual UAT (end-of-phase): backdrop renders offline; markers positioned; clusters counted; tenant/status filters work; drill-down shows the 5 fields; unlocated count shown.
</verification>

<success_criteria>
GMAP-01/02/03 delivered: an air-gapped fleet map plots status-colored agent markers by location, collapses dense cells into counted clusters, filters by tenant and status, drills a marker into hostname/LAN IP/public IP/resolved location/status, and surfaces unlocated agents as a count — with no external network dependency.
</success_criteria>

<output>
Create `.planning/phases/49-fleet-geo-map/49-04-SUMMARY.md` when done.
</output>
