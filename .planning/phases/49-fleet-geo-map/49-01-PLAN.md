---
phase: 49-fleet-geo-map
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/agent_fleet_geo_endpoints.py
  - backend/router_registry.py
  - backend/tests/test_agent_fleet_geo.py
autonomous: true
requirements: [GMAP-02, GMAP-03]
must_haves:
  truths:
    - "GET /api/fleet/geo returns the fleet's agents with map-relevant fields (id, hostname, status, tenantId, lanIp, publicIp, geo{city,country,country_code,latitude,longitude})"
    - "A super-admin sees agents across all tenants; a non-super-admin's query is scoped to their own tenantId (clones 48-03 gating)"
    - "Agents missing lat/lon are still returned (geo=null) and counted as unlocated, never dropped (D-07)"
    - "The response carries located_count, unlocated_count, total, and the distinct tenants list for the client tenant filter"
    - "The handler reads the request-scoped wrapped get_database(), never db._db (T-48-07 cross-tenant leak)"
  artifacts:
    - path: "backend/agent_fleet_geo_endpoints.py"
      provides: "GET /api/fleet/geo cross-tenant fleet-geo aggregate endpoint"
      contains: "/geo"
      min_lines: 40
    - path: "backend/router_registry.py"
      provides: "registration of agent_fleet_geo_endpoints router"
      contains: "agent_fleet_geo_endpoints"
    - path: "backend/tests/test_agent_fleet_geo.py"
      provides: "hermetic tests for tenant gating + located/unlocated counting + projection"
      contains: "fleet/geo"
      min_lines: 60
  key_links:
    - from: "backend/router_registry.py"
      to: "backend/agent_fleet_geo_endpoints.py"
      via: "_load(app, 'agent_fleet_geo_endpoints', 'router')"
      pattern: "agent_fleet_geo_endpoints"
  prohibitions:
    - "MUST NOT access db._db in the handler (use wrapped get_database() + is_super_admin) — T-48-07"
    - "MUST NOT add a new offline/status heuristic — return the stored status verbatim"
    - "MUST NOT drop agents lacking coordinates — return them as unlocated (D-07)"
---

<objective>
Deliver the GMAP-02/03 data source: a thin, admin-facing cross-tenant `GET /api/fleet/geo` aggregate that returns each agent's map-relevant fields (identity, status, tenant, LAN/public IP, resolved geo with lat/lon), cloning the 48-03 `agent_fleet_observability_endpoints` tenant-gating verbatim (super-admin → whole fleet; else own tenant). The client does all clustering/filtering; this endpoint does RBAC + tenant scoping + projection only.

Purpose: GMAP-02's tenant filter requires a cross-tenant admin view, and GMAP-03's drill-down needs LAN/public IP + resolved location — the existing per-caller `/api/agents` list is tenant-scoped and shaped for a different view. D-02 locks a dedicated endpoint mirroring the proven 48-03 pattern.
Output: `agent_fleet_geo_endpoints.py` + router registration + hermetic tests (failing-first).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@.planning/phases/49-fleet-geo-map/49-RESEARCH.md
@.planning/phases/49-fleet-geo-map/49-CONTEXT.md

# Read-first source anchors (the exact clone target):
@backend/agent_fleet_observability_endpoints.py   # 48-03: router prefix, tenant gating, _PROJECTION/_project, wrapped db
@backend/router_registry.py                        # line ~281 _load(...) for the fleet-observability router — add ours after it
@backend/tests/test_agent_fleet_observability.py   # hermetic test harness: mocked async cursor, dependency_overrides
</execution_context>

<artifacts>
## Artifacts This Plan Produces
- NEW `backend/agent_fleet_geo_endpoints.py` — `GET /api/fleet/geo`, `APIRouter(prefix="/api/fleet", tags=["Fleet Geo Map"])`.
- EDIT `backend/router_registry.py` — `_load(app, "agent_fleet_geo_endpoints", "router")` after the fleet-observability load.
- NEW `backend/tests/test_agent_fleet_geo.py` — hermetic tenant-gating + counting + projection tests.
</artifacts>

<tasks>

<task type="tdd">
  <name>Task 1: failing tests for GET /api/fleet/geo (tenant gating, counting, projection)</name>
  <files>backend/tests/test_agent_fleet_geo.py</files>
  <read_first>backend/tests/test_agent_fleet_observability.py — clone its `_AsyncCursor`, `_client(router, user)`, `_user(role, tenant_id)`, and `dependency_overrides[get_current_user]` harness verbatim.</read_first>
  <action>Write hermetic tests (no real Mongo, no network) importing `router` from `agent_fleet_geo_endpoints`. Cases: (a) super-admin (`role="super_admin"`) → the mocked `db.agents.find` receives an empty query (no tenantId filter) and the response includes agents from >1 tenant. (b) non-super-admin (`role="security_analyst", tenant_id="tenant-a"`) → `find` is called with a query containing `tenantId == "tenant-a"`. (c) counting: given 2 agents with lat/lon and 1 without, `located_count == 2`, `unlocated_count == 1`, `total == 3`, and the coordinate-less agent is present with `geo` null/None. (d) projection: each returned agent carries `id, hostname, status, tenantId, lanIp, publicIp` and (when located) `geo.latitude/longitude/city/country/country_code`. (e) `tenants` is the distinct sorted list of tenantIds in the result. Assert on the query passed to the mocked `find` (capture its call args) for (a)/(b).</action>
  <verify>
    <automated>backend/venv/bin/python -m pytest backend/tests/test_agent_fleet_geo.py -q 2>&1 | tail -15</automated>
  </verify>
  <done>Tests exist and FAIL (module/endpoint not yet implemented) — red state confirmed.</done>
</task>

<task type="auto">
  <name>Task 2: implement GET /api/fleet/geo endpoint + register router</name>
  <files>backend/agent_fleet_geo_endpoints.py, backend/router_registry.py</files>
  <read_first>backend/agent_fleet_observability_endpoints.py — mirror the router prefix, `get_current_user` dep, `is_super_admin` gate, wrapped `get_database()`, and `_PROJECTION`/`_project` shape.</read_first>
  <action>Create `agent_fleet_geo_endpoints.py`: `from database import get_database`, `from authentication_service import get_current_user`, `from rbac_utils import is_super_admin`. `router = APIRouter(prefix="/api/fleet", tags=["Fleet Geo Map"])`. Define `_PROJECTION = {"_id":0,"id":1,"hostname":1,"status":1,"tenantId":1,"ipAddress":1,"publicIp":1,"geo":1}`. `_project(agent)` returns `{id, hostname, status, tenantId, lanIp: agent.get("ipAddress"), publicIp: agent.get("publicIp"), geo: _geo(agent.get("geo"))}` where `_geo(g)` returns `None` when `g` is falsy or lacks numeric `latitude`&`longitude`, else `{city, country, country_code, latitude, longitude}`. Handler `@router.get("/geo")` `async def get_fleet_geo(current_user=Depends(get_current_user))`: `db = get_database()` (NEVER `db._db` — T-48-07); `query = {}`; `if not is_super_admin(getattr(current_user,"role",None)): query["tenantId"] = getattr(current_user,"tenant_id",None)`; `agents = await db.agents.find(query, _PROJECTION).to_list(length=None)`; project all; located = those whose projected `geo is not None`; return `{"agents": projected, "total": len(projected), "located_count": len(located), "unlocated_count": len(projected)-len(located), "tenants": sorted({a["tenantId"] for a in projected if a.get("tenantId")})}`. Then add `_load(app, "agent_fleet_geo_endpoints", "router")` immediately after the fleet-observability `_load` in `backend/router_registry.py`.</action>
  <verify>
    <automated>backend/venv/bin/python -m pytest backend/tests/test_agent_fleet_geo.py -q 2>&1 | tail -15</automated>
  </verify>
  <done>All Task 1 tests pass; router registered via router_registry._load.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → GET /api/fleet/geo | Authenticated user; server enforces tenant scope + role (clones 48-03) |
| get_database() wrapper | Request-scoped tenant enforcement; bypassed only by raw db._db (forbidden) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-01 | Information Disclosure | cross-tenant agent leak via raw db access | mitigate | Wrapped get_database() + is_super_admin gate; test (b) asserts tenantId filter for non-super-admins (T-48-07 clone) |
| T-49-02 | Information Disclosure | private geo/IP exposed to under-privileged caller | mitigate | Same tenant/role gate; nav visibility additionally gated by manage:agents (49-05) |
| T-49-SC | Tampering | package installs | accept | No new packages this plan |
</threat_model>

<verification>
- `pytest backend/tests/test_agent_fleet_geo.py` green (tenant gating, located/unlocated counting, projection, tenants list).
- Full backend suite shows no new failures beyond the documented pre-existing set.
</verification>

<success_criteria>
GMAP-02/03 data source exists: `GET /api/fleet/geo` returns cross-tenant (super-admin) or tenant-scoped (else) agents with identity, status, LAN/public IP, and resolved lat/lon geo; unlocated agents are surfaced not dropped; registered via router_registry; hermetically tested; no db._db access.
</success_criteria>

<output>
Create `.planning/phases/49-fleet-geo-map/49-01-SUMMARY.md` when done.
</output>
