---
phase: 49-fleet-geo-map
plan: 05
type: execute
wave: 3
depends_on: ["49-02", "49-04"]
files_modified:
  - App.tsx
  - components/Sidebar.tsx
autonomous: true
requirements: [GMAP-01]
must_haves:
  truths:
    - "A new admin-gated 'Fleet Geo Map' nav entry is reachable from the sidebar"
    - "The entry + view are gated by the manage:agents permission (D-03)"
    - "Selecting it renders FleetGeoMap inside an ErrorBoundary"
    - "Users without manage:agents do not see the entry and cannot reach the view"
  artifacts:
    - path: "App.tsx"
      provides: "lazy import + manage:agents permission map entry + render switch case for fleetGeoMap"
      contains: "FleetGeoMap"
    - path: "components/Sidebar.tsx"
      provides: "Fleet Geo Map nav entry gated by manage:agents"
      contains: "fleetGeoMap"
  key_links:
    - from: "App.tsx"
      to: "components/FleetGeoMap.tsx"
      via: "lazy import + permission map (manage:agents) + render switch case"
      pattern: "FleetGeoMap"
    - from: "components/Sidebar.tsx"
      to: "App.tsx"
      via: "nav entry view:'fleetGeoMap' permission:'manage:agents'"
      pattern: "fleetGeoMap"
  prohibitions:
    - "MUST NOT gate the entry/view on anything other than manage:agents (D-03)"
    - "MUST NOT modify the existing geographicMap entry or its permission"
---

<objective>
Register the fleet geo map into navigation by cloning the 48-05 four-file pattern (here three files, since the `fleetGeoMap` AppView member already landed in 49-02): App.tsx lazy import + permission map + render switch case, and a Sidebar entry — all gated by `manage:agents`, using the existing `GlobeIcon`.

Purpose: D-03 locks the same admin-gated registration used for the Phase 47 security panel and the 48-05 fleet-observability page. This makes GMAP-01's map reachable only to `manage:agents` holders.
Output: the nav registration wiring FleetGeoMap into the app.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@.planning/phases/49-fleet-geo-map/49-RESEARCH.md
@.planning/phases/49-fleet-geo-map/49-CONTEXT.md

# Read-first source anchors (the 48-05 clone target — exact pattern):
@App.tsx                     # line ~166 lazy import (FleetObservabilityDashboard), ~369 permission map (fleetObservability: 'manage:agents'), ~1915 render switch case
@components/Sidebar.tsx      # line ~417 the fleetObservability nav entry to clone
@components/icons.tsx        # GlobeIcon (line ~187)
</execution_context>

<artifacts>
## Artifacts This Plan Produces
- EDIT `App.tsx` — lazy import for `FleetGeoMap`, `fleetGeoMap: 'manage:agents'` permission entry, and a `case 'fleetGeoMap'` render.
- EDIT `components/Sidebar.tsx` — one nav entry `fleetGeoMap` gated by `manage:agents`, `GlobeIcon`.
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: App.tsx registration (lazy import + permission + switch case)</name>
  <files>App.tsx</files>
  <read_first>App.tsx lines ~166 / ~369 / ~1915 — the fleetObservability lazy import, permission-map entry, and switch case. Clone each, swapping the view key + component.</read_first>
  <action>Add near the fleetObservability lazy import: `const FleetGeoMap = lazy(() => import('./components/FleetGeoMap').then(m => ({ default: m.FleetGeoMap })));`. Add `fleetGeoMap: 'manage:agents'` to the permission map next to `fleetObservability`. Add `case 'fleetGeoMap': return <ErrorBoundary name="FleetGeoMap"><FleetGeoMap /></ErrorBoundary>;` in the render switch next to the fleetObservability case. Permission MUST be `manage:agents` and nothing else (D-03). Do NOT touch the geographicMap case.</action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5 && npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>App.tsx builds; fleetGeoMap lazy-imported, permission-mapped to manage:agents, and rendered in an ErrorBoundary.</done>
</task>

<task type="auto">
  <name>Task 2: Sidebar nav entry</name>
  <files>components/Sidebar.tsx</files>
  <read_first>components/Sidebar.tsx line ~417 the fleetObservability entry; components/icons.tsx GlobeIcon import.</read_first>
  <action>Add a nav entry next to the fleetObservability row: `{ view: 'fleetGeoMap', label: 'Fleet Geo Map', icon: <GlobeIcon size={20} />, permission: 'manage:agents' }`. Ensure `GlobeIcon` is imported (add to the existing icons import if absent). Permission MUST be `manage:agents` (D-03).</action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5 && npm run build 2>&1 | tail -3</automated>
    <human-check>As an admin with manage:agents, confirm the "Fleet Geo Map" sidebar entry appears and opens the map. As a user without manage:agents, confirm the entry is hidden and the view is unreachable.</human-check>
  </verify>
  <done>Sidebar entry present, gated by manage:agents, opens the map; app builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nav/view gating | Client-side manage:agents gate hides the entry; the /api/fleet/geo endpoint remains the server-side authority (49-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-07 | Elevation of Privilege | non-admin reaching the fleet map | mitigate | Nav + view gated by manage:agents (D-03); endpoint independently enforces tenant/role (49-01) — client gate is convenience, server is authority |
| T-49-SC | Tampering | package installs | accept | No new packages this plan |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; `npm run build` succeeds.
- Manual UAT (end-of-phase): Fleet Geo Map reachable for manage:agents holders, hidden/unreachable otherwise; geographicMap entry unchanged.
</verification>

<success_criteria>
The fleet geo map is registered into navigation: a `manage:agents`-gated "Fleet Geo Map" sidebar entry (GlobeIcon) opens `FleetGeoMap` in an ErrorBoundary; users without the permission never see or reach it; the existing geographicMap entry is untouched.
</success_criteria>

<output>
Create `.planning/phases/49-fleet-geo-map/49-05-SUMMARY.md` when done.
</output>
