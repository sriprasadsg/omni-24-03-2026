---
phase: 49-fleet-geo-map
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - utils/worldMap.ts
  - components/worldMapAsset.ts
  - src/__tests__/worldMap.test.ts
requirements: [GMAP-01]
autonomous: true
must_haves:
  truths:
    - "utils/worldMap.ts exports a projection mapping (lat, lon) to equirectangular coordinates: x = lon + 180, y = 90 - lat, scalable to any width/height"
    - "components/worldMapAsset.ts exports a self-contained world backdrop (public-domain land-outline SVG string, or a graticule fallback) rendered in a viewBox 0 0 360 180 coordinate space"
    - "No external network/tile/CDN reference exists in either file (fully air-gapped)"
  artifacts:
    - path: "utils/worldMap.ts"
      provides: "projectLatLon(lat, lon, width, height) equirectangular projection"
      contains: "projectLatLon"
      min_lines: 15
    - path: "components/worldMapAsset.ts"
      provides: "self-contained world backdrop (land-outline SVG string or graticule) in viewBox 0 0 360 180"
      contains: "viewBox"
      min_lines: 10
  key_links:
    - from: "components/worldMapAsset.ts"
      to: "utils/worldMap.ts"
      via: "shared 360x180 equirectangular coordinate space (markers project into the same viewBox)"
      pattern: "360"
  prohibitions:
    - "MUST NOT reference any http(s) URL, tile server, CDN, or remote image in either file"
    - "MUST NOT add a map/projection npm dependency (d3-geo, topojson-client, leaflet)"
---

<objective>
Deliver the two air-gapped building blocks for GMAP-01: an equirectangular projection helper and a self-contained world backdrop. Both live in the same `viewBox="0 0 360 180"` coordinate space so markers layered over the backdrop align with the land automatically — no library, no trig, no network.

Purpose: D-01 locks a bundled-SVG + linear equirectangular approach (`x = lon+180`, `y = 90-lat`). Splitting these out keeps the map component (49-04) lean under the 500-line rule and makes the projection unit-testable in isolation.
Output: `utils/worldMap.ts` (projection) + `components/worldMapAsset.ts` (backdrop) + a projection unit test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@.planning/phases/49-fleet-geo-map/49-RESEARCH.md
@.planning/phases/49-fleet-geo-map/49-CONTEXT.md

# Read-first source anchors:
@components/NetworkTopologyMap.tsx   # repo convention for inline-SVG / data-URI, air-gapped rendering
</execution_context>

<artifacts>
## Artifacts This Plan Produces
- NEW `utils/worldMap.ts` — `projectLatLon(lat, lon, width, height)` + the 360×180 constants.
- NEW `components/worldMapAsset.ts` — exported world backdrop string/markup in the shared coordinate space.
- NEW `src/__tests__/worldMap.test.ts` — projection unit tests (corners + centre).
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: equirectangular projection helper</name>
  <files>utils/worldMap.ts</files>
  <action>Create `utils/worldMap.ts` exporting `export const MAP_VIEW_W = 360; export const MAP_VIEW_H = 180;` and `export function projectLatLon(lat: number, lon: number, width = MAP_VIEW_W, height = MAP_VIEW_H): { x: number; y: number }` returning `{ x: ((lon + 180) / 360) * width, y: ((90 - lat) / 180) * height }`. Clamp `lat` to [-90,90] and `lon` to [-180,180] before projecting (defensive — bad coords must not throw). Add a short doc comment with the formula and the north-up note.</action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>Projection compiles; clamps out-of-range input.</done>
</task>

<task type="auto">
  <name>Task 2: self-contained world backdrop</name>
  <files>components/worldMapAsset.ts</files>
  <read_first>components/NetworkTopologyMap.tsx for the inline-SVG string convention.</read_first>
  <action>Create `components/worldMapAsset.ts` exporting `WORLD_BACKDROP_SVG` — an SVG **inner markup string** authored for `viewBox="0 0 360 180"` (so it shares the projection's coordinate space). Preferred: paste a compact (<80 KB) **public-domain** equirectangular world land-outline (Natural Earth 1:110m is public domain) as `<path d="…" />` fills. If a vetted public-domain outline is not available to bundle offline, ship the guaranteed **graticule fallback**: a group of `<line>` gridlines every 30° of longitude (x = 0,30,…,360) and latitude (y = 0,30,…,180), with the equator (y=90) and prime meridian (x=180) emphasized, plus a faint full-canvas `<rect>` ocean fill. Either way the file contains ONLY inline SVG markup — no `<img>`, no `http(s)` URL, no `fetch`. Export also `WORLD_VIEWBOX = "0 0 360 180"`.</action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5 && grep -nE "http|fetch\(|tile|cdn|<img" components/worldMapAsset.ts || echo "AIRGAP-OK: no external refs"</automated>
  </verify>
  <done>Backdrop compiles; grep confirms zero external references (air-gapped).</done>
</task>

<task type="auto">
  <name>Task 3: projection unit tests</name>
  <files>src/__tests__/worldMap.test.ts</files>
  <read_first>an existing test under src/__tests__ for the vitest import + assertion style (the repo runs `vitest run`).</read_first>
  <action>Add a vitest test (importing from `../../utils/worldMap`) asserting: `projectLatLon(0,0)` → centre `{x:180,y:90}`; `projectLatLon(90,-180)` → top-left `{x:0,y:0}`; `projectLatLon(-90,180)` → bottom-right `{x:360,y:180}`; scaling with `width=720,height=360` doubles the coordinates; out-of-range `lat=200`/`lon=-500` clamp rather than throw. Match the repo's vitest style in src/__tests__.</action>
  <verify>
    <automated>npx vitest run src/__tests__/worldMap.test.ts 2>&1 | tail -12</automated>
  </verify>
  <done>Projection tests pass (corners, centre, scaling, clamping).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bundled asset → browser | Fully static, self-contained; no external fetch surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-04 | Information Disclosure | outbound tile/CDN request leaking that the console is in use / IP | mitigate | No external refs (air-gap); grep gate in Task 2 verify + 49-04 prohibitions |
| T-49-SC | Tampering | package installs | accept | No new packages this plan |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; `npx vitest run src/__tests__/worldMap.test.ts` passes.
- `grep -nE "http|fetch\(|tile|cdn|<img" components/worldMapAsset.ts utils/worldMap.ts` returns nothing.
</verification>

<success_criteria>
GMAP-01's air-gapped foundation exists: a linear equirectangular `projectLatLon` and a self-contained world backdrop share a 360×180 coordinate space, with unit-tested projection corners and zero external network references.
</success_criteria>

<output>
Create `.planning/phases/49-fleet-geo-map/49-03-SUMMARY.md` when done.
</output>
