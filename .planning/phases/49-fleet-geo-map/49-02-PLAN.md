---
phase: 49-fleet-geo-map
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - types.ts
  - services/apiService.ts
requirements: [GMAP-01, GMAP-02, GMAP-03]
autonomous: true
must_haves:
  truths:
    - "types.ts defines FleetGeoAgent + FleetGeoResponse matching the 49-01 endpoint shape"
    - "types.ts AppView union includes a new 'fleetGeoMap' member, distinct from the existing 'geographicMap'"
    - "services/apiService.ts exports fetchFleetGeo() calling GET /api/fleet/geo via authFetch"
  artifacts:
    - path: "types.ts"
      provides: "FleetGeoAgent, FleetGeoResponse types + fleetGeoMap AppView member"
      contains: "fleetGeoMap"
    - path: "services/apiService.ts"
      provides: "fetchFleetGeo client"
      contains: "fetchFleetGeo"
  key_links:
    - from: "services/apiService.ts"
      to: "backend/agent_fleet_geo_endpoints.py"
      via: "authFetch(`${API_BASE}/fleet/geo`)"
      pattern: "fleet/geo"
  prohibitions:
    - "MUST NOT reuse or rename the existing 'geographicMap' AppView or 'view:geographic_map' permission"
    - "MUST NOT hand-roll a fetch — reuse the authFetch + error-shape helper used by fetchFleetObservability"
---

<objective>
Establish the frontend contract for the fleet geo map: the `FleetGeoAgent` / `FleetGeoResponse` types matching the 49-01 payload, the new `fleetGeoMap` AppView union member (distinct from the pre-existing security-attack `geographicMap`), and the `fetchFleetGeo()` API client. Defining this first lets 49-03/49-04 compile against a stable contract.

Purpose: contract-first so the map component and the nav registration have concrete types to import. D-03 requires a brand-new AppView, not a reuse of `geographicMap`.
Output: types + `fetchFleetGeo`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@.planning/phases/49-fleet-geo-map/49-RESEARCH.md
@.planning/phases/49-fleet-geo-map/49-CONTEXT.md

# Read-first source anchors:
@types.ts                     # GeoLocation (line ~696), AppView union (fleetObservability member ~line 177 area), AgentStatus
@services/apiService.ts       # line ~5033 fetchFleetObservability — clone its authFetch + error shape
</execution_context>

<artifacts>
## Artifacts This Plan Produces
- EDIT `types.ts` — `FleetGeoAgent`, `FleetGeoResponse` interfaces; `| 'fleetGeoMap'` in the AppView union.
- EDIT `services/apiService.ts` — `fetchFleetGeo(): Promise<FleetGeoResponse>` → `GET /api/fleet/geo`.
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: FleetGeoAgent/FleetGeoResponse types + fleetGeoMap AppView member</name>
  <files>types.ts</files>
  <read_first>types.ts — the `GeoLocation` interface, the `AppView` union (find the `fleetObservability` member and add alongside), and `AgentStatus`.</read_first>
  <action>Add to `types.ts`: `export interface FleetGeoAgent { id: string; hostname: string; status: AgentStatus; tenantId: string; lanIp?: string; publicIp?: string; geo: { city?: string; country?: string; country_code?: string; latitude: number; longitude: number } | null; }` and `export interface FleetGeoResponse { agents: FleetGeoAgent[]; total: number; located_count: number; unlocated_count: number; tenants: string[]; }`. Add `| 'fleetGeoMap'` to the `AppView` union next to `'fleetObservability'`. Do NOT touch `'geographicMap'` or `'view:geographic_map'` (D-03 — separate feature).</action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>Types compile; `fleetGeoMap` is a valid AppView; `geographicMap` untouched.</done>
</task>

<task type="auto">
  <name>Task 2: fetchFleetGeo API client</name>
  <files>services/apiService.ts</files>
  <read_first>services/apiService.ts line ~5033 `fetchFleetObservability` — clone the authFetch call + error handling exactly.</read_first>
  <action>Add `export const fetchFleetGeo = async (): Promise<FleetGeoResponse> => { const res = await authFetch(`${API_BASE}/fleet/geo`); if (!res.ok) { const err = await res.json().catch(() => ({})); throw new Error(err.detail || 'Failed to load fleet geo data'); } return await res.json(); };` — importing `FleetGeoResponse` from `../types` in the same style existing types are imported. Match the surrounding module's import/export conventions.</action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5 && npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>`fetchFleetGeo` typechecks and builds; hits `/api/fleet/geo` via authFetch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| apiService → GET /api/fleet/geo | authFetch attaches the session token; server is the RBAC/tenant authority (49-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-03 | Spoofing | unauthenticated fetch | mitigate | Reuses authFetch (session token) like every other client fetcher |
| T-49-SC | Tampering | package installs | accept | No new packages this plan |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; `npm run build` succeeds.
- `fleetGeoMap` present in AppView; `geographicMap` unchanged (grep confirms both present, distinct).
</verification>

<success_criteria>
The frontend contract exists: `FleetGeoAgent`/`FleetGeoResponse` match the 49-01 payload, `fleetGeoMap` is a new AppView distinct from `geographicMap`, and `fetchFleetGeo()` calls the endpoint via authFetch with the standard error shape.
</success_criteria>

<output>
Create `.planning/phases/49-fleet-geo-map/49-02-SUMMARY.md` when done.
</output>
