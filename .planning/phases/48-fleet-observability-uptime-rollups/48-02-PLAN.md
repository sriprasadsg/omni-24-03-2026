---
phase: 48-fleet-observability-uptime-rollups
plan: 02
type: execute
wave: 2
depends_on: ["48-01"]
files_modified:
  - backend/app_background_tasks.py
  - backend/app_startup.py
  - backend/retention_service.py
  - backend/tests/test_agent_uptime_rollup_loop.py
autonomous: true
requirements: [FOBS-02]
must_haves:
  truths:
    - "agent_uptime_rollup_loop() writes one row per agent per day into agent_uptime_rollups (upsert keyed on agent_id + date)"
    - "Each rollup row stores timestamp as a native BSON Date (datetime), never an ISO string"
    - "The sweep iterates all tenants via raw db._db.tenants and reuses compute_uptime (no duplicated uptime math)"
    - "retention_service.run_cleanup() deletes agent_uptime_rollups rows older than the configured retention_days"
    - "First run performs no historical backfill — it only writes the current day"
  artifacts:
    - path: "backend/app_background_tasks.py"
      provides: "agent_uptime_rollup_loop daily sweep"
      contains: "async def agent_uptime_rollup_loop"
    - path: "backend/retention_service.py"
      provides: "cleanup_agent_uptime_rollups + run_cleanup wiring"
      contains: "cleanup_agent_uptime_rollups"
    - path: "backend/tests/test_agent_uptime_rollup_loop.py"
      provides: "Hermetic coverage of the sweep write shape + retention delete"
  key_links:
    - from: "backend/app_startup.py"
      to: "backend/app_background_tasks.py"
      via: "asyncio.create_task(_safe_bg_task(agent_uptime_rollup_loop(), ...))"
      pattern: "agent_uptime_rollup"
    - from: "backend/app_background_tasks.py"
      to: "backend/agent_uptime_service.py"
      via: "reuses compute_uptime for the daily %"
      pattern: "compute_uptime"
    - from: "backend/retention_service.py run_cleanup"
      to: "cleanup_agent_uptime_rollups"
      via: "run_cleanup calls the new cleanup method and reports its count"
      pattern: "agent_uptime_rollups"
  prohibitions:
    - "The sweep MUST use raw db._db for cross-tenant reads/writes (Pattern 1), never the ambient request-scoped wrapped db"
    - "MUST NOT backfill historical days on first run (D-08)"
    - "MUST NOT add retention to agent_metrics itself (out of scope per D-02 / Pitfall 4)"
---

<objective>
Deliver the FOBS-02 rollup groundwork (D-01 part b): a daily background sweep that writes a per-agent daily uptime % into a new `agent_uptime_rollups` collection, plus its retention wiring — so aggregate/long-range uptime is cheap and the deferred longer-range UI is a switch-on, not a rebuild.

Purpose: D-01 requires BOTH the on-the-fly path (48-01) AND a daily rollup sweep. Per D-02 keep it minimal (write daily %); do NOT build any longer-range UI. Per Claude's Discretion + RESEARCH: daily cadence, raw-db tenant iteration cloning `snapshot_compliance_scores_loop`, native BSON Date timestamps cloning `agent_location_history`, retention routed through `retention_service` cloning the Phase 46 precedent.
Output: `agent_uptime_rollup_loop()` (registered in startup), `cleanup_agent_uptime_rollups()` (wired into run_cleanup), and hermetic tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-fleet-observability-uptime-rollups/48-RESEARCH.md

# Read-first source anchors:
@backend/app_background_tasks.py    # lines 129-224 — snapshot_compliance_scores_loop: raw db._db.tenants iteration, set_tenant_id/reset_tenant_id, upsert-by-date-key idiom
@backend/app_startup.py             # lines 455-590 — _safe_bg_task wrapper + the create_task registration block
@backend/retention_service.py       # lines 37-72 — cleanup_agent_location_history (native-BSON-Date delete) + run_cleanup report dict
@backend/agent_uptime_service.py    # compute_uptime signature (produced by 48-01)
</context>

<artifacts>
## Artifacts This Plan Produces
- NEW collection `agent_uptime_rollups` — docs `{agent_id, tenant_id, date, uptime_percent, timestamp: <BSON Date>}`, upsert keyed on `{agent_id, date}`.
- EDIT `backend/app_background_tasks.py` — `agent_uptime_rollup_loop()` (daily, raw-db tenant iteration, reuses `compute_uptime`).
- EDIT `backend/app_startup.py` — import + `asyncio.create_task(_safe_bg_task(agent_uptime_rollup_loop(), "agent_uptime_rollup"))`.
- EDIT `backend/retention_service.py` — `cleanup_agent_uptime_rollups()` + `run_cleanup` wiring.
- NEW `backend/tests/test_agent_uptime_rollup_loop.py`.
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: agent_uptime_rollup_loop daily sweep + startup registration (FOBS-02, D-01b, D-08)</name>
  <files>backend/app_background_tasks.py, backend/app_startup.py, backend/tests/test_agent_uptime_rollup_loop.py</files>
  <read_first>backend/app_background_tasks.py lines 129-224 (snapshot_compliance_scores_loop) — clone its raw db._db.tenants iteration + set_tenant_id/reset_tenant_id/finally shape + upsert-by-date-key. backend/app_startup.py lines 455-590 for _safe_bg_task and the registration block.</read_first>
  <action>Add `async def agent_uptime_rollup_loop()` to `backend/app_background_tasks.py`, cloning `snapshot_compliance_scores_loop`'s structure: `while True: await asyncio.sleep(86400)`, acquire `db = get_database()`, set a platform-admin tenant token, read tenants via `db._db.tenants.find({}, {"id": 1})` (raw — Pattern 1, cross-tenant), then for each tenant `set_tenant_id(tid)` and list that tenant's agents. For each agent, read the last 24h of `agent_metrics` rows and call `compute_uptime(rows, 24)` (import from `agent_uptime_service`), then upsert into `db._db.agent_uptime_rollups` keyed on `{"agent_id": <id>, "date": <YYYY-MM-DD string>}` with `$set` of `{tenant_id, uptime_percent, timestamp: datetime.now(timezone.utc)}`. Per Pattern 3 the `timestamp` field MUST be a native `datetime` (BSON Date), NOT `.isoformat()` — retention compares it with `$lt` on a datetime. Per D-08 there is NO historical backfill — only the current day is written; document this in the function docstring so an empty collection on day 1 is not mistaken for a bug. Wrap per-tenant work in try/except logging like the precedent; use the `finally: reset_tenant_id(token)` guard. Register it in `backend/app_startup.py` by adding `agent_uptime_rollup_loop` to the `from app_background_tasks import (...)` block and one `asyncio.create_task(_safe_bg_task(agent_uptime_rollup_loop(), "agent_uptime_rollup"))` line alongside the existing loop registrations. Write `backend/tests/test_agent_uptime_rollup_loop.py` (hermetic, mock db) that drives one iteration (call the loop body / a factored inner function once rather than the infinite `while` — factor the single-pass body into a helper the test can call directly) and asserts: one upsert per agent per tenant, the upsert doc's `timestamp` is a `datetime` instance (not str), and reads went through `db._db` (raw).</action>
  <verify>
    <automated>backend/venv/bin/python -m pytest backend/tests/test_agent_uptime_rollup_loop.py -q && backend/venv/bin/python -c "import app_background_tasks, app_startup" 2>&1 | tail -1</automated>
  </verify>
  <done>Single-pass sweep upserts one dated row per agent with a BSON-Date timestamp via raw db._db; loop is registered in startup; no backfill.</done>
</task>

<task type="auto">
  <name>Task 2: agent_uptime_rollups retention wiring (FOBS-02, Pitfall 4, Phase 46 precedent)</name>
  <files>backend/retention_service.py, backend/tests/test_agent_uptime_rollup_loop.py</files>
  <read_first>backend/retention_service.py lines 37-72 — cleanup_agent_location_history (datetime cutoff delete, no tenantId filter) and run_cleanup's report dict.</read_first>
  <action>Add `async def cleanup_agent_uptime_rollups(self, retention_days: int = 90)` to `RetentionService`, cloning `cleanup_agent_location_history` exactly: compute `cutoff = datetime.now(timezone.utc) - timedelta(days=retention_days)` and `delete_many({"timestamp": {"$lt": cutoff}})` on `self.db.agent_uptime_rollups` (datetime comparison — the write path stores a native BSON Date, matching the location-history convention), platform-wide, no tenantId filter. Wire it into `run_cleanup()`: call it with `p.get("agent_uptime_rollups", 90)` and add `"agent_uptime_rollups_deleted": <count>` to the returned report dict. Do NOT add any retention to `agent_metrics` itself (out of scope per D-02 / Pitfall 4). Add a test in `test_agent_uptime_rollup_loop.py` (or a co-located retention test) asserting `cleanup_agent_uptime_rollups` issues a `$lt` datetime delete and `run_cleanup`'s report includes `agent_uptime_rollups_deleted`.</action>
  <verify>
    <automated>backend/venv/bin/python -m pytest backend/tests/test_agent_uptime_rollup_loop.py -q</automated>
  </verify>
  <done>run_cleanup deletes stale agent_uptime_rollups by datetime cutoff and reports the count; agent_metrics retention untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| background loop → all tenants (raw db._db) | Sweep legitimately crosses tenant scope; must be raw-db + explicit tenant_id per row |
| retention sweep → agent_uptime_rollups | Platform-wide delete of aged rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-04 | Information Disclosure | rollup rows missing tenant_id → cross-tenant mixing | mitigate | Every upserted row carries an explicit `tenant_id`; per-tenant iteration via set_tenant_id, raw write keyed with tenant_id |
| T-48-05 | Denial of Service | unbounded rollup growth | mitigate | cleanup_agent_uptime_rollups wired into run_cleanup (Pitfall 4 — do not repeat the agent_metrics omission) |
| T-48-06 | Repudiation | wrong-typed timestamp breaks retention `$lt` | mitigate | Store native BSON Date (Pattern 3); test asserts datetime type |
| T-48-SC | Tampering | package installs | accept | No new packages this plan |
</threat_model>

<verification>
- `backend/venv/bin/python -m pytest backend/tests/test_agent_uptime_rollup_loop.py -q` green.
- `import app_background_tasks, app_startup` succeeds (loop registered).
- Full backend suite shows no new failures vs documented pre-existing.
</verification>

<success_criteria>
FOBS-02 rollup groundwork exists: a daily raw-db sweep writes per-agent daily uptime % (BSON-Date timestamped) into `agent_uptime_rollups` with no first-run backfill, and `retention_service.run_cleanup` prunes it — with zero longer-range UI built this phase.
</success_criteria>

<output>
Create `.planning/phases/48-fleet-observability-uptime-rollups/48-02-SUMMARY.md` when done.
</output>
