---
phase: 48-fleet-observability-uptime-rollups
plan: 05
type: execute
wave: 3
depends_on: ["48-03", "48-04"]
files_modified:
  - components/FleetObservabilityDashboard.tsx
  - services/apiService.ts
  - types.ts
  - App.tsx
  - components/Sidebar.tsx
autonomous: true
requirements: [FOBS-03]
must_haves:
  truths:
    - "A new admin-gated 'Fleet Observability' nav page is reachable from the sidebar"
    - "The page lists offline agents and version-drifted agents from GET /api/fleet/observability"
    - "The page + nav entry are gated by the manage:agents permission (D-07)"
    - "Users without manage:agents do not see the nav entry and cannot reach the view"
  artifacts:
    - path: "components/FleetObservabilityDashboard.tsx"
      provides: "Admin-gated fleet observability page (offline + version-drift lists)"
      min_lines: 40
    - path: "services/apiService.ts"
      provides: "fetchFleetObservability"
      contains: "fetchFleetObservability"
    - path: "types.ts"
      provides: "fleetObservability AppView member"
      contains: "fleetObservability"
  key_links:
    - from: "components/FleetObservabilityDashboard.tsx"
      to: "services/apiService.ts"
      via: "calls fetchFleetObservability() → /api/fleet/observability"
      pattern: "fetchFleetObservability"
    - from: "App.tsx"
      to: "components/FleetObservabilityDashboard.tsx"
      via: "lazy import + permission map (manage:agents) + render switch case"
      pattern: "FleetObservabilityDashboard"
  prohibitions:
    - "MUST NOT gate the page on anything other than manage:agents (D-07)"
    - "MUST NOT rebuild offline detection or version compare on the client (reads the 48-03 aggregate)"
---

<objective>
Deliver the FOBS-03 UI: a new admin-gated "Fleet Observability" nav page listing offline agents and version-drifted agents, cloning the Phase 47 Security-panel 4-file registration pattern (D-03), gated by `manage:agents` (D-07), backed by the 48-03 aggregate endpoint.

Purpose: D-03 locks a dedicated admin-gated nav page cloning `SecuritySettingsDashboard`'s registration (types.ts + App.tsx lazy-import/permission-map/switch + Sidebar entry). The page is purely a renderer over `GET /api/fleet/observability` — no client-side offline/version logic (Don't Hand-Roll).
Output: `FleetObservabilityDashboard.tsx`, `fetchFleetObservability`, and the 4-file registration.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-fleet-observability-uptime-rollups/48-RESEARCH.md
@.planning/phases/48-fleet-observability-uptime-rollups/48-CONTEXT.md

# Read-first source anchors (the Phase 47 clone target — exact 4-file pattern):
@components/SecuritySettingsDashboard.tsx   # dashboard shell/styling to mirror
@App.tsx                                    # line 165 lazy import, line 367 permission map (geoSecurity: 'manage:settings'), line 1912 render switch case
@components/Sidebar.tsx                     # the geoSecurity nav entry row to clone (with its permission gate)
@services/apiService.ts                     # fetch/error shape for the new function
</context>

<artifacts>
## Artifacts This Plan Produces
- NEW `components/FleetObservabilityDashboard.tsx` — admin page rendering offline_agents + version_drift.
- EDIT `services/apiService.ts` — `fetchFleetObservability()` → GET /api/fleet/observability.
- EDIT `types.ts` — add `'fleetObservability'` to the AppView union.
- EDIT `App.tsx` — lazy import + `fleetObservability: 'manage:agents'` permission entry + render switch case.
- EDIT `components/Sidebar.tsx` — one nav entry gated by `manage:agents`.
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: fetchFleetObservability + FleetObservabilityDashboard component (FOBS-03, D-03)</name>
  <files>services/apiService.ts, components/FleetObservabilityDashboard.tsx</files>
  <read_first>components/SecuritySettingsDashboard.tsx for the dashboard shell/styling; services/apiService.ts for the fetch/error shape.</read_first>
  <action>Add `fetchFleetObservability()` to `services/apiService.ts` → `GET /api/fleet/observability`, returning `{latest_version, offline_agents, version_drift, offline_count, drift_count}` (the 48-03 shape). Mirror an existing fetcher's auth headers + error handling. Create `components/FleetObservabilityDashboard.tsx` exporting a named `FleetObservabilityDashboard` (matching the lazy-import shape App.tsx uses for SecuritySettingsDashboard), cloning that dashboard's layout/styling. On mount call `fetchFleetObservability` and render two sections: "Offline Agents" (hostname, status, last-seen if present) and "Version Drift" (hostname, reported version, latest version), each with a friendly empty state ("No offline agents" / "All agents on the latest version") and a loading/error state. Do NOT compute offline/drift on the client — render exactly what the endpoint returns.</action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5 && npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>Component typechecks and builds; renders offline + drift lists with empty/loading/error states from the endpoint.</done>
</task>

<task type="auto">
  <name>Task 2: 4-file admin-gated nav registration (FOBS-03, D-03, D-07, Pattern 4)</name>
  <files>types.ts, App.tsx, components/Sidebar.tsx</files>
  <read_first>App.tsx lines 165 / 367 / 1912 and components/Sidebar.tsx (the geoSecurity row) — clone each, swapping the view key, label, and permission.</read_first>
  <action>Clone the Phase 47 registration across the three remaining files, swapping `geoSecurity`→`fleetObservability` and `manage:settings`→`manage:agents` (D-07): (1) `types.ts` — add `| 'fleetObservability'` to the AppView union. (2) `App.tsx` — add a lazy import `const FleetObservabilityDashboard = lazy(() => import('./components/FleetObservabilityDashboard').then(m => ({ default: m.FleetObservabilityDashboard })));` near line 165; add `fleetObservability: 'manage:agents'` to the permission map near line 367; add `case 'fleetObservability': return <ErrorBoundary name="FleetObservabilityDashboard"><FleetObservabilityDashboard /></ErrorBoundary>;` near line 1912. (3) `components/Sidebar.tsx` — add a nav entry `{ view: 'fleetObservability', label: 'Fleet Observability', icon: <ActivityIcon size={20} />, permission: 'manage:agents' }` (reuse an already-imported icon; ActivityIcon is used nearby). The permission MUST be `manage:agents` and nothing else (D-07).</action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5 && npm run build 2>&1 | tail -3</automated>
    <human-check>As an admin with manage:agents, confirm the "Fleet Observability" sidebar entry appears and opens the page showing offline + version-drift lists. As a user without manage:agents, confirm the entry is hidden and the view is unreachable.</human-check>
  </verify>
  <done>Nav entry + view registered across all 4 files; gated by manage:agents; app builds; page reachable only for permitted users.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → GET /api/fleet/observability | Authenticated admin; server enforces tenant scope + role (48-03) |
| nav/view gating | Client-side permission gate (manage:agents) hides the entry; server remains the authority |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-13 | Elevation of Privilege | non-admin reaching the fleet view | mitigate | Nav + view gated by manage:agents (D-07); endpoint independently enforces role/tenant (48-03) — client gate is convenience, server is authority |
| T-48-14 | Information Disclosure | client-side offline/drift recomputation drifting from server | accept | Page renders endpoint output only; no client logic to diverge |
| T-48-SC | Tampering | package installs | accept | No new packages this plan |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; `npm run build` succeeds.
- Manual UAT (end-of-phase): Fleet Observability page reachable for manage:agents holders, hidden/blocked otherwise; lists match endpoint data.
</verification>

<success_criteria>
FOBS-03 UI exists: a new admin-gated "Fleet Observability" nav page (registered across types.ts/App.tsx/Sidebar.tsx, gated by manage:agents) lists offline agents and version-drifted agents from the 48-03 aggregate endpoint, with no client-side offline/version logic.
</success_criteria>

<output>
Create `.planning/phases/48-fleet-observability-uptime-rollups/48-05-SUMMARY.md` when done.
</output>
