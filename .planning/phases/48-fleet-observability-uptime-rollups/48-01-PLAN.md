---
phase: 48-fleet-observability-uptime-rollups
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/agent_uptime_service.py
  - backend/agent_uptime_endpoints.py
  - backend/router_registry.py
  - backend/tests/test_agent_uptime_service.py
autonomous: true
requirements: [FOBS-02]
must_haves:
  truths:
    - "GET /api/agents/{id}/uptime?hours=N returns an uptime_percent and a bucketed timeline"
    - "Uptime % = received_buckets / expected_buckets at a fixed 30s cadence, clipped to 100%"
    - "An agent with zero heartbeats in the window returns 0% uptime without raising"
    - "A non-super-admin only receives uptime for agents inside their own tenant (404 otherwise)"
    - "hours is clamped to the 1..48 range before querying (no larger windows)"
  artifacts:
    - path: "backend/agent_uptime_service.py"
      provides: "On-the-fly gap-detection uptime computation over agent_metrics rows"
      contains: "def compute_uptime"
      min_lines: 30
    - path: "backend/agent_uptime_endpoints.py"
      provides: "GET /api/agents/{agent_id}/uptime endpoint"
      contains: "uptime"
    - path: "backend/tests/test_agent_uptime_service.py"
      provides: "Unit coverage of bucketing, empty-window, and clip-to-100 behavior"
  key_links:
    - from: "backend/agent_uptime_endpoints.py"
      to: "backend/agent_uptime_service.py"
      via: "endpoint calls compute_uptime(rows, hours)"
      pattern: "compute_uptime"
    - from: "backend/agent_uptime_service.py"
      to: "agent_metrics collection"
      via: "reads agent_metrics.timestamp over the requested window"
      pattern: "agent_metrics"
  prohibitions:
    - "Does NOT read the capped agent_metrics_history collection for uptime (decoy, ~25-50 min only)"
    - "Does NOT use raw db._db inside the request handler (tenant-leak risk — Pitfall 5)"
---

<objective>
Deliver the FOBS-02 on-the-fly uptime read path (D-01 part a): a service that computes a per-agent heartbeat-presence uptime timeline + uptime % over a ≤48h window from the existing `agent_metrics` collection, and a tenant-isolated endpoint exposing it.

Purpose: Uptime is defined (D-01) as heartbeat-presence ratio (received/expected at ~30s cadence), not status-transition duration. This is the live backing for the selectable-range uptime % the agent detail UI (48-04) renders, and the compute function the daily rollup sweep (48-02) reuses.
Output: `backend/agent_uptime_service.py`, `backend/agent_uptime_endpoints.py` (registered), and its Wave-0 unit test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-fleet-observability-uptime-rollups/48-RESEARCH.md
@.planning/phases/48-fleet-observability-uptime-rollups/48-CONTEXT.md

# Read-first source anchors (do not re-read beyond the cited ranges):
@backend/agent_metrics_endpoints.py   # lines 97-148 — GET /{agent_id}/metrics/history: agent_metrics query shape, _METRICS_SUPER_ROLES tenant gating, 2880 cap, ISO-string timestamp
</context>

<artifacts>
## Artifacts This Plan Produces
- NEW `backend/agent_uptime_service.py` — `compute_uptime(rows, hours, cadence_seconds=30)` returning `{uptime_percent, expected_buckets, received_buckets, timeline}`.
- NEW `backend/agent_uptime_endpoints.py` — `GET /api/agents/{agent_id}/uptime?hours=N` (APIRouter `prefix="/api/agents"`, mirrors `agent_metrics_endpoints.py`).
- EDIT `backend/router_registry.py` — register the new uptime router.
- NEW `backend/tests/test_agent_uptime_service.py` — Wave-0 gap-detection unit coverage.
</artifacts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 test scaffold + compute_uptime service (FOBS-02, D-01a, D-09)</name>
  <files>backend/tests/test_agent_uptime_service.py, backend/agent_uptime_service.py</files>
  <read_first>backend/agent_metrics_endpoints.py (lines 97-148) for the agent_metrics row shape (timestamp is an ISO-8601 string, sorted ascending)</read_first>
  <behavior>
    - Given N heartbeat timestamps evenly spaced at 30s across an H-hour window with zero gaps → uptime_percent == 100.0, received_buckets == expected_buckets.
    - Given exactly half the expected 30s buckets present → uptime_percent == 50.0.
    - Given an empty rows list (never-heartbeat agent) → uptime_percent == 0.0, no exception, timeline is a list (possibly all-down buckets).
    - Given more rows than expected buckets (agent faster than 30s cadence) → uptime_percent clips at 100.0 (never >100).
    - timeline is a list of bucket dicts each with a start timestamp (ISO string) and an up boolean.
  </behavior>
  <action>Per D-09, uptime source is the `agent_metrics` collection (per-heartbeat rows); `agent_metrics_history` is a capped decoy and MUST NOT be used. Create `compute_uptime(rows, hours, cadence_seconds=30)` in a NEW `backend/agent_uptime_service.py`. Parse each row's ISO `timestamp` into an aware datetime; build the expected-bucket grid over `[now - hours, now]` at the fixed `cadence_seconds` (30s per D-09, matching monitor_agent_status's own assumption — document in a module docstring that no per-agent interval is persisted so 30s is an assumption, not a measured fact). A bucket is `up` if at least one heartbeat timestamp falls in it. `uptime_percent = round(min(received/expected, 1.0) * 100, 1)` guarding `expected == 0`. Return `{uptime_percent, expected_buckets, received_buckets, timeline}`. For windows near 48h, coarsen the returned `timeline` to a readable display granularity (e.g. 5-15 min display blocks aggregating the 30s detection buckets) so the payload is not thousands of points; keep the % computed on the fine 30s grid. Keep the file focused and under 500 lines. Write the tests first in `backend/tests/test_agent_uptime_service.py` using `asyncio.run()` only if needed (compute_uptime is pure/sync — no pytest-asyncio in this env); cover all five behaviors above with deterministic synthesized timestamps.</action>
  <verify>
    <automated>backend/venv/bin/python -m pytest backend/tests/test_agent_uptime_service.py -q</automated>
  </verify>
  <done>All five behavior cases pass; empty-window yields 0% with no exception; over-cadence clips at 100%.</done>
</task>

<task type="auto">
  <name>Task 2: Tenant-isolated GET /uptime endpoint + router registration (FOBS-02, V4/V5)</name>
  <files>backend/agent_uptime_endpoints.py, backend/router_registry.py, backend/tests/test_agent_uptime_service.py</files>
  <read_first>backend/agent_metrics_endpoints.py (lines 100-131) — copy the exact tenant-gating shape: resolve role/tenant_id, build agent_filter with tenantId only when role not in the super set, 404 if agent not found, then query agent_metrics with a matching tenant_id filter. backend/router_registry.py (line 279) for where agent_metrics_endpoints registers.</read_first>
  <action>Create `backend/agent_uptime_endpoints.py` with `router = APIRouter(prefix="/api/agents", tags=["Agent Uptime"])` and `GET /{agent_id}/uptime` taking `hours: int = 24` and `current_user=Depends(get_current_user)`. V5 input validation: clamp `hours` to `max(1, min(hours, 48))` before use (D-02 locks ≤48h; DoS mitigation). V4 access control: reproduce `agent_metrics_endpoints.get_agent_metrics_history`'s tenant gating verbatim — a non-super-admin gets `tenantId` added to the agent lookup and `tenant_id` added to the agent_metrics query; 404 when the agent is not in scope. Query `db.agent_metrics` (the request-scoped wrapped `get_database()` handle — NEVER `db._db` here, per Pitfall 5) for `{agent_id, timestamp >= since}` projecting `{_id:0, timestamp:1}`, sorted ascending, `to_list(length=5760)`. Pass the rows to `compute_uptime(rows, hours)` and return `{agent_id, hours, ...compute_uptime result}`. Register the router in `backend/router_registry.py` alongside the `agent_metrics_endpoints` line (line 279 area) via `_load(app, "agent_uptime_endpoints", "router")`. Add an endpoint-level test to `test_agent_uptime_service.py` (or a co-located hermetic mock-db test) asserting the hours-clamp and that a tenant-mismatched agent_id yields 404.</action>
  <verify>
    <automated>backend/venv/bin/python -m pytest backend/tests/test_agent_uptime_service.py -q && backend/venv/bin/python -c "import agent_uptime_endpoints, router_registry" 2>&1 | tail -1</automated>
  </verify>
  <done>Endpoint imports cleanly and is registered; hours clamps to ≤48; cross-tenant agent lookup 404s; response includes uptime_percent + timeline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → GET /api/agents/{id}/uptime | Authenticated admin requests uptime; `agent_id`, `hours` are untrusted input |
| endpoint → agent_metrics (Mongo) | Tenant scope must be enforced on the query for non-super-admins |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-01 | Information Disclosure | GET /uptime cross-tenant read | mitigate | Reproduce agent_metrics_endpoints tenant gating; use request-scoped wrapped `db` only, never `db._db` in the handler (Pitfall 5) |
| T-48-02 | Denial of Service | unbounded `hours` param | mitigate | Clamp hours to 1..48 before query; agent_metrics query already length-capped |
| T-48-03 | Tampering | forged/absent agent_metrics rows | accept | Uptime is an informational health metric, not a security gate; empty window yields 0% by design |
| T-48-SC | Tampering | package installs | accept | No new packages this plan (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `backend/venv/bin/python -m pytest backend/tests/test_agent_uptime_service.py -q` green.
- New uptime router imports and registers without error.
- Full backend suite (`backend/venv/bin/python -m pytest backend/ -q --continue-on-collection-errors`) shows no new failures beyond documented pre-existing ones.
</verification>

<success_criteria>
FOBS-02 on-the-fly path exists: a tenant-isolated `GET /api/agents/{id}/uptime?hours=N` returns a heartbeat-presence uptime % and bucketed timeline over a ≤48h window, computed from `agent_metrics`, with never-heartbeat agents returning 0% rather than crashing.
</success_criteria>

<output>
Create `.planning/phases/48-fleet-observability-uptime-rollups/48-01-SUMMARY.md` when done.
</output>
