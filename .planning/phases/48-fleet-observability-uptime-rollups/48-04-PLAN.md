---
phase: 48-fleet-observability-uptime-rollups
plan: 04
type: execute
wave: 2
depends_on: ["48-01"]
files_modified:
  - components/AgentMetricsTab.tsx
  - components/AgentUptimeTimeline.tsx
  - services/apiService.ts
  - components/AgentDetailModal.tsx
autonomous: true
requirements: [FOBS-01, FOBS-02]
must_haves:
  truths:
    - "The agent detail view has a Metrics tab that renders CPU, memory, and disk history as recharts AreaCharts"
    - "The same tab renders a per-agent uptime timeline and an uptime %"
    - "A ≤48h range selector (1h / 6h / 24h / 48h) drives both the charts and the uptime figure"
    - "Charts consume GET /api/agents/{id}/metrics/history?hours=N; uptime consumes GET /api/agents/{id}/uptime?hours=N"
    - "AgentDetailModal.tsx gains only a tab button + one conditional render (its per-tab footprint), not inline chart JSX"
  artifacts:
    - path: "components/AgentMetricsTab.tsx"
      provides: "New agent-scoped metrics + uptime tab (recharts + range selector)"
      min_lines: 40
    - path: "components/AgentUptimeTimeline.tsx"
      provides: "Uptime timeline + % sub-component"
    - path: "services/apiService.ts"
      provides: "fetchAgentMetricsHistory + fetchAgentUptime"
      contains: "fetchAgentUptime"
  key_links:
    - from: "components/AgentMetricsTab.tsx"
      to: "services/apiService.ts"
      via: "calls fetchAgentMetricsHistory(agentId, hours) + fetchAgentUptime(agentId, hours)"
      pattern: "fetchAgentUptime"
    - from: "components/AgentDetailModal.tsx"
      to: "components/AgentMetricsTab.tsx"
      via: "renders <AgentMetricsTab> for the new tab case"
      pattern: "AgentMetricsTab"
  prohibitions:
    - "Chart/timeline JSX MUST NOT be inlined into AgentDetailModal.tsx (already 703 lines, over the 500 cap — Pitfall 6)"
    - "MUST NOT edit asset_metrics_endpoints.py (dead/shadowed code — Pitfall 1)"
    - "MUST NOT wire this tab to the asset-metrics endpoint; FOBS-01 consumes the agent metrics-history endpoint per REQUIREMENTS"
---

<objective>
Deliver the FOBS-01 charts + FOBS-02 timeline UI: a new agent-scoped `AgentMetricsTab.tsx` mounted as a new tab in `AgentDetailModal.tsx`, rendering CPU/memory/disk history as recharts AreaCharts and a per-agent uptime timeline + %, driven by a ≤48h range selector.

Purpose: FOBS-01 (REQUIREMENTS) consumes `GET /agents/{id}/metrics/history` via recharts; FOBS-02 exposes the selectable-range uptime % from the 48-01 endpoint. Per D-04 CORRECTION + Pitfall 6, `AgentDetailModal.tsx` is already 703 lines (>500 cap) so the tab content MUST be its own file(s); the modal gets only a button + one render line, matching every other tab's footprint.
Output: `AgentMetricsTab.tsx`, `AgentUptimeTimeline.tsx`, two new `apiService.ts` functions, and the modal wiring.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-fleet-observability-uptime-rollups/48-RESEARCH.md
@.planning/phases/48-fleet-observability-uptime-rollups/48-CONTEXT.md

# Read-first source anchors:
@components/MetricsChartsTab.tsx     # recharts AreaChart structure + range-preset button styling to mirror (chart layout only; its asset fetch is NOT reused here)
@components/AgentDetailModal.tsx     # the one-tab-per-component pattern: tab-button array + conditional-render block (~4-6 lines per tab); reuse the existing assetId/agent derivation for the agent id
@services/apiService.ts              # line 1587 fetchAssetMetrics — mirror its fetch/error shape for the two new functions
</context>

<artifacts>
## Artifacts This Plan Produces
- NEW `components/AgentMetricsTab.tsx` — CPU/mem/disk AreaCharts + range selector (1h/6h/24h/48h) + embeds the uptime timeline.
- NEW `components/AgentUptimeTimeline.tsx` — renders the uptime % + bucketed timeline from the uptime endpoint.
- EDIT `services/apiService.ts` — `fetchAgentMetricsHistory(agentId, hours)` → GET /api/agents/{id}/metrics/history?hours=N; `fetchAgentUptime(agentId, hours)` → GET /api/agents/{id}/uptime?hours=N.
- EDIT `components/AgentDetailModal.tsx` — +1 tab button, +1 conditional render line.
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: apiService fetchers for agent metrics-history + uptime (FOBS-01/02, D-02)</name>
  <files>services/apiService.ts</files>
  <read_first>services/apiService.ts line 1587 (fetchAssetMetrics) for the fetch + auth-header + error-handling shape to mirror.</read_first>
  <action>Add two functions near `fetchAssetMetrics`. `fetchAgentMetricsHistory(agentId: string, hours: 1 | 6 | 24 | 48)` → `GET /api/agents/${agentId}/metrics/history?hours=${hours}`, returning the `{agent_id, hours, metrics, summary}` payload. `fetchAgentUptime(agentId: string, hours: 1 | 6 | 24 | 48)` → `GET /api/agents/${agentId}/uptime?hours=${hours}`, returning `{agent_id, hours, uptime_percent, expected_buckets, received_buckets, timeline}`. Mirror `fetchAssetMetrics`'s auth headers and error handling exactly. Use the ≤48h union type (D-02) — do not introduce 7d/30d.</action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>Both functions typecheck; hours param is the ≤48h union; endpoints match the 48-01 routes.</done>
</task>

<task type="auto">
  <name>Task 2: AgentMetricsTab + AgentUptimeTimeline components (FOBS-01/02, D-04, Pattern 5)</name>
  <files>components/AgentMetricsTab.tsx, components/AgentUptimeTimeline.tsx</files>
  <read_first>components/MetricsChartsTab.tsx for the recharts AreaChart layout + range-button styling to mirror; components/AgentDetailModal.tsx for the existing agent-id derivation used by other tabs.</read_first>
  <action>Create `components/AgentUptimeTimeline.tsx`: a component taking `agentId` and the selected `hours`; on change it calls `fetchAgentUptime`, shows the `uptime_percent` prominently (with a small note that % is a heartbeat-presence estimate at an assumed 30s cadence — Pitfall 3 honesty), and renders the `timeline` buckets as a compact horizontal up/down strip. Create `components/AgentMetricsTab.tsx`: takes the agent (and derives `agentId` using the same derivation other tabs use — do not invent a new one), owns a range-selector state defaulting to 24h with buttons `1h / 6h / 24h / 48h` (styling mirrored from MetricsChartsTab; NO 7d/30d per D-02), calls `fetchAgentMetricsHistory(agentId, hours)`, and renders three recharts AreaCharts (CPU %, memory %, disk %) from the `metrics` array (fields `cpu_percent`/`memory_percent`/`disk_percent`, x-axis `timestamp`) mirroring MetricsChartsTab's chart structure. Embed `<AgentUptimeTimeline agentId hours />` in the same tab so both react to the shared range selector. Handle empty-data (no metrics / 0 buckets) gracefully with a friendly message, not a crash. Keep each file focused and under 500 lines.</action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5 && npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>Both components typecheck and the app builds; range selector exposes only 1h/6h/24h/48h; charts + uptime share the range.</done>
</task>

<task type="auto">
  <name>Task 3: Mount the Metrics tab in AgentDetailModal (FOBS-01/02, Pitfall 6)</name>
  <files>components/AgentDetailModal.tsx</files>
  <read_first>components/AgentDetailModal.tsx — the tab-button array and the conditional-render block; copy the ~4-6 line footprint of an existing tab (e.g. the compliance tab).</read_first>
  <action>Add ONE new tab entry (button + label, e.g. "Metrics") to the modal's tab list and ONE conditional-render line that renders `<AgentMetricsTab agent={agent} />` (passing whatever the sibling tabs receive so the agent-id derivation works). Do NOT inline any chart/timeline JSX here (Pitfall 6 — the file is already 703 lines). The net diff to this file must be roughly ≤10 added lines, matching every other tab's footprint.</action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5 && npm run build 2>&1 | tail -3</automated>
    <human-check>Open an agent detail view, click the new Metrics tab: confirm CPU/memory/disk AreaCharts render, the uptime timeline + % show, and switching 1h/6h/24h/48h updates both charts and uptime.</human-check>
  </verify>
  <done>Modal shows a working Metrics tab; file grew only by the per-tab footprint; app builds clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → GET /api/agents/{id}/metrics/history, /uptime | Authenticated admin fetch; tenant scope enforced server-side (48-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-10 | Information Disclosure | client requesting another agent's metrics | mitigate | Server-side tenant gating on both endpoints (48-01) — client cannot widen scope |
| T-48-11 | Tampering | client sending arbitrary hours | mitigate | UI sends only 1/6/24/48; server clamps to ≤48 regardless (48-01) |
| T-48-12 | Repudiation | uptime % misread as exact | accept | UI copy states % is a heartbeat-presence estimate at assumed 30s cadence (Pitfall 3) |
| T-48-SC | Tampering | package installs | accept | No new packages (recharts already installed) |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; `npm run build` succeeds.
- Manual UAT (end-of-phase, per human_verify_mode): Metrics tab renders charts + uptime and reacts to the range selector.
</verification>

<success_criteria>
FOBS-01 + FOBS-02 UI exist: an agent detail Metrics tab renders CPU/mem/disk recharts AreaCharts and a per-agent uptime timeline + %, driven by a ≤48h range selector, consuming the agent metrics-history + uptime endpoints — with AgentDetailModal.tsx unchanged beyond its per-tab footprint.
</success_criteria>

<output>
Create `.planning/phases/48-fleet-observability-uptime-rollups/48-04-SUMMARY.md` when done.
</output>
