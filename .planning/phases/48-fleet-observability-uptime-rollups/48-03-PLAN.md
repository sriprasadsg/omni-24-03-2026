---
phase: 48-fleet-observability-uptime-rollups
plan: 03
type: execute
wave: 2
depends_on: ["48-01"]
files_modified:
  - backend/agent_fleet_observability_endpoints.py
  - backend/router_registry.py
  - backend/tests/test_agent_fleet_observability.py
autonomous: true
requirements: [FOBS-03]
must_haves:
  truths:
    - "GET /api/fleet/observability returns an offline_agents list and a version_drift list plus the latest_version"
    - "Version-drift is computed via agent_auto_update_service._parse_ver vs _LATEST_AGENT_VERSION"
    - "A non-super-admin sees only their own tenant's agents; a super-admin sees the full fleet"
    - "A malformed or missing reported version is treated as not-comparable (excluded from drift) rather than crashing"
    - "Offline set is read from agents.status == 'Offline' (maintained by monitor_agent_status) — not a new heuristic"
  artifacts:
    - path: "backend/agent_fleet_observability_endpoints.py"
      provides: "GET /api/fleet/observability aggregate endpoint"
      contains: "observability"
    - path: "backend/tests/test_agent_fleet_observability.py"
      provides: "Offline-set + version-drift + tenant-scope coverage"
  key_links:
    - from: "backend/agent_fleet_observability_endpoints.py"
      to: "backend/agent_auto_update_service.py"
      via: "imports _parse_ver + _LATEST_AGENT_VERSION for drift compare"
      pattern: "_parse_ver"
    - from: "backend/agent_fleet_observability_endpoints.py"
      to: "agents collection"
      via: "reads agents.status / agents.version through the request-scoped wrapped db"
      pattern: "db.agents"
  prohibitions:
    - "MUST NOT use raw db._db.agents inside the request handler (tenant-leak — Pitfall 5)"
    - "MUST NOT build a new offline-detection heuristic or a new version parser (Don't Hand-Roll)"
---

<objective>
Deliver the FOBS-03 backend: one new admin aggregate endpoint returning the fleet's offline agents (from the existing `status == "Offline"` set) and its version-drift list (reported version vs the single global `_LATEST_AGENT_VERSION`), tenant-scoped for non-super-admins.

Purpose: No existing endpoint aggregates offline + version-drift; `GET /api/agents` is a paginated list, not a counts/aggregate shape (Claude's Discretion → one new endpoint, per RESEARCH). Reuse `monitor_agent_status()`'s offline set and `_parse_ver` — do not rebuild either (D-03, Don't Hand-Roll). This backs the Fleet Observability nav page (48-05).
Output: `backend/agent_fleet_observability_endpoints.py` (registered) + its unit test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-fleet-observability-uptime-rollups/48-RESEARCH.md

# Read-first source anchors:
@backend/agent_core_endpoints.py        # lines 1-140 — GET /api/agents: is_super_admin(role) gating whether tenantId is added to the query filter (clone this exact shape)
@backend/agent_auto_update_service.py    # lines 19-25 — _LATEST_AGENT_VERSION + _parse_ver
@backend/router_registry.py              # line 279 area — router registration convention
</context>

<artifacts>
## Artifacts This Plan Produces
- NEW `backend/agent_fleet_observability_endpoints.py` — `router = APIRouter(prefix="/api/fleet", tags=["Fleet Observability"])`, `GET /observability`.
- EDIT `backend/router_registry.py` — register the fleet router.
- NEW `backend/tests/test_agent_fleet_observability.py`.
</artifacts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 test scaffold + fleet aggregate endpoint (FOBS-03, D-03, V4)</name>
  <files>backend/tests/test_agent_fleet_observability.py, backend/agent_fleet_observability_endpoints.py</files>
  <read_first>backend/agent_core_endpoints.py lines 1-140 for the is_super_admin tenant-gating pattern; backend/agent_auto_update_service.py lines 19-25 for _parse_ver / _LATEST_AGENT_VERSION.</read_first>
  <behavior>
    - A super-admin caller sees agents across all tenants; offline_agents == agents with status == "Offline".
    - A tenant-admin caller sees only their own tenant's agents (query gains a tenantId filter).
    - version_drift contains only agents whose _parse_ver(version) is not None AND < _parse_ver(_LATEST_AGENT_VERSION).
    - An agent with a malformed/absent version (_parse_ver → None) is NOT in version_drift and does not raise.
    - Response includes latest_version == _LATEST_AGENT_VERSION and counts for offline + drifted.
  </behavior>
  <action>Create `backend/agent_fleet_observability_endpoints.py` with `router = APIRouter(prefix="/api/fleet", tags=["Fleet Observability"])` and `GET /observability` taking `current_user=Depends(get_current_user)`. Access control (V4, Pitfall 5): follow `agent_core_endpoints.get_agents()` exactly — build the agents query filter, and only when NOT `is_super_admin(current_user.role)` add `tenantId = current_user.tenant_id`. Read through the request-scoped wrapped `db.agents` from `get_database()` — NEVER `db._db.agents` (that would leak cross-tenant). Import `_parse_ver` and `_LATEST_AGENT_VERSION` from `agent_auto_update_service` (do not re-implement version parsing — Don't Hand-Roll). Build `offline_agents` from the fetched set where `status == "Offline"` (the set monitor_agent_status maintains — do not invent a freshness heuristic). Build `version_drift` where `_parse_ver(a.get("version"))` is not None and `< _parse_ver(_LATEST_AGENT_VERSION)`; agents whose parse is None are simply excluded (fails closed, no crash). Return `{latest_version, offline_agents, version_drift, offline_count, drift_count}` with each agent entry projected to a small shape (id, hostname, status, version, tenantId). Write `backend/tests/test_agent_fleet_observability.py` (hermetic mock db) covering all five behaviors, including a `-k version_drift` selectable case for the malformed-version guard.</action>
  <verify>
    <automated>backend/venv/bin/python -m pytest backend/tests/test_agent_fleet_observability.py -q</automated>
  </verify>
  <done>Endpoint returns correct offline + drift lists; tenant-admin is scoped to own tenant; malformed version excluded without error.</done>
</task>

<task type="auto">
  <name>Task 2: Register the fleet router (FOBS-03)</name>
  <files>backend/router_registry.py, backend/tests/test_agent_fleet_observability.py</files>
  <read_first>backend/router_registry.py line 279 area — the _load(app, "<module>", "router") convention.</read_first>
  <action>Register the new router in `backend/router_registry.py` via `_load(app, "agent_fleet_observability_endpoints", "router")`, placed near the other agent endpoint registrations (line 279 area). Ensure the `/api/fleet` prefix does not collide with any existing route (grep confirms `/api/fleet` is new). Add an import-smoke assertion to the test file (or verify via the command below) that the module and router_registry import cleanly with the new route present.</action>
  <verify>
    <automated>backend/venv/bin/python -c "import agent_fleet_observability_endpoints, router_registry" 2>&1 | tail -1 && backend/venv/bin/python -m pytest backend/tests/test_agent_fleet_observability.py -q</automated>
  </verify>
  <done>Fleet router imports and registers without error; no prefix collision; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → GET /api/fleet/observability | Authenticated admin; role decides fleet-wide vs tenant-scoped visibility |
| endpoint → agents (Mongo) | Tenant scope enforced via wrapped db + is_super_admin gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-07 | Information Disclosure | tenant-admin seeing other tenants' offline/drift agents | mitigate | is_super_admin gate adds tenantId filter for non-super; request-scoped wrapped db only, never db._db (Pitfall 5) |
| T-48-08 | Tampering | forged `version` string to evade drift flag | accept | Version-drift is informational fleet-health, not a security gate; _parse_ver fails closed to None on malformed input |
| T-48-09 | Denial of Service | large fleet aggregate | accept | Read-only projection of existing agents docs; bounded by tenant's agent count, mirrors existing GET /api/agents |
| T-48-SC | Tampering | package installs | accept | No new packages this plan |
</threat_model>

<verification>
- `backend/venv/bin/python -m pytest backend/tests/test_agent_fleet_observability.py -q` green.
- Fleet router imports and registers.
- Full backend suite shows no new failures vs documented pre-existing.
</verification>

<success_criteria>
FOBS-03 backend exists: `GET /api/fleet/observability` returns the offline set and the version-drift list (reusing monitor_agent_status's status field and `_parse_ver`), tenant-scoped for non-super-admins, without any raw-db cross-tenant access in the request path.
</success_criteria>

<output>
Create `.planning/phases/48-fleet-observability-uptime-rollups/48-03-SUMMARY.md` when done.
</output>
