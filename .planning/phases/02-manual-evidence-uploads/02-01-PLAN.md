---
phase: 02-manual-evidence-uploads
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/compliance_artifacts_endpoints.py
  - backend/compliance_evidence_endpoints.py
  - backend/tests/test_evidence_uploads.py
autonomous: true
requirements:
  - EVID-01
  - EVID-02
  - EVID-04
  - EVID-05
must_haves:
  truths:
    - "Uploading a PDF/PNG/JPEG/DOCX/XLSX <=25 MB to the evidence endpoint returns 200 (EVID-01)"
    - "Uploading a file larger than 25 MB returns 413 (EVID-01)"
    - "A stored manual evidence record contains uploaded_by, description, tenantId, source:'manual', systemGenerated:false (EVID-02)"
    - "A file owner can delete their own evidence; an admin can delete any tenant's evidence (EVID-04)"
    - "Deleting a systemGenerated (automated) evidence record is rejected with 403 (EVID-04)"
    - "Uploading a .js file renamed to .pdf is rejected because the magic bytes do not match the extension (EVID-05)"
  artifacts:
    - path: "backend/compliance_artifacts_endpoints.py"
      provides: "_check_magic helper and _MAGIC_SIGNATURES mapping (stdlib bytes slicing)"
      contains: "_check_magic"
    - path: "backend/compliance_evidence_endpoints.py"
      provides: "Extended upload handler with size cap + full metadata + magic check; new asset-scoped DELETE handler"
      contains: "def delete_compliance_evidence"
    - path: "backend/tests/test_evidence_uploads.py"
      provides: "Pytest coverage for EVID-01/02/04/05"
      exports: ["test_upload_size_limit", "test_upload_record_schema", "test_delete_own_evidence", "test_magic_bytes_mismatch"]
  key_links:
    - from: "backend/compliance_evidence_endpoints.py"
      to: "backend/compliance_artifacts_endpoints.py"
      via: "import _check_magic"
      pattern: "from compliance_artifacts_endpoints import.*_check_magic"
    - from: "backend/compliance_evidence_endpoints.py (DELETE handler)"
      to: "db.asset_compliance"
      via: "$pull evidence sub-document by id"
      pattern: "\\$pull.*evidence"
---

<objective>
Close the four backend gaps in the manual evidence upload flow: enforce a 25 MB size cap (EVID-01), write full metadata on the evidence record (EVID-02), add an asset-scoped DELETE endpoint with owner/admin RBAC (EVID-04), and add stdlib magic-byte MIME validation (EVID-05).

Purpose: The upload endpoint already exists and works for the happy path, but it accepts oversized files, writes an under-populated record, has no delete path, and trusts the browser-reported Content-Type. These gaps block EVID-01/02/04/05.
Output: Extended `compliance_evidence_endpoints.py`, a `_check_magic` helper in `compliance_artifacts_endpoints.py`, and a new pytest module covering all four requirements.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-manual-evidence-uploads/02-RESEARCH.md
@.planning/phases/02-manual-evidence-uploads/02-PATTERNS.md
@backend/compliance_evidence_endpoints.py
@backend/compliance_artifacts_endpoints.py
@backend/tests/conftest.py
@backend/tests/test_rust_heartbeat_parity.py

Key constraints (from CLAUDE.md and RESEARCH.md):
- Do NOT create any new source files beyond the test file; extend the two existing backend files only.
- Keep both backend files under 500 lines.
- Magic-byte check must use ONLY Python stdlib (bytes slicing). Do NOT add python-magic / filetype / libmagic.
- Do NOT redeclare UPLOAD_DIR, _write_binary, _ALLOWED_UPLOAD_EXTENSIONS — they live in compliance_artifacts_endpoints.py and are imported.
- Canonical super-role set is `_SUPER_ROLES` at compliance_evidence_endpoints.py L18.
- Validate size and magic bytes on the in-memory `file_content` BEFORE writing to disk. Read the upload stream exactly once.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0: Scaffold failing test module for EVID-01/02/04/05</name>
  <files>backend/tests/test_evidence_uploads.py</files>
  <behavior>
    Nyquist Wave 0: `backend/tests/test_evidence_uploads.py` does not exist yet. Create it first so every later task has an automated gate. Reuse fixtures from `backend/tests/conftest.py` (mock `current_user`, mock async MongoDB) following the style in `test_rust_heartbeat_parity.py`. Tests target the handler functions directly with AsyncMock DB (CI-compatible, no live server).
    - test_upload_allowed_types: a valid PDF (content starts with b"%PDF-") under 25 MB returns success and writes a record (EVID-01)
    - test_upload_size_limit: 26 MB payload raises HTTPException 413 (EVID-01)
    - test_upload_record_schema: returned evidence record has keys uploaded_by, description, tenantId, source=="manual", systemGenerated==False (EVID-02)
    - test_magic_bytes_valid_pdf: .pdf with b"%PDF-" magic passes the magic check (EVID-05)
    - test_magic_bytes_mismatch: .pdf whose content starts with b"<script" raises HTTPException 400 (EVID-05)
    - test_delete_own_evidence: owner deletes their own manual record -> 200 / success (EVID-04)
    - test_delete_other_user_evidence: non-owner non-admin -> HTTPException 403 (EVID-04)
    - test_admin_delete_any_evidence: admin role deletes another tenant's manual record -> success (EVID-04)
    - test_delete_automated_evidence_blocked: record with systemGenerated==True -> HTTPException 403 (EVID-04)
  </behavior>
  <action>Create the test module importing `upload_compliance_evidence` and `delete_compliance_evidence` from `compliance_evidence_endpoints` and `_check_magic` from `compliance_artifacts_endpoints`. Mock `get_database` to return an AsyncMock whose `asset_compliance.aggregate(...).to_list` and `asset_compliance.update_one` are stubbed per case, and `assets.find_one` returns a tenant-matching asset. Use `pytest.mark.asyncio`. Build a fake `UploadFile`/`current_user` per the conftest fixtures. **Limiter test strategy (REVIEW MEDIUM #3):** the upload handler carries `@limiter.limit(...)` + a `request: Request` param, so slowapi resolves the limiter from `request.app.state.limiter`. Do NOT call the decorated coroutine with a bare Mock request — either (a) build a `Request`-like object whose `.app.state.limiter` is a `MagicMock()` that returns a truthy allow, or (b) drive the handler through `TestClient` with `app.dependency_overrides` for `get_current_user`/`get_database`. Pick one and state it in a module docstring so later tasks stay consistent. The tests MUST fail initially (RED) because the handlers do not yet enforce size/magic/metadata/delete — that is expected. Do not weaken assertions to make them pass.</action>
  <verify>
    <automated>cd backend && venv/bin/python -m pytest tests/test_evidence_uploads.py --collect-only -q >/dev/null 2>&1 && venv/bin/python -m pytest tests/test_evidence_uploads.py -q 2>&1 | grep -Eq '[0-9]+ failed' && echo RED-OK</automated>
  </verify>
  <done>Test module exists, collects cleanly (the `--collect-only` gate exits 0, proving no import/collection errors), and the run reports at least one genuine test FAILURE (a `N failed` summary line, not a collection/import error) on the not-yet-implemented behaviors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Add _check_magic helper + narrowed allowlist; harden upload handler (EVID-01, EVID-02, EVID-05)</name>
  <files>backend/compliance_artifacts_endpoints.py, backend/compliance_evidence_endpoints.py</files>
  <behavior>
    - _check_magic(content, ext) returns True when content's leading bytes match the signature for ext, False otherwise; returns True for extensions with no defined signature (EVID-05)
    - Upload of a .js file renamed to .pdf (content b"<script...") -> 400 "File content does not match extension" (EVID-05)
    - Upload >25 MB -> 413 (EVID-01)
    - Stored record includes uploaded_by, description, tenantId, source:"manual", systemGenerated:false (EVID-02)
  </behavior>
  <action>In `compliance_artifacts_endpoints.py`, add module-level `_MAGIC_SIGNATURES` (.pdf=b"%PDF-", .png=b"\x89PNG\r\n\x1a\n", .jpg/.jpeg=b"\xFF\xD8\xFF", .docx/.xlsx=b"PK\x03\x04") and a `_check_magic(content: bytes, ext: str) -> bool` function that returns `content[:len(sig)] == sig` (True when no signature defined). Export it so `compliance_evidence_endpoints.py` can import it. **Accepted limitation (REVIEW LOW #6):** `.docx`/`.xlsx` share the generic ZIP magic `PK\x03\x04`, so any ZIP renamed to `.docx`/`.xlsx` passes this check; deep OOXML container inspection is intentionally out of scope for a stdlib-only check — add a one-line code comment noting this accepted limitation next to the signature map. In `compliance_evidence_endpoints.py`: import `_check_magic`; add module-level `_EVIDENCE_ALLOWED_EXTENSIONS = frozenset({".pdf",".png",".jpg",".jpeg",".docx",".xlsx"})` and `_EVIDENCE_ALLOWED_MIME_PREFIXES = ("application/pdf","application/vnd.openxmlformats","application/msword","application/vnd.ms-excel","image/png","image/jpeg")`. Update `upload_compliance_evidence` to (a) add `description: str = Form("", max_length=1000)` param, (b) validate ext against `_EVIDENCE_ALLOWED_EXTENSIONS` and content_type against `_EVIDENCE_ALLOWED_MIME_PREFIXES` (narrowed, local — do NOT use the broad shared constants here per EVID-01), (c) after the single `file_content = await file.read()`, check `len(file_content) > 25 * 1024 * 1024` -> 413 (EVID-01), (d) call `_check_magic(file_content, file_ext)` -> 400 on mismatch (EVID-05), then write to disk, (e) compute `uploader = getattr(current_user, "username", "unknown")` and `tenant_id = getattr(current_user, "tenant_id", None) or ""`, generate id as `f"ev-manual-{uuid.uuid4().hex}"`, and add to the evidence_record: uploaded_by, description, tenantId, `source:"manual"`, `systemGenerated:False` (EVID-02). Also add `"tenantId": tenant_id` to the `$set` block so the download/delete tenant filter resolves (RESEARCH Pitfall 2). Preserve the existing try/except, rate-limit decorator if present, and `_write_binary`/`asyncio.to_thread` write path. Keep file under 500 lines.</action>
  <verify>
    <automated>cd backend && venv/bin/python -m pytest tests/test_evidence_uploads.py::test_upload_size_limit tests/test_evidence_uploads.py::test_upload_record_schema tests/test_evidence_uploads.py::test_magic_bytes_valid_pdf tests/test_evidence_uploads.py::test_magic_bytes_mismatch -x -q</automated>
  </verify>
  <done>Size cap, narrowed allowlist, magic-byte check, and full metadata are enforced; the four named tests pass; both files stay under 500 lines.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add asset-scoped DELETE endpoint with owner/admin RBAC + disk cleanup (EVID-04)</name>
  <files>backend/compliance_evidence_endpoints.py</files>
  <behavior>
    - Owner deletes own manual evidence -> success, sub-doc $pulled, file removed from disk (EVID-04)
    - Non-owner non-admin -> 403 (EVID-04)
    - Admin (_SUPER_ROLES) deletes any tenant's manual evidence -> success (EVID-04)
    - systemGenerated==True record -> 403, never $pulled (EVID-04)
    - Evidence id not found -> 404
  </behavior>
  <action>Add `DELETE /api/assets/{asset_id}/compliance/evidence/{evidence_id}` handler `delete_compliance_evidence` in `compliance_evidence_endpoints.py`. Steps: resolve `user_role`/`caller_tenant`/`caller_username` via getattr; `is_super = user_role in _SUPER_ROLES`. Look up the evidence sub-doc via aggregate pipeline (`$unwind` evidence, `$match` on `{"assetId": asset_id, "evidence.id": evidence_id}`, project evidence + tenantId) -> 404 if absent. Reject `ev.get("systemGenerated")` with 403 "Automated evidence cannot be deleted" (never $pull automated records — RESEARCH anti-pattern). Tenant scope: if not is_super and caller_tenant and `doc_tenant != caller_tenant` -> 403. Owner check: if not is_super and `ev.get("uploaded_by") != caller_username` -> 403. Then `$pull` the sub-doc — **re-scope the update by tenant, not just the lookup (REVIEW LOW #5):** build the update filter as `{"assetId": asset_id, "evidence.id": evidence_id}` plus, when `not is_super and caller_tenant`, `"tenantId": caller_tenant`, then `update_one(filter, {"$pull": {"evidence": {"id": evidence_id}}})`. Carrying the tenant filter into the update (and not only the aggregate lookup) closes the benign cross-tenant TOCTOU window. Disk cleanup: derive filename from `ev["url"]` via `Path(url).name`, apply the path-traversal guard from the download handler (resolve under `Path(UPLOAD_DIR).resolve()`, startswith check), and `unlink(missing_ok=True)` if it exists. Wrap in the same try/except (re-raise HTTPException, log + 500 otherwise). Use the canonical L18 `_SUPER_ROLES`. Route is asset-scoped per the locked phase decision (frontend calls `/api/assets/{assetId}/compliance/evidence/{evidenceId}`).</action>
  <verify>
    <automated>cd backend && venv/bin/python -m pytest tests/test_evidence_uploads.py::test_delete_own_evidence tests/test_evidence_uploads.py::test_delete_other_user_evidence tests/test_evidence_uploads.py::test_admin_delete_any_evidence tests/test_evidence_uploads.py::test_delete_automated_evidence_blocked -x -q</automated>
  </verify>
  <done>DELETE endpoint enforces systemGenerated guard, tenant isolation, owner/admin rules, $pulls the sub-doc, and cleans up the disk file; the four delete tests pass; file under 500 lines.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> upload API | Untrusted multipart file content, filename, content-type, description cross here |
| browser -> delete API | Untrusted evidence_id + asset_id; caller identity from JWT only |
| DB record -> disk path | `evidence.url` filename used to locate a file on disk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | upload_compliance_evidence (malicious file disguised as PDF) | mitigate | `_check_magic(file_content, ext)` validates leading bytes before disk write (EVID-05) |
| T-02-02 | Denial of Service | upload_compliance_evidence (oversized file) | mitigate | 413 when `len(file_content) > 25*1024*1024` (EVID-01) |
| T-02-03 | Elevation of Privilege | delete_compliance_evidence (deleting another user's evidence) | mitigate | Owner check `uploaded_by == caller_username` for non-admins (EVID-04) |
| T-02-04 | Information Disclosure | delete_compliance_evidence (cross-tenant access) | mitigate | Tenant scope check `doc_tenant == caller_tenant` for non-super roles |
| T-02-05 | Tampering | delete_compliance_evidence (deleting automated evidence) | mitigate | Reject `systemGenerated == True` with 403 before any $pull |
| T-02-06 | Tampering / EoP | disk cleanup path from `evidence.url` | mitigate | `Path(UPLOAD_DIR).resolve()` + startswith guard before `unlink` |
| T-02-SC | Tampering | npm/pip installs | accept | No new packages introduced this phase (RESEARCH: stdlib-only); audit table N/A |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (new symbols — exclude from drift verification)
- `_MAGIC_SIGNATURES` (module-level dict, compliance_artifacts_endpoints.py)
- `_check_magic(content, ext) -> bool` (function, compliance_artifacts_endpoints.py)
- `_EVIDENCE_ALLOWED_EXTENSIONS`, `_EVIDENCE_ALLOWED_MIME_PREFIXES` (module-level constants, compliance_evidence_endpoints.py)
- `delete_compliance_evidence` (async handler, compliance_evidence_endpoints.py)
- `description` Form param on `upload_compliance_evidence`
- Evidence-record fields written by upload: `uploaded_by`, `description`, `tenantId`, `source`, `systemGenerated`
- New file: `backend/tests/test_evidence_uploads.py` (tests: `test_upload_allowed_types`, `test_upload_size_limit`, `test_upload_record_schema`, `test_magic_bytes_valid_pdf`, `test_magic_bytes_mismatch`, `test_delete_own_evidence`, `test_delete_other_user_evidence`, `test_admin_delete_any_evidence`, `test_delete_automated_evidence_blocked`)
</artifacts_produced>

<review_resolution>
## Cross-AI review disposition (02-REVIEWS.md)
- HIGH — Task 0 RED gate falsifiable: FIXED (verify now `--collect-only` exit-0 + `N failed` summary-line assert, distinguishing genuine failure from import/collection error).
- MEDIUM — slowapi limiter/`Request` test friction: FIXED (Task 0 action now states the limiter test strategy explicitly).
- LOW — `$pull` not tenant-scoped: FIXED (Task 2 carries the tenant filter into `update_one`).
- LOW — docx/xlsx magic weakness: ACKNOWLEDGED (Task 1 documents the accepted ZIP-magic limitation).
- MEDIUM (WR-02) missing-tenant non-super 403 and (CR-02) uploads moved off the public static mount to `private_uploads/`: ALREADY RESOLVED in shipped code — no re-plan required (see 02-REVIEWS.md disposition).
- LOW/MEDIUM (cycle 3) — verify gates invoked nonexistent `python` binary (fail-closed): FIXED. All three `<automated>` gates (Task 0/1/2) and both `<verification>` pytest lines already `cd backend` first, so the runner is now the `backend/`-relative `venv/bin/python -m pytest …` (per reviewer: `python` is not a command on host — `python3` only, deps live in `backend/venv/bin/python`; the redundant `backend/` prefix is dropped so it does not resolve to `backend/backend/venv/...`). Runner-path fix only — pytest args, grep gates, and `<done>` semantics unchanged.
</review_resolution>

<verification>
- `cd backend && venv/bin/python -m pytest tests/test_evidence_uploads.py -q` passes all EVID-01/02/04/05 cases.
- `cd backend && venv/bin/python -m pytest tests/ -q` shows no regressions in existing evidence/compliance tests.
- `grep -n "_check_magic" backend/compliance_artifacts_endpoints.py backend/compliance_evidence_endpoints.py` shows definition + import.
- Both backend files remain under 500 lines (`wc -l`).
</verification>

<success_criteria>
- Upload of an allowed type <=25 MB succeeds; >25 MB returns 413.
- Stored manual record includes uploaded_by, description, tenantId, source:"manual", systemGenerated:false.
- DELETE removes owner's record + disk file; blocks non-owner, cross-tenant, and automated deletions; admin can delete any tenant's manual evidence.
- Extension/content mismatch is rejected via magic-byte check using stdlib only.
- No new packages added; no new source files beyond the test module.
</success_criteria>

<output>
Create `.planning/phases/02-manual-evidence-uploads/02-01-SUMMARY.md` when done
</output>
