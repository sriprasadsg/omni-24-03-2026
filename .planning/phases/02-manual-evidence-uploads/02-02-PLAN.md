---
phase: 02-manual-evidence-uploads
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - services/apiService.ts
  - components/AssetComplianceList.tsx
autonomous: false
requirements:
  - EVID-03
  - EVID-04
must_haves:
  truths:
    - "uploadComplianceEvidence sends FormData without an explicit Content-Type header so the browser sets the multipart boundary (bug fix)"
    - "A user can type a description in the upload flow and it is sent with the file (EVID-03 / supports EVID-02 metadata)"
    - "Each evidence row shows a source badge: 'Manual' for uploaded files, 'Automated' for system-generated (EVID-03)"
    - "Each manual evidence row has a delete button that calls the DELETE endpoint and refreshes the list (EVID-04)"
    - "deleteComplianceEvidence(assetId, controlId, evidenceId) calls DELETE /api/assets/{assetId}/compliance/evidence/{evidenceId} (EVID-04)"
  artifacts:
    - path: "services/apiService.ts"
      provides: "Fixed uploadComplianceEvidence (no Content-Type override, description param) + new deleteComplianceEvidence"
      contains: "deleteComplianceEvidence"
    - path: "components/AssetComplianceList.tsx"
      provides: "Description input, source badge per row, delete button per manual evidence row"
      contains: "Manual"
  key_links:
    - from: "components/AssetComplianceList.tsx (delete button)"
      to: "services/apiService.ts deleteComplianceEvidence"
      via: "onDeleteEvidence callback"
      pattern: "onDeleteEvidence"
    - from: "services/apiService.ts deleteComplianceEvidence"
      to: "DELETE /api/assets/{assetId}/compliance/evidence/{evidenceId}"
      via: "authFetch method DELETE"
      pattern: "method:\\s*'DELETE'"
---

<objective>
Wire the frontend to the hardened backend: fix the multipart Content-Type bug, add a description field to the upload flow, add a source badge and a delete button to each evidence row, and add the `deleteComplianceEvidence` API wrapper.

Purpose: With the backend now writing full metadata and exposing a DELETE route (02-01), the UI must let users provide a description, distinguish manual vs automated evidence visually (EVID-03), and delete their own uploads (EVID-04). The existing `uploadComplianceEvidence` also carries a boundary bug that breaks uploads until fixed.
Output: Updated `services/apiService.ts` and `components/AssetComplianceList.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-manual-evidence-uploads/02-RESEARCH.md
@.planning/phases/02-manual-evidence-uploads/02-PATTERNS.md
@components/AssetComplianceList.tsx
@services/apiService.ts

Depends on 02-01: the asset-scoped DELETE route `/api/assets/{asset_id}/compliance/evidence/{evidence_id}` and the manual-record fields (`source`, `uploaded_by`, `systemGenerated:false`) must already exist server-side. The `description` Form param is read by the upload handler from 02-01.

Constraints (from CLAUDE.md / RESEARCH.md):
- Modify only the two existing files; do NOT create new files.
- Do NOT set `Content-Type` on any FormData fetch — `authFetch` (apiService.ts ~L206-208) strips it so the browser sets the multipart boundary. The explicit override is the bug to remove.
- Source discrimination follows existing convention: `ev.systemGenerated === true` => Automated; otherwise Manual.
- Reuse existing icon-button styling; import a trash icon from `./icons`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Content-Type bug + add description param + add deleteComplianceEvidence wrapper (EVID-04)</name>
  <files>services/apiService.ts</files>
  <action>In `uploadComplianceEvidence` (~L633-646): remove the `headers: { 'Content-Type': 'multipart/form-data' }` line entirely so `authFetch` handles the multipart boundary. Add an optional `description?: string` parameter and, when provided, `formData.append('description', description)`. Then add a new exported `deleteComplianceEvidence(assetId: string, controlId: string, evidenceId: string): Promise<void>` that calls `authFetch(\`${API_BASE}/assets/${assetId}/compliance/evidence/${evidenceId}\`, { method: 'DELETE' })` (no Content-Type) and throws `new Error("Evidence delete failed")` when `!res.ok`. The `controlId` param is kept in the signature for caller symmetry even though the route is asset+evidence scoped.</action>
  <verify>
    <automated>cd /home/user/enterprise-omni-agent-ai-platform && grep -q "deleteComplianceEvidence" services/apiService.ts && ! awk '/uploadComplianceEvidence = async/{f=1} f&&/^\s*(const|export)[^=]*=/&&!/uploadComplianceEvidence/{f=0} f' services/apiService.ts | grep -q "Content-Type" && ! (npx tsc --noEmit 2>&1 | grep -v node_modules | grep -q "error TS") && echo OK</automated>
  </verify>
  <done>Content-Type override removed from the upload call, description optionally appended, deleteComplianceEvidence exported, and TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Add description input + source badge + delete button to AssetComplianceList (EVID-03, EVID-04)</name>
  <files>components/AssetComplianceList.tsx</files>
  <action>Extend the props interface: change `onUploadEvidence` to `(assetId: string, file: File, description?: string)` and add `onDeleteEvidence: (assetId: string, controlId: string, evidenceId: string) => Promise<void>`. Add a `description` text input rendered inline in the Actions cell (or adjacent to the upload button) bound to a `descriptionMap` keyed by assetId in local state; give the input `maxLength={1000}` to mirror the server's 1000-char cap (REVIEW LOW #7) so the client cannot submit a value the backend would reject/truncate; pass the current value into `onUploadEvidence(selectedAssetId, file, descriptionMap[selectedAssetId])` inside `handleFileChange`, then clear it. In the evidence render loop (the `statusRecord.evidence.map`), for each row add: (a) a source badge — when `ev.systemGenerated` truthy render an "Automated" badge (e.g. gray/blue pill), otherwise render a "Manual" badge (e.g. green pill); reuse the existing pill class pattern from the status cell; (b) a delete button rendered only when `!ev.systemGenerated`, using a trash icon imported from `./icons`, with `onClick={() => onDeleteEvidence(asset.id, control.id, ev.id || ev.evidence_id)}`, a `window.confirm` guard before calling, and `title="Delete evidence"`. Keep the existing markdown-vs-download branch intact. Do not delete automated rows (button hidden for them). Keep file under 500 lines.</action>
  <verify>
    <automated>cd /home/user/enterprise-omni-agent-ai-platform && grep -q "onDeleteEvidence" components/AssetComplianceList.tsx && grep -q "Manual" components/AssetComplianceList.tsx && grep -q "Automated" components/AssetComplianceList.tsx && grep -q "maxLength={1000}" components/AssetComplianceList.tsx && ! (npx tsc --noEmit 2>&1 | grep -v node_modules | grep -q "error TS") && echo OK</automated>
  </verify>
  <done>Description input present and wired into upload; each evidence row shows a Manual/Automated badge; manual rows show a confirm-guarded delete button calling onDeleteEvidence; TypeScript compiles.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Frontend manual-evidence UI: description field, Manual/Automated source badges, and a delete button per manual evidence row, wired end-to-end to the 02-01 backend (upload with metadata + DELETE).</what-built>
  <how-to-verify>
    1. Start the stack and open a compliance control with at least one asset.
    2. Type a description, click the upload button, and select a PDF (<=25 MB). Confirm the file appears in the Evidence column with a "Manual" badge.
    3. Confirm any agent-collected evidence on the same control shows an "Automated" badge.
    4. Click the delete button on the manual evidence row, confirm the prompt; the row disappears and does not reappear on refresh.
    5. Confirm the Automated row has NO delete button.
    6. (Optional) Try uploading a .js file renamed to .pdf — expect a clear rejection error from the backend (EVID-05).
  </how-to-verify>
  <resume-signal>Type "approved" or describe what rendered incorrectly</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser fetch -> backend | FormData (file + description) and DELETE requests cross here; backend (02-01) is authoritative for validation, tenant isolation, and owner checks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Tampering | uploadComplianceEvidence multipart boundary | mitigate | Remove explicit Content-Type so the browser sets a valid boundary; backend re-validates all content |
| T-02-08 | Elevation of Privilege | client-side delete button visibility | accept | UI hides delete on automated/non-owned rows for UX; backend (02-01) enforces owner/admin/tenant authority authoritatively |
| T-02-SC | Tampering | npm installs | accept | No new packages added; trash icon sourced from existing ./icons |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (new symbols — exclude from drift verification)
- `deleteComplianceEvidence(assetId, controlId, evidenceId)` (exported wrapper, services/apiService.ts)
- `description?` optional param on `uploadComplianceEvidence` (services/apiService.ts)
- `onDeleteEvidence` prop + `descriptionMap` local state (components/AssetComplianceList.tsx)
- "Manual"/"Automated" source badges + per-row delete button (components/AssetComplianceList.tsx)
</artifacts_produced>

<review_resolution>
## Cross-AI review disposition (02-REVIEWS.md)
- HIGH — inverted `tsc` gate (`grep -Eqv "error"`): FIXED — both tasks now use `! (... | grep -q "error TS")`, which fails when tsc emits errors.
- MEDIUM — fragile fixed-window `grep -A6` Content-Type guard: FIXED — Task 1 verify now uses an `awk` body-scoped range over the `uploadComplianceEvidence` function instead of a fixed 6-line window.
- LOW — client description length guard: FIXED — Task 2 adds `maxLength={1000}` mirroring the server cap.
- LOW — unused `controlId` param kept for caller symmetry: INTENTIONAL — retained by design (documented in Task 1 action).
</review_resolution>

<verification>
- `grep -n "deleteComplianceEvidence" services/apiService.ts` shows the new wrapper.
- The `uploadComplianceEvidence` function body contains NO `Content-Type` header (body-scoped `awk` range check, not a fixed-line window).
- `grep -n "Manual\|Automated\|onDeleteEvidence" components/AssetComplianceList.tsx` shows badges + delete wiring; `grep -q "maxLength={1000}"` confirms the client-side length guard.
- `! (npx tsc --noEmit 2>&1 | grep -v node_modules | grep -q "error TS")` — build has no type errors (gate fails on any `error TS`).
- Human verify checkpoint approved.
</verification>

<success_criteria>
- Multipart upload bug fixed (no explicit Content-Type on FormData fetch).
- Description is captured in the UI and sent with the file.
- Each evidence row is visually labelled Manual vs Automated (EVID-03).
- Manual evidence can be deleted from the UI with a confirm guard; automated evidence shows no delete affordance (EVID-04).
- No new files created; component and service stay under 500 lines.
</success_criteria>

<output>
Create `.planning/phases/02-manual-evidence-uploads/02-02-SUMMARY.md` when done
</output>
