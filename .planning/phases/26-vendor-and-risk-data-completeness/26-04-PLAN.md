---
phase: 26-vendor-and-risk-data-completeness
plan: 04
type: execute
wave: 2
depends_on: ["26-01", "26-02"]
files_modified:
  - components/VendorDetailModal.tsx
  - components/VendorManagement.tsx
autonomous: true
requirements: [VRISK-02]
must_haves:
  truths:
    - "Clicking a vendor row's action button opens a detail modal — VendorManagement's selectedVendor state is no longer dead code"
    - "The modal lists the vendor's subprocessors and an admin can add one (POST /api/vendors/{id}/subprocessors) and remove one (DELETE /api/vendors/{id}/subprocessors/{sub_id})"
    - "The modal shows DPA status for this vendor by fetching /api/dpa and filtering on vendor_id"
    - "VendorDetailModal is imported and rendered inside VendorManagement.tsx (reachable, not an orphaned component)"
    - "npm run build succeeds with the new component wired in"
  artifacts:
    - "components/VendorDetailModal.tsx (new — first-ever vendor detail view)"
    - "row onClick + VendorDetailModal render in components/VendorManagement.tsx"
  key_links:
    - "the vendor-row action button MUST call setSelectedVendor(vendor); today it has NO onClick (dead code) so the modal would be unreachable (Pitfall 1 class — built-but-not-wired)"
    - "add/remove MUST target the 26-02 subprocessor routes; the modal must never post through a generic vendor update"
    - "DPA status MUST filter /api/dpa (26-01) by vendor_id — the field 26-01 added on the DPA doc specifically so the vendor view can show DPA status per vendor"
---

<objective>
Deliver the frontend half of VRISK-02: build the vendor detail view that does not exist today, as a modal, and surface a vendor's subprocessors (list/add/remove) plus its DPA status.

Purpose: `VendorManagement.tsx` already declares `selectedVendor` state and renders a `MoreHorizontal` action button, but the button has no `onClick` and the state is never read — dead code. "Surfaced in the vendor risk view" (VRISK-02) requires a real detail view. This is a modal (not a routed dashboard), so it needs NO App.tsx/Sidebar.tsx/types.ts nav-wiring — but it MUST be wired into VendorManagement so it is actually reachable (the recurring built-but-unreachable defect class in this project's history).

Output: `components/VendorDetailModal.tsx` (new) and the two-line wiring in `components/VendorManagement.tsx`. Uses `authFetch` directly (mirroring `BAAManagement.tsx`) — no new apiService or types.ts edits.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-vendor-and-risk-data-completeness/26-RESEARCH.md
@.planning/phases/26-vendor-and-risk-data-completeness/26-PATTERNS.md
@components/VendorManagement.tsx
@components/RiskFormModal.tsx
@components/BAAManagement.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create components/VendorDetailModal.tsx</name>
  <files>components/VendorDetailModal.tsx</files>
  <read_first>
    - components/RiskFormModal.tsx lines 1-45 (the lightweight overlay + rounded panel + X-close modal chrome to copy) and 116-131 (footer button styling)
    - components/BAAManagement.tsx lines 1-3, 59, 72, 86, 107 (authFetch import and the GET/POST/DELETE-with-body call shape to mirror for the DPA fetch and subprocessor mutations)
    - components/VendorManagement.tsx lines 9-31 (the local Vendor/VendorAssessment interface shape) and 55-62 (getCriticalityColor)
    - 26-PATTERNS.md "components/VendorDetailModal.tsx (NEW)" section (lines 346-391)
  </read_first>
  <action>
    Create components/VendorDetailModal.tsx as a named export (`export const VendorDetailModal`), matching the RiskFormModal/VendorFormModal named-export convention. Import React/useState/useEffect, the X icon from lucide-react, and `authFetch` from '../services/apiService'.

    Define local interfaces in this file (this repo defines component interfaces locally — see VendorManagement/RiskRegister):
    - Subprocessor: id, name, purpose, data_categories (string[]), location, added_at (all strings except data_categories).
    - DpaSummary: id, status (string), signed_by_us (boolean), signed_by_vendor (boolean), vendor_id (optional string).
    - A LOOSE vendor prop type so VendorManagement's richer local Vendor is assignable: an interface with id (string), name (string), criticality (optional string), subprocessors (optional Subprocessor array).
    Props: isOpen (boolean), onClose (function), vendor (that vendor type or null), onRefresh (optional function). Guard with an early `if (!isOpen || !vendor) return null;`.

    Copy the RiskFormModal overlay+panel chrome verbatim (fixed inset-0 bg-black/50 flex items-center justify-center z-50 p-4 wrapping bg-white dark:bg-gray-800 rounded-xl shadow-xl w-full max-w-lg, with a header row showing the vendor name as the h3 and the X close button). A slightly wider panel (max-w-2xl) is fine; keep the same class vocabulary.

    State: subs initialized from vendor.subprocessors or an empty array; dpas as an empty array; a small form state object for the add-subprocessor inputs (name, purpose, data_categories as a comma string, location) and a submitting flag. Sync subs when the vendor prop changes via a useEffect keyed on [vendor] that resets subs to vendor.subprocessors or an empty array.

    On open, fetch DPA status: a useEffect keyed on isOpen and vendor id calls authFetch('/api/dpa'), reads r.ok ? r.json() : [], and stores the returned list filtered to entries whose vendor_id equals vendor.id. Render a "Data Processing Agreements" section: if the filtered list is empty show a "No DPA on file" message; otherwise list each DPA with its status (draft/pending/active/terminated) rendered as a small status pill.

    Render a "Subprocessors" section listing each sub (name, location, purpose) with a remove button that calls authFetch on the path /api/vendors/{vendor.id}/subprocessors/{sub.id} with method DELETE; on res.ok remove it from subs and call onRefresh if provided. Below the list, an inline add form: on submit call authFetch on /api/vendors/{vendor.id}/subprocessors with method POST and a JSON body containing name, purpose, data_categories (the comma string split and trimmed into an array), and location; on res.ok append the returned sub to subs, clear the form, and call onRefresh if provided. Do NOT set an explicit Content-Type header (browser/authFetch handles JSON, consistent with BAAManagement's POST). On a non-ok response leave state unchanged (no throw needed for this UI).
  </action>
  <verify>
    <automated>cd /home/user/enterprise-omni-agent-ai-platform && grep -q "export const VendorDetailModal" components/VendorDetailModal.tsx && grep -q "/api/vendors/" components/VendorDetailModal.tsx && grep -q "subprocessors" components/VendorDetailModal.tsx && grep -q "/api/dpa" components/VendorDetailModal.tsx && grep -q "authFetch" components/VendorDetailModal.tsx</automated>
  </verify>
  <acceptance_criteria>
    - VendorDetailModal.tsx exists as a named export with isOpen/onClose/vendor/onRefresh props and returns null when closed or vendor is null
    - it lists subprocessors, has a working add form (POST) and remove button (DELETE) against /api/vendors/{id}/subprocessors[/{sub_id}]
    - it fetches /api/dpa and shows DPA status filtered by vendor_id
    - it uses authFetch and the RiskFormModal overlay/panel chrome
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wire VendorDetailModal into VendorManagement.tsx and verify reachability</name>
  <files>components/VendorManagement.tsx</files>
  <read_first>
    - components/VendorManagement.tsx line 7 (VendorFormModal import), 38 (selectedVendor state — currently dead), 174-178 (the MoreHorizontal action button with no onClick), 185-192 (the existing VendorFormModal render block to mirror)
    - 26-PATTERNS.md "components/VendorManagement.tsx (EXTEND)" section (lines 395-424)
  </read_first>
  <action>
    Three edits, mirroring the existing showAddModal/VendorFormModal wiring:
    1. Add an `import { VendorDetailModal } from './VendorDetailModal';` alongside the VendorFormModal import (line 7).
    2. Give the vendor-row action button an onClick that calls setSelectedVendor(vendor), on the button at lines 174-178 (keep its existing className and the MoreHorizontal icon).
    3. After the existing VendorFormModal render block (around line 192, still inside the outer div), render a VendorDetailModal element with isOpen set to whether selectedVendor is truthy, onClose clearing selectedVendor to null, vendor set to selectedVendor, and onRefresh set to fetchVendors.

    selectedVendor and setSelectedVendor already exist (line 38) — reuse them; do not add new state. Do not otherwise restructure the component. VendorManagement's local Vendor type is structurally assignable to the modal's loose vendor prop, so no interface change is required.
  </action>
  <verify>
    <automated>cd /home/user/enterprise-omni-agent-ai-platform && grep -q "import { VendorDetailModal }" components/VendorManagement.tsx && grep -q "setSelectedVendor(vendor)" components/VendorManagement.tsx && grep -q "onRefresh={fetchVendors}" components/VendorManagement.tsx && npm run build</automated>
    <human-check>Open the app, go to Vendor Management, click a vendor row's action (MoreHorizontal) button, and confirm the detail modal opens showing the Subprocessors section (with working add + remove) and a Data Processing Agreements status section. Confirms the modal is actually reachable, not just built (this project's recurring built-but-unwired defect class).</human-check>
  </verify>
  <acceptance_criteria>
    - VendorManagement imports and renders VendorDetailModal, opened by the row action button via setSelectedVendor(vendor)
    - onRefresh={fetchVendors} is passed so the list refreshes after add/remove
    - npm run build succeeds (the component is compiled into the app, not orphaned)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| VendorDetailModal → /api/vendors/{id}/subprocessors, /api/dpa | Browser sends authenticated (authFetch) mutations/reads; server-side RBAC + tenant scope (26-01/26-02) are the authoritative access controls |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-09 | Information Disclosure | client-side DPA filtering by vendor_id | medium | mitigate | /api/dpa is already tenant-scoped server-side (26-01) — the client filter is display-only and never the security boundary; the modal only reads what the tenant-scoped API returns |
| T-26-10 | Tampering (XSS) | rendering subprocessor/DPA free-text (name, purpose, location) | low | accept | React escapes JSX text nodes by default; no dangerouslySetInnerHTML is used, so injected markup renders as inert text |
| T-26-SC | Tampering | npm installs | low | accept | No new npm packages this plan — uses existing react/lucide-react/authFetch; Package Legitimacy Gate N/A |
</threat_model>

<verification>
- `npm run build` succeeds
- `grep` confirms VendorDetailModal is imported and rendered in VendorManagement.tsx (reachability, per the project's chunk-output verification convention)
- Human check: modal opens from a vendor row and shows subprocessors (add/remove) + DPA status
</verification>

<success_criteria>
- The vendor row action button opens a detail modal (selectedVendor wired)
- Subprocessors can be listed, added, and removed from the modal against the 26-02 routes
- DPA status per vendor is shown from /api/dpa filtered on vendor_id
- Build succeeds; component is reachable
</success_criteria>

## Artifacts This Plan Produces

- **New component:** `components/VendorDetailModal.tsx` — named export `VendorDetailModal`; props `{ isOpen, onClose, vendor, onRefresh? }`
- **New local interfaces (in VendorDetailModal.tsx):** `Subprocessor`, `DpaSummary`, and a loose vendor prop interface
- **Modified component:** `components/VendorManagement.tsx` — VendorDetailModal import, row action `onClick={() => setSelectedVendor(vendor)}`, and the `<VendorDetailModal .../>` render block wired to `fetchVendors`

<output>
Create `.planning/phases/26-vendor-and-risk-data-completeness/26-04-SUMMARY.md` when done
</output>
</content>
