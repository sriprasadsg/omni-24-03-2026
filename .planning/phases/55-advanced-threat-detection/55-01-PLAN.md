---
phase: 55-advanced-threat-detection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/siem_engine.py
  - backend/threat_intel_endpoints.py
  - backend/tests/test_siem_engine.py
autonomous: true
requirements: [INT-04]
user_setup: []

must_haves:
  truths:
    - "Native v3.4 findings (security_scan_results, vulnerabilities, fim_events, remediation_audit) are read per-tenant, normalized into the SIEM event shape, and evaluated against the tenant's existing siem_rules — a matching rule creates a security_cases document (INT-04, D-01)."
    - "Every correlation read is bounded with .to_list(length=N) — no unbounded .find({}) scan of any of the four native collections (INT-04)."
    - "Correlation reads run under set_tenant_id(tenant_id)/reset_tenant_id and go through the tenant-isolated db handle — never db._db (scope fence, ASVS V4)."
  artifacts:
    - "backend/siem_engine.py — new async correlate_native_findings(self, tenant_id) method on SiemEngine"
    - "backend/threat_intel_endpoints.py — new companion trigger route that invokes correlate_native_findings for the caller's tenant"
    - "backend/tests/test_siem_engine.py — new test file (module has zero direct coverage today)"
  key_links:
    - "correlate_native_findings() normalizes the 4 native collections into the SAME event dict shape _normalize_log emits, then calls the EXISTING self._evaluate_rules(events, tenant_id) — so existing siem_rules apply to native findings with no rule redefinition."
    - "_trigger_alert() -> db.security_cases.insert_one is the existing case-creation sink reused unchanged."
  prohibitions:
    - statement: "MUST NOT add a second/parallel correlation loop or a new NativeFindingsCorrelator class — extend SiemEngine and reuse _evaluate_rules/_match_rule/_trigger_alert (D-01)."
      status: flagged
    - statement: "MUST NOT load unbounded correlation history — every native-collection read is capped via .to_list(length=N)."
      status: flagged
  flagged_assumptions:
    - "INT-04 was returned 'unclassified/unresolved' by the deterministic edge-probe (it correctly abstained on this domain-specific correlation requirement). Acceptance criteria here are authored from RESEARCH.md's Phase-Requirements Test Map and Pattern 1, not from an auto-classified probe result — flagged so a reviewer treats them as planner-authored, not probe-derived."
    - "RESEARCH Open Question 2: the correlation extension is implemented as a method ON SiemEngine (Option a) so existing siem_rules apply to native findings 'for free' — the strongest reading of D-01. If operators expect native-finding correlation to use a distinct rule set, that is a separate design and out of this plan's scope."
---

<objective>
INT-04 (threat-intel correlation): extend the existing native SIEM engine so that
the v3.4 native finding sources (NSCAN/VULN/FIM + remediation history) become
correlation inputs, evaluated against the tenant's existing correlation rules and
producing security_cases — WITHOUT building a parallel correlation path (D-01).

Purpose: This is the phase's correlation spine. It leads Phase 55 as a tracer: one
native finding read -> normalized -> rule-matched -> security_case created, proven
end-to-end before the AUT-03/COMM-01 paths expand out.

Output: correlate_native_findings() on SiemEngine, a companion trigger route in
threat_intel_endpoints.py, and the first-ever direct test file for siem_engine.py.

## Artifacts this phase produces (Plan 55-01)
- SiemEngine.correlate_native_findings(self, tenant_id) — new async method (backend/siem_engine.py)
- companion correlation-trigger route in backend/threat_intel_endpoints.py (new @router handler; reuses the file's existing _get_tenant/RBAC dependency pattern)
- backend/tests/test_siem_engine.py — new test file

## Flagged assumptions (surfaced, not dropped)
- See must_haves.flagged_assumptions — INT-04 probe abstention + Open-Question-2 design choice.
</objective>

<execution_context>
@/home/user/enterprise-omni-agent-ai-platform/.claude/gsd-core/workflows/execute-plan.md
@/home/user/enterprise-omni-agent-ai-platform/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-advanced-threat-detection/55-CONTEXT.md
@.planning/phases/55-advanced-threat-detection/55-RESEARCH.md
@.planning/phases/55-advanced-threat-detection/55-PATTERNS.md

@backend/siem_engine.py
@backend/native_security_ops_endpoints.py
@backend/threat_intel_endpoints.py
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end native-finding correlation — one path only</name>
  <files>backend/siem_engine.py, backend/tests/test_siem_engine.py</files>
  <read_first>
    - backend/siem_engine.py (the class being extended: _normalize_log's output event shape, _evaluate_rules/_match_rule/_trigger_alert, get_siem_engine factory)
    - backend/native_security_ops_endpoints.py (Phase 54 bounded-read/normalize/merge precedent, lines ~20-45 — the exact .to_list(length=200) shape to replicate)
    - backend/autonomous_remediation_service.py lines 131-190 (scan_for_remediable_findings — the set_tenant_id(tenant_id)/reset_tenant_id(_tctx) per-tenant read block pattern to mirror)
    - backend/database.py lines 1-160 (confirm security_scan_results/vulnerabilities/fim_events/remediation_audit are NOT in the tenant-isolation exemption list, so set_tenant_id is mandatory)
  </read_first>
  <action>
    Add async method correlate_native_findings(self, tenant_id) to SiemEngine. Wrap all
    DB access in set_tenant_id(tenant_id)/reset_tenant_id(_tctx) (import from tenant_context,
    finally-block reset). Read the four native collections bounded at length=200 each:
    security_scan_results, vulnerabilities, fim_events, remediation_audit — all filtered by
    {"tenantId": tenant_id}. Normalize each doc into the SAME event dict keys _normalize_log
    emits (id, tenant_id, agent_id, timestamp, source, raw, category, action, user), setting
    source to a stable per-collection label (native_scan / native_vuln / native_fim /
    native_remediation) and category/action derived from each doc's own severity/type/verdict
    fields. Then call the EXISTING self._evaluate_rules(normalized_events, tenant_id) — do not
    reimplement rule matching or case creation. For the tracer, wire ONLY this single path
    end-to-end (read -> normalize -> evaluate -> existing _trigger_alert -> security_cases insert);
    no batching across tenants, no scheduler. Return a small summary dict with an evaluated count
    and the list of sources.
    In test_siem_engine.py create the FIRST direct test: seed one security_scan_results doc plus
    one enabled siem_rules doc whose conditions match the normalized event's source/category,
    run correlate_native_findings for that tenant against a mock/in-memory db, and assert exactly
    one security_cases doc was inserted. Clone the fixture/mocking style from
    test_remediation_guards.py.
  </action>
  <verify>
    <automated>backend/venv/bin/python -m pytest backend/tests/test_siem_engine.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - backend/siem_engine.py contains `async def correlate_native_findings(self, tenant_id`
    - grep shows a `set_tenant_id(` call and a `reset_tenant_id(` call inside siem_engine.py
    - `grep -v '^#' backend/siem_engine.py | grep -c 'db\._db'` returns 0 and every native read uses `.to_list(length=`
    - test_siem_engine.py exists and asserts exactly one security_cases insert on a matching rule; `pytest backend/tests/test_siem_engine.py -x` exits 0
    - the new method calls `self._evaluate_rules(` (rule-eval is reused, not reimplemented)
  </acceptance_criteria>
  <done>One native finding is read (bounded, tenant-scoped), normalized, matched against an existing siem_rule, and produces a security_cases document — proven by a green test.</done>
</task>

<task type="auto">
  <name>Task 2: Companion correlation-trigger route (INT-04) + bounded-read regression assertions</name>
  <files>backend/threat_intel_endpoints.py, backend/tests/test_siem_engine.py</files>
  <read_first>
    - backend/threat_intel_endpoints.py (the file being modified: existing @router prefix "/api/threat-intel", _get_tenant(user)/_get_role helpers, the enrich_security_event handler's Depends/RBAC shape to mirror)
    - backend/siem_engine.py (get_siem_engine(db) factory + the new correlate_native_findings method from Task 1)
    - backend/native_security_ops_endpoints.py (RBAC dependency convention for security-ops reads)
  </read_first>
  <action>
    Add a companion trigger route to threat_intel_endpoints.py (per D-01's "add a companion
    enrichment path for native findings") — do NOT add it to siem_endpoints.py (already 736
    lines, over the 500 cap). New handler POST /api/threat-intel/correlate-native: resolve the
    caller's tenant via the file's existing _get_tenant(user) helper and the same
    Depends(get_current_user)+role/permission gate the other handlers in this file use (no new
    auth surface — ASVS V2 note in RESEARCH), call
    get_siem_engine(db).correlate_native_findings(tenant_id), and return its summary dict. Keep
    the handler thin (resolve tenant -> call method -> return) so threat_intel_endpoints.py stays
    well under 500 lines.
    In test_siem_engine.py add a bounded-read regression test: patch each native collection's
    find(...) and assert every call passes length<=200 to to_list (INT-04 "reads are bounded"
    acceptance), and a cross-tenant test asserting a rule/find for tenant B is not evaluated
    against tenant A's findings.
  </action>
  <verify>
    <automated>backend/venv/bin/python -m pytest backend/tests/test_siem_engine.py -x -q && grep -n "correlate-native" backend/threat_intel_endpoints.py</automated>
  </verify>
  <acceptance_criteria>
    - threat_intel_endpoints.py contains a route path string `/correlate-native` on an existing `@router` (prefix `/api/threat-intel`)
    - the handler calls `correlate_native_findings(` and resolves tenant via the existing `_get_tenant(` helper (not a new tenant-resolution mechanism)
    - test asserts every native-collection read caps `to_list(length=...)` at <= 200
    - a cross-tenant test proves tenant B's findings are not correlated into tenant A's cases
    - `wc -l backend/threat_intel_endpoints.py` reports < 500
  </acceptance_criteria>
  <done>Operators can trigger native-finding correlation through an authenticated, tenant-scoped route; bounded-read and cross-tenant isolation are proven by tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated operator -> /api/threat-intel/correlate-native | request carries a tenant identity that MUST scope every downstream read |
| correlation engine -> Mongo native-finding collections | tenant-scoped reads; a missing tenantId filter would merge findings across tenants |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-55-01 | Information Disclosure | correlate_native_findings reads of the 4 native collections | high | mitigate | Every read runs under set_tenant_id(tenant_id)+tenant-isolated db handle and an explicit {"tenantId": tenant_id} filter; never db._db. Cross-tenant test asserts isolation. |
| T-55-02 | Denial of Service | unbounded correlation-input read on a high-volume tenant | medium | mitigate | All four reads capped at .to_list(length=200) (Pattern 1); test asserts the cap. |
| T-55-03 | Elevation of Privilege | unauthenticated/cross-role trigger of correlation | medium | mitigate | Reuse the file's existing get_current_user + role/permission Depends; no new auth surface. |
</threat_model>

<verification>
- `backend/venv/bin/python -m pytest backend/tests/test_siem_engine.py -x -q` green
- Per-wave: `cd backend && venv/bin/python -m pytest -q` (re-baseline; no new failures vs. the documented pre-existing set)
- `grep -v '^#' backend/siem_engine.py | grep -c 'db\._db'` returns 0
</verification>

<success_criteria>
Native findings correlate through the existing SiemEngine rule loop into security_cases,
tenant-scoped and bounded, reachable via an authenticated companion route — with the first
direct test coverage siem_engine.py has ever had.
</success_criteria>

<output>
Create `.planning/phases/55-advanced-threat-detection/55-01-SUMMARY.md` when done
</output>
