---
phase: 25-cloud-checks-execution-gaps
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/cloud_checks_service.py
  - backend/cloud_checks_endpoints.py
  - backend/cloud_account_endpoints.py
  - backend/mcp_server_endpoints.py
  - backend/tests/test_cloud_checks_expansion.py
  - backend/tests/test_cloud_accounts.py
autonomous: true
requirements: [CHK-01]
must_haves:
  truths:
    - "run_checks() evaluates kubernetes and digitalocean provider checks and returns ran > 0 (not an error dict) for a registered account of those providers (CHK-01)"
    - "A cloud account with provider=kubernetes or provider=digitalocean can be registered via POST /api/cloud-accounts and returns 200 (CHK-01, closes Gate 1)"
    - "POST /api/cloud-checks/run and the MCP run_cloud_check tool accept kubernetes/digitalocean without a 400 (CHK-01, Gates 2 and 3)"
    - "The cloud-checks summary coverage denominator (_RUNNABLE_CHECKS_COUNT) includes k8s + DO checks so coverage neither exceeds 100% nor is unreachable at 100%"
    - "Tenant isolation on run_checks()/scan_account() is unchanged — existing test_tenant_isolation and test_scan_sets_status still pass (T-25-01)"
  artifacts:
    - "backend/tests/test_cloud_checks_expansion.py filled with test_run_checks_evaluates_kubernetes, test_run_checks_evaluates_digitalocean, test_coverage_denominator_includes_new_providers"
    - "test_register_kubernetes_account and test_register_digitalocean_account in backend/tests/test_cloud_accounts.py"
    - "RUNNABLE_PROVIDERS and _VALID_PROVIDERS widened to include kubernetes + digitalocean"
  key_links:
    - "RUNNABLE_PROVIDERS (cloud_checks_service.py) ↔ _VALID_PROVIDERS (cloud_account_endpoints.py) ↔ provider check (cloud_checks_endpoints.py) ↔ provider check (mcp_server_endpoints.py) all enumerate the same 5-provider set"
---

<objective>
Close CHK-01: Kubernetes and DigitalOcean checks are already defined and imported into CLOUD_CHECKS (20 + 10 = 30 checks) but are never evaluated because four independent provider-allowlists hard-gate on ("aws", "azure", "gcp"). Widen all four gates in lockstep so the two providers are runnable through every trigger path (account registration → scan, direct run, MCP tool), and fill the currently-empty test stub that was flagged as a pre-existing Phase 17 coverage gap.

Purpose: Make the catalogued k8s/DO checks actually reachable end-to-end through the multi-account UI flow, not just via one API entry point. Per RESEARCH.md Pattern 1 and the phase decision recorded in the planning context, all four gates are widened together — leaving Gate 1 (registration) narrower than Gate 4 (execution) would leave the multi-account flow still broken, which defeats the purpose of CHK-01.

Output: Four widened provider-allowlists, a filled CHK-01 test suite, and k8s/DO account-registration tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-cloud-checks-execution-gaps/25-RESEARCH.md
@.planning/phases/25-cloud-checks-execution-gaps/25-PATTERNS.md
@backend/cloud_checks_service.py
@backend/cloud_checks_endpoints.py
@backend/cloud_account_endpoints.py
@backend/mcp_server_endpoints.py
@backend/cloud_accounts_service.py
@backend/tests/test_cloud_accounts.py
@backend/tests/test_iac_scanner.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author Wave 0 CHK-01 tests (RED)</name>
  <read_first>backend/tests/test_iac_scanner.py (for the _mkdb/_mkuser/_build harness pattern), backend/tests/test_cloud_accounts.py (existing _mkdb/_build + test_register_gcp_account to mirror), backend/cloud_checks_service.py (run_checks signature + how self._db() resolves collections via db.cloud_accounts / db.cloud_findings / db.cloud_check_results — NO db._db prefix)</read_first>
  <files>backend/tests/test_cloud_checks_expansion.py, backend/tests/test_cloud_accounts.py</files>
  <action>Fill the empty stub backend/tests/test_cloud_checks_expansion.py with three async tests exercising cloud_checks_service.run_checks() directly (pytest.ini has asyncio_mode=auto, so async def test functions run without asyncio.run). Import cloud_checks_service. Build a mock db whose collection attributes are set directly on db (setattr(db, col, c)) NOT on db._db — cloud_checks_service accesses db.cloud_accounts / db.cloud_findings / db.cloud_check_results with no _db prefix (confirmed by direct read of cloud_checks_service.py; this differs from iac_scanner_service which uses db._db). Patch database.get_database to return the mock (run_checks calls self._db() which does `from database import get_database`). (1) test_run_checks_evaluates_kubernetes: cloud_accounts.find_one returns {"id":"acct-1","tenantId":"tenant-a","provider":"kubernetes"}; cloud_findings.find(...).to_list returns []; cloud_check_results.update_one is an AsyncMock; call await run_checks("acct-1","kubernetes","tenant-a") and assert result.get("error") is None and result["ran"] == 20 (K8S_CHECKS count). (2) test_run_checks_evaluates_digitalocean: same shape with provider="digitalocean" and assert result["ran"] == 10 (DO_CHECKS count). (3) test_coverage_denominator_includes_new_providers: import RUNNABLE_PROVIDERS, _RUNNABLE_CHECKS_COUNT, CLOUD_CHECKS from cloud_checks_service and assert "kubernetes" in RUNNABLE_PROVIDERS and "digitalocean" in RUNNABLE_PROVIDERS and _RUNNABLE_CHECKS_COUNT == len(CLOUD_CHECKS). Then in backend/tests/test_cloud_accounts.py add test_register_kubernetes_account and test_register_digitalocean_account mirroring test_register_gcp_account exactly (same _mkdb/_build), POSTing {"provider":"kubernetes","account_id":"cluster-1","account_name":"K8s Prod"} and {"provider":"digitalocean","account_id":"do-123","account_name":"DO Prod"} and asserting status_code == 200. Do NOT modify any existing test in either file. These new tests are expected to FAIL now (RED) because the gates still reject the two providers.</action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_cloud_checks_expansion.py tests/test_cloud_accounts.py::test_register_kubernetes_account tests/test_cloud_accounts.py::test_register_digitalocean_account -x 2>&1 | tail -20; echo "EXPECT: new tests present and collectable"</automated>
  </verify>
  <acceptance_criteria>The five new test functions exist and are collected by pytest. They currently fail (RED) because provider gates still reject kubernetes/digitalocean — this is the expected Wave 0 state. No existing test is modified. test_cloud_checks_expansion.py is no longer a 0-line stub.</acceptance_criteria>
  <done>Five new tests authored across the two files; existing tests untouched; new tests collectable and currently red.</done>
</task>

<task type="auto">
  <name>Task 2: Widen all four provider-allowlist gates in lockstep (GREEN)</name>
  <read_first>backend/cloud_checks_service.py (line 34 RUNNABLE_PROVIDERS + line 35 _RUNNABLE_CHECKS_COUNT + line 64 execution gate), backend/cloud_checks_endpoints.py (line 73 provider validation), backend/cloud_account_endpoints.py (line 13 _VALID_PROVIDERS), backend/mcp_server_endpoints.py (line 24 tool-schema description + line 78 provider validation)</read_first>
  <files>backend/cloud_checks_service.py, backend/cloud_checks_endpoints.py, backend/cloud_account_endpoints.py, backend/mcp_server_endpoints.py</files>
  <action>Widen the four provider-allowlists identified in RESEARCH.md Pattern 1 / PATTERNS.md Shared Patterns to include "kubernetes" and "digitalocean", in a single commit. (Gate 4) In cloud_checks_service.py change RUNNABLE_PROVIDERS = ("aws", "azure", "gcp") to ("aws", "azure", "gcp", "kubernetes", "digitalocean"); leave line 35 `_RUNNABLE_CHECKS_COUNT = len([...])` unchanged in shape so the denominator recomputes for free at import time (Pitfall 2 — do NOT hardcode the count elsewhere). Update the code comment above the tuple so it no longer claims k8s/DO are excluded. (Gate 2) In cloud_checks_endpoints.py update the `if payload.provider not in ("aws","azure","gcp")` check and its "provider must be aws, azure, or gcp" HTTPException detail string to the widened set in lockstep. (Gate 1) In cloud_account_endpoints.py widen `_VALID_PROVIDERS = {"aws","azure","gcp"}` to include "kubernetes" and "digitalocean" (the error message at line 41 is derived from the set via sorted(), so no separate string edit). (Gate 3) In mcp_server_endpoints.py update the `if provider not in ("aws","azure","gcp")` check and its detail string at line ~78, and update the cosmetic tool-schema description at line 24 from "string (aws/azure/gcp)" to reflect the widened providers. Do NOT introduce a shared constants module (RESEARCH.md Don't Hand-Roll — out of scope for this Tier-1 fix); edit each literal directly. Do NOT touch any tenantId-scoping in run_checks()/scan_account() — only the provider literals change (T-25-01).</action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_cloud_checks_expansion.py tests/test_cloud_accounts.py tests/test_iac_scanner.py -v 2>&1 | tail -30</automated>
    <automated>cd backend && grep -rn 'kubernetes' cloud_checks_service.py cloud_checks_endpoints.py cloud_account_endpoints.py mcp_server_endpoints.py | grep -v '^#' | wc -l; echo "EXPECT >= 4 (one widened allowlist per gate file)"</automated>
  </verify>
  <acceptance_criteria>All three new CHK-01 tests in test_cloud_checks_expansion.py pass; test_register_kubernetes_account and test_register_digitalocean_account pass; every pre-existing test in test_cloud_accounts.py (including test_tenant_isolation and test_scan_sets_status) and test_iac_scanner.py still passes with zero modifications. `grep` confirms all four gate files reference the widened provider set. run_checks() returns ran > 0 for kubernetes (20) and digitalocean (10).</acceptance_criteria>
  <done>All four gates widened in one commit; new CHK-01 tests green; all pre-existing tests still green; tenant-isolation tests unmodified and passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → POST /api/cloud-accounts | Untrusted provider string crosses at registration (Gate 1) |
| client → POST /api/cloud-checks/run | Untrusted provider string crosses at direct run (Gate 2) |
| MCP client → POST /api/mcp/execute/run_cloud_check | Untrusted provider string crosses at tool call (Gate 3) |
| service → run_checks() / scan_account() | tenantId-scoped DB queries against cloud_accounts / cloud_findings / cloud_check_results |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-01 | Tampering / inconsistent enforcement | The 4 provider-allowlist gates + run_checks()/scan_account() tenant scoping | high | mitigate | Widen all four gates in lockstep (Task 2) so no gate accepts a provider another rejects; make ZERO changes to the tenantId filters in run_checks()/scan_account(); regression-guard with the existing test_tenant_isolation and test_scan_sets_status (which asserts every write is scoped to the account's tenant) |
| T-25-01b | Elevation of Privilege | POST /api/cloud-accounts / /scan RBAC (has_permission gates) | low | accept | RBAC gates (manage:settings for register, view:cloud_security for scan) are untouched by this plan; existing test_insufficient_permission_rejected covers regression |
| T-25-SC | Tampering (supply chain) | package installs | low | accept | No new dependencies added this phase (RESEARCH.md Package Legitimacy Audit: N/A — zero pip/npm changes); no install task exists |
</threat_model>

<verification>
- `cd backend && python -m pytest tests/test_cloud_checks_expansion.py tests/test_cloud_accounts.py -v` — all green
- `cd backend && python -m pytest tests/ -v` — full suite green (no regression across the four edited gate files)
- Validation map rows satisfied: CHK-01 k8s run, CHK-01 DO run, CHK-01 coverage denominator, CHK-01 k8s/DO registration (T-25-01) — all in this plan (Task IDs 25-01-T1 authoring, 25-01-T2 implementation).
</verification>

<success_criteria>
- run_checks() evaluates kubernetes (20 checks) and digitalocean (10 checks) instead of returning an error.
- k8s/DO accounts register (200) and are scannable through POST /api/cloud-accounts/{id}/scan.
- Coverage denominator includes the new providers; no >100% or unreachable-100% drift.
- Tenant isolation demonstrably unchanged (isolation tests pass unmodified).
</success_criteria>

<artifacts_produced>
New test functions:
- test_run_checks_evaluates_kubernetes, test_run_checks_evaluates_digitalocean, test_coverage_denominator_includes_new_providers (backend/tests/test_cloud_checks_expansion.py)
- test_register_kubernetes_account, test_register_digitalocean_account (backend/tests/test_cloud_accounts.py)

Modified constants (widened, not new):
- RUNNABLE_PROVIDERS (backend/cloud_checks_service.py) — now 5 providers
- _VALID_PROVIDERS (backend/cloud_account_endpoints.py) — now 5 providers
- Inline provider tuples in cloud_checks_endpoints.py and mcp_server_endpoints.py — now 5 providers

No new constants, fields, or files created (test_cloud_checks_expansion.py already existed as an empty stub).
</artifacts_produced>

<output>
Create `.planning/phases/25-cloud-checks-execution-gaps/25-01-SUMMARY.md` when done.
</output>
