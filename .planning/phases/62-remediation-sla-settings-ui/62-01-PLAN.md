---
phase: 62-remediation-sla-settings-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/apiService.ts
  - components/RemediationSlaSettings.tsx
  - components/SettingsDashboard.tsx
  - src/__tests__/RemediationSlaSettings.test.tsx
autonomous: true
requirements: [SLA-03]

must_haves:
  truths:
    - "A 'Remediation' tab renders in the Settings tab bar for every authenticated user, outside every permission-gated tab group, and clicking it mounts the Remediation SLA panel (D-01/D-02/D-03)."
    - "The panel displays the tenant's current at-risk window in days and a save persists it through the already-live PATCH route, confirmed by the success toast 'SLA window updated' (SLA-03)."
    - "The At-Risk Window input renders immediately holding 7 before the mount fetch resolves — there is no spinner or skeleton state for this single-field panel."
    - "When the read wrapper fails (network error or non-2xx), the panel still renders a usable input holding 7 — it never renders blank and never throws to the caller."
    - "Any save failure — a non-admin's 403, a 422, a 5xx, or a network error — surfaces the one generic error toast 'Failed to save threshold — please try again'; no permission-specific or role-revealing message is ever shown (D-04)."
    - "Typing a value outside 1-365 clamps the input back into range on change, and whenever the held value is out of range the primary button is disabled and 'Must be between 1 and 365 days.' renders in red."
    - "Concurrency (SLA-03 edge-probe, auto-covered): while a save is in flight the primary button is disabled, so this panel cannot issue a second overlapping write; if the panel is unmounted and remounted mid-flight the mount read re-runs, so the displayed value always re-derives from the server rather than from interrupted local state."
    - statement: "While a save is in flight the primary button is disabled and its label reads 'Saving...'"
      verification: backstop
  prohibitions:
    - statement: "MUST NOT add a client-side role or permission conditional around the Remediation tab button, the input, or the primary button — a client-side gate would present a false authorization boundary that a later reader could mistake for the real server-side one (D-04)."
      category: transparency
      status: resolved
      verification: test
    - statement: "MUST NOT show the success toast unless the write request actually succeeded — a false confirmation tells a tenant admin that a compliance-affecting setting changed when it did not."
      category: transparency
      status: resolved
      verification: test
    - statement: "MUST NOT modify, weaken, or duplicate the existing server-side authorization, tenant scoping, or range validation for this route — this phase is a UI consumer only and the server remains the sole source of truth."
      category: safety
      status: resolved
      verification: test
  artifacts:
    - path: "components/RemediationSlaSettings.tsx"
      provides: "Remediation SLA at-risk-window settings panel"
      exports: ["RemediationSlaSettings"]
      min_lines: 60
    - path: "services/apiService.ts"
      provides: "Read/write client wrappers for the remediation-sla settings route"
      exports: ["fetchRemediationSlaWindow", "saveRemediationSlaWindow"]
    - path: "components/SettingsDashboard.tsx"
      provides: "Remediation tab wiring (union member, icon import, component import, tab button, panel mount)"
      contains: "activeView === 'remediation'"
    - path: "src/__tests__/RemediationSlaSettings.test.tsx"
      provides: "SLA-03 behavior coverage (renders / fetch / save / error / validation)"
      min_lines: 70
  key_links:
    - from: "components/SettingsDashboard.tsx"
      to: "components/RemediationSlaSettings.tsx"
      via: "SettingsView union member 'remediation' -> tab button setActiveView -> panel mount conditional; all three must agree on the exact string or the tab renders but does nothing"
      pattern: "activeView === 'remediation'"
    - from: "components/RemediationSlaSettings.tsx"
      to: "services/apiService.ts"
      via: "namespace import calls fetchRemediationSlaWindow on mount and saveRemediationSlaWindow on click"
      pattern: "api\\.(fetch|save)RemediationSlaWindow"
    - from: "services/apiService.ts"
      to: "the already-live settings route"
      via: "authFetch(`${API_BASE}/settings/remediation-sla`) — never a bare fetch, so bearer/refresh handling is preserved"
      pattern: "authFetch\\(`\\$\\{API_BASE\\}/settings/remediation-sla`"
---

<objective>
Build the UI consumer for the `GET/PATCH /api/settings/remediation-sla` route that has been live with no UI since Phase 44-03 and was flagged twice by UI audits as a deliberate, tracked deferral. Delivers requirement **SLA-03**.

Purpose: close the last piece of the v3.2 Remediation SLA & Escalation feature — tenants can finally see and change the at-risk window that drives SLA flagging, instead of it being an invisible server-side constant.

Output: a new `components/RemediationSlaSettings.tsx` panel, two new client wrappers in `services/apiService.ts`, a new unrestricted "Remediation" tab in `components/SettingsDashboard.tsx` (D-01/D-02/D-03), and a new `src/__tests__/RemediationSlaSettings.test.tsx` covering all five SLA-03 behaviors. Non-admin save behavior is the backend 403 surfaced as the shared generic error toast with no client-side permission logic (D-04). Requirement traceability (D-05) is already in place — `.planning/REQUIREMENTS.md` line 44 carries SLA-03 and the ROADMAP Phase 62 entry already reads `Requirements: SLA-03`; the planner completes that entry's `Plans:` list, so no task edits planning docs.

This phase is pure UI. No server-side file is created, modified, or read at runtime by this plan.
</objective>

<execution_context>
@/home/user/enterprise-omni-agent-ai-platform/.claude/gsd-core/workflows/execute-plan.md
@/home/user/enterprise-omni-agent-ai-platform/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/62-remediation-sla-settings-ui/62-CONTEXT.md
@.planning/phases/62-remediation-sla-settings-ui/62-UI-SPEC.md
@.planning/phases/62-remediation-sla-settings-ui/62-PATTERNS.md
@.planning/phases/62-remediation-sla-settings-ui/62-VALIDATION.md
@components/EvidenceSettings.tsx
@src/__tests__/ITAMCatalogPanel.test.tsx

<!-- services/apiService.ts is 4600+ lines — do NOT load it whole. Read only lines 4583-4600 (the pattern pair) and line 39 / line 202 (API_BASE, authFetch), as directed per-task in <read_first>. -->
</context>

<artifacts_this_phase_produces>
Every symbol below is **created by this phase** and therefore did not exist in the codebase before it. Source-grounding / drift verification passes MUST exclude these from "symbol not found in source" checks:

| Symbol | Kind | File |
|--------|------|------|
| `RemediationSlaSettings` | React component (named export) | `components/RemediationSlaSettings.tsx` (new file) |
| `fetchRemediationSlaWindow` | async function (named export) | `services/apiService.ts` (new export) |
| `saveRemediationSlaWindow` | async function (named export) | `services/apiService.ts` (new export) |
| `'remediation'` | new member of the `SettingsView` string-union type | `components/SettingsDashboard.tsx` |
| Remediation tab `<button>` | JSX element calling `setActiveView('remediation')` | `components/SettingsDashboard.tsx` |
| Remediation panel mount | JSX conditional `activeView === 'remediation'` | `components/SettingsDashboard.tsx` |
| `RemediationSlaSettings.test.tsx` suite | Vitest test file | `src/__tests__/RemediationSlaSettings.test.tsx` (new file) |

Pre-existing symbols this phase only *consumes* (unchanged): `authFetch`, `API_BASE`, `showToast`, `ClipboardListIcon`, `EvidenceSettings`, `SettingsView`, `setActiveView`, `activeView`.
</artifacts_this_phase_produces>

<!-- planner-discipline-allow: canManageSettings -->
<!-- planner-discipline-allow: isSuperAdmin -->
<!-- planner-discipline-allow: hasPermission -->
<!-- planner-discipline-allow: canManageRBAC -->
<!-- planner-discipline-allow: font-semibold -->
<!-- planner-discipline-allow: ^backend/ -->

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "an admin changes the at-risk window" — one path, all four layers</name>

  <precondition>The settings route this UI consumes is already registered and live (`grep -c 'compliance_remediation_sla_endpoints' backend/router_registry.py` returns 1) and `ClipboardListIcon` is exported from `components/icons.tsx` — both are Phase 44-03 / pre-existing artifacts this plan does not create. Halt if either check fails.</precondition>

  <reversibility rating="reversible">Purely additive frontend: one new component file, two new named exports appended to an existing service module, and four additive lines in the settings shell. Reverting is a file delete plus four line removals — no persisted state, no schema, no server contract, no data migration.</reversibility>

  <files>services/apiService.ts, components/RemediationSlaSettings.tsx, components/SettingsDashboard.tsx, src/__tests__/RemediationSlaSettings.test.tsx</files>

  <read_first>
    - `components/EvidenceSettings.tsx` (all 70 lines) — the byte-for-byte structural clone source. Read it before writing the new component; do not work from the excerpt in PATTERNS.md alone.
    - `services/apiService.ts` lines 4583-4600 only — the `fetchStalenessThreshold` / `saveStalenessThreshold` pair whose exact shape the two new functions mirror. Also read line 39 (`API_BASE`) and line 202 (`authFetch`) to confirm both are module-level and already in scope (no new import needed).
    - `components/SettingsDashboard.tsx` — read lines 1-70 (imports + `SettingsView` union at line 65), lines 280-300 (the tab-button row, including the Security/Evidence unrestricted pair at 285-290 and the first permission-gated group that follows), and lines 348-360 (the panel-mount block, Evidence mount at 354-356).
    - `src/__tests__/ITAMCatalogPanel.test.tsx` lines 1-30 — the hoisted-const + passthrough-factory `vi.mock` shape this project's component tests use.
    - `.planning/phases/62-remediation-sla-settings-ui/62-UI-SPEC.md` §Copywriting Contract and §Typography — the locked strings and the one declared className substitution.
    - `backend/compliance_remediation_sla_endpoints.py` — READ ONLY, to confirm the request/response shape (`{windowDays}`, `Field(ge=1, le=365)`, admin gate on write only). This file must not be edited.
  </read_first>

  <behavior>
    The one end-to-end test written in this task (the tracer's proof) asserts the full happy path in a single flow:
    - The read wrapper resolves `{ windowDays: 21 }`; the panel mounts; the numeric input's value is `21`.
    - The user changes the input to `30`; the primary button is enabled.
    - Clicking the primary button calls `saveRemediationSlaWindow` exactly once with `30`.
    - On resolve, `showToast` is called with `'SLA window updated'` and `'success'`.
    Test name must contain the substring `save` so `npx vitest run src/__tests__/RemediationSlaSettings.test.tsx -t "save"` selects it (VALIDATION.md task 62-01-03 contract).
  </behavior>

  <action>
Wire ONE path — mount, read, edit, write, toast — through all four layers this phase touches. No extra call sites, no batching, no second entry point.

**Layer 1 — `services/apiService.ts`.** Append two new exported functions immediately after `saveStalenessThreshold` (line ~4600), mirroring that pair's shape exactly. Do NOT parameterize or reuse the existing evidence-staleness functions (RESEARCH.md anti-pattern) — these are two separate, independently-evolvable exports.
  - `fetchRemediationSlaWindow(): Promise<{ windowDays: number }>` — `authFetch(\`${API_BASE}/settings/remediation-sla\`)` inside a `try`; on `!res.ok` return `{ windowDays: 7 }`; otherwise return `await res.json()`; the `catch` also returns `{ windowDays: 7 }`.
  - `saveRemediationSlaWindow(windowDays: number): Promise<void>` — `authFetch(\`${API_BASE}/settings/remediation-sla\`, { method: 'PATCH', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ windowDays }) })`; `if (!res.ok) throw new Error('Failed to save remediation SLA window')`.
  - The error-handling asymmetry is load-bearing and deliberate (RESEARCH.md Pitfall 3): the read soft-fails to a default so the panel is never blank, the write throws so the component's `catch` can raise the error toast. Do not homogenize them.
  - Both go through `authFetch`, never a bare `fetch` — this is what preserves bearer-token attachment and refresh (threat T-62-05).

**Layer 2 — `components/RemediationSlaSettings.tsx` (new file, flat `components/` root, NOT nested under `components/itam/`).** Structural clone of `components/EvidenceSettings.tsx`: same import trio (`React, { useState, useEffect }`, `* as api from '../services/apiService'`, `showToast` from `'../utils/toast'`), same `space-y-6` wrapper -> bordered card (`bg-gray-50 dark:bg-gray-700/50 rounded-lg border border-gray-200 dark:border-gray-600 p-4`) -> `space-y-2` field group -> `flex items-center` input row -> conditional validation paragraph -> `mt-4` button row. Named export `RemediationSlaSettings: React.FC`. Substitutions, all locked:
  - State: `const [windowDays, setWindowDays] = useState<number>(7)` and `const [saving, setSaving] = useState(false)`.
  - Mount effect: `api.fetchRemediationSlaWindow().then(d => setWindowDays(d.windowDays ?? 7))` with `[]` deps.
  - `const isValid = windowDays >= 1 && windowDays <= 365;` — identical bounds to the clone source and to the server's own range; copy it, do not re-derive.
  - `handleSave`: early-return when `!isValid`; `setSaving(true)`; `try` calls `api.saveRemediationSlaWindow(windowDays)` then `showToast('SLA window updated', 'success')`; `catch` calls `showToast('Failed to save threshold — please try again', 'error')`; `finally` calls `setSaving(false)`.
  - Copy, character-for-character from the UI-SPEC Copywriting Contract: section label `Remediation SLA`; field label `At-Risk Window`; helper text `Tasks with fewer than this many days until their due date are flagged "at risk".`; unit suffix `days`; validation message `Must be between 1 and 365 days.`; button label `Save SLA Window` when idle and `Saving...` while in flight.
  - Input: `type="number" min={1} max={365} value={windowDays}` with `onChange={e => setWindowDays(Math.min(365, Math.max(1, parseInt(e.target.value, 10) || 1)))}` and className `w-24 px-3 py-2 rounded-md border border-gray-300 dark:border-gray-600 bg-white dark:bg-gray-700 text-sm text-gray-900 dark:text-gray-100`.
  - Button: `disabled={!isValid || saving}`, className `px-4 py-2 text-sm font-medium text-white bg-primary-600 hover:bg-primary-700 rounded-md disabled:opacity-50 disabled:cursor-not-allowed`.
  - **The one deliberate className substitution against the clone source:** the section-label paragraph uses `text-sm font-medium text-gray-900 dark:text-white mb-4`. The clone source uses a heavier weight there; carrying it over would put three type weights in this component and break the project's two-weight typography ceiling (UI-SPEC §Typography declared deviation). Weights 400 and 500 only — no heavier utility class anywhere in this file.
  - Accent (`bg-primary-*`) is reserved for the primary button only — never the input border, never any label, never the helper or validation text.
  - Per D-04: this component contains no role/permission conditional of any kind, on any element. The server's write gate is the sole authorization boundary; a non-admin's 403 travels the same `catch` as a network failure and produces the same generic toast. Do not add, and do not comment about, any permission-helper identifier.

**Layer 3 — `components/SettingsDashboard.tsx` (four additive edits, matching the existing `'evidence'` tab's exact JSX shape).**
  1. Line 65: append `| 'remediation'` to the `SettingsView` union type.
  2. Line 4 import from `'./icons'`: add `ClipboardListIcon` to the existing named-import list (D-03 — chosen over reusing the Evidence tab's clock glyph to stay visually distinct).
  3. Near line 30: add `import { RemediationSlaSettings } from './RemediationSlaSettings';` alongside the existing `EvidenceSettings` import.
  4. Insert the new tab button immediately AFTER the Evidence button (line ~290) and BEFORE the first permission-gated tab group that follows it — so it sits in the unrestricted trio with Security and Evidence (D-01). Same `<button>` shape and same className ternary as the Evidence button, with `onClick={() => setActiveView('remediation')}`, `activeView === 'remediation'` in the ternary, `<ClipboardListIcon size={18} className="mr-2" />`, and the single-word label `Remediation` (D-02 — exactly one word, matching the terse sibling labels; not a two-word variant).
  5. Insert `{activeView === 'remediation' && (<RemediationSlaSettings />)}` immediately after the Evidence panel mount (line ~354-356), matching its formatting.
  Neither the button nor the mount is wrapped in any permission conditional — the read side of the route has no admin gate, so the tab must not have one either.

**Layer 4 — `src/__tests__/RemediationSlaSettings.test.tsx` (new file).** Create the Wave 0 test scaffold plus the ONE end-to-end happy-path test described in `<behavior>`. Mock shape copied from `ITAMCatalogPanel.test.tsx`: hoisted `const fetchRemediationSlaWindow = vi.fn()` / `const saveRemediationSlaWindow = vi.fn()` / `const showToast = vi.fn()`, then `vi.mock('../../services/apiService', () => ({ fetchRemediationSlaWindow: (...args: unknown[]) => fetchRemediationSlaWindow(...args), saveRemediationSlaWindow: (...args: unknown[]) => saveRemediationSlaWindow(...args) }))` and `vi.mock('../../utils/toast', () => ({ showToast: (...args: unknown[]) => showToast(...args) }))`. Note the toast mock uses the passthrough form, NOT the precedent's bare `vi.fn()` inline form, because later tasks must assert on the toast arguments. Import the component as `import { RemediationSlaSettings } from '../../components/RemediationSlaSettings';` after the mocks. Add a `beforeEach` that resets all three spies. Use `waitFor` around the post-mount value assertion so the mount promise flushes.

Do NOT create, edit, or stage any server-side Python file. Do NOT add or remove any dependency — `package.json` and `package-lock.json` stay byte-identical (threat T-62-SC; zero new packages per RESEARCH.md's legitimacy audit).
  </action>

  <acceptance_criteria>
    - `npx vitest run src/__tests__/RemediationSlaSettings.test.tsx` exits 0 with at least 1 test passing.
    - `npx vitest run src/__tests__/RemediationSlaSettings.test.tsx -t "save"` selects and passes at least 1 test (VALIDATION.md 62-01-03 `-t` contract).
    - `npx tsc --noEmit` exits 0 (baseline measured clean this planning session — any error is newly introduced).
    - `npx vitest run src/__tests__` exits 0 with 24 test files passing (baseline measured this session: 23 files / 166 tests green; this task adds exactly one file).
    - `grep -c 'export const fetchRemediationSlaWindow' services/apiService.ts` returns 1 and `grep -c 'export const saveRemediationSlaWindow' services/apiService.ts` returns 1.
    - `grep -c 'authFetch(`${API_BASE}/settings/remediation-sla`' services/apiService.ts` returns 2, and `grep -c "fetch(\`\${API_BASE}/settings/remediation-sla" services/apiService.ts` returns 0 outside those two `authFetch` calls (no bare fetch introduced).
    - `grep -c "windowDays: 7" services/apiService.ts` returns 2 (the read wrapper's two soft-fail return paths — non-ok branch and catch branch).
    - `grep -c "throw new Error('Failed to save remediation SLA window')" services/apiService.ts` returns 1 (the write wrapper throws; the asymmetry is intact).
    - `grep -vE '^\s*(//|\*|/\*|\{/\*)' components/SettingsDashboard.tsx | grep -c "activeView === 'remediation'"` returns exactly 2 (tab-button ternary + panel mount).
    - `grep -c "setActiveView('remediation')" components/SettingsDashboard.tsx` returns 1 and `grep -c "| 'remediation'" components/SettingsDashboard.tsx` returns 1.
    - `grep -c 'ClipboardListIcon' components/SettingsDashboard.tsx` returns 2 (import + single usage) and `grep -c 'RemediationSlaSettings' components/SettingsDashboard.tsx` returns 2 (import + single mount).
    - `git show --name-only --format= HEAD | grep -c '^backend/'` returns 0 for this task's commit, and `git show --name-only --format= HEAD | grep -cE '^(package|package-lock)\.json$'` returns 0.
    - `test -f components/RemediationSlaSettings.tsx` succeeds and `test -f components/itam/RemediationSlaSettings.tsx` fails (flat placement, not the ITAM sub-console nesting).
  </acceptance_criteria>

  <verify>
    <automated>npx vitest run src/__tests__/RemediationSlaSettings.test.tsx && npx tsc --noEmit && npx vitest run src/__tests__</automated>
  </verify>

  <done>The single happy path — open the Remediation tab, see the tenant's current at-risk window, change it, save it, get the success toast — works end to end through all four layers and is committed. The panel is reachable from the Settings tab bar with no permission conditional anywhere on the path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand SLA-03 behavior coverage out from the proven slice</name>

  <files>src/__tests__/RemediationSlaSettings.test.tsx</files>

  <read_first>
    - `src/__tests__/RemediationSlaSettings.test.tsx` — the file created in Task 1; read it so the new tests reuse its existing mock scaffold rather than redeclaring mocks.
    - `components/RemediationSlaSettings.tsx` — the component under test as actually written in Task 1 (element order, className strings, the exact copy text the queries must match).
    - `.planning/phases/62-remediation-sla-settings-ui/62-VALIDATION.md` §Per-Task Verification Map — the five `-t` filter substrings (`renders`, `fetch`, `save`, `error`, `validat`) each new test name must satisfy.
    - `.planning/phases/62-remediation-sla-settings-ui/62-UI-SPEC.md` §UI Considerations — the five resolved state considerations these tests encode.
  </read_first>

  <behavior>
    Add these tests to the existing file (Task 1 already covers the `save` happy path):

    - **`renders`** — mount with the read wrapper resolving `{ windowDays: 14 }`; assert the section label `Remediation SLA`, the field label `At-Risk Window`, the helper text containing `flagged "at risk"`, the unit suffix `days`, and the idle button label `Save SLA Window` are all in the document. This is the Copywriting-Contract conformance test at the DOM level.
    - **`fetch` (happy)** — read wrapper resolves `{ windowDays: 14 }`; assert the numeric input's value settles to `14` (wrap in `waitFor`).
    - **`fetch` (soft-fail)** — read wrapper resolves `{ windowDays: 7 }` (the wrapper's own soft-fail contract) AND a second case where it resolves an object with no `windowDays` key; assert the input holds `7` in both and that nothing throws. Encodes UI-SPEC's "error (fetch failure)" row: the panel never renders blank.
    - **`error`** — read resolves `{ windowDays: 14 }`, then `saveRemediationSlaWindow` is made to reject (a plain `Error`, standing in for the 403 / 422 / 5xx / network cases the client deliberately does not distinguish). After clicking the primary button, assert `showToast` was called with exactly `'Failed to save threshold — please try again'` and `'error'`, AND assert `showToast` was NOT called with `'SLA window updated'`. That second assertion is the mechanical form of the no-false-confirmation prohibition. Also assert the toast text contains no role name, no HTTP status, and no endpoint path (D-04: one generic message, no permission-specific variant).
    - **`validat` (clamp)** — assert the input carries `min="1"` and `max="365"`; fire a change to `999` and assert the input settles to `365`; fire a change to `0` and assert it settles to `1`.
    - **`validat` (out-of-range held value)** — the only reachable path to an invalid held value is a server response outside the range, since the onChange clamp blocks user entry: make the read wrapper resolve `{ windowDays: 400 }`, then assert `Must be between 1 and 365 days.` is in the document and the primary button is disabled.
    - **`save` (in-flight)** — OPTIONAL, corresponds to the UI-SPEC `backstop` row. Make the write wrapper return a promise you control; after the click and before resolving, assert the button label reads `Saving...` and the button is disabled. Add it if it lands cleanly; it is not a hard requirement.

    Do not add a `SettingsDashboard`-level integration test — that shell takes a large required-prop surface plus a user context, and its tab wiring is already asserted structurally in Task 1 and Task 3 (RESEARCH.md explicitly scopes this out).
  </behavior>

  <action>
Extend the existing test file with the behaviors enumerated above, reusing Task 1's hoisted mocks and `beforeEach` reset — do not redeclare `vi.mock` factories, and do not create a second test file.

Every test name must contain one of the five VALIDATION.md filter substrings so the per-behavior `-t` commands in that document resolve to at least one test each. Prefer `waitFor` over bare `await` for any assertion that depends on the mount promise having flushed; the component's effect has no loading flag to await on.

Use Testing Library queries that bind to the locked copy (`getByLabelText`, `getByText`, `getByRole('button', { name: ... })`) rather than to className strings or DOM position — the copy is the contract, the markup is not. Query the numeric input by its label association or by `getByRole('spinbutton')`.

For the rejection case, reject with a generic `Error` and rely on the component's untyped `catch`. Do not construct a fake `Response` or assert on HTTP status codes — the component deliberately cannot see them, and a test that pretends otherwise would encode a distinction D-04 explicitly removed.

Do not touch `components/RemediationSlaSettings.tsx`, `components/SettingsDashboard.tsx`, or `services/apiService.ts` in this task. If a test fails because the component is wrong (not because the test is wrong), stop and fix it in Task 3's conformance pass rather than silently loosening the assertion.
  </action>

  <acceptance_criteria>
    - `npx vitest run src/__tests__/RemediationSlaSettings.test.tsx` exits 0 with at least 7 tests passing.
    - Each of these selects and passes at least one test: `npx vitest run src/__tests__/RemediationSlaSettings.test.tsx -t "renders"`, `... -t "fetch"`, `... -t "save"`, `... -t "error"`, `... -t "validat"` (all five VALIDATION.md rows 62-01-01 through 62-01-05 resolve).
    - `npx vitest run src/__tests__` exits 0 with 24 test files passing and no failures.
    - `grep -c "not.toHaveBeenCalledWith" src/__tests__/RemediationSlaSettings.test.tsx` returns at least 1 — the no-false-confirmation assertion exists and is negative.
    - `grep -c "windowDays: 400" src/__tests__/RemediationSlaSettings.test.tsx` returns at least 1 — the out-of-range held-value path is actually exercised, not merely asserted about.
    - `git show --name-only --format= HEAD` lists only `src/__tests__/RemediationSlaSettings.test.tsx` (plus planning docs) — no production file changed in this task.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>

  <verify>
    <automated>npx vitest run src/__tests__/RemediationSlaSettings.test.tsx -t "renders" && npx vitest run src/__tests__/RemediationSlaSettings.test.tsx -t "fetch" && npx vitest run src/__tests__/RemediationSlaSettings.test.tsx -t "error" && npx vitest run src/__tests__/RemediationSlaSettings.test.tsx -t "validat" && npx vitest run src/__tests__ && npx tsc --noEmit</automated>
  </verify>

  <done>All five SLA-03 behaviors from VALIDATION.md's verification map have a passing automated test selectable by its documented `-t` filter, and the project-owned suite (`src/__tests__`) is green with 24 files.</done>
</task>

<task type="auto">
  <name>Task 3: Contract-conformance gate — locked copy, typography deviation, unrestricted placement, no client-side gate</name>

  <files>components/RemediationSlaSettings.tsx, components/SettingsDashboard.tsx</files>

  <read_first>
    - `components/RemediationSlaSettings.tsx` — as actually written, to compare against the contract rather than against memory.
    - `components/SettingsDashboard.tsx` lines 280-300 and 348-360 — the tab row and panel-mount regions, to confirm the new entries landed in the unrestricted group.
    - `.planning/phases/62-remediation-sla-settings-ui/62-UI-SPEC.md` §Copywriting Contract, §Typography, §Color — the nine locked strings, the two-weight ceiling with its one declared substitution, and the accent-reservation rule.
    - `.planning/phases/62-remediation-sla-settings-ui/62-CONTEXT.md` §Implementation Decisions — D-01 through D-05, the authority for placement, label, icon, and the deliberate absence of client-side permission logic.
    - `components/EvidenceSettings.tsx` — to confirm the new file diverges from it in exactly the documented places and nowhere else.
  </read_first>

  <action>
Run the conformance assertion set below against the code as written and correct any drift found in the two files listed. This is the phase's anti-drift gate: RESEARCH.md names copy-string drift (Pitfall 2) and scope creep past D-04 as this phase's two dominant risks, and both are invisible to the behavior tests in Task 2.

**Copy conformance (D-02, UI-SPEC Copywriting Contract).** Confirm each of the nine locked strings appears in the new component or the settings shell exactly as contracted — `Remediation` (tab label, one word), `Remediation SLA`, `At-Risk Window`, `Tasks with fewer than this many days until their due date are flagged "at risk".`, `days`, `Must be between 1 and 365 days.`, `Save SLA Window`, `Saving...`, `SLA window updated`, and the shared error string `Failed to save threshold — please try again`. Correct any paraphrase, hyphenation change, punctuation change, or capitalization change back to the contracted form. The double-quote characters inside the helper text are part of the string.

**Typography conformance (UI-SPEC §Typography declared deviation).** Confirm the new component uses only the two contracted type weights — the regular default for helper text, unit suffix and validation message, and `font-medium` for the field label, the section label, and the button label. The section label in particular must carry the medium weight, not the heavier weight the clone source uses there. No heavier weight utility class appears anywhere in the file.

**Color conformance (UI-SPEC §Color).** Confirm `bg-primary-*` appears only on the primary button, and `text-primary-*` / `border-primary-*` only inside the new tab button's active-state ternary. Confirm the destructive red pair appears only on the validation paragraph.

**Placement conformance (D-01).** Confirm the new tab button is positioned between the Evidence button and the first permission-gated tab group, i.e. inside the unrestricted run, and that neither it nor the panel mount is wrapped in any permission conditional.

**Scope conformance (D-04, prohibition P1).** Confirm the new component contains no permission-helper identifier and no role conditional in code — and none echoed in a comment either, since a comment naming one is indistinguishable from the real thing to a grep-based reviewer. Describe the decision, if at all, by concept ("authorization is enforced server-side on write") and never by naming a helper.

**Isolation conformance (prohibition P3).** Confirm this phase's commits touched no server-side Python file and no dependency manifest.

Fix drift by editing the two files named in `<files>`. If a fix changes rendered text that Task 2 asserts on, update the assertion in the same commit so the suite stays green and the contract stays authoritative.
  </action>

  <acceptance_criteria>
    - Locked copy present (each returns at least 1): `grep -c 'Remediation SLA' components/RemediationSlaSettings.tsx`; `grep -c 'At-Risk Window' components/RemediationSlaSettings.tsx`; `grep -c 'flagged "at risk"' components/RemediationSlaSettings.tsx`; `grep -c 'Must be between 1 and 365 days.' components/RemediationSlaSettings.tsx`; `grep -c 'Save SLA Window' components/RemediationSlaSettings.tsx`; `grep -c "showToast('SLA window updated', 'success')" components/RemediationSlaSettings.tsx`; `grep -c "showToast('Failed to save threshold — please try again', 'error')" components/RemediationSlaSettings.tsx`.
    - Tab label is the single contracted word: `grep -cE '/> ?Remediation *$' components/SettingsDashboard.tsx` returns 1, and `grep -c 'Remediation SLA' components/SettingsDashboard.tsx` returns 0 (the tab label is not the two-word section label).
    - Typography ceiling held: `grep -vE '^\s*(//|\*|/\*|\{/\*)' components/RemediationSlaSettings.tsx | grep -cE 'font-(semibold|bold|extrabold|black)'` returns 0, and `grep -c 'text-sm font-medium text-gray-900 dark:text-white mb-4' components/RemediationSlaSettings.tsx` returns 1 (the section label carries the substituted medium weight).
    - Accent reserved: `grep -c 'bg-primary-600 hover:bg-primary-700' components/RemediationSlaSettings.tsx` returns 1, and `grep -cE 'primary-[0-9]{3}' components/RemediationSlaSettings.tsx` returns 1 (that single button className line is the only primary-palette usage in the file).
    - Destructive reserved: `grep -c 'text-red-600 dark:text-red-400' components/RemediationSlaSettings.tsx` returns 1.
    - No client-side permission logic in the panel (code or comment): `grep -cE 'canManageSettings|canManageRBAC|isSuperAdmin|hasPermission|useUser\(' components/RemediationSlaSettings.tsx` returns 0.
    - Unrestricted placement: `sed -n "/setActiveView('evidence')/,/canManageSettings && (/p" components/SettingsDashboard.tsx | grep -c "setActiveView('remediation')"` returns 1 — the new button sits after Evidence and before the first gated group.
    - Clone fidelity: `diff <(grep -oE 'className="[^"]*"' components/EvidenceSettings.tsx) <(grep -oE 'className="[^"]*"' components/RemediationSlaSettings.tsx)` reports exactly one differing line pair (the section-label weight substitution) and no other className divergence.
    - Server-side and dependency isolation across the whole plan: `git log --name-only --format= -3 | grep -c '^backend/'` returns 0 and `git log --name-only --format= -3 | grep -cE '^(package|package-lock)\.json$'` returns 0.
    - `npx vitest run src/__tests__` exits 0 with 24 test files passing, and `npx tsc --noEmit` exits 0.
  </acceptance_criteria>

  <verify>
    <automated>grep -vE '^\s*(//|\*|/\*|\{/\*)' components/RemediationSlaSettings.tsx | grep -cE 'font-(semibold|bold|extrabold|black)' | grep -qx 0 && grep -cE 'canManageSettings|canManageRBAC|isSuperAdmin|hasPermission|useUser\(' components/RemediationSlaSettings.tsx | grep -qx 0 && sed -n "/setActiveView('evidence')/,/canManageSettings \&\& (/p" components/SettingsDashboard.tsx | grep -qc "setActiveView('remediation')" && npx vitest run src/__tests__ && npx tsc --noEmit</automated>
    <human-check>Open Settings in the running app, click the "Remediation" tab, and confirm: the tab reads one word with the clipboard-list glyph; the bordered numeric input is the first thing the eye lands on; the section label sits at the same visual weight as the field label (not heavier); the only accent-colored elements are the Save button and the active tab underline. Then sign in as a non-admin, change the value, click save, and confirm the toast reads exactly "Failed to save threshold — please try again" with no mention of roles or permissions.</human-check>
  </verify>

  <done>Every locked string, the declared typography substitution, the accent reservation, the unrestricted tab placement, and the deliberate absence of client-side permission logic are each confirmed by a concrete assertion; any drift found has been corrected and the project-owned suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> `/api/settings/remediation-sla` | The only boundary this phase crosses. An authenticated user's `windowDays` value crosses here on write; the tenant's stored value crosses back on read. Authorization (admin-only write) and range validation both live on the far side and are unchanged by this phase. |
| `RemediationSlaSettings` -> `services/apiService.authFetch` | In-process. The bearer token and refresh handling are attached here; a bare `fetch` would bypass them. |
| `SettingsDashboard` tab row -> panel mount | In-process. Determines which authenticated users can *see* the panel. Deliberately ungated (D-01), mirroring the route's own ungated read. |

## STRIDE Threat Register

ASVS level 1; blocking threshold: high.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-62-01 | Elevation of Privilege | `RemediationSlaSettings.handleSave` / the new tab button | high | mitigate | Rely solely on the server-side admin gate on the write route. The risk here is inverted from the usual: adding a client-side permission conditional would create a *false* authorization boundary that a later reader could mistake for the real one, and would diverge from every sibling unrestricted tab. Task 3 asserts `grep -cE 'canManageSettings\|canManageRBAC\|isSuperAdmin\|hasPermission\|useUser\(' components/RemediationSlaSettings.tsx` returns 0; Task 2 asserts a rejected write produces the generic error toast. |
| T-62-02 | Tampering | `saveRemediationSlaWindow` request body `windowDays` | medium | mitigate | Client clamps the input to [1,365] on change and disables the primary button whenever the held value is out of range; the authoritative bound stays the server's own range validation (422 on violation). The clamp is documented as defense-in-depth UX, never the boundary. Task 2's `validat` tests exercise both the clamp and the out-of-range held-value path. |
| T-62-03 | Information Disclosure | error toast copy on write failure | low | mitigate | One generic error string for every failure mode (403 / 422 / 5xx / network). No role name, HTTP status, endpoint path, or raw response body is read by the component or echoed into the toast — the component's `catch` takes no argument. Asserted in Task 2's `error` test. |
| T-62-04 | Information Disclosure | cross-tenant read of `windowDays` via `fetchRemediationSlaWindow` | medium | accept | Tenant scoping is enforced server-side against the authenticated principal; the new client wrapper sends no tenant parameter and therefore cannot influence scoping. Accepted as an inherited, unmodified control — this phase adds no new surface. Re-verification belongs to `/gsd-secure-phase` over the unchanged server module, not here. |
| T-62-05 | Spoofing | auth-token attachment on the two new client calls | low | mitigate | Both new wrappers route through the shared `authFetch` helper (bearer header + refresh), never a bare `fetch`. Task 1 asserts both call sites use `authFetch` and that no bare-fetch call to this path exists. |
| T-62-SC | Tampering | npm/pip/cargo installs | high | mitigate | Zero package installs in this phase (RESEARCH.md Package Legitimacy Audit: no new dependencies; no `[ASSUMED]`, `[SUS]`, or `[SLOP]` rows). No install task exists in this plan, so no legitimacy checkpoint is required. Enforced structurally: Tasks 1 and 3 assert this plan's commits touch neither `package.json` nor `package-lock.json`. |

No threat is disposed `accept` at severity high or above, so the configured blocking threshold is satisfied.

**Canon-referral breadcrumbs (surfaced by the prohibition probe, deliberately not minted as bespoke prohibitions):** cross-tenant read/write isolation and raw-error-body disclosure are canon access-control / information-disclosure concerns already owned by `/gsd-secure-phase` and captured as T-62-03 / T-62-04 above; they are not minted into `must_haves.prohibitions`.
</threat_model>

<assumptions>
Flagged assumptions carried into execution. None is silently resolved.

- **A-01 (from RESEARCH.md Assumptions Log A1) — read-wrapper soft-fail placeholder is `7`.** UI-only; the value is displayed for the moment before the real read resolves, or after a read failure, and is never persisted. Any in-range value is functionally equivalent. Locked to `7` here to match the sibling settings panel and UI-SPEC's "Empty state" row. Risk if wrong: cosmetic only.
- **A-02 (edge-probe fallback, SLA-03 `concurrency`) — cross-client write semantics are inherited, not defined by this phase.** The deterministic probe surfaced one applicable edge for SLA-03 (`concurrency`, initially `unresolved`) with the prompt *"If interrupted or run in parallel, what is guaranteed?"* It is auto-resolved **covered** for everything inside this phase's boundary — the in-flight disable prevents an overlapping write from this panel, and an interrupted panel re-reads from the server on remount (see the corresponding `must_haves.truths` entry). What this phase does **not** define, and explicitly does not change, is the multi-client case: the write route has no optimistic-concurrency mechanism (no ETag, no `If-Match`, no version field), so two admins saving simultaneously resolve last-write-wins with no conflict surfaced to either. That semantic is inherited unchanged from the Phase 44-03 contract. Flagged rather than dropped; adding conflict detection would be a server-side change and is out of this phase's boundary.
- **A-03 (measured this session; corrects `62-VALIDATION.md`) — `npm test` is not a usable green gate in this repository.** VALIDATION.md names `npm test` (`vitest run`) as the full-suite command, but the bare glob reaches vendored trees that are not this project's code — `.claude/worktrees/**/code-review-graph-main/tests/fixtures/`, `servers/src/**/__tests__/`, `github-mcp-server` — because `vite.config.ts`'s `exclude` list only matches `code-review-graph-main/**` at the top level, not nested under worktrees. Measured 2026-08-10: `npm test` -> 79 failed files / 49 failed tests (all pre-existing, none in this project's own source); `npx vitest run src/__tests__` -> 23 files / 166 tests, all green. Every gate in this plan therefore uses the scoped path `npx vitest run src/__tests__`. A second pre-existing failure, `components/ui/__tests__/ToastProvider.test.tsx` ("No test suite found in file" — the file is empty), is why `components/ui/__tests__` is also excluded from the plan's gates. Neither is in this phase's scope to fix.
- **A-04 — baselines are measured, not recalled.** `npx tsc --noEmit` exit 0 and `npx vitest run src/__tests__` at 23 files / 166 tests were both executed during this planning session on the current working tree. Post-phase expectation is 24 files. If the executor finds a different baseline, the tree moved — re-measure before treating a gate failure as this phase's regression.
</assumptions>

<verification>
Phase-level checks, run after all three tasks are committed:

1. `npx tsc --noEmit` exits 0 — proves the `SettingsView` union accepts `'remediation'` at every call site (RESEARCH.md Pitfall 1's primary warning sign).
2. `npx vitest run src/__tests__` exits 0 with 24 test files and no failures.
3. Each of the five VALIDATION.md `-t` filters selects at least one passing test: `renders`, `fetch`, `save`, `error`, `validat`.
4. All three `SettingsDashboard.tsx` touch points agree on the string: `grep -c "setActiveView('remediation')"` returns 1, `grep -vE '^\s*(//|\*|/\*|\{/\*)' components/SettingsDashboard.tsx | grep -c "activeView === 'remediation'"` returns 2, `grep -c "| 'remediation'"` returns 1.
5. `git log --name-only --format= -3 | grep -c '^backend/'` returns 0 — the phase stayed pure UI.
6. `git log --name-only --format= -3 | grep -cE '^(package|package-lock)\.json$'` returns 0 — zero dependency change (T-62-SC).
7. The `<human-check>` in Task 3 runs at end of phase (`workflow.human_verify_mode: end-of-phase`), covering the visual-hierarchy and non-admin-toast items no automated check can settle.
</verification>

<success_criteria>
1. A "Remediation" tab is present in the Settings tab bar for every authenticated user, in the unrestricted run alongside Security and Evidence, labelled with exactly one word and the clipboard-list glyph (ROADMAP criterion 1; D-01/D-02/D-03).
2. The tab mounts a panel showing the tenant's current remediation SLA at-risk window in days, and saving persists it through the already-live write route with the success toast `SLA window updated` (ROADMAP criterion 2; SLA-03).
3. A non-admin sees the identical field, and a save attempt surfaces the backend 403 as the shared generic error toast with no client-side permission logic anywhere in the component or its wiring (ROADMAP criterion 3; D-04).
4. All five SLA-03 behaviors in `62-VALIDATION.md`'s verification map have passing automated tests selectable by their documented `-t` filters.
5. No server-side file and no dependency manifest changed.
</success_criteria>

<output>
Create `.planning/phases/62-remediation-sla-settings-ui/62-01-SUMMARY.md` when done.
</output>
