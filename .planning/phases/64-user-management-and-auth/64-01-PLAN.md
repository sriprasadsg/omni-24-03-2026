---
phase: 64-user-management-and-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/user_endpoints.py
  - backend/rbac_service.py
  - backend/sso_service.py
autonomous: true
requirements:
  - ITAM-USR-01
  - ITAM-USR-02
  - ITAM-USR-03
  - ITAM-USR-04
  - ITAM-USR-05
  - ITAM-USR-06
user_setup: []

must_haves:
  truths:
    - User CRUD, RBAC, LDAP, SAML/SSO, API tokens, and 2FA are integrated into the ITAM console.
  artifacts:
    - backend/ldap_service.py
  key_links:
    - ITAM-console-auth-flow-to-backend-auth-services
---

<objective>
Implement and extend User Management & Auth features for the ITAM-Backlog milestone.

Purpose: Bridge gap between current ITAM console and Snipe-IT functionality.
Output: Extended Auth/User services, new LDAP integration, API token lifecycle, and verified 2FA.
</objective>

<execution_context>
@/home/user/enterprise-omni-agent-ai-platform/.claude/gsd-core/workflows/execute-plan.md
@/home/user/enterprise-omni-agent-ai-platform/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/64-user-management-and-auth/RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and extend existing auth modules</name>
  <files>
    backend/user_endpoints.py
    backend/rbac_service.py
  </files>
  <action>
    Audit `user_endpoints.py` for full ITAM-user-CRUD capability (especially tenant-scoped CRUD and ITAM-role assignment).
    Audit `rbac_service.py` for normalization consistency and ITAM-specific roles.
    Fix identified pitfalls (role normalization, etc.).
  </action>
  <verify>pytest backend/tests/test_user_management.py -x</verify>
  <done>User CRUD and RBAC are extended to meet ITAM requirements.</done>
</task>

<task type="auto">
  <name>Task 2: Implement LDAP/AD integration</name>
  <files>
    backend/ldap_service.py
  </files>
  <action>
    Build new `ldap_service.py` for directory queries.
    Use `ldap3` library for LDAP integration (pure Python, easy to deploy).
    Add endpoint for LDAP configuration and provisioning.
  </action>
  <verify>pytest backend/tests/test_ldap_service.py -x</verify>
  <done>LDAP/AD integration is implemented and verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- Client → API (SSO/2FA/Login/User CRUD)

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-64-01 | Tampering | LDAP queries | medium | mitigate | Use `ldap3` for parameterization/sanitization |
| T-64-02 | Spoofing | SSO Callback | high | mitigate | Validate state + nonce |
</threat_model>

<verification>
Automated: Run full backend test suite.
Manual: Verify SSO login and 2FA flow in live environment.
</verification>

<success_criteria>
All 6 requirements (ITAM-USR-01/02/03/04/05/06) verified.
</success_criteria>

<output>
Create .planning/phases/64-user-management-and-auth/64-01-SUMMARY.md when done
</output>
