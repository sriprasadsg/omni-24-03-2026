---
phase: 64-user-management-and-auth
plan: 02
type: execute
wave: 2
depends_on:
  - 64-01
files_modified:
  - backend/mfa_service.py
  - backend/api_key_auth.py
  - backend/sso_service.py
  - backend/user_endpoints.py
autonomous: true
requirements:
  - ITAM-USR-04
  - ITAM-USR-05
  - ITAM-USR-06
user_setup: []

must_haves:
  truths:
    - SAML/SSO is integrated and works end-to-end.
    - API Access Token Management is fully functional.
    - Two-Factor Authentication (2FA) is working and integrated into the login flow.
  artifacts:
    - (N/A - modifications to existing files)
  key_links:
    - SAML-IdP-to-SP-connection
    - API-token-generation-and-validation
    - 2FA-login-flow
---

<objective>
Complete the implementation of SAML/SSO, API Access Token Management, and Two-Factor Authentication (2FA).

Purpose: Fulfill remaining ITAM-USR requirements (ITAM-USR-04, ITAM-USR-05, ITAM-USR-06).
Output: Fully functional SSO, API token management, and 2FA.
</objective>

<execution_context>
@/home/user/enterprise-omni-agent-ai-platform/.claude/gsd-core/workflows/execute-plan.md
@/home/user/enterprise-omni-agent-ai-platform/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/64-user-management-and-auth/RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement SAML/SSO authentication</name>
  <files>
    backend/sso_service.py
    backend/user_endpoints.py
  </files>
  <action>
    Audit `sso_service.py` to confirm SAML support (Research suggests it might be OAuth2 only).
    If SAML is not fully supported, integrate `pysaml2` or `python3-saml` for SAML 2.0 IdP consumer.
    Add endpoints for SAML callback (ACS) and metadata.
    Ensure user provisioning is handled correctly for SAML users.
  </action>
  <verify>pytest backend/tests/test_sso_saml.py -x</verify>
  <done>SAML/SSO authentication is fully implemented and tested.</done>
</task>

<task type="auto">
  <name>Task 2: Enhance API Access Token Management</name>
  <files>
    backend/api_key_auth.py
    backend/user_endpoints.py
  </files>
  <action>
    Extend `api_key_auth.py` to support full API token lifecycle (create, list, revoke, scope, rate limit).
    Add new endpoints in `user_endpoints.py` for managing API tokens.
    Ensure API tokens can be scoped to specific permissions.
  </action>
  <verify>pytest backend/tests/test_api_token_management.py -x</verify>
  <done>API Access Token Management has full lifecycle support and is secured.</done>
</task>

<task type="auto">
  <name>Task 3: Verify and integrate 2FA</name>
  <files>
    backend/mfa_service.py
    backend/user_endpoints.py
  </files>
  <action>
    Audit `mfa_service.py` for completeness (TOTP, backup codes, encryption).
    Fix identified pitfalls (in-memory MFA sessions, base64 fallback for secrets).
    Ensure frontend wiring is complete for 2FA enrollment and login flows.
  </action>
  <verify>pytest backend/tests/test_mfa_flow.py -x</verify>
  <done>Two-Factor Authentication (2FA) is fully integrated, secure, and functional.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- Client → API (SSO/2FA/Login/User CRUD)

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-64-03 | Elevation of Privilege | MFA Session Token | high | mitigate | Implement persistent storage (Redis/DB TTL) |
| T-64-04 | Information Disclosure | TOTP Secret Encryption | high | mitigate | Enforce `encryption_service` use, fail loudly on fallback |
| T-64-05 | Elevation of Privilege | API Token Scope | high | mitigate | Implement granular scopes/permissions for API keys |
</threat_model>

<verification>
Automated: Run full backend test suite.
Manual: Verify SSO login, API token creation/use, and 2FA flow in live environment.
</verification>

<success_criteria>
All 6 requirements (ITAM-USR-01/02/03/04/05/06) verified.
</success_criteria>

<output>
Create .planning/phases/64-user-management-and-auth/64-02-SUMMARY.md when done
</output>
