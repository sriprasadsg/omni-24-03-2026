---
phase: 28-governance-document-management
plan: 03
type: execute
wave: 3
depends_on: [28-02]
files_modified:
  - components/GovernanceDocumentsDashboard.tsx
  - App.tsx
  - components/Sidebar.tsx
  - types.ts
autonomous: true
requirements: [DOC-01, DOC-02]
must_haves:
  truths:
    - "A user can open a Governance Documents dashboard from the sidebar's Governance & Compliance section, create a draft, add versions, submit for approval, publish, sign, and export a signed PDF."
    - "The new dashboard is actually reachable in the running app — wired into App.tsx (lazy import + permission map + switch-case), Sidebar.tsx (nav item), and types.ts (AppView union) — not an orphaned component."
    - "The sign form sends only a typed name and a consent checkbox to the server; it never sends signer identity, IP, or timestamp (those are server-derived)."
  artifacts:
    - "components/GovernanceDocumentsDashboard.tsx — tab-list-modal dashboard"
    - "App.tsx GovernanceDocumentsDashboard lazy import + governanceDocuments permission entry + render case"
    - "components/Sidebar.tsx governanceDocuments nav item inside Governance & Compliance"
    - "types.ts AppView 'governanceDocuments' member"
  key_links:
    - "Sidebar governanceDocuments item to App.tsx switch-case 'governanceDocuments' to GovernanceDocumentsDashboard (reachability)"
    - "Dashboard authFetch calls to /api/governance/documents endpoints from plans 28-01/28-02"
---

<objective>
Ship the DOC-01/DOC-02 frontend: a Governance Documents dashboard that lists documents, creates drafts, adds versions, submits for approval, publishes, signs (typed name + consent), and exports the signed PDF — and wire it into navigation so it is actually reachable.

Purpose: This project has a 6-instance documented history (STATE.md) of shipping fully-built dashboards that were never wired into navigation (5 dashboards in the v2.0 backfill plus ProgramsDashboard). Nav-wiring here is MANDATORY, not optional (Pitfall 6). GovernanceDocumentsDashboard is a NEW ROUTED DASHBOARD (unlike Phase 26's modal), so it needs the full App.tsx + Sidebar.tsx + types.ts wiring, added in the same commit as the component.

Output: the dashboard component plus verified wiring across all three integration files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-governance-document-management/28-RESEARCH.md
@.planning/phases/28-governance-document-management/28-PATTERNS.md

# Endpoint contracts this dashboard consumes (from plans 28-01 / 28-02):
@.planning/phases/28-governance-document-management/28-01-PLAN.md
@.planning/phases/28-governance-document-management/28-02-PLAN.md

# Analog source files (read only the cited line ranges):
@components/PrivacyLegalDashboard.tsx
@components/BAAManagement.tsx
@components/Sidebar.tsx
@App.tsx
@types.ts
</context>

<artifacts_produced>
New symbols this plan creates:

- File: components/GovernanceDocumentsDashboard.tsx (exports GovernanceDocumentsDashboard React.FC)
- App.tsx: a GovernanceDocumentsDashboard lazy import; a permission-map entry governanceDocuments: 'view:compliance'; a render case 'governanceDocuments'
- components/Sidebar.tsx: a nav item { view: 'governanceDocuments', label: 'Governance Documents', icon: FileTextIcon, permission: 'view:compliance' } inside the existing Governance & Compliance section
- types.ts: AppView union member 'governanceDocuments'
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Build GovernanceDocumentsDashboard.tsx</name>
  <files>components/GovernanceDocumentsDashboard.tsx</files>
  <read_first>
    - components/PrivacyLegalDashboard.tsx lines 1-74 (state + fetch-by-tab + tab-bar shape)
    - components/BAAManagement.tsx lines 56-81 (Promise.all authFetch + fallback; sign-action POST pattern)
  </read_first>
  <action>
    Create components/GovernanceDocumentsDashboard.tsx following PrivacyLegalDashboard's tab-list-modal shape and BAAManagement's authFetch + showToast conventions. Import React hooks, authFetch from ../services/apiService, showToast from ../utils/toast, and FileTextIcon from ./icons.

    Tabs: Documents (all), Pending Approval (status pending_approval), Signed (documents with a non-empty signatures array). Load the list via authFetch('/api/governance/documents') with a status query where relevant, applying the Promise.all + catch fallback so a failed fetch shows a toast and leaves an empty list.

    Actions wired to the plan 28-01/28-02 endpoints: create draft (modal collecting title, doc_type, content; POST /api/governance/documents); add new version (POST /documents/{id}/new-version); submit for approval (POST /documents/{id}/submit-for-approval); publish (POST /documents/{id}/publish); sign (modal with a required typed-name text input and a required "I agree this constitutes my legal signature" consent checkbox; POST /documents/{id}/sign with a body of ONLY { typed_name, consent } — never send signer identity, IP, or timestamp, which the server derives, threat T-28-07); export signed PDF (GET /documents/{id}/export-signed-pdf, then open/download the returned url). Show success/error toasts and refresh the list after each mutation. Disable the sign submit button until both typed name is non-empty and consent is checked. Keep the file under the 500-line CLAUDE.md limit.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>GovernanceDocumentsDashboard.tsx compiles into the production bundle; it renders the three tabs, a create-draft modal, and a sign modal whose POST body contains only typed_name and consent.</done>
  <acceptance_criteria>
    - npm run build succeeds with the new component.
    - The sign modal disables submit until typed name is non-empty and consent is checked.
    - The sign POST body carries only typed_name and consent (no ip/timestamp/identity fields).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Mandatory nav-wiring — App.tsx + Sidebar.tsx + types.ts</name>
  <files>App.tsx, components/Sidebar.tsx, types.ts</files>
  <read_first>
    - App.tsx line 197 (BAAManagement lazy import), line 388 (baaManagement permission-map entry), line 1898 (baaManagement render case) — mirror each for governanceDocuments
    - components/Sidebar.tsx lines 346-374 (the Governance & Compliance section items array; the baaManagement entry is the last item, line 373)
    - types.ts line 144 (baaManagement in the AppView union)
  </read_first>
  <action>
    In App.tsx: add a lazy import near line 197 for GovernanceDocumentsDashboard importing from ./components/GovernanceDocumentsDashboard and mapping its named export to default, mirroring the BAAManagement lazy import. Add governanceDocuments: 'view:compliance' to the permission map near line 388 (sibling to baaManagement). Add a render case 'governanceDocuments' near line 1898 that renders GovernanceDocumentsDashboard wrapped in the same ErrorBoundary + Suspense fallback pattern as the baaManagement case.

    In components/Sidebar.tsx: add a new item to the existing Governance & Compliance section's items array (lines 346-374), immediately after the baaManagement entry at line 373, of the form { view: 'governanceDocuments', label: 'Governance Documents', icon: FileTextIcon at size 20, permission: 'view:compliance' }. Do NOT create a new top-level nav section — it belongs inside the existing Governance & Compliance section.

    In types.ts: add 'governanceDocuments' to the AppView union adjacent to baaManagement at line 144.
  </action>
  <verify>
    <automated>grep -c "governanceDocuments" App.tsx | grep -qx 3 && grep -c "governanceDocuments" components/Sidebar.tsx | grep -qx 1 && grep -c "governanceDocuments" types.ts | grep -qx 1 && npm run build</automated>
  </verify>
  <done>governanceDocuments appears 3 times in App.tsx (lazy import + permission map + render case), once in Sidebar.tsx (nav item under Governance & Compliance), once in types.ts (AppView union); the app builds. The dashboard is reachable, not orphaned.</done>
  <acceptance_criteria>
    - App.tsx contains the lazy import, the permission-map entry, and the render case (3 occurrences of governanceDocuments).
    - Sidebar.tsx contains exactly one governanceDocuments nav item, inside the Governance & Compliance section (no new top-level section).
    - types.ts AppView union includes 'governanceDocuments'.
    - npm run build succeeds.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser to API | Dashboard sends authFetch (JWT bearer) requests; the sign form submits {typed_name, consent} only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-07 | Spoofing | client sign form | medium | mitigate | The sign form submits only typed_name + consent; all authoritative signature metadata (identity, IP, user-agent, timestamp) is server-derived (defense-in-depth reinforcing the backend guarantee in 28-02 / T-28-02). |
| T-28-01 | Information Disclosure | dashboard data fetch | medium | mitigate | All reads go through authFetch (JWT bearer); backend enforces tenant isolation and RBAC (view:compliance) — the client renders only what the tenant-scoped API returns. |
| T-28-SC | Tampering | npm installs (supply chain) | low | accept | No new npm packages introduced — the dashboard reuses existing React + apiService + toast + icons. No [ASSUMED]/[SUS] packages to gate. |
</threat_model>

<verification>
- npm run build succeeds with the new component and wiring.
- grep -rn "GovernanceDocumentsDashboard" App.tsx returns the lazy import + render case (proves not orphaned).
- Reachability click-through (see human-check below).
</verification>

<success_criteria>
- The Governance Documents dashboard is reachable from the Governance & Compliance sidebar section and drives all DOC-01/DOC-02 flows.
- Nav-wiring exists in all three integration files; the app builds.
</success_criteria>

<verify>
  <human-check>
    Per this project's human_verify_mode: end-of-phase — at the end of the phase, run the app, open the sidebar's "Governance & Compliance" section, confirm a "Governance Documents" entry exists and opens the new dashboard, then create a draft, submit for approval, approve it (via the existing Approvals surface), publish, sign with a typed name + consent, and export the signed PDF. Confirm the exported PDF names the signer and timestamp.
  </human-check>
</verify>

<output>
Create `.planning/phases/28-governance-document-management/28-03-SUMMARY.md` when done.
</output>
