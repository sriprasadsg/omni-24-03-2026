---
phase: 64-rotate_key-autonomous-remediation-action
plan: 03
type: execute
wave: 2
depends_on: ["64-01", "64-02"]
files_modified:
  - agent-rust/src/remediation_actions.rs
  - agent-rust/src/main.rs
  - agent-rust/src/shell.rs
  - agent-rust/src/crypto.rs
  - agent-rust/src/scanner/ssh_key_rotation.rs
  - agent-rust/src/scanner/mod.rs
  - agent-rust/src/scanner/scanner.rs
autonomous: true
requirements: ["AUTO-02"]

must_haves:
  truths:
    - The Rust agent can autonomously rotate weak SSH keys.
    - Key rotation includes generating a new strong key pair and updating authorized_keys if necessary.
    - A post-rotation verification confirms the new key is functional and strong.
  artifacts:
    - agent-rust/src/scanner/ssh_key_rotation.rs exists and contains key rotation logic.
    - agent-rust/src/remediation_actions.rs includes a `RotateKey` action.
  key_links:
    - The `RotateKey` remediation action successfully triggers the key rotation logic and verifies it.
---

<objective>
Implement SSH key rotation mechanics in the Rust agent, integrate it as an autonomous remediation action, and add post-rotation verification.

Purpose: To fully enable the agent to remediate weak SSH keys by automatically replacing them with strong ones, closing the loop on a critical security vulnerability.
Output: A functional `rotate_key` autonomous remediation action within the agent, capable of generating new keys and verifying their functionality.
</objective>

<execution_context>
@/home/user/enterprise-omni-agent-ai-platform/.claude/gsd-core/workflows/execute-plan.md
@/home/user/enterprise-omni-agent-ai-platform/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@agent-rust/src/remediation_actions.rs
@agent-rust/src/main.rs
@agent-rust/src/shell.rs
@agent-rust/src/crypto.rs
@agent-rust/src/scanner/mod.rs
@agent-rust/src/scanner/scanner.rs
@agent-rust/src/scanner/ssh_key_checks.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement SSH Key Rotation Logic</name>
  <files>
    agent-rust/src/scanner/ssh_key_rotation.rs
    agent-rust/src/scanner/ssh_key_rotation_test.rs
    agent-rust/src/crypto.rs
  </files>
  <behavior>
    - Test 1: Generate a new Ed25519 key pair at a specified path.
    - Test 2: Backup an existing key before rotation.
    - Test 3: Update `authorized_keys` with the new public key.
    - Test 4: Perform a basic post-rotation verification (e.g., check new key permissions, read public part).
    - Test 5: Handle cases where `authorized_keys` doesn't exist.
  </behavior>
  <action>
    Create `agent-rust/src/scanner/ssh_key_rotation.rs`.
    Implement functions:
    - `backup_key(key_path: &Path)`: Renames original private key to `*.bak`.
    - `generate_new_key(path: &Path)`: Generates a new strong SSH key pair (e.g., Ed25519 or RSA 4096). This might require leveraging/creating utilities in `agent-rust/src/crypto.rs` for key generation.
    - `update_authorized_keys(old_pub_key: &str, new_pub_key: &str)`: Locates `authorized_keys` and replaces/adds the new public key.
    - `verify_rotation(key_path: &Path)`: Checks if the new key exists, has correct permissions, and its public part is valid.
    Create corresponding unit tests in a `_test.rs` file, using temporary files for key paths.
  </action>
  <verify>
    <automated>cd agent-rust && cargo test --test ssh_key_rotation_test</automated>
  </verify>
  <done>
    `agent-rust/src/scanner/ssh_key_rotation.rs` exists, has logic for backing up, generating, updating, and verifying SSH key rotation, and all unit tests pass. Dependency on `crypto.rs` is established for key generation.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate Key Rotation into Remediation Actions</name>
  <files>
    agent-rust/src/remediation_actions.rs
    agent-rust/src/main.rs
    agent-rust/src/scanner/mod.rs
    agent-rust/src/scanner/scanner.rs
    agent-rust/src/scanner/ssh_key_checks.rs
  </files>
  <action>
    - In `agent-rust/src/scanner/mod.rs`, declare `mod ssh_key_rotation;`.
    - In `agent-rust/src/remediation_actions.rs`:
      - Add `RotateKey(String)` variant to the `RemediationAction` enum, where the string is the path to the key to rotate.
      - Implement the `execute` logic for `RotateKey` to call functions from `ssh_key_rotation.rs`.
      - Ensure proper error handling and logging during rotation.
    - In `agent-rust/src/main.rs` (or the agent's command dispatch loop):
      - Update the action dispatch to handle the new `RotateKey` action.
    - Update `agent-rust/src/scanner/ssh_key_checks.rs` to, on detection of a weak key, also provide the necessary context (e.g., key path) that can be passed to the `RotateKey` action.
    - Add an end-to-end test in `agent-rust/src/scanner/tests/integration_test.rs` (or a new `remediation_test.rs`) that:
      - Creates a temporary weak SSH key.
      - Triggers the `RotateKey` remediation action with the path to the weak key.
      - Verifies that the old key is backed up, a new strong key is generated, `authorized_keys` is updated, and the new key passes the strength check.
  </action>
  <verify>
    <automated>cd agent-rust && cargo test --test remediation_test -- --nocapture</automated>
  </verify>
  <done>
    The `RotateKey` action is fully integrated into the agent's remediation system. An end-to-end test confirms successful execution of key rotation from action trigger to post-rotation verification, including updating `authorized_keys` and ensuring the new key is strong.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent (local) | Local file system access for SSH keys and `authorized_keys`. |
| agent (local) | Execution of `ssh-keygen` or similar utilities for key generation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-64-03 | Tampering | `ssh_key_rotation.rs` | critical | mitigate | Implement robust backup/rollback mechanisms. Ensure new keys are generated with secure permissions and best practices for key types/lengths. |
| T-64-04 | Privilege Escalation | Key modification | critical | mitigate | Ensure the agent operates with the minimum necessary privileges for key rotation. Strictly validate key paths to prevent arbitrary file writes. |
| T-64-05 | Denial of Service | Key corruption | high | mitigate | Verify new key functionality before removing old key backups. If `authorized_keys` update fails, ensure the service isn't left in an unrecoverable state. |
| T-64-SC | Tampering | npm/pip/cargo installs | high | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
Manually verify that triggering a `RotateKey` action on a test system successfully replaces a weak SSH key with a strong one, backs up the old key, and updates `authorized_keys`.
</verification>

<success_criteria>
The Rust agent successfully executes the `rotate_key` autonomous remediation action, replacing weak SSH keys with strong ones and providing post-rotation verification, as evidenced by passing integration tests and manual verification.
</success_criteria>

<output>
Create `.planning/phases/64-rotate_key-autonomous-remediation-action/64-03-PLAN.md` when done
</output>
