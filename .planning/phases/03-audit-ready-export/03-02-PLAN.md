---
phase: 03-audit-ready-export
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - backend/compliance_reporting_pdf.py
  - backend/compliance_reporting_excel.py
  - backend/compliance_reporting_service.py
  - backend/tests/test_audit_export.py
autonomous: true
requirements: [AUDIT-01, AUDIT-02, AUDIT-03]
must_haves:
  truths:
    - "The PDF header line shows framework name, tenant name, and export date"
    - "The XLSX header rows show framework name, tenant name, and export date"
    - "The PDF control table renders the Auto Evidence and Manual Evidence columns"
    - "The XLSX Control Details sheet renders the Auto Evidence and Manual Evidence columns"
    - "_generate_pdf and _generate_excel resolve the tenant display name from db.tenants"
  artifacts:
    - path: "backend/compliance_reporting_pdf.py"
      provides: "Tenant-name header line + source evidence columns in PDF"
      contains: "tenant"
    - path: "backend/compliance_reporting_excel.py"
      provides: "Tenant-name header row + source evidence columns in XLSX"
      contains: "tenant"
  key_links:
    - from: "backend/compliance_reporting_pdf.py::_generate_pdf"
      to: "db.tenants"
      via: "find_one tenant name lookup"
      pattern: "tenants\\.find_one"
    - from: "backend/compliance_reporting_excel.py::_generate_excel"
      to: "db.tenants"
      via: "find_one tenant name lookup"
      pattern: "tenants\\.find_one"
---

<objective>
Render the auditor-required header fields (framework name, tenant name, export date) and the per-source evidence columns (Auto Evidence, Manual Evidence) in both the PDF and XLSX exports, consuming the `tenant_id` threading and `auto_count`/`manual_count` data fields established in Plan 03-01.

Purpose: AUDIT-01 (PDF header completeness), AUDIT-02 (XLSX header completeness), and the rendering half of AUDIT-03 (source-labelled evidence visible in both output formats) require the generators to fetch the tenant display name and surface the new evidence columns produced by the data layer.
Output: A tenant-named, source-column-aware PDF generator and XLSX generator, plus the two header-field tests turned GREEN.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-audit-ready-export/03-RESEARCH.md
@.planning/phases/03-audit-ready-export/03-PATTERNS.md
@.planning/phases/03-audit-ready-export/03-01-PLAN.md
@CLAUDE.md

@backend/compliance_reporting_pdf.py
@backend/compliance_reporting_excel.py
@backend/compliance_reporting_service.py
@backend/compliance_reporting_data.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add tenant-name header line and source evidence columns to the PDF (AUDIT-01, AUDIT-03)</name>
  <files>backend/compliance_reporting_pdf.py, backend/compliance_reporting_service.py</files>
  <behavior>
    - `_generate_pdf` accepts an optional trailing `tenant_id: str = None` and resolves a display name via `db.tenants.find_one({"id": tenant_id})`, falling back to `tenant_id` (or "Unknown Tenant" when None).
    - The PDF subtitle paragraph (currently lines 92-97) includes `Tenant: {tenant_name}` and an explicit `Export Date: {YYYY-MM-DD}` segment alongside the existing Generated/Controls/Assets fields.
    - The Control Details table renders the `Auto Evidence` and `Manual Evidence` columns that `_build_report_data` now emits — because the table is built from `list(control_rows[0].keys())`, these columns appear automatically; the per-column width hint list (`det_widths`, line 122) is extended so the two new numeric columns get a narrow width and the table still fits the landscape page.
  </behavior>
  <action>Edit `backend/compliance_reporting_pdf.py`. (a) Change `_generate_pdf(framework_id, reports_dir)` signature (line 42) to `_generate_pdf(framework_id: str, reports_dir: str, tenant_id: str = None)`. (b) At the top of the function (before line 43's `_build_report_data` call) add `from database import get_database`, `db = get_database()`, resolve `tenant_name = tenant_id or "Unknown Tenant"` then `if tenant_id: tenant_doc = await db.tenants.find_one({"id": tenant_id}); tenant_name = tenant_doc.get("name", tenant_id) if tenant_doc else tenant_id`. Pass `tenant_id` into the data call: change line 43 to `await _build_report_data(framework_id, tenant_id)`. (c) Update the subtitle Paragraph (lines 92-97): add `f"Tenant: {tenant_name} &nbsp;|&nbsp; "` and `f"Export Date: {datetime.now().strftime('%Y-%m-%d')} &nbsp;|&nbsp; "` segments before the existing `Generated:` segment so the line reads framework/tenant/export-date plus the existing controls/assets counts. (d) The `det_widths` list (line 122) currently has 13 entries for 13 columns; `_build_report_data` now appends `Auto Evidence` and `Manual Evidence` after `Evidence Count`, so the column count grows. Insert two narrow width hints (e.g. `0.6, 0.6`) into `det_widths` at the position matching where the two new keys land (immediately after the `Evidence Count` width). Since the code already slices `det_widths[:len(det_headers)]` (line 123), over-providing widths is safe, but the new columns must have hints so they are not zero-width — append/insert two values so the list length is at least `len(det_headers)`. Do NOT hardcode the header list; keep deriving from `control_rows[0].keys()`. (e) `compliance_reporting_service.py`: update `generate_pdf_report` (line 133) to `return await _generate_pdf(framework_id, self.reports_dir, tenant_id)`. Keep `compliance_reporting_pdf.py` under 500 lines (currently ~139).</action>
  <verify>
    <automated>cd backend && ./venv/bin/python -m pytest tests/test_audit_export.py::test_pdf_header_fields -x -q</automated>
  </verify>
  <done>`_generate_pdf` accepts `tenant_id` and resolves tenant name from `db.tenants`; subtitle includes Tenant and Export Date; `det_widths` covers the two new evidence columns; `generate_pdf_report` forwards `tenant_id`; test_pdf_header_fields passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add tenant-name header row and Auto/Manual evidence columns to the XLSX (AUDIT-02, AUDIT-03)</name>
  <files>backend/compliance_reporting_excel.py, backend/compliance_reporting_service.py</files>
  <behavior>
    - `_generate_excel` accepts an optional trailing `tenant_id: str = None` and resolves a tenant display name via `db.tenants.find_one({"id": tenant_id})` (same fallback semantics as the PDF generator).
    - Sheet 1 ("Asset Summary") gains a header row stating the tenant name and an explicit export-date row, alongside the existing `Compliance Report: {fw_name}` title (line 88) and `Generated:` row (line 90).
    - The "Control Details" sheet (ws2) automatically renders the `Auto Evidence` and `Manual Evidence` columns because headers derive from `list(control_rows[0].keys())` (line 119); no header list is hardcoded, so the new data-layer columns flow through. Confirm `_apply_status_colors`/`_apply_url_hyperlink` index lookups still resolve (they look up `Control Status`, `Asset Status`, `Evidence URLs` by name, so inserting numeric columns does not break them).
    - `_generate_all_excel` accepts an optional `tenant_id: str = None` and forwards it to its internal `_build_report_data` calls (line 179) so all-frameworks data stays tenant-scoped; the Overview sheet gains a tenant-name line.
  </behavior>
  <action>Edit `backend/compliance_reporting_excel.py`. (a) `_generate_excel` signature (line 80) → `_generate_excel(framework_id: str, reports_dir: str, tenant_id: str = None)`. At function top add `from database import get_database`, `db = get_database()`, and resolve `tenant_name` exactly as in the PDF task (find_one on `db.tenants`, fallback to tenant_id / "Unknown Tenant"). Change line 81 to `await _build_report_data(framework_id, tenant_id)`. (b) On ws1, after the title row (line 89) insert `ws1.append([f"Tenant: {tenant_name}"])` and after the `Generated:` row (line 90) add `ws1.append([f"Export Date: {datetime.now().strftime('%Y-%m-%d')}"])` so the header block shows framework, tenant, generated timestamp, and export date before the existing blank+section rows. (c) The ws2 Control Details sheet needs no header-list edits — it derives `headers2 = list(control_rows[0].keys())` (line 119), so `Auto Evidence`/`Manual Evidence` appear automatically; verify the name-based index lookups at lines 121-123 (`Control Status`, `Asset Status`, `Evidence URLs`) still succeed (they do — those keys are unchanged). (d) `_generate_all_excel` signature (line 144) → add trailing `tenant_id: str = None`; pass it into the per-framework `_build_report_data(fw_id, tenant_id)` call (line 179); after the `Generated:` row on the Overview sheet (line 162) add `ov.append([f"Tenant: {tenant_id or 'Unknown Tenant'}"])` (resolving the display name once at the top of the function as in (a) and using it here). (e) `compliance_reporting_service.py`: update `generate_excel_report` (line 130) → `return await _generate_excel(framework_id, self.reports_dir, tenant_id)` and `generate_all_excel_report` (line 139) → `return await _generate_all_excel(self.reports_dir, tenant_id=tenant_id)` (matching the existing `db=None` keyword-style signature; pass tenant_id positionally only if the param order is reports_dir-then-tenant_id — add `tenant_id` BEFORE the existing `db=None` param so `_generate_all_excel(reports_dir, tenant_id=None, db=None)`). Keep `compliance_reporting_excel.py` under 500 lines (currently ~248).</action>
  <verify>
    <automated>cd backend && ./venv/bin/python -m pytest tests/test_audit_export.py::test_xlsx_header_fields -x -q</automated>
  </verify>
  <done>`_generate_excel` and `_generate_all_excel` accept `tenant_id`; ws1 header block shows tenant name and export date; Control Details sheet renders Auto Evidence and Manual Evidence columns; status-color/hyperlink index lookups intact; service methods forward `tenant_id`; test_xlsx_header_fields passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → database (tenant lookup) | `_generate_pdf`/`_generate_excel` read `db.tenants` by the caller's own tenant_id |
| generator → filesystem | Report bytes written to `static/reports/` by trusted server code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Information Disclosure | tenant-name lookup in generators | accept | `db.tenants.find_one({"id": tenant_id})` uses the caller's own tenant_id (threaded from JWT via Plan 03-01); no cross-tenant key supplied |
| T-03-05 | Denial of Service | PDF/XLSX table overflow from oversized evidence cells | accept | Generators export only name/url/date/desc + integer counts; `content` field excluded per RESEARCH Pitfall 5 |
| T-03-SC | Tampering | npm/pip installs | accept | No new packages; reportlab 4.5.1 + openpyxl 3.1.5 already installed |
</threat_model>

<verification>
- `cd backend && ./venv/bin/python -m pytest tests/test_audit_export.py -x -q` — all six tests pass (Plan 03-01's four plus this plan's two header tests).
- `cd backend && ./venv/bin/python -m pytest tests/ -x -q` — full suite green.
- `grep -n "tenants.find_one" backend/compliance_reporting_pdf.py backend/compliance_reporting_excel.py` — tenant lookup present in both generators.
- `grep -nc "Tenant:" backend/compliance_reporting_excel.py` — tenant header row present.
</verification>

<success_criteria>
- PDF subtitle shows framework name, tenant name, and export date; PDF control table renders Auto Evidence and Manual Evidence columns (AUDIT-01, AUDIT-03).
- XLSX Asset Summary sheet shows framework name, tenant name, generated timestamp, and export date; Control Details sheet renders Auto Evidence and Manual Evidence columns (AUDIT-02, AUDIT-03).
- `_generate_pdf`, `_generate_excel`, and `_generate_all_excel` accept and forward `tenant_id`; service methods pass it through.
- All backend files remain under 500 lines; no new pip packages; no Co-Authored-By trailer in the commit.
</success_criteria>

<output>
Create `.planning/phases/03-audit-ready-export/03-02-SUMMARY.md` when done.
</output>
