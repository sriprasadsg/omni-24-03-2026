---
phase: 03-audit-ready-export
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_audit_export.py
  - backend/compliance_reporting_data.py
  - backend/compliance_reports_endpoints.py
  - backend/compliance_reporting_service.py
autonomous: true
requirements: [AUDIT-03, AUDIT-04]
must_haves:
  truths:
    - "_flatten_evidence returns separate auto_count and manual_count buckets"
    - "Each evidence name is prefixed with [Auto] or [Manual]"
    - "control_rows entries carry Auto Evidence and Manual Evidence integer columns"
    - "A tenant-A user cannot download a tenant-B report via the legacy /api/compliance/reports/download/{filename} route"
    - "tenant_id is threaded from ComplianceReportingService down to _build_report_data"
  artifacts:
    - path: "backend/tests/test_audit_export.py"
      provides: "Failing-first unit tests for AUDIT-01..04"
      contains: "def test_evidence_source_labelling"
    - path: "backend/compliance_reporting_data.py"
      provides: "_flatten_evidence source bucketing + _build_report_data tenant_id param"
      contains: "auto_count"
    - path: "backend/compliance_reports_endpoints.py"
      provides: "Tenant ownership check on legacy download route"
      contains: "compliance_reports.find_one"
  key_links:
    - from: "backend/compliance_reporting_service.py"
      to: "backend/compliance_reporting_data.py::_build_report_data"
      via: "tenant_id keyword argument"
      pattern: "_build_report_data\\([^)]*tenant_id"
    - from: "backend/compliance_reports_endpoints.py"
      to: "db.compliance_reports"
      via: "find_one tenant ownership check"
      pattern: "compliance_reports\\.find_one"
---

<objective>
Close the AUDIT-03 (evidence source labelling) and AUDIT-04 (legacy download tenant isolation) gaps in the compliance reporting data layer, and thread `tenant_id` through the service-to-data call chain so Wave 2 renderers can consume it.

Purpose: The data layer must emit auto/manual evidence buckets and the legacy download route must enforce tenant ownership before the PDF/XLSX renderers (Plan 03-02) can surface those fields and before the phase can claim per-tenant isolation.
Output: A failing-first test module, source-labelled evidence in `_flatten_evidence`/`_build_report_data`, a tenant-checked legacy download route, and a `tenant_id`-aware service-to-data call chain.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-audit-ready-export/03-RESEARCH.md
@.planning/phases/03-audit-ready-export/03-PATTERNS.md
@CLAUDE.md

@backend/compliance_reporting_data.py
@backend/compliance_reporting_service.py
@backend/compliance_reports_endpoints.py
@backend/compliance_report_endpoints.py
@backend/tests/conftest.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0: Create failing-first test scaffold backend/tests/test_audit_export.py</name>
  <files>backend/tests/test_audit_export.py</files>
  <behavior>
    - test_evidence_source_labelling (AUDIT-03): calling `_flatten_evidence` on a list of one automated item (`{"id": "a1", "name": "Admin Check: X", "systemGenerated": True}`) and one manual item (`{"id": "m1", "name": "soc2.pdf", "source": "manual", "systemGenerated": False}`) returns a dict whose `auto_count == 1`, `manual_count == 1`, `count == 2`, and whose `names` list contains exactly `"[Auto] Admin Check: X"` and `"[Manual] soc2.pdf"`.
    - test_build_report_data_accepts_tenant_id (AUDIT-04/data-layer): `_build_report_data` is callable with a `tenant_id` keyword argument (signature contains `tenant_id`); assert via `inspect.signature(_build_report_data).parameters` containing `"tenant_id"`.
    - test_legacy_download_blocks_cross_tenant (AUDIT-04): build a test app from `compliance_reports_endpoints.router` with `current_user_override` = a tenant-A token and a `db_override` whose `compliance_reports.find_one` returns a doc with `tenantId == "tenant-b"`; a GET to `/api/compliance/reports/download/compliance_report_x_1.pdf` returns HTTP 403. (This test starts RED — the route currently has no tenant check.)
    - test_legacy_download_allows_owner (AUDIT-04): same setup but `compliance_reports.find_one` returns `tenantId == "tenant-a"` and the file exists on disk; assert the response is NOT 403 (200 or 404-if-file-absent both acceptable; the assertion is `status_code != 403`).
  </behavior>
  <action>Create `backend/tests/test_audit_export.py`. Import helpers from conftest: `from tests.conftest import make_token_data, make_test_app` is NOT importable directly — instead import the standalone helpers the way existing tests do (e.g. `test_tenant_isolation.py`): add `import sys, os; sys.path.insert(0, ...)` is already handled by conftest, so import via `from conftest import make_token_data, make_test_app` is the established pattern — match whatever `backend/tests/test_tenant_isolation.py` uses; read that file first and copy its import style verbatim. Import the units under test with `from compliance_reporting_data import _flatten_evidence, _build_report_data` and `from compliance_reports_endpoints import router as legacy_router`. Use `fastapi.testclient.TestClient` to exercise the download route, wiring `get_database` to the `db_override` via `app.dependency_overrides` or by monkeypatching `compliance_reports_endpoints.get_database` to return the mock (the legacy file imports the service, not `get_database`, so monkeypatch the module attribute you add in Task 2). For the synchronous-vs-async test runner, follow the `asyncio.run(...)` pattern recorded in STATE.md decision "02-01: asyncio.run() used for async test cases (pytest-asyncio not installed)" for any coroutine assertions. Do NOT place fenced code in this action; the `<behavior>` block above is the spec. Tests for evidence labelling and the tenant-403 path MUST fail on first run (RED) because the production code is not yet patched — that is expected and correct.</action>
  <verify>
    <automated>cd backend && ./venv/bin/python -m pytest tests/test_audit_export.py -q 2>&1 | grep -E "(failed|error|passed)"</automated>
  </verify>
  <done>Test file exists; collection succeeds with no import errors; test_evidence_source_labelling and test_legacy_download_blocks_cross_tenant FAIL (RED) while the scaffold itself is structurally valid.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Source-bucket evidence in _flatten_evidence and surface counts in _build_report_data (AUDIT-03)</name>
  <files>backend/compliance_reporting_data.py</files>
  <behavior>
    - After the change, `_flatten_evidence` returns a dict that additionally includes `auto_count` (int) and `manual_count` (int), and its `names` entries are each prefixed with `[Auto] ` or `[Manual] `.
    - Classification rule: an item is automated when `e.get("systemGenerated") is True` OR `e.get("source") is None`; otherwise it is manual (covers `source == "manual"` and `systemGenerated is False`). This mirrors STATE.md decision "02-02: isAutomated check uses ev.systemGenerated===true OR ev.source==='auto'".
    - Every `control_rows` entry produced by `_build_report_data` gains two integer keys: `"Auto Evidence"` and `"Manual Evidence"`, populated from `ev["auto_count"]` and `ev["manual_count"]`.
  </behavior>
  <action>Edit `backend/compliance_reporting_data.py`. (a) In `_flatten_evidence` (lines 33-66): inside the per-item loop, after computing `name` (line 45), compute `is_auto = e.get("systemGenerated") is True or e.get("source") is None` and `label = "[Auto]" if is_auto else "[Manual]"`; change the `if name:` block (lines 53-54) so the appended value is `f"{label} {name}"` and track auto vs manual counts (add two counters initialised to 0 alongside the lists at line 38, increment the matching counter only when `name` is truthy so counts align with rendered names). In the return dict (lines 63-66) add `"auto_count": <auto counter>, "manual_count": <manual counter>` — keep all existing keys (`names`, `urls`, `descriptions`, `uploaded_ats`, `statuses`, `count`) unchanged so no existing caller breaks. (b) In `_build_report_data`, both `control_rows.append({...})` blocks (the no-matching branch at lines 142-152 and the per-asset branch at lines 162-174) must add `"Auto Evidence": ev["auto_count"], "Manual Evidence": ev["manual_count"],` — insert them immediately after the existing `"Evidence Count": ev["count"],` line in each block so column ordering keeps evidence fields grouped. Do NOT reorder other keys. Per Research Pitfall 1, the keys are dropped silently if not added to control_rows — both branches must be edited. Keep the file under 500 lines (it is ~177 lines; this adds ~8 lines).</action>
  <verify>
    <automated>cd backend && ./venv/bin/python -m pytest tests/test_audit_export.py::test_evidence_source_labelling -x -q</automated>
  </verify>
  <done>test_evidence_source_labelling passes; `_flatten_evidence` return dict contains `auto_count` and `manual_count`; both `control_rows` append branches include `Auto Evidence` and `Manual Evidence` keys; all pre-existing return keys retained.</done>
</task>

<task type="auto">
  <name>Task 2: Add tenant ownership check to legacy download route + thread tenant_id through the service chain (AUDIT-04)</name>
  <files>backend/compliance_reports_endpoints.py, backend/compliance_reporting_service.py</files>
  <action>Two coordinated edits.

(a) `backend/compliance_reports_endpoints.py` download route `download_compliance_report` (lines 86-110): add a tenant ownership check matching the authoritative guard in `compliance_report_endpoints.py` lines 137-145. After the existing path-traversal and `os.path.exists` checks (after line 94) and before building `media_type` (line 96), add: `from database import get_database` at module top (line ~4, alongside existing imports), then `db = get_database()`, read `caller_tenant = getattr(current_user, "tenant_id", None)` and `caller_role = getattr(current_user, "role", "")`, define `_SUPER_ADMIN_ROLES = {"Super Admin", "super_admin", "superadmin", "platform-admin"}`, and if `caller_role not in _SUPER_ADMIN_ROLES`: `report_meta = await db.compliance_reports.find_one({"filename": filename})` and if `not report_meta or report_meta.get("tenantId") != caller_tenant: raise HTTPException(status_code=403, detail="Not authorized to access this report")`. Use the SAME `_SUPER_ADMIN_ROLES` set and 403 detail string as the authoritative route so behaviour is identical. Do NOT remove the route or alter the path-traversal guard.

(b) `backend/compliance_reporting_service.py`: thread `tenant_id` from the service methods into the data layer so Wave 2 generators receive it. Change the four single-framework renderer entrypoints to forward tenant_id: `generate_report` (line 128) calls `_generate_csv(framework_id, self.reports_dir)` — leave the CSV path's signature change OUT OF SCOPE for this plan UNLESS trivial; instead, the minimal required change for AUDIT-04 data-layer threading is to update the `_build_report_data` signature (in `compliance_reporting_data.py`) to `async def _build_report_data(framework_id: str, tenant_id: str = None):` with `tenant_id` defaulting to None so ALL existing callers (`_generate_csv`, `_generate_excel`, `_generate_pdf`, `_generate_all_*`) remain valid without edits. Apply that signature change in `compliance_reporting_data.py`. Then in `compliance_reporting_service.py` `_generate_csv` (line 29) update the call to `await _build_report_data(framework_id, tenant_id)` and add `tenant_id: str = None` as the third parameter of `_generate_csv` (line 28), passing it from `generate_report` (line 128: `await _generate_csv(framework_id, self.reports_dir, tenant_id)`). This establishes the threading pattern; Wave 2 (Plan 03-02) extends `_generate_pdf`/`_generate_excel` to accept and forward `tenant_id` the same way. Keep `tenant_id` as a trailing optional param (`str = None`) per STATE.md decision on backward-compatible param addition. Both files stay under 500 lines.</action>
  <verify>
    <automated>cd backend && ./venv/bin/python -m pytest tests/test_audit_export.py::test_legacy_download_blocks_cross_tenant tests/test_audit_export.py::test_legacy_download_allows_owner tests/test_audit_export.py::test_build_report_data_accepts_tenant_id -x -q</automated>
  </verify>
  <done>Cross-tenant download returns 403; owner download is not 403; `_build_report_data` signature accepts `tenant_id`; `_generate_csv` forwards `tenant_id` to `_build_report_data`; no existing caller breaks (full test module collects clean).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API (download route) | Untrusted `filename` path param + caller JWT cross here |
| API → database | Tenant-scoped reads; `compliance_reports` ownership lookup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | `compliance_reports_endpoints.py` legacy `/download/{filename}` | mitigate | Add `compliance_reports.find_one({"filename": filename})` + `tenantId == caller_tenant` check; 403 on mismatch (Task 2) |
| T-03-02 | Tampering | `filename` path param on legacy download route | accept | Existing `os.path.abspath(...).startswith(_REPORTS_DIR + os.sep)` traversal guard retained unchanged (lines 91-92) |
| T-03-03 | Information Disclosure | Framework ID enumeration in `_build_report_data` | accept | Frameworks are platform-global reference data; `asset_compliance`/`compliance_artifacts` already tenant-filtered via TenantIsolatedCollection (RESEARCH AUDIT-04) |
| T-03-SC | Tampering | npm/pip installs | accept | No new packages this phase; reportlab 4.5.1 + openpyxl 3.1.5 already installed (RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
- `cd backend && ./venv/bin/python -m pytest tests/test_audit_export.py -x -q` — all four tests pass after Task 2.
- `cd backend && ./venv/bin/python -m pytest tests/ -x -q` — full suite green (no regression in existing report/evidence tests).
- `grep -n "auto_count\|manual_count" backend/compliance_reporting_data.py` — both keys present in `_flatten_evidence` return.
- `grep -n "compliance_reports.find_one" backend/compliance_reports_endpoints.py` — tenant ownership check present.
</verification>

<success_criteria>
- `_flatten_evidence` returns `auto_count` and `manual_count`; evidence names carry `[Auto]`/`[Manual]` prefixes (AUDIT-03 data layer).
- Every `control_rows` entry has `Auto Evidence` and `Manual Evidence` integer columns (AUDIT-03 data layer).
- Legacy `/api/compliance/reports/download/{filename}` returns 403 for cross-tenant access and serves owner/super-admin requests (AUDIT-04).
- `_build_report_data` accepts an optional `tenant_id` parameter; `_generate_csv` forwards it; no existing caller breaks.
- All backend files remain under 500 lines; no new pip packages; no Co-Authored-By trailer in the commit.
</success_criteria>

<output>
Create `.planning/phases/03-audit-ready-export/03-01-SUMMARY.md` when done.
</output>
