---
phase: 64-rotate_key-autonomous-remediation-action
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/vault_client.py
  - backend/config.py
  - backend/agent_instructions.py
  - backend/agent_action_handlers.py
  - backend/api/endpoints/autonomous_remediation.py # For tracer
  - backend/tests/integration/test_rotate_key_tracer.py
autonomous: true
requirements: [AUTO-02]

must_haves:
  truths:
    - The backend can securely connect to and interact with HashiCorp Vault.
    - The agent infrastructure recognizes and dispatches the 'rotate_key' instruction.
    - An end-to-end tracer path successfully executes a simulated 'rotate_key' action through the system.
  artifacts:
    - backend/vault_client.py: A module for integrating with HashiCorp Vault.
    - backend/agent_instructions.py: Updated to include ROTATE_KEY instruction.
    - backend/agent_action_handlers.py: Updated handler for ROTATE_KEY.
    - backend/tests/integration/test_rotate_key_tracer.py: Integration test proving the tracer.
  key_links:
    - Agent action handler successfully invokes the Vault client.
    - API trigger correctly initiates the agent instruction dispatch.
---

<objective>
Establish the foundational components for the 'rotate_key' autonomous remediation action. This includes integrating with HashiCorp Vault for secure key management, defining the agent instruction, and creating a minimal end-to-end tracer to validate the core flow.

Purpose: To provide the secure key management capability and the command processing necessary for autonomous key rotation, ensuring architectural integrity with a tracer.
Output: A functional Vault client, a recognized agent instruction, and a verified end-to-end execution path for 'rotate_key'.
</objective>

<execution_context>
@/home/user/enterprise-omni-agent-ai-platform/.claude/gsd-core/workflows/execute-plan.md
@/home/user/enterprise-omni-agent-ai-platform/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v3.4-ROADMAP.md
@.planning/milestones/v3.4-REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate HashiCorp Vault Client and Configuration</name>
  <files>
    backend/vault_client.py
    backend/config.py
    requirements.txt
  </files>
  <action>
    Install the 'hvac' library via pip.
    Create `backend/vault_client.py` with a class `VaultClient` that handles initialization (Vault address, token/AppRole), secret reading, and secret writing. Implement basic methods like `read_secret(path)` and `write_secret(path, data)`.
    Add Vault configuration (e.g., `VAULT_ADDR`, `VAULT_TOKEN`) to `backend/config.py`, making them loadable from environment variables.
    Ensure `VaultClient` handles authentication securely (e.g., using token from environment or AppRole).
  </action>
  <verify>
    <automated>pytest backend/tests/unit/test_vault_client.py</automated>
  </verify>
  <done>
    `VaultClient` is implemented, can be initialized with configured credentials, and unit tests confirm it can mock basic read/write operations without actual Vault connection.
  </done>
</task>

<task type="auto">
  <name>Task 2: Define 'rotate_key' Agent Instruction and Handler</name>
  <files>
    backend/agent_instructions.py
    backend/agent_action_handlers.py
  </files>
  <action>
    In `backend/agent_instructions.py`, add a new enum member `ROTATE_KEY` to the appropriate instruction enum (e.g., `AgentInstructionType`).
    In `backend/agent_action_handlers.py`, create a new function `handle_rotate_key(instruction: AgentInstruction)` that accepts the instruction. For now, this handler should log the instruction details and return a success status, simulating interaction with a key rotation mechanism. It should NOT yet interact with the Vault client directly, only prepare for it.
    Update the main agent dispatch logic (if exists) to map `ROTATE_KEY` to `handle_rotate_key`.
  </action>
  <verify>
    <automated>pytest backend/tests/unit/test_agent_instructions.py</automated>
  </verify>
  <done>
    The `ROTATE_KEY` instruction is defined, and a dedicated handler `handle_rotate_key` is registered and can be invoked with the correct instruction type.
  </done>
</task>

<task type="tracer">
  <name>Task 3: End-to-End Tracer for 'rotate_key' Action</name>
  <files>
    backend/api/endpoints/autonomous_remediation.py
    backend/services/autonomous_remediation_service.py
    backend/tests/integration/test_rotate_key_tracer.py
  </files>
  <action>
    Create a new (or extend existing) API endpoint in `backend/api/endpoints/autonomous_remediation.py` (e.g., `POST /api/v1/autonomous-remediation/rotate-key-tracer`) that accepts minimal parameters (e.g., `key_id`). This endpoint will call a service layer.
    In `backend/services/autonomous_remediation_service.py`, implement a `trigger_rotate_key_tracer(key_id: str)` method. This method will construct a `ROTATE_KEY` agent instruction and dispatch it through the existing agent communication mechanism (simulated if necessary, or via the real queue/dispatch).
    Modify `handle_rotate_key` in `backend/agent_action_handlers.py` to log a specific tracer message when invoked through this path.
    Create an integration test `backend/tests/integration/test_rotate_key_tracer.py` that calls the API endpoint and asserts that the tracer message (from the agent handler) appears in logs or a mock agent output.
  </action>
  <verify>
    <automated>pytest backend/tests/integration/test_rotate_key_tracer.py</automated>
  </verify>
  <done>
    An API endpoint can trigger the `ROTATE_KEY` agent instruction, which is processed by its handler, and the end-to-end flow is validated by an integration test.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API→Backend | Untrusted input (key_id) crosses here for remediation action. |
| Backend→Vault | Credentials (token, AppRole) used to access Vault. |
| Backend→Agent | Commands dispatched to the agent must be authenticated/authorized. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-64-01 | Spoofing | Vault Access | critical | mitigate | Use AppRole authentication with least privilege; Vault client handles token refresh. |
| T-64-02 | Tampering | Agent Instruction | high | mitigate | Agent instructions signed/verified; input validation on key_id. |
| T-64-03 | Information Disclosure | Key Material | critical | mitigate | Vault client uses secure channels (TLS); keys never logged. |
| T-64-04 | Denial of Service | Vault Client | medium | mitigate | Implement retry logic with backoff; circuit breaker for Vault client. |
| T-64-SC | Tampering | npm/pip/cargo installs | high | mitigate | slopcheck + blocking human checkpoint for hvac package. |
</threat_model>

<verification>
All unit and integration tests for Vault client, agent instruction parsing, and the end-to-end tracer pass.
</verification>

<success_criteria>
- `backend/vault_client.py` exists and passes unit tests.
- `ROTATE_KEY` instruction is recognized and dispatched to `handle_rotate_key`.
- `POST /api/v1/autonomous-remediation/rotate-key-tracer` successfully triggers the agent handler and is verified by an integration test.
</success_criteria>

<output>
Create `.planning/phases/64-rotate_key-autonomous-remediation-action/64-01-SUMMARY.md` when done
</output>