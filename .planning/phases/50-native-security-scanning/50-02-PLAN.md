---
phase: 50-native-security-scanning
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - agent-install/omni-agent-rs/src/capabilities/feed_bundle.rs
  - agent-install/omni-agent-rs/src/capabilities/mod.rs
autonomous: true
requirements: [NSCAN-02]
must_haves:
  truths:
    - "The agent fetches the feed bundle from GET /api/agents/security/feed-bundle, sending its current version so an up-to-date agent gets a no-op"
    - "The agent verifies the detached ed25519 signature with an EMBEDDED public key BEFORE loading the bundle; a verification failure never loads the bytes (fail-closed)"
    - "A verified bundle is cached to a local path and opened via rusqlite for lookups (hash_sigs, url_feed, ip_feed, yara_rules)"
    - "Missing/corrupt/unverified bundle degrades gracefully — lookups return an unknown/degraded result and the agent never crashes"
  artifacts:
    - path: "agent-install/omni-agent-rs/src/capabilities/feed_bundle.rs"
      provides: "fetch + ed25519-verify + local sqlite cache + lookup helpers (hash/url/ip/yara) with graceful-degrade"
      contains: "ed25519"
      min_lines: 60
  key_links:
    - from: "agent-install/omni-agent-rs/src/capabilities/mod.rs"
      to: "agent-install/omni-agent-rs/src/capabilities/feed_bundle.rs"
      via: "pub mod feed_bundle;"
      pattern: "feed_bundle"
  prohibitions:
    - "MUST NOT load a bundle whose ed25519 signature fails verification (fail-closed)"
    - "MUST NOT panic/crash when the bundle is absent or corrupt — return a degraded result"
    - "MUST NOT embed the private key — only the ed25519 PUBLIC key is compiled in"
---

<objective>
Deliver the agent-side half of the signed feed foundation: a `feed_bundle` capability module that fetches the bundle (versioned), verifies its detached ed25519 signature against an embedded public key, caches it locally (sqlite), and exposes lookup helpers — all fail-closed and graceful-degrading. 50-03's scan engine queries this.

Purpose: D-03 — NSCAN-02's "no live lookup" means the agent must carry a locally verified feed. This module owns fetch/verify/cache/lookup; the scanner just calls it.
Output: `capabilities/feed_bundle.rs` + module registration.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@.planning/phases/50-native-security-scanning/50-RESEARCH.md
@.planning/phases/50-native-security-scanning/50-CONTEXT.md

# Read-first anchors:
@agent-install/omni-agent-rs/src/capabilities/network_discovery.rs  # module + pub fn + reqwest usage pattern
@agent-install/omni-agent-rs/src/capabilities/mod.rs                 # module registration
@agent-install/omni-agent-rs/src/config.rs                          # api_base_url + agent_token access
</execution_context>

<artifacts>
- NEW `agent-install/omni-agent-rs/src/capabilities/feed_bundle.rs` — fetch/verify/cache/lookup.
- EDIT `agent-install/omni-agent-rs/src/capabilities/mod.rs` — `pub mod feed_bundle;`.
- NOTE: `ed25519-dalek` Cargo dep is ADDED in 50-05 (single Cargo.toml edit for the phase); this plan may compile only once that dep lands — coordinate the build check accordingly (see verify).
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: feed_bundle fetch + ed25519 verify + sqlite cache</name>
  <files>agent-install/omni-agent-rs/src/capabilities/feed_bundle.rs, agent-install/omni-agent-rs/src/capabilities/mod.rs</files>
  <read_first>network_discovery.rs for the reqwest client + config access; config.rs for api_base_url/agent_token.</read_first>
  <action>Create `feed_bundle.rs`: a compiled-in `const FEED_PUBLIC_KEY: [u8; 32]` (placeholder value, replaced with the real 50-01 public key at integration — documented). `pub fn update(cfg) -> Result<UpdateOutcome>`: GET `{api_base}/api/agents/security/feed-bundle?have={local_version}` with bearer auth; on a no-op response, keep the current cache; otherwise read bundle bytes + detached signature, verify with `ed25519_dalek::VerifyingKey::from_bytes(&FEED_PUBLIC_KEY).verify(bytes, sig)` — on failure, log + KEEP the previous good cache and return without writing (fail-closed); on success, atomically write the bytes to the local cache path and record the version. `pub fn open_cache() -> Option<rusqlite::Connection>` opens the cached DB or None. Lookup helpers: `pub fn lookup_hash(sha256) -> Verdict`, `pub fn lookup_url(url) -> Verdict`, `pub fn lookup_ip(ip) -> Verdict` — query the cached DB; when no cache/DB, return `Verdict::unknown_degraded()`. Define a small `Verdict { verdict: String, confidence: f32, source: &'static str }` (source "native"). Register `pub mod feed_bundle;` in mod.rs.</action>
  <verify>
    <automated>cd agent-install/omni-agent-rs && cargo check 2>&1 | tail -15</automated>
  </verify>
  <done>Module compiles (after 50-05 adds ed25519-dalek); verify/fetch/cache/lookup + graceful-degrade present.</done>
</task>

<task type="auto">
  <name>Task 2: unit test — verify-fail is fail-closed, absent-cache degrades</name>
  <files>agent-install/omni-agent-rs/src/capabilities/feed_bundle.rs</files>
  <action>Add `#[cfg(test)] mod tests` covering: a tampered/invalid signature causes `verify`-path to reject (no cache write); `lookup_hash`/`lookup_url`/`lookup_ip` on a missing cache return the degraded unknown verdict without panicking; a hand-built in-memory sqlite with a known EICAR hash row yields Malicious. Use `rusqlite` in-memory for the DB cases.</action>
  <verify>
    <automated>cd agent-install/omni-agent-rs && cargo test feed_bundle 2>&1 | tail -12</automated>
  </verify>
  <done>Fail-closed + graceful-degrade + positive-lookup unit tests pass.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-50-04 | Tampering | loading a forged/modified feed | mitigate | ed25519 verify with embedded public key BEFORE load; fail-closed keeps last good cache |
| T-50-05 | DoS | corrupt bundle crashes agent | mitigate | Graceful-degrade: unknown verdict, agent stays up (no panic on absent/corrupt) |
| T-50-SC | Tampering | Cargo deps | accept | ed25519-dalek added in 50-05 (single Cargo edit) |
</threat_model>

<verification>
- `cargo check` clean (once 50-05 lands ed25519-dalek); `cargo test feed_bundle` green.
- Signature-fail path writes nothing; absent cache returns degraded, no panic.

<sequencing_note>
This plan and 50-05 both touch Cargo/agent build. If executed strictly wave-1-first, `cargo check` here fails until 50-05 adds `ed25519-dalek`. Acceptable options: (a) run the Cargo dep addition (yara-x + ed25519-dalek) as a shared Wave-1 prerequisite, or (b) accept that 50-02's build check is confirmed during 50-05. The executor should add `ed25519-dalek` when it first compiles this module.
</sequencing_note>
</verification>

<success_criteria>
The agent fetches the versioned feed bundle, verifies its ed25519 signature against an embedded public key before loading (fail-closed), caches it in sqlite, exposes hash/url/ip lookups, and degrades gracefully (unknown verdict, no crash) when the bundle is absent or invalid.
</success_criteria>

<output>
Create `.planning/phases/50-native-security-scanning/50-02-SUMMARY.md` when done.
</output>
