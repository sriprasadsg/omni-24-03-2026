---
phase: 50-native-security-scanning
plan: 04
type: execute
wave: 2
depends_on: ["50-01"]
files_modified:
  - backend/agent_security_scan_endpoints.py
  - backend/router_registry.py
  - backend/tests/test_agent_security_scan_ingest.py
autonomous: true
requirements: [NSCAN-03]
must_haves:
  truths:
    - "POST /api/agents/{agent_id}/security/scan-result ingests a native scan verdict, tenant-scoped, and persists it"
    - "A Malicious verdict raises a critical security alert via the existing alert path (cloned from _raise_malware_alert), tagged source: native"
    - "The scan capability is registered/advertised so an operator can trigger scans (fully surfaced in Phase 54)"
    - "The handler reads the request-scoped wrapped get_database(), never db._db, and does not touch the existing VirusTotal fim-events path"
  artifacts:
    - path: "backend/agent_security_scan_endpoints.py"
      provides: "native scan-result ingestion + Malicious alert (source: native)"
      contains: "scan-result"
      min_lines: 45
    - path: "backend/tests/test_agent_security_scan_ingest.py"
      provides: "hermetic tests: verdict persisted, Malicious raises alert, tenant scoping"
      contains: "scan-result"
      min_lines: 50
  key_links:
    - from: "backend/router_registry.py"
      to: "backend/agent_security_scan_endpoints.py"
      via: "_load(app, 'agent_security_scan_endpoints', 'router')"
      pattern: "agent_security_scan_endpoints"
  prohibitions:
    - "MUST NOT modify agent_security_endpoints.py's VirusTotal fim-events path"
    - "MUST NOT access db._db — use wrapped get_database() (cross-tenant leak, T-48-07)"
    - "MUST NOT set source to virustotal — native verdicts are source: native"
---

<objective>
Deliver the NSCAN-03 backend seam: a tenant-scoped `POST /api/agents/{agent_id}/security/scan-result` that ingests native scan verdicts from the agent, persists them, and raises a critical alert on Malicious via the existing alert path — cloning `_raise_malware_alert` but tagged `source: native`. The agent (50-05) POSTs here.

Purpose: NSCAN-03 requires verdicts ingested per-tenant; D-05 reuses the proven alert pattern rather than a parallel one.
Output: scan-result ingestion endpoint + registration + tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@.planning/phases/50-native-security-scanning/50-RESEARCH.md
@.planning/phases/50-native-security-scanning/50-CONTEXT.md

# Read-first anchors:
@backend/agent_security_endpoints.py   # _raise_malware_alert shape + tenant scoping to CLONE (do not modify)
@backend/agent_fleet_geo_endpoints.py  # wrapped get_database() + tenant pattern
@backend/router_registry.py            # _load seam
</execution_context>

<artifacts>
- NEW `backend/agent_security_scan_endpoints.py` — `POST /{agent_id}/security/scan-result` ingestion + alert.
- EDIT `backend/router_registry.py` — register the router.
- NEW `backend/tests/test_agent_security_scan_ingest.py` — hermetic ingestion + alert + tenant tests.
</artifacts>

<tasks>

<task type="tdd">
  <name>Task 1: failing tests for scan-result ingestion + Malicious alert</name>
  <files>backend/tests/test_agent_security_scan_ingest.py</files>
  <read_first>backend/tests/test_agent_fleet_geo.py harness; agent_security_endpoints.py for the alert doc shape.</read_first>
  <action>Hermetic tests: (a) POST a `Clean` verdict → 200, a verdict doc is persisted (mock db insert captured), no alert raised; (b) POST a `Malicious` verdict (with sha256, target, matched) → a critical security alert doc is inserted with `source: "native"` and the target/sha256 in the description; (c) the persisted/alert docs carry the caller's `tenantId` (tenant scoping); (d) the handler uses the wrapped db (assert it does not reach for `db._db`). Mock `get_database()` per the fleet-geo test pattern.</action>
  <verify>
    <automated>backend/venv/bin/python -m pytest backend/tests/test_agent_security_scan_ingest.py -q 2>&1 | tail -12</automated>
  </verify>
  <done>Tests exist and FAIL (endpoint not implemented).</done>
</task>

<task type="auto">
  <name>Task 2: implement scan-result ingestion + native alert + register</name>
  <files>backend/agent_security_scan_endpoints.py, backend/router_registry.py</files>
  <read_first>agent_security_endpoints.py::_raise_malware_alert to clone the alert doc shape (swap the VT fields for native verdict/confidence).</read_first>
  <action>Create `agent_security_scan_endpoints.py`: `router = APIRouter(prefix="/api/agents", tags=["Agent Security Scan"])`. `POST /{agent_id}/security/scan-result` (agent-authenticated via `Depends(verify_agent_key)` — the same dep `agent_tasks_endpoints` uses; the tenant comes from the verified agent key) accepts `{type: file|url|ip|hash, target, verdict, confidence, sha256?, matched?}`. `db = get_database()` (wrapped, never db._db). Resolve tenant from the agent/current_user like the existing agent endpoints. Persist the verdict to a `security_scan_results` collection with tenantId + agentId + received_at. On `verdict == "Malicious"`, insert a critical alert cloning `_raise_malware_alert`'s doc shape but with `source: "native"`, native `verdict`/`confidence` fields (no VT ratio). Return `{status: "ok", alerted: <bool>}`. Register `_load(app, "agent_security_scan_endpoints", "router")`. Do NOT touch `agent_security_endpoints.py`.</action>
  <verify>
    <automated>backend/venv/bin/python -m pytest backend/tests/test_agent_security_scan_ingest.py -q 2>&1 | tail -8</automated>
  </verify>
  <done>Tests pass; endpoint registered; VT fim-events path untouched.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-50-08 | Info Disclosure | cross-tenant verdict/alert leak | mitigate | Wrapped get_database() + tenant scoping (T-48-07 clone) |
| T-50-09 | Spoofing | forged scan verdict from a non-agent | mitigate | Agent-authenticated route (same scheme as existing agent endpoints) |
| T-50-SC | Tampering | package installs | accept | No new pip dep |
</threat_model>

<verification>
- `pytest backend/tests/test_agent_security_scan_ingest.py` green (persist, Malicious→native alert, tenant scoping, no db._db).
- `agent_security_endpoints.py` unchanged (git diff shows no edit).

<sequencing_note>Runs in Wave 2 parallel with 50-03 (no shared files). Depends only on 50-01 for the security prefix/patterns, not on the agent engine.</sequencing_note>
</verification>

<success_criteria>
`POST /api/agents/{agent_id}/security/scan-result` ingests native verdicts tenant-scoped, persists them, and raises a critical `source: native` alert on Malicious via the existing alert path — without touching the VirusTotal fim-events path or `db._db`.
</success_criteria>

<output>
Create `.planning/phases/50-native-security-scanning/50-04-SUMMARY.md` when done.
</output>
