---
phase: 50-native-security-scanning
plan: 03
type: execute
wave: 2
depends_on: ["50-01", "50-02"]
files_modified:
  - agent-install/omni-agent-rs/src/capabilities/security_scan.rs
  - agent-install/omni-agent-rs/src/capabilities/mod.rs
autonomous: true
requirements: [NSCAN-01, NSCAN-02]
must_haves:
  truths:
    - "scan_file(path) hashes the file and checks the hash-signature DB, then runs yara-x rules from the bundle, returning verdict Clean/Suspicious/Malicious + confidence, fully offline"
    - "scan_hash/scan_url/scan_ip return a reputation verdict from the locally cached signed feed — no network at scan time"
    - "A hash hit yields Malicious high-confidence; a YARA match yields Suspicious/Malicious per rule severity; no match yields Clean"
    - "The EICAR test file is detected as Malicious; a benign file is Clean; all paths degrade gracefully when the feed is absent"
  artifacts:
    - path: "agent-install/omni-agent-rs/src/capabilities/security_scan.rs"
      provides: "scan_file (yara-x + hash-sig DB) + scan_hash/scan_url/scan_ip (feed lookup) returning verdict JSON"
      contains: "scan_file"
      min_lines: 70
  key_links:
    - from: "agent-install/omni-agent-rs/src/capabilities/security_scan.rs"
      to: "agent-install/omni-agent-rs/src/capabilities/feed_bundle.rs"
      via: "feed_bundle::lookup_hash/lookup_url/lookup_ip + yara rules from the cached bundle"
      pattern: "feed_bundle"
  prohibitions:
    - "MUST NOT make any network call at scan time (offline only)"
    - "MUST NOT link libclamav/libyara — yara-x pure Rust only"
    - "MUST NOT panic on a missing feed/unreadable file — return a degraded/error verdict JSON"
---

<objective>
Deliver the NSCAN-01/02 engine: a `security_scan` capability with `scan_file` (yara-x rule matching + hash-signature DB lookup) and `scan_url`/`scan_ip`/`scan_hash` (bundled-feed reputation), all offline, returning verdict JSON. Reads everything through 50-02's `feed_bundle`.

Purpose: the core native scan capability replacing VirusTotal. D-01 (yara-x + hash DB) + D-02 (bundled feeds).
Output: `capabilities/security_scan.rs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@.planning/phases/50-native-security-scanning/50-RESEARCH.md
@.planning/phases/50-native-security-scanning/50-CONTEXT.md

# Read-first anchors:
@agent-install/omni-agent-rs/src/capabilities/feed_bundle.rs   # lookup helpers + cached bundle (yara rules table)
@agent-install/omni-agent-rs/src/capabilities/network_discovery.rs  # pub fn -> serde_json::Value return shape
</execution_context>

<artifacts>
- NEW `agent-install/omni-agent-rs/src/capabilities/security_scan.rs` — the scan engine.
- EDIT `agent-install/omni-agent-rs/src/capabilities/mod.rs` — `pub mod security_scan;`.
</artifacts>

<tasks>

<task type="auto">
  <name>Task 0: yara-x Windows cross-compile gate</name>
  <files>agent-install/omni-agent-rs/src/capabilities/security_scan.rs</files>
  <action>BEFORE building the engine on yara-x, confirm the crate cross-compiles to Windows. With `yara-x` added to Cargo (default-features = false), run `cargo check --target x86_64-pc-windows-gnu`. If it fails due to a C-accelerator feature, disable features until it's pure-Rust. If yara-x cannot be made to cross-build, take the documented fallback: implement NSCAN-01's signature half via the hash-sig DB + `aho-corasick`/`regex` byte-pattern matching and record a follow-up for YARA-rule support (do NOT silently drop it). Record the outcome in the summary.</action>
  <verify>
    <automated>cd agent-install/omni-agent-rs && cargo check 2>&1 | tail -5 && cargo check --target x86_64-pc-windows-gnu 2>&1 | tail -8</automated>
  </verify>
  <done>Both linux + windows-gnu cargo check pass with the chosen engine config (or the documented fallback is in place).</done>
</task>

<task type="auto">
  <name>Task 1: scan_file (hash-sig + yara-x) + scan_hash/url/ip (feed lookup)</name>
  <files>agent-install/omni-agent-rs/src/capabilities/security_scan.rs, agent-install/omni-agent-rs/src/capabilities/mod.rs</files>
  <read_first>feed_bundle.rs for lookup helpers + how yara rules are stored in the cached DB.</read_first>
  <action>Implement `security_scan.rs` returning `serde_json::Value` from each fn (mirrors network_discovery). `pub fn scan_file(path: &str) -> Value`: read the file (bounded — cap max scanned size, e.g. 64 MB, larger → Suspicious "too large to fully scan" or streamed hash-only), compute SHA256 (sha2), `feed_bundle::lookup_hash(sha256)` → if Malicious return early (confidence ~0.95); else compile the bundle's yara_rules via `yara_x::compile` (cache compiled rules) and scan the bytes — map matched-rule severity to Suspicious/Malicious + confidence; no match → Clean (1.0). Return `{verdict, confidence, matched: [...], sha256, engine: "native"}`. `pub fn scan_hash(h) / scan_url(u) / scan_ip(ip) -> Value`: delegate to the feed_bundle lookups → `{verdict, confidence, source: "native", target}`. All paths: missing feed → degraded `{verdict: "unknown", degraded: true}`; unreadable file → `{verdict: "error", error: ...}`. No network anywhere. Register `pub mod security_scan;`.</action>
  <verify>
    <automated>cd agent-install/omni-agent-rs && cargo check 2>&1 | tail -8</automated>
  </verify>
  <done>Engine compiles; scan_file + scan_hash/url/ip implemented, offline, graceful.</done>
</task>

<task type="auto">
  <name>Task 2: unit tests — EICAR malicious, benign clean, degraded</name>
  <files>agent-install/omni-agent-rs/src/capabilities/security_scan.rs</files>
  <action>Add `#[cfg(test)] mod tests`: write the EICAR test string to a temp file with its known SHA256 present in an in-memory feed cache → `scan_file` returns Malicious; a benign temp file with no hash/rule match → Clean; with no feed cache → degraded unknown (no panic); a matching yara rule (compile a trivial rule matching a byte string) → Suspicious/Malicious per severity. Reuse feed_bundle test helpers where possible.</action>
  <verify>
    <automated>cd agent-install/omni-agent-rs && cargo test security_scan 2>&1 | tail -12</automated>
  </verify>
  <done>EICAR→Malicious, benign→Clean, no-feed→degraded, yara-match→per-severity all pass.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-50-06 | DoS | huge/hostile file exhausts memory | mitigate | Bounded scan size; oversized → hash-only + Suspicious, never OOM |
| T-50-07 | Info Disclosure | scan-time network leak | mitigate | No network in this module; feed is local only |
| T-50-SC | Tampering | yara-x C accelerator pulled in | mitigate | default-features=false; Task 0 windows cross-check gate |
</threat_model>

<verification>
- `cargo check` (linux + x86_64-pc-windows-gnu) clean; `cargo test security_scan` green.
- EICAR detected Malicious offline; benign Clean; no-feed degraded (no panic); no network calls.
</verification>

<success_criteria>
`security_scan` scans files offline (yara-x rules + hash-sig DB) and resolves URL/IP/hash reputation from the bundled feed, returning Clean/Suspicious/Malicious + confidence, with EICAR detected, benign clean, and graceful degradation when the feed is absent — no network, no C libs.
</success_criteria>

<output>
Create `.planning/phases/50-native-security-scanning/50-03-SUMMARY.md` when done.
</output>
