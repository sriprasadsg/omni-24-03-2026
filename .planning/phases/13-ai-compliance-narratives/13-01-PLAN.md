---
phase: 13-ai-compliance-narratives
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/compliance_narrative_service.py
  - backend/tests/test_compliance_narrative_service.py
  - backend/scheduled_reports_service.py
autonomous: true
requirements:
  - AI-05
  - AI-06
must_haves:
  truths:
    - "Scheduled compliance PDF reports contain an AI-generated executive summary of ≤ 150 words before the metrics table (AI-05)"
    - "Each framework section in the PDF contains an AI-generated findings narrative of ≤ 200 words naming the top failing controls (AI-06)"
    - "When generate_text returns BLOCKED: or Error: prefix, the static fallback template is returned and PDF delivery is never blocked (AI-06)"
    - "All MongoDB-sourced strings are sanitised through _sanitise() before prompt embedding"
    - "NarrativeOutput Pydantic model validates every generate_text response before PDF injection"
    - "scheduled_reports_service.py stays at or under 500 lines after wiring"
    - "All 8 unit tests in test_compliance_narrative_service.py pass green"
  artifacts:
    - path: "backend/compliance_narrative_service.py"
      provides: "_sanitise, _trim_to_words, NarrativeOutput, generate_executive_summary, generate_framework_narrative, enrich_report_data"
      exports: ["generate_executive_summary", "generate_framework_narrative", "enrich_report_data", "NarrativeOutput"]
    - path: "backend/tests/test_compliance_narrative_service.py"
      provides: "8-test TDD suite"
      contains: "TestGenerateExecutiveSummary"
    - path: "backend/scheduled_reports_service.py"
      provides: "narrative wiring in _generate_report and _build_pdf"
      contains: "enrich_report_data"
  key_links:
    - from: "backend/scheduled_reports_service.py:_generate_report"
      to: "backend/compliance_narrative_service.py:enrich_report_data"
      via: "await enrich_report_data(data, db, tenant_id) inside compliance_summary branch"
      pattern: "enrich_report_data"
    - from: "backend/scheduled_reports_service.py:_build_pdf"
      to: "report_data dict"
      via: "report_data.get(\"ai_executive_summary\", \"\") and report_data.get(\"ai_framework_narratives\", {})"
      pattern: "ai_executive_summary"
    - from: "backend/compliance_narrative_service.py:generate_executive_summary"
      to: "backend/ai_service.py:IncidentAnalyzer.generate_text"
      via: "await ai_service.generate_text(prompt, source=\"compliance_narrative\")"
      pattern: "ai_service.generate_text"
---

<objective>
Create compliance_narrative_service.py (thin prompt builder with _sanitise, _trim_to_words, NarrativeOutput model, generate_executive_summary, generate_framework_narrative, enrich_report_data), write its 8-test TDD suite first (RED), implement the service (GREEN), wire enrich_report_data into _generate_report and narrative rendering into _build_pdf in scheduled_reports_service.py.

Purpose: Delivers AI-05 (executive summary ≤ 150 words in PDF before metrics table) and AI-06 (per-framework findings narrative ≤ 200 words, fallback on failure). All AI logic lives in compliance_narrative_service.py; scheduled_reports_service.py receives only 2-3 lines of integration code to stay under the 500-line CLAUDE.md limit.

Output:
- backend/compliance_narrative_service.py (all narrative AI logic, under 500 lines)
- backend/tests/test_compliance_narrative_service.py (8 tests, all passing)
- backend/scheduled_reports_service.py (modified: import + enrich_report_data call in _generate_report + narrative rendering in _build_pdf, stays ≤ 500 lines)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-ai-compliance-narratives/13-RESEARCH.md
@.planning/phases/13-ai-compliance-narratives/13-AI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Write failing test suite for compliance_narrative_service</name>
  <files>backend/tests/test_compliance_narrative_service.py</files>

  <read_first>
    - backend/tests/test_agentic_ai.py — asyncio.get_event_loop().run_until_complete() and import-inside-test pattern
    - backend/tests/test_scheduled_reports.py — _col() helper, AsyncMock/MagicMock patterns, sys.path.insert header
    - .planning/phases/13-ai-compliance-narratives/13-RESEARCH.md — Test Pattern (Pattern 3) and Pitfall 5 (import inside test method)
    - .planning/phases/13-ai-compliance-narratives/13-AI-SPEC.md — Section 4b.1 NarrativeOutput model structure, D6 word budget enforcement
  </read_first>

  <behavior>
    - Test 1 (TestGenerateExecutiveSummary.test_returns_str_on_success): mock ai_service.generate_text with AsyncMock returning "SOC 2 compliance is good." — assert result is str and len(result.split()) <= 150
    - Test 2 (TestWordBudget.test_trims_to_150_words): mock generate_text returning a string of 200 words — assert returned value has len(text.split()) <= 150
    - Test 3 (TestFallbackOnError.test_returns_fallback_on_error_prefix): mock generate_text returning "Error: timeout" — assert result is str and "compliance" in result.lower() (fallback text) and no exception raised
    - Test 4 (TestFallbackOnBlocked.test_returns_fallback_on_blocked_prefix): mock generate_text returning "BLOCKED: PII detected" — assert result is str and no exception raised
    - Test 5 (TestSanitise.test_strips_unsafe_chars): import _sanitise from compliance_narrative_service — assert _sanitise("foo<bar>{baz}[qux]\\x") == "foobarbazquxx"
    - Test 6 (TestNarrativeOutput.test_from_raw_raises_on_empty): import NarrativeOutput from compliance_narrative_service — assert NarrativeOutput.from_raw("") raises ValidationError
    - Test 7 (TestFrameworkNarrative.test_framework_narrative_within_200_words): mock generate_text returning a 250-word string — assert len(result.split()) <= 200
    - Test 8 (TestBuildPdfIntegration.test_narrative_in_pdf_not_in_metrics_table): call _build_pdf with report_data containing ai_executive_summary="Test summary." and ai_framework_narratives={} — assert _build_pdf returns bytes and "Test summary." text is present (check by re-reading PDF text or by inspecting story construction via mock); also assert "ai_executive_summary" does NOT appear verbatim as a metrics row key in the returned bytes
  </behavior>

  <action>
    Create backend/tests/test_compliance_narrative_service.py with this exact structure:

    Header block (lines 1-13):
    ```
    """
    Unit tests for Phase 13 AI Compliance Narrative Service.
    Tests: compliance_narrative_service.py
    Pattern: asyncio.get_event_loop().run_until_complete() + AsyncMock (NO pytest-asyncio).
    All 8 tests FAIL (ImportError) before compliance_narrative_service.py exists (RED phase).
    """
    import sys, os
    sys.path.insert(0, os.path.join(os.path.dirname(__file__), ".."))
    import asyncio
    import pytest
    from unittest.mock import AsyncMock, MagicMock, patch
    from pydantic import ValidationError
    ```

    Each test class imports from compliance_narrative_service INSIDE the test method body (not at module level) — this mirrors the Phase 12 pattern from test_agentic_ai.py and ensures that ImportError is the RED failure, not a module-level cascade.

    Test 1 — TestGenerateExecutiveSummary.test_returns_str_on_success:
    Patch "compliance_narrative_service.ai_service" with a MagicMock whose generate_text attribute is AsyncMock(return_value="The SOC 2 compliance posture is sound.").
    Import generate_executive_summary inside the async def run().
    Call with framework_name="SOC 2", score=85.0, failing_controls=["CC6.1"], period="2026-06-01 to 2026-06-23".
    Assert isinstance(result, str) and len(result.split()) <= 150.

    Test 2 — TestWordBudget.test_trims_to_150_words:
    mock generate_text returns " ".join(["word"] * 200).
    Assert len(result.split()) <= 150.

    Test 3 — TestFallbackOnError.test_returns_fallback_on_error_prefix:
    mock generate_text returns "Error: API timeout".
    Assert isinstance(result, str) and result != "Error: API timeout" (fallback returned, not raw error).
    Assert no exception is raised (wrap in try/except AssertionError pass, let pytest catch via return value check).

    Test 4 — TestFallbackOnBlocked.test_returns_fallback_on_blocked_prefix:
    mock generate_text returns "BLOCKED: PII detected in output".
    Assert isinstance(result, str) and not result.startswith("BLOCKED:").

    Test 5 — TestSanitise.test_strips_unsafe_chars:
    Import _sanitise from compliance_narrative_service (no async needed — sync function).
    Call _sanitise("foo<bar>{baz}[qux]\\evil").
    Assert "<" not in result and ">" not in result and "{" not in result and "}" not in result and "[" not in result and "]" not in result and "\\" not in result.

    Test 6 — TestNarrativeOutput.test_from_raw_raises_on_empty:
    Import NarrativeOutput from compliance_narrative_service.
    Assert pytest.raises(ValidationError): NarrativeOutput.from_raw("").

    Test 7 — TestFrameworkNarrative.test_framework_narrative_within_200_words:
    Import generate_framework_narrative inside async def run().
    Patch compliance_narrative_service.ai_service with generate_text returning " ".join(["word"] * 250).
    Call generate_framework_narrative(framework_name="ISO 27001", score=72.0, failing_controls=["A.9.4.1", "A.12.6.1"], remediation_summary=None).
    Assert len(result.split()) <= 200.

    Test 8 — TestBuildPdfIntegration.test_narrative_not_in_metrics_table:
    Import _build_pdf from scheduled_reports_service.
    Call with report_data = {"report_type": "compliance_summary", "report_name": "Test", "generated_at": "2026-06-23T00:00:00Z", "tenant_id": "t1", "period_start": "2026-06-16T00:00:00Z", "period_end": "2026-06-23T00:00:00Z", "ai_executive_summary": "Exec summary text.", "ai_framework_narratives": {"SOC 2": "Framework narrative text."}, "top_failing_controls": ["CC6.1"]}.
    Assert pdf_bytes is not None and isinstance(pdf_bytes, bytes).
    Assert b"Ai Executive Summary" not in pdf_bytes (key not leaked into metrics table row).
    Assert b"Exec summary text." in pdf_bytes (narrative IS in PDF but as narrative section, not table row).

    After writing the file, run:
      cd /home/user/enterprise-omni-agent-ai-platform/backend && python -m pytest tests/test_compliance_narrative_service.py -v 2>&1 | head -40
    Confirm ALL 8 tests fail with ImportError (RED state confirmed). Commit: test(13-01): add failing tests for compliance_narrative_service
  </action>

  <verify>
    <automated>cd /home/user/enterprise-omni-agent-ai-platform/backend && python -m pytest tests/test_compliance_narrative_service.py -v 2>&1 | grep -E "ERROR|FAILED|ImportError" | wc -l</automated>
  </verify>

  <done>All 8 tests collected and all 8 fail with ImportError (compliance_narrative_service.py does not exist yet). RED phase confirmed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Implement compliance_narrative_service.py</name>
  <files>backend/compliance_narrative_service.py</files>

  <read_first>
    - backend/tests/test_compliance_narrative_service.py — the 8 tests this implementation must make pass
    - .planning/phases/13-ai-compliance-narratives/13-AI-SPEC.md — Section 3 (core imports, _sanitise, _trim_to_words, NarrativeOutput), Section 4 (generate_executive_summary, generate_framework_narrative, _validated_narrative), Section 4b.1 (NarrativeOutput field_validator syntax)
    - .planning/phases/13-ai-compliance-narratives/13-RESEARCH.md — Code Examples (complete NarrativeOutput class, generate_executive_summary body), Pitfall 3 (catch ValidationError, return fallback — never re-raise), Pattern 1 (from ai_service import ai_service)
    - backend/ai_service.py lines 205-230 — generate_text(prompt, source, _retries, provider) signature; confirm singleton at line 500
    - backend/agentic_service.py lines 1-35 — Pydantic v2 field_validator import pattern
  </read_first>

  <behavior>
    - _sanitise(value, max_len=200): strips chars matching r"[<>{}\[\]\\]" using compiled re.compile pattern _UNSAFE, then strips whitespace, truncates to max_len; returns str
    - _trim_to_words(text, limit): splits on whitespace, joins first limit words; returns text unchanged if already within limit
    - NarrativeOutput(BaseModel): fields text: str and word_count: int; field_validator "text" raises ValueError if stripped text is empty or starts with "BLOCKED:" or "Error:"; field_validator "word_count" raises ValueError if v > 250; classmethod from_raw(raw: str) computes words = raw.split() and returns cls(text=raw.strip(), word_count=len(words))
    - _validated_narrative(raw, fallback): calls NarrativeOutput.from_raw(raw), catches ValidationError, logs WARNING, returns fallback; on success returns output.text
    - generate_executive_summary(framework_name, score, failing_controls, period): async; sanitises framework_name (max 200) and each control name (max 80, first 5 only); constructs fallback string; builds two-part prompt with system instruction paragraph then XML-tagged compliance_data block; calls await ai_service.generate_text(full_prompt, source="compliance_narrative"); if result starts with "BLOCKED:" or "Error:", logs WARNING and returns fallback; else calls _trim_to_words(result.strip(), 150) then _validated_narrative(trimmed, fallback)
    - generate_framework_narrative(framework_name, score, failing_controls, remediation_summary): async; sanitises framework_name (max 200), each control (max 80, first 7), remediation_summary (max 300); builds prompt with source="compliance_narrative_framework"; fallback is f"The {safe_fw} framework scored {score:.1f}%. Detailed findings are available in the attached control table."; trims to 200 words
    - enrich_report_data(data, db, tenant_id): async; called from _generate_report inside the compliance_summary branch; queries top failing controls via joining db._db.compliance_frameworks controls array with db.asset_compliance (Non-Compliant/Fail/Failed/Not Implemented statuses); stores up to 7 failing control names sorted by severity (Critical first) in data["top_failing_controls"]; generates executive summary using first framework in data["frameworks"] (or "N/A"/0.0 if empty); generates per-framework narrative for each framework in data["frameworks"]; stores results in data["ai_executive_summary"] and data["ai_framework_narratives"] = {fw_name: narrative_str}; never raises — all exceptions are caught, logged at WARNING, and fallback strings are used
  </behavior>

  <action>
    Create backend/compliance_narrative_service.py. The file MUST stay under 500 lines.

    Module structure:
    1. Module docstring (3 lines max)
    2. Imports: import re, import logging; from pydantic import BaseModel, field_validator, ValidationError; from ai_service import ai_service
    3. logger = logging.getLogger(__name__)
    4. _UNSAFE = re.compile(r"[<>{}\[\]\\]")
    5. def _sanitise(value: str, max_len: int = 200) -> str
    6. def _trim_to_words(text: str, limit: int) -> str
    7. class NarrativeOutput(BaseModel) with text: str, word_count: int, two field_validators, and from_raw classmethod
    8. def _validated_narrative(raw: str, fallback: str) -> str
    9. def _fallback_executive_summary(framework: str, score: float) -> str — returns: f"This report presents the {framework} compliance posture for the reporting period. The overall compliance score is {score:.1f}%. Detailed findings are available in the per-framework section below. Please review the failing controls and engage your compliance team for remediation guidance."
    10. async def generate_executive_summary(framework_name, score, failing_controls, period) -> str
    11. async def generate_framework_narrative(framework_name, score, failing_controls, remediation_summary) -> str
    12. async def enrich_report_data(data: dict, db, tenant_id: str) -> None

    For enrich_report_data — failing controls query:
    The compliance_score_endpoints.py pattern shows that compliance_frameworks is a global collection accessed via db._db.compliance_frameworks, and asset_compliance is TenantIsolated via db.asset_compliance. The failing controls derivation steps:
    a. frameworks = [fw async for fw in db._db.compliance_frameworks.find({})] (same as score endpoint line 82)
    b. Build control_id_to_name: dict mapping control id to control name by iterating fw["controls"] for each fw
    c. Build control_id_to_severity: same loop, using _CATEGORY_SEVERITY from compliance_score_endpoints pattern — define the same mapping inline: {"Access Control": "Critical", "Cryptography": "Critical", "Incident Response": "Critical", "Audit": "High", "Configuration": "High", "Vulnerability": "High", "Operations": "Medium", "Risk Management": "Medium"} with default "Low"
    d. _SEVERITY_ORDER = {"Critical": 0, "High": 1, "Medium": 2, "Low": 3}
    e. failing_docs = await db.asset_compliance.find({"controlId": {"$in": list(control_id_to_name.keys())}, "status": {"$in": ["Non-Compliant", "Fail", "Failed", "Not Implemented", "fail", "failed"]}}).to_list(length=50)
    f. For each doc: get controlId, look up name and severity; collect as (severity_order, name) tuples; sort ascending by severity_order; take first 7 names; store in data["top_failing_controls"]
    g. If any exception occurs in steps a-f, log WARNING, set data["top_failing_controls"] = []

    All generate_text calls and NarrativeOutput validation are wrapped in try/except Exception inside enrich_report_data to guarantee delivery is never blocked.

    System instruction for generate_executive_summary prompt (static, never contains DB data):
    "You are a compliance analyst writing an executive summary for a PDF report. Write in clear, non-technical prose suitable for a C-level audience. Do not invent control names, scores, or findings not present in the data below. Respond with the summary text only — no preamble, no markdown, no bullet points."

    User data block for generate_executive_summary:
    f"<compliance_data>\nFramework: {safe_fw}\nCompliance score: {score:.1f}%\nReporting period: {period}\nTop failing controls:\n{controls_block}\n</compliance_data>\n\nWrite an executive summary of no more than 150 words."

    System instruction for generate_framework_narrative:
    "You are a compliance analyst writing a findings narrative for a PDF report. Write factual, concise prose. Do not reference controls not listed below. Respond with narrative text only — no headings, no markdown."

    User data block for generate_framework_narrative:
    f"<compliance_data>\nFramework: {safe_fw}\nScore: {score:.1f}%\nFailing controls:\n{ctrl_block}\nRemediation notes: {safe_rem}\n</compliance_data>\n\nWrite a findings narrative of no more than 200 words."

    After writing the file, run:
      wc -l /home/user/enterprise-omni-agent-ai-platform/backend/compliance_narrative_service.py
    Assert line count <= 500.

    Run tests to confirm GREEN:
      cd /home/user/enterprise-omni-agent-ai-platform/backend && python -m pytest tests/test_compliance_narrative_service.py -v
    All 8 must pass.

    Commit: feat(13-01): implement compliance_narrative_service with generate_text wiring and fallback
  </action>

  <verify>
    <automated>cd /home/user/enterprise-omni-agent-ai-platform/backend && python -m pytest tests/test_compliance_narrative_service.py -v && echo "LINE_COUNT=$(wc -l < compliance_narrative_service.py)"</automated>
  </verify>

  <done>All 8 tests pass. compliance_narrative_service.py exists and is under 500 lines. GREEN phase confirmed.</done>
</task>

<task type="auto">
  <name>Task 3: Wire narrative into _generate_report and _build_pdf in scheduled_reports_service.py</name>
  <files>backend/scheduled_reports_service.py</files>

  <read_first>
    - backend/scheduled_reports_service.py — read the FULL file first; note current line count (496 lines before edit); identify: (a) the compliance_summary branch in _generate_report starting at line 293; (b) _build_pdf starting at line 314 — the story construction, the skip set at line 338, and the Table append at line 355
    - backend/compliance_narrative_service.py — confirm enrich_report_data signature: async def enrich_report_data(data: dict, db, tenant_id: str) -> None
    - .planning/phases/13-ai-compliance-narratives/13-RESEARCH.md — Pattern 2 (_build_pdf injection point, exact skip set additions), Pitfall 4 (ai_executive_summary must be in skip set)
    - .planning/phases/13-ai-compliance-narratives/13-AI-SPEC.md — Section 4 State Management (import pattern, one await call in _generate_report), two-part _build_pdf injection (narrative before metrics table, framework narratives after)
  </read_first>

  <action>
    Make exactly two scoped edits to backend/scheduled_reports_service.py. Do NOT rewrite the file.

    EDIT 1 — Add import at the top of the file, after the existing imports block (after line 16 "from email_service import email_service"):
    Add this line:
      from compliance_narrative_service import enrich_report_data

    EDIT 2 — Inside _generate_report, add one await call at the END of the compliance_summary branch (after line 295 "data["frameworks"] = [...]):
      await enrich_report_data(data, db, tenant_id)

    EDIT 3 — Inside _build_pdf, make three changes:
    a. EXPAND the skip set on line 338 by adding "ai_executive_summary", "ai_framework_narratives", "top_failing_controls" to it:
       skip = {"report_type", "report_name", "generated_at", "tenant_id", "period_start", "period_end", "ai_executive_summary", "ai_framework_narratives", "top_failing_controls"}

    b. AFTER story.append(Spacer(1, 24)) (line 336) and BEFORE the skip set definition, add the executive summary rendering block:
       ai_summary = report_data.get("ai_executive_summary", "")
       if ai_summary:
           story.append(Paragraph("Executive Summary", styles["Heading2"]))
           story.append(Paragraph(ai_summary, styles["Normal"]))
           story.append(Spacer(1, 12))

    c. AFTER story.append(tbl) (line 355), add the per-framework narratives block:
       ai_fw_narratives = report_data.get("ai_framework_narratives", {})
       for fw_name, narrative in ai_fw_narratives.items():
           if narrative:
               story.append(Spacer(1, 8))
               story.append(Paragraph(f"{fw_name} — Findings", styles["Heading2"]))
               story.append(Paragraph(narrative, styles["Normal"]))

    After all edits, verify line count:
      wc -l /home/user/enterprise-omni-agent-ai-platform/backend/scheduled_reports_service.py
    Must be <= 500. If it exceeds 500 lines, move the per-framework narrative loop into a helper function _render_framework_narratives(story, report_data, styles) inside scheduled_reports_service.py and replace the loop with a single call — this keeps the net addition under the line budget.

    Run the full existing scheduled reports test suite to confirm no regression:
      cd /home/user/enterprise-omni-agent-ai-platform/backend && python -m pytest tests/test_scheduled_reports.py -v

    Run the narrative tests again to confirm Test 8 (TestBuildPdfIntegration) passes:
      cd /home/user/enterprise-omni-agent-ai-platform/backend && python -m pytest tests/test_compliance_narrative_service.py::TestBuildPdfIntegration -v

    Run the full test suite:
      cd /home/user/enterprise-omni-agent-ai-platform/backend && python -m pytest tests/ -v --tb=short 2>&1 | tail -20

    Commit: feat(13-01): wire narrative into _generate_report and _build_pdf
  </action>

  <verify>
    <automated>cd /home/user/enterprise-omni-agent-ai-platform/backend && python -m pytest tests/test_compliance_narrative_service.py tests/test_scheduled_reports.py -v --tb=short && echo "LINE_COUNT=$(wc -l < scheduled_reports_service.py)"</automated>
  </verify>

  <done>
    - All 8 narrative tests pass
    - All existing scheduled_reports tests pass (no regression)
    - scheduled_reports_service.py is <= 500 lines
    - _build_pdf includes "ai_executive_summary" and "ai_framework_narratives" in its skip set
    - _generate_report calls await enrich_report_data(data, db, tenant_id) in the compliance_summary branch
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MongoDB → prompt | DB-sourced strings (control names, framework names) flow into LLM prompt; untrusted user-supplied values |
| LLM output → PDF | generate_text return value is embedded in reportlab PDF; must be validated before use |
| Scheduler → narrative service | Called from background task; no user authentication context; tenant_id isolation is the only isolation boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | compliance_narrative_service._sanitise | mitigate | Strip r"[<>{}\[\]\\]" chars from all DB-sourced strings before prompt embedding; max_len cap per field (200 chars for names, 80 for control names, 300 for remediation notes) |
| T-13-02 | Tampering | generate_executive_summary prompt | mitigate | System instruction block is static and never contains DB data; only the XML-tagged compliance_data block contains sanitised DB values; _UNSAFE re pattern compiled at module level |
| T-13-03 | Information Disclosure | generate_text output → PDF | mitigate | Existing guardrail_service.scan_and_log on output inside ai_service.generate_text catches PII; "BLOCKED:" prefix triggers static fallback before PDF injection |
| T-13-04 | Information Disclosure | Cross-tenant narrative | accept | compliance_narrative_service is stateless; _generate_report passes single tenant_id per call; no shared mutable state between tenants; architectural isolation sufficient |
| T-13-05 | Denial of Service | NarrativeOutput.from_raw on overly large output | mitigate | _trim_to_words(result, 200) applied BEFORE NarrativeOutput.from_raw; within_budget validator catches any remainder; ValidationError caught, fallback returned |
| T-13-06 | Elevation of Privilege | ai_service.generate_text called without auth | accept | Narrative service is called from background scheduler, not user-facing endpoint; tenant_id comes from the already-authenticated schedule document; no new auth surface |
| T-13-SC | Tampering | npm/pip installs | accept | No new packages installed in Phase 13; all dependencies pre-existing and verified in 13-RESEARCH.md Package Legitimacy Audit |
</threat_model>

<verification>
## Phase Gate Checks

1. All 8 tests in test_compliance_narrative_service.py pass:
   `cd /home/user/enterprise-omni-agent-ai-platform/backend && python -m pytest tests/test_compliance_narrative_service.py -v`

2. All existing scheduled reports tests continue to pass (no regression):
   `cd /home/user/enterprise-omni-agent-ai-platform/backend && python -m pytest tests/test_scheduled_reports.py -v`

3. Full test suite passes:
   `cd /home/user/enterprise-omni-agent-ai-platform/backend && python -m pytest tests/ -v --tb=short 2>&1 | tail -30`

4. File size checks:
   `wc -l /home/user/enterprise-omni-agent-ai-platform/backend/compliance_narrative_service.py` — must be < 500
   `wc -l /home/user/enterprise-omni-agent-ai-platform/backend/scheduled_reports_service.py` — must be <= 500

5. scheduled_reports_service.py contains the integration points:
   `grep -c "enrich_report_data" /home/user/enterprise-omni-agent-ai-platform/backend/scheduled_reports_service.py` — must be >= 2 (import + call)
   `grep -v '^#' /home/user/enterprise-omni-agent-ai-platform/backend/scheduled_reports_service.py | grep -c "ai_executive_summary"` — must be >= 2 (skip set + get call)

6. Fallback path works (no exception propagates):
   `python -c "import asyncio; from unittest.mock import AsyncMock, patch; import sys; sys.path.insert(0,'backend'); exec(open('backend/tests/test_compliance_narrative_service.py').read())"`
</verification>

<success_criteria>
- backend/compliance_narrative_service.py exists with all 6 exported symbols: _sanitise, _trim_to_words, NarrativeOutput, generate_executive_summary, generate_framework_narrative, enrich_report_data
- backend/tests/test_compliance_narrative_service.py exists with 8 tests, all passing
- backend/scheduled_reports_service.py has enrich_report_data wired in _generate_report (compliance_summary branch), ai_executive_summary / ai_framework_narratives / top_failing_controls in _build_pdf skip set, and narrative Paragraph blocks rendered before and after metrics table
- Both files are under 500 lines
- All 8 new tests pass; zero regressions in existing test suite
- AI-05: executive summary ≤ 150 words appears in PDF before metrics table
- AI-06: per-framework narratives ≤ 200 words appear after metrics table; BLOCKED:/Error: returns fallback, never blocks delivery
</success_criteria>

## Artifacts This Phase Produces

### New Files

| File | Key Symbols |
|------|------------|
| backend/compliance_narrative_service.py | `_UNSAFE: re.Pattern`, `_CATEGORY_SEVERITY: dict`, `_SEVERITY_ORDER: dict`, `_sanitise(value: str, max_len: int = 200) -> str`, `_trim_to_words(text: str, limit: int) -> str`, `NarrativeOutput(BaseModel)` with `text: str`, `word_count: int`, validators `text_not_empty`, `within_budget`, classmethod `from_raw(raw: str) -> NarrativeOutput`, `_validated_narrative(raw: str, fallback: str) -> str`, `_fallback_executive_summary(framework: str, score: float) -> str`, `async generate_executive_summary(framework_name: str, score: float, failing_controls: list[str], period: str) -> str`, `async generate_framework_narrative(framework_name: str, score: float, failing_controls: list[str], remediation_summary: str | None) -> str`, `async enrich_report_data(data: dict, db, tenant_id: str) -> None` |
| backend/tests/test_compliance_narrative_service.py | `TestGenerateExecutiveSummary`, `TestWordBudget`, `TestFallbackOnError`, `TestFallbackOnBlocked`, `TestSanitise`, `TestNarrativeOutput`, `TestFrameworkNarrative`, `TestBuildPdfIntegration` |

### Modified Files

| File | Changes |
|------|---------|
| backend/scheduled_reports_service.py | Import `enrich_report_data` from compliance_narrative_service; `await enrich_report_data(data, db, tenant_id)` at end of compliance_summary branch in `_generate_report`; expand `skip` set with `"ai_executive_summary"`, `"ai_framework_narratives"`, `"top_failing_controls"`; add executive summary `Paragraph` block after `Spacer(1, 24)` in `_build_pdf`; add per-framework narrative `Paragraph` blocks after `story.append(tbl)` in `_build_pdf` |

### generate_text Call Signatures Used

| Function | source label |
|----------|-------------|
| generate_executive_summary | "compliance_narrative" |
| generate_framework_narrative | "compliance_narrative_framework" |

<output>
Create `.planning/phases/13-ai-compliance-narratives/13-01-SUMMARY.md` when done.
</output>
