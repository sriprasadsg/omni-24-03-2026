---
phase: 42-comment-threads-on-compliance-controls
plan: 03
type: execute
wave: 2
depends_on: [42-01]
files_modified:
  - services/apiService.ts
  - components/ControlCommentsPanel.tsx
  - components/FrameworkDetail.tsx
autonomous: false
requirements: [CMT-01]
must_haves:
  truths:
    - "Expanding a control row in FrameworkDetail renders a Comments panel below the Chain of Custody panel"
    - "A reviewer/admin can type and post a comment and see it appear immediately in the thread; non-reviewers see the read-only thread with no composer"
    - "Comment text renders through React's default JSX escaping (no raw-HTML injection), with @tokens visually highlighted"
    - "Comment history is retrievable on re-expand (persists across sessions)"
  artifacts:
    - path: "services/apiService.ts"
      provides: "fetchControlComments + postControlComment wrappers"
      exports: ["fetchControlComments", "postControlComment"]
    - path: "components/ControlCommentsPanel.tsx"
      provides: "fetch-on-mount comment thread + role-gated composer"
      exports: ["ControlCommentsPanel"]
    - path: "components/FrameworkDetail.tsx"
      provides: "ControlCommentsPanel mounted after ChainOfCustodyPanel"
      contains: "ControlCommentsPanel"
  key_links:
    - from: "components/ControlCommentsPanel.tsx"
      to: "services/apiService.ts"
      via: "fetchControlComments / postControlComment"
      pattern: "fetchControlComments|postControlComment"
    - from: "components/FrameworkDetail.tsx"
      to: "components/ControlCommentsPanel.tsx"
      via: "JSX mount in expanded control row"
      pattern: "ControlCommentsPanel"
---

<objective>
Build the frontend comment thread: two thin API wrappers, a `ControlCommentsPanel.tsx` that fetches comments on mount and shows a role-gated composer, and the mount of that panel inside `FrameworkDetail.tsx`'s expanded-control row immediately after the existing `ChainOfCustodyPanel`. All visual/interaction values come from the existing design system (UI-SPEC) — no new tokens.

Purpose: Satisfies Success Criterion 1 (see comment appear in the control's thread in the detail view) and 4 (history retrievable from the detail view). Closes Pitfall 5 (built-but-unmounted) by making the mount edit an explicit task with a live verification gate.
Output: `fetchControlComments`/`postControlComment` in apiService.ts, the `ControlCommentsPanel` component, the FrameworkDetail mount, and a human-verify checkpoint for live render + mention delivery.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-comment-threads-on-compliance-controls/42-RESEARCH.md
@.planning/phases/42-comment-threads-on-compliance-controls/42-PATTERNS.md
@.planning/phases/42-comment-threads-on-compliance-controls/42-UI-SPEC.md
</context>

<artifacts_this_phase_produces>
Symbols introduced by THIS plan:
- `services/apiService.ts`: `fetchControlComments(controlId)`, `postControlComment(controlId, text)`
- `components/ControlCommentsPanel.tsx`: `ControlCommentsPanel` (React.FC, prop `controlId`), local helpers `formatTimestamp`, `renderCommentText`, and the hand-synced `_REVIEWER_ROLES` literal
- `components/FrameworkDetail.tsx`: `<ControlCommentsPanel controlId={control.id} />` mount + import
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add fetch/post API wrappers</name>
  <files>services/apiService.ts</files>
  <read_first>
    - services/apiService.ts (lines 4564-4572 — `fetchControlAuditLog` wrapper shape: authFetch, API_BASE, try/catch, non-throwing GET)
    - .planning/phases/42-comment-threads-on-compliance-controls/42-PATTERNS.md (apiService section, lines 246-284)
  </read_first>
  <action>
    Add `export const fetchControlComments = async (controlId: string): Promise<any[]>` cloning the `fetchControlAuditLog` structure: `authFetch(`${API_BASE}/control-comments?control_id=${controlId}`)`, return `[]` on non-ok or thrown error (non-throwing read). Add `export const postControlComment = async (controlId: string, text: string): Promise<any>` that POSTs JSON `{ control_id: controlId, text }` to `${API_BASE}/control-comments` with `Content-Type: application/json` via `authFetch`, throws `new Error('Failed to post comment')` on non-ok, and returns the parsed comment. Place both near the existing control-related wrappers.
  </action>
  <verify>
    <automated>grep -Eq "export const fetchControlComments" services/apiService.ts && grep -Eq "export const postControlComment" services/apiService.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Both `fetchControlComments` and `postControlComment` are exported.
    - `fetchControlComments` returns `[]` (not throw) on error; `postControlComment` throws on non-ok.
    - POST body serializes `{ control_id, text }`.
  </acceptance_criteria>
  <done>Two API wrappers present, matching the existing authFetch convention.</done>
</task>

<task type="auto">
  <name>Task 2: Build ControlCommentsPanel.tsx (fetch-on-mount thread + role-gated composer)</name>
  <files>components/ControlCommentsPanel.tsx</files>
  <read_first>
    - components/ChainOfCustodyPanel.tsx (full file — structure, `formatTimestamp` lines 14-26, inline SVG spinner markup, header-bar chrome, empty-state style)
    - components/EvidenceReviewPanel.tsx (lines 8-15 role-gate literal + "kept in sync by hand" comment; whole-block `{isReviewer && (...)}` conditional composer; "Submitting..." in-flight label)
    - .planning/phases/42-comment-threads-on-compliance-controls/42-UI-SPEC.md (Copywriting Contract, Color, Typography, Component Notes)
    - .planning/phases/42-comment-threads-on-compliance-controls/42-PATTERNS.md (component section, lines 288-386)
  </read_first>
  <action>
    Create `components/ControlCommentsPanel.tsx` exporting `ControlCommentsPanel: React.FC<{ controlId: string }>`. Import icons from local `./icons` (MessageSquareIcon, SendIcon, ClockIcon, ChevronDownIcon) to match the adjacent CoC panel, and `* as api` from `../services/apiService`. Clone `formatTimestamp` verbatim from ChainOfCustodyPanel. On mount (`useEffect` keyed on `controlId`) auto-fetch `api.fetchControlComments(controlId)` into state with loading/error handling — comments render immediately when the row is expanded (per A3, not a second click). Header bar: icon + `Comments (N)` count. Empty state: single italic centered line `No comments yet on this control.` (`text-gray-400`). Render each comment oldest-first (server already sorts ascending) with author `font-semibold text-xs`, timestamp regular, body `text-xs`. Copy `_REVIEWER_ROLES = ['admin', 'super_admin', 'compliance_reviewer']` with the "kept in sync by hand with backend `_COMMENT_AUTHOR_ROLES`; backend re-enforces" comment; compute `isReviewer` from the current user. Render the composer `<form>` ONLY inside `{isReviewer && (...)}` — no disabled input for non-reviewers. Composer: textarea with placeholder `Add a comment... (use @username to mention someone)`, submit button labelled `Post Comment` / `Posting...` while submitting (use `py-2`, NOT `py-3.5`), calling `api.postControlComment(controlId, text)` then re-fetching or appending, clearing the input, and surfacing errors. Render comment text via a `renderCommentText` helper that splits on `/(@[\w.-]+)/g` and wraps `@token` substrings in `<span className="text-blue-600 dark:text-blue-400 font-medium">`; all text goes through React's default JSX child escaping and MUST NOT use the raw-HTML injection prop.
    <!-- planner-discipline-allow: dangerouslySetInnerHTML -->
    Do not call `dangerouslySetInnerHTML` anywhere in this component. Reuse ChainOfCustodyPanel's inline SVG spinner for the loading state. Keep the file under 500 lines.
  </action>
  <verify>
    <automated>test -f components/ControlCommentsPanel.tsx && grep -q "export const ControlCommentsPanel" components/ControlCommentsPanel.tsx && ! grep -q "dangerouslySetInnerHTML" components/ControlCommentsPanel.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `components/ControlCommentsPanel.tsx` exists and exports `ControlCommentsPanel`.
    - Source assertion: `grep -c 'dangerouslySetInnerHTML' components/ControlCommentsPanel.tsx` returns 0 (XSS-safe rendering).
    - Composer is wrapped in an `isReviewer` conditional (hidden entirely for non-reviewers).
    - `_REVIEWER_ROLES` literal is duplicated with the hand-sync comment; submit button uses `py-2` and toggles `Post Comment`/`Posting...`.
    - Panel fetches on mount (useEffect keyed on controlId), renders `Comments (N)` header and the empty-state line.
  </acceptance_criteria>
  <done>Component renders a fetch-on-mount thread with an XSS-safe, role-gated composer matching the UI-SPEC.</done>
</task>

<task type="auto">
  <name>Task 3: Mount ControlCommentsPanel in FrameworkDetail.tsx</name>
  <files>components/FrameworkDetail.tsx</files>
  <read_first>
    - components/FrameworkDetail.tsx (line 5 existing ChainOfCustodyPanel import; line 432 mount `{canViewCoC && <ChainOfCustodyPanel controlId={control.id} />}` — the exact insertion anchor)
    - .planning/phases/42-comment-threads-on-compliance-controls/42-PATTERNS.md (FrameworkDetail section, lines 390-410)
  </read_first>
  <action>
    Add `import { ControlCommentsPanel } from './ControlCommentsPanel';` alongside the existing ChainOfCustodyPanel import near line 5. Immediately after the existing line `{canViewCoC && <ChainOfCustodyPanel controlId={control.id} />}` (line 432), add an unconditional `<ControlCommentsPanel controlId={control.id} />` — the read side of the thread is visible to all authenticated tenant users (A2); composer visibility is gated inside the panel itself, so no `canViewCoC`-style guard wraps this mount. Change nothing else in the row.
  </action>
  <verify>
    <automated>grep -q "import { ControlCommentsPanel }" components/FrameworkDetail.tsx && grep -q "<ControlCommentsPanel controlId={control.id} />" components/FrameworkDetail.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `components/FrameworkDetail.tsx` imports `ControlCommentsPanel` and renders `<ControlCommentsPanel controlId={control.id} />` immediately after the ChainOfCustodyPanel mount.
    - The new mount is not wrapped in the `canViewCoC` guard.
  </acceptance_criteria>
  <done>Panel is wired into the expanded control row (closes Pitfall 5 built-but-unmounted).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human-verify live render, post, persistence, and @mention delivery</name>
  <action>Pause execution and hand off to the human for the live browser verification steps below. This is a blocking manual gate — do not auto-approve. Resume only after the human confirms all six checks pass or reports issues.</action>
  <what-built>Backend control-comments API (42-01/42-02) plus the frontend ControlCommentsPanel mounted in FrameworkDetail's expanded control row.</what-built>
  <how-to-verify>
    1. Start backend (uvicorn :5000) and frontend (Vite :3000); log in as an admin/compliance_reviewer.
    2. Open a framework, expand any control row — confirm a "Comments" panel appears below the Chain of Custody panel.
    3. Post a comment; confirm it appears immediately in the thread and survives a collapse/re-expand (persistence).
    4. Post a comment containing `@<another-tenant-user's username>`; log in as that user and confirm the bell icon / NotificationsDashboard shows a "You were mentioned" notification.
    5. Log in as a non-reviewer role — confirm the thread is readable but NO composer/input is shown.
    6. Confirm comment text with characters like `<b>` renders as literal text (escaped), not as HTML.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server comment text → browser DOM | Stored comment text rendered in the panel could carry markup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-04 | Tampering | ControlCommentsPanel comment rendering | mitigate | Text rendered via React default JSX escaping; @tokens wrapped via safe split/map; the raw-HTML injection prop is never used (source-grep gated) |
| T-42-01f | Elevation of Privilege | composer visibility | mitigate | Frontend `isReviewer` gate is UX-only; backend `_COMMENT_AUTHOR_ROLES` (42-01) is the authoritative enforcement — a raw POST from a non-reviewer still 403s |
| T-42-SC | Tampering | package installs | accept | No new frontend packages introduced this phase |
</threat_model>

<verification>
- `grep -c 'dangerouslySetInnerHTML' components/ControlCommentsPanel.tsx` == 0.
- `grep -q "<ControlCommentsPanel controlId={control.id} />" components/FrameworkDetail.tsx`.
- Human-verify checkpoint: live render, post, persistence, @mention notification delivery, non-reviewer read-only, escaped-text rendering.
</verification>

<success_criteria>
The comment thread renders in the control detail view, reviewers can post and immediately see comments, history persists across re-expand, @mentions surface in the mentioned user's notifications, non-reviewers see a read-only thread, and comment text renders escaped.
</success_criteria>

<output>
Create `.planning/phases/42-comment-threads-on-compliance-controls/42-03-SUMMARY.md` when done
</output>
