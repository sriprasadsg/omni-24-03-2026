---
phase: 43-remediation-to-ticketing-bridge
plan: 04
type: execute
wave: 3
depends_on: ["43-03"]
files_modified:
  - types.ts
  - services/apiService.ts
  - components/RemediationTaskModal.tsx
autonomous: false
requirements: [REM-01]
must_haves:
  truths:
    - "A compliance admin sees a Create Ticket action on a saved remediation task when ticketing is configured, with a provider picker only when both Jira and ServiceNow are configured (D-02)"
    - "After a ticket exists (ticket_ref truthy) the button/picker are hidden and a read-only block shows the provider badge, ticket reference, and a working outbound link (Success Criterion 2)"
    - "The Ticketing section is hidden entirely for an unsaved task or when neither provider is configured"
    - "A failed ticket creation surfaces an error toast and never blocks the task view (D-04)"
  artifacts:
    - path: "services/apiService.ts"
      provides: "createTicketForRemediationTask + getTicketingConfig client wrappers"
      contains: "createTicketForRemediationTask"
    - path: "components/RemediationTaskModal.tsx"
      provides: "Ticketing section (button, provider picker, ticket-display block)"
      contains: "handleCreateTicket"
    - path: "types.ts"
      provides: "ticket_provider/ticket_ref/ticket_url on RemediationTask"
      contains: "ticket_ref"
  key_links:
    - from: "components/RemediationTaskModal.tsx::handleCreateTicket"
      to: "services/apiService.ts::createTicketForRemediationTask"
      via: "await api.createTicketForRemediationTask(task.id, provider) then onRefresh + showToast"
      pattern: "createTicketForRemediationTask"
    - from: "services/apiService.ts::createTicketForRemediationTask"
      to: "backend POST /api/compliance-remediation/tasks/{id}/create-ticket"
      via: "authFetch POST with {provider}"
      pattern: "tasks/.*create-ticket"
---

<objective>
Add the frontend Ticketing surface to `components/RemediationTaskModal.tsx` (REM-01, Success Criteria 1-2): a "Create Ticket" action with a provider picker (D-02), and a read-only ticket-display block once a ticket exists — plus the `apiService.ts` client wrappers and the `types.ts` field additions they need.

Purpose: The backend (Plans 01-03) creates and links tickets; this plan is where an admin actually triggers a manual ticket and sees the provider/ref/url afterward. Everything reuses the modal's existing async-action+toast pattern (`handleSuggest`/`handleSave`), the existing indigo CTA convention, and the provider-availability data already returned by `GET /api/ticketing/config` — no new backend surface. Auto-create (D-01) has no UI moment; its result appears in the same read-only block.
Output: modified `types.ts`, `services/apiService.ts`, `components/RemediationTaskModal.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-remediation-to-ticketing-bridge/43-CONTEXT.md
@.planning/phases/43-remediation-to-ticketing-bridge/43-UI-SPEC.md
@.planning/phases/43-remediation-to-ticketing-bridge/43-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ticket fields to RemediationTask type + apiService client wrappers</name>
  <files>types.ts, services/apiService.ts</files>
  <read_first>
    - types.ts (RemediationTask interface lines 1685-1704)
    - services/apiService.ts (API_BASE line 35, authFetch line 198, and an existing remediation wrapper such as updateRemediationTask/suggestRemediation for the throw-on-non-2xx shape)
    - .planning/phases/43-remediation-to-ticketing-bridge/43-PATTERNS.md (apiService createTicketForRemediationTask + getTicketingConfig spec)
    - backend/compliance_remediation_endpoints.py (the create-ticket route contract from Plan 03) and backend/ticketing_endpoints.py (GET /api/ticketing/config)
  </read_first>
  <action>
    In `types.ts`, add three optional fields to the `RemediationTask` interface: `ticket_provider?: 'jira' | 'servicenow'`, `ticket_ref?: string`, `ticket_url?: string`.

    In `services/apiService.ts`, add `createTicketForRemediationTask(taskId, provider)` returning `{ ticket_provider, ticket_ref, ticket_url }`: `authFetch` POST to `${API_BASE}/compliance-remediation/tasks/${taskId}/create-ticket` with JSON body `{ provider }`, throw on non-2xx, return parsed JSON (clone the throwing wrapper shape). Add `getTicketingConfig()` that GETs `${API_BASE}/ticketing/config`; if an equivalent wrapper already exists, reuse it — otherwise clone the safe-default (try/catch returns a benign object) shape rather than throwing, since it is a background-derived read used to compute `hasJira`/`hasServiceNow` from `jira_url`/`snow_instance` presence. Type the provider param `'jira' | 'servicenow'`.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `RemediationTask` has `ticket_provider`/`ticket_ref`/`ticket_url` optional fields.
    - `services/apiService.ts` exports `createTicketForRemediationTask` (POST, throws on non-2xx) and `getTicketingConfig` (GET, safe default).
    - `npm run build` (vite build / tsc) succeeds with no type errors.
  </acceptance_criteria>
  <done>Type + client wrappers exist and the project type-checks/builds.</done>
</task>

<task type="auto">
  <name>Task 2: Add the Ticketing section (button, provider picker, ticket-display) to RemediationTaskModal.tsx</name>
  <files>components/RemediationTaskModal.tsx</files>
  <read_first>
    - components/RemediationTaskModal.tsx (imports lines 1-5; state block lines 25-34; handleSuggest lines 68-82; the Save Task button + spinner markup ~lines 260-269; the `disabled={!task?.id}` gating precedent ~line 164)
    - components/AddCloudAccountModal.tsx (provider radio-tile pattern lines 112-125 — structural analog for the picker)
    - components/RemediationDashboard.tsx (STATUS_COLORS pill class string ~line 192 — badge shape to clone)
    - .planning/phases/43-remediation-to-ticketing-bridge/43-UI-SPEC.md (three-state render logic, copy contract, color/spacing rules — p-2 not p-2.5, indigo accent, blue Jira / green ServiceNow badges)
    - components/TicketingIntegration.tsx (Jira=blue / ServiceNow=green color association)
  </read_first>
  <action>
    In `components/RemediationTaskModal.tsx`: extend the lucide-react import with `TicketIcon` and `ExternalLinkIcon`. Add state: `creatingTicket` (bool), a selected `ticketProvider` ('jira'|'servicenow'), and `hasJira`/`hasServiceNow` derived from `getTicketingConfig()` loaded in an effect when the modal opens (best-effort, safe default false/false). Add `handleCreateTicket(provider)` cloning `handleSuggest`'s shape exactly: guard `task?.id`, set `creatingTicket`, `await api.createTicketForRemediationTask(task.id, provider)`, `showToast('Ticket created.', 'success')`, `onRefresh()`, catch -> `showToast('Failed to create ticket — please try again.', 'error')`, finally clear the flag.

    Render a "Ticketing" section inside the existing `space-y-4` field stack (above the button row) with three-state logic in priority order per UI-SPEC: (1) `task?.ticket_ref` truthy -> render only the read-only ticket-display block: a `Ticket` label, a provider badge (clone RemediationDashboard's pill class `px-2 py-0.5 text-xs font-semibold rounded-full`, swapping colors — Jira `bg-blue-100 text-blue-800 dark:bg-blue-900/30 dark:text-blue-400`, ServiceNow `bg-green-100 text-green-800 dark:bg-green-900/30 dark:text-green-400`), the `ticket_ref` text, and an `<a href={task.ticket_url} target="_blank" rel="noopener noreferrer">` reading `View in Jira` / `View in ServiceNow` with an `ExternalLinkIcon size={12}`; (2) `!task?.id` OR neither provider configured -> render nothing (section hidden); (3) otherwise -> if both providers configured, show a `Choose a provider` label + two radio tiles (clone AddCloudAccountModal's radio-tile label pattern but with `p-2` padding and indigo selected-state `border-indigo-600 bg-indigo-50 dark:bg-indigo-900/20`), then a `Create Ticket` button; if exactly one configured, show the `Create Ticket` button directly targeting that provider (no picker). The button clones the Save Task disabled+spinner shape with `bg-indigo-600 hover:bg-indigo-700`, showing `Creating...` + the inline spinner span while `creatingTicket`. Provider values are the strict literals only — never a free-text input.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `handleCreateTicket` calls `api.createTicketForRemediationTask` and shows the exact success/error toast copy from UI-SPEC.
    - Three-state render is implemented: display-block when `ticket_ref` truthy; hidden when unsaved or no provider configured; button (+ picker only when both configured) otherwise.
    - Provider selection uses the `'jira' | 'servicenow'` literals (radio tiles, no free-text), with indigo selected state and `p-2` (not `p-2.5`) padding.
    - Provider badge reuses the existing pill class string with only the color pair swapped (blue Jira / green ServiceNow).
    - `npm run build` succeeds.
  </acceptance_criteria>
  <done>The Ticketing section renders all three states per UI-SPEC, wired to the endpoint via apiService, and the project builds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of the Ticketing UI</name>
  <files>components/RemediationTaskModal.tsx</files>
  <action>Human-only visual/interaction verification of the Create Ticket action, provider picker, ticket-display block, hidden-state rules, and error toast — no code changes; approve or report issues.</action>
  <what-built>The remediation task modal now shows a "Create Ticket" action (with a provider picker when both Jira and ServiceNow are configured) and a read-only ticket-display block (provider badge, ticket reference, outbound link) once a ticket exists; failures raise an error toast without blocking the task.</what-built>
  <how-to-verify>
    1. Start the app (`npm run dev`, backend on :5000 per project runtime notes) and open a saved remediation task in the Remediation dashboard.
    2. With ticketing configured for the tenant (TicketingIntegration settings), confirm the "Create Ticket" action appears; if both providers are configured, confirm the "Choose a provider" picker with Jira/ServiceNow tiles (indigo selected state) shows; if only one is configured, confirm the button creates directly with no picker.
    3. Click Create Ticket — confirm the button shows "Creating..." + spinner, then a "Ticket created." success toast, and the section switches to the read-only block showing the provider badge (blue Jira / green ServiceNow), the ticket reference, and a working "View in Jira/ServiceNow ↗" link opening in a new tab.
    4. Confirm the Ticketing section is hidden entirely for an unsaved (new) task and for a tenant with no ticketing configured.
    5. Confirm a forced failure (e.g. invalid ticketing credentials) surfaces the "Failed to create ticket — please try again." error toast and leaves the task otherwise usable.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<artifacts_this_phase_produces>
Modified `types.ts` — `RemediationTask.ticket_provider` / `ticket_ref` / `ticket_url` (optional).
Modified `services/apiService.ts` — `createTicketForRemediationTask(taskId, provider)`, `getTicketingConfig()`.
Modified `components/RemediationTaskModal.tsx` — `handleCreateTicket`, `creatingTicket`/`ticketProvider`/`hasJira`/`hasServiceNow` state, and the three-state Ticketing section (button, provider radio picker, read-only ticket-display block). New icons: `TicketIcon`, `ExternalLinkIcon`.
No new routes or collections.
</artifacts_this_phase_produces>

<verification>
- `npm run build` succeeds (type-check clean).
- Manual UAT checkpoint approved (no automated frontend test framework for these components).
</verification>

<success_criteria>
An admin can manually create a Jira/ServiceNow ticket from the remediation task view and afterward see the ticket provider, reference, and link (REM-01, Success Criteria 1-2); the section is correctly hidden/shown across the three states, and failures are non-blocking with a toast (D-04).
</success_criteria>

<output>
Create `.planning/phases/43-remediation-to-ticketing-bridge/43-04-SUMMARY.md` when done.
</output>
