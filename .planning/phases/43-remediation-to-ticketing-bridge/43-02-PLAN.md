---
phase: 43-remediation-to-ticketing-bridge
plan: 02
type: execute
wave: 2
depends_on: ["43-01"]
files_modified:
  - backend/compliance_remediation_service.py
  - backend/tests/test_remediation_workflow.py
autonomous: true
requirements: [REM-01]
must_haves:
  truths:
    - "Creating a remediation task at critical, high, or medium priority auto-creates a ticket (D-01, revised) via ticketing_bridge, in the same code path regardless of which caller invoked create_task"
    - "A ticketing failure during auto-create never blocks or fails the task's own creation — the task is still persisted and returned (D-04 best-effort)"
  artifacts:
    - path: "backend/compliance_remediation_service.py"
      provides: "Auto-create-on-critical/high/medium hook inside create_task"
      contains: "ticketing_bridge"
    - path: "backend/tests/test_remediation_workflow.py"
      provides: "autocreate_nonfatal test coverage"
      contains: "autocreate_nonfatal"
  key_links:
    - from: "backend/compliance_remediation_service.py::create_task"
      to: "backend/ticketing_bridge.py::create_ticket_for_remediation_task"
      via: "priority-gated try/except call after insert_one (lazy import, non-fatal)"
      pattern: "create_ticket_for_remediation_task"
---

<objective>
Add the auto-create-on-critical/high/medium hook (D-01, revised 2026-07-21) to `compliance_remediation_service.create_task`, calling `ticketing_bridge.create_ticket_for_remediation_task` wrapped in a non-blocking try/except (D-04), and extend the existing workflow test file with an `autocreate_nonfatal` case.

Purpose: REM-01 requires manual ticket creation (Plan 03) and, per D-01, an automatic trigger for tasks at critical, high, or medium priority (threshold widened 2026-07-21 from high/critical; low still only gets a ticket via the manual button). This hook must fire for any caller of `create_task` (HTTP endpoint, other services, tests) — not just the HTTP layer — which is why it lives in the service, cloning the file's own existing conditional-side-effect precedent (`update_task`'s `dispatch_rescan` call).
Output: modified `create_task`, extended `test_remediation_workflow.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-remediation-to-ticketing-bridge/43-CONTEXT.md
@.planning/phases/43-remediation-to-ticketing-bridge/43-PATTERNS.md
@.planning/phases/43-remediation-to-ticketing-bridge/43-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add priority-gated auto-create hook to create_task</name>
  <files>backend/compliance_remediation_service.py</files>
  <read_first>
    - backend/compliance_remediation_service.py (create_task lines 30-67 — the task dict is built and `insert_one` at line 65; update_task's conditional-side-effect precedent lines 115-118)
    - .planning/phases/43-remediation-to-ticketing-bridge/43-PATTERNS.md (the exact hook shape for create_task and the self-analog conditional pattern)
    - .planning/phases/43-remediation-to-ticketing-bridge/43-CONTEXT.md (D-01 auto-create trigger, D-04 non-blocking failure handling)
    - backend/ticketing_bridge.py (create_ticket_for_remediation_task signature from Plan 01)
  </read_first>
  <action>
    In `backend/compliance_remediation_service.py::create_task`, after `await db.compliance_remediation_tasks.insert_one(task)` and `task.pop("_id", None)` (line 65-66) and before `return task`, add a priority-gated hook per D-01 (revised 2026-07-21 — threshold widened from high/critical to critical/high/medium): when `task.get("priority") in ("critical", "high", "medium")`, inside a `try/except Exception`, lazily `import ticketing_bridge` and `await ticketing_bridge.create_ticket_for_remediation_task(db, task, task.get("tenantId", ""))` (no `provider_override` — auto-create falls back to `config["provider"]` per Pattern 3). Note `low` remains excluded — it still only gets a ticket via the manual button. On exception, `logger.warning("Auto-create ticket failed (non-fatal): %s", exc)` and continue — the task must still be returned (D-04). Do NOT re-raise, and do NOT let the ticket result change the returned task shape. Reference decision D-01 and D-04 in a brief comment.
  </action>
  <verify>
    <automated>cd backend && venv/bin/python -c "import inspect, compliance_remediation_service as s; src=inspect.getsource(s.create_task); assert 'ticketing_bridge' in src and 'critical' in src and 'high' in src and 'medium' in src and 'try' in src"</automated>
  </verify>
  <acceptance_criteria>
    - `create_task` source references `ticketing_bridge.create_ticket_for_remediation_task`, gated on priority in ("critical","high","medium"), inside a try/except that logs a warning and does not re-raise.
    - The hook is placed after `insert_one` so the task is already persisted before the ticket attempt.
    - `create_task` still returns the persisted `task` dict unchanged on both success and ticketing-failure paths.
    - No `provider_override` is passed (auto-create uses the tenant's configured provider).
  </acceptance_criteria>
  <done>create_task auto-creates a ticket for critical/high/medium tasks via ticketing_bridge, non-blocking, without altering its return contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add autocreate_nonfatal test to test_remediation_workflow.py</name>
  <files>backend/tests/test_remediation_workflow.py</files>
  <read_first>
    - backend/tests/test_remediation_workflow.py (existing `_mock_db()` factory lines 29-57 and `test_rem01_create_task_persists` lines 63-104 for the create_task invocation shape)
    - .planning/phases/43-remediation-to-ticketing-bridge/43-VALIDATION.md (autocreate_nonfatal row — `pytest tests/test_remediation_workflow.py -k autocreate_nonfatal -x`)
    - backend/compliance_remediation_service.py (create_task, as modified in Task 1)
  </read_first>
  <behavior>
    - Test `autocreate_nonfatal`: patch `ticketing_bridge.create_ticket_for_remediation_task` to raise; call `create_task` with a priority inside the widened set (D-01 revised — use "critical" or "medium"); assert the returned task is not None, has status "open" and the expected tenantId — proving the ticketing failure did not block task creation (D-04).
    - Secondary assertion: when priority is "low" (the one level OUTSIDE the widened critical/high/medium auto-create set), the auto-create path is NOT invoked (patch asserts not called) — confirming low still only gets a ticket via the manual button.
  </behavior>
  <action>
    Extend `backend/tests/test_remediation_workflow.py` with a test function whose name matches `-k autocreate_nonfatal`. Reuse the existing `_mock_db()`. Use `unittest.mock.patch` to replace `ticketing_bridge.create_ticket_for_remediation_task` with an AsyncMock that raises (nonfatal case, driven with a "critical" or "medium" priority task per the widened D-01 set) and an assert-not-called check for a "low"-priority task (outside the critical/high/medium set). Drive `compliance_remediation_service.create_task` the same way `test_rem01_create_task_persists` does.
  </action>
  <verify>
    <automated>cd backend && venv/bin/python -m pytest tests/test_remediation_workflow.py -k autocreate_nonfatal -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_remediation_workflow.py -k autocreate_nonfatal -x` passes.
    - The full `tests/test_remediation_workflow.py` file still passes (`pytest tests/test_remediation_workflow.py -q`).
  </acceptance_criteria>
  <done>autocreate_nonfatal test exists and passes, proving auto-create failure is non-blocking.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| create_task auto-create hook → external SaaS | server-initiated ticket creation during task persistence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-04 | Denial of Service | ticketing outage blocking task creation | mitigate | auto-create wrapped in try/except after insert_one; failure is logged and swallowed so the task action always succeeds (D-04) |
| T-43-05 | Elevation of Privilege | cross-tenant auto-create | mitigate | hook passes `task.get("tenantId")` explicitly; create_ticket_for_remediation_task scopes config + $set to that tenant |
</threat_model>

<artifacts_this_phase_produces>
Modified `backend/compliance_remediation_service.py::create_task` — adds a priority-gated (`critical`/`high`/`medium`, D-01 revised 2026-07-21) non-blocking auto-create ticket hook calling `ticketing_bridge.create_ticket_for_remediation_task`.
Modified `backend/tests/test_remediation_workflow.py` — adds `autocreate_nonfatal` test.
No new symbols, endpoints, or collections.
</artifacts_this_phase_produces>

<verification>
- `cd backend && venv/bin/python -m pytest tests/test_remediation_workflow.py -q` all green.
- create_task return contract unchanged (still returns the persisted task dict).
</verification>

<success_criteria>
Critical/high/medium remediation tasks auto-create a ticket on creation (D-01, revised 2026-07-21), and a ticketing failure never blocks task creation (D-04), verified by the autocreate_nonfatal test.
</success_criteria>

<output>
Create `.planning/phases/43-remediation-to-ticketing-bridge/43-02-SUMMARY.md` when done.
</output>
