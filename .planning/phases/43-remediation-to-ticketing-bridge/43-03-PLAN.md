---
phase: 43-remediation-to-ticketing-bridge
plan: 03
type: execute
wave: 2
depends_on: ["43-01"]
files_modified:
  - backend/compliance_remediation_endpoints.py
  - backend/app_startup.py
  - backend/tests/test_ticketing_bridge.py
autonomous: true
requirements: [REM-01, REM-02]
must_haves:
  truths:
    - "A compliance admin can POST to /api/compliance-remediation/tasks/{task_id}/create-ticket with a provider and get a real ticket created, tenant-scoped, returning the ticket provider/ref/url"
    - "The provider field is validated as Literal['jira','servicenow'] and arbitrary strings are rejected before reaching bridge branch logic"
    - "ticket_provider/ticket_ref/ticket_url are never accepted on the client-writable TaskUpdate PATCH model (server-controlled only)"
    - "The close-loop scheduler is registered at app startup with the raw mongodb.db object (never get_database), so it polls across tenants without failing closed"
  artifacts:
    - path: "backend/compliance_remediation_endpoints.py"
      provides: "POST /tasks/{task_id}/create-ticket route + CreateTicketRequest model"
      contains: "create-ticket"
    - path: "backend/app_startup.py"
      provides: "close-loop scheduler registration (raw _mdb.db)"
      contains: "start_close_loop_scheduler"
    - path: "backend/tests/test_ticketing_bridge.py"
      provides: "endpoint test coverage"
      contains: "endpoint"
  key_links:
    - from: "backend/compliance_remediation_endpoints.py::create_ticket"
      to: "backend/ticketing_bridge.py::create_ticket_for_remediation_task"
      via: "provider_override=body.provider after _tenant_filter + get_task 404 guard"
      pattern: "create_ticket_for_remediation_task"
    - from: "backend/app_startup.py"
      to: "backend/ticketing_bridge.py::start_close_loop_scheduler"
      via: "asyncio.create_task(start_close_loop_scheduler(_mdb.db))"
      pattern: "start_close_loop_scheduler"
---

<objective>
Expose the manual "Create Ticket" action as `POST /api/compliance-remediation/tasks/{task_id}/create-ticket` on the already-registered compliance-remediation router (REM-01, D-02 provider picker's backend), register the close-loop scheduler at app startup with the raw db (REM-02), and add the endpoint unit test.

Purpose: Plan 01 built the bridge logic; this plan wires it into the running app — the HTTP surface admins call and the background loop that closes the REM-02 loop. Both are thin glue over existing, proven patterns (`suggest_remediation`'s route shape; the `tickets_escalation_service` startup block). No new endpoint file is created (Pitfall 3/7) — the route is added to the existing `compliance_remediation_endpoints.py`.
Output: modified `compliance_remediation_endpoints.py`, modified `app_startup.py`, extended `test_ticketing_bridge.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-remediation-to-ticketing-bridge/43-CONTEXT.md
@.planning/phases/43-remediation-to-ticketing-bridge/43-PATTERNS.md
@.planning/phases/43-remediation-to-ticketing-bridge/43-RESEARCH.md
@.planning/phases/43-remediation-to-ticketing-bridge/43-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add POST /tasks/{task_id}/create-ticket route + CreateTicketRequest model</name>
  <files>backend/compliance_remediation_endpoints.py</files>
  <read_first>
    - backend/compliance_remediation_endpoints.py (full file — `_tenant_filter` lines 34-41, TaskUpdate model lines 60-65, suggest_remediation route + $set persist-back precedent lines 128-171, existing imports lines 14-26)
    - .planning/phases/43-remediation-to-ticketing-bridge/43-PATTERNS.md (the specified CreateTicketRequest + create_ticket route shape)
    - .planning/phases/43-remediation-to-ticketing-bridge/43-RESEARCH.md (Security Domain V4/V5 — reuse _tenant_filter, validate provider Literal)
    - backend/ticketing_bridge.py (create_ticket_for_remediation_task signature)
  </read_first>
  <action>
    In `backend/compliance_remediation_endpoints.py`: add `import ticketing_bridge` alongside the existing `import compliance_remediation_service as svc` (line 25-26). Add a request model `class CreateTicketRequest(BaseModel)` with a single field `provider: Literal["jira", "servicenow"]` (V5 input validation — rejects arbitrary strings). Add `@router.post("/tasks/{task_id}/create-ticket")` `async def create_ticket(task_id, body: CreateTicketRequest, current_user=Depends(get_current_user))`: compute `tf = _tenant_filter(current_user)`; `db = get_database()`; `task = await svc.get_task(db, task_id, tf)`; if None, raise 404 (tenant-scoped, matches existing routes — a foreign task_id 404s, no info leak); resolve `tenant_id = tf.get("tenantId") or task.get("tenantId", "")`; call `result = await ticketing_bridge.create_ticket_for_remediation_task(db, task, tenant_id, provider_override=body.provider)`; if falsy, raise `HTTPException(status_code=502, detail="Ticket creation failed")`; else return `result`. Do NOT add ticket_provider/ticket_ref/ticket_url to `TaskUpdate` — they remain server-controlled `$set`-only (written inside the bridge), never client-writable.
  </action>
  <verify>
    <automated>cd backend && venv/bin/python -c "import inspect, compliance_remediation_endpoints as e; assert hasattr(e,'CreateTicketRequest'); src=inspect.getsource(e); assert 'create-ticket' in src and 'Literal[\"jira\", \"servicenow\"]' in src.replace(\"'\",'\"'); tu=inspect.getsource(e.TaskUpdate); assert 'ticket_ref' not in tu and 'ticket_url' not in tu and 'ticket_provider' not in tu"</automated>
  </verify>
  <acceptance_criteria>
    - `CreateTicketRequest.provider` is typed `Literal["jira","servicenow"]`.
    - The route uses `_tenant_filter` and `svc.get_task`, returning 404 for a task outside tenant scope and 502 when the connector fails.
    - `TaskUpdate` model source contains none of `ticket_provider`/`ticket_ref`/`ticket_url` (server-controlled fields never client-writable).
    - No new endpoint module is created; the route lives in `compliance_remediation_endpoints.py` (already registered at router_registry.py:169).
  </acceptance_criteria>
  <done>The manual create-ticket route exists on the existing router with a Literal-validated provider and no client-writable ticket fields.</done>
</task>

<task type="auto">
  <name>Task 2: Register close-loop scheduler in app_startup.py with raw mongodb.db</name>
  <files>backend/app_startup.py</files>
  <read_first>
    - backend/app_startup.py (the tickets_escalation_service registration block lines 602-608 — exact pattern to clone)
    - .planning/phases/43-remediation-to-ticketing-bridge/43-PATTERNS.md (app_startup registration block spec)
    - .planning/phases/43-remediation-to-ticketing-bridge/43-RESEARCH.md (Pattern 4 / Pitfall 1 — must pass raw `_mdb.db`, never get_database)
    - backend/ticketing_bridge.py (start_close_loop_scheduler signature)
  </read_first>
  <action>
    In `backend/app_startup.py`, directly after the existing `tickets_escalation_service` registration block (after line 608), add a new `try/except` block cloning that exact shape: `from ticketing_bridge import start_close_loop_scheduler`; `from database import mongodb as _mdb`; `asyncio.create_task(start_close_loop_scheduler(_mdb.db))`; `logger.info("[Ticketing] Remediation close-loop scheduler started")`; on exception `logger.warning("[Ticketing] Close-loop scheduler failed to start: %s", _e)`. Must pass raw `_mdb.db` (Pitfall 1) — never `get_database()`. This is the 5th scheduler registration of this pattern; do not alter the surrounding blocks.
  </action>
  <verify>
    <automated>cd backend && venv/bin/python -c "import inspect, app_startup; src=inspect.getsource(app_startup); assert 'start_close_loop_scheduler(_mdb.db)' in src and 'Remediation close-loop scheduler' in src"</automated>
  </verify>
  <acceptance_criteria>
    - `app_startup.py` registers `start_close_loop_scheduler(_mdb.db)` via `asyncio.create_task`, passing raw `_mdb.db` (grep confirms `start_close_loop_scheduler(_mdb.db)`; no `start_close_loop_scheduler(get_database` anywhere).
    - The block is wrapped in try/except with a non-fatal warning, matching the sibling scheduler blocks.
  </acceptance_criteria>
  <done>Close-loop scheduler is registered at startup with raw db, non-fatally, following the established pattern.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add endpoint test to test_ticketing_bridge.py</name>
  <files>backend/tests/test_ticketing_bridge.py</files>
  <read_first>
    - backend/tests/test_ticketing_bridge.py (created in Plan 01 — reuse its mock-db helpers)
    - backend/tests/test_tickets.py (FastAPI TestClient / dependency-override pattern if used elsewhere for auth)
    - .planning/phases/43-remediation-to-ticketing-bridge/43-VALIDATION.md (endpoint row — `pytest tests/test_ticketing_bridge.py -k endpoint -x`)
    - backend/compliance_remediation_endpoints.py (the create_ticket route from Task 1)
  </read_first>
  <behavior>
    - Test `endpoint`: exercising `POST /tasks/{task_id}/create-ticket` (via TestClient with overridden `get_current_user`/`get_database`, or by calling the route function directly with a stubbed user and patched `svc.get_task` + `ticketing_bridge.create_ticket_for_remediation_task`) returns the ticket provider/ref/url dict on success; a provider outside `{"jira","servicenow"}` is rejected (422 validation) ; a missing task yields 404; a connector failure (bridge returns None) yields 502.
  </behavior>
  <action>
    Append an `endpoint`-named test (matching `-k endpoint`) to `backend/tests/test_ticketing_bridge.py`. Prefer the lightest approach that works with the existing test harness: either FastAPI `TestClient` with `app.dependency_overrides` for `get_current_user`/`get_database`, or a direct async call to the route coroutine with a fake user object and `unittest.mock.patch` on `compliance_remediation_service.get_task` and `ticketing_bridge.create_ticket_for_remediation_task`. Cover success (200 + ticket fields), invalid provider (422 — only meaningful under TestClient), 404 (task not found), and 502 (bridge returns None). Keep all external HTTP mocked.
  </action>
  <verify>
    <automated>cd backend && venv/bin/python -m pytest tests/test_ticketing_bridge.py -k endpoint -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_ticketing_bridge.py -k endpoint -x` passes.
    - The full `tests/test_ticketing_bridge.py` still passes (`pytest tests/test_ticketing_bridge.py -q`).
  </acceptance_criteria>
  <done>The endpoint test exists and passes, covering success/404/502 (and 422 if TestClient is used).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → create-ticket endpoint | authenticated admin submits provider + task_id |
| app startup → background scheduler | raw-db cross-tenant polling task launched |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-06 | Tampering | client-supplied ticket_ref/url spoofing | mitigate | ticket fields never added to TaskUpdate; only server `$set` inside the bridge writes them |
| T-43-07 | Spoofing/Input | arbitrary provider string | mitigate | `provider: Literal["jira","servicenow"]` rejects other values with 422 before branch logic |
| T-43-08 | Information Disclosure | task_id enumeration across tenants | mitigate | `_tenant_filter(user)` + `svc.get_task` return 404 for foreign task_id, matching existing routes |
| T-43-03 | Denial of Service | scheduler registered via get_database fails closed | mitigate | registration passes raw `_mdb.db` (Pitfall 1); verified in Task 2 acceptance |
</threat_model>

<artifacts_this_phase_produces>
Modified `backend/compliance_remediation_endpoints.py`:
- `CreateTicketRequest` Pydantic model (`provider: Literal["jira","servicenow"]`)
- `POST /api/compliance-remediation/tasks/{task_id}/create-ticket` route (`create_ticket`)
Modified `backend/app_startup.py` — registers `start_close_loop_scheduler(_mdb.db)` (5th scheduler block).
Modified `backend/tests/test_ticketing_bridge.py` — adds `endpoint` test.
No new collections; no client-writable ticket fields.
</artifacts_this_phase_produces>

<verification>
- `cd backend && venv/bin/python -m pytest tests/test_ticketing_bridge.py -q` all green.
- `grep -c 'start_close_loop_scheduler(_mdb.db)' backend/app_startup.py` returns 1.
- TaskUpdate model contains no ticket_* fields.
</verification>

<success_criteria>
The manual create-ticket endpoint is live on the existing router with strict provider validation and tenant scoping (REM-01), ticket fields remain server-controlled, and the close-loop scheduler is registered at startup with raw db (REM-02) — endpoint behavior covered by a passing test.
</success_criteria>

<output>
Create `.planning/phases/43-remediation-to-ticketing-bridge/43-03-SUMMARY.md` when done.
</output>
