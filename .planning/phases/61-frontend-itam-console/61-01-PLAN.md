# Plan 61-01: ITAM Console Nav and UI Scaffolding

**Goal:** Implement admin-gated ITAM console nav and base UI structure.

**Tasks:**
- Add `manage:itam` permission.
- Add "ITAM" item to Sidebar (gated).
- Implement `ITAMConsole` AppView with skeleton sections.
- Add tests in `tests/test_itam_ui.py`.
