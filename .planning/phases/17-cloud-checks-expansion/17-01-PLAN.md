phase: 17-cloud-checks-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/cloud_checks_service.py
autonomous: true
requirements:
  - CC-EXP-01
  - CC-EXP-02
must_haves:
  truths:
    - "cloud_checks_service.py CLOUD_CHECKS list grows from 67 to 300+ check definitions (CC-EXP-01)"
    - "New AWS checks added: EKS (cluster auth, node group IMDSv2, logging), Lambda (resource policy, env var encryption, VPC config, tracing), CloudFront (HTTPS only, WAF attached, logging), WAF (managed rules attached), SNS (encryption at rest, cross-account), SQS (server-side encryption, queue policy), ElasticSearch (domain in VPC, encryption, access policy), Route53 (DNSSEC, query logging), ACM (cert expiry <30d warning), Inspector (activated), SSM (managed instances), Backup (plan exists) (CC-EXP-01)"
    - "New Azure checks added: App Service (HTTPS only, managed identity, TLS 1.2), Azure Container Registry (admin disabled, geo-replication), Azure AKS (RBAC, authorized IPs, managed identity) (CC-EXP-01)"
    - "New GCP checks added: BigQuery (public access, encryption, authorized views), GKE (workload identity, shielded nodes, network policy enabled, authorized networks) (CC-EXP-01)"
    - "Each new check has: id, name, description, provider, service, severity, frameworks list, remediation (CC-EXP-02) — matches the pre-existing schema used by the original 67 checks and by CloudChecksScanner.tsx; `control_ids`/`remediation_steps` in the original wording was a naming mismatch, not a distinct required field pair"
    - "File stays under 500 lines — move checks into per-provider modules if needed: cloud_checks_aws.py, cloud_checks_azure.py, cloud_checks_gcp.py imported by cloud_checks_service.py (CC-EXP-02)"
    - "Existing 67 checks are not modified or removed"
    - "GET /api/cloud-checks still returns all checks with working filters"

<objective>
Expand the cloud security check library from 67 to 300+ checks, matching Prowler's coverage depth. This is the #2 gap from the audit: our cloud scanner has good structure but limited check breadth.

**New AWS service checks to add:**

| Service | New checks |
|---------|-----------|
| EKS | cluster public endpoint disabled, node group uses IMDSv2, control plane logging enabled |
| Lambda | no resource-based policy allows *, env vars encrypted, in VPC, X-Ray tracing enabled |
| CloudFront | HTTP redirects to HTTPS, WAF WebACL attached, access logging enabled |
| WAF | managed rule group attached to WebACL |
| SNS | server-side encryption enabled, no cross-account publish |
| SQS | SSE enabled, queue policy not public |
| ElasticSearch | domain in VPC, encryption at rest, fine-grained access control |
| Route53 | DNSSEC enabled, query logging enabled |
| ACM | no cert expiring within 30 days |
| Inspector | Inspector v2 activated for EC2 and Lambda |
| SSM | EC2 instances managed by SSM (no unmanaged) |
| Backup | Backup plan exists with ≥1 rule |

**New Azure checks:** App Service HTTPS, ACR admin disabled, AKS RBAC enabled, AKS authorized IP ranges, AKS managed identity, Azure Container Registry geo-replication

**New GCP checks:** BigQuery public access disabled, BigQuery CMEK, GKE workload identity, GKE shielded nodes, GKE network policy, GKE authorized networks, GKE private cluster

**File size strategy:**
- If cloud_checks_service.py would exceed 500 lines: split check definitions into `cloud_checks_aws.py`, `cloud_checks_azure.py`, `cloud_checks_gcp.py`
- cloud_checks_service.py imports from these modules: `from cloud_checks_aws import AWS_CHECKS; from cloud_checks_azure import AZURE_CHECKS; from cloud_checks_gcp import GCP_CHECKS; CLOUD_CHECKS = AWS_CHECKS + AZURE_CHECKS + GCP_CHECKS`
- ALWAYS read cloud_checks_service.py before editing to see current line count

**Implementation:**
1. Read current cloud_checks_service.py (check line count)
2. If > 350 lines already: extract existing checks into three module files, then add new checks to each
3. If < 350 lines: add new checks inline keeping total under 500
4. Verify GET /api/cloud-checks returns new checks in response

Output: Expanded check library, file structure preserved under 500-line limit per file
</objective>
