---
phase: 10-scheduled-reports
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - components/ScheduledReportsDashboard.tsx
  - components/ScheduleHistoryPanel.tsx
autonomous: true
requirements:
  - SCHED-01
  - SCHED-02

must_haves:
  truths:
    - "When creating a compliance_summary schedule, the admin sees a framework dropdown and selects a specific framework (e.g. SOC 2); that framework name appears on the schedule card after creation"
    - "Clicking 'Run Now' on any schedule card successfully triggers delivery (no 404 — the URL bug is fixed)"
    - "Each schedule card has a 'History' button; clicking it reveals a compact table of past delivery attempts showing date/time, status (green success / red failure), and recipients"
    - "A failed delivery shows the error message in the history table so the admin knows what went wrong"
  artifacts:
    - path: "components/ScheduledReportsDashboard.tsx"
      provides: "Framework picker in create modal, fixed runNow URL, framework_name on card, History toggle per card"
      contains: "run-now"
    - path: "components/ScheduleHistoryPanel.tsx"
      provides: "Delivery history table component (split out if ScheduledReportsDashboard.tsx would exceed 500 lines)"
  key_links:
    - from: "components/ScheduledReportsDashboard.tsx (runNow)"
      to: "backend/scheduled_reports_endpoints.py (POST /{id}/run-now)"
      via: "authFetch(`/api/reports/scheduled/${id}/run-now`, { method: 'POST' })"
      pattern: "run-now"
    - from: "components/ScheduledReportsDashboard.tsx (History button)"
      to: "backend/scheduled_reports_endpoints.py (GET /{id}/history)"
      via: "authFetch(`/api/reports/scheduled/${id}/history`)"
      pattern: "/history"
    - from: "components/ScheduledReportsDashboard.tsx (create modal framework picker)"
      to: "services/apiService.ts (fetchComplianceFrameworks)"
      via: "returns ComplianceFramework[] with id and name fields; framework picker maps these to option elements"
      pattern: "fetchComplianceFrameworks"
---

<objective>
Extend ScheduledReportsDashboard.tsx with three targeted additions: a framework picker in
the create modal (SCHED-01), the run-now URL fix (live bug), and a per-card delivery history
panel (SCHED-02). If the component would exceed 500 lines after edits, the history table is
split into ScheduleHistoryPanel.tsx.

Purpose: Makes the backend changes from Plan 01 visible and usable to tenant admins.
Output: Updated ScheduledReportsDashboard.tsx (under 500 lines) and optionally
        ScheduleHistoryPanel.tsx, both wired to the Plan 01 backend changes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-scheduled-reports/10-RESEARCH.md
@.planning/phases/10-scheduled-reports/10-01-SUMMARY.md

# Source files — read in full before any edits
@components/ScheduledReportsDashboard.tsx
@services/apiService.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix runNow URL bug, add framework_id to ScheduledReport interface and form state, fetch frameworks on mount, add framework picker to create modal, display framework_name on card</name>
  <files>components/ScheduledReportsDashboard.tsx</files>
  <behavior>
    - runNow() calls /api/reports/scheduled/${id}/run-now (not /run) — the 404 live bug is gone
    - ScheduledReport interface has framework_id?: string | null and framework_name?: string | null
    - form state has framework_id: string field (default empty string)
    - On mount, fetchComplianceFrameworks() is called and results stored in frameworks state
      (ComplianceFramework[] from apiService.ts — has id and name fields)
    - When form.report_type === 'compliance_summary' or 'custom_framework', a framework
      select dropdown appears in the create modal between Report Type and Frequency
    - Selecting a framework sets form.framework_id to the chosen framework's id
    - createReport() includes framework_id in the POST body when it is non-empty, alongside
      framework_name (derived by finding the selected framework's name in the frameworks list)
    - Each schedule card shows framework_name below the report_type line when framework_name
      is set (small gray text, e.g. "SOC 2" or "ISO 27001")
  </behavior>
  <action>
    Read components/ScheduledReportsDashboard.tsx before editing. The file is 312 lines.
    Apply changes using Edit (scoped replacements):

    1. Fix runNow URL (line 119): change `/api/reports/scheduled/${id}/run` to
       `/api/reports/scheduled/${id}/run-now`.

    2. Add to ScheduledReport interface after the existing run_count line:
         framework_id?: string | null;
         framework_name?: string | null;

    3. Add import for fetchComplianceFrameworks from apiService:
       The file already imports authFetch from '../services/apiService'. Extend the import
       to also import fetchComplianceFrameworks. Then import ComplianceFramework from types:
         import type { ComplianceFramework } from '../types';

    4. Add frameworks state variable after the existing useState declarations:
         const [frameworks, setFrameworks] = useState<ComplianceFramework[]>([]);

    5. Add framework_id: '' to the form initial state object (both in useState and in the
       setForm reset call inside createReport).

    6. In useEffect, add: fetchComplianceFrameworks().then(setFrameworks);
       alongside the existing loadReports() call.

    7. In createReport(), add to the body object:
         if (form.framework_id) {
           body.framework_id = form.framework_id;
           body.framework_name = frameworks.find(f => f.id === form.framework_id)?.name ?? '';
         }

    8. In the create modal, after the Report Type select block, add a conditional framework
       picker that renders only when form.report_type is 'compliance_summary' or
       'custom_framework':
         A labeled select with options mapping frameworks to {value: f.id, label: f.name}.
         First option is a placeholder: value="" disabled "Select framework...".
         Selecting an option updates form.framework_id.

    9. In the schedule card JSX, after the `<p className="text-xs text-gray-400 capitalize...`
       line that shows report_type, add:
         {rep.framework_name && (
           <p className="text-xs text-gray-500 mt-0.5">{rep.framework_name}</p>
         )}

    After editing, check line count: `wc -l components/ScheduledReportsDashboard.tsx`
    Record the count. If it is already 380 or more, signal that Task 2 must split the
    history panel into ScheduleHistoryPanel.tsx before adding more lines.
  </action>
  <verify>
    <automated>wc -l /home/user/enterprise-omni-agent-ai-platform/components/ScheduledReportsDashboard.tsx && grep -c "run-now" /home/user/enterprise-omni-agent-ai-platform/components/ScheduledReportsDashboard.tsx && grep -c "framework_id" /home/user/enterprise-omni-agent-ai-platform/components/ScheduledReportsDashboard.tsx</automated>
  </verify>
  <done>
    "run-now" appears at least once in the file (URL fixed).
    "framework_id" appears in the interface, form state, POST body, and modal JSX.
    File line count is recorded for Task 2 planning.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add per-card delivery history toggle — fetch GET /{id}/history and render inline history panel; split to ScheduleHistoryPanel.tsx if total lines would exceed 500</name>
  <files>components/ScheduledReportsDashboard.tsx, components/ScheduleHistoryPanel.tsx</files>
  <behavior>
    - Each schedule card has a "History" button in the action row alongside Run Now and Delete
    - Clicking History toggles an expandable panel below that card (open state tracked per
      schedule id in a Record<string, boolean> state variable named historyOpen)
    - When the panel opens for a schedule, the component fetches
      GET /api/reports/scheduled/{id}/history and stores results in a
      Record<string, DeliveryLog[]> state variable named historyLogs
    - DeliveryLog interface has: id, run_at, status ("success"|"failure"), recipients,
      error (string|null), format (string), filename (string|null)
    - The history panel renders a compact table with columns: Date/Time, Status, Recipients, Error
    - Status column shows a green "Success" badge for status==="success" and a red "Failed"
      badge for status==="failure"
    - Error column shows the error string truncated to 60 chars with title tooltip for full
      text; shows "—" when null
    - While loading, the panel shows a single "Loading..." row
    - If the history array is empty, the panel shows "No delivery history yet"
    - Split decision: after adding the history state variables, toggle function, and panel JSX,
      measure the projected line count. If ScheduledReportsDashboard.tsx would exceed 499 lines:
        a. Extract the history panel JSX and DeliveryLog interface into
           components/ScheduleHistoryPanel.tsx as a named export ScheduleHistoryPanel
           accepting props: {logs: DeliveryLog[], loading: boolean}
        b. Import ScheduleHistoryPanel in ScheduledReportsDashboard.tsx
        c. Keep fetch logic and historyLogs state in ScheduledReportsDashboard.tsx
        d. ScheduleHistoryPanel.tsx must itself be under 500 lines
  </behavior>
  <action>
    Determine the current line count of ScheduledReportsDashboard.tsx (from Task 1 result).
    The original file was 312 lines; Task 1 additions will push it to approximately 345-360
    lines. Adding history state (2 state vars, 1 fetch function) plus panel JSX (~40-60 lines
    of table markup per card, though rendered inline) will land around 400-430 lines — safely
    under 500. Inline approach is preferred unless the actual count after Task 1 is >= 380.

    Inline approach (if current line count after Task 1 is under 380):

    1. Add DeliveryLog interface after the ScheduledReport interface.

    2. Add two state variables after the existing frameworks state:
         const [historyOpen, setHistoryOpen] = useState<Record<string, boolean>>({});
         const [historyLogs, setHistoryLogs] = useState<Record<string, DeliveryLog[]>>({});
       And a loading state:
         const [historyLoading, setHistoryLoading] = useState<Record<string, boolean>>({});

    3. Add a toggleHistory(id: string) function:
         async function toggleHistory(id: string) {
           const opening = !historyOpen[id];
           setHistoryOpen(prev => ({ ...prev, [id]: opening }));
           if (opening && !historyLogs[id]) {
             setHistoryLoading(prev => ({ ...prev, [id]: true }));
             try {
               const r = await authFetch(`/api/reports/scheduled/${id}/history`);
               const d = await r.json();
               setHistoryLogs(prev => ({ ...prev, [id]: d.logs || [] }));
             } catch { /* silently leave logs empty */ }
             finally { setHistoryLoading(prev => ({ ...prev, [id]: false })); }
           }
         }

    4. In the schedule card action row, add a "History" button between Run Now and Delete:
         <button onClick={() => toggleHistory(rep.id)}
           className="bg-gray-700 hover:bg-gray-600 px-3 py-1.5 rounded text-xs">
           {historyOpen[rep.id] ? 'Hide' : 'History'}
         </button>

    5. After the closing </div> of the action row inside each card, add the collapsible
       history panel:
         {historyOpen[rep.id] && (
           <div className="mt-3 border-t border-gray-700 pt-3">
             {historyLoading[rep.id] ? (
               <p className="text-xs text-gray-500">Loading...</p>
             ) : !historyLogs[rep.id]?.length ? (
               <p className="text-xs text-gray-500">No delivery history yet</p>
             ) : (
               <table className="w-full text-xs text-gray-300">
                 <thead>
                   <tr className="text-gray-500 border-b border-gray-700">
                     <th className="pb-1 text-left">Date / Time</th>
                     <th className="pb-1 text-left">Status</th>
                     <th className="pb-1 text-left">Recipients</th>
                     <th className="pb-1 text-left">Error</th>
                   </tr>
                 </thead>
                 <tbody>
                   {historyLogs[rep.id].map(log => (
                     <tr key={log.id} className="border-b border-gray-800">
                       <td className="py-1">{new Date(log.run_at).toLocaleString()}</td>
                       <td className="py-1">
                         {log.status === 'success'
                           ? <span className="text-green-400 font-medium">Success</span>
                           : <span className="text-red-400 font-medium">Failed</span>}
                       </td>
                       <td className="py-1">{log.recipients?.join(', ') || '—'}</td>
                       <td className="py-1" title={log.error || ''}>
                         {log.error ? log.error.slice(0, 60) + (log.error.length > 60 ? '…' : '') : '—'}
                       </td>
                     </tr>
                   ))}
                 </tbody>
               </table>
             )}
           </div>
         )}

    Split approach (if current line count after Task 1 is >= 380):
    Extract the table JSX and DeliveryLog interface into components/ScheduleHistoryPanel.tsx
    as described in the behavior block. ScheduledReportsDashboard.tsx imports it as a named
    import and renders <ScheduleHistoryPanel logs={historyLogs[rep.id] ?? []}
    loading={!!historyLoading[rep.id]} />.

    After edits, check final line count:
    `wc -l components/ScheduledReportsDashboard.tsx`
    If a split was created: `wc -l components/ScheduleHistoryPanel.tsx`
    Both must be under 500.
  </action>
  <verify>
    <automated>wc -l /home/user/enterprise-omni-agent-ai-platform/components/ScheduledReportsDashboard.tsx && grep -c "history" /home/user/enterprise-omni-agent-ai-platform/components/ScheduledReportsDashboard.tsx && grep -c "toggleHistory" /home/user/enterprise-omni-agent-ai-platform/components/ScheduledReportsDashboard.tsx</automated>
  </verify>
  <done>
    toggleHistory function exists in ScheduledReportsDashboard.tsx.
    "history" string appears in the file (route URL, state var names, or panel JSX).
    ScheduledReportsDashboard.tsx is under 500 lines.
    If ScheduleHistoryPanel.tsx was created, it is also under 500 lines.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → backend API | Authenticated tenant admin session. authFetch attaches JWT. No new cross-origin calls introduced. |
| Frontend → fetchComplianceFrameworks | Reads framework list from /api/compliance — same endpoint used by ReportingDashboard; no new surface. |
| Delivery history panel → GET /history | Returns only logs for the requesting tenant's schedule; enforced backend-side (Plan 01). Frontend does not attempt to access other tenants' history. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-FE-01 | Information Disclosure | History panel renders error strings from backend | accept | Error messages are server-generated and scoped to the tenant's own delivery failures; no cross-tenant data surfaces here |
| T-10-FE-02 | Tampering | framework_id sent in POST body | accept | Backend validates framework_id by querying compliance_frameworks collection; unknown IDs produce an empty PDF, not a security breach |
| T-10-FE-03 | Spoofing | authFetch JWT attachment | accept | authFetch already handles JWT for all existing API calls in this component; no new auth mechanism introduced |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No new packages. ComplianceFramework type imported from existing types.ts; fetchComplianceFrameworks imported from existing apiService.ts |
</threat_model>

<verification>
After both frontend tasks:

```bash
# Verify URL fix is in place
grep "run-now" /home/user/enterprise-omni-agent-ai-platform/components/ScheduledReportsDashboard.tsx

# Verify framework_id wiring
grep "framework_id" /home/user/enterprise-omni-agent-ai-platform/components/ScheduledReportsDashboard.tsx

# Verify history fetch wiring
grep "toggleHistory\|/history" /home/user/enterprise-omni-agent-ai-platform/components/ScheduledReportsDashboard.tsx

# Verify line counts
wc -l /home/user/enterprise-omni-agent-ai-platform/components/ScheduledReportsDashboard.tsx

# TypeScript compile check (if tsc available in project)
cd /home/user/enterprise-omni-agent-ai-platform && npx tsc --noEmit 2>&1 | grep -i "ScheduledReports\|ScheduleHistory" | head -20
```

Expected:
- "run-now" appears in the file
- "framework_id" appears in interface, form state, POST body, and modal JSX
- "toggleHistory" appears as a function definition
- "/history" appears in the fetch URL
- ScheduledReportsDashboard.tsx: under 500 lines
- TypeScript compile: no errors in the modified files
</verification>

<success_criteria>
- runNow() calls /run-now (not /run): the 404 live bug is resolved
- ScheduledReport TypeScript interface includes framework_id and framework_name
- Framework dropdown appears in the create modal when report_type is compliance_summary or custom_framework
- Created schedule with a selected framework shows framework_name on the card
- Each card has a History button that toggles the delivery log panel
- History panel shows run timestamp, green/red status badge, recipient list, and error text
- ScheduledReportsDashboard.tsx is 499 lines or fewer
- ScheduleHistoryPanel.tsx is 499 lines or fewer if it was created
</success_criteria>

<output>
Create `.planning/phases/10-scheduled-reports/10-02-SUMMARY.md` when done.
</output>
