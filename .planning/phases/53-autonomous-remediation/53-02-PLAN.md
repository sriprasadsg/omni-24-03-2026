---
phase: 53-autonomous-remediation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - agent-install/omni-agent-rs/src/capabilities/remediation_actions.rs
  - agent-install/omni-agent-rs/src/capabilities/mod.rs
  - agent-install/omni-agent-rs/src/instructions.rs
autonomous: true
requirements: [AUTO-02]
must_haves:
  truths:
    - "instructions.rs dispatches kill_process, restore_file, block_ip, rotate_key, disable_service commands to remediation_actions and returns a structured result"
    - "Each action is bounded/safe and returns {status: success|error, detail}; patch reuses the existing install/upgrade_software arm"
    - "Failures are handled (no panic); the agent builds linux + windows-gnu"
  artifacts:
    - path: "agent-install/omni-agent-rs/src/capabilities/remediation_actions.rs"
      provides: "kill_process/restore_file/block_ip/rotate_key/disable_service implementations returning structured results"
      contains: "kill_process"
      min_lines: 70
    - path: "agent-install/omni-agent-rs/src/instructions.rs"
      provides: "dispatch arms for the 5 new actions"
      contains: "kill_process"
  key_links:
    - from: "agent-install/omni-agent-rs/src/instructions.rs"
      to: "agent-install/omni-agent-rs/src/capabilities/remediation_actions.rs"
      via: "match arm -> remediation_actions::<action>(params) -> result"
      pattern: "remediation_actions"
  prohibitions:
    - "MUST NOT execute an unbounded/destructive command without validating params (bound targets)"
    - "MUST NOT panic on failure — return {status:error}"
    - "MUST NOT introduce a C dependency; windows-gnu must still check"
---

<objective>
Give the agent the five new remediation action commands the engine dispatches — kill_process, restore_file, block_ip, rotate_key, disable_service — each bounded and returning a structured result the backend verifies. Patch reuses the existing install/upgrade_software arm.

Purpose: AUTO-02 execution surface. D-03.
Output: `remediation_actions.rs` + `instructions.rs` dispatch arms.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@.planning/phases/53-autonomous-remediation/53-RESEARCH.md
@.planning/phases/53-autonomous-remediation/53-CONTEXT.md

# Read-first anchors:
@agent-install/omni-agent-rs/src/instructions.rs   # existing arms (install_software/disable_rdp) to mirror + params extraction
@agent-install/omni-agent-rs/src/capabilities/fim_baseline.rs  # baseline/backup source for restore_file (Phase 52)
</execution_context>

<artifacts>
- NEW `agent-install/omni-agent-rs/src/capabilities/remediation_actions.rs` — the 5 actions.
- EDIT `agent-install/omni-agent-rs/src/capabilities/mod.rs` — `pub mod remediation_actions;`.
- EDIT `agent-install/omni-agent-rs/src/instructions.rs` — 5 dispatch arms.
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: remediation_actions module (5 bounded actions)</name>
  <files>agent-install/omni-agent-rs/src/capabilities/remediation_actions.rs, agent-install/omni-agent-rs/src/capabilities/mod.rs</files>
  <read_first>instructions.rs param-extraction pattern; fim_baseline for restore source.</read_first>
  <action>Implement each `pub fn <action>(params: &Value) -> Value` returning `{status: "success"|"error", detail}`: `kill_process` (by pid or exact name via sysinfo/OS kill; validate target present); `restore_file` (restore a path from the FIM baseline/backup hash or a provided backup path; refuse if no known-good source); `block_ip` (add a host firewall rule — iptables/nft on Linux, netsh advfirewall on Windows — validate a well-formed IP/CIDR); `rotate_key` (rotate a *named, allowlisted* credential/key target only — refuse arbitrary paths); `disable_service` (stop + disable a named service — systemctl / sc). Every action validates its params, is bounded to safe targets, and never panics (errors → `{status:error}`). Register `pub mod remediation_actions;`.</action>
  <verify>
    <automated>cd agent-install/omni-agent-rs && cargo build 2>&1 | tail -6</automated>
  </verify>
  <done>Five actions implemented, param-validated, bounded, non-panicking.</done>
</task>

<task type="auto">
  <name>Task 2: instructions.rs dispatch arms + windows check</name>
  <files>agent-install/omni-agent-rs/src/instructions.rs</files>
  <read_first>the existing "install_software"/"disable_rdp" arms.</read_first>
  <action>Add match arms `"kill_process"`, `"restore_file"`, `"block_ip"`, `"rotate_key"`, `"disable_service"`, each extracting params from `item.parameters`, calling `capabilities::remediation_actions::<action>(&params)`, and returning its result as the arm's status JSON (so the engine can verify). Mirror the existing arm structure. Confirm `cargo check --target x86_64-pc-windows-gnu`.</action>
  <verify>
    <automated>cd agent-install/omni-agent-rs && cargo build 2>&1 | tail -5 && cargo check --target x86_64-pc-windows-gnu 2>&1 | tail -6 && cargo test remediation_actions 2>&1 | tail -8</automated>
  </verify>
  <done>Five arms dispatch to the actions; builds linux + windows-gnu; tests green.</done>
</task>

<task type="auto">
  <name>Task 3: unit tests — param validation + error paths</name>
  <files>agent-install/omni-agent-rs/src/capabilities/remediation_actions.rs</files>
  <action>Add `#[cfg(test)] mod tests`: each action with missing/invalid params returns `{status:error}` (no panic); block_ip rejects a malformed IP; rotate_key refuses a non-allowlisted target; restore_file refuses when no known-good source. Keep hermetic (do not actually kill processes / modify firewall in tests — test the param-validation + branching, gating real OS calls behind a guard).</action>
  <verify>
    <automated>cd agent-install/omni-agent-rs && cargo test remediation_actions 2>&1 | tail -10</automated>
  </verify>
  <done>Param-validation + refusal + error-path unit tests pass.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-53-03 | EoP | arbitrary process kill / service disable / key rotation | mitigate | Param validation + allowlisted targets for rotate_key; approval gate (53-04) before dispatch of destructive steps |
| T-53-04 | DoS | firewall rule locks out management | accept/mitigate | block_ip validates CIDR; rollback (unblock_ip) defined in the playbook |
| T-53-05 | Tampering | windows cross-build regression | mitigate | windows-gnu check in Task 2 |
</threat_model>

<verification>
- `cargo build` (linux) + `cargo check --target x86_64-pc-windows-gnu` clean; `cargo test remediation_actions` green.
- Five actions dispatch + return structured results; params validated; bounded; non-panicking.
</verification>

<success_criteria>
The agent exposes kill_process/restore_file/block_ip/rotate_key/disable_service as bounded, param-validated, non-panicking command arms returning structured results the engine verifies, building on linux + windows-gnu.
</success_criteria>

<output>
Create `.planning/phases/53-autonomous-remediation/53-02-SUMMARY.md` when done.
</output>
