---
phase: 53-autonomous-remediation
plan: 04
type: execute
wave: 3
depends_on: ["53-03"]
files_modified:
  - backend/autonomous_remediation_service.py
  - backend/remediation_control_endpoints.py
  - backend/router_registry.py
  - backend/tests/test_remediation_guards.py
autonomous: true
requirements: [AUTO-03, AUTO-04]
must_haves:
  truths:
    - "A remediation with any destructive step enters pending_approval and only dispatches after an operator approve; deny cancels it (approval gate)"
    - "On a failed verify: a REVERSIBLE action runs the playbook's rollback steps; an IRREVERSIBLE action (kill_process/patch_package) raises a human-escalation alert + audit flag, never an automated undo"
    - "Per-agent concurrent remediations are capped via a DB lease (holds across uvicorn workers); over the cap defers without deadlock; the lease self-releases on completion/timeout (TTL)"
    - "A tenant-scoped GET exposes the immutable remediation_audit trail; the approve/deny action is recorded as an override in the audit"
  artifacts:
    - path: "backend/autonomous_remediation_service.py"
      provides: "approval gate + rollback-on-failure + concurrency cap"
      contains: "pending_approval"
    - path: "backend/remediation_control_endpoints.py"
      provides: "approve/deny remediation + GET audit trail (tenant-scoped)"
      contains: "approve"
      min_lines: 40
  key_links:
    - from: "backend/remediation_control_endpoints.py"
      to: "backend/remediation_audit_service.py"
      via: "GET reads list_audit; approve/deny writes an override audit record"
      pattern: "list_audit"
    - from: "backend/router_registry.py"
      to: "backend/remediation_control_endpoints.py"
      via: "_load(app, 'remediation_control_endpoints', 'router')"
      pattern: "remediation_control_endpoints"
  prohibitions:
    - "MUST NOT dispatch a destructive step before approval when the gate applies"
    - "MUST NOT track concurrency in-process (in-memory map) — use a DB lease that holds across workers"
    - "MUST NOT auto-undo an irreversible action — escalate to a human instead"
    - "MUST NOT mutate audit records (approve/deny is a NEW override record)"
    - "MUST NOT access db._db"
---

<objective>
Add the AUTO-03 safety guards on top of the 53-03 loop — approval gate for destructive actions, rollback on verification failure, and a per-agent concurrency cap — plus an operator approve/deny + audit-read endpoint (AUTO-04 read).

Purpose: AUTO-03 + the human-override surface (AUTO-01/04).
Output: guards in the engine + control endpoints + tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@.planning/phases/53-autonomous-remediation/53-RESEARCH.md
@.planning/phases/53-autonomous-remediation/53-CONTEXT.md

# Read-first anchors:
@backend/autonomous_remediation_service.py   # the 53-03 remediate loop seams (is_destructive check, rollback hook, verify)
@backend/remediation_audit_service.py        # write_audit / list_audit
@backend/agent_fleet_geo_endpoints.py        # router/tenant pattern; router_registry _load seam
</execution_context>

<artifacts>
- EDIT `backend/autonomous_remediation_service.py` — approval gate + rollback + concurrency cap.
- NEW `backend/remediation_control_endpoints.py` — approve/deny + GET audit trail.
- EDIT `backend/router_registry.py` — register the router.
- NEW `backend/tests/test_remediation_guards.py` — guard + endpoint tests.
</artifacts>

<tasks>

<task type="tdd">
  <name>Task 1: failing tests for the guards + control endpoints</name>
  <files>backend/tests/test_remediation_guards.py</files>
  <action>Tests: (a) a playbook with a `destructive: true` step → `remediate` sets `pending_approval` and does NOT insert an agent_instruction until approved; POST approve → steps dispatch; POST deny → cancelled + audit override recorded; (b) a `failed` verify on a REVERSIBLE action (block_ip) dispatches the rollback (assert unblock_ip); a `failed` verify on an IRREVERSIBLE action (kill_process) raises a human-escalation alert + `escalated` flag and does NOT dispatch a rollback; (c) DB-lease concurrency: with `MAX_CONCURRENT_PER_AGENT` active leases for an agent, a new remediation defers (no dispatch); releasing/expiring a lease frees a slot; the count is read from the `remediation_inflight` collection (not process memory); (d) `GET /api/remediation/audit` returns the tenant's audit trail and rejects cross-tenant. Mock db + execute_plan + verify.</action>
  <verify>
    <automated>backend/venv/bin/python -m pytest backend/tests/test_remediation_guards.py -q 2>&1 | tail -12</automated>
  </verify>
  <done>Tests exist and FAIL.</done>
</task>

<task type="auto">
  <name>Task 2: approval gate + rollback + concurrency cap</name>
  <files>backend/autonomous_remediation_service.py</files>
  <read_first>the 53-03 seams (is_destructive check point, rollback hook, verify result).</read_first>
  <action>At the pre-dispatch seam: if the selected playbook has any `destructive: true` step (and the tenant policy requires approval), set the remediation `pending_approval`, write an audit record, and STOP before dispatch until approved. On a `failed` verify: if the playbook has `rollback` steps AND the failed action is reversible, dispatch the rollback steps (via execute_plan); if the action is irreversible (kill_process/patch_package) or no rollback is defined, do NOT guess — raise a critical human-escalation alert (via the existing alert path) + set `rollback_unavailable`/`escalated` on the record + audit it. Add concurrency tracking via a **DB lease**: a `remediation_inflight` collection keyed by agentId with a TTL/heartbeat; acquire before dispatch, over `MAX_CONCURRENT_PER_AGENT` (count active leases for the agent) → defer/re-queue rather than dispatch; release the lease on completion, and let the TTL expire it on a crash/timeout (works across uvicorn workers, never deadlocks). Keep DRY_RUN + severity ceiling + dedup.</action>
  <verify>
    <automated>backend/venv/bin/python -m pytest backend/tests/test_remediation_guards.py -q -k "approval or rollback or concurren" 2>&1 | tail -8</automated>
  </verify>
  <done>Approval gate blocks destructive pre-approval; rollback on failure; concurrency cap defers + releases.</done>
</task>

<task type="auto">
  <name>Task 3: approve/deny + audit-read endpoints + register</name>
  <files>backend/remediation_control_endpoints.py, backend/router_registry.py</files>
  <read_first>agent_fleet_geo_endpoints.py for router/tenant/permission pattern.</read_first>
  <action>Create `remediation_control_endpoints.py`: `POST /api/remediation/{id}/approve` + `/deny` (permission-gated to an ops role; approve resumes dispatch, deny cancels; both write an override audit record via write_audit), and `GET /api/remediation/audit` (tenant-scoped, reads list_audit, optional filters). Wrapped db, never db._db. Register `_load(app, "remediation_control_endpoints", "router")`.</action>
  <verify>
    <automated>backend/venv/bin/python -m pytest backend/tests/test_remediation_guards.py -q 2>&1 | tail -8</automated>
  </verify>
  <done>Approve/deny + audit GET work, tenant-scoped, override recorded; router registered.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-53-09 | EoP | destructive action auto-runs without human sign-off | mitigate | Approval gate default-on for destructive; deny cancels; recorded |
| T-53-10 | Integrity | failed remediation leaves system worse | mitigate | Rollback on failed verify; missing-rollback flagged not guessed |
| T-53-11 | DoS | remediation storm on one agent | mitigate | MAX_CONCURRENT_PER_AGENT cap; defer + release |
| T-53-12 | Info Disclosure | cross-tenant audit/approve | mitigate | Tenant-scoped + permission-gated; wrapped db |
</threat_model>

<verification>
- `pytest test_remediation_guards.py` green (approval gate, rollback, concurrency cap, audit GET, cross-tenant reject).
- Full backend `tests/` (known ignores) shows no new failures.

<human-check>End-to-end: a seeded malicious file (EICAR) / a vuln / a FIM drift triggers a remediation; a destructive playbook waits for approval; approve → agent executes → verify → resolved; force a verify failure → rollback runs; confirm the immutable audit trail shows finding/playbook/steps/verification/override.</human-check>
</verification>

<success_criteria>
Destructive remediations require operator approval, verification failures roll back, per-agent concurrency is capped without deadlock, and the immutable audit trail is readable per-tenant with overrides recorded — completing AUTO-03/04 on the 53-03 loop.
</success_criteria>

<output>
Create `.planning/phases/53-autonomous-remediation/53-04-SUMMARY.md` when done.
</output>
