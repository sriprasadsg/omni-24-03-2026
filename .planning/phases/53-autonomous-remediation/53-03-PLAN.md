---
phase: 53-autonomous-remediation
plan: 03
type: execute
wave: 2
depends_on: ["53-01"]
files_modified:
  - backend/autonomous_remediation_service.py
  - backend/remediation_audit_service.py
  - backend/tests/test_autonomous_remediation_loop.py
autonomous: true
requirements: [AUTO-01, AUTO-04]
must_haves:
  truths:
    - "scan_for_remediable_findings also surfaces NSCAN (malicious scan), VULN, and FIM findings as RemediationFindings"
    - "The engine selects a YAML playbook (53-01) for a finding, dispatches its steps via the existing agent_instructions queue, then verifies the fix (re-run the finding's check)"
    - "Verification yields resolved | unverified | failed; the engine emits a completion event; unverified never reported as fixed"
    - "Every remediation writes an append-only remediation_audit record (finding, playbook, steps, verification, override) that is never mutated after write"
  artifacts:
    - path: "backend/autonomous_remediation_service.py"
      provides: "new finding sources + playbook selection + execute + verify loop + completion"
      contains: "select_playbook"
    - path: "backend/remediation_audit_service.py"
      provides: "append-only remediation_audit writer (insert-only)"
      contains: "remediation_audit"
      min_lines: 35
  key_links:
    - from: "backend/autonomous_remediation_service.py"
      to: "backend/remediation_playbook_service.py"
      via: "select_playbook(finding) then execute steps"
      pattern: "select_playbook"
    - from: "backend/autonomous_remediation_service.py"
      to: "backend/remediation_audit_service.py"
      via: "write_audit at each remediation transition"
      pattern: "remediation_audit"
  prohibitions:
    - "MUST NOT report an unverified fix as resolved"
    - "MUST NOT build a second dispatch path — reuse execute_plan/agent_instructions"
    - "MUST NOT update or delete a remediation_audit record after insert (append-only)"
    - "MUST NOT access db._db"
---

<objective>
Close the loop: extend the engine to consume the native findings, select a YAML playbook, execute its steps (existing dispatch), verify the fix, emit completion, and write an immutable audit record at each step.

Purpose: AUTO-01 (finding→playbook→execute→verify→complete) + AUTO-04 (immutable audit). D-01/D-05.
Output: engine loop + `remediation_audit_service` + tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@.planning/phases/53-autonomous-remediation/53-RESEARCH.md
@.planning/phases/53-autonomous-remediation/53-CONTEXT.md

# Read-first anchors:
@backend/autonomous_remediation_service.py   # scan_for_remediable_findings + execute_plan (reuse) + _log_decision
@backend/remediation_playbook_service.py     # select_playbook + ACTION_MAP (53-01)
@backend/agent_security_scan_endpoints.py    # NSCAN verdict store (finding source)
@backend/vuln_service.py                      # VULN store (finding source); fim_events (FIM source)
</execution_context>

<artifacts>
- EDIT `backend/autonomous_remediation_service.py` — new finding sources + playbook loop + verify + completion.
- NEW `backend/remediation_audit_service.py` — append-only audit writer.
- NEW `backend/tests/test_autonomous_remediation_loop.py` — loop + verify + audit tests.
</artifacts>

<tasks>

<task type="tdd">
  <name>Task 1: failing tests for the remediate loop + audit</name>
  <files>backend/tests/test_autonomous_remediation_loop.py</files>
  <action>Hermetic tests (mock db + the agent result): (a) a malicious NSCAN verdict / a VULN finding / a FIM drift each surface as a RemediationFinding; (b) `remediate(finding)` selects the matching playbook (53-01), dispatches its steps via execute_plan (assert agent_instructions inserts), and calls verify; (c) verify returning "still present" → status `failed` (not resolved); verify returning "cleared" → `resolved`; a verify timeout → `unverified` (never `resolved`); (d) an append-only `remediation_audit` record is written with finding/playbook/steps/verification, and the audit service exposes no update/delete. Mock `remediation_playbook_service.select_playbook` + the verify check + get_database.</action>
  <verify>
    <automated>backend/venv/bin/python -m pytest backend/tests/test_autonomous_remediation_loop.py -q 2>&1 | tail -12</automated>
  </verify>
  <done>Tests exist and FAIL.</done>
</task>

<task type="auto">
  <name>Task 2: audit service (append-only)</name>
  <files>backend/remediation_audit_service.py</files>
  <read_first>the remediation_escalations / location-history append-only write pattern.</read_first>
  <action>Create `remediation_audit_service.py`: `async def write_audit(db, tenant_id, record)` inserts into `remediation_audit` (insert-only — no update/delete methods exposed); records carry `remediation_id, tenantId, agentId, finding, playbook, steps_dispatched, verification_result, override, ts`. Tenant-scoped. `async def list_audit(db, tenant_id, filters)` for the read (used by 53-04). Use the wrapped db; never db._db.</action>
  <verify>
    <automated>backend/venv/bin/python -m pytest backend/tests/test_autonomous_remediation_loop.py -q -k audit 2>&1 | tail -6</automated>
  </verify>
  <done>Append-only audit writer + reader; no mutation methods.</done>
</task>

<task type="auto">
  <name>Task 3: finding sources + playbook loop + verify + completion</name>
  <files>backend/autonomous_remediation_service.py</files>
  <read_first>scan_for_remediable_findings + execute_plan.</read_first>
  <action>Extend `scan_for_remediable_findings` to also read NSCAN malicious verdicts (`security_scan_results`), VULN findings (`vulnerabilities`), and FIM drift (`fim_events`) into `RemediationFinding`s (finding_type nscan/vuln/fim). Add `async def remediate(finding)`: `select_playbook(finding)` → for each step, `execute_plan(step_as_plan)` (reuse the existing dispatch + DRY_RUN + dedup) → after steps, `verify(finding)` re-runs the finding's own check within a bounded timeout → `resolved|unverified|failed` (unverified/failed NEVER reported as resolved) → emit a completion event → `write_audit(...)` at selection, post-dispatch, and post-verify. Rollback + approval gate + concurrency cap are added in 53-04 (leave clean seams: a `requires_approval`/`is_destructive` check point before dispatch, and a rollback hook after a `failed` verify).</action>
  <verify>
    <automated>backend/venv/bin/python -m pytest backend/tests/test_autonomous_remediation_loop.py -q 2>&1 | tail -8</automated>
  </verify>
  <done>New findings ingested; playbook loop + verify + completion + audit writes; unverified≠resolved; reuses execute_plan.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-53-06 | Tampering | audit record altered to hide a bad remediation | mitigate | Append-only collection; no update/delete methods (D-05) |
| T-53-07 | Integrity | unverified fix marked resolved | mitigate | verify → resolved only on a definite clear; timeout → unverified |
| T-53-08 | Info Disclosure | cross-tenant finding/audit | mitigate | tenant-scoped reads + wrapped db |
</threat_model>

<verification>
- `pytest test_autonomous_remediation_loop.py` green (finding sources, playbook loop, verify states, append-only audit).
- Reuses execute_plan/agent_instructions; no second dispatch path; audit never mutated.

<sequencing_note>Depends on 53-01 (playbook service). Guards (53-04) extend the seams left here.</sequencing_note>
</verification>

<success_criteria>
The engine ingests NSCAN/VULN/FIM findings, selects a YAML playbook, executes its steps via the existing dispatch, verifies the fix (never marking unverified as resolved), emits completion, and writes an immutable audit record at each step.
</success_criteria>

<output>
Create `.planning/phases/53-autonomous-remediation/53-03-SUMMARY.md` when done.
</output>
