---
phase: 29-public-trust-center
plan: 03
type: execute
wave: 2
depends_on: ["29-01"]
files_modified:
  - services/apiService.ts
  - components/TrustCenter.tsx
autonomous: true
requirements: [TRUST-01, TRUST-03]
tags: [trust-center, admin-ui, react, toast, accessibility, custom-domain]

must_haves:
  truths:
    - "An admin can edit the trust profile (company name, description, contact email, logo, frameworks, public + private documents) and save it to the DB-backed PUT /api/trust-center/profile."
    - "An admin sees the read-only Public Trust Page URL ({PLATFORM_URL}/trust/{trust_slug}) with a working Copy Link action."
    - "An admin can set an optional Custom Domain that persists via the profile update route."
    - "Approve/Deny actions show a success/error toast (previously silent) and the icon-only buttons carry aria-labels."
  artifacts:
    - "services/apiService.ts — updateTrustProfile(updates) PUT client function"
    - "components/TrustCenter.tsx — profile edit form, Custom Domain field, Public Trust Page URL + Copy Link, approve/deny toasts, aria-labels"
  key_links:
    - "updateTrustProfile posts to PUT /api/trust-center/profile — the route extended in 29-01 that persists profile fields to trust_profiles and trust_domain to db.tenants."
    - "The admin edit form's private_documents URL is an admin-only reference; it is never surfaced to the public page (public URL stripping is server-side in 29-02)."
---

<objective>
Close the two real frontend gaps this phase requires: `TrustCenter.tsx` currently renders the profile read-only (no edit form) and `apiService.ts` has no client function for the already-existing `PUT /api/trust-center/profile` route. Add the profile edit form, the TRUST-03 admin surface (read-only Public Trust Page URL + Copy Link, editable Custom Domain field), the missing `updateTrustProfile()` API function, and the small UX/accessibility additions the UI-SPEC flagged (approve/deny toasts + aria-labels on the existing icon-only buttons).

Purpose: TRUST-01's admin management view must actually write the now-DB-backed profile; TRUST-03 needs an admin surface to set the custom domain and copy the public link. These are gaps in the existing file, not new scope.
Output: `updateTrustProfile()` in `apiService.ts` and an incrementally-extended `TrustCenter.tsx` (edit form + sharing/domain field + toasts + aria-labels) that type-checks and builds.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-public-trust-center/29-PATTERNS.md
@.planning/phases/29-public-trust-center/29-UI-SPEC.md
@components/TrustCenter.tsx
@services/apiService.ts
</context>

## Artifacts this phase produces (this plan)

- **`services/apiService.ts` new function:** `updateTrustProfile(updates: Partial<TrustProfile>): Promise<TrustProfile>` — `PUT ${API_BASE}/trust-center/profile`, mirrors the adjacent `updateTrustRequest` shape (authFetch, no local-cache sync). `TrustProfile` is already imported (line 12); no new import needed.
- **`components/TrustCenter.tsx` new state/handlers:** `isEditing` boolean state; `editForm` state (a draft `TrustProfile` + `trust_domain`); `handleSaveProfile` (calls `updateTrustProfile`, toasts success/error); `handleCopyLink` (clipboard + toast); extended `handleRequestAction` (adds success/error toasts); `trustSlug`/`trustDomain` derived from the profile fetch response. New imports: `showToast` from `../utils/toast`; add/remove-row controls for `compliance_frameworks[]`, `public_documents[]`, `private_documents[]`.
- **`components/TrustCenter.tsx` interface extension:** `TrustProfile` interface (local) gains optional `trust_slug?: string` and `trust_domain?: string` fields to receive the 29-01-augmented GET /profile response.
- **Accessibility:** `aria-label="Approve request"` / `aria-label="Deny request"` on the existing `UserCheck`/`XCircle` buttons.

<tasks>

<task type="auto">
  <name>Task 1: Add updateTrustProfile() to apiService.ts</name>
  <files>services/apiService.ts</files>
  <read_first>
    - services/apiService.ts lines 1411-1441 (fetchTrustProfile / fetchTrustRequests / updateTrustRequest — the sibling shape and exact placement to insert below)
    - services/apiService.ts line 12 (TrustProfile already imported), line 35 (API_BASE = '/api'), authFetch definition
    - .planning/phases/29-public-trust-center/29-PATTERNS.md (§ services/apiService.ts — the exact recommended updateTrustProfile body)
  </read_first>
  <action>
    Insert `updateTrustProfile` directly below `updateTrustRequest` (line ~1441), matching that function's shape exactly: `export const updateTrustProfile = async (updates: Partial<TrustProfile>): Promise<TrustProfile> => { const res = await authFetch(`${API_BASE}/trust-center/profile`, { method: 'PUT', body: JSON.stringify(updates) }); if (!res.ok) throw new Error("Failed to update trust profile"); return await res.json(); };`. Do not add a new import (TrustProfile already imported). Do not add local-cache sync (there is no in-memory array for trust profiles).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "apiService" || echo "apiService typechecks clean"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export const updateTrustProfile" services/apiService.ts` returns 1.
    - The function targets `PUT ${API_BASE}/trust-center/profile` via `authFetch` and returns the parsed JSON.
    - `npx tsc --noEmit` reports no new errors referencing `apiService.ts`.
  </acceptance_criteria>
  <done>apiService.ts exposes updateTrustProfile() calling the existing PUT /api/trust-center/profile route.</done>
</task>

<task type="auto">
  <name>Task 2: Profile edit form + Custom Domain field + Public Trust Page URL / Copy Link</name>
  <files>components/TrustCenter.tsx</files>
  <read_first>
    - components/TrustCenter.tsx (full file — lines 1-16 interface, 27-51 state/fetch, 105-165 read-only profile render to extend, 168-186 side panel)
    - services/apiService.ts (updateTrustProfile from Task 1; fetchTrustProfile returns trust_slug/trust_domain after 29-01)
    - utils/toast.ts + components/ToastContainer.tsx (showToast API — success/error variants)
    - .planning/phases/29-public-trust-center/29-UI-SPEC.md (Surface B: Typography/Color/Copywriting Contract, Component Inventory — label style `text-sm font-medium text-gray-900 dark:text-white mb-1`, copy for "Save Changes", "Public Trust Page URL", "Copy Link", "Custom Domain (optional)" + helper text)
  </read_first>
  <action>
    Extend the local `TrustProfile` interface with optional `trust_slug?: string` and `trust_domain?: string`. Add `isEditing` + `editForm` state. Add an "Edit Profile" button on the Profile tab that toggles the read-only render into an editable form with fields: company name, description (textarea), contact email, logo URL, and add/remove-row editors for `compliance_frameworks[]`, `public_documents[]` (name+url), and `private_documents[]` (name+url — admin-only reference). Reuse the file's existing Tailwind conventions (label `text-sm font-medium text-gray-900 dark:text-white mb-1`, `text-sm` inputs) — do NOT introduce new type sizes/weights or hues (UI-SPEC Surface B). Add a "Sharing" sub-section inside the Profile tab: a read-only "Public Trust Page URL" showing `{base}/trust/{profile.trust_slug}` (derive base from `window.location.origin`) with a "Copy Link" button (`navigator.clipboard.writeText` → `showToast('Public link copied to clipboard', 'success')`), and an editable "Custom Domain (optional)" text input bound to `trust_domain` with the exact helper text from the Copywriting Contract. Add `handleSaveProfile` that calls `api.updateTrustProfile(editForm)` (including `trust_domain`), on success sets `profile`, exits edit mode, and `showToast('Trust profile saved.', 'success')`; on failure `showToast('Could not save trust profile. Please try again.', 'error')`. Import `showToast` from `../utils/toast`. If these additions push the file over 500 lines (CLAUDE.md limit), extract the edit form into a new `components/TrustProfileEditForm.tsx` and import it (precedent: 08-02 BulkEvidenceUploadModal split).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` succeeds (Vite production build, no TS errors).
    - `grep -c "updateTrustProfile\|showToast\|Custom Domain\|Public Trust Page URL\|Copy Link" components/TrustCenter.tsx` ≥ 5 (all four copy strings + the API call present).
    - The "Public Trust Page URL" renders `.../trust/{trust_slug}` and Copy Link calls `navigator.clipboard.writeText` + `showToast(..., 'success')`.
    - The Custom Domain input is bound to and submitted as `trust_domain`.
    - `components/TrustCenter.tsx` (or the extracted form file) stays ≤ 500 lines.
  </acceptance_criteria>
  <done>Admins can edit and save the DB-backed trust profile, copy the public link, and set a custom domain — all through the existing authenticated routes.</done>
</task>

<task type="auto">
  <name>Task 3: Approve/Deny success+error toasts and aria-labels</name>
  <files>components/TrustCenter.tsx</files>
  <read_first>
    - components/TrustCenter.tsx lines 53-62 (handleRequestAction — currently updates state silently, no toast) and lines 219-236 (the icon-only UserCheck/XCircle buttons to label)
    - utils/toast.ts (showToast success/error signature)
    - .planning/phases/29-public-trust-center/29-UI-SPEC.md (Copywriting Contract Surface B: "Request approved."/"Request denied." success toasts; "Could not update request status. Please try again." error toast; accessibility note requiring aria-label on the icon-only buttons; explicit instruction NOT to add a confirmation modal)
  </read_first>
  <action>
    In `handleRequestAction`, after a successful `api.updateTrustRequest(...)`, call `showToast(status === 'Approved' ? 'Request approved.' : 'Request denied.', 'success')`; in the catch block call `showToast('Could not update request status. Please try again.', 'error')`. Add `aria-label="Approve request"` to the `UserCheck` button and `aria-label="Deny request"` to the `XCircle` button (keep the existing `title` attributes). Do NOT add a confirmation modal or change the reversible status-transition interaction model (UI-SPEC: incremental, not a redesign). Reuse the `showToast` import added in Task 2.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` succeeds.
    - `grep -c "aria-label=\"Approve request\"\|aria-label=\"Deny request\"" components/TrustCenter.tsx` returns 2.
    - `handleRequestAction` calls `showToast(..., 'success')` on success and `showToast(..., 'error')` in the catch block.
    - No confirmation modal was added to Approve/Deny (the icon-only buttons remain, now labelled).
  </acceptance_criteria>
  <done>Approve/Deny give clear toast feedback and are screen-reader accessible, with no change to the interaction model.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated admin browser → API | All admin actions use authFetch (JWT bearer); there is no unauthenticated data path in this plan |
| admin edit form → trust profile | Admin supplies profile fields incl. private_documents URLs (admin-only reference), submitted to the authenticated PUT route |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Status | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|--------|-----------------|
| T-29-01 | Information Disclosure | admin routes consumed by TrustCenter.tsx | high | mitigate | closed | UI reads/writes exclusively via `authFetch` (JWT); approve/deny and profile-save hit the authenticated `_TRUST_ADMIN_ROLES`-gated routes; no public data path is introduced client-side. Server-side gate verified by 29-01/29-02 `-k admin_auth`. |
| T-29-03 | Information Disclosure | private_documents URL in the admin edit form | medium | mitigate | closed | Private-document URLs entered here are the admin's internal reference only; the public surface strips them server-side (29-02 `_public_view`) — the frontend never sends them to the public page and never constructs a public link from them. |
| T-29-07 | Input Validation | Custom Domain field value | low | mitigate | closed | The field submits a bare hostname string; the backend (29-01) normalizes/length-caps it before persisting to db.tenants — the client does not trust-format it into any query. |
| T-29-SC | Tampering | npm installs | low | accept | closed | No new npm packages (lucide-react + Tailwind + utils/toast are all pre-existing) — no install checkpoint required. |
</threat_model>

<verification>
- `npm run build` — production build succeeds with the new form, sharing section, toasts, and aria-labels.
- `npx tsc --noEmit` — no new type errors.
- Manual (phase gate, per 29-VALIDATION.md Manual-Only): log in as an admin, edit + save the profile, copy the public link, set a custom domain, approve/deny a request and confirm the toast appears.
</verification>

<success_criteria>
- Admin can edit/save the DB-backed trust profile and set a custom domain via the existing authenticated routes.
- Public Trust Page URL is shown with a working Copy Link.
- Approve/Deny give toast feedback and are accessible (aria-labels), with no interaction-model change.
- Build and typecheck pass.
</success_criteria>

<output>
Create `.planning/phases/29-public-trust-center/29-03-SUMMARY.md` when done.
</output>
