---
phase: 05-integration-and-e2e-verification
plan: 01
type: execute
wave: 1
depends_on: ["05-00"]
files_modified:
  - backend/tests/test_e2e_integration.py
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [INT-01, INT-02, INT-03, INT-04, INT-05, INT-08, INT-09, INT-10, INT-13, INT-14, INT-15]
tags: [integration, tenant-isolation, regression, golden-path, compliance]

must_haves:
  truths:
    - "A golden-path integration test exercises the full flow: heartbeat evidence → manual upload → export sees both → remediation task creation — all within one tenant"
    - "Cross-tenant isolation is proven for Phase 2 upload, Phase 3 report download, and Phase 4 task CRUD (tenant-A cannot touch tenant-B data)"
    - "The existing Python agent evidence flow still works after Phase 4's agent_tasks_endpoints.py changes (no regression)"
    - "STATE.md and ROADMAP.md mark Phase 5 complete with Phase 5 plan list filled in"
  artifacts:
    - path: "backend/tests/test_e2e_integration.py"
      provides: "Golden-path, cross-tenant isolation, and regression integration tests (replaces Wave 0 placeholder)"
      min_lines: 200
    - path: ".planning/STATE.md"
      provides: "Phase 5 marked complete; progress updated to 5/5 phases"
      contains: "Integration and E2E Verification"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 5 checkbox checked, Plans list populated"
      contains: "Phase 5: Integration and E2E Verification"
  key_links:
    - from: "backend/tests/test_e2e_integration.py:golden_path"
      to: "backend/compliance_evidence_processor.py:process_automated_evidence"
      via: "direct async call with mocked tenant_context"
      pattern: "process_automated_evidence"
    - from: "backend/tests/test_e2e_integration.py:cross_tenant"
      to: "backend/compliance_reports_endpoints.py + compliance_remediation_endpoints.py"
      via: "TestClient with tenant-mismatched dependency_overrides"
      pattern: "dependency_overrides"
---

<objective>
Replace the Wave 0 golden-path placeholder with the real cross-phase integration suite: golden path, cross-tenant isolation, and regression coverage. Then mark Phase 5 complete in STATE.md and ROADMAP.md.

This is a verification-and-fix phase: no new features and no new source files. All work lands in the single test file created in Wave 0 plus two planning-doc updates.

Purpose: Prove all four prior capabilities (Rust agent evidence, manual uploads, audit export, remediation workflow) compose into one coherent tenant-scoped portal, and that no tenant boundary leaks. Depends on 05-00 because the golden path and remediation tests require the GAP-1 fix to be live.

Output: Integration tests added to backend/tests/test_e2e_integration.py; STATE.md and ROADMAP.md finalized.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-integration-and-e2e-verification/05-RESEARCH.md
@.planning/phases/05-integration-and-e2e-verification/05-PATTERNS.md

# Seam files under test (read-only verification — do NOT modify)
@backend/compliance_evidence_processor.py
@backend/compliance_evidence_endpoints.py
@backend/compliance_reporting_data.py
@backend/compliance_remediation_endpoints.py
@backend/agent_tasks_endpoints.py

# Patterns and contracts
@backend/tests/test_audit_export.py
@backend/tests/test_rust_heartbeat_parity.py
@backend/tests/conftest.py
@backend/auth_types.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Golden-path integration test — heartbeat → upload → export → remediation, single tenant</name>
  <files>backend/tests/test_e2e_integration.py</files>
  <behavior>
    - test_golden_path_evidence_to_remediation (INT-01, INT-03, INT-05): within tenant-a, simulate the full chain and assert each seam's contract holds:
      1. process_automated_evidence(hostname, compliance_data, db, agent_type="rust") writes an asset_compliance evidence record with systemGenerated=True and agent_type="rust" (inspect db.asset_compliance.update_one.call_args_list for the $push evidence dict and $set agent_type — pattern from test_rust_heartbeat_parity.py).
      2. A manual evidence record (source="manual", systemGenerated=False, uploaded_by set, tenantId="tenant-a") can coexist in the same evidence[] array — assert both an auto record and a manual record are accepted by _flatten_evidence.
      3. _flatten_evidence([auto_ev, manual_ev]) returns auto_count=1, manual_count=1, count=2, with names prefixed "[Auto]" and "[Manual]" (INT-04 contract from compliance_reporting_data).
      4. create_task via compliance_remediation_endpoints with a tenant-a TokenData user returns a task stamped with tenant-a (uses the GAP-1-fixed _tenant_filter — confirms Wave 0 unblocked the loop). Mock svc.create_task / get_database appropriately.
    - The test must use the tenant_context sys.modules mock (patch.dict("sys.modules", {"tenant_context": ctx})) before importing compliance_evidence_processor, per PATTERNS.md Pitfall 5.
  </behavior>
  <action>
    Remove the test_golden_path_placeholder (the skipped stub from Wave 0) and implement the real golden-path test in backend/tests/test_e2e_integration.py. Follow the direct-async-call + mock-DB pattern from test_rust_heartbeat_parity.py for the processor leg, and reuse the _make_user(role, tenant_id) → TokenData helper for the remediation leg.

    For the processor leg: build a mock db where db.asset_compliance.update_one is an AsyncMock and db.assets.find_one returns an asset doc with tenantId="tenant-a" so tenant resolution succeeds. Mock tenant_context via patch.dict("sys.modules", {"tenant_context": ctx}) where ctx.get_tenant_id returns None and ctx.set_tenant_id is a MagicMock. Drive process_automated_evidence with a compliance_data dict containing at least one check whose name is in COMPLIANCE_CHECK_MAPPINGS (e.g. "Windows Defender") so an evidence $push actually fires; assert agent_type="rust" appears in the $set.

    For the _flatten_evidence leg: import from compliance_reporting_data and assert the auto/manual counts and "[Auto]"/"[Manual]" name prefixes exactly as test_audit_export.py::test_evidence_source_labelling does.

    For the remediation leg: patch.object(compliance_remediation_endpoints, "get_database", ...) and patch svc.create_task to echo back the tenant_filter it received; assert the call carried tenant_filter={"tenantId": "tenant-a"} and created_by derived from the TokenData username. Do NOT modify any source file. Keep the file under 500 lines.
  </action>
  <verify>
    <automated>backend/venv/bin/python3 -m pytest backend/tests/test_e2e_integration.py -k "golden_path" -v --tb=short 2>&1 | grep -E "PASSED|FAILED|ERROR" | grep -v '^#'</automated>
  </verify>
  <done>test_golden_path_evidence_to_remediation PASSES: rust evidence written with agent_type, auto+manual evidence flatten to counts 1/1, and a tenant-a remediation task is created with the correct tenant_filter. Placeholder removed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cross-tenant isolation tests — upload, report download, task CRUD all blocked</name>
  <files>backend/tests/test_e2e_integration.py</files>
  <behavior>
    - test_cross_tenant_report_download_blocked (INT-07, INT-15): TestClient over compliance_reports_endpoints.router with get_current_user overridden to a tenant-a TokenData; db.compliance_reports.find_one returns a doc with tenantId="tenant-b"; GET /api/compliance/reports/download/{filename} returns 403. (Mirror test_audit_export.py::test_legacy_download_blocks_cross_tenant.)
    - test_cross_tenant_report_download_owner_allowed: same setup but report tenantId="tenant-a" → status != 403.
    - test_cross_tenant_report_list_shows_own_only (INT-15, GAP-2): TestClient GET /api/compliance/reports with tenant-a caller; mock db.compliance_reports.find(...).to_list returns mixed tenant docs but the endpoint query filter restricts to tenant-a; assert response contains only tenant-a filenames (relies on Wave 0 Task 2 fix).
    - test_cross_tenant_task_list_blocked (INT-14): list_tasks with a tenant-a TokenData passes tenant_filter={"tenantId":"tenant-a"} into svc.list_tasks (assert via patched svc), so tenant-b tasks are never queried. Super-admin TokenData → tenant_filter={} (test_super_admin_sees_all_tasks).
    - test_cross_tenant_evidence_upload_blocked (INT-13): upload_compliance_evidence called for an asset whose db doc has tenantId="tenant-b" with a tenant-a caller → raises HTTPException 403 (asset ownership check). Use the direct-async-call pattern from test_evidence_uploads.py.
  </behavior>
  <action>
    Add the cross-tenant isolation tests to backend/tests/test_e2e_integration.py. For HTTP-layer 403 checks use fastapi.testclient.TestClient with app.dependency_overrides[get_current_user] = lambda: caller and raise_server_exceptions=False, and patch.object(module, "get_database", return_value=mock_db, create=True) — exactly the pattern in test_audit_export.py lines 82–105 and PATTERNS.md "Two-tenant TestClient pattern". The rule under test is: caller.tenant_id != resource.tenantId → 403.

    For the remediation task-list isolation check, patch.object(compliance_remediation_endpoints, "get_database", ...) and patch svc.list_tasks to capture the tenant_filter argument; assert it equals {"tenantId": "tenant-a"} for a tenant user and {} for a super-admin (_SUPER_ADMIN_ROLES). For the evidence-upload block, follow test_evidence_uploads.py: mock db.assets.find_one to return a tenant-b asset, call upload_compliance_evidence with a tenant-a TokenData, and assert it raises HTTPException with status 403.

    Do NOT modify any source file — these tests verify existing tenant-isolation behavior plus the Wave 0 GAP-2 fix. Keep the file under 500 lines.
  </action>
  <verify>
    <automated>backend/venv/bin/python3 -m pytest backend/tests/test_e2e_integration.py -k "cross_tenant or super_admin" -v --tb=short 2>&1 | grep -E "PASSED|FAILED|ERROR" | grep -v '^#'</automated>
  </verify>
  <done>All cross-tenant tests PASS: cross-tenant report download → 403, owner download → not 403, report list → own-tenant only, task list filter tenant-scoped (super-admin empty), cross-tenant evidence upload → 403.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Regression test for Python agent evidence flow + mark Phase 5 complete</name>
  <files>backend/tests/test_e2e_integration.py, .planning/STATE.md, .planning/ROADMAP.md</files>
  <behavior>
    - test_python_agent_evidence_regression (INT-08 regression): process_automated_evidence(hostname, compliance_data, db) called WITHOUT agent_type (default None) — i.e. the legacy Python agent path — still writes asset_compliance evidence records. Assert update_one fired with a $push evidence record and that absence of agent_type does not break the write (agent_type was added as an optional trailing param in Phase 1, so old callers must still work). This guards against regression from Phase 4's agent_tasks_endpoints.py modification, which re-routes the result path through process_automated_evidence.
    - test_report_instruction_result_path_intact: confirm agent_tasks_endpoints imports/uses process_automated_evidence without requiring agent_type (signature back-compat) — assert via inspect that agent_type and fallback_tenant_id both default to None so existing 3-arg calls remain valid.
  </behavior>
  <action>
    Add the two regression tests to backend/tests/test_e2e_integration.py. Use the tenant_context sys.modules mock and direct-async-call pattern. For test_python_agent_evidence_regression, call process_automated_evidence with only (hostname, compliance_data, db) — no agent_type — and assert the evidence $push still occurs (the Python flow must be byte-for-byte compatible with pre-Phase-1 behavior aside from the optional agent_type tag). For the signature back-compat check, use inspect.signature(process_automated_evidence) and assert agent_type and fallback_tenant_id both have default None so all existing 3-positional-arg call sites (including agent_tasks_endpoints.report_instruction_result) remain valid.

    Then update the planning docs:
    - .planning/STATE.md: set status/progress to reflect Phase 5 complete (completed_phases: 5, percent: 100), update the Phases table row "5 | Integration and E2E Verification" to Complete, and update the Last Session / stopped_at line to note Phase 5 plans 05-00 and 05-01 done. Append a Decisions entry noting the three gap fixes (GAP-1 getattr on TokenData, GAP-2 db.compliance_reports tenant filter, GAP-3 fallback_tenant_id).
    - .planning/ROADMAP.md: check the Phase 5 checkbox in the Phases list (line 16), fill in the Phase 5 "Plans: TBD" with the two plans (05-00 bug fixes, 05-01 integration tests) including the Wave 0 / Wave 1 structure, and update the Progress Table row for Phase 5 to "2/2 | Complete | 2026-06-18".

    Do NOT touch any backend source file in this task. Do NOT fix the 96 pre-existing unrelated test failures (out of scope per 05-RESEARCH). Keep test_e2e_integration.py under 500 lines.
  </action>
  <verify>
    <automated>backend/venv/bin/python3 -m pytest backend/tests/test_e2e_integration.py -v --tb=short 2>&1 | grep -E "passed|failed|error" | grep -v '^#'</automated>
  </verify>
  <done>Full test_e2e_integration.py suite PASSES (golden path + cross-tenant + regression, no skips remaining). STATE.md and ROADMAP.md show Phase 5 complete with both plans listed and Phase 5 checkbox checked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tenant-A JWT → tenant-B resource | The core boundary verified by this plan; every cross-tenant test asserts a 403 or empty result |
| Python agent (legacy path) → evidence processor | Regression boundary: old 3-arg calls must still write evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Information Disclosure | Cross-tenant report download / list | mitigate | TestClient tests assert 403 and own-tenant-only listing (Tasks 2) |
| T-05-05 | Information Disclosure | Cross-tenant remediation task visibility | mitigate | Assert tenant_filter scopes svc.list_tasks; super-admin empty filter is intentional (Task 2) |
| T-05-06 | Elevation of Privilege | Cross-tenant evidence upload to foreign asset | mitigate | Assert upload_compliance_evidence raises 403 on tenant mismatch (Task 2) |
| T-05-07 | Denial of Service | Regression breaking legacy Python evidence flow | mitigate | Assert 3-arg process_automated_evidence still writes evidence (Task 3) |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages introduced this phase; nothing to install |
</threat_model>

<verification>
Full Phase 5 integration suite green:

backend/venv/bin/python3 -m pytest backend/tests/test_e2e_integration.py -v --tb=short

Phase-specific regression baseline unchanged (per 05-RESEARCH Validation Architecture full phase gate):

backend/venv/bin/python3 -m pytest backend/tests/test_rust_heartbeat_parity.py backend/tests/test_evidence_uploads.py backend/tests/test_audit_export.py backend/tests/test_remediation_workflow.py backend/tests/test_tenant_isolation.py backend/tests/test_tenant_security.py backend/tests/test_e2e_integration.py -v --tb=short

Frontend build must still pass (no source touched, but confirm):

npm run build

The 96 pre-existing unrelated failures remain OUT OF SCOPE and must NOT be modified.
</verification>

<success_criteria>
- Golden-path test proves heartbeat evidence → manual upload coexistence → export labelling → tenant-scoped remediation task creation
- Cross-tenant tests prove upload, report download/list, and task CRUD all block or scope by tenant; super-admin sees all
- Regression test proves legacy Python agent evidence flow (3-arg call) still writes evidence after Phase 4 changes
- No new packages, no new source files, no backend source edits in this plan
- STATE.md and ROADMAP.md mark Phase 5 complete with both plans (05-00, 05-01) listed
- Phase-specific regression suite still green; 96 pre-existing failures untouched
</success_criteria>

<output>
Create `.planning/phases/05-integration-and-e2e-verification/05-01-SUMMARY.md` when done
</output>
