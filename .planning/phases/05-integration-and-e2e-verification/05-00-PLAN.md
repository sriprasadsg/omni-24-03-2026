---
phase: 05-integration-and-e2e-verification
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/tests/test_e2e_integration.py
  - backend/compliance_remediation_endpoints.py
  - backend/compliance_reports_endpoints.py
  - backend/agent_heartbeat_endpoints.py
  - backend/compliance_evidence_processor.py
autonomous: true
requirements: [INT-01, INT-06, INT-07, INT-08, INT-13, INT-14, INT-15]
tags: [integration, tenant-isolation, bugfix, compliance]

must_haves:
  truths:
    - "POST/GET/PATCH /api/compliance-remediation/tasks no longer crash with HTTP 500 when called with a real TokenData user (GAP-1 fixed)"
    - "GET /api/compliance/reports returns only the caller's tenant reports for non-super-admins (GAP-2 fixed)"
    - "First-heartbeat evidence is written with the heartbeat's tenant_id instead of None when no asset record exists yet (GAP-3 fixed)"
    - "A failing RED test scaffold exists for each gap and for the golden path before any fix is applied"
  artifacts:
    - path: "backend/tests/test_e2e_integration.py"
      provides: "RED test scaffold for GAP-1/2/3 plus golden-path integration placeholder"
      min_lines: 120
    - path: "backend/compliance_remediation_endpoints.py"
      provides: "_tenant_filter using getattr; created_by using getattr"
      contains: "getattr(user"
    - path: "backend/compliance_reports_endpoints.py"
      provides: "list_compliance_reports querying db.compliance_reports with tenant filter"
      contains: "db.compliance_reports"
    - path: "backend/compliance_evidence_processor.py"
      provides: "process_automated_evidence with fallback_tenant_id parameter"
      contains: "fallback_tenant_id"
  key_links:
    - from: "backend/compliance_remediation_endpoints.py:_tenant_filter"
      to: "auth_types.TokenData"
      via: "getattr(user, 'tenant_id', '')"
      pattern: "getattr\\(user, \"tenant_id\""
    - from: "backend/agent_heartbeat_endpoints.py"
      to: "backend/compliance_evidence_processor.py:process_automated_evidence"
      via: "fallback_tenant_id=_hb_tenant_id keyword argument"
      pattern: "fallback_tenant_id=_hb_tenant_id"
---

<objective>
Fix the three integration gaps discovered during Phase 5 research, gated by a failing RED test scaffold written first.

This is a verification-and-fix phase: no new features. Each fix is minimal (1–5 lines) and corrects an integration defect found by tracing the cross-phase golden path with real JWT tokens.

Purpose: Unblock Wave 1 integration tests. GAP-1 currently makes every remediation endpoint return HTTP 500 with a real TokenData user, so the Phase 4 → Phase 5 remediation loop cannot be exercised until it is fixed. GAP-2 and GAP-3 are tenant-isolation defects (filename metadata leak and orphaned first-heartbeat evidence).

Output: One new test file (`backend/tests/test_e2e_integration.py`) holding RED tests, and minimal edits to three existing source files plus the processor signature.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-integration-and-e2e-verification/05-RESEARCH.md
@.planning/phases/05-integration-and-e2e-verification/05-PATTERNS.md

# Gap fix targets (read before editing)
@backend/compliance_remediation_endpoints.py
@backend/compliance_reports_endpoints.py
@backend/agent_heartbeat_endpoints.py
@backend/compliance_evidence_processor.py

# Contracts and patterns
@backend/auth_types.py
@backend/tests/test_audit_export.py
@backend/tests/conftest.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0: Create RED test scaffold for all three gaps + golden-path placeholder</name>
  <files>backend/tests/test_e2e_integration.py</files>
  <behavior>
    - test_remediation_tenant_filter_accepts_token_data (GAP-1): calls _tenant_filter from compliance_remediation_endpoints with a TokenData user (role="User", tenant_id="tenant-a") and asserts it returns {"tenantId": "tenant-a"} without raising AttributeError. Super-admin TokenData → returns {} (empty dict). FAILS today because _tenant_filter calls user.get().
    - test_remediation_created_by_uses_username (GAP-1): asserts create_task/update_task derive created_by via getattr(current_user, "username", ...) — verify by reading the module source contains "getattr(current_user" and does NOT contain 'current_user.get("email")'. FAILS today.
    - test_list_reports_filters_by_tenant (GAP-2): builds a mock db whose compliance_reports.find(...).to_list returns one tenant-a report; calls list_compliance_reports with a tenant-a TokenData user; asserts the result contains only tenant-a filenames and that os.listdir was NOT used as the source of truth (assert db.compliance_reports.find called). FAILS today because the endpoint uses os.listdir.
    - test_process_evidence_has_fallback_tenant_param (GAP-3): inspect.signature(process_automated_evidence) contains a parameter named "fallback_tenant_id". FAILS today.
    - test_golden_path_placeholder (integration): a single skip-marked or always-failing placeholder asserting the golden path test will be implemented in Wave 1 (05-01). Use pytest.mark.skip(reason="implemented in 05-01") so it does not block the Wave 0 gate but records intent.
  </behavior>
  <action>
    Create backend/tests/test_e2e_integration.py following the established test conventions from test_audit_export.py and conftest.py. Start the module with the sys.path injection: insert backend parent dir via sys.path.insert(0, os.path.join(os.path.dirname(__file__), "..")) before any backend imports (no conftest does this automatically for function-body imports). Import MagicMock, AsyncMock, patch from unittest.mock and pytest.

    Reuse the TokenData helper convention: a local _make_user(role, tenant_id) that returns auth_types.TokenData(username="user@example.com", role=role, tenant_id=tenant_id). For GAP-2, build a mock db where db.compliance_reports.find returns a MagicMock whose .to_list is an AsyncMock returning the seeded report docs (the find→to_list chain, NOT AsyncMock on find itself — see PATTERNS.md DB mock factory). Patch the endpoint module's get_database via patch.object(compliance_reports_endpoints, "get_database", return_value=db, create=True).

    Write all five tests so that the four gap tests FAIL against current source (RED) and the placeholder is skip-marked. Do NOT apply any fix in this task — the fixes land in Tasks 1–3. Keep the file under 500 lines.
  </action>
  <verify>
    <automated>backend/venv/bin/python3 -m pytest backend/tests/test_e2e_integration.py -v --tb=short 2>&1 | grep -E "FAILED|PASSED|SKIPPED|ERROR" | grep -v '^#'</automated>
  </verify>
  <done>Four gap tests collected and FAILING (RED); golden-path placeholder SKIPPED. No import/collection errors.</done>
</task>

<task type="auto">
  <name>Task 1: Fix GAP-1 — _tenant_filter and created_by use getattr on TokenData</name>
  <files>backend/compliance_remediation_endpoints.py</files>
  <action>
    GAP-1 (CRITICAL, per 05-RESEARCH.md Integration Gaps): get_current_user returns an auth_types.TokenData dataclass (fields: username, role, tenant_id, mfa_verified) — NOT a dict. The current code calls .get() on it, raising AttributeError → HTTP 500 on every remediation endpoint.

    In _tenant_filter (lines 33–37): replace user.get("role") with getattr(user, "role", "") or "", and replace `tenant = user.get("tenantId") or user.get("tenant_id") or ""` with `tenant = getattr(user, "tenant_id", "") or ""`. TokenData uses tenant_id (underscore), NOT tenantId. Keep the return shape identical: {} for super-admin roles, {"tenantId": tenant} when a tenant is present, {} otherwise.

    At line 74 (create_task) and line 97 (update_task): replace `created_by = current_user.get("email") or current_user.get("username") or "unknown"` with `created_by = getattr(current_user, "username", None) or "unknown"`. TokenData has NO email field, so do not reference email at all.

    Leave the type hints as-is or relax `user: dict`/`current_user: dict` if convenient — runtime behavior is what matters. Do not change any other logic, route, or the broadcast block. Total change is ~5 lines.
  </action>
  <verify>
    <automated>backend/venv/bin/python3 -m pytest backend/tests/test_e2e_integration.py -k "tenant_filter or created_by" -v --tb=short 2>&1 | grep -E "PASSED|FAILED|ERROR" | grep -v '^#'</automated>
  </verify>
  <done>GAP-1 RED tests now PASS. _tenant_filter returns {"tenantId": "tenant-a"} for a tenant TokenData and {} for super-admin; no AttributeError. Module source no longer contains `current_user.get("email")`.</done>
</task>

<task type="auto">
  <name>Task 2: Fix GAP-2 — list_compliance_reports filters by tenant via db.compliance_reports</name>
  <files>backend/compliance_reports_endpoints.py</files>
  <action>
    GAP-2 (LOW, per 05-RESEARCH.md): list_compliance_reports (lines 123–140) reads the filesystem via os.listdir and returns every .csv/.xlsx/.pdf file with no tenant filter, leaking tenant-B report filenames to tenant-A.

    Rewrite the function body to source the file list from db.compliance_reports instead of os.listdir. Use get_database() (already imported in this module). Resolve the caller's tenant and role with getattr(current_user, "tenant_id", None) and getattr(current_user, "role", "") — the same getattr pattern already used by download_compliance_report at lines 101–103, and reuse the module-level _SUPER_ADMIN_ROLES set (defined at line 87). Build the query filter: super-admins (role in _SUPER_ADMIN_ROLES) → {} (see all reports); non-super-admins with a tenant → {"tenantId": caller_tenant}; non-super-admins without a tenant → return [] (no tenant context). Query via `await db.compliance_reports.find(query_filter).to_list(length=None)` and map each doc to {"filename", "url": f"/api/compliance/reports/download/{filename}", "created"} preserving the existing response shape (filename, url, created). Use the stored doc's created/generatedAt timestamp if present, else fall back to the existing os.path.getctime behavior only for files that exist. Keep the result sorted by created desc.

    Do not change the download route. Keep the function under the existing file's 500-line budget. This is a tenant-isolation fix (V4 Access Control, mitigates Information Disclosure threat in 05-RESEARCH Security Domain).
  </action>
  <verify>
    <automated>backend/venv/bin/python3 -m pytest backend/tests/test_e2e_integration.py -k "list_reports" -v --tb=short 2>&1 | grep -E "PASSED|FAILED|ERROR" | grep -v '^#'</automated>
  </verify>
  <done>test_list_reports_filters_by_tenant PASSES. A tenant-a caller receives only tenant-a report filenames; the query went through db.compliance_reports.find, not os.listdir. Super-admin receives all.</done>
</task>

<task type="auto">
  <name>Task 3: Fix GAP-3 — pass heartbeat tenant_id as fallback to process_automated_evidence</name>
  <files>backend/compliance_evidence_processor.py, backend/agent_heartbeat_endpoints.py</files>
  <action>
    GAP-3 (LOW, per 05-RESEARCH.md): on a brand-new agent's first heartbeat the assets record may not exist yet when process_automated_evidence runs, so its asset/agent tenant lookup yields None and evidence is written with tenantId=None (TenantIsolatedCollection then stamps NON_EXISTENT_TENANT_ISOLATION_EMERGENCY, orphaning the evidence).

    In compliance_evidence_processor.py: change the signature at line 147 from
    `async def process_automated_evidence(agent_hostname, compliance_data, db, agent_type=None)`
    to add a trailing optional keyword param `fallback_tenant_id: str | None = None` (append after agent_type to preserve backward compatibility with all existing callers — same convention as the agent_type addition recorded in STATE.md). After the existing asset/agent lookup block (lines 160–170) where tenant_id is resolved, add: if tenant_id is falsy and fallback_tenant_id is truthy, set tenant_id = fallback_tenant_id. This is the only behavioral change — the rest of the function is untouched.

    In agent_heartbeat_endpoints.py: at the call site (lines 231–236, inside `if "compliance_enforcement" in meta`), add the keyword argument `fallback_tenant_id=_hb_tenant_id` to the process_automated_evidence(...) call. `_hb_tenant_id` is already in scope (set at line 31 from the agent's verified token). Do not change anything else.

    Total change: ~3 lines across the two files.
  </action>
  <verify>
    <automated>backend/venv/bin/python3 -m pytest backend/tests/test_e2e_integration.py -k "fallback_tenant" -v --tb=short 2>&1 | grep -E "PASSED|FAILED|ERROR" | grep -v '^#'</automated>
  </verify>
  <done>test_process_evidence_has_fallback_tenant_param PASSES. process_automated_evidence signature includes fallback_tenant_id with default None; the heartbeat call site passes fallback_tenant_id=_hb_tenant_id.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (JWT) → remediation API | Untrusted user identity crosses here; tenant scoping must derive from TokenData server-side |
| client (JWT) → reports list API | Caller must only see their own tenant's report metadata |
| Rust agent (agent key) → heartbeat → evidence processor | Agent-supplied compliance data is stamped with the verified agent tenant |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | `_tenant_filter` reading attributes off TokenData | mitigate | Use `getattr(user, "tenant_id", "")` not `.get()`; covered by GAP-1 RED tests (Task 1) |
| T-05-02 | Information Disclosure | `GET /api/compliance/reports` filename listing | mitigate | Filter via `db.compliance_reports.find({tenantId})` for non-super-admins (Task 2) |
| T-05-03 | Information Disclosure | First-heartbeat evidence written with tenantId=None | mitigate | Pass `fallback_tenant_id=_hb_tenant_id` so evidence is tenant-stamped (Task 3) |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages introduced this phase; nothing to install |
</threat_model>

<verification>
After all tasks, the four gap tests in test_e2e_integration.py pass and the golden-path placeholder is skipped:

backend/venv/bin/python3 -m pytest backend/tests/test_e2e_integration.py -v --tb=short

Phase-specific regression baseline must remain green (per 05-RESEARCH Validation Architecture):

backend/venv/bin/python3 -m pytest backend/tests/test_rust_heartbeat_parity.py backend/tests/test_evidence_uploads.py backend/tests/test_audit_export.py backend/tests/test_remediation_workflow.py backend/tests/test_tenant_isolation.py backend/tests/test_tenant_security.py -v --tb=short

The 96 pre-existing unrelated failures (test_smoke_endpoints, test_soar_and_ml, etc.) are OUT OF SCOPE and must NOT be touched.
</verification>

<success_criteria>
- backend/tests/test_e2e_integration.py exists with four gap tests + one skipped golden-path placeholder
- GAP-1: _tenant_filter and created_by use getattr; remediation endpoints no longer 500 on TokenData
- GAP-2: list_compliance_reports queries db.compliance_reports with tenant filter; super-admins see all
- GAP-3: process_automated_evidence accepts fallback_tenant_id; heartbeat passes _hb_tenant_id
- Each source fix is minimal (1–5 lines); no new packages, no new source files
- Phase-specific regression suite (44+ tests) still green
</success_criteria>

<output>
Create `.planning/phases/05-integration-and-e2e-verification/05-00-SUMMARY.md` when done
</output>
