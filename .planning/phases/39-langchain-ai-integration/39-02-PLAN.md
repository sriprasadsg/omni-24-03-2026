---
phase: 39-langchain-ai-integration
plan: 02
type: execute
wave: 2
depends_on: ['39-01']
files_modified:
  - pytest.ini
  - backend/tests/eval_langchain/__init__.py
  - backend/tests/eval_langchain/conftest.py
  - backend/tests/eval_langchain/test_router_passthrough.py
  - backend/tests/eval_langchain/data/README.md
autonomous: true
requirements:
  - AISPEC-39-S3    # 9router OpenAI-compatible wire format assumption
  - AISPEC-39-S5    # Evaluation Strategy — pytest markers, eval_langchain/ scaffold
  - RESEARCH-OQ3    # Open Question 3 — 9router tools/response_format passthrough (LOW confidence)
  - RESEARCH-PitE   # Pitfall E — unregistered pytest markers no-op silently

must_haves:
  truths:
    - "A live smoke test proves whether the 9router gateway forwards tools + response_format to Anthropic, or records a documented fallback decision"
    - "pytest recognizes the eval and llm markers (no PytestUnknownMarkWarning)"
    - "pytest --collect-only -m eval selects a non-zero test count"
    - "Two seeded eval tenants (A subject, B canary-source) are available as shared fixtures"
  artifacts:
    - path: "pytest.ini"
      provides: "Registered eval and llm markers"
      contains: "markers"
    - path: "backend/tests/eval_langchain/conftest.py"
      provides: "Two-tenant eval fixtures with canary strings"
      contains: "canary"
    - path: "backend/tests/eval_langchain/test_router_passthrough.py"
      provides: "Live 9router tools/response_format passthrough smoke test + fallback decision record"
      contains: "response_format"
  key_links:
    - from: "backend/tests/eval_langchain/test_router_passthrough.py"
      to: "AI_ROUTER_URL gateway"
      via: "init_chat_model bound tool + structured schema invoke against live gateway"
      pattern: "init_chat_model"
    - from: "pytest.ini"
      to: "backend/tests/eval_langchain/"
      via: "eval marker selection"
      pattern: "eval"
---

<objective>
Resolve the single highest-uncertainty integration point in the phase (RESEARCH Open Question 3, LOW confidence): does the project-local 9router gateway transparently forward tools/tool_choice/response_format fields to Anthropic, or strip/reject them? create_agent and response_format on every downstream surface depend on this. Also stand up the eval harness scaffold (marker registration + directory + two-tenant fixtures) that RESEARCH Pitfall E flagged as silently no-opping today.

Purpose: Front-load the go/no-go decision for the create_agent approach so the agent-migration plans (39-06 auditor, 39-07 chat, 39-08 questionnaire, 39-09 narrative) inherit a known-good path or a documented fallback, and make the CI eval gate real instead of a silent zero-test pass.
Output: A live passthrough smoke test with a recorded fallback decision, registered pytest markers, and the eval_langchain/ scaffold + tenant fixtures.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/39-langchain-ai-integration/39-AI-SPEC.md
@.planning/phases/39-langchain-ai-integration/39-RESEARCH.md
@.planning/phases/39-langchain-ai-integration/39-PATTERNS.md
@backend/tests/eval_questionnaire_auto_answer.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live 9router passthrough smoke test + fallback decision record</name>
  <files>backend/tests/eval_langchain/__init__.py, backend/tests/eval_langchain/test_router_passthrough.py</files>
  <read_first>
    - .planning/phases/39-langchain-ai-integration/39-RESEARCH.md  (Open Question 3; OpenAICompatProvider sends bare /v1/chat/completions with no tools/response_format today — untested territory for the gateway itself)
    - .planning/phases/39-langchain-ai-integration/39-AI-SPEC.md  (Section 3 Entry Point Pattern: init_chat_model model_provider="openai", base_url=AI_ROUTER_URL)
    - backend/tests/eval_questionnaire_auto_answer.py  (graceful try/except ImportError + env-guarded live-call pattern to mirror)
  </read_first>
  <action>
    Create backend/tests/eval_langchain/ as a package (empty __init__.py) and a smoke test that builds
    init_chat_model with model_provider="openai", base_url from AI_ROUTER_URL, api_key from AI_ROUTER_KEY,
    model from AI_ROUTER_MODEL defaulting to claude-sonnet-4-6. Bind one trivial @tool named echo taking
    a text string, then run two async ainvoke checks: (a) a prompt that should trigger the bound tool to
    verify tool-call passthrough, and (b) a call with a minimal two-field Pydantic response_format schema
    to verify structured-output passthrough. Decorate the test with the eval and llm markers (it spends
    tokens and needs the live gateway). Guard with a pytest skip when AI_ROUTER_URL is unset so a gateway-
    less CI does not fail collection. On success assert both a tool call and a validated structured
    response were returned. Record the outcome as a machine-readable decision line the plan SUMMARY must
    surface: PASS means downstream agents use response_format directly; FAIL means downstream agents use
    the documented fallback (native langchain-anthropic ChatAnthropic direct route, or ToolStrategy tool-
    call emulation) per AI-SPEC Pitfall 6 / Section 4b. Use only ainvoke — never a bare invoke, never
    asyncio.run (event-loop rule from AI-SPEC Section 4b).
  </action>
  <verify>
    <automated>backend/venv/bin/python -m pytest backend/tests/eval_langchain/test_router_passthrough.py -m "eval and llm" -q --no-header 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - backend/tests/eval_langchain/test_router_passthrough.py exists and is decorated with the eval marker
    - Test skips (not errors) when AI_ROUTER_URL is unset: grep for a skip guard returns >= 1 hit
    - Test body contains response_format and ainvoke, and contains no `asyncio.run(` and no bare `.invoke(`
    - The plan SUMMARY records an explicit PASS/FAIL passthrough decision and the resulting downstream path
  </acceptance_criteria>
  <done>Passthrough smoke test exists, runs (or skips cleanly), and a fallback decision is recorded for downstream agent plans.</done>
</task>

<task type="auto">
  <name>Task 2: Register eval/llm markers + two-tenant eval fixtures</name>
  <files>pytest.ini, backend/tests/eval_langchain/conftest.py, backend/tests/eval_langchain/data/README.md</files>
  <read_first>
    - pytest.ini  (root [pytest] config — testpaths, asyncio_mode=auto, filterwarnings; add a markers section here, not backend/pyproject.toml which has no pytest section)
    - .planning/phases/39-langchain-ai-integration/39-RESEARCH.md  (Pitfall E: no marker registration exists; --strict-markers not set, so typo'd markers select 0 tests with exit 0)
    - .planning/phases/39-langchain-ai-integration/39-AI-SPEC.md  (Section 5 Reference Dataset: two seeded eval tenants A=subject, B=canary source)
    - backend/tests/eval_questionnaire_auto_answer.py  (existing eval fixture/skip conventions)
  </read_first>
  <action>
    In root pytest.ini under [pytest], add a markers section registering exactly two markers with
    descriptions: eval for code-based and judged eval-harness tests, and llm for the subset that spends
    tokens or needs a live model (excluded by the CI gate expression eval and not llm). Do NOT add
    --strict-markers globally — that would break the wider suite's existing ad-hoc marks; registering the
    two markers is enough to silence PytestUnknownMarkWarning for them. Create conftest.py providing
    session fixtures: two eval tenant IDs (eval_tenant_a subject, eval_tenant_b canary source), a set of
    canary sentinel strings that must never appear in tenant A output, and helper fixtures for seeding
    tenant-scoped evidence records reused by later eval plans. Create data/README.md documenting the
    fixture layout (gold controls, questionnaire Q&A pairs, chat questions, adversarial inputs per AI-SPEC
    Section 5 composition) so 39-10 populates it (the reference dataset) and 39-11/39-12 consume it
    (code-based and judged eval dimensions). Keep all fixtures free of real PII.
  </action>
  <verify>
    <automated>backend/venv/bin/python -m pytest -W error::pytest.PytestUnknownMarkWarning backend/tests/eval_langchain/ --collect-only -m eval -q >/dev/null 2>&1 && echo MARKERS_OK</automated>
  </verify>
  <acceptance_criteria>
    - pytest.ini contains a markers section registering eval and llm (grep for `eval` under a markers key)
    - pytest --collect-only -m eval selects a non-zero test count (the passthrough test at minimum)
    - Running collection with PytestUnknownMarkWarning promoted to error still succeeds (prints MARKERS_OK)
    - conftest.py defines two distinct tenant fixtures and at least one canary string constant
  </acceptance_criteria>
  <done>eval/llm markers registered and warning-clean; eval_langchain/ scaffold + two-tenant canary fixtures exist; data/ layout documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend → 9router gateway | Untrusted tool/structured-output payload shapes cross to the external model proxy |
| eval fixtures → test outputs | Canary strings must not cross tenant boundary in any surface output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-02-A | Information Disclosure | 9router passthrough of tool args | mitigate | Smoke test confirms wire behavior before agents depend on it; fallback path documented if fields are stripped |
| T-39-02-B | Repudiation | Silent eval no-op (Pitfall E) | mitigate | Marker registration + collect-only non-zero assertion; PytestUnknownMarkWarning promoted to error in verify |
</threat_model>

## Artifacts this phase produces

- **New file:** `pytest.ini` markers section registering `eval` and `llm` markers.
- **New package:** `backend/tests/eval_langchain/__init__.py`.
- **New file:** `backend/tests/eval_langchain/test_router_passthrough.py` — live 9router `tools`/`response_format` passthrough smoke test (markers: `eval`, `llm`).
- **New file:** `backend/tests/eval_langchain/conftest.py` — fixtures: `eval_tenant_a`, `eval_tenant_b`, canary-string constant(s), tenant-scoped evidence seeder.
- **New file:** `backend/tests/eval_langchain/data/README.md` — reference-dataset layout doc.
- **Decision recorded (SUMMARY):** 9router passthrough PASS/FAIL and the downstream agent path (direct `response_format` vs fallback).

<verification>
- `backend/venv/bin/python -m pytest backend/tests/eval_langchain/ --collect-only -m eval -q` selects >= 1 test
- Marker warning-as-error collection prints `MARKERS_OK`
- Passthrough test skips cleanly with `AI_ROUTER_URL` unset (no error)
</verification>

<success_criteria>
The create_agent go/no-go decision is resolved by a live smoke test (or a recorded fallback), eval markers are real, and the two-tenant canary fixture base exists for all downstream eval work.
</success_criteria>

<output>
Create `.planning/phases/39-langchain-ai-integration/39-02-SUMMARY.md` when done (must state the passthrough PASS/FAIL decision).
</output>
