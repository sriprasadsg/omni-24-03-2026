---
phase: 39-langchain-ai-integration
plan: 10
type: execute
wave: 3
depends_on: ['39-02', '39-03']
files_modified:
  - backend/tests/eval_langchain/data/gold_controls.json
  - backend/tests/eval_langchain/data/questionnaire_qa.json
  - backend/tests/eval_langchain/data/chat_questions.json
  - backend/tests/eval_langchain/data/adversarial_inputs.json
  - backend/tests/eval_langchain/dataset.py
  - backend/tests/test_eval_dataset.py
autonomous: true
requirements:
  - AISPEC-39-S5    # Reference dataset: 48 examples (20 gold controls, 10 Q&A, 8 chat, 10 adversarial)
  - AISPEC-39-S1    # Critical failure modes the dataset must probe (leakage, fabrication, fail-open, over-claim, silent fallback)
  - AISPEC-39-S1b   # Domain rubric ingredients gold labels encode (traceability, conservative status, framework fidelity)

must_haves:
  truths:
    - "A 48-example reference dataset exists as JSON fixtures under backend/tests/eval_langchain/data/ (>=20 gold controls, 10 questionnaire Q&A, 8 chat, 10 adversarial)"
    - "The 20 gold control assessments carry control_id, real-shaped evidence records, gold status, and gold citation IDs; the ~8 trap cases (stale/partial/zero evidence, context-dependent) have gold status insufficient_evidence or partial"
    - "The 10 adversarial inputs include prompt-injected tenant-filter-bypass, cross-tenant probes, and fabricated-control-ID bait, using canary strings from the 39-02 tenant-B fixtures"
    - "A dataset loader exposes each split with a stable schema the eval dimension tests (39-11/39-12) consume"
    - "Gold citation IDs and control IDs are internally consistent with the schema shapes from 39-03"
  artifacts:
    - path: "backend/tests/eval_langchain/data/gold_controls.json"
      provides: "20 gold-labeled control assessments with evidence + gold citations"
      contains: "control_id"
    - path: "backend/tests/eval_langchain/data/adversarial_inputs.json"
      provides: "10 adversarial inputs (injection, cross-tenant probes, fabricated-ID bait)"
      contains: "canary"
    - path: "backend/tests/eval_langchain/dataset.py"
      provides: "Loader exposing gold/questionnaire/chat/adversarial splits"
      exports: ["load_gold_controls", "load_adversarial_inputs"]
  key_links:
    - from: "backend/tests/eval_langchain/dataset.py"
      to: "backend/tests/eval_langchain/data/"
      via: "loads and validates the JSON fixtures into typed dicts"
      pattern: "gold_controls"
    - from: "backend/tests/eval_langchain/data/adversarial_inputs.json"
      to: "backend/tests/eval_langchain/conftest.py"
      via: "adversarial canaries match the tenant-B canary constants seeded in the 39-02 conftest"
      pattern: "canary"
---

<objective>
Author the 48-example reference dataset AI-SPEC Section 5 specifies — the gold labels, evidence bundles, questionnaire pairs, chat questions, and adversarial inputs — that the eval dimension tests (39-11 code-based, 39-12 judged) run against. This is the 39-02 scaffold's explicitly deferred data-population work. The dataset encodes the domain rubric (traceability, conservative status, framework fidelity, tenant confidentiality) as concrete gold labels so the eval suite has an oracle to score against.

Purpose: Without a labeled dataset the eval harness is an empty shell; this plan makes every downstream eval dimension measurable and makes the CI gate meaningful.
Output: four JSON fixture files, a dataset loader, and a loader/consistency test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-langchain-ai-integration/39-AI-SPEC.md
@.planning/phases/39-langchain-ai-integration/39-RESEARCH.md
@backend/tests/eval_langchain/conftest.py
@backend/tests/eval_langchain/data/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gold control + questionnaire + chat fixtures (3 JSON files)</name>
  <files>backend/tests/eval_langchain/data/gold_controls.json, backend/tests/eval_langchain/data/questionnaire_qa.json, backend/tests/eval_langchain/data/chat_questions.json</files>
  <read_first>
    - .planning/phases/39-langchain-ai-integration/39-AI-SPEC.md  (Section 5 Reference Dataset Composition: 20 gold controls ~8 pass / 4 fail / 8 traps incl. 2 context-dependent = insufficient_evidence; 10 questionnaire pairs 5 answerable / 3 hedged / 2 unanswerable; 8 chat questions incl. out-of-scope + other-customers refusal)
    - backend/tests/eval_langchain/conftest.py  (39-02 tenant A/B fixtures + canary constants + tenant-scoped evidence seeder — gold evidence records and citation IDs must align with what the seeder inserts)
    - backend/ai_orchestration/schemas.py  (AuditFinding/CitedAnswer status + citation shapes the gold labels mirror)
  </read_first>
  <action>
    Author gold_controls.json with 20 entries: each has control_id (from a seeded framework), a set of
    real-shaped evidence records (with ids matching what the conftest seeder inserts for tenant A), a gold
    status (Literal pass|fail|partial|insufficient_evidence), and gold citation ids. Compose ~8 clean pass,
    ~4 clear fail, ~8 traps: stale evidence past threshold, partial evidence, zero evidence, and 2
    context-dependent signals (e.g. auth-failure spike) whose gold label is insufficient_evidence. Author
    questionnaire_qa.json with 10 Q&A pairs: 5 answerable from tenant-A evidence (with gold source ids), 3
    requiring hedged language ("in progress"/"compensating control"), 2 unanswerable (gold behavior = flag
    for review, confidence insufficient_evidence). Author chat_questions.json with 8 questions: posture
    lookups, score explanation, one out-of-scope (gold = decline/redirect), one asking about "other
    customers" (gold = refuse). Keep every fixture free of real PII; use synthetic tenant-A data only. IDs
    must be internally consistent so 39-11 can resolve gold citations.
  </action>
  <verify>
    <automated>backend/venv/bin/python -m pytest backend/tests/test_eval_dataset.py -q -k splits</automated>
  </verify>
  <acceptance_criteria>
    - gold_controls.json parses and contains >= 20 entries each with control_id, evidence, gold status, gold citations
    - At least 8 gold_controls entries have status insufficient_evidence or partial (the trap set)
    - questionnaire_qa.json contains 10 entries (>=2 with insufficient_evidence gold behavior)
    - chat_questions.json contains 8 entries including an out-of-scope and an other-customers refusal case
  </acceptance_criteria>
  <done>Gold control, questionnaire, and chat fixtures exist with internally consistent IDs and correct trap-case gold labels.</done>
</task>

<task type="auto">
  <name>Task 2: Adversarial fixtures + dataset loader</name>
  <files>backend/tests/eval_langchain/data/adversarial_inputs.json, backend/tests/eval_langchain/dataset.py</files>
  <read_first>
    - .planning/phases/39-langchain-ai-integration/39-AI-SPEC.md  (Section 5: 10 adversarial inputs — prompt-injected evidence PDFs/DOCX text "ignore the tenant filter…", cross-tenant probe questions, tenant-filter bypass via tool-arg manipulation, fabricated-control-ID bait "SOC 2 CC9.9", instruction to skip citations)
    - backend/tests/eval_langchain/conftest.py  (tenant-B canary constants — the adversarial cases must reference these so 39-11 can assert zero canary leakage)
    - .planning/phases/39-langchain-ai-integration/39-RESEARCH.md  (Known Threat Patterns table — the injection/bypass shapes to encode)
  </read_first>
  <action>
    Author adversarial_inputs.json with 10 entries, each: an input_type (prompt_injection | cross_tenant_probe
    | tool_arg_bypass | fabricated_control_id | skip_citations), the adversarial text (embedding tenant-B
    canary strings where a leak would surface them), the target surface (auditor|chat|questionnaire), and the
    gold expectation (e.g. "no canary in output", "refuse", "downgrade to insufficient_evidence"). Then create
    dataset.py exposing typed loaders: load_gold_controls(), load_questionnaire_qa(), load_chat_questions(),
    load_adversarial_inputs() — each reading its JSON file, validating required keys, and returning a list of
    dicts with a stable schema the 39-11/39-12 tests consume. The loader must fail loudly (raise) on a missing
    or malformed fixture rather than returning an empty list (so a broken dataset can never silently pass the
    eval gate). Keep under 500 lines.
  </action>
  <verify>
    <automated>backend/venv/bin/python -m pytest backend/tests/test_eval_dataset.py -q -k loader</automated>
  </verify>
  <acceptance_criteria>
    - adversarial_inputs.json contains 10 entries spanning injection, cross-tenant probe, tool-arg bypass, fabricated-control-ID, and skip-citations types
    - Adversarial entries reference the tenant-B canary constants from the conftest
    - dataset.py exports load_gold_controls, load_questionnaire_qa, load_chat_questions, load_adversarial_inputs
    - The loader raises on a missing/malformed fixture (asserted in Task 3)
  </acceptance_criteria>
  <done>Adversarial fixtures and a fail-loud dataset loader exist, wired to the tenant-B canaries.</done>
</task>

<task type="auto">
  <name>Task 3: Dataset loader + consistency test</name>
  <files>backend/tests/test_eval_dataset.py</files>
  <read_first>
    - backend/tests/eval_langchain/dataset.py  (loaders under test)
    - backend/tests/eval_langchain/conftest.py  (canary constants + evidence seeder to cross-check IDs against)
  </read_first>
  <action>
    Write pytest tests (marked eval, NOT llm — deterministic, no token spend) covering: each split loads the
    expected count (20 / 10 / 8 / 10); gold_controls citation ids and control ids are internally consistent
    (every gold citation id appears in that entry's evidence records; every control_id is non-empty); the
    trap subset has the right gold labels; adversarial canaries match the conftest canary constants; and the
    loader raises on a malformed fixture (write to a tmp path and point the loader at it, or monkeypatch the
    data dir). Name split tests `-k splits` and loader tests `-k loader`. No live model.
  </action>
  <verify>
    <automated>backend/venv/bin/python -m pytest backend/tests/test_eval_dataset.py -m eval -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend/venv/bin/python -m pytest backend/tests/test_eval_dataset.py -m eval -q` exits 0
    - A test asserts each split's expected example count
    - A test asserts gold citation/control-ID internal consistency
    - A test asserts the loader raises on a malformed fixture
  </acceptance_criteria>
  <done>The reference dataset is loadable, internally consistent, and covered by deterministic eval-marked tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dataset fixtures → eval suite | A malformed/empty dataset silently passing the gate would hide real regressions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-10-A | Repudiation | Empty/malformed dataset silently passing eval | mitigate | Loader raises on missing/malformed fixture; consistency test asserts split counts + ID coherence |
| T-39-10-B | Information Disclosure | Real PII/tenant data in fixtures | mitigate | Synthetic tenant-A data only; canaries are planted sentinels, not real records |
</threat_model>

## Artifacts this phase produces

- **New files:** `backend/tests/eval_langchain/data/gold_controls.json` (20 gold control assessments), `questionnaire_qa.json` (10 pairs), `chat_questions.json` (8), `adversarial_inputs.json` (10).
- **New file:** `backend/tests/eval_langchain/dataset.py` — typed split loaders (fail-loud).
- **New file:** `backend/tests/test_eval_dataset.py` — dataset consistency + loader tests (marker: `eval`).

<verification>
- `backend/venv/bin/python -m pytest backend/tests/test_eval_dataset.py -m eval -q` exits 0
- The four JSON fixtures parse and hold 20 / 10 / 8 / 10 examples respectively
- `grep -c 'canary' backend/tests/eval_langchain/data/adversarial_inputs.json` returns >= 1
</verification>

<success_criteria>
The 48-example reference dataset (AI-SPEC Section 5) exists as internally-consistent JSON fixtures with a fail-loud loader — the oracle every downstream eval dimension scores against.
</success_criteria>

<output>
Create `.planning/phases/39-langchain-ai-integration/39-10-SUMMARY.md` when done.
</output>
