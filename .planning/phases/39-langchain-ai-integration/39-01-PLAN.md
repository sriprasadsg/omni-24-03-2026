---
phase: 39-langchain-ai-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/requirements.txt
autonomous: false
requirements:
  - AISPEC-39-S2   # Framework Decision — pinned LangChain package set
  - AISPEC-39-S3   # Framework Quick Reference — install list
user_setup: []

must_haves:
  truths:
    - "The 7 pinned LangChain-ecosystem packages install cleanly into backend/venv"
    - "A human reviewer confirms each SUS-flagged package against its public registry before install"
    - "All new package imports resolve in backend/venv without error"
  artifacts:
    - path: "backend/requirements.txt"
      provides: "Pinned LangChain runtime dependency block"
      contains: "langchain==1.3.14"
  key_links:
    - from: "backend/requirements.txt"
      to: "backend/venv"
      via: "pip install of pinned versions"
      pattern: "langchain==1.3.14"
---

<objective>
Install and pin the LangChain 1.x runtime dependency set that every other Phase 39 plan builds on, gated behind a human legitimacy checkpoint because the automated PyPI checker flagged all packages SUS (recency + no download-count source; corroborated as genuine in 39-RESEARCH.md Package Legitimacy Audit).

Purpose: No orchestration code can be written or tested until these packages exist in backend/venv. This is the hard prerequisite for the whole phase.
Output: Updated backend/requirements.txt with a pinned LangChain block, packages installed into backend/venv, imports verified.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-langchain-ai-integration/39-AI-SPEC.md
@.planning/phases/39-langchain-ai-integration/39-RESEARCH.md
@.planning/phases/39-langchain-ai-integration/39-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Legitimacy checkpoint — confirm 7 SUS-flagged packages</name>
  <read_first>
    - .planning/phases/39-langchain-ai-integration/39-RESEARCH.md  (Package Legitimacy Audit table + note: SUS verdicts are `too-new`/`unknown-downloads` checker artifacts, corroborated genuine)
  </read_first>
  <what-built>
    Nothing is installed yet. This checkpoint precedes the install task because the automated
    package-legitimacy checker returned SUS for every package (recency window + the PyPI checker
    has no download-count source at all — it fires on every PyPI package). 39-RESEARCH corroborated
    each against docs.langchain.com and github.com/langchain-ai source repos, and version pins match
    pypi.org/pypi/langchain/json fetched same-day.
  </what-built>
  <how-to-verify>
    Confirm each package exists and is the real LangChain-AI-org package on its registry page:
    1. https://pypi.org/project/langchain/1.3.14/
    2. https://pypi.org/project/langchain-core/1.4.9/
    3. https://pypi.org/project/langgraph/1.2.9/
    4. https://pypi.org/project/langchain-openai/1.3.5/
    5. https://pypi.org/project/langchain-anthropic/1.4.8/
    6. https://pypi.org/project/langchain-ollama/1.1.0/
    7. https://pypi.org/project/openinference-instrumentation-langchain/0.1.67/
    8. https://pypi.org/project/langgraph-checkpoint-sqlite/3.1.0/
    Each page must show the langchain-ai (or Arize-ai for openinference) org as maintainer/homepage.
    langchain-chroma is intentionally NOT in this list (Pitfall 2 — out of scope this phase).
  </how-to-verify>
  <acceptance_criteria>
    - Human confirms all 8 registry pages resolve to the genuine maintainer org
    - Human types "approved" (or lists any package to reject, halting the install task)
  </acceptance_criteria>
  <resume-signal>Type "approved" to proceed to install, or name any package to reject.</resume-signal>
  <done>Human approval recorded; no [SLOP] package present; langchain-chroma excluded.</done>
</task>

<task type="auto">
  <name>Task 2: Pin and install the LangChain runtime block</name>
  <files>backend/requirements.txt</files>
  <read_first>
    - backend/requirements.txt  (see existing openinference-instrumentation-anthropic pin at line 67 — add the LangChain block in the same AI/observability region, matching the file's exact-pin style)
    - .planning/phases/39-langchain-ai-integration/39-AI-SPEC.md  (Section 3 Installation list)
    - .planning/phases/39-langchain-ai-integration/39-RESEARCH.md  (Wave 0 Gaps install command; langgraph-checkpoint-sqlite==3.1.0 must be added — it is NOT in AI-SPEC's pinned list but is required for the prod checkpointer)
  </read_first>
  <action>
    Add exact pins to backend/requirements.txt for: langchain==1.3.14, langchain-core==1.4.9,
    langgraph==1.2.9, langchain-openai==1.3.5, langchain-anthropic==1.4.8, langchain-ollama==1.1.0,
    langgraph-checkpoint-sqlite==3.1.0, openinference-instrumentation-langchain==0.1.67. Do NOT add
    langchain-chroma (Pitfall 2 — rag_service.py stays wrapped, never rewrapped). Install into the
    project's test/runtime env using backend/venv/bin/pip install for each pinned line. These are
    additive to the existing anthropic 0.107.1 SDK stack — they do not replace it. After install,
    prove every top-level import resolves by importing langchain.agents.create_agent,
    langchain.chat_models.init_chat_model, langchain.tools.tool,
    langgraph.checkpoint.sqlite.aio.AsyncSqliteSaver, and
    openinference.instrumentation.langchain.LangChainInstrumentor in a single backend/venv python -c.
  </action>
  <verify>
    <automated>backend/venv/bin/python -c "import langchain; import langchain_core; import langgraph; import langchain_openai; import langchain_anthropic; import langchain_ollama; from langchain.agents import create_agent; from langchain.chat_models import init_chat_model; from langgraph.checkpoint.sqlite.aio import AsyncSqliteSaver; from openinference.instrumentation.langchain import LangChainInstrumentor; print('OK', langchain.__version__)"</automated>
  </verify>
  <acceptance_criteria>
    - backend/requirements.txt contains the literal line langchain==1.3.14
    - backend/requirements.txt contains langgraph-checkpoint-sqlite==3.1.0
    - backend/requirements.txt does NOT contain langchain-chroma
    - The import verify command prints `OK 1.3.14` and exits 0
  </acceptance_criteria>
  <done>All 8 packages pinned and installed; every documented import resolves in backend/venv; langchain-chroma absent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI → backend/venv | Third-party package code enters the runtime; slopsquat/typosquat risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-SC | Tampering | pip installs (8 LangChain packages) | mitigate | Blocking human-verify checkpoint (Task 1) against public registry pages before any install; exact version pins; langchain-chroma excluded |
| T-39-01-A | Tampering | requirements.txt drift | mitigate | Exact `==` pins only; no floating ranges introduced |
</threat_model>

## Artifacts this phase produces

- **Modified file:** `backend/requirements.txt` — new pinned dependency lines: `langchain==1.3.14`, `langchain-core==1.4.9`, `langgraph==1.2.9`, `langchain-openai==1.3.5`, `langchain-anthropic==1.4.8`, `langchain-ollama==1.1.0`, `langgraph-checkpoint-sqlite==3.1.0`, `openinference-instrumentation-langchain==0.1.67`.
- **Installed into `backend/venv`:** the 8 packages above.

<verification>
- `backend/venv/bin/pip show langchain` reports version 1.3.14
- Import smoke command (Task 2 verify) exits 0
- `grep -c 'langchain-chroma' backend/requirements.txt` returns 0
</verification>

<success_criteria>
LangChain 1.x runtime stack installed and pinned; legitimacy human-verified; imports resolve; the phase's core dependency prerequisite is satisfied for all downstream plans.
</success_criteria>

<output>
Create `.planning/phases/39-langchain-ai-integration/39-01-SUMMARY.md` when done.
</output>
