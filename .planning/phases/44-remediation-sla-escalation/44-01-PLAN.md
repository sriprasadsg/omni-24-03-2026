---
phase: 44-remediation-sla-escalation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/compliance_remediation_sla_service.py
  - backend/tests/test_compliance_remediation_sla.py
  - backend/compliance_remediation_service.py
  - backend/compliance_remediation_endpoints.py
  - backend/database.py
autonomous: true
requirements: [SLA-01]
must_haves:
  truths:
    - "compute_remediation_sla() returns ok/at_risk/breached/none at day-scale boundaries driven by a configurable at-risk window"
    - "A newly created remediation task carries sla_status/escalated/escalation_level defaults (D-01)"
    - "The per-tenant at-risk window resolves tenant-doc then global-doc then a hardcoded default (D-02)"
  artifacts:
    - path: "backend/compliance_remediation_sla_service.py"
      provides: "Pure compute_remediation_sla + compute_escalation_level + get_sla_at_risk_window"
      contains: "def compute_remediation_sla"
    - path: "backend/tests/test_compliance_remediation_sla.py"
      provides: "Wave-0 test scaffold covering all 5 verification-map rows"
      contains: "def _mock_db"
    - path: "backend/database.py"
      provides: "Compound indexes on compliance_remediation_tasks"
      contains: "compliance_remediation_tasks.create_index"
  key_links:
    - from: "backend/compliance_remediation_service.py"
      to: "compliance_remediation_tasks document"
      via: "create_task inserts sla_status/escalated/escalation_level defaults"
      pattern: "sla_status"
---

<objective>
Build the pure SLA-computation layer and its storage plumbing: the day-scale `compute_remediation_sla()` function, the tiered `compute_escalation_level()`, the per-tenant configurable at-risk-window lookup, the new task-schema defaults (D-01), and the missing compound indexes. Also lay down the full Wave-0 test scaffold that all later plans in this phase verify against.

Purpose: Everything else in Phase 44 (the sweep, the notification, the history, the UI) depends on a correct, tenant-configurable SLA computation. This plan is the foundation and the single Wave-0 test file.
Output: `compliance_remediation_sla_service.py` (compute + settings-lookup helpers), task-schema defaults, DB indexes, and `tests/test_compliance_remediation_sla.py` (all 5 verification-map groups scaffolded; the `compute_sla` group green at end of this plan, the other 4 RED until 44-02/44-03 implement them).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-remediation-sla-escalation/44-CONTEXT.md
@.planning/phases/44-remediation-sla-escalation/44-RESEARCH.md
@.planning/phases/44-remediation-sla-escalation/44-PATTERNS.md
@.planning/phases/44-remediation-sla-escalation/44-VALIDATION.md
@backend/compliance_remediation_service.py
@backend/evidence_staleness.py
@backend/tickets_helpers.py
@backend/tests/test_ticketing_bridge.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 test scaffold covering all five verification-map rows</name>
  <files>backend/tests/test_compliance_remediation_sla.py</files>
  <read_first>
    - backend/tests/test_ticketing_bridge.py — clone its module-level `_mock_db()` factory (line ~36) and the file-level docstring-as-test-index convention; same collection, Phase 43 precedent
    - backend/tests/test_remediation_workflow.py — the other `_mock_db()` precedent for the async cursor/insert mock shape
    - .planning/phases/44-remediation-sla-escalation/44-VALIDATION.md — the Per-Task Verification Map (5 rows: compute_sla, run_sla_pass, raw_db_registration, escalation_history, tenant_scope) and the `-k` marker contract each row asserts
  </read_first>
  <behavior>
    - compute_sla group: a task ~2 days from due_date under a 3-day window → "at_risk"; a task past due_date → "breached"; a task well before window → "ok"; a task with no due_date → "none"; a resolved task → "ok" (not tracked)
    - run_sla_pass group (RED until 44-02): a breached open task drives escalation-level compute, a remediation_escalations insert, and a send_alert call for assignee + tenant admins; a task with no tenantId is skipped
    - raw_db_registration group (RED until 44-02): app_startup registers start_remediation_sla_scheduler using raw mongodb.db, never get_database (regression guard cloned from the tickets/ticketing_bridge precedent)
    - escalation_history group (RED until 44-03): the escalations resource exposes a GET only — no PATCH/DELETE/PUT route targets the escalations path (route-table introspection, not text grep)
    - tenant_scope group (RED until 44-03): a cross-tenant read of another tenant's escalations returns empty; the read query is AND'd with tenantId
  </behavior>
  <action>
    Create `test_compliance_remediation_sla.py`. Clone `_mock_db()` from test_ticketing_bridge.py so `db.compliance_remediation_tasks`, `db.system_settings`, `db.remediation_escalations`, and `db.users` are all mockable with the same async find/find_one/insert_one/update_one shape. Organize into five test classes whose names align to the `-k` selectors `compute_sla`, `run_sla_pass`, `raw_db_registration`, `escalation_history`, `tenant_scope` (verify each selector matches exactly the intended class per 44-VALIDATION.md). Only the `compute_sla` group is expected green at the end of this plan; write the other four groups as real assertions against the not-yet-existing symbols (`run_sla_pass`, `start_remediation_sla_scheduler`, the escalations GET endpoint, the settings endpoints) so they are RED now and go green in 44-02/44-03 — do NOT stub them to pass. For the escalation_history route-absence assertion, introspect the endpoints router's `.routes` and assert no route whose path ends in `/escalations` permits PATCH/DELETE/PUT (route-table check, never a source-text grep — the same file legitimately contains a settings PATCH). Keep the file under 500 lines; if it would exceed, split the compute cases into a parametrized case list rather than deleting coverage.
  </action>
  <verify>
    <automated>cd backend && venv/bin/python -m pytest tests/test_compliance_remediation_sla.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_compliance_remediation_sla.py --collect-only -q` lists test items under all five `-k` selectors (compute_sla, run_sla_pass, raw_db_registration, escalation_history, tenant_scope)
    - `_mock_db` is defined in the file
    - File is ≤ 500 lines
  </acceptance_criteria>
  <done>All five verification-map groups exist and are collectible; the four not-yet-implemented groups fail (RED), not error-on-collection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SLA compute + tiered level + configurable at-risk-window lookup</name>
  <files>backend/compliance_remediation_sla_service.py</files>
  <read_first>
    - backend/evidence_staleness.py — get_staleness_threshold(): clone the tenant-doc → global-doc → hardcoded-default lookup and the `db._db if hasattr(db,"_db") else db` unwrap guard verbatim, renaming type→"remediation_sla_at_risk" and field→"windowDays"
    - backend/tickets_helpers.py — _compute_sla(): port ONLY the defensive due-date parsing try/except shape; do NOT port its hour-scale (< 3600s) at-risk cutoff or its ("resolved","closed") status check (Pitfall 2 — wrong for compliance_remediation_tasks' 3-value status enum)
    - .planning/phases/44-remediation-sla-escalation/44-RESEARCH.md — Code Examples section for the exact compute_remediation_sla / compute_escalation_level shape (day-scale, configurable window, only "resolved" short-circuits)
  </read_first>
  <behavior>
    - compute_remediation_sla(task, at_risk_window_days): status=="resolved" → sla_status "ok"; missing/unparseable due_date → "none"; now > due → "breached"; days_remaining <= window → "at_risk"; else "ok"; mutates and returns the task dict
    - compute_escalation_level(days_overdue): tiered per D-04 at 1/3/7 days → level 0/1/2/3 (below day 1 = 0)
    - get_sla_at_risk_window(db, tenant_id): tenant-scoped system_settings doc, then global doc, then hardcoded default 3; clamps to >= 1
  </behavior>
  <action>
    Create `compliance_remediation_sla_service.py`. Implement the three pure/lookup helpers per D-02/D-04 and RESEARCH Code Examples: `compute_remediation_sla(task, at_risk_window_days)`, `compute_escalation_level(days_overdue)` with tier boundaries `[1, 3, 7]` as a module constant (Claude's discretion confirmed reasonable per CONTEXT — a small justified default, not locked), and `async def get_sla_at_risk_window(db, tenant_id)` cloning evidence_staleness with type `"remediation_sla_at_risk"`, value field `windowDays`, default `3` (Assumption A1). Keep the at-risk-window default and the tier constant as named module constants with a one-line rationale each. Do NOT call get_database() anywhere in this file — the sweep will pass raw db in (Pitfall 1); keep the `db._db if hasattr` unwrap guard for the dual request-scoped call-site. This file will also host the sweep/scheduler in 44-02; keep it under 500 lines with room to grow.
  </action>
  <verify>
    <automated>cd backend && venv/bin/python -m pytest tests/test_compliance_remediation_sla.py -k compute_sla -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_compliance_remediation_sla.py -k compute_sla -x` passes
    - `compute_remediation_sla`, `compute_escalation_level`, `get_sla_at_risk_window` are all defined
    - No occurrence of `get_database` in compliance_remediation_sla_service.py (grep, non-comment lines): `grep -v '^\s*#' backend/compliance_remediation_sla_service.py | grep -c get_database` returns 0
    - File is ≤ 500 lines
  </acceptance_criteria>
  <done>The compute_sla verification-map row is green; SLA status is correct at day-scale boundaries driven by the configurable window; the module never resolves its own db.</done>
</task>

<task type="auto">
  <name>Task 3: Task-schema SLA defaults + compound indexes</name>
  <files>backend/compliance_remediation_service.py, backend/compliance_remediation_endpoints.py, backend/database.py</files>
  <read_first>
    - backend/compliance_remediation_service.py — the create_task() task dict (lines ~44-64) where the new default fields are inserted alongside the existing status/priority fields
    - backend/compliance_remediation_endpoints.py — the TaskUpdate Pydantic model; confirm whether it should accept the SLA fields (it should NOT — sla_status/escalated/escalation_level are system-managed only per D-01; leave TaskUpdate unchanged and note the confirmation)
    - backend/database.py — the existing `tickets` compound-index block (lines ~277-278) to use as the direct template for the new collection's indexes (Pitfall 3)
  </read_first>
  <action>
    In `compliance_remediation_service.py::create_task`, add three defaults to the persisted task dict per D-01: `sla_status: "ok"` (or "none" when no due_date — either is acceptable since the sweep recomputes; pick "none" when `due_date` is falsy, else "ok"), `escalated: False`, `escalation_level: 0`. Do not disturb existing status/priority branching. In `compliance_remediation_endpoints.py`, review `TaskUpdate` and confirm it does NOT expose the three system-managed SLA fields for manual override (leave unchanged; record the confirmation in the SUMMARY — this satisfies RESEARCH Open Question 2's reset-semantics concern by keeping due_date/SLA edits out of the manual-override surface). In `database.py`, add two indexes in the same block as the tickets indexes, cloning that template: a compound `(tenantId, due_date, status)` and a `(tenantId, escalated)` on `compliance_remediation_tasks` (Pitfall 3 — confirmed absent).
  </action>
  <verify>
    <automated>cd backend && venv/bin/python -c "import ast,sys; src=open('compliance_remediation_service.py').read(); assert 'escalation_level' in src and 'escalated' in src and 'sla_status' in src; idx=open('database.py').read(); assert idx.count('compliance_remediation_tasks.create_index')>=2; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - create_task's persisted dict contains sla_status, escalated, escalation_level
    - database.py contains at least two `compliance_remediation_tasks.create_index` calls, one compound `(tenantId, due_date, status)` and one `(tenantId, escalated)`
    - TaskUpdate does not add sla_status/escalated/escalation_level as writable fields
    - `cd backend && venv/bin/python -m pytest tests/test_compliance_remediation_sla.py -k compute_sla -x` still passes
  </acceptance_criteria>
  <done>New tasks persist SLA defaults (D-01); the sweep query has supporting compound indexes; system-managed SLA fields are not manually writable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| background task → db | The sweep runs with no request/tenant context; a malformed doc with no tenantId must not be processed |
| request → settings endpoint | at-risk-window value crosses from client into system_settings (handled in 44-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-01 | Tampering | compute_remediation_sla parsing untrusted due_date string | mitigate | defensive try/except around fromisoformat; unparseable → "none", never raise |
| T-44-02 | Denial of Service | full-collection scan on every sweep cycle | mitigate | compound indexes (tenantId, due_date, status) and (tenantId, escalated) added in Task 3 |
| T-44-SC | Tampering | dependency installs | accept | this phase installs no packages — no supply-chain surface |
</threat_model>

<verification>
- `cd backend && venv/bin/python -m pytest tests/test_compliance_remediation_sla.py -k compute_sla -x` green
- `cd backend && venv/bin/python -c "import compliance_remediation_sla_service"` exits 0
- Full backend suite unregressed: `cd backend && venv/bin/python -m pytest -q`
</verification>

<success_criteria>
- compute_sla verification-map row green; the other four rows collect and are RED (awaiting 44-02/44-03)
- New tasks persist D-01 defaults; compound indexes present
- No get_database call inside compliance_remediation_sla_service.py
</success_criteria>

<artifacts>
## Artifacts this phase produces (this plan)
- `backend/compliance_remediation_sla_service.py` (NEW) — compute_remediation_sla, compute_escalation_level, get_sla_at_risk_window
- `backend/tests/test_compliance_remediation_sla.py` (NEW) — Wave-0 scaffold, all 5 verification-map groups
- `backend/compliance_remediation_service.py` (MODIFIED) — create_task SLA defaults (D-01)
- `backend/compliance_remediation_endpoints.py` (MODIFIED/reviewed) — TaskUpdate confirmed system-managed-only
- `backend/database.py` (MODIFIED) — compliance_remediation_tasks compound indexes
</artifacts>

<output>
Create `.planning/phases/44-remediation-sla-escalation/44-01-SUMMARY.md` when done
</output>
