---
phase: 44-remediation-sla-escalation
plan: 04
type: execute
wave: 3
depends_on: ["44-03"]
files_modified:
  - services/apiService.ts
  - components/RemediationDashboard.tsx
  - components/RemediationTaskModal.tsx
autonomous: false
requirements: [SLA-01, SLA-02]
must_haves:
  truths:
    - "The remediation task view shows each task's SLA status (ok/at_risk/breached/none) as a colored badge, auto-reflecting due_date"
    - "A compliance admin can open a task and view an append-only, timestamped escalation history panel with no edit/delete controls"
  artifacts:
    - path: "components/RemediationDashboard.tsx"
      provides: "SLA column + SLA_COLORS badge in the task table"
      contains: "SLA_COLORS"
    - path: "components/RemediationTaskModal.tsx"
      provides: "Escalation history panel (toggle-fetch-render, read-only)"
      contains: "Escalation History"
    - path: "services/apiService.ts"
      provides: "fetchRemediationEscalations(taskId)"
      contains: "fetchRemediationEscalations"
  key_links:
    - from: "components/RemediationTaskModal.tsx"
      to: "services/apiService.ts"
      via: "fetchRemediationEscalations(taskId) on panel expand"
      pattern: "fetchRemediationEscalations"
    - from: "services/apiService.ts"
      to: "backend GET /api/compliance/remediation-tasks/{id}/escalations"
      via: "authFetch to the escalations endpoint"
      pattern: "remediation-tasks/.*escalations"
---

<objective>
Wire the SLA computation and escalation history into the actual remediation UI: an SLA status badge column in `RemediationDashboard.tsx` (clone STATUS_COLORS into SLA_COLORS) and a read-only, append-only "Escalation History" panel in `RemediationTaskModal.tsx` (structural clone of ChainOfCustodyPanel.tsx). This is what makes Success Criteria 1 and 3 observable to a user.

Purpose: Delivers "SLA status visible in the remediation task view" (SLA-01, Criterion 1) and "compliance admin can view append-only escalation history" (SLA-02, Criterion 3). Guards against the "built but not wired" Pitfall 6 by editing the existing components in place.
Output: apiService client function, SLA badge column, escalation history panel.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-remediation-sla-escalation/44-CONTEXT.md
@.planning/phases/44-remediation-sla-escalation/44-PATTERNS.md
@.planning/phases/44-remediation-sla-escalation/44-UI-SPEC.md
@.planning/phases/44-03-PLAN.md
@components/RemediationDashboard.tsx
@components/RemediationTaskModal.tsx
@components/ChainOfCustodyPanel.tsx
@services/apiService.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: apiService client for escalation history</name>
  <files>services/apiService.ts</files>
  <read_first>
    - services/apiService.ts — fetchControlAuditLog (line ~4586) and the evidence-staleness settings calls (lines ~4559-4568): clone the authFetch shape and return-typing convention
    - .planning/phases/44-remediation-sla-escalation/44-UI-SPEC.md — the minimum response shape the panel copy contract assumes: `{ task_id, entries: [{ escalation_level, created_at, notified: string[] }] }`
  </read_first>
  <action>
    Add `fetchRemediationEscalations(taskId)` to apiService.ts, cloning fetchControlAuditLog's authFetch pattern, calling `GET ${API_BASE}/compliance/remediation-tasks/${taskId}/escalations` and returning `{ task_id, entries }` typed to the UI-SPEC shape. Place it beside the other remediation/audit-log helpers. Do not introduce a new fetch wrapper — reuse authFetch.
  </action>
  <verify>
    <automated>cd $(git rev-parse --show-toplevel) && npx tsc --noEmit 2>&1 | grep -c "apiService" | grep -qx 0 && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - `fetchRemediationEscalations` is exported from apiService.ts and calls the `/compliance/remediation-tasks/{id}/escalations` endpoint
    - `npx tsc --noEmit` reports no new errors in apiService.ts
  </acceptance_criteria>
  <done>The frontend has a typed client for the escalation-history endpoint.</done>
</task>

<task type="auto">
  <name>Task 2: SLA badge column in RemediationDashboard + escalation panel in RemediationTaskModal</name>
  <files>components/RemediationDashboard.tsx, components/RemediationTaskModal.tsx</files>
  <read_first>
    - components/RemediationDashboard.tsx — STATUS_COLORS (lines ~9-14) and the badge markup (line ~192), plus the table header row and the empty-state colSpan
    - components/RemediationTaskModal.tsx — the Ticketing block (~line 358) and the buttons block (~line 361) between which the panel is placed; the existing lucide-react import line
    - components/ChainOfCustodyPanel.tsx — the full toggle/fetch-on-expand/loading/error/empty/entries structure to clone; local ./icons import convention (HistoryIcon, ChevronDownIcon, AlertTriangleIcon)
    - .planning/phases/44-remediation-sla-escalation/44-UI-SPEC.md — SLA_COLORS map values, badge markup, column placement, copywriting contract (panel header, empty/error copy, entry meta line), the locked "no edit/delete control of any kind" constraint, and the icon-source rule
  </read_first>
  <action>
    In `RemediationDashboard.tsx`: add an `SLA_COLORS` map directly beside `STATUS_COLORS` (keys ok/at_risk/breached/none with the exact classes from UI-SPEC); add a `<th>SLA</th>` header between Status and Actions; add a badge `<td>` cloning the existing status-badge markup byte-for-structure using `SLA_COLORS[task.sla_status] ?? SLA_COLORS.none` and rendering `(task.sla_status ?? 'none').replace('_',' ')`; bump the empty-state `colSpan={6}` to `colSpan={7}`. Always fall back to the `none` neutral pill for tasks missing `sla_status` (legacy pre-migration rows) — never render blank/undefined. In `RemediationTaskModal.tsx`: add an Escalation History panel (inline, or extract to a new `components/EscalationHistoryPanel.tsx` beside ChainOfCustodyPanel.tsx if inlining would push the modal past the 500-line CLAUDE.md limit — planner authorizes the extraction). Structurally clone ChainOfCustodyPanel: toggle bar, fetch-on-first-expand via `fetchRemediationEscalations(task.id)`, loading/error/empty/entries states with the exact UI-SPEC copy ("Escalation History (N events)", "No escalations recorded for this task.", "Failed to load escalation history. Retry by collapsing and expanding this panel.", entry line "Tier {escalation_level} escalation" + "{timestamp} UTC · Notified: {…}"). Import HistoryIcon/ChevronDownIcon/AlertTriangleIcon from local `./icons`, not lucide-react. Render only when `task?.id` is set (never in create mode). Place it after the Ticketing block, before the buttons. Add NO edit, delete, confirm, or destructive control anywhere in the panel — this is a locked SLA-02 constraint. Show the escalation tier as plain text, not a second colored pill.
  </action>
  <verify>
    <automated>cd $(git rev-parse --show-toplevel) && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` (Vite) succeeds and `npx tsc --noEmit` is clean
    - RemediationDashboard.tsx contains SLA_COLORS and a SLA column; empty-state colSpan is 7
    - RemediationTaskModal.tsx (or a new EscalationHistoryPanel.tsx) renders the panel only when task.id is set, fetches on expand, and contains no edit/delete/confirm/destructive control (grep the panel region for delete/remove buttons returns none)
    - Icons for the panel come from `./icons`, not lucide-react
  </acceptance_criteria>
  <done>The SLA badge and read-only escalation history are wired into the live remediation views (Pitfall 6 avoided).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of SLA badge, escalation panel, and notification delivery</name>
  <action>Pause for human verification. All automated backend/frontend checks from 44-01 through 44-04 Task 2 must be green first. Present the how-to-verify steps to the operator and wait for the resume-signal before marking the phase done.</action>
  <what-built>SLA status badge column in the remediation dashboard, a read-only append-only escalation history panel in the task modal, and (backend, from 44-02) in-app escalation notifications to assignee + tenant admins.</what-built>
  <how-to-verify>
    1. Run backend (uvicorn :5000) and frontend (Vite :3000) per project runtime topology.
    2. Open the remediation dashboard; confirm each task shows an SLA badge (ok=green, at_risk=yellow, breached=red, none=gray) reflecting its due_date. Tasks with no due_date show a neutral "none" pill, never a blank cell.
    3. Open a task that has escalations; confirm the "Escalation History (N events)" panel expands, lists timestamped "Tier N escalation" entries with a Notified summary, and has NO edit or delete controls.
    4. Force a task past its due_date, wait one scheduler pass (~5 min or trigger run_sla_pass manually), and confirm BOTH the assignee and a tenant admin see a new bell-icon notification (D-03).
    5. Confirm a second tenant's user cannot see the first tenant's escalation history.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client render → escalation data | Panel renders only server-provided, tenant-scoped data; no client-side mutation of history |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-10 | Tampering | UI offering an edit/delete affordance on an immutable history | mitigate | panel is render-only; no edit/delete/confirm control rendered (SLA-02 locked constraint), enforced by the backend having no mutation route (44-03) |
| T-44-11 | Information Disclosure | badge/panel rendering a task missing sla_status as blank/undefined | mitigate | always fall back to the `none` neutral pill; render only server-provided entries |
| T-44-SC | Tampering | dependency installs | accept | this phase installs no new npm packages |
</threat_model>

<verification>
- `npm run build` succeeds; `npx tsc --noEmit` clean
- Human checkpoint (Task 3) approved: badge visible + auto-computed, escalation panel read-only, notification delivered to assignee + admins, tenant isolation confirmed
</verification>

<success_criteria>
- SLA status visible in the remediation task view, auto-computed (Criterion 1, SLA-01)
- Append-only escalation history viewable by a compliance admin, no edit/delete (Criterion 3, SLA-02)
- Human-verified end-to-end including notification delivery (Criterion 2) and tenant scoping (Criterion 4)
</success_criteria>

<artifacts>
## Artifacts this phase produces (this plan)
- `services/apiService.ts` (MODIFIED) — fetchRemediationEscalations(taskId)
- `components/RemediationDashboard.tsx` (MODIFIED) — SLA_COLORS + SLA column badge
- `components/RemediationTaskModal.tsx` (MODIFIED) — escalation history panel (or new components/EscalationHistoryPanel.tsx if extracted for the 500-line limit)
</artifacts>

<output>
Create `.planning/phases/44-remediation-sla-escalation/44-04-SUMMARY.md` when done
</output>
