---
phase: 44-remediation-sla-escalation
plan: 03
type: execute
wave: 2
depends_on: ["44-01"]
files_modified:
  - backend/compliance_remediation_sla_endpoints.py
  - backend/router_registry.py
autonomous: true
requirements: [SLA-02]
must_haves:
  truths:
    - "A compliance admin can read a task's append-only escalation history via GET; the query is AND'd with tenantId so a cross-tenant read returns empty"
    - "No PATCH/DELETE/PUT route exists for the escalations resource — immutability by omission (SLA-02)"
    - "The per-tenant at-risk window is readable (GET) and admin-gated writable (PATCH) with Field(ge=1, le=365) bounds (D-02)"
  artifacts:
    - path: "backend/compliance_remediation_sla_endpoints.py"
      provides: "GET escalations, GET/PATCH at-risk-window settings"
      contains: "remediation-tasks/{task_id}/escalations"
    - path: "backend/router_registry.py"
      provides: "Registration of the new endpoints router (Pitfall 5)"
      contains: "compliance_remediation_sla_endpoints"
  key_links:
    - from: "backend/compliance_remediation_sla_endpoints.py"
      to: "remediation_escalations collection"
      via: "tenant-scoped find AND'd with tenantId"
      pattern: "remediation_escalations.find"
    - from: "backend/router_registry.py"
      to: "backend/compliance_remediation_sla_endpoints.py"
      via: "_load(app, ...) registration"
      pattern: "compliance_remediation_sla_endpoints"
---

<objective>
Expose the read/settings surface: a tenant-scoped GET for a task's append-only escalation history (SLA-02), and the per-tenant at-risk-window GET/PATCH settings pair (D-02), then register the new router so it is reachable (Pitfall 5). No mutation route exists for the escalations resource — that omission is the immutability enforcement.

Purpose: Delivers SLA-02's "immutable, append-only escalation history viewable by a compliance admin" and the configurable-threshold half of D-02. Runs in parallel with 44-02 (no shared files).
Output: `compliance_remediation_sla_endpoints.py` (GET escalations + GET/PATCH settings) registered in `router_registry.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-remediation-sla-escalation/44-CONTEXT.md
@.planning/phases/44-remediation-sla-escalation/44-RESEARCH.md
@.planning/phases/44-remediation-sla-escalation/44-PATTERNS.md
@.planning/phases/44-01-PLAN.md
@backend/compliance_evidence_lifecycle_endpoints.py
@backend/router_registry.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escalation-history GET + at-risk-window GET/PATCH endpoints</name>
  <files>backend/compliance_remediation_sla_endpoints.py</files>
  <read_first>
    - backend/compliance_evidence_lifecycle_endpoints.py — the STALE-02 settings GET/PATCH pair (lines ~20-101) and the COC-02 audit-log read endpoint (lines ~108-135): clone the _require_admin gate, the _SETTINGS_ADMIN_ROLES set, the StalenessThresholdUpdate Field(ge=1, le=365) model, the upsert tenant/global doc split, and the tenant-scoped read-endpoint shape
    - backend/compliance_remediation_sla_service.py — import get_sla_at_risk_window (from 44-01) for the GET settings handler; do not re-implement the lookup here
    - .planning/phases/44-remediation-sla-escalation/44-PATTERNS.md — the exact endpoint bodies to clone (settings GET/PATCH, escalation read) and the "no mutation route" enforcement note
  </read_first>
  <action>
    Create `compliance_remediation_sla_endpoints.py` with `router = APIRouter()`. Add three routes: (1) `GET /api/compliance/remediation-tasks/{task_id}/escalations` — request-scoped, resolves `db = get_database()` (endpoint context is fine here, unlike the sweep), unwraps `raw = db._db if hasattr(db,"_db") else db`, builds `query = {"task_id": task_id}` and, when `current_user.tenant_id` is present, ANDs `query["tenantId"] = tenant_id` (T-44-06 — never rely solely on write-time correctness), returns entries sorted by created_at ascending, `_id` projected out; (2) `GET /api/settings/remediation-sla` — no admin gate (non-sensitive config), returns `{"windowDays": await get_sla_at_risk_window(db, tenant_id)}`; (3) `PATCH /api/settings/remediation-sla` — admin-gated via a `_require_admin` cloned from compliance_evidence_lifecycle_endpoints, body `SlaWindowUpdate(BaseModel)` with `windowDays: int = Field(ge=1, le=365)`, upserts the `{"type": "remediation_sla_at_risk"}` system_settings doc with the tenant/global split exactly as STALE-02 does. Reuse this file's own `_SETTINGS_ADMIN_ROLES` for the PATCH gate (settings-mutation convention); do not cross-wire it with notification_manager's set. Define NO patch/delete/put route targeting the escalations resource — its absence is SLA-02's enforcement (verbatim rationale from control_comments). Keep the file under 500 lines.
  </action>
  <verify>
    <automated>cd backend && venv/bin/python -m pytest tests/test_compliance_remediation_sla.py -k "escalation_history or tenant_scope" -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_compliance_remediation_sla.py -k "escalation_history or tenant_scope" -x` passes
    - Route-table introspection: no route whose path ends in `/escalations` permits PATCH/DELETE/PUT (GET only)
    - The escalations read query includes a tenantId term when tenant_id is present; a cross-tenant read returns an empty entries list
    - PATCH body model uses Field(ge=1, le=365); PATCH is admin-gated (non-admin → 403)
    - File is ≤ 500 lines
  </acceptance_criteria>
  <done>escalation_history and tenant_scope verification-map rows green; history is read-only and tenant-scoped; the at-risk window is admin-configurable within 1-365 days.</done>
</task>

<task type="auto">
  <name>Task 2: Register the new endpoints router</name>
  <files>backend/router_registry.py</files>
  <read_first>
    - backend/router_registry.py — the `_load(app, "compliance_remediation_endpoints", "router")` / `_load(app, "compliance_evidence_lifecycle_endpoints", "router")` registration lines (~169-170) to clone the exact registration call form
  </read_first>
  <action>
    Add `_load(app, "compliance_remediation_sla_endpoints", "router")` alongside the existing compliance_remediation / evidence_lifecycle registrations (Pitfall 5 — every new endpoints module needs an explicit registration or it 404s in the live app despite passing unit tests). Match the surrounding registration style exactly.
  </action>
  <verify>
    <automated>cd backend && venv/bin/python -c "import router_registry,inspect; s=inspect.getsource(router_registry); assert 'compliance_remediation_sla_endpoints' in s; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - router_registry.py references `compliance_remediation_sla_endpoints`
    - `cd backend && venv/bin/python -c "import compliance_remediation_sla_endpoints"` exits 0
    - `cd backend && venv/bin/python -c "import app_startup"` exits 0
  </acceptance_criteria>
  <done>The SLA endpoints router is registered and reachable, not stranded (Pitfall 5 avoided).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → GET escalations | Tenant B may guess a task_id to read tenant A's history |
| client → PATCH settings | at-risk-window value crosses into system_settings; must be bounded + admin-gated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-06 | Information Disclosure | cross-tenant escalation-history read by guessing task_id | mitigate | read query AND'd with {"tenantId": current_user.tenant_id} at read time, never relying on write-time correctness |
| T-44-07 | Elevation of Privilege | non-admin mutating the at-risk window | mitigate | _require_admin gate on PATCH via _SETTINGS_ADMIN_ROLES |
| T-44-08 | Input Validation | out-of-range/negative at-risk window | mitigate | Pydantic Field(ge=1, le=365) on SlaWindowUpdate, cloned from StalenessThresholdUpdate |
| T-44-09 | Tampering | attempt to edit/delete an escalation entry | mitigate | no PATCH/DELETE/PUT route for the escalations resource — immutability by omission (SLA-02) |
| T-44-SC | Tampering | dependency installs | accept | this phase installs no packages |
</threat_model>

<verification>
- `cd backend && venv/bin/python -m pytest tests/test_compliance_remediation_sla.py -k "escalation_history or tenant_scope" -x` green
- `cd backend && venv/bin/python -c "import compliance_remediation_sla_endpoints, router_registry"` exits 0
- Full backend suite unregressed: `cd backend && venv/bin/python -m pytest -q`
</verification>

<success_criteria>
- Append-only escalation history viewable by a compliance admin, tenant-scoped (SLA-02, Success Criterion 3 + 4)
- No edit/delete path for escalation entries
- At-risk window admin-configurable within 1-365 days (D-02)
- Router registered (reachable in the live app)
</success_criteria>

<artifacts>
## Artifacts this phase produces (this plan)
- `backend/compliance_remediation_sla_endpoints.py` (NEW) — GET escalations, GET/PATCH remediation-sla settings
- `backend/router_registry.py` (MODIFIED) — registers the new router
</artifacts>

<output>
Create `.planning/phases/44-remediation-sla-escalation/44-03-SUMMARY.md` when done
</output>
