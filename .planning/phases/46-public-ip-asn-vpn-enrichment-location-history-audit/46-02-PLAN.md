---
phase: 46-public-ip-asn-vpn-enrichment-location-history-audit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/agent_location_history_service.py
  - backend/migrations/003_agent_location_history_indexes.py
  - backend/tests/test_agent_location_history.py
autonomous: true
requirements: [GAUD-01]

must_haves:
  truths:
    - "A publicIp OR resolved city/country change vs the last confirmed location writes exactly one new agent_location_history row (D-05, success criterion 3)."
    - "A public IP that flip-flops A→B→A within the debounce window (~10 min) never promotes B to a written row — write volume tracks confirmed changes, not heartbeat frequency (D-06/D-07)."
    - "Two genuine changes spaced further apart than the debounce window each write exactly one row."
    - "agent_location_history.timestamp is inserted as a native datetime (BSON Date), never an ISO string (RESEARCH Pitfall 6 / anti-pattern)."
    - "get_track_agent_location(db, tenant_id) resolves per-tenant → global → default True (D-02); when it returns False the record path is a no-op."
    - "record_location_change never mutates or deletes any existing agent_location_history row — it only insert_one's and updates the agent doc's locationConfirmed/locationPending shadow fields."
  artifacts:
    - path: "backend/agent_location_history_service.py"
      provides: "Change-detection + NAT-flip de-noise state machine + append-only write + per-tenant toggle lookup"
      exports: ["record_location_change", "get_track_agent_location"]
      min_lines: 60
    - path: "backend/migrations/003_agent_location_history_indexes.py"
      provides: "Compound indexes on agent_location_history (agent_id + tenantId + timestamp)"
    - path: "backend/tests/test_agent_location_history.py"
      provides: "change-detection, de-noise, toggle-gating, BSON-Date-type coverage"
  key_links:
    - from: "backend/agent_location_history_service.py"
      to: "system_settings collection"
      via: "get_track_agent_location reads type=track_agent_location doc (tenant→global→default)"
      pattern: "track_agent_location"
  prohibitions:
    - statement: "The new agent_location_history collection has no update/delete write path in the service — append-only (D-10 backend half)."
      status: resolved
      verification: "grep -nE 'agent_location_history\\.(update|delete|replace|find_one_and)' backend/agent_location_history_service.py returns nothing"
    - statement: "timestamp is a native datetime object, never .isoformat() on the history row."
      status: resolved
      verification: "grep -n 'isoformat' backend/agent_location_history_service.py shows no isoformat applied to the inserted timestamp field"
---

<objective>
Build `agent_location_history_service.py`: the change-detection + NAT-flip de-noise state machine that decides when a public-IP/geo change is real enough to append an immutable `agent_location_history` row (GAUD-01), plus the per-tenant `track_agent_location` toggle lookup (D-02). This is the load-bearing logic of the phase — it is called inline from the heartbeat/register handlers (wired in 46-05) with the `existing_agent` doc already in hand (zero extra reads, D-05).

Purpose: Satisfy success criterion 3 (one row per change, not per heartbeat) via the de-noise design in RESEARCH Pitfall 1, without ever mutating a written history row.
Output: the service module, a `003` index migration, and `backend/tests/test_agent_location_history.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-public-ip-asn-vpn-enrichment-location-history-audit/46-CONTEXT.md
@.planning/phases/46-public-ip-asn-vpn-enrichment-location-history-audit/46-RESEARCH.md
@.planning/phases/46-public-ip-asn-vpn-enrichment-location-history-audit/46-PATTERNS.md
@backend/compliance_remediation_sla_service.py
@backend/migrations/002_scale_indexes.py
</context>

## Artifacts this phase produces
See 46-01-PLAN.md "Artifacts this phase produces" — same shared list for the phase (`agent_location_history_service.py` exports `record_location_change`, `get_track_agent_location`; agent-doc fields `locationConfirmed`/`locationPending`; collection `agent_location_history`).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write change-detection + de-noise + toggle test scaffold (Wave 0)</name>
  <files>backend/tests/test_agent_location_history.py</files>
  <read_first>
    - .planning/phases/46-public-ip-asn-vpn-enrichment-location-history-audit/46-RESEARCH.md (§ "Pitfall 1" — the 4-branch state machine; § "Validation Architecture" → Test Map)
    - backend/compliance_remediation_sla_service.py (get_sla_at_risk_window toggle-lookup + append-only insert_one shapes)
    - backend/tests/test_compliance_remediation_sla.py (hermetic AsyncMock/mongomock pytest shape)
  </read_first>
  <behavior>
    - change_detection: first-ever call with a public IP (no locationConfirmed) writes one row and sets locationConfirmed.
    - change_detection: a call whose IP+geo equals locationConfirmed writes no row (common case).
    - denoise: A(confirmed)→B(pending, within window)→A again → no row written for B; pending discarded; still one confirmed row (A).
    - denoise: A→B, then B again after the debounce window elapses → B promoted, one new row, locationConfirmed=B.
    - denoise: two genuine changes A→B→C each spaced beyond the window each write exactly one row.
    - toggle: get_track_agent_location returns True by default, honors a per-tenant False doc, honors a global False doc; when False, record_location_change writes nothing.
    - bson_date: the inserted row's timestamp is a datetime instance, not a str.
    - city/country change with same IP still triggers a row (D-05 "publicIp OR city/country").
  </behavior>
  <action>
    Create backend/tests/test_agent_location_history.py. Use a mongomock or AsyncMock-backed fake db exposing agent_location_history.insert_one and agents.update_one, plus system_settings.find_one for the toggle. Drive record_location_change with a controllable "now" (inject a clock or monkeypatch datetime) so the debounce window is deterministic. Assert insert_one call counts, the type of the inserted timestamp, and that no update/delete is ever issued against agent_location_history. Cover every behavior above as a separately named test aligned to the RESEARCH Test Map -k selectors (change_detection, denoise, toggle, bson_date, tenant_scope stub deferred to 46-04). Tests must be hermetic — no real Mongo, no network.
  </action>
  <verify>
    <automated>cd backend && venv/bin/python -m pytest tests/test_agent_location_history.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Test file defines named tests matching -k change_detection, -k denoise, -k toggle, -k bson_date.
    - Tests currently fail only on the missing `agent_location_history_service` import (RED).
    - `grep -nE 'pymongo\.MongoClient|motor\.motor_asyncio\.AsyncIOMotorClient\(' backend/tests/test_agent_location_history.py` returns nothing (no live Mongo connection).
  </acceptance_criteria>
  <done>Scaffold present, failing solely on the not-yet-created service import.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement agent_location_history_service.py (state machine + toggle + append-only write)</name>
  <files>backend/agent_location_history_service.py</files>
  <read_first>
    - .planning/phases/46-public-ip-asn-vpn-enrichment-location-history-audit/46-RESEARCH.md (§ "Pitfall 1" 4-branch logic verbatim; § "Pattern: Per-tenant toggle"; § anti-pattern BSON Date)
    - .planning/phases/46-public-ip-asn-vpn-enrichment-location-history-audit/46-PATTERNS.md (§ agent_location_history_service.py analogs A/B)
    - backend/compliance_remediation_sla_service.py (lines ~112-152 toggle lookup, ~268-275 append-only insert_one)
  </read_first>
  <action>
    Implement backend/agent_location_history_service.py per D-05/D-06/D-07. Provide async get_track_agent_location(db, tenant_id) -> bool cloning get_sla_at_risk_window's tenant→global→default resolution, with type "track_agent_location", boolean field "enabled", default True (D-02); unwrap raw = db._db if hasattr(db,"_db") else db as the SLA helper does. Provide async record_location_change(db, existing_agent, agent_id, tenant_id, public_ip, geo, asn_enrichment, now=None) implementing the RESEARCH Pitfall-1 state machine over two agent-doc shadow fields: locationConfirmed {publicIp, geo, confirmedAt} and locationPending {publicIp, geo, firstSeenAt}. Branch logic: (1) candidate equals confirmed → no-op; (2) candidate equals pending AND now - firstSeenAt >= DEBOUNCE_WINDOW → promote: insert_one an agent_location_history row and $set locationConfirmed=candidate, clear locationPending; (3) candidate equals pending but window not elapsed → no-op; (4) new candidate → $set locationPending={..., firstSeenAt: now}. First-ever case (no locationConfirmed) promotes immediately (write initial row). Define DEBOUNCE_WINDOW as a module constant (~10 minutes, Claude's Discretion per CONTEXT) as a timedelta. The inserted row carries agent_id, tenantId, publicIp, geo, the asn/vpn_heuristic enrichment, and timestamp as datetime.now(timezone.utc) — a native datetime, NEVER .isoformat(). Compare city/country from geo (not just publicIp) so a same-IP geo change still counts as a change (D-05). Never issue update/delete against agent_location_history. Keep both the shadow-field $set updates batchable into the caller's existing agents.update_one where practical, but a dedicated update is acceptable; document which. Under 500 lines.
  </action>
  <verify>
    <automated>cd backend && venv/bin/python -m pytest tests/test_agent_location_history.py -q</automated>
  </verify>
  <acceptance_criteria>
    - All tests in test_agent_location_history.py pass (GREEN).
    - `grep -nE 'agent_location_history\.(update|delete|replace|find_one_and)' backend/agent_location_history_service.py` returns nothing.
    - The inserted timestamp is a datetime: `grep -n 'isoformat' backend/agent_location_history_service.py` shows no isoformat on the row timestamp.
    - `grep -c 'track_agent_location' backend/agent_location_history_service.py` >= 1 and default resolves True (verified by the -k toggle test).
    - `wc -l < backend/agent_location_history_service.py` under 500.
  </acceptance_criteria>
  <done>State machine passes all change-detection/de-noise/toggle/BSON-date tests; append-only guarantee holds (no mutation of history rows).</done>
</task>

<task type="auto">
  <name>Task 3: Add 003 index migration for agent_location_history</name>
  <files>backend/migrations/003_agent_location_history_indexes.py</files>
  <read_first>
    - backend/migrations/002_scale_indexes.py (compound-index create_index convention + runner entry shape)
    - .planning/phases/46-public-ip-asn-vpn-enrichment-location-history-audit/46-RESEARCH.md (§ anti-pattern — timestamp must be Date-typed; no TTL index in this phase, retention is app-level sweep)
  </read_first>
  <action>
    Create backend/migrations/003_agent_location_history_indexes.py following 002_scale_indexes.py's structure. Create a compound index on agent_location_history for (agent_id, tenantId, timestamp) supporting the tenant-scoped GET-by-agent sorted-ascending read, and a standalone index on timestamp supporting the retention $lt sweep. Do NOT create a TTL/expireAfterSeconds index (retention is the app-level cleanup_agent_location_history sweep added in 46-03; a TTL index on a string field would be a silent no-op per Pitfall 6, and timestamp here is a real Date). Mirror 002's idempotent create_index calls and the migration's discoverability by the runner.
  </action>
  <verify>
    <automated>cd backend && venv/bin/python -c "import ast; ast.parse(open('migrations/003_agent_location_history_indexes.py').read()); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'expireAfterSeconds|TTL' backend/migrations/003_agent_location_history_indexes.py` returns nothing.
    - `grep -c 'agent_location_history' backend/migrations/003_agent_location_history_indexes.py` >= 1.
    - File parses as valid Python (ast.parse ok).
    - Index tuple includes agent_id, tenantId, and timestamp (source assertion: all three field names appear in a create_index call).
  </acceptance_criteria>
  <done>Migration file present, parses, creates the compound + timestamp indexes with no TTL index.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| heartbeat payload publicIp/geo → history write | Agent-supplied values drive change-detection and the persisted row |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-02-A | Tampering | agent_location_history rows | mitigate | Append-only: service issues only insert_one; no update/delete path exists (verified by grep gate) |
| T-46-02-B | Repudiation | audit trail integrity | mitigate | timestamp stored as native BSON Date so retention/ordering comparisons are correct and immutable (Pitfall 6 guard) |
| T-46-02-C | Denial of Service (write amplification) | heartbeat write volume | mitigate | De-noise state machine collapses NAT flip-flops so row volume tracks confirmed changes, not heartbeat frequency (D-07) |
</threat_model>

<verification>
- `cd backend && venv/bin/python -m pytest tests/test_agent_location_history.py -q` green.
- Grep gates for append-only + BSON-Date + no-TTL pass.
</verification>

<success_criteria>
- One row per confirmed change; NAT flip-flops de-noised; toggle default ON and gates the write; timestamp is a BSON Date; history rows are never mutated.
</success_criteria>

<output>
Create `.planning/phases/46-public-ip-asn-vpn-enrichment-location-history-audit/46-02-SUMMARY.md` when done.
</output>
