---
phase: 46-public-ip-asn-vpn-enrichment-location-history-audit
plan: 04
type: execute
wave: 2
depends_on: ["46-02"]
files_modified:
  - backend/agent_location_history_endpoints.py
  - backend/router_registry.py
  - backend/tests/test_agent_location_history_endpoints.py
autonomous: true
requirements: [GAUD-01, GAUD-02]

must_haves:
  truths:
    - "GET /api/agents/{agent_id}/location-history returns only the calling tenant's rows, sorted ascending by timestamp, with a belt-and-braces application-level tenant re-filter after the query (T-44-06 pattern)."
    - "The GET response includes a read-time-computed dwell value per row (dwell = next.timestamp - this.timestamp; last row = now - timestamp) — never a stored dwell field (Pitfall 2)."
    - "No PATCH/PUT/DELETE route exists anywhere for the location-history resource — immutability is enforced by the absence of a mutation route (D-10)."
    - "GET/PATCH /api/settings/agent-location-tracking read/write the track_agent_location toggle; PATCH is admin-gated (D-02) with a Pydantic bool body."
    - "The new router is registered in router_registry.py so the routes are reachable in the running app."
  artifacts:
    - path: "backend/agent_location_history_endpoints.py"
      provides: "GET location-history (tenant-scoped, dwell-at-read-time) + GET/PATCH agent-location-tracking toggle"
      exports: ["router"]
      min_lines: 40
    - path: "backend/tests/test_agent_location_history_endpoints.py"
      provides: "tenant-scope isolation, ascending sort, dwell computation, admin-gated PATCH, immutability (no mutation route) coverage"
  key_links:
    - from: "backend/agent_location_history_endpoints.py"
      to: "agent_location_history collection"
      via: "tenant-scoped find sorted ascending + belt-and-braces re-filter"
      pattern: "location-history"
    - from: "backend/router_registry.py"
      to: "backend/agent_location_history_endpoints.py"
      via: "_load registers the router"
      pattern: "agent_location_history_endpoints"
  prohibitions:
    - statement: "No PATCH/PUT/DELETE route targets /api/agents/{agent_id}/location-history (append-only, D-10)."
      status: resolved
      verification: "grep -nE '@router\\.(patch|put|delete)' backend/agent_location_history_endpoints.py returns no line whose path contains location-history"
---

<objective>
Expose the location-history read surface (GAUD-02) and the privacy toggle backend (D-02): a tenant-scoped GET-only `/api/agents/{agent_id}/location-history` cloned verbatim from `get_remediation_escalations` (belt-and-braces tenant re-filter, ascending sort), computing dwell time at read time (Pitfall 2), plus admin-gated GET/PATCH `/api/settings/agent-location-tracking`. Register the router.

Purpose: Immutability (D-10) is guaranteed by the ABSENCE of any mutation route — this plan must not add one, and the plan-checker/tests verify none exists, exactly like SLA-02 verifies for remediation_escalations.
Output: `agent_location_history_endpoints.py`, router registration, and endpoint tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-public-ip-asn-vpn-enrichment-location-history-audit/46-CONTEXT.md
@.planning/phases/46-public-ip-asn-vpn-enrichment-location-history-audit/46-RESEARCH.md
@.planning/phases/46-public-ip-asn-vpn-enrichment-location-history-audit/46-PATTERNS.md
@backend/compliance_remediation_sla_endpoints.py
@backend/agent_location_history_service.py
</context>

## Artifacts this phase produces
See 46-01-PLAN.md "Artifacts this phase produces". This plan adds the endpoints `GET /api/agents/{agent_id}/location-history` and `GET`/`PATCH /api/settings/agent-location-tracking`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write endpoint tests (tenant-scope, sort, dwell, admin PATCH, immutability)</name>
  <files>backend/tests/test_agent_location_history_endpoints.py</files>
  <read_first>
    - backend/compliance_remediation_sla_endpoints.py (get_remediation_escalations + patch_remediation_sla_settings + _require_admin templates)
    - .planning/phases/46-public-ip-asn-vpn-enrichment-location-history-audit/46-RESEARCH.md (§ Pitfall 2 dwell, § Pitfall 3 belt-and-braces)
    - backend/tests/test_compliance_remediation_sla.py (FastAPI TestClient + dependency-override conventions in this repo)
  </read_first>
  <behavior>
    - tenant_scope: two tenants' rows seeded for the same agent_id; tenant A's GET returns only A's rows.
    - sort: rows returned ascending by timestamp (oldest first).
    - dwell: response rows carry a computed dwell value; the last (most recent) row's dwell reflects now - its timestamp (not a stored field); intermediate rows reflect the gap to the next row.
    - toggle_get: GET agent-location-tracking returns the effective enabled value (default true).
    - toggle_patch_admin: PATCH with an admin user sets enabled=false; a subsequent GET reflects it.
    - toggle_patch_forbidden: PATCH with a non-admin user returns 403.
    - immutability: there is no route allowing mutation of a location-history entry (assert via the app's route table that no PATCH/PUT/DELETE path contains "location-history").
  </behavior>
  <action>
    Create backend/tests/test_agent_location_history_endpoints.py using FastAPI TestClient with the repo's get_database/get_current_user dependency-override pattern (follow test_compliance_remediation_sla.py). Seed agent_location_history rows with native datetime timestamps across two tenants. Assert tenant isolation, ascending order, dwell presence/derivation, the admin-gated toggle PATCH (200 for admin, 403 for non-admin), and enumerate app.routes (or _fastapi_app.routes if the socketio wrap is present — see STATE.md note about app.py's socketio wrapper) to assert no mutation route targets the location-history path. Tests fail initially on the missing endpoints module (RED).
  </action>
  <verify>
    <automated>cd backend && venv/bin/python -m pytest tests/test_agent_location_history_endpoints.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Named tests exist for tenant_scope, sort, dwell, toggle admin/forbidden, and immutability route-absence.
    - Immutability test enumerates the route table rather than trusting source inspection.
    - Tests currently fail only on the missing `agent_location_history_endpoints` import (RED).
  </acceptance_criteria>
  <done>Endpoint test scaffold present, failing solely on the not-yet-created endpoints module.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement location-history GET + toggle GET/PATCH endpoints and register router</name>
  <files>backend/agent_location_history_endpoints.py, backend/router_registry.py</files>
  <read_first>
    - backend/compliance_remediation_sla_endpoints.py (clone get_remediation_escalations lines ~45-80; _SETTINGS_ADMIN_ROLES/_require_admin/patch shape lines ~24-34,108-144)
    - backend/agent_location_history_service.py (get_track_agent_location for the toggle GET)
    - backend/router_registry.py (the _load(app, "<module>", "router") registration convention; agent_endpoints is registered near line 64)
    - .planning/phases/46-public-ip-asn-vpn-enrichment-location-history-audit/46-PATTERNS.md (§ agent_location_history_endpoints.py clone + admin PATCH template)
  </read_first>
  <action>
    Implement backend/agent_location_history_endpoints.py. Define GET /api/agents/{agent_id}/location-history cloning get_remediation_escalations verbatim: get_database, unwrap raw = db._db if hasattr(db,"_db") else db, tenant_id = getattr(current_user, "tenant_id", None), query {"agent_id": agent_id, plus tenantId when present}, find(..., {"_id": 0}).sort("timestamp", 1).to_list(length=500), then the belt-and-braces application re-filter [e for e in entries if e.get("tenantId")==tenant_id] (Pitfall 3). Compute dwell at read time and attach it to each returned row: for row i ascending, dwell_seconds = timestamp(i+1) - timestamp(i); for the last row, now - timestamp(last) — never read a stored dwell field (Pitfall 2). Return {"agent_id": ..., "entries": [...]}. Add GET /api/settings/agent-location-tracking returning {"enabled": <get_track_agent_location(...)>} and PATCH /api/settings/agent-location-tracking with a Pydantic body {"enabled": bool}, admin-gated via a _SETTINGS_ADMIN_ROLES set + _require_admin clone, upserting the tenant-scoped (or global) system_settings doc of type track_agent_location exactly like patch_remediation_sla_settings. Do NOT define any PATCH/PUT/DELETE route on the location-history path (D-10). Register the router: add one _load(app, "agent_location_history_endpoints", "router") line in router_registry.py near the other agent endpoint registrations. Keep the file under 500 lines.
  </action>
  <verify>
    <automated>cd backend && venv/bin/python -m pytest tests/test_agent_location_history_endpoints.py -q && venv/bin/python -c "import agent_location_history_endpoints as m; print([r.path for r in m.router.routes])"</automated>
  </verify>
  <acceptance_criteria>
    - All endpoint tests pass (GREEN), including tenant isolation and the admin-gated PATCH.
    - `grep -nE '@router\.(patch|put|delete)' backend/agent_location_history_endpoints.py` returns no line whose path contains `location-history`.
    - `grep -c 'agent_location_history_endpoints' backend/router_registry.py` >= 1 (router registered).
    - The printed route list contains `/api/agents/{agent_id}/location-history` and `/api/settings/agent-location-tracking`.
    - Belt-and-braces re-filter present: `grep -n 'tenantId' backend/agent_location_history_endpoints.py` shows an application-level list re-filter after the query.
  </acceptance_criteria>
  <done>GET returns tenant-scoped, ascending, dwell-annotated rows; toggle GET/PATCH work with admin gating; no mutation route exists; router registered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → GET location-history | Attacker may pass another tenant's agent_id |
| client → PATCH agent-location-tracking | Non-admin may attempt to disable/enable tracking |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-04-A | Information Disclosure | GET /location-history | mitigate | Tenant-scoped query + belt-and-braces application-level re-filter cloned from SLA-02 (Pitfall 3) |
| T-46-04-B | Tampering | location-history immutability | mitigate | No PATCH/PUT/DELETE route on the resource; verified by route-table enumeration test + grep gate (D-10) |
| T-46-04-C | Elevation of Privilege | PATCH toggle | mitigate | Admin-role gate (_require_admin clone) — non-admin PATCH returns 403 |
</threat_model>

<verification>
- `cd backend && venv/bin/python -m pytest tests/test_agent_location_history_endpoints.py -q` green.
- Route-absence + registration grep gates pass.
</verification>

<success_criteria>
- Tenant-scoped, ascending, dwell-annotated GET (GAUD-02); admin-gated toggle (D-02); immutability via mutation-route absence (D-10); router reachable.
</success_criteria>

<output>
Create `.planning/phases/46-public-ip-asn-vpn-enrichment-location-history-audit/46-04-SUMMARY.md` when done.
</output>
