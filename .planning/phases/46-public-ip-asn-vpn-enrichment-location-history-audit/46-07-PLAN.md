---
phase: 46-public-ip-asn-vpn-enrichment-location-history-audit
plan: 07
type: execute
wave: 4
depends_on: ["46-06"]
files_modified:
  - components/PrivacyDashboard.tsx
autonomous: true
requirements: [GAUD-01]

must_haves:
  truths:
    - "An admin sees a short disclosure note in a settings surface explaining that agent public-IP/geo is tracked and why (employee-device IP/geo) — D-03."
    - "An admin can view and toggle the per-tenant track_agent_location setting (default ON) from that surface, persisting via setAgentLocationTracking (D-02 frontend consumer)."
    - "The disclosure note + toggle reuse existing tokens/components (no new palette, font, or component library) and add no new dashboard route."
  artifacts:
    - path: "components/PrivacyDashboard.tsx"
      provides: "Agent Location Tracking disclosure note + per-tenant toggle control"
      contains: "track"
  key_links:
    - from: "components/PrivacyDashboard.tsx"
      to: "services/apiService.ts"
      via: "getAgentLocationTracking on mount + setAgentLocationTracking on toggle"
      pattern: "AgentLocationTracking"
---

<objective>
Deliver the privacy half of the phase's locked decisions in the UI: a short disclosure note (D-03) plus a per-tenant `track_agent_location` toggle control (D-02) added to the existing `PrivacyDashboard.tsx` settings surface (GDPR/works-council-relevant — the exact rationale D-02 gives for the opt-out). This closes GAUD-01's privacy pre-implementation gate on the user-facing side.

Purpose: D-02 and D-03 are locked decisions; the panel-scoped 46-UI-SPEC covered only the timeline, so the toggle + disclosure land here as a minimal addition to an existing surface — no new dashboard, route, palette, or component library (consistent with the UI-SPEC's "no new visual language" intent). Without this, the backend toggle (46-04) would have no UI consumer and D-03 would be unmet.
Output: modified `components/PrivacyDashboard.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-public-ip-asn-vpn-enrichment-location-history-audit/46-CONTEXT.md
@.planning/phases/46-public-ip-asn-vpn-enrichment-location-history-audit/46-UI-SPEC.md
@components/PrivacyDashboard.tsx
</context>

## Artifacts this phase produces
See 46-01-PLAN.md "Artifacts this phase produces". This plan consumes the `getAgentLocationTracking`/`setAgentLocationTracking` clients added in 46-06 and adds the disclosure + toggle UI to PrivacyDashboard.tsx.

<tasks>

<task type="auto">
  <name>Task 1: Add Agent Location Tracking disclosure note + toggle to PrivacyDashboard</name>
  <files>components/PrivacyDashboard.tsx</files>
  <read_first>
    - components/PrivacyDashboard.tsx (full file — existing section/card styling, showToast usage, authFetch/apiService import style)
    - services/apiService.ts (getAgentLocationTracking / setAgentLocationTracking added in 46-06 — signatures + return shapes)
    - .planning/phases/46-public-ip-asn-vpn-enrichment-location-history-audit/46-CONTEXT.md (D-02 default ON + OFF semantics; D-03 disclosure wording intent)
    - .planning/phases/46-public-ip-asn-vpn-enrichment-location-history-audit/46-UI-SPEC.md (color/typography/spacing tokens to reuse; amber caution vocabulary)
  </read_first>
  <action>
    Add an "Agent Location Tracking" section to PrivacyDashboard.tsx using the file's existing card/section styling (no new tokens). Include: (1) a short disclosure note explaining what is tracked and why — that when enabled, each agent's public (WAN) IP and resolved city/country are recorded to an immutable location-history audit trail for security/observability, that this is employee-device network location data, retained 365 days, and that it can be disabled here for works-council/GDPR reasons (D-03). (2) A toggle control reflecting the current per-tenant setting: on mount call getAgentLocationTracking() and initialize the control from { enabled } (default true); on change call setAgentLocationTracking(next) and showToast success/error. Disable the control while the request is in flight. When toggled OFF, the copy should make clear no new location-history rows are written and ASN/VPN enrichment is skipped for this tenant (D-02 OFF semantics). Reuse the existing dark/light class conventions in the file; do not introduce a new palette, font, or component library, and do not add a new dashboard route or nav entry.
  </action>
  <verify>
    <automated>cd /home/user/enterprise-omni-agent-ai-platform && npx tsc --noEmit && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 and `npm run build` succeeds.
    - `grep -ciE 'location tracking|agent location' components/PrivacyDashboard.tsx` >= 1 (disclosure section present).
    - `grep -c 'getAgentLocationTracking' components/PrivacyDashboard.tsx` >= 1 and `grep -c 'setAgentLocationTracking' components/PrivacyDashboard.tsx` >= 1 (toggle wired to both read + write).
    - `grep -ciE '365' components/PrivacyDashboard.tsx` >= 1 (retention disclosed) OR the disclosure copy references the retention window in words.
    - No new npm dependency added (`git diff --stat package.json` shows no change).
  </acceptance_criteria>
  <done>Disclosure note + working per-tenant toggle present in an existing settings surface, wired to the backend toggle, typecheck + build clean, no new dependency/route.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin → toggle PATCH | Admin changes tenant-wide location-tracking posture |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-07-A | Elevation of Privilege | toggle PATCH | mitigate | Backend PATCH is admin-gated (46-04); the UI control simply calls it — non-admins receive 403 and the toast surfaces the failure |
| T-46-07-B | Repudiation (privacy compliance) | disclosure | mitigate | Explicit disclosure note satisfies D-03/D-04's pre-implementation privacy gate on the user-facing side |
</threat_model>

<verification>
- `npx tsc --noEmit` + `npm run build` succeed.
- Disclosure + toggle grep gates pass.
- Manual: open the Privacy settings surface, flip the toggle, confirm the toast and persistence across reload.
</verification>

<success_criteria>
- Disclosure note (D-03) + per-tenant toggle (D-02) live in an existing settings surface, wired to the 46-04 backend, no new visual language or route.
</success_criteria>

<output>
Create `.planning/phases/46-public-ip-asn-vpn-enrichment-location-history-audit/46-07-SUMMARY.md` when done.
</output>
