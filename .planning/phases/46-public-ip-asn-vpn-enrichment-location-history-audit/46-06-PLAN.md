---
phase: 46-public-ip-asn-vpn-enrichment-location-history-audit
plan: 06
type: execute
wave: 3
depends_on: ["46-04"]
files_modified:
  - utils/geo.ts
  - components/AgentList.tsx
  - services/apiService.ts
  - components/AgentLocationHistory.tsx
  - components/AgentOverviewTab.tsx
autonomous: true
requirements: [GAUD-02]

must_haves:
  truths:
    - "An admin opening an agent's detail view (Overview tab) sees a collapsible Location History panel that lazy-fetches on first expand and renders one row per recorded location change (GAUD-02, D-08)."
    - "Each row shows, in order: country flag + city/country, an optional 'likely VPN/hosting' heuristic badge, public IP (monospace), timestamp, and dwell time — dwell computed client-side, never from a stored field (D-09, Pitfall 2)."
    - "The VPN/hosting badge is muted amber and labelled as a heuristic caution; it never uses red and never phrases the flag as an authoritative detection (GSEC-01/D-09)."
    - "The panel is read-only — no edit/delete/mutation affordance on the panel or any row (D-10)."
  artifacts:
    - path: "components/AgentLocationHistory.tsx"
      provides: "Read-only lazy-expand location-history timeline panel cloned from EscalationHistoryPanel"
      min_lines: 60
    - path: "utils/geo.ts"
      provides: "Shared flagEmoji + formatGeo helpers extracted from AgentList.tsx"
      exports: ["flagEmoji", "formatGeo"]
    - path: "services/apiService.ts"
      provides: "fetchAgentLocationHistory + getAgentLocationTracking + setAgentLocationTracking clients"
      exports: ["fetchAgentLocationHistory", "getAgentLocationTracking", "setAgentLocationTracking"]
  key_links:
    - from: "components/AgentLocationHistory.tsx"
      to: "services/apiService.ts"
      via: "fetchAgentLocationHistory(agentId) on first expand"
      pattern: "fetchAgentLocationHistory"
    - from: "components/AgentOverviewTab.tsx"
      to: "components/AgentLocationHistory.tsx"
      via: "panel mounted after the Tenant DetailRow, before Performance Metrics"
      pattern: "AgentLocationHistory"
  prohibitions:
    - statement: "The VPN/hosting badge never uses an authoritative-detection phrasing and never uses a red color token."
      status: resolved
      verification: "See acceptance criteria negative-grep for authoritative-detection wording and red classes on the badge"
    - statement: "No edit/delete/mutation control exists anywhere in AgentLocationHistory.tsx (D-10 read-only)."
      status: resolved
      verification: "grep -niE 'onDelete|onEdit|deleteLocation|editLocation|<button' components/AgentLocationHistory.tsx shows no destructive/mutation control (toggle header button excepted)"
---

<objective>
Build the GAUD-02 timeline: `AgentLocationHistory.tsx`, a read-only lazy-expand panel cloned from `EscalationHistoryPanel.tsx`, mounted inside the Overview tab of `AgentDetailModal` (locked mount point per UI-SPEC), rendering each location change per the D-09 field set and the UI-SPEC Copywriting Contract. Extract the shared `flagEmoji`/`formatGeo` helpers into `utils/geo.ts`, and add the three apiService client functions this phase's frontend needs.

Purpose: The single new frontend surface of the phase (UI-SPEC scope lock). Every token is inherited — no new palette/font/component library.
Output: `utils/geo.ts`, `components/AgentLocationHistory.tsx`, apiService client functions, and the Overview-tab mount.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-public-ip-asn-vpn-enrichment-location-history-audit/46-CONTEXT.md
@.planning/phases/46-public-ip-asn-vpn-enrichment-location-history-audit/46-UI-SPEC.md
@.planning/phases/46-public-ip-asn-vpn-enrichment-location-history-audit/46-PATTERNS.md
@components/EscalationHistoryPanel.tsx
@components/AgentOverviewTab.tsx
</context>

## Artifacts this phase produces
See 46-01-PLAN.md "Artifacts this phase produces". This plan adds `utils/geo.ts` (`flagEmoji`, `formatGeo`), the apiService clients (`fetchAgentLocationHistory`, `getAgentLocationTracking`, `setAgentLocationTracking`), and `AgentLocationHistory.tsx`.

<tasks>

<task type="auto">
  <name>Task 1: Extract geo helpers to utils/geo.ts + add apiService client functions</name>
  <files>utils/geo.ts, components/AgentList.tsx, services/apiService.ts</files>
  <read_first>
    - components/AgentList.tsx (lines ~78-86 — the module-private flagEmoji/formatGeo to extract)
    - services/apiService.ts (line ~4596 fetchRemediationEscalations — the clone template; authFetch/API_BASE usage)
    - .planning/phases/46-public-ip-asn-vpn-enrichment-location-history-audit/46-PATTERNS.md (§ apiService.ts + § reusable helpers)
  </read_first>
  <action>
    Create utils/geo.ts exporting flagEmoji(code?: string): string and formatGeo(geo?): string, moved verbatim from AgentList.tsx (the emoji codepoint math and the [city, region, country].filter(Boolean).join(', ') logic). Update AgentList.tsx to import them from utils/geo.ts and delete its local copies (no behavior change — the card view must render identically). In services/apiService.ts add three functions cloning fetchRemediationEscalations' fail-safe shape (return an empty/default value on non-ok/throw, never reject): fetchAgentLocationHistory(agentId) → GET agents/{agentId}/location-history returning { agent_id, entries }; getAgentLocationTracking() → GET settings/agent-location-tracking returning { enabled }; setAgentLocationTracking(enabled: boolean) → PATCH settings/agent-location-tracking with a JSON body. Define a LocationHistoryEntry type (publicIp, geo, vpn_heuristic, timestamp, and the read-time dwell value returned by the backend) exported for reuse by the panel.
  </action>
  <verify>
    <automated>cd /home/user/enterprise-omni-agent-ai-platform && npx tsc --noEmit && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `grep -c 'export const flagEmoji\|export function flagEmoji' utils/geo.ts` >= 1 and same for formatGeo.
    - `grep -c "from '../utils/geo'" components/AgentList.tsx` >= 1 and AgentList.tsx no longer defines a local `const flagEmoji`.
    - `grep -c 'fetchAgentLocationHistory\|getAgentLocationTracking\|setAgentLocationTracking' services/apiService.ts` >= 3.
  </acceptance_criteria>
  <done>Shared helpers extracted (AgentList unchanged in behavior), three client functions added, typecheck + build clean.</done>
</task>

<task type="auto">
  <name>Task 2: Build AgentLocationHistory.tsx and mount it in the Overview tab</name>
  <files>components/AgentLocationHistory.tsx, components/AgentOverviewTab.tsx</files>
  <read_first>
    - components/EscalationHistoryPanel.tsx (full file — clone the isExpanded/fetched/loading/error state + collapsible header + row-render structure verbatim)
    - .planning/phases/46-public-ip-asn-vpn-enrichment-location-history-audit/46-UI-SPEC.md (§ Copywriting Contract, § Single Row Anatomy, § States, § Behavior Contract, § Mount Point)
    - components/AgentOverviewTab.tsx (lines ~205-213 — insertion point after the Tenant DetailRow / before Performance Metrics)
    - utils/geo.ts, services/apiService.ts (helpers + client + LocationHistoryEntry type from Task 1)
    - components/icons.tsx (HistoryIcon, ChevronDownIcon, GlobeIcon, WifiIcon exports)
  </read_first>
  <action>
    Create components/AgentLocationHistory.tsx cloning EscalationHistoryPanel.tsx's shape: props { agentId }, lazy fetch on first expand only (fetched guard, no fetch on mount, no refetch on re-expand), the collapsible header (HistoryIcon size 14 + "Location History" text-sm font-semibold + "({N} locations)" count + rotating ChevronDownIcon), and the mutually-exclusive loading spinner / error / empty / row-list body — all copy taken verbatim from the UI-SPEC Copywriting Contract (empty: "No location changes recorded for this agent."; error: "Failed to load location history. Retry by collapsing and expanding this panel."; aria-labels "Expand/Collapse location history panel"). Render rows sorted ascending (oldest first); each row uses a leading GlobeIcon (accent color) + Line 1 (flagEmoji(geo.country_code) + formatGeo(geo) + the conditional heuristic badge) + Line 2 (publicIp in font-mono · timestamp UTC · dwell). The heuristic badge renders only when vpn_heuristic === true, uses the locked amber classes (bg-amber-100 text-amber-800 dark:bg-amber-900/50 dark:text-amber-300 text-[11px] font-semibold px-2 py-1 rounded-full uppercase tracking-wide) with a small WifiIcon, and its label is the exact heuristic-caution wording from the UI-SPEC — phrase it as a likelihood, never as an authoritative/confirmed finding, and never use a red color token for it. Compute dwell client-side per the UI-SPEC Dwell time format (just arrived / {N}m / {N}h {N}m / {N}d {N}h, with the most-recent row suffixed " (ongoing)"); ignore any stored dwell-shaped field (Pitfall 2). No row is interactive; no edit/delete affordance anywhere (D-10). Mount <AgentLocationHistory agentId={agent.id} /> in AgentOverviewTab.tsx wrapped in a pt-4 border-t border-gray-200 dark:border-gray-700 block inserted directly after the closing </dl> (Tenant DetailRow) and before the Performance Metrics section.
  </action>
  <verify>
    <automated>cd /home/user/enterprise-omni-agent-ai-platform && npx tsc --noEmit && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 and `npm run build` succeeds.
    - `grep -c 'Location History' components/AgentLocationHistory.tsx` >= 1 and `grep -c 'likely VPN/hosting' components/AgentLocationHistory.tsx` >= 1 (exact heuristic copy present).
    - `grep -c 'AgentLocationHistory' components/AgentOverviewTab.tsx` >= 1 (panel mounted).
    - `grep -niE 'bg-red-|text-red-500' components/AgentLocationHistory.tsx` matches only the fetch-error state text, never the VPN badge (badge uses amber classes present via `grep -c 'amber' components/AgentLocationHistory.tsx` >= 1).
    <!-- planner-discipline-allow: detected -->
    - `grep -niE 'vpn detected|detected vpn|proxy detected|vpn/proxy detected' components/AgentLocationHistory.tsx` returns nothing (no authoritative-detection phrasing).
    - `grep -niE 'onDelete|onEdit|deleteLocation|editLocation' components/AgentLocationHistory.tsx` returns nothing (read-only, D-10).
  </acceptance_criteria>
  <done>Panel renders the D-09 field set with client-side dwell and a heuristic-only amber badge, mounted in the Overview tab, read-only, typecheck + build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend response → panel render | Server-supplied geo/IP/vpn_heuristic values rendered into the DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-06-A | Spoofing (misrepresentation) | VPN/hosting badge | mitigate | Badge is muted amber + heuristic-caution copy, never red, never an authoritative-detection claim — an unlicensed heuristic must not read as a confirmed security detection (GSEC-01/D-09) |
| T-46-06-B | Tampering | location-history rows via UI | mitigate | Panel is read-only with no mutation affordance; there is no backend mutation route to call even if one were added (D-10) |
| T-46-06-C | Information Disclosure | XSS via rendered geo/IP | accept | React escapes rendered text by default; values are short IP/geo strings with no dangerouslySetInnerHTML used |
</threat_model>

<verification>
- `npx tsc --noEmit` and `npm run build` succeed.
- Copy/color/read-only grep gates pass.
- Manual (46-VALIDATION Manual-Only): open an agent detail view after a public-IP change and confirm the timeline rows render with flag/city/IP/VPN-badge/timestamp/dwell.
</verification>

<success_criteria>
- Read-only Location History timeline in the Overview tab (GAUD-02, D-08/D-09/D-10); heuristic amber badge never authoritative/red; dwell computed client-side.
</success_criteria>

<output>
Create `.planning/phases/46-public-ip-asn-vpn-enrichment-location-history-audit/46-06-SUMMARY.md` when done.
</output>
