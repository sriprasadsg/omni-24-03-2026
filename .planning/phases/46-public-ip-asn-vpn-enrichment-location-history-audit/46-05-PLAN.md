---
phase: 46-public-ip-asn-vpn-enrichment-location-history-audit
plan: 05
type: execute
wave: 2
depends_on: ["46-01", "46-02"]
files_modified:
  - backend/agent_heartbeat_endpoints.py
  - backend/agent_registry_endpoints.py
  - backend/tests/test_agent_location_history_wiring.py
autonomous: true
requirements: [GAUD-01]

must_haves:
  truths:
    - "On heartbeat with a public IP, agent_asn_service.lookup() runs immediately after geoip_service.lookup(), and geo.asn/geo.vpn_heuristic persist onto the agent doc (D-13)."
    - "record_location_change is invoked on both heartbeat and registration using the already-fetched existing_agent doc (no extra read, D-05)."
    - "Both the ASN enrichment call and the location-history write are gated on get_track_agent_location; when the toggle is OFF, neither runs — but the pre-existing geoip_service.lookup() city/country enrichment still runs (scope boundary: toggle does NOT blank agent.geo)."
    - "First-ever registration with a public IP writes an initial location-history row (no prior entry to diff against)."
  artifacts:
    - path: "backend/agent_heartbeat_endpoints.py"
      provides: "inline ASN enrichment + location-history hook in the existing geo block"
      contains: "agent_asn_service"
    - path: "backend/agent_registry_endpoints.py"
      provides: "same inline hook on the registration path"
      contains: "agent_asn_service"
    - path: "backend/tests/test_agent_location_history_wiring.py"
      provides: "toggle-gating + geo.asn persistence + record_location_change invocation coverage on both paths"
  key_links:
    - from: "backend/agent_heartbeat_endpoints.py"
      to: "backend/agent_asn_service.py"
      via: "lookup(public_ip) inline after geoip_service.lookup"
      pattern: "agent_asn_service\\.lookup"
    - from: "backend/agent_heartbeat_endpoints.py"
      to: "backend/agent_location_history_service.py"
      via: "record_location_change(existing_agent, ...) gated on get_track_agent_location"
      pattern: "record_location_change"
  prohibitions:
    - statement: "The track_agent_location toggle does NOT gate or blank the pre-existing geoip_service.lookup() city/country enrichment (out-of-scope regression guard)."
      status: resolved
      verification: "In both endpoint files, geoip_service.lookup(public_ip) is called unconditionally within the existing public_ip block, not inside the get_track_agent_location branch"
---

<objective>
Wire the two new services into the exact spot where `geoip_service.lookup()` already runs on the heartbeat and registration paths (D-13), gated on the per-tenant `track_agent_location` toggle (D-02). This is a surgical in-place extension of two existing blocks — the `existing_agent` doc is already fetched just above (D-05, zero extra reads).

Purpose: Turn the isolated services from 46-01/46-02 into live behavior: agents accumulate `geo.asn`/`geo.vpn_heuristic` and an append-only location-history trail on real IP/geo changes.
Output: modified `agent_heartbeat_endpoints.py` + `agent_registry_endpoints.py`, and wiring tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-public-ip-asn-vpn-enrichment-location-history-audit/46-CONTEXT.md
@.planning/phases/46-public-ip-asn-vpn-enrichment-location-history-audit/46-RESEARCH.md
@.planning/phases/46-public-ip-asn-vpn-enrichment-location-history-audit/46-PATTERNS.md
@backend/agent_heartbeat_endpoints.py
@backend/agent_registry_endpoints.py
@backend/agent_asn_service.py
@backend/agent_location_history_service.py
</context>

## Artifacts this phase produces
See 46-01-PLAN.md "Artifacts this phase produces". This plan wires `agent_asn_service.lookup` + `record_location_change` into the heartbeat and registration handlers and persists `geo.asn`/`geo.vpn_heuristic`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write wiring tests (toggle gating, geo.asn persistence, record invocation)</name>
  <files>backend/tests/test_agent_location_history_wiring.py</files>
  <read_first>
    - backend/agent_heartbeat_endpoints.py (lines ~55-135 — existing_agent fetch at 59, geo block 116-130)
    - backend/agent_registry_endpoints.py (lines ~30-90 — existing_agent at 41, geo block 80-88)
    - .planning/phases/46-public-ip-asn-vpn-enrichment-location-history-audit/46-RESEARCH.md (§ "Pattern: Per-tenant toggle" scope-boundary note)
    - backend/tests/test_rust_heartbeat_parity.py (existing heartbeat-path test conventions)
  </read_first>
  <behavior>
    - toggle_on: heartbeat with a public IP and toggle ON invokes agent_asn_service.lookup and record_location_change; geo.asn is present in the agents.update_one $set.
    - toggle_off: heartbeat with toggle OFF does NOT invoke record_location_change and does NOT call agent_asn_service.lookup, but geoip_service.lookup (city/country) is STILL called and geo persists.
    - register_first_seen: first-ever registration with a public IP (no existing_agent) writes an initial location-history row when toggle ON.
    - private_ip: heartbeat with no/private public IP performs no ASN lookup and writes no history row.
  </behavior>
  <action>
    Create backend/tests/test_agent_location_history_wiring.py. Patch agent_asn_service.lookup, agent_location_history_service.record_location_change, agent_location_history_service.get_track_agent_location, and geoip_service.lookup with mocks; drive the heartbeat and registration handlers (via TestClient or by calling the handler with a mocked db, matching the existing heartbeat test style) and assert the call/no-call expectations and the $set payload for geo.asn. Explicitly assert the scope boundary: with the toggle OFF, geoip_service.lookup is still called exactly once. Tests fail initially because the wiring does not yet exist (RED).
  </action>
  <verify>
    <automated>cd backend && venv/bin/python -m pytest tests/test_agent_location_history_wiring.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Named tests exist for toggle_on, toggle_off (with the geoip-still-called assertion), register_first_seen, private_ip.
    - Tests currently fail only because the enrichment/history calls are not yet present in the handlers (RED).
  </acceptance_criteria>
  <done>Wiring test scaffold present, failing solely on the absent handler wiring.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire ASN enrichment + location-history into heartbeat and registration</name>
  <files>backend/agent_heartbeat_endpoints.py, backend/agent_registry_endpoints.py</files>
  <read_first>
    - backend/agent_heartbeat_endpoints.py (geo block lines ~116-130; existing_agent line 59; update_one line ~126)
    - backend/agent_registry_endpoints.py (geo block lines ~80-88; existing_agent line 41; set_tenant_id line 34)
    - .planning/phases/46-public-ip-asn-vpn-enrichment-location-history-audit/46-PATTERNS.md (§ MODIFY blocks for both endpoint files)
    - backend/agent_asn_service.py, backend/agent_location_history_service.py (the exports being called)
  </read_first>
  <action>
    In agent_heartbeat_endpoints.py, inside the existing `if public_ip:` block, immediately after `geo = geoip_service.lookup(public_ip)` (which stays unconditional): resolve the tenant toggle via get_track_agent_location(db, tenant_id); when ON, call agent_asn_service.lookup(public_ip) and merge its asn/vpn_heuristic into update_data["geo"] (persisting geo.asn/geo.vpn_heuristic per D-13), then call record_location_change(db, existing_agent, agent_id, tenant_id, public_ip, geo, asn_enrichment) so the append-only write + shadow-field update happen in the same request. When OFF, skip both the ASN lookup and record_location_change, but leave the geoip_service.lookup/geo persistence untouched (scope boundary). Keep the change-detection using existing_agent (fetched at line 59) — no extra find_one. Apply the identical extension shape to agent_registry_endpoints.py's geo block (existing_agent at line 41, tenant context already set at line 34); first-ever registration (no existing_agent) will write an initial row through record_location_change's first-ever branch. Preserve both files under the 500-line CLAUDE.md cap — if adding the calls would breach it, keep the actual logic in the service modules (it already is) and add only thin call lines. Import the two service modules at the top of each file.
  </action>
  <verify>
    <automated>cd backend && venv/bin/python -m pytest tests/test_agent_location_history_wiring.py tests/test_rust_heartbeat_parity.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Wiring tests pass (GREEN); test_rust_heartbeat_parity.py shows no NEW failure vs the pre-existing baseline.
    - `grep -c 'agent_asn_service\.lookup' backend/agent_heartbeat_endpoints.py` >= 1 and same for agent_registry_endpoints.py.
    - `grep -c 'record_location_change' backend/agent_heartbeat_endpoints.py` >= 1 and same for agent_registry_endpoints.py.
    - In both files, `geoip_service.lookup(public_ip)` is called inside the existing `if public_ip:` block and NOT nested inside the get_track_agent_location branch (scope-boundary assertion).
    - `wc -l < backend/agent_heartbeat_endpoints.py` and the registry file both under 500.
  </acceptance_criteria>
  <done>Both handlers enrich + record on-change when the toggle is ON, skip enrichment/history (but not city/country geo) when OFF, and first-registration writes an initial row.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent heartbeat/register → enrichment + history write | Agent-supplied publicIp drives enrichment and the persisted audit row |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-05-A | Information Disclosure (privacy) | location tracking | mitigate | Per-tenant track_agent_location toggle gates enrichment + history write (works-council/GDPR opt-out, D-02) |
| T-46-05-B | Tampering (scope regression) | existing geo enrichment | mitigate | Toggle explicitly does NOT gate geoip_service.lookup; asserted by test + grep so the opt-out cannot silently blank existing agent.geo |
| T-46-05-C | Denial of Service | heartbeat latency | mitigate | Enrichment is local (no network); record_location_change is a same-request state update reusing the already-fetched existing_agent (no extra read) |
</threat_model>

<verification>
- `cd backend && venv/bin/python -m pytest tests/test_agent_location_history_wiring.py -q` green.
- Full backend suite shows no NEW failures vs the 1343-pass baseline (3 known pre-existing fails allowed).
</verification>

<success_criteria>
- geo.asn/geo.vpn_heuristic persist on agents (D-13); record_location_change fires on-change from both paths using existing_agent (D-05); toggle gates enrichment + history but not city/country geo (D-02 scope boundary); first registration writes an initial row.
</success_criteria>

<output>
Create `.planning/phases/46-public-ip-asn-vpn-enrichment-location-history-audit/46-05-SUMMARY.md` when done.
</output>
