---
phase: 06-asset-compliance-status-ui-fix
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - services/apiService.ts
  - components/FrameworkDetail.tsx
  - components/AssetComplianceList.tsx
autonomous: true
requirements: [STATUS-01, UI-01]

must_haves:
  truths:
    - "Clicking Mark Compliant or Mark Non-Compliant in AssetComplianceList calls PATCH /api/assets/{asset_id}/compliance/status with the correct control_id and status"
    - "A failed PATCH shows a toast error via showToast(..., 'error')"
    - "A successful PATCH refreshes the asset compliance data via refreshAssetCompliance"
    - "Source badges (Automated / Manual) use text-xs instead of text-[10px] in AssetComplianceList.tsx"
    - "FrameworkDetail.tsx onUpdateStatus callback is async and captures control.id via closure"
    - "updateAssetComplianceStatus is exported from services/apiService.ts"
  artifacts:
    - path: "services/apiService.ts"
      provides: "updateAssetComplianceStatus(assetId, controlId, status) — PATCH wrapper"
      contains: "updateAssetComplianceStatus"
    - path: "components/FrameworkDetail.tsx"
      provides: "onUpdateStatus wired to real API call at line ~770"
      contains: "updateAssetComplianceStatus"
    - path: "components/AssetComplianceList.tsx"
      provides: "UI-01 badge fix — text-xs replaces text-[10px]"
      not_contains: "text-[10px]"
  key_links:
    - from: "components/FrameworkDetail.tsx onUpdateStatus callback"
      to: "services/apiService.updateAssetComplianceStatus"
      via: "api.updateAssetComplianceStatus(assetId, control.id, status)"
      pattern: "updateAssetComplianceStatus"
    - from: "services/apiService.updateAssetComplianceStatus"
      to: "PATCH /api/assets/{asset_id}/compliance/status"
      via: "authFetch with method: 'PATCH', body JSON {control_id, status}"
      pattern: "authFetch.*compliance/status"
---

<objective>
Wire the Mark Compliant / Mark Non-Compliant buttons in `AssetComplianceList.tsx` to the backend endpoint created in Plan 01. Three changes:
1. Add `updateAssetComplianceStatus` to `services/apiService.ts`
2. Replace the no-op `console.log` stub in `FrameworkDetail.tsx` line 770 with a real async callback
3. Fix UI-01: change `text-[10px]` → `text-xs` for both evidence source badges in `AssetComplianceList.tsx` (lines 142 and 144)

Purpose: Delivers STATUS-01 (status persists) from the frontend perspective and UI-01 (WCAG AA badge contrast).

Output: One new exported function in `apiService.ts`; updated `onUpdateStatus` prop in `FrameworkDetail.tsx`; two `text-[10px]` → `text-xs` substitutions in `AssetComplianceList.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-asset-compliance-status-ui-fix/06-RESEARCH.md
@services/apiService.ts
@components/FrameworkDetail.tsx
@components/AssetComplianceList.tsx
</context>

<task id="1">
<name>Add updateAssetComplianceStatus to apiService.ts</name>

<read_first>
- services/apiService.ts — lines 632–680 for the existing `uploadComplianceEvidence` and `deleteComplianceEvidence` pattern (authFetch, method, body, error handling)
- services/apiService.ts — line 197 for the `authFetch` signature
</read_first>

<action>
Add after the `deleteComplianceEvidence` function (around line 660) in `services/apiService.ts`:

```
export const updateAssetComplianceStatus = async (
    assetId: string,
    controlId: string,
    status: 'Compliant' | 'Non-Compliant' | 'Pending_Evidence',
    notes?: string
): Promise<void> => {
    const res = await authFetch(`${API_BASE}/assets/${assetId}/compliance/status`, {
        method: 'PATCH',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ control_id: controlId, status, notes: notes ?? '' }),
    });
    if (!res.ok) throw new Error(`Failed to update compliance status: ${res.status}`);
};
```

Do not add any other changes to apiService.ts.
</action>

<acceptance_criteria>
- `services/apiService.ts` contains `export const updateAssetComplianceStatus`
- Function accepts `(assetId: string, controlId: string, status: 'Compliant' | 'Non-Compliant' | 'Pending_Evidence'`
- Function calls `authFetch` with URL `${API_BASE}/assets/${assetId}/compliance/status`
- Method is `'PATCH'`
- Body JSON includes `control_id: controlId` and `status`
- Function throws on non-ok response
- `services/apiService.ts` compiles without new TypeScript errors (no new type errors introduced)
</acceptance_criteria>
</task>

<task id="2">
<name>Wire onUpdateStatus in FrameworkDetail.tsx</name>

<read_first>
- components/FrameworkDetail.tsx — lines 760–815 (the full `AssetComplianceList` prop block); specifically line 770 (the current no-op) and line 773 (`control.id` usage in onUploadEvidence to confirm closure scope)
- components/FrameworkDetail.tsx — line 9 import section to verify `showToast` is already imported (`import { showToast } from '../utils/toast'`)
- services/apiService.ts — `updateAssetComplianceStatus` just added, for the import alias (`import * as api from '../services/apiService'`)
</read_first>

<action>
In `components/FrameworkDetail.tsx` at line 770, replace:

```
onUpdateStatus={(assetId, status) => console.log('Update status', assetId, status)}
```

with:

```
onUpdateStatus={async (assetId, status) => {
    try {
        await api.updateAssetComplianceStatus(assetId, control.id, status);
        await refreshAssetCompliance(assetId);
    } catch (e) {
        console.error('Failed to update compliance status', e);
        showToast('Failed to update compliance status — please try again', 'error');
    }
}}
```

`control.id` is already in scope from the enclosing `controls.map(control => ...)` lambda. `refreshAssetCompliance` is already defined and used on lines 804 and the upload handler. `showToast` is already imported at line 9. `api` is the existing import alias for `../services/apiService`.

No other changes to FrameworkDetail.tsx.
</action>

<acceptance_criteria>
- `components/FrameworkDetail.tsx` no longer contains `console.log('Update status'`
- File contains `api.updateAssetComplianceStatus(assetId, control.id, status)`
- File contains `refreshAssetCompliance(assetId)` inside the onUpdateStatus callback
- File contains `showToast('Failed to update compliance status` in the catch block
- `onUpdateStatus` callback is `async`
- No new TypeScript errors introduced in the file
</acceptance_criteria>
</task>

<task id="3">
<name>Fix UI-01 — source badge text size in AssetComplianceList.tsx</name>

<read_first>
- components/AssetComplianceList.tsx — lines 138–150 for the full evidence badge block; find both `text-[10px]` instances (one on the Automated badge span, one on the Manual badge span)
</read_first>

<action>
In `components/AssetComplianceList.tsx`, replace both occurrences of `text-[10px]` with `text-xs`.

Line 142 (Automated badge):
Change: `<span className="px-1.5 py-0.5 text-[10px] font-semibold rounded-full bg-blue-100 text-blue-700 dark:bg-blue-900 dark:text-blue-300">Automated</span>`
To: `<span className="px-1.5 py-0.5 text-xs font-semibold rounded-full bg-blue-100 text-blue-700 dark:bg-blue-900 dark:text-blue-300">Automated</span>`

Line 144 (Manual badge):
Change: `<span className="px-1.5 py-0.5 text-[10px] font-semibold rounded-full bg-green-100 text-green-700 dark:bg-green-900 dark:text-green-300">Manual</span>`
To: `<span className="px-1.5 py-0.5 text-xs font-semibold rounded-full bg-green-100 text-green-700 dark:bg-green-900 dark:text-green-300">Manual</span>`

No other changes to AssetComplianceList.tsx.
</action>

<acceptance_criteria>
- `components/AssetComplianceList.tsx` contains zero instances of `text-[10px]`
- File contains `text-xs font-semibold rounded-full bg-blue-100 text-blue-700` (Automated badge)
- File contains `text-xs font-semibold rounded-full bg-green-100 text-green-700` (Manual badge)
- No other lines in the file were changed
</acceptance_criteria>
</task>

## Artifacts This Phase Produces

- `services/apiService.ts` — modified: adds `updateAssetComplianceStatus` export
- `components/FrameworkDetail.tsx` — modified: `onUpdateStatus` at line 770 is now a real async callback
- `components/AssetComplianceList.tsx` — modified: `text-[10px]` → `text-xs` at lines 142 and 144
