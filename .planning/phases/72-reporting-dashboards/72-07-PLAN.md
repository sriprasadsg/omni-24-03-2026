---
phase: 72-reporting-dashboards
plan: 07
type: execute
wave: 5
depends_on:
  - 72-04
  - 72-06
files_modified:
  - components/itam/ItamKpiPanel.tsx
  - components/itam/ITAMConsole.tsx
  - src/__tests__/ITAMKpiPanel.test.tsx
  - src/__tests__/ITAMConsole.test.tsx
autonomous: true
requirements:
  - ITAM-REP-04
user_setup: []

must_haves:
  truths:
    - "The ITAM console's Reports tab opens on a four-tile KPI grid showing total asset value with count by lifecycle status, licence seat utilisation percent, upcoming warranty expirations, and the overdue check-in/audit count (D-15, D-16)."
    - "Each tile is clickable and drills into the corresponding pre-built report rather than being a static read-only number (D-18)."
    - "Charts are rendered with recharts, the library already used by CXODashboard.tsx and ExecutiveDashboard.tsx — no new charting dependency is added (D-17)."
    - "A KPI tile with zero underlying data renders the 'No data yet' empty state per UI-SPEC element E6 — never a misleading 0% or empty chart."
    - "'No data yet' plus entity-specific next-step copy renders inside the tile, rather than a misleading zero percent or empty chart."
    - "Each tile shows the existing Loading… convention on the initial fetch."
    - "A single tile's fetch failure falls back to that tile's empty-state copy rather than a dashboard-wide error banner — avoids one failed metric disrupting the whole KPI row."
    - "A populated tile shows its computed value together with its recharts visualisation per D-17."
    - "Per the UI-SPEC visual-hierarchy note, the KPI tiles sit at the top of the Reports tab as the primary anchor and the two-section report list sits below them as secondary content."
    - "KPI chart segments render in the order the KPI endpoint returns them and are never re-sorted by count in the component."
  artifacts:
    - components/itam/ItamKpiPanel.tsx
    - src/__tests__/ITAMKpiPanel.test.tsx
  key_links:
    - "ItamKpiPanel is mounted above ReportsPanel in ITAMConsole.tsx's reports branch, so the tab renders KPIs first and the report sections below."
    - "A tile click sets the console's existing reportFocus state from the KPI payload's drilldownReportKey, which ReportsPanel already consumes through its focusReportKey prop."
    - "The panel's only data source is fetchItamKpis, so every displayed number comes from the tenant-scoped backend aggregate rather than a client-side reduction."
  prohibitions:
    - requirement_id: ITAM-REP-04
      category: transparency
      status: resolved
      verification: test
      statement: "MUST NOT present a fabricated zero or placeholder KPI value when the underlying dataset is empty — an absent metric renders the 'No data yet' empty state, never a numerically plausible 0 or 100% that reads as a real measurement."
---

<objective>
Put the four KPIs on screen as the Reports tab's primary anchor, each one a clickable drill-down into the pre-built report behind it.

Purpose: ITAM-REP-04 is a dashboard requirement — the aggregates computed in plan 72-04 are only delivered once an operator can see and act on them.
Output: `ItamKpiPanel.tsx` with recharts visualisations, its mount in the console's reports branch, and its test coverage.
</objective>

<execution_context>
@/home/user/enterprise-omni-agent-ai-platform/.claude/gsd-core/workflows/execute-plan.md
@/home/user/enterprise-omni-agent-ai-platform/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-reporting-dashboards/72-CONTEXT.md
@.planning/phases/72-reporting-dashboards/72-UI-SPEC.md
@.planning/phases/72-reporting-dashboards/72-PATTERNS.md
@.planning/phases/72-reporting-dashboards/72-04-SUMMARY.md
@.planning/phases/72-reporting-dashboards/72-06-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ItamKpiPanel — four recharts tiles with per-tile states</name>
  <read_first>
    components/CXODashboard.tsx
    components/itam/ReportsPanel.tsx
    components/itam/LicensesPanel.tsx
    components/icons.tsx
    services/apiService.ts
    .planning/phases/72-reporting-dashboards/72-UI-SPEC.md
    backend/itam_reporting_kpis.py
  </read_first>
  <files>
    components/itam/ItamKpiPanel.tsx
    src/__tests__/ITAMKpiPanel.test.tsx
  </files>
  <behavior>
    - The panel fetches KPIs once on mount via fetchItamKpis and renders exactly four tiles.
    - While the fetch is in flight each tile shows the existing Loading… text.
    - A tile whose payload reports no data renders the heading "No data yet" plus its entity-specific next-step sentence, and renders no numeric value and no chart.
    - A licence tile with no licences renders the empty state rather than a zero or hundred percent.
    - A populated asset-value tile renders its total plus a chart of the status breakdown.
    - The status-breakdown segments render in the order the payload lists them, unchanged by count.
    - A rejected fetch leaves every tile in its empty state and renders no dashboard-wide error banner.
    - Clicking a tile invokes the onDrillDown callback with that tile's drilldownReportKey.
    - Each tile is reachable as a button element so keyboard activation works.
  </behavior>
  <action>
Create `components/itam/ItamKpiPanel.tsx`, cloning `components/CXODashboard.tsx`'s recharts usage rather than introducing any new chart approach. Props: `{ onDrillDown(reportKey: string): void }`.

Fetch once on mount with `fetchItamKpis`, holding `{ data, loading, failed }` state. Render a responsive four-tile grid using the console's existing card convention (`bg-gray-800 rounded-xl border border-gray-700 p-4`), one tile per KPI key returned by the endpoint — `assetValue`, `licenseUtilization`, `warrantyExpirations`, `overdue` (D-16). Each tile is a `button` element (so click and keyboard activation both work) that calls `onDrillDown` with that KPI's `drilldownReportKey`, carries an accessible label naming the KPI, and holds three regions: an accent-tinted icon chip using the existing `DollarSignIcon`, `PieChartIcon`, `CalendarDaysIcon` and `AlertTriangleIcon` from `components/icons`; the headline value; and the visualisation.

Visualisations, all inside `ResponsiveContainer` and imported from `recharts` exactly as `CXODashboard.tsx` imports them: a `PieChart` with `Pie`/`Cell`/`Tooltip`/`Legend` for the asset-count-by-lifecycle-status breakdown, rendering `statusBreakdown` in the array order the payload supplies and never re-sorting it in the component; a `PieChart` for assigned versus available seats; a `BarChart` or `AreaChart` over the `timeline` array for warranty expirations; and a plain numeric split (overdue audits versus overdue check-ins) for the overdue tile. The first series colour is the tenant accent, following the UI-SPEC's accent rules.

Per-tile states, per UI-SPEC element E6: while `loading`, each tile shows the console's `text-gray-400 text-sm` Loading text; when a KPI's `hasData` is false — or the whole fetch failed — that tile shows the heading `No data yet` and the body sentence `Add {entity} from the {Tab name} tab to see this here.` with the entity and tab name filled per KPI (for example a software licence from the Licenses & Consumables tab), and renders neither a number nor a chart. A failed fetch degrades every tile to that same empty state; do not render a panel-wide error banner and do not substitute zero for an absent measurement.

Create `src/__tests__/ITAMKpiPanel.test.tsx` following `src/__tests__/ITAMActivityLogPanel.test.tsx`'s vitest conventions, mocking `../../services/apiService`'s `fetchItamKpis`, with one test per behavior above. recharts needs a sized container under jsdom — set an explicit width/height on the mocked container or assert on the tile text and drill-down callback rather than on rendered SVG geometry.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/ITAMKpiPanel.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/ITAMKpiPanel.test.tsx` exits 0.
    - A test asserts four tiles render for a fully-populated payload.
    - A test asserts a KPI payload with `hasData` false renders the literal string `No data yet` and that the rendered tile text contains neither `0%` nor `100%`.
    - A test asserts clicking a tile calls `onDrillDown` with that KPI's `drilldownReportKey`.
    - A test asserts the rendered status labels appear in the payload's array order for a payload whose counts are deliberately out of descending order.
    - `grep -c "from 'recharts'" components/itam/ItamKpiPanel.tsx` returns 1.
    - `grep -c 'recharts' package.json` returns at least 1 and no dependency was added to `package.json` in this plan.
  </acceptance_criteria>
  <done>Four KPI tiles render real tenant-scoped figures with recharts visualisations, degrade honestly to a no-data state, and report which report each one drills into.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mount the KPI grid above the report sections and wire drill-down</name>
  <read_first>
    components/itam/ITAMConsole.tsx
    components/itam/ItamKpiPanel.tsx
    components/itam/ReportsPanel.tsx
    src/__tests__/ITAMConsole.test.tsx
    .planning/phases/72-reporting-dashboards/72-UI-SPEC.md
  </read_first>
  <files>
    components/itam/ITAMConsole.tsx
    src/__tests__/ITAMConsole.test.tsx
  </files>
  <behavior>
    - Selecting the Reports tab renders the KPI grid above the report sections.
    - Clicking a KPI tile sets the console's reportFocus state to that tile's drilldownReportKey.
    - The focused report key reaches ReportsPanel through its existing focusReportKey prop.
    - ReportsPanel's onFocusHandled callback clears the focus so a later tab switch does not re-trigger the drill-down.
    - The other ten tabs render unchanged.
  </behavior>
  <action>
In `components/itam/ITAMConsole.tsx`, import `ItamKpiPanel` and change the reports branch of the `main` conditional so it renders `ItamKpiPanel` first and `ReportsPanel` second, matching the UI-SPEC's visual-hierarchy note that puts the KPI row at the top as the primary anchor and the report list below as secondary content. Pass `onDrillDown={(reportKey) => setReportFocus(reportKey)}` to the KPI panel; `reportFocus`, `setReportFocus` and the `focusReportKey`/`onFocusHandled` prop pair on `ReportsPanel` already exist from plan 72-01, so this task wires the existing seam rather than introducing new state. Change no other tab branch and no nav markup.

In `src/__tests__/ITAMConsole.test.tsx`, add `fetchItamKpis` to the existing `vi.mock('../../services/apiService', ...)` factory object (the factory returns an explicit object, so an unmocked import resolves to undefined at call time), resolving it to a four-KPI payload whose entries carry `hasData` false so the tiles render their empty states without chart geometry under jsdom. Add tests for the behaviors above, driving the drill-down by clicking a tile and asserting the reports section receives the focused key.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/ITAMConsole.test.tsx src/__tests__/ITAMKpiPanel.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/ITAMConsole.test.tsx src/__tests__/ITAMKpiPanel.test.tsx` exits 0.
    - `grep -c 'ItamKpiPanel' components/itam/ITAMConsole.tsx` returns at least 2 (the import and the render).
    - A test asserts that after clicking the Reports tab both the KPI grid and the pre-built report section are present, with the KPI grid appearing first in document order.
    - A test asserts a tile click results in the reports section receiving the drilled-into report key.
    - `npx vitest run src/__tests__` exits 0 and `npm run build` exits 0.
  </acceptance_criteria>
  <done>Opening the Reports tab shows KPIs first and the report sections below, and clicking a KPI tile takes the operator straight to the matching pre-built report.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/itam/kpis | The KPI panel requests fleet-wide aggregates for the caller's tenant. |
| API response → DOM/chart | Aggregate values and status labels are rendered into tiles and chart legends. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-72-15 | Information Disclosure | KPI figures rendered for a non-admin viewer | high | mitigate | The panel's only data source is the `_require_itam_admin`-gated `/api/itam/kpis` route; a rejected request degrades the tiles to their no-data state rather than rendering any partial figure. |
| T-72-16 | Spoofing | Misleading metric presentation (a zero standing in for an absent measurement) | medium | mitigate | Tiles branch on the backend's `hasData` signal and render the `No data yet` empty state instead of a number, asserted by a test that also checks no percentage string is present. |
| T-72-13b | Tampering | Status labels rendered into chart legends | low | mitigate | Labels render as React text children (escaped by default); no raw HTML injection path is introduced. |
| T-72-SC | Tampering | npm/pip installs | low | accept | recharts is already a declared dependency used by three existing dashboards; no package is added. |
</threat_model>

<verification>
- `npx vitest run src/__tests__` passes.
- `npm run build` exits 0.
- Manual (end-of-phase human verification): the KPI row is the first thing visible on the Reports tab, the charts use the tenant accent as the primary series colour, and a tile with no data reads as absent rather than as a zero measurement.
</verification>

<success_criteria>
- Four KPI tiles render on the Reports tab above the report sections.
- Each tile drills into its matching pre-built report through the console's existing focus seam.
- No tile ever displays a fabricated zero for an absent measurement.
</success_criteria>

## Artifacts this phase produces

New files: `components/itam/ItamKpiPanel.tsx`, `src/__tests__/ITAMKpiPanel.test.tsx`.

New frontend symbols: `ItamKpiPanel` (prop `onDrillDown`); the `ItamKpiPanel` mount and `onDrillDown` wiring inside `ITAMConsole`'s reports branch.

<output>
Create `.planning/phases/72-reporting-dashboards/72-07-SUMMARY.md` when done
</output>
