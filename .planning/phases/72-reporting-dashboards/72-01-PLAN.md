---
phase: 72-reporting-dashboards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/itam_reporting_service.py
  - backend/itam_reporting_prebuilt.py
  - backend/itam_reporting_endpoints.py
  - backend/router_registry.py
  - backend/tests/itam_reporting_test_support.py
  - backend/tests/test_itam_reporting_export.py
  - services/apiService.ts
  - components/itam/ReportsPanel.tsx
  - components/itam/ITAMConsole.tsx
  - components/itam/itamI18n.tsx
  - src/__tests__/ITAMReportsPanel.test.tsx
  - src/__tests__/ITAMConsole.test.tsx
autonomous: true
requirements:
  - ITAM-REP-02
  - ITAM-REP-03
user_setup: []

must_haves:
  truths:
    - "An ITAM admin opens the new Reports tab in the ITAM console, sees the Warranty Expiring pre-built report card, clicks Run Report, and sees matching assets in an on-screen paginated table (D-06, D-08, D-10)."
    - "Clicking Export CSV on that report downloads a .csv file whose rows are every asset matching the report, not just the visible preview page (D-14, D-20)."
    - "Warranty-expiring report reuses itam_finance_service's existing alert-window boundary logic verbatim (Phase 71) — no new boundary rule introduced by this phase's report wrapper."
    - "Exporting a report with zero matching rows still generates a valid file with headers and no data rows (never an error) — mirrors compliance_reporting_pdf.py's existing empty-report behavior."
    - "Warranty Expiring rows are ordered ascending by warranty expiry date, and the same order appears in the preview and in the exported file."
    - "Every reporting route rejects a caller without the ITAM admin permission with HTTP 403, using the same _require_itam_admin dependency every other ITAM management router imports (D-07)."
    - "A report file generated for one tenant cannot be downloaded by a caller from another tenant, and a filename containing path-traversal segments is rejected before any filesystem read."
    - "Report generation is on-demand only — no scheduled or recurring delivery path is introduced by this phase (D-13)."
    - "Reports tab preview table shows the empty-state heading No matching assets with the body copy Adjust your filters and run the report again. when a run returns zero rows (UI-SPEC E4 empty)."
    - "Reports tab preview shows the existing text-gray-400 Loading… convention while a run is in flight (UI-SPEC E4 loading)."
    - "A failed run surfaces via showToast with the error variant rather than replacing the panel with an error page (UI-SPEC E4 error)."
    - "Preview renders as the standard console table pattern — table w-full text-sm with a border-b border-gray-700 header row — matching LicensesPanel.tsx (UI-SPEC E4 populated)."
    - "A joined or absent field on a given report row renders as an em dash, never blank or the string undefined (UI-SPEC E4 partial)."
    - "A single-row result renders as a one-row table with no special-casing; many rows page via the preview pagination (UI-SPEC E4 zero-one-many)."
    - "Initial Reports-tab open renders the pre-built report list immediately with no full-page skeleton; only in-flight report runs show the per-section loading state (UI-SPEC E8 loading)."
    - statement: "The preview table stays horizontally scrollable via an overflow-x-auto wrapper when many columns are selected."
      verification: backstop
    - statement: "Preview cells truncate with the truncate class plus a title tooltip rather than growing row height."
      verification: backstop
  artifacts:
    - backend/itam_reporting_service.py
    - backend/itam_reporting_prebuilt.py
    - backend/itam_reporting_endpoints.py
    - backend/tests/itam_reporting_test_support.py
    - backend/tests/test_itam_reporting_export.py
    - components/itam/ReportsPanel.tsx
    - src/__tests__/ITAMReportsPanel.test.tsx
  key_links:
    - "itam_reporting_endpoints.router registered in backend/router_registry.py alongside the other itam_* routers, so /api/itam/reports/* resolves in the real app."
    - "ITAMConsole.tsx Tab union and TABS array carry a reports entry that renders ReportsPanel, with itamI18n.tsx supplying tabs.reports in both en and es."
    - "ReportsPanel accepts focusReportKey/onFocusHandled props wired to ITAMConsole state, the seam the KPI drill-down (D-18) attaches to in plan 72-07."
    - "Generated export filenames are recorded in db.itam_report_exports with tenantId, and the download route reads that document back before streaming."
  prohibitions:
    - requirement_id: ITAM-REP-03
      category: transparency
      status: resolved
      verification: test
      statement: "MUST NOT silently truncate an export — an exported file contains every row matching the report, and where a hard row ceiling is reached the response and file state that the result was truncated rather than presenting a partial file as complete."
---

<objective>
Prove the entire ITAM reporting stack end-to-end on one real path: an ITAM admin opens a new Reports tab, runs the Warranty Expiring pre-built report, sees a paginated preview, exports it as CSV, and downloads the file.

This is the tracer slice. It is production-quality, not a prototype — the module boundaries, the report-row contract, the renderer registry, the admin gate, the tenant-safe download, and the console tab it lands in are all the real ones every later plan in this phase expands. The only thing deliberately narrow is coverage: one of six pre-built reports (D-08), one of three export formats (D-11/D-12), no custom builder yet, no KPI tiles yet.

Purpose: catch an architectural dead-end (row-shape, tenant isolation, renderer dispatch, tab wiring) after one commit instead of after six.
Output: backend reporting service + pre-built registry + router, the Reports tab, the client functions, and the first two test files.
</objective>

<execution_context>
@/home/user/enterprise-omni-agent-ai-platform/.claude/gsd-core/workflows/execute-plan.md
@/home/user/enterprise-omni-agent-ai-platform/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-reporting-dashboards/72-CONTEXT.md
@.planning/phases/72-reporting-dashboards/72-RESEARCH.md
@.planning/phases/72-reporting-dashboards/72-PATTERNS.md
@.planning/phases/72-reporting-dashboards/72-UI-SPEC.md
</context>

<assumption_delta_decision>
`assumption-delta scan 72` reported `detected: true` with one `chosen` signal on the term "custom" in the ROADMAP goal ("Provide custom report building…"). Decision: **no-change**. "Custom report" is a new capability name in this phase, not a generalization of an existing derived value — there is no prior singular/required/derived noun in the codebase being widened by it, so there is nothing to promote or add alongside. The one genuine derived→chosen transition in this phase (the low-stock threshold, D-19) is recorded separately in plan 72-02.
</assumption_delta_decision>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "run and export the Warranty Expiring report" — one path only</name>
  <read_first>
    backend/compliance_reporting_service.py
    backend/compliance_reporting_data.py
    backend/compliance_report_endpoints.py
    backend/itam_asset_endpoints.py
    backend/itam_finance_service.py
    backend/router_registry.py
    backend/tests/conftest.py
    backend/tests/itam_finance_test_support.py
    components/itam/ITAMConsole.tsx
    components/itam/itamI18n.tsx
    components/itam/LicensesPanel.tsx
    services/apiService.ts
  </read_first>
  <files>
    backend/itam_reporting_service.py
    backend/itam_reporting_prebuilt.py
    backend/itam_reporting_endpoints.py
    backend/router_registry.py
    backend/tests/itam_reporting_test_support.py
    backend/tests/test_itam_reporting_export.py
    services/apiService.ts
    components/itam/ReportsPanel.tsx
    components/itam/ITAMConsole.tsx
    components/itam/itamI18n.tsx
  </files>
  <action>
Wire one report from MongoDB to a downloaded CSV file through every layer, with no other call sites and no second report or format.

Backend, `backend/itam_reporting_service.py` (new — shared contract every later plan plugs into, mirroring `compliance_reporting_service.py`'s aggregator role):
- Module constant `_REPORTS_DIR` resolving to `static/reports` next to this module, and `_ensure_reports_dir()`, cloned from `compliance_report_endpoints.py`.
- `async def build_report_rows(db, kind, key, tenant_id, definition=None, limit=None)` returning a dict with keys `key`, `title`, `columns`, `rows`, `rowCount`, `truncated`. `kind` accepts the string `prebuilt` and dispatches to `itam_reporting_prebuilt.run_prebuilt_report`; any other `kind` raises `ValueError`. `rows` is a list of plain dicts whose keys are display headers, and `columns` is `list(rows[0].keys())` when rows exist, else the report's declared header list — this row shape is the single source of truth all three renderers read (D-12).
- Module constant `MAX_REPORT_ROWS = 10000` and a `truncated` boolean on the returned dict set true only when the ceiling is actually hit; never drop rows without setting it.
- `RENDERERS: Dict[str, Callable]` initialised with the single entry `csv` mapped to `_generate_csv`. Later plans register `pdf` and `xlsx` into this same dict; export routes must validate the requested format against `RENDERERS` at call time, never against a hardcoded literal tuple, so registering a renderer is what activates a format.
- `async def _generate_csv(report, reports_dir, tenant_id)` writing `itam_report_{key}_{timestamp}.csv` with `csv.writer`, header row from `report["columns"]`, each cell passed through `_sanitize_cell` imported from `compliance_reporting_data` (CSV formula-injection defence, CWE-1236), and a trailing single-cell row reading `TRUNCATED at 10000 rows` only when `report["truncated"]` is true. Return `{filename, url, generatedAt, rowCount, truncated}` with `url` of the form `/static/reports/{filename}`, matching `compliance_reporting_service._generate_csv`'s return contract.
- `async def _store_report_meta(filename, tenant_id, kind, key, fmt, row_count, username)` upserting into `db.itam_report_exports` keyed on `filename`, storing `filename`, `tenantId`, `kind`, `reportKey`, `format`, `rowCount`, `generatedAt`, `generatedBy` — the document the download route's ownership check reads back.
- Class `ItamReportingService` with `self.reports_dir` set in `__init__` (creating the directory) and `async def generate(self, kind, key, fmt, tenant_id, username, definition=None, db=None)` that builds rows with `limit=None`, looks the renderer up in `RENDERERS`, raises `ValueError` for an unregistered format, calls `_store_report_meta`, and returns the renderer result. Module-level singleton `itam_reporting_service = ItamReportingService()`.
- No FastAPI import in this module, matching the service/endpoint split used by `itam_finance_service.py`.

Backend, `backend/itam_reporting_prebuilt.py` (new — the six-report registry, one entry now):
- `PREBUILT_REPORTS` dict keyed by report key. Each value carries `key`, `title`, `description`, `columns` (declared display headers, used when a run returns zero rows), `defaultSort` (a human-readable description of the fixed order) and `builder` (the async row-building callable).
- The single entry `warranty_expiring`, title `Warranty Expiring`. Its builder reads `db.assets` through the tenant-isolated handle, resolves the alert window once via `itam_finance_service.get_warranty_alert_window(db, tenant_id)`, and for each asset calls `itam_finance_service.compute_warranty_status(purchase_date, warranty_months, now, alert_window_days)` — do not re-derive expiry or the window threshold here. Keep only rows whose `warrantyStatus` equals `itam_finance_service.WARRANTY_STATUS_EXPIRING`. Emit display-header rows with keys `Asset Tag`, `Name`, `Lifecycle Status`, `Warranty Expires`, `Days To Expiry`, `Status`; render a value that is None as an em dash. Sort ascending by `warrantyExpiresAt` so the preview and the export agree.
- `def list_prebuilt_reports()` returning the registry metadata without the `builder` callable (this is what the tab's pre-built section renders).
- `async def run_prebuilt_report(db, key, tenant_id, limit=None)` raising `ValueError` on an unknown key, applying the row ceiling, and returning the `{key,title,columns,rows,rowCount,truncated}` dict `build_report_rows` re-emits.

Backend, `backend/itam_reporting_endpoints.py` (new — structural clone of `compliance_report_endpoints.py`):
- `router = APIRouter(prefix="/api/itam/reports", tags=["ITAM Reports"])`, `from itam_asset_endpoints import _require_itam_admin` (per D-07 and RESEARCH.md Pitfall 1 the real dependency checks `manage:assets`; do not define a parallel gate), and `Depends(_require_itam_admin)` on every route in the file.
- `GET ""` returning `list_prebuilt_reports()`.
- `POST "/prebuilt/{report_key}/run"` with query params `page` (int, ge=1, default 1) and `page_size` (int, ge=1, le=200, default 50). Builds rows with `limit=None`, slices the page in Python, and returns `{key,title,columns,rows,rowCount,page,pageSize,totalPages,truncated}` where `rows` is only the requested page and `rowCount` is the full match count. Unknown key raises 404.
- `POST "/prebuilt/{report_key}/export"` with query param `format` (default `csv`), calling `itam_reporting_service.generate(...)` with `kind` `prebuilt` and no limit; an unregistered format raises 400 naming the formats currently registered; unknown report key raises 404; any other exception raises 500 with a generic detail, matching the existing `ValueError`→404 / `Exception`→500 convention.
- `GET "/download/{filename}"` cloned verbatim from `compliance_report_endpoints.download_report`: resolve `_REPORTS_DIR` and the joined path with `Path(...).resolve()`, reject with 400 when the resolved path does not start with the resolved reports dir, 404 when the file is absent, then for a non-super-admin caller read `db.itam_report_exports.find_one({"filename": filename})` and raise 403 when the document is missing or its `tenantId` differs from the caller's. Serve with `FileResponse` and the same media-type map plus a Content-Disposition attachment header.
- Register the router in `backend/router_registry.py` with `_load(app, "itam_reporting_endpoints", "router")` immediately after the `itam_asset_request_endpoints` line, following the existing one-line-per-router comment style.

Backend tests, `backend/tests/itam_reporting_test_support.py` (new, deliberately not named test_*): clone the `MockTenantIsolatedCollection`/`MockTenantIsolatedDatabase` pair and fixture style from `backend/tests/itam_finance_test_support.py`, keeping the `self._db` attribute name (the `hasattr(db, "_db")` unwrap guard `get_warranty_alert_window` relies on). Seed collections `assets`, `licenses`, `itam_consumables`, `components`, `assignment_history`, `license_assignments`, `asset_models`, `system_settings`, `itam_report_exports` via `_make_col()` so later plans' join tests reuse it unchanged. Provide a `reporting_app` fixture mounting `itam_reporting_endpoints.router` through `make_test_app`, patching `get_database` at the bound name inside both `itam_reporting_endpoints` and `itam_reporting_service`, and patching `verify_permission` on `itam_asset_endpoints` (the module the gate is imported from). Provide a `set_current_tenant_id` callable and asset/licence/consumable factory helpers.

Backend tests, `backend/tests/test_itam_reporting_export.py` (new): the tracer's end-to-end proof. Cover a run returning the expiring asset and excluding an in-warranty and an expired one; a CSV export writing a real file whose header row matches the declared columns and whose data rows equal the full match set (not the preview page); a zero-row export still producing a header-only file; download of that filename returning 200 for the owning tenant; download returning 403 for a different tenant; a filename containing traversal segments returning 400; and an unregistered format returning 400.

Frontend, `services/apiService.ts`: add, next to the existing ITAM client functions and following `generateComplianceReport`/`downloadComplianceReport`'s `authFetch` + error-detail-throw + blob-anchor shape — `fetchItamPrebuiltReports()`, `runItamPrebuiltReport(reportKey, page, pageSize)`, `generateItamReport(kind, reportKey, format)`, and `downloadItamReport(filename)` pointed at `${API_BASE}/itam/reports/...` with `encodeURIComponent` on the filename.

Frontend, `components/itam/ReportsPanel.tsx` (new): props `{ focusReportKey?: string | null; onFocusHandled?: () => void }` — this prop pair is the drill-down seam plan 72-07 attaches to; implement it now for real by auto-running the named report on mount when `focusReportKey` is set and then calling `onFocusHandled`. Render a `Pre-built Reports` section listing the fetched report metadata as cards in a responsive Tailwind grid using the console's `bg-gray-800 rounded-xl border border-gray-700 p-4` card convention, each with a `Run Report` button; a results region below it rendering the preview table (`table w-full text-sm`, `border-b border-gray-700` header) inside an `overflow-x-auto` wrapper with `truncate` plus `title` on cells; pagination controls driven by the response's `page`/`totalPages`; and three-format export buttons rendered from the formats the panel supports today — render only `Export CSV` in this task, with the label text `Export CSV` exactly as the Copywriting Contract specifies. Use the existing `DownloadIcon`/`BarChart3Icon` from `components/icons`, existing `showToast` for errors, no new dependency, no new UI library.

Frontend, `components/itam/ITAMConsole.tsx`: add `'reports'` to the `Tab` union and a `{ id: 'reports', labelKey: 'tabs.reports' }` entry to the `TABS` array positioned immediately before `activity` — the console currently has ten tabs (catalog, lifecycle, finance, requests, licenses, compliance, software, activity, data, settings), so this is the eleventh, correcting D-15's stale six-tab count per RESEARCH.md Pitfall 2. Hold `const [reportFocus, setReportFocus] = useState<string | null>(null)` in `ITAMConsole`, thread it plus `setTab` down through `ItamConsoleBodyProps`, and render `{tab === 'reports' && <ReportsPanel focusReportKey={reportFocus} onFocusHandled={() => setReportFocus(null)} />}` in the existing `main` conditional block.

Frontend, `components/itam/itamI18n.tsx`: add `'tabs.reports'` to both the `en` map (value `Reports`) and the `es` map (value `Informes`), keeping the existing key ordering convention.
  </action>
  <verify>
    <automated>backend/venv/bin/python -m pytest backend/tests/test_itam_reporting_export.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `backend/venv/bin/python -m pytest backend/tests/test_itam_reporting_export.py -x` exits 0 with at least 7 tests collected.
    - `backend/venv/bin/python -c "import router_registry, itam_reporting_endpoints; print(itam_reporting_endpoints.router.prefix)"` run from `backend/` prints `/api/itam/reports`.
    - `grep -v '^\s*#' backend/itam_reporting_endpoints.py | grep -c 'Depends(_require_itam_admin)'` returns at least 4.
    - `grep -c 'itam_reporting_endpoints' backend/router_registry.py` returns at least 1.
    - `grep -c "'reports'" components/itam/ITAMConsole.tsx` returns at least 2 (the `Tab` union member and the `TABS` entry).
    - `grep -c "tabs.reports" components/itam/itamI18n.tsx` returns 2 (en and es).
    - A download request for a filename whose resolved path escapes the reports directory returns HTTP 400 and a download request from a non-owning tenant returns HTTP 403, both asserted in `test_itam_reporting_export.py`.
    - An export of a report whose filter matches zero assets writes a file containing exactly one header line and zero data lines, asserted in `test_itam_reporting_export.py`.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>An ITAM admin can open the Reports tab, run the Warranty Expiring report, page through the preview, export it as CSV, and download the file; a caller from another tenant cannot download it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preview-table states and Reports-tab test coverage</name>
  <read_first>
    components/itam/ReportsPanel.tsx
    components/itam/ITAMConsole.tsx
    src/__tests__/ITAMConsole.test.tsx
    src/__tests__/ITAMActivityLogPanel.test.tsx
    .planning/phases/72-reporting-dashboards/72-UI-SPEC.md
  </read_first>
  <files>
    components/itam/ReportsPanel.tsx
    src/__tests__/ITAMReportsPanel.test.tsx
    src/__tests__/ITAMConsole.test.tsx
  </files>
  <behavior>
    - Reports tab renders the pre-built report list on mount without a full-page skeleton.
    - Running a report that returns rows renders a table whose header cells equal the response `columns`.
    - Running a report that returns zero rows renders the heading "No matching assets" and the body "Adjust your filters and run the report again."
    - While a run is in flight the panel shows the existing "Loading…" text.
    - A rejected run calls showToast with the 'error' variant and leaves the previous table state intact rather than unmounting the panel.
    - A row whose value is null renders an em dash, not an empty cell or the text "undefined".
    - A single-row response renders one table body row.
    - Switching ITAMConsole to the Reports tab mounts ReportsPanel.
  </behavior>
  <action>
Refine the tracer slice with its empty, loading, error and partial states, then lock them with tests.

In `components/itam/ReportsPanel.tsx`, add the states the UI-SPEC's E4 rows require, using the copy verbatim from the Copywriting Contract: the zero-row empty state renders the heading `No matching assets` and the body `Adjust your filters and run the report again.` inside the console's existing `bg-gray-800 rounded-xl border border-gray-700 p-8 text-center` empty-box; the in-flight state renders the existing `text-gray-400 text-sm` Loading indicator; a rejected run routes through `showToast` with the error variant and the export failure uses the Copywriting Contract's export-error sentence. Keep the existing table markup — no restructure.

Create `src/__tests__/ITAMReportsPanel.test.tsx` following `src/__tests__/ITAMActivityLogPanel.test.tsx`'s vitest + Testing Library conventions: `vi.mock` of `../../services/apiService` exposing `fetchItamPrebuiltReports`, `runItamPrebuiltReport`, `generateItamReport` and `downloadItamReport`, then one test per behavior listed above.

Update `src/__tests__/ITAMConsole.test.tsx`: the existing `vi.mock('../../services/apiService', ...)` factory returns an explicit object, so every function `ReportsPanel` imports must be added to it or the mounted panel calls undefined. Add `fetchItamPrebuiltReports` resolving to an empty array, `runItamPrebuiltReport` resolving to a zero-row payload, and `generateItamReport`/`downloadItamReport` as plain mocks; then add a case that clicks the Reports tab and asserts the panel mounts. Also update the file's header comment so it no longer claims a six-tab console.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/ITAMReportsPanel.test.tsx src/__tests__/ITAMConsole.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/ITAMReportsPanel.test.tsx src/__tests__/ITAMConsole.test.tsx` exits 0.
    - `src/__tests__/ITAMReportsPanel.test.tsx` contains a test asserting the literal string `No matching assets` is rendered for a zero-row response.
    - `src/__tests__/ITAMReportsPanel.test.tsx` contains a test asserting `showToast` receives `'error'` when the run promise rejects.
    - `grep -c 'fetchItamPrebuiltReports' src/__tests__/ITAMConsole.test.tsx` returns at least 1.
    - `npx vitest run src/__tests__` exits 0 (no pre-existing frontend test regressed).
  </acceptance_criteria>
  <done>The Reports tab's empty, loading, error and partial states match the UI-SPEC contract and are covered by passing vitest tests; the console's own test suite still passes with the new tab present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/itam/reports/* | Authenticated but untrusted request parameters (report key, page size, format, filename) cross here. |
| API → MongoDB | Report queries execute against multi-tenant collections through the tenant-isolated database handle. |
| API → filesystem (backend/static/reports) | Generated report files are written and later streamed back by filename. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-72-03 | Tampering / Information Disclosure | `itam_reporting_endpoints.download_report` filename param | high | mitigate | Resolve `_REPORTS_DIR` and the joined path with `Path(...).resolve()` and reject when the resolved path does not start with the resolved directory, cloned verbatim from `compliance_report_endpoints.download_report`. |
| T-72-04 | Information Disclosure | Report file download by guessed/enumerated filename | high | mitigate | Persist `{filename, tenantId}` in `db.itam_report_exports` on generate; download reads it back and returns 403 when absent or tenant-mismatched (super-admin roles exempt, matching the compliance route). |
| T-72-05 | Elevation of Privilege | Every `/api/itam/reports/*` route | high | mitigate | `from itam_asset_endpoints import _require_itam_admin` used as `Depends` on every route; no parallel permission check defined in this module (D-07, RESEARCH Pitfall 1). |
| T-72-06 | Tampering | CSV cell values reaching a spreadsheet application | medium | mitigate | Every cell passes through `_sanitize_cell` from `compliance_reporting_data` before it is written (CWE-1236). |
| T-72-08 | Denial of Service | Unbounded report row set on export | medium | mitigate | `MAX_REPORT_ROWS = 10000` ceiling with an explicit `truncated` flag in the response and a truncation marker row in the file — bounded but never silent. |
| T-72-SC | Tampering | npm/pip installs | low | accept | This phase installs zero new packages; RESEARCH.md's Package Legitimacy Audit records reportlab/openpyxl/recharts as pre-existing, already-imported dependencies. No install task exists to gate. |
</threat_model>

<verification>
- `backend/venv/bin/python -m pytest backend/tests/test_itam_reporting_export.py -x` passes.
- `backend/venv/bin/python -m pytest` shows no new failures against the pre-existing baseline.
- `npx vitest run src/__tests__` passes.
- `npm run build` exits 0.
- Manual (end-of-phase human verification): the Reports tab appears as the eleventh tab in the ITAM console and its accent underline matches the tenant brand colour.
</verification>

<success_criteria>
- The Reports tab exists in `ITAMConsole.tsx` and renders `ReportsPanel`.
- `GET /api/itam/reports` lists the pre-built registry; `POST /api/itam/reports/prebuilt/warranty_expiring/run` returns a paginated result; `POST .../export?format=csv` returns a filename; `GET /api/itam/reports/download/{filename}` streams it.
- Path traversal returns 400 and cross-tenant download returns 403, both proven by tests.
- All four downstream module seams exist and are imported by name: `build_report_rows`, `RENDERERS`, `PREBUILT_REPORTS`, `_store_report_meta`.
</success_criteria>

## Artifacts this phase produces

New files: `backend/itam_reporting_service.py`, `backend/itam_reporting_prebuilt.py`, `backend/itam_reporting_endpoints.py`, `backend/tests/itam_reporting_test_support.py`, `backend/tests/test_itam_reporting_export.py`, `components/itam/ReportsPanel.tsx`, `src/__tests__/ITAMReportsPanel.test.tsx`.

New backend symbols: `_REPORTS_DIR`, `_ensure_reports_dir`, `MAX_REPORT_ROWS`, `RENDERERS`, `build_report_rows`, `_generate_csv`, `_store_report_meta`, `ItamReportingService`, `itam_reporting_service` (singleton), `PREBUILT_REPORTS`, `list_prebuilt_reports`, `run_prebuilt_report`, `router` (prefix `/api/itam/reports`).

New backend routes: `GET /api/itam/reports`, `POST /api/itam/reports/prebuilt/{report_key}/run`, `POST /api/itam/reports/prebuilt/{report_key}/export`, `GET /api/itam/reports/download/{filename}`.

New MongoDB collection: `itam_report_exports`.

New frontend symbols: `ReportsPanel` (props `focusReportKey`, `onFocusHandled`), `fetchItamPrebuiltReports`, `runItamPrebuiltReport`, `generateItamReport`, `downloadItamReport`, the `reports` member of `ITAMConsole`'s `Tab` union, the `reportFocus`/`setReportFocus` console state, and the `tabs.reports` i18n key.

## Multi-source coverage audit (phase-level, all 7 plans)

```
SOURCE    | ID     | Feature / Requirement                                        | Plan     | Status
--------- | ------ | ------------------------------------------------------------ | -------- | -------
GOAL      | —      | Custom report building                                       | 03,06    | COVERED
GOAL      | —      | Pre-built reports                                            | 01,02,06 | COVERED
GOAL      | —      | Export functionality                                         | 01,05,06 | COVERED
GOAL      | —      | KPI dashboard                                                | 04,07    | COVERED
REQ       | REP-01 | Custom Report Builder (build + save)                         | 03,06    | COVERED
REQ       | REP-02 | Pre-built reports for asset/licence data                     | 01,02,06 | COVERED
REQ       | REP-03 | Export support (PDF, CSV, Excel)                             | 01,05,06 | COVERED
REQ       | REP-04 | ITAM dashboard with KPIs/visualisations                      | 04,07    | COVERED
RESEARCH  | —      | New itam_reporting_service + endpoints module pair            | 01       | COVERED
RESEARCH  | —      | itam_reporting_pdf / itam_reporting_excel renderer clones      | 05       | COVERED
RESEARCH  | —      | Shared row-builder feeding three renderers                    | 01,05    | COVERED
RESEARCH  | —      | Tenant-safe download clone (path traversal + ownership)       | 01       | COVERED
RESEARCH  | —      | Import _require_itam_admin (manage:assets, Pitfall 1)         | 01,03,04 | COVERED
RESEARCH  | —      | Eleven-tab console, not seven (Pitfall 2)                     | 01       | COVERED
RESEARCH  | —      | Consumable id shape caveat (Pitfall 3)                        | 02       | COVERED
RESEARCH  | —      | Python-side joins, no aggregation join (Pitfall 4)            | 02,03    | COVERED
RESEARCH  | —      | Closed-vocabulary filter translator (Pitfall 5)               | 03       | COVERED
RESEARCH  | —      | Reuse compute_book_value / warranty / seat / overdue helpers   | 02,04    | COVERED
RESEARCH  | —      | recharts KPI clone of CXODashboard                            | 07       | COVERED
RESEARCH  | —      | Six Wave-0 test files + shared cross-entity fixture module     | 01-07    | COVERED
CONTEXT   | D-01   | Asset-rooted reports only                                    | 03       | COVERED
CONTEXT   | D-02   | Field + filter picker UX                                     | 03,06    | COVERED
CONTEXT   | D-03   | Three operator families                                      | 03,06    | COVERED
CONTEXT   | D-04   | Saved definitions shared tenant-wide                         | 03,06    | COVERED
CONTEXT   | D-05   | No cap on saved reports                                      | 03,06    | COVERED
CONTEXT   | D-06   | On-screen paginated preview before export                    | 01,05,06 | COVERED
CONTEXT   | D-07   | Admin gate via _require_itam_admin                           | 01,04    | COVERED
CONTEXT   | D-08   | Six pre-built reports                                        | 01,02,06 | COVERED
CONTEXT   | D-09   | Pre-built reports fixed / no user params                     | 02,06    | COVERED
CONTEXT   | D-10   | One Reports tab, two sections                                | 01,06    | COVERED
CONTEXT   | D-11   | reportlab + openpyxl export pattern                          | 01,05    | COVERED
CONTEXT   | D-12   | One shared report-data function                              | 01,03,05 | COVERED
CONTEXT   | D-13   | On-demand export only, no scheduling                         | 01       | COVERED
CONTEXT   | D-14   | Export applies to custom AND pre-built                       | 01,03,05,06 | COVERED
CONTEXT   | D-15   | Dashboard is a new console tab                               | 01,07    | COVERED
CONTEXT   | D-16   | Four KPIs                                                    | 04,07    | COVERED
CONTEXT   | D-17   | recharts, no new charting dependency                         | 07       | COVERED
CONTEXT   | D-18   | KPI tiles clickable, drill into reports                      | 01,04,07 | COVERED
CONTEXT   | D-19   | Optional reorderThreshold + fixed fallback heuristic         | 01,02    | COVERED
CONTEXT   | D-20   | Preview and export share one data-building path              | 01,03    | COVERED
```

Excluded (not gaps): CONTEXT.md Deferred Ideas — scheduled/recurring ITAM report delivery (D-13's future integration point) and non-asset-rooted report roots (D-01's deferral). `api-coverage.cjs --json` reported `detected: false` for this phase (no external API/SDK integration), so no COVERAGE.md is produced. The schema-push gate found no ORM schema files (this project is MongoDB/Motor with Pydantic models, no Payload/Prisma/Drizzle/Supabase/TypeORM surface), so no schema-push task is injected.

Canon-referral breadcrumbs (surfaced by the prohibition recall, deliberately NOT minted as prohibitions): NoSQL injection via a client-supplied operator, path traversal on the download filename, CSV/XLSX formula injection, cross-tenant data leakage, and generated-file retention are all canon security/compliance items covered by `/gsd-secure-phase` and this phase's STRIDE registers — breadcrumbed, not duplicated into `must_haves.prohibitions`.

<output>
Create `.planning/phases/72-reporting-dashboards/72-01-SUMMARY.md` when done
</output>
