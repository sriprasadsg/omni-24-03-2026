---
phase: 63-close-gap-itam-lic-02-03-rbac-itam-cat-05-label-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/itam_consumable_endpoints.py
  - backend/itam_component_endpoints.py
  - backend/tests/test_itam_consumable.py
  - backend/tests/test_itam_component.py
autonomous: true
requirements: [ITAM-LIC-02, ITAM-LIC-03, ITAM-UI-01]
tags: [rbac, fastapi, authorization, itam, gap-closure]

must_haves:
  truths:
    - "A non-admin authenticated user (no manage:assets permission) receives HTTP 403 from every route on backend/itam_consumable_endpoints.py (ITAM-LIC-02, D-05)."
    - "A non-admin authenticated user receives HTTP 403 from every route on backend/itam_component_endpoints.py, on BOTH the `router` and the `asset_components_router` objects (ITAM-LIC-03, D-05)."
    - "An admin user (manage:assets granted) still succeeds on every pre-existing consumable and component route — all 16 pre-existing tests pass unmodified in behavior (ITAM-LIC-02, ITAM-LIC-03)."
    - "The 403 assertions are real, not harness artifacts: the monkeypatched `verify_permission` is patched on the same module object the route dependency resolves at request time."
    - "All 8 backend/itam_*_endpoints.py router files gate on the single shared `_require_itam_admin` definition; none redefines it locally (ITAM-UI-01, D-05)."
  artifacts:
    - "backend/itam_consumable_endpoints.py — imports `_require_itam_admin` from itam_asset_endpoints; zero bare-authentication dependencies remain"
    - "backend/itam_component_endpoints.py — same, across both router objects"
    - "backend/tests/test_itam_consumable.py — new `TestConsumableRbac` class"
    - "backend/tests/test_itam_component.py — new `TestComponentRbac` class (2 tests, one per router object)"
  key_links:
    - "backend/itam_consumable_endpoints.py -> itam_asset_endpoints._require_itam_admin (import, not local redefinition)"
    - "backend/itam_component_endpoints.py -> itam_asset_endpoints._require_itam_admin (import, both router objects)"
    - "backend/tests/test_itam_consumable.py::consumable_app fixture -> the UNPREFIXED `itam_asset_endpoints` module object (the one the route dependency actually resolves)"
    - "_require_itam_admin -> rbac_utils.verify_permission(current_user, 'manage:assets') -> 403"
---

<objective>
Close the BLOCKER RBAC gap identified in `.planning/milestones/v4.0-MILESTONE-AUDIT.md`: `backend/itam_consumable_endpoints.py` (7 routes) and `backend/itam_component_endpoints.py` (5 routes across two router objects) authenticate but never authorize, so any authenticated user of any role can mutate ITAM sub-inventory by calling the API directly, bypassing the admin-only console. All 6 sibling ITAM routers already gate on `_require_itam_admin`.

Scope is exactly D-05: swap the route dependency to `Depends(_require_itam_admin)` imported from `itam_asset_endpoints`, remove the now-unused authentication import, and add a 403-for-non-admin regression test per router object, mirroring `test_itam_finance.py::TestFinanceRbacAndTenantIsolation`. Per D-06, the separately-flagged `manage:assets` vs `manage:itam`/`view:itam` permission-string coupling tech debt is explicitly NOT touched, NOT tested, and stays tracked in the milestone audit.

Purpose: eliminate Broken Function Level Authorization (OWASP API5:2023) on two production routers, restoring the intent Phase 60's own CONTEXT.md stated ("`_require_itam_admin` — RBAC gate to reuse, not reinvent").
Output: 2 edited router files, 2 edited test files, 3 new 403 tests, zero new files, zero new packages.
</objective>

<execution_context>
@/home/user/enterprise-omni-agent-ai-platform/.claude/gsd-core/workflows/execute-plan.md
@/home/user/enterprise-omni-agent-ai-platform/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-close-gap-itam-lic-02-03-rbac-itam-cat-05-label-ui/63-CONTEXT.md
@.planning/phases/63-close-gap-itam-lic-02-03-rbac-itam-cat-05-label-ui/63-RESEARCH.md
@.planning/phases/63-close-gap-itam-lic-02-03-rbac-itam-cat-05-label-ui/63-PATTERNS.md

Reference implementations to clone (read, do not modify):
@backend/itam_asset_endpoints.py
@backend/itam_license_endpoints.py
@backend/tests/test_itam_finance.py
</context>

## Artifacts this phase produces

**Plan 63-01 (this plan) — no new files, no new production symbols.**

| Artifact | Kind | File |
|----------|------|------|
| `TestConsumableRbac` | new test class | `backend/tests/test_itam_consumable.py` |
| `TestConsumableRbac.test_rbac_denied_create_returns_403` | new test method | `backend/tests/test_itam_consumable.py` |
| `TestComponentRbac` | new test class | `backend/tests/test_itam_component.py` |
| `TestComponentRbac.test_rbac_denied_create_returns_403` | new test method | `backend/tests/test_itam_component.py` |
| `TestComponentRbac.test_rbac_denied_list_asset_components_returns_403` | new test method | `backend/tests/test_itam_component.py` |

**Symbols imported (not created):** `_require_itam_admin` (from `itam_asset_endpoints`), `TokenData` (from `auth_types`).
**Symbols removed:** the module-level `get_current_user` import in both router files; the unused `itam_asset_verify_permission` alias in `backend/tests/test_itam_consumable.py`.

<!-- planner-discipline-allow: Depends(get_current_user) -->
<!-- planner-discipline-allow: from authentication_service import get_current_user -->

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Consumables router — RBAC gate proven end-to-end (request → dependency → verify_permission → 403)</name>

  <precondition>The backend virtualenv exists and pytest runs: `backend/venv/bin/python -m pytest --version` exits 0. Baseline for this file today is 7 passing tests.</precondition>

  <reversibility rating="reversible">Adding an authorization dependency to a router is a one-line-per-route change that can be reverted by restoring the prior dependency; no data migration, no persisted state, no API contract change for authorized callers.</reversibility>

  <files>backend/itam_consumable_endpoints.py, backend/tests/test_itam_consumable.py</files>

  <read_first>
    - backend/itam_consumable_endpoints.py (all 122 lines — the file being modified)
    - backend/itam_asset_endpoints.py lines 1-45 (the `_require_itam_admin` definition to import; do NOT modify this file)
    - backend/itam_license_endpoints.py lines 1-40 (canonical target import + dependency style to replicate)
    - backend/tests/test_itam_consumable.py (all 281 lines — especially line 17, the `consumable_app` fixture at lines 140-146, and one existing test to match style)
    - backend/tests/test_itam_finance.py lines 185-200 (the exact working 403-test pattern to clone; note `test_itam_license.py` has NO RBAC test despite CONTEXT.md's pointer)
  </read_first>

  <behavior>
    - Test 1 (new, RED first): with `verify_permission` stubbed to return False and a non-admin `TokenData` (role="user", tenant_id="tenant-a"), `POST /api/itam/consumables` returns 403. On today's unmodified router this test FAILS (the route returns 201), which is the proof the gap is real.
    - Test 2 (existing, must stay GREEN): all 7 pre-existing tests in `test_itam_consumable.py` still pass with zero changes to their bodies — the `consumable_app` fixture already stubs `verify_permission` to True.
  </behavior>

  <action>
Do the test work first so the RED state is observed on unmodified router code.

**Step 1 — fix the fixture's patch target (blocking correctness issue, not cosmetic).**
In `backend/tests/test_itam_consumable.py` the `consumable_app` fixture (line ~142) currently binds the module via `import backend.itam_asset_endpoints as itam_asset_endpoints` and monkeypatches `verify_permission` on it. That is the wrong module object. This test harness puts BOTH the repository root and `backend/` on `sys.path` (`backend/tests/conftest.py` line 8 plus `backend/__init__.py`), so `backend.itam_asset_endpoints` and `itam_asset_endpoints` are two distinct module objects with two distinct `__dict__`s — verified in this environment: importing both and comparing identity returns False. After Step 3, `backend/itam_consumable_endpoints.py` resolves `_require_itam_admin` from the UNPREFIXED `itam_asset_endpoints`, and that function looks up `verify_permission` in the unprefixed module's globals at request time. Patching the prefixed copy would therefore be a no-op and the real `rbac_utils.verify_permission` would run against the mock database, breaking all 7 existing tests.

Change the fixture to bind the unprefixed module — `import itam_asset_endpoints` — exactly as `backend/tests/test_itam_component.py` line 144 and `backend/tests/test_itam_finance.py` line 191 already do. Keep the existing `monkeypatch.setattr(itam_asset_endpoints, "verify_permission", AsyncMock(return_value=True))` line unchanged.

Also delete line 17, `from backend.itam_asset_endpoints import verify_permission as itam_asset_verify_permission` — grep confirms the `itam_asset_verify_permission` alias has no other reference in the file, so it is dead code with a misleading module binding.

**Step 2 — add the 403 regression test.**
Append a class `TestConsumableRbac` to `backend/tests/test_itam_consumable.py`, cloning `TestFinanceRbacAndTenantIsolation` from `backend/tests/test_itam_finance.py` lines 185-197. One method, `test_rbac_denied_create_returns_403`, decorated `@pytest.mark.asyncio`, taking `(self, mock_db, consumable_app, monkeypatch)`. Inside the test body: `import itam_asset_endpoints` (unprefixed) and `monkeypatch.setattr(itam_asset_endpoints, "verify_permission", AsyncMock(return_value=False))` — applied inside the test so it overrides the fixture-level True stub. Build `current_user = make_token_data(tenant_id="tenant-a", role="user", username="user@example.com")` and set `consumable_app.dependency_overrides[real_get_current_user] = lambda: current_user`. Issue `await ac.post("/api/itam/consumables", json={"name": "Cat6 Cable", "quantity": 10})` through `AsyncClient(transport=ASGITransport(app=consumable_app), base_url="http://testserver")` and assert `r.status_code == 403, r.text`. Use `role="user"` and never a super-admin role name — `rbac_utils.verify_permission` short-circuits to True for super-admin variants, which would make the test vacuous.

Run the file now and confirm the new test FAILS (RED). Do not proceed until you have seen it fail for the right reason (a success status instead of 403).

**Step 3 — apply the RBAC swap (per D-05).**
In `backend/itam_consumable_endpoints.py`: add `from auth_types import TokenData` and `from itam_asset_endpoints import _require_itam_admin` to the import block; delete the module-level import of `get_current_user` from `authentication_service` at line 8 (it has no other use in this file — verified). Then replace the current-user parameter on all 7 route handlers (lines 25, 40, 54, 72, 88, 105, 120 — `create_consumable_endpoint`, `list_consumables_endpoint`, `get_consumable_endpoint`, `update_consumable_endpoint`, `delete_consumable_endpoint`, `checkout_consumable_endpoint`, `checkin_consumable_endpoint`) with the annotated form `current_user: TokenData = Depends(_require_itam_admin),`, matching `itam_license_endpoints.py` exactly.

Do NOT redefine `_require_itam_admin` locally, do NOT add a `dependencies=[...]` gate at `APIRouter(...)` or at registration in `router_registry.py`, and do NOT alter any handler body — `itam_consumable_service` reads `getattr(current_user, "tenant_id", ...)` and `_require_itam_admin` returns the identical `TokenData` object, so service code is unaffected.

Run the file again and confirm GREEN: 8 passed.
  </action>

  <verify>
    <automated>cd backend && venv/bin/python -m pytest tests/test_itam_consumable.py -q 2>&1 | tail -3</automated>
  </verify>

  <acceptance_criteria>
    - `cd backend && venv/bin/python -m pytest tests/test_itam_consumable.py -q` reports `8 passed` (7 pre-existing + 1 new) and 0 failed.
    - `cd backend && venv/bin/python -m pytest tests/test_itam_consumable.py -k Rbac -x -q` reports `1 passed`.
    - `grep -v '^\s*#' backend/itam_consumable_endpoints.py | grep -c 'Depends(get_current_user)'` outputs `0`.
    - `grep -v '^\s*#' backend/itam_consumable_endpoints.py | grep -c 'from authentication_service import get_current_user'` outputs `0`.
    - `grep -c '_require_itam_admin' backend/itam_consumable_endpoints.py` outputs `8` (1 import + 7 route dependencies).
    - `grep -c 'async def _require_itam_admin' backend/itam_consumable_endpoints.py` outputs `0` — imported, never redefined.
    - `grep -c 'backend.itam_asset_endpoints' backend/tests/test_itam_consumable.py` outputs `0` — no prefixed-module patch target remains.
    - Source assertion: `backend/tests/test_itam_consumable.py` contains `class TestConsumableRbac` and, within it, `AsyncMock(return_value=False)`.
    - No existing test body in `backend/tests/test_itam_consumable.py` was modified: `git diff --stat backend/tests/test_itam_consumable.py` shows a net addition, and `git diff backend/tests/test_itam_consumable.py` shows removals only on the two import/fixture lines described in Steps 1.
  </acceptance_criteria>

  <done>Every route on the consumables router requires `manage:assets`; a non-admin gets 403 proven by a test that fails on the pre-fix code; all 7 pre-existing tests pass unmodified; the test harness patches the module the dependency actually resolves.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Components router — RBAC gate on BOTH router objects</name>

  <reversibility rating="reversible">Same shape as Task 1 — a per-route dependency swap, revertible by restoring the prior dependency.</reversibility>

  <files>backend/itam_component_endpoints.py, backend/tests/test_itam_component.py</files>

  <read_first>
    - backend/itam_component_endpoints.py (all 94 lines — note TWO `APIRouter` objects: `asset_components_router` declared at line 20 with its single route at lines 23-35, and `router` declared at line 12 with 4 routes at lines 37-94)
    - backend/router_registry.py lines 82-90 (confirms `router` and `asset_components_router` are registered as two separate `_load()` calls — both are live)
    - backend/tests/test_itam_component.py (all 358 lines — the `component_app` fixture at lines 142-148 already binds the UNPREFIXED module correctly; it needs no change)
    - backend/itam_finance_endpoints.py lines 18-35 (5-occurrence reference implementation of the same swap)
    - backend/tests/test_itam_finance.py lines 185-200 (403-test pattern)
  </read_first>

  <behavior>
    - Test 1 (new, RED first): non-admin `POST /api/itam/components` returns 403 — covers the `router` object.
    - Test 2 (new, RED first): non-admin `GET /api/assets/asset-1/components` returns 403 — covers the `asset_components_router` object independently. Without this second test, missing the sub-resource router would ship silently.
    - Test 3 (existing, must stay GREEN): all 9 pre-existing tests in `test_itam_component.py` pass with no body changes.
  </behavior>

  <action>
Write the two new tests first, observe RED on unmodified router code, then swap.

**Step 1 — add the 403 tests.** Append a class `TestComponentRbac` to `backend/tests/test_itam_component.py`, cloning the `TestFinanceRbacAndTenantIsolation` shape. Two `@pytest.mark.asyncio` methods taking `(self, mock_db, component_app, monkeypatch)`:
- `test_rbac_denied_create_returns_403` — stub `itam_asset_endpoints.verify_permission` (unprefixed module, `import itam_asset_endpoints` inside the test) with `AsyncMock(return_value=False)`, set `component_app.dependency_overrides[real_get_current_user]` to a `make_token_data(tenant_id="tenant-a", role="user", username="user@example.com")` lambda, `await ac.post("/api/itam/components", json={"name": "8GB DIMM", "quantity": 4})`, assert 403.
- `test_rbac_denied_list_asset_components_returns_403` — identical setup, `await ac.get("/api/assets/asset-1/components")`, assert 403. This route lives on `asset_components_router`, which is a separate object mounted by the same `component_app` fixture (`make_test_app(router, asset_components_router)`), so no fixture change is needed.

The `component_app` fixture already binds the unprefixed `itam_asset_endpoints` module (line 144), so unlike Task 1 there is no patch-target correction to make here — leave the fixture alone. Use `role="user"`, never a super-admin role name.

Run the file and confirm both new tests FAIL (RED) against the current router.

**Step 2 — apply the RBAC swap (per D-05).** In `backend/itam_component_endpoints.py`: add `from auth_types import TokenData` and `from itam_asset_endpoints import _require_itam_admin`; delete the line-8 module-level import of `get_current_user` from `authentication_service` (no other use in this file — verified). Replace the current-user parameter on all 5 handlers with `current_user: TokenData = Depends(_require_itam_admin),`:
- `list_asset_components_endpoint` (line ~32, on `asset_components_router` — this is the one that is easy to miss, it is declared ABOVE `router`'s own routes in file order)
- `create_component_endpoint` (line ~47), `list_components_endpoint` (line ~62 — currently the last parameter with no trailing comma; keep it valid), `attach_component_endpoint` (line ~77), `detach_component_endpoint` (line ~92)

Normalize the inconsistent spacing in this file (some lines currently use spaces around the `=`, some do not) to the annotated `current_user: TokenData = Depends(_require_itam_admin)` form used by every sibling router. Do not redefine the gate locally, do not add router-level or registry-level `dependencies=`, and do not touch handler bodies.

Run the file again and confirm GREEN: 11 passed.
  </action>

  <verify>
    <automated>cd backend && venv/bin/python -m pytest tests/test_itam_component.py -q 2>&1 | tail -3</automated>
  </verify>

  <acceptance_criteria>
    - `cd backend && venv/bin/python -m pytest tests/test_itam_component.py -q` reports `11 passed` (9 pre-existing + 2 new) and 0 failed.
    - `cd backend && venv/bin/python -m pytest tests/test_itam_component.py -k Rbac -q` reports `2 passed`.
    - `grep -v '^\s*#' backend/itam_component_endpoints.py | grep -c 'Depends(get_current_user)'` outputs `0`.
    - `grep -v '^\s*#' backend/itam_component_endpoints.py | grep -c 'from authentication_service import get_current_user'` outputs `0`.
    - `grep -c '_require_itam_admin' backend/itam_component_endpoints.py` outputs `6` (1 import + 5 route dependencies).
    - `grep -c 'async def _require_itam_admin' backend/itam_component_endpoints.py` outputs `0`.
    - Region assertion for the sub-resource router: `sed -n '20,40p' backend/itam_component_endpoints.py | grep -c '_require_itam_admin'` outputs `1` — proves `asset_components_router`'s route was not skipped.
    - Source assertion: `backend/tests/test_itam_component.py` contains `class TestComponentRbac`, and a test issuing a GET against the `/api/assets/{asset_id}/components` path asserting 403.
    - `backend/itam_component_endpoints.py` remains under 500 lines (project file-size rule).
  </acceptance_criteria>

  <done>All 5 component routes across both router objects require `manage:assets`; non-admins get 403 on both the `/api/itam/components` router and the `/api/assets/{id}/components` sub-resource router; all 9 pre-existing tests pass unmodified.</done>
</task>

<task type="auto">
  <name>Task 3: Cross-router authorization invariant + full backend regression gate</name>

  <files>backend/itam_consumable_endpoints.py, backend/itam_component_endpoints.py</files>

  <read_first>
    - backend/itam_consumable_endpoints.py and backend/itam_component_endpoints.py (post-edit state from Tasks 1-2)
    - .planning/milestones/v4.0-MILESTONE-AUDIT.md lines 110-150 (the two BLOCKER findings and the recommended remediation this plan implements)
  </read_first>

  <action>
Assert the phase-level invariant and prove nothing else regressed — in particular that the module-identity fix in Task 1 did not disturb any other test file (grep confirmed `test_itam_consumable.py` was the only file binding the prefixed module, but verify empirically).

Run the full backend suite with `cd backend && venv/bin/python -m pytest tests/ -q`. This repository has 3 known pre-existing failures unrelated to ITAM (e2e evidence, rust parity, agentic tool_choice). Assert that the failure count did not grow and that no failure is in an `tests/test_itam_*.py` node id. If a new failure appears, fix it within this plan's four files — do not mask it by editing an unrelated test.

Then confirm the audit's remediation item 1 is fully satisfied: every `backend/itam_*_endpoints.py` file now references the shared gate. Do not modify any file in this task unless a regression is found; this is a gate, not an edit.

Per D-06, do NOT add, modify, or test anything relating to the `manage:assets` versus `manage:itam`/`view:itam` permission-string coupling — that tech debt stays tracked in the milestone audit and out of this phase.
  </action>

  <verify>
    <automated>cd backend && venv/bin/python -m pytest tests/ -q 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - Full backend suite: failures are a subset of the 3 known pre-existing failures; `cd backend && venv/bin/python -m pytest tests/ -q 2>&1 | grep -E '^(FAILED|ERROR)' | grep -c 'test_itam_'` outputs `0`.
    - `cd backend && venv/bin/python -m pytest tests/test_itam_consumable.py tests/test_itam_component.py -q 2>&1 | tail -1` reports `19 passed`.
    - Invariant: `cd backend && for f in itam_*_endpoints.py; do grep -q '_require_itam_admin' "$f" || echo "UNGATED:$f"; done` prints nothing — all 8 ITAM router files reference the shared gate.
    - Single definition: `cd backend && grep -rc 'async def _require_itam_admin' itam_*_endpoints.py | grep -v ':0$'` matches only `itam_asset_endpoints.py:1` — exactly one definition project-wide.
    - `cd backend && venv/bin/python -c "import itam_consumable_endpoints, itam_component_endpoints"` exits 0 — no import cycle introduced by depending on `itam_asset_endpoints`.
  </acceptance_criteria>

  <done>The full backend suite shows no new failures, all 8 ITAM routers gate on one shared `_require_itam_admin` definition, and both previously ungated routers import rather than redefine it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated non-admin client → ITAM consumables/components API | A valid JWT crosses this boundary. Authentication is proven; authorization (role/permission) is what this plan adds. The frontend nav gate is NOT a boundary — it is client-side and trivially bypassed by calling the API directly with a valid token. |
| FastAPI route → tenant-isolated database handle | Already enforced via `TenantIsolatedDatabase`; unchanged by this plan. Cross-tenant reads are already blocked; the gap being closed is intra-tenant privilege escalation. |
| test harness → module registry (`sys.modules`) | A verification-integrity boundary: a stub applied to the wrong module object produces a passing test that proves nothing about the deployed dependency chain. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-63-01 | Elevation of Privilege | `backend/itam_consumable_endpoints.py` — all 7 routes, notably `POST /{id}/checkout`, `POST /{id}/checkin`, `DELETE /{id}` | high | mitigate | Task 1: swap the route dependency to `Depends(_require_itam_admin)` per D-05, which calls `verify_permission(current_user, "manage:assets")` and raises 403. Proven by `TestConsumableRbac::test_rbac_denied_create_returns_403`, which fails on the pre-fix code. |
| T-63-02 | Elevation of Privilege | `backend/itam_component_endpoints.py` — `router` (create/list/attach/detach) | high | mitigate | Task 2: identical dependency swap on all 4 `router` routes. Proven by `TestComponentRbac::test_rbac_denied_create_returns_403`. Attach/detach mutate parent-asset composition, so this is state-changing, not advisory. |
| T-63-03 | Elevation of Privilege | `backend/itam_component_endpoints.py` — `asset_components_router` (`GET /api/assets/{asset_id}/components`), registered separately in `router_registry.py` line 88 | high | mitigate | Task 2 explicitly covers the second router object plus a dedicated test (`test_rbac_denied_list_asset_components_returns_403`) and a region-scoped grep on lines 20-40. Fixing only `router` would leave this route open — the single most likely way this fix ships incomplete. |
| T-63-04 | Information Disclosure | Read routes on both routers (consumable list/detail, asset component list) exposing tenant sub-inventory to any authenticated role | medium | mitigate | Same gate, applied to GET routes as well as mutating ones — the swap is per-route and unconditional, not write-only. |
| T-63-05 | Tampering | Verification signal: `consumable_app` stubs `verify_permission` on `backend.itam_asset_endpoints` while the route dependency resolves `itam_asset_endpoints` — two distinct module objects (verified: identity comparison returns False) | high | mitigate | Task 1 Step 1 repoints the fixture at the unprefixed module, matching `test_itam_component.py` and `test_itam_finance.py`. Without this, existing admin-path tests break loudly and the RBAC evidence for ITAM-LIC-02 would be untrustworthy. Task 3 re-runs the full suite to confirm no other test file had the same mismatch. |
| T-63-06 | Elevation of Privilege | Drift risk — a second, locally redefined copy of the RBAC gate diverging from the canonical one | medium | mitigate | Import-only enforcement: Tasks 1-2 forbid local redefinition; Task 3 asserts exactly one `async def _require_itam_admin` exists across all `itam_*_endpoints.py`. |
| T-63-07 | Spoofing | Test using a super-admin role name would make the 403 assertion vacuous, since `rbac_utils.verify_permission` short-circuits to True for super-admin variants | medium | mitigate | Tests pin `role="user"` explicitly and stub `verify_permission` to False, exercising the deny path rather than relying on role-table lookups against a mock database. |
| T-63-SC | Tampering | npm/pip/cargo installs (supply chain) | low | accept | This plan installs zero packages — RESEARCH.md's Package Legitimacy Audit records "Not applicable"; the Package Legitimacy Gate protocol is out of scope by its own trigger condition. No `[ASSUMED]`/`[SUS]`/`[SLOP]` packages exist to verify. Rationale for acceptance: no new dependency surface is introduced. |

**ASVS L1 / V4 Access Control:** T-63-01, T-63-02, T-63-03, T-63-05 are `high` and all carry `mitigate` dispositions with concrete, test-backed implementations, satisfying the `security_block_on: high` gate.
</threat_model>

<verification>
- `cd backend && venv/bin/python -m pytest tests/test_itam_consumable.py tests/test_itam_component.py -q` → `19 passed` (16 pre-existing + 3 new).
- `cd backend && venv/bin/python -m pytest tests/ -q` → no new failures beyond the 3 known pre-existing non-ITAM failures.
- `grep -v '^\s*#' backend/itam_consumable_endpoints.py backend/itam_component_endpoints.py | grep -c 'Depends(get_current_user)'` → `0`.
- `cd backend && for f in itam_*_endpoints.py; do grep -q '_require_itam_admin' "$f" || echo "UNGATED:$f"; done` → no output.
- `git diff --stat backend/` shows exactly 4 changed files and no changes to `itam_asset_endpoints.py`, `router_registry.py`, or any service module.
</verification>

<success_criteria>
- ITAM-LIC-02: non-admin gets 403 on consumables routes (automated test).
- ITAM-LIC-03: non-admin gets 403 on component routes, both router objects (2 automated tests).
- ITAM-UI-01: the API-layer admin gate now matches the console's admin-only intent on all 8 ITAM routers.
- Admin behavior unchanged: 16 pre-existing tests pass with unmodified bodies.
- D-05 implemented exactly; D-06 untouched; zero new files, zero new packages, zero handler-body changes.
</success_criteria>

<output>
Create `.planning/phases/63-close-gap-itam-lic-02-03-rbac-itam-cat-05-label-ui/63-01-SUMMARY.md` when done.
</output>
