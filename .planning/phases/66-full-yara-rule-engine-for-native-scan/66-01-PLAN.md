---
phase: 66-full-yara-rule-engine-for-native-scan
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml (workspace root)
  - agent-rust/Cargo.toml
  - agent-rust/build.rs
  - agent-rust/src/main.rs # For temporary test
  - agent-rust/src/lib.rs # If a new public module is added
autonomous: true
requirements:
  - NSCAN-01
user_setup: []

must_haves:
  truths:
    - The `yara` crate is successfully added as a dependency.
    - A minimal YARA rule can be compiled and used to scan a buffer on the host machine.
    - `omni-agent-rs` successfully cross-compiles for `x86_64-pc-windows-gnu` with the `yara` crate integrated.
  artifacts:
    - `omni-agent-rs/Cargo.toml` updated with `yara` dependency.
    - `omni-agent-rs/target/x86_64-pc-windows-gnu/debug/omni-agent-rs.exe` (or release) exists and links `yara`.
  key_links:
    - `yara` crate's C library linking for Windows cross-compilation.
    - Successful compilation of a simple YARA rule.
---

<objective>
Integrate the `yara` crate into the `omni-agent-rs` project and verify that it compiles correctly for both the host and the Windows target (`x86_64-pc-windows-gnu`). This plan addresses the primary blocker identified in research: cross-compilation with C dependencies.

Purpose: Establish the foundational build environment and verify the core `yara` library can be used.
Output: A buildable `omni-agent-rs` project with `yara` dependency, proven to cross-compile for Windows.
</objective>

<execution_context>
@/home/user/enterprise-omni-agent-ai-platform/.claude/gsd-core/workflows/execute-plan.md
@/home/user/enterprise-omni-agent-ai-platform/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-full-yara-rule-engine-for-native-scan/RESEARCH.md
@.planning/milestones/v3.4-phases/50-native-security-scanning/50-CONTEXT.md
</context>

<tasks>

<task type="tracer">
  <name>Tracer: Integrate `yara` crate and verify basic host compilation</name>
  <files>
    omni-agent-rs/Cargo.toml
    omni-agent-rs/src/main.rs
  </files>
  <action>
    Add the `yara` crate to `omni-agent-rs/Cargo.toml`. Since `yara` links against a C library, ensure the `build` feature is enabled for static linking if needed, or follow its documentation for basic integration.
    Temporarily add a simple YARA rule compilation and scan into `omni-agent-rs/src/main.rs` to verify the crate is linked and functional. This test code will be removed in a later task after the engine module is established.

    Note: This explicitly deviates from Phase 50's D-01 ("No C libraries") as `yara` crate requires C library bindings. This deviation is per Phase 66's roadmap goal to achieve "Full YARA-rule engine" after `yara-x` was rejected for JIT bloat.
  </action>
  <verify>
    <automated>
      cd omni-agent-rs && cargo build && cargo run
      # The output should show the YARA test code executing, e.g., "Scan successful, rule 'test_rule' matched."
    </automated>
  </verify>
  <done>
    `omni-agent-rs` builds successfully with the `yara` crate, and a basic YARA rule compilation and scan function executes correctly on the host.
  </done>
</task>

<task type="auto">
  <name>Configure and verify `yara` crate cross-compilation for Windows</name>
  <files>
    omni-agent-rs/Cargo.toml
    omni-agent-rs/build.rs
  </files>
  <action>
    Ensure `mingw-w64` toolchain is available for Rust target `x86_64-pc-windows-gnu`.
    Configure `omni-agent-rs/build.rs` (or `Cargo.toml` as per `yara` crate documentation) to correctly link the YARA C library when cross-compiling to Windows. This often involves specifying `vcpkg` or manual linker flags for the target.
    The goal is to get `cargo build --target x86_64-pc-windows-gnu` to pass without linker errors related to YARA.
  </action>
  <verify>
    <automated>
      cd omni-agent-rs && cargo build --target x86_64-pc-windows-gnu
      # Verify the command completes successfully without linker errors.
    </automated>
  </verify>
  <done>
    `omni-agent-rs` successfully cross-compiles to `x86_64-pc-windows-gnu` with the `yara` crate, and the resulting executable links correctly against the YARA C library for the Windows target.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| omni-agent-rs internal | Interaction with new `yara` library bindings |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-66-01 | Tampering | YARA rule source | high | mitigate | Rely on existing feed bundle signature verification (D-03 from Phase 50) for rules loaded. |
| T-66-02 | Denial of Service | Malformed/complex YARA rules | medium | mitigate | YARA rule compilation errors handled gracefully; explore YARA scan timeouts in `yara` crate. |
| T-66-03 | Information Disclosure | YARA rules themselves | low | accept | Rules are from trusted, internal feed. |
| T-66-SC | Tampering | npm/pip/cargo installs | high | mitigate | slopcheck + blocking human checkpoint for `yara` crate (already flagged in RESEARCH.md as `[ASSUMED]`). |
</threat_model>

<verification>
Successfully build for both host and Windows target after `yara` crate integration.
</verification>

<success_criteria>
- `omni-agent-rs` builds and runs on the host with `yara` functionality.
- `omni-agent-rs` successfully cross-compiles for `x86_64-pc-windows-gnu` with `yara` functionality.
</success_criteria>

<output>
Create `.planning/phases/66-full-yara-rule-engine-for-native-scan/66-01-SUMMARY.md` when done
</output>
