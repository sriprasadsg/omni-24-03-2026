---
phase: 66-full-yara-rule-engine-for-native-scan
plan: 02
type: execute
wave: 2
depends_on:
  - 66-01
files_modified:
  - agent-rust/src/capabilities/mod.rs
  - agent-rust/src/capabilities/security_scan.rs
  - agent-rust/src/scanner/mod.rs
  - agent-rust/src/scanner/yara_engine.rs
  - agent-rust/src/main.rs # Clean up temporary code
autonomous: true
requirements:
  - NSCAN-01
user_setup: []

must_haves:
  truths:
    - A new `yara_engine` module exists that encapsulates YARA rule compilation and scanning.
    - `security_scan` capability uses the `yara_engine` to scan files with bundled YARA rules.
    - Temporary YARA test code is removed from `main.rs`.
    - The agent's `scan_file` instruction correctly triggers a YARA scan.
  artifacts:
    - `omni-agent-rs/src/scanner/yara_engine.rs` (new file)
    - `omni-agent-rs/src/scanner/mod.rs` (new file, if needed for module structure)
    - `omni-agent-rs/src/capabilities/security_scan.rs` modified to use the new engine.
  key_links:
    - Connection between `security_scan.rs` and the `yara_engine` module.
    - Loading of YARA rules from the existing feed bundle (Phase 50).
---

<objective>
Implement the dedicated YARA engine module, integrate it into the `security_scan` capability, and clean up temporary test code. This plan leverages the successful `yara` crate integration from Plan 66-01 to provide a production-ready YARA scanning capability.

Purpose: Provide a robust, encapsulated YARA scanning component for the agent's native scanner.
Output: A functional YARA rule engine integrated into the agent's file scanning logic.
</objective>

<execution_context>
@/home/user/enterprise-omni-agent-ai-platform/.claude/gsd-core/workflows/execute-plan.md
@/home/user/enterprise-omni-agent-ai-platform/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-full-yara-rule-engine-for-native-scan/RESEARCH.md
@.planning/milestones/v3.4-phases/50-native-security-scanning/50-CONTEXT.md
@.planning/phases/66-full-yara-rule-engine-for-native-scan/66-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Create `yara_engine` module for YARA rule management and scanning</name>
  <files>
    omni-agent-rs/src/scanner/mod.rs
    omni-agent-rs/src/scanner/yara_engine.rs
  </files>
  <action>
    Create a new module `omni-agent-rs/src/scanner/yara_engine.rs`.
    This module should contain:
    - A function to load and compile YARA rules (e.g., `Rules::new().add_string(...)`). This will eventually load from the bundled feed's `yara_rules` table. For now, use a hardcoded simple rule string.
    - A function to scan a byte slice with the compiled rules, returning matches (e.g., `Scanner::new(&rules).scan_mem(...)`).
    - Error handling for rule compilation and scanning.
    Update `omni-agent-rs/src/scanner/mod.rs` to expose the new `yara_engine` module.
  </action>
  <verify>
    <automated>
      cd omni-agent-rs && cargo test --test yara_engine_unit_tests # A temporary unit test file could be created or main.rs can call it
      # Or, ensure compilation passes after creating the module.
    </automated>
  </verify>
  <done>
    The `yara_engine` module is created, compiles, and provides functions for YARA rule compilation and scanning.
  </done>
</task>

<task type="auto">
  <name>Integrate `yara_engine` into `security_scan` capability and clean up `main.rs`</name>
  <files>
    omni-agent-rs/src/capabilities/security_scan.rs
    omni-agent-rs/src/main.rs
  </files>
  <action>
    Modify `omni-agent-rs/src/capabilities/security_scan.rs`:
    - Import the new `yara_engine` module.
    - In the `scan_file` function, use the `yara_engine` to compile a placeholder rule (or load from a dummy source) and scan the file content.
    - Combine YARA scan results with existing hash-signature scan results (if any from Phase 50) to derive a comprehensive verdict.
    Remove the temporary YARA test code added to `omni-agent-rs/src/main.rs` in Plan 66-01.
  </action>
  <verify>
    <automated>
      cd omni-agent-rs && cargo build
      # After building, a functional test could involve triggering the agent's scan_file capability (if accessible via test harness).
      # For now, successful compilation and unit tests (if added in previous task) are sufficient.
    </automated>
  </verify>
  <done>
    `security_scan.rs` utilizes the `yara_engine` for file scanning, and the temporary `main.rs` code is removed, restoring it to its original state (before 66-01's tracer).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| omni-agent-rs internal | Interaction between `security_scan` and new `yara_engine` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-66-01 | Tampering | YARA rule source | high | mitigate | Rely on existing feed bundle signature verification (D-03 from Phase 50) for rules loaded. |
| T-66-02 | Denial of Service | Malformed/complex YARA rules | medium | mitigate | YARA rule compilation errors handled gracefully; explore YARA scan timeouts in `yara` crate. |
| T-66-03 | Information Disclosure | YARA rules themselves | low | accept | Rules are from trusted, internal feed. |
| T-66-SC | Tampering | npm/pip/cargo installs | high | mitigate | slopcheck + blocking human checkpoint for `yara` crate (already flagged in RESEARCH.md as `[ASSUMED]`). |
</threat_model>

<verification>
Agent's native scanner now includes YARA rule evaluation.
</verification>

<success_criteria>
- `yara_engine` module is fully implemented and tested.
- `security_scan` capability uses `yara_engine` for file scanning.
- `main.rs` is clean of temporary YARA test code.
</success_criteria>

<output>
Create `.planning/phases/66-full-yara-rule-engine-for-native-scan/66-02-SUMMARY.md` when done
</output>
