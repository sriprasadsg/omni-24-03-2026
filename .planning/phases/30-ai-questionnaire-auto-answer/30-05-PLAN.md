---
phase: 30-ai-questionnaire-auto-answer
plan: 05
type: execute
wave: 3
depends_on: [30-01, 30-02, 30-03, 30-04]
files_modified:
  - backend/router_registry.py
  - backend/tests/test_questionnaire_auto_answer_e2e.py
  - backend/requirements-eval.txt
  - backend/tests/fixtures/questionnaire_eval_set.json
  - backend/tests/eval_questionnaire_auto_answer.py
autonomous: false
requirements: [RAG-01, RAG-02]
user_setup:
  - service: eval-tooling
    why: "Opt-in RAGAS/Arize-Phoenix eval + tracing (first eval tooling for this codebase); packages are [ASSUMED] in the legitimacy audit and require human confirmation before install"
    env_vars: []
must_haves:
  truths:
    - "The three Phase 30 routers (questionnaire_inbound_endpoints, questionnaire_answer_draft_endpoints, questionnaire_answer_review_endpoints) are registered in backend/router_registry.py so they are mounted on the real FastAPI app — not only inside the e2e test harness's hand-built TestClient"
    - "A TestClient end-to-end run drives create-question-set → generate-draft → review → approve → submit and asserts the draft ends 'submitted'"
    - "The same e2e test asserts a direct pending_review→submit call is rejected and leaves the draft unchanged"
    - "backend/requirements-eval.txt pins ragas==0.4.3, arize-phoenix==17.20.0, opentelemetry-sdk==1.43.0 and is separate from runtime requirements.txt"
    - "The eval harness and 15-example fixture exist as scaffolding without being a runtime import dependency"
  artifacts:
    - "backend/router_registry.py (three new _load(app, ...) registrations for the Phase 30 routers)"
    - "backend/tests/test_questionnaire_auto_answer_e2e.py"
    - "backend/requirements-eval.txt"
    - "backend/tests/fixtures/questionnaire_eval_set.json"
    - "backend/tests/eval_questionnaire_auto_answer.py"
  key_links:
    - "router_registry.register_all_routers -> _load(app, 'questionnaire_inbound_endpoints'|'questionnaire_answer_draft_endpoints'|'questionnaire_answer_review_endpoints') -> routes live on the deployed app that 30-06 apiService.ts calls (without this the frontend 404s in production)"
    - "e2e: POST /api/questionnaires/inbound -> POST /api/questionnaire-answer-drafts/generate -> POST review -> PATCH approve -> POST submit"
    - "eval harness instruments rag_service.query() + ai_service.generate_text() spans (opt-in)"
---

<objective>
Consolidate the router registration that 30-02/30-03/30-04 each deferred here (mount the three new questionnaire routers into the real app via `router_registry.py`), prove the full Phase 30 pipeline works end-to-end over real HTTP (the class of bug unit tests structurally miss — see the Phase 25 `response: Response` precedent), and stand up the platform's first AI eval/tracing scaffold (RAGAS + Arize Phoenix) as opt-in, non-runtime tooling per 30-AI-SPEC.md Section 5.

Purpose: Registering the inbound/draft/review routers in `router_registry.register_all_routers` is what actually makes RAG-01/RAG-02's endpoints reachable on the deployed FastAPI app (30-02/30-03/30-04 create the routers but explicitly defer their registration to this plan) — without it the 30-06 `apiService.ts` calls all 404 in production. A single TestClient test that exercises create→draft→review→approve→submit AND the review-bypass rejection gives RAG-01/RAG-02 a real integration guarantee; the eval scaffold gives `/gsd-eval-review`/Verify phase something concrete to label against without adding a hard dependency to the deployed service.
Output: Three router registrations in `router_registry.py`, an e2e test, `requirements-eval.txt`, a 15-example fixture skeleton, and an eval-harness scaffold — with the eval packages gated behind a blocking human legitimacy check.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-ai-questionnaire-auto-answer/30-AI-SPEC.md
@.planning/phases/30-ai-questionnaire-auto-answer/30-RESEARCH.md
@.planning/phases/30-ai-questionnaire-auto-answer/30-VALIDATION.md
@backend/tests/test_evidence_review.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register the three new Phase 30 routers into the real app (router_registry.py)</name>
  <files>backend/router_registry.py</files>
  <read_first>
    - backend/router_registry.py:135-164 (the "Compliance &amp; Governance" block; the existing `_load(app, "questionnaire_endpoints",   "router")` line — currently the last-listed compliance router — is the exact pattern/location to match)
    - 30-02-PLAN.md / 30-03-PLAN.md / 30-04-PLAN.md "Artifacts this phase produces" (each states its router registration is "consolidated in 30-05"; module names + route prefixes are listed there)
  </read_first>
  <action>
    In `backend/router_registry.py`, inside `register_all_routers(app)`'s "Compliance &amp; Governance" section, immediately after the existing `_load(app, "questionnaire_endpoints",   "router")` line, add three sibling registrations matching that exact pattern — a plain `_load(app, "&lt;module&gt;", "router")` call with NO `prefix`/`tags` kwargs, because each of these routers declares its own prefix internally (30-02's router uses `APIRouter(prefix="/api/questionnaires/inbound")`; 30-03's and 30-04's declare full `/api/questionnaire-answer-drafts/...` paths on `APIRouter()`):
    - `_load(app, "questionnaire_inbound_endpoints", "router")` — mounts 30-02's `/api/questionnaires/inbound` intake surface.
    - `_load(app, "questionnaire_answer_draft_endpoints", "router")` — mounts 30-04's `/api/questionnaire-answer-drafts` generate/list surface.
    - `_load(app, "questionnaire_answer_review_endpoints", "router")` — mounts 30-03's `/api/questionnaire-answer-drafts/{id}/review|submit` + `/pending-review` surface.
    Add them as ordinary (non-required) `_load` calls — do NOT add any of them to `_REQUIRED_ROUTERS`, matching the sibling `questionnaire_endpoints` line, so a transient import error logs at ERROR without aborting whole-app startup. This is the wiring that 30-02/30-03/30-04 each explicitly deferred to this plan; without it the three routers are never mounted on the real FastAPI app built by `app.py` (`register_all_routers(app)` at app.py:157), so every 30-06 `apiService.ts` call to these paths 404s in production even though the e2e test's hand-built TestClient passes green.
  </action>
  <acceptance_criteria>
    - `router_registry.py` contains, inside `register_all_routers`, a `_load(app, "questionnaire_inbound_endpoints", "router")`, a `_load(app, "questionnaire_answer_draft_endpoints", "router")`, and a `_load(app, "questionnaire_answer_review_endpoints", "router")` call, positioned alongside the existing `questionnaire_endpoints` registration.
    - None of the three is added to `_REQUIRED_ROUTERS`.
    - The guarantee is proven against the production registration path (`router_registry.register_all_routers` / `router_registry.py`) — the same function `app.py` calls at startup — NOT against the e2e test's hand-built `TestClient` that does its own `app.include_router(...)`.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && for m in questionnaire_inbound_endpoints questionnaire_answer_draft_endpoints questionnaire_answer_review_endpoints; do grep -q "_load(app, \"$m\"" router_registry.py || { echo "MISSING $m"; exit 1; }; done; echo OK</automated>
  </verify>
  <done>All three Phase 30 routers are wired into the real app via `router_registry.py`; RAG-01/RAG-02 endpoints are reachable on the deployed FastAPI app, not only inside the e2e test harness.</done>
</task>

<task type="auto">
  <name>Task 2: Full-path e2e test (create → draft → review → approve → submit + bypass rejection)</name>
  <files>backend/tests/test_questionnaire_auto_answer_e2e.py</files>
  <read_first>
    - backend/tests/test_evidence_review.py:86-108 (`_build_client` — build a fresh FastAPI, `app.include_router(mod.router)` for each endpoint module, override `get_current_user`, patch `get_database`; plus the autouse `_stop_patchers` fixture)
    - 30-04-PLAN.md + 30-03-PLAN.md "Artifacts this phase produces" (endpoint paths + shared draft-doc contract)
    - 30-VALIDATION.md Per-Task Verification Map row T-30-05
  </read_first>
  <action>
    Create `backend/tests/test_questionnaire_auto_answer_e2e.py`. Build a single `TestClient` app that includes all four new routers (`questionnaire_inbound_endpoints`, `questionnaire_answer_draft_endpoints`, `questionnaire_answer_review_endpoints`) with `get_current_user` overridden and `get_database` patched to a mock/fixture Mongo (clone the `_build_client`/`_make_user`/`_stop_patchers` helpers). Patch `rag_service.query` (returns one deterministic tenant-scoped evidence chunk) and `ai_service.generate_text` (AsyncMock returning a well-formed grounded-answer JSON with a cited index) so the draft is deterministic.
    Happy path: POST create an inbound question set with one question → POST `/api/questionnaire-answer-drafts/generate` for that set → assert a `pending_review` draft with non-empty `sourceEvidenceIds`; POST a review on the draft (as a reviewer user) → PATCH the review `decision:"approved"` → POST `/api/questionnaire-answer-drafts/{draft_id}/submit` → assert final draft status `submitted` and that `reviewer_id` equals the reviewer's username.
    Bypass path: on a fresh `pending_review` draft, POST `/submit` directly → assert a non-2xx (404/409) and that the draft is still `pending_review` (RAG-02/T3 proven over HTTP, not just at the service layer).
    Use `TestClient(app, raise_server_exceptions=False)` so a 500 surfaces as a response, not a swallowed exception.
  </action>
  <acceptance_criteria>
    - The happy-path assertions (draft pending_review → approved → submitted, server-derived reviewer_id) all pass.
    - The bypass assertion (direct pending_review→submit rejected, draft unchanged) passes.
    - The test uses real router inclusion over HTTP, not module-level function calls.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python -m pytest tests/test_questionnaire_auto_answer_e2e.py -x</automated>
  </verify>
  <done>RAG-01/RAG-02 verified end-to-end over HTTP, including the review-bypass rejection.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>A `requirements-eval.txt` manifest will pin three first-time eval-tooling packages the codebase has never installed: `ragas==0.4.3`, `arize-phoenix==17.20.0`, `opentelemetry-sdk==1.43.0`. 30-RESEARCH.md's Package Legitimacy Audit verified their PyPI version history but marked download-count/source-repo as [ASSUMED] (not independently confirmed).</what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/ragas/ , https://pypi.org/project/arize-phoenix/ , https://pypi.org/project/opentelemetry-sdk/ and confirm each is the well-known project (explodinggradients/ragas, Arize-ai/phoenix, open-telemetry/opentelemetry-python), actively maintained, with the pinned versions available.
    2. Confirm these are opt-in eval/tracing deps only — they are NOT added to runtime `backend/requirements.txt` and the deployed service does not import them.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with writing the manifest/fixture/harness scaffold, or name a package to remove/replace.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: requirements-eval.txt + 15-example fixture + eval-harness scaffold (opt-in, non-runtime)</name>
  <files>backend/requirements-eval.txt, backend/tests/fixtures/questionnaire_eval_set.json, backend/tests/eval_questionnaire_auto_answer.py</files>
  <read_first>
    - 30-AI-SPEC.md Section 5 (Eval Tooling, Setup, Reference Dataset composition, CI/CD integration)
    - 30-RESEARCH.md Standard Stack table (exact pinned versions) and Package Legitimacy Audit
    - 30-PATTERNS.md "No Analog Found" rows (fixture + requirements-eval.txt + harness are net-new)
  </read_first>
  <action>
    Create `backend/requirements-eval.txt` pinning exactly `ragas==0.4.3`, `arize-phoenix==17.20.0`, `opentelemetry-sdk==1.43.0`, with a header comment noting it is opt-in eval tooling kept separate from runtime `requirements.txt` and must NOT be installed into the deployed service. Do NOT run `pip install` as part of this task — installation is deferred to eval/Verify time and was human-confirmed in the preceding checkpoint.
    Create `backend/tests/fixtures/questionnaire_eval_set.json` as a 15-example skeleton matching AI-SPEC Section 5 composition: 5 critical-path, 3 optimistic-drift, 2 scope-mismatch, 2 zero-evidence, 1 stale-evidence, 2 cross-tenant adversarial. Each entry has `id`, `category`, `question_text`, `evidence` (list), `expected_confidence`, and `gold_answer` (leave `gold_answer` as an empty string with a `"_label_status":"unlabeled"` marker — labeling happens during Verify per AI-SPEC line 450). Valid JSON, exactly 15 entries.
    Create `backend/tests/eval_questionnaire_auto_answer.py` as the harness scaffold: accept a `--dataset` arg, wrap the `rag_service.query()`/`ai_service.generate_text()` call sites in OpenTelemetry spans (Phoenix tracer) and compute RAGAS `faithfulness`/`answer_relevancy`/`context_precision` over the dataset. Guard the `ragas`/`phoenix`/`opentelemetry` imports in a `try/except ImportError` that prints an actionable "install backend/requirements-eval.txt" message and exits cleanly — so the file's mere presence never breaks `pytest` collection when eval deps are absent. This script is NOT part of the blocking pytest suite.
  </action>
  <acceptance_criteria>
    - `requirements-eval.txt` pins the three exact versions and is separate from `requirements.txt`.
    - `questionnaire_eval_set.json` is valid JSON with exactly 15 entries spanning the six documented categories.
    - `eval_questionnaire_auto_answer.py` parses and imports cleanly (guarded eval-dep imports) without the eval packages installed.
    - No `pip install` is executed by this task.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python -c "import json,ast; d=json.load(open('tests/fixtures/questionnaire_eval_set.json')); assert len(d)==15, len(d); ast.parse(open('tests/eval_questionnaire_auto_answer.py').read()); assert 'ragas==0.4.3' in open('requirements-eval.txt').read(); print('OK')"</automated>
  </verify>
  <done>Eval scaffolding exists and is opt-in; the deployed service gains no new runtime dependency.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package registry → dev/CI environment | First-time eval-tooling install crosses a supply-chain boundary |
| test harness → services | e2e drives real router logic with mocked LLM/RAG |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-SC | Tampering (supply chain) | ragas / arize-phoenix / opentelemetry-sdk install | high | mitigate | Blocking-human legitimacy checkpoint verifying each package on pypi.org before the manifest is written/used ([ASSUMED] verdicts, first eval-tooling install); kept out of runtime requirements.txt; not auto-approvable |
| T3 (e2e) | Tampering / Elevation of Privilege | submit transition over HTTP | critical | mitigate | e2e bypass assertion proves a direct pending_review→submit call is rejected at the API boundary |
</threat_model>

## Artifacts this phase produces (Plan 30-05)

- **Modified wiring** `backend/router_registry.py`: three new `_load(app, ...)` registrations (`questionnaire_inbound_endpoints`, `questionnaire_answer_draft_endpoints`, `questionnaire_answer_review_endpoints`) added beside the existing `questionnaire_endpoints` line — the registration deferred here by 30-02/30-03/30-04, mounting RAG-01/RAG-02's endpoints on the real app.
- **New test** `backend/tests/test_questionnaire_auto_answer_e2e.py` (TestClient full-path + bypass).
- **New config** `backend/requirements-eval.txt` (`ragas==0.4.3`, `arize-phoenix==17.20.0`, `opentelemetry-sdk==1.43.0`; opt-in, non-runtime).
- **New fixture** `backend/tests/fixtures/questionnaire_eval_set.json` (15 examples, `gold_answer` unlabeled pending Verify).
- **New eval harness** `backend/tests/eval_questionnaire_auto_answer.py` (RAGAS + Phoenix spans; guarded imports; not in the blocking suite).

<verification>
- The three `_load(app, "questionnaire_inbound_endpoints"|"questionnaire_answer_draft_endpoints"|"questionnaire_answer_review_endpoints", "router")` registrations are present in `backend/router_registry.py` (production wiring, not the e2e harness).
- `cd backend && python -m pytest tests/test_questionnaire_auto_answer_e2e.py -x` passes.
- Fixture/harness/manifest structural check passes.
- `cd backend && python -m pytest tests/ -q` green (eval harness does not break collection).
</verification>

<success_criteria>
- End-to-end RAG-01/RAG-02 proven over HTTP; eval scaffold in place as opt-in tooling with a supply-chain-gated install.
</success_criteria>

<output>
Create `.planning/phases/30-ai-questionnaire-auto-answer/30-05-SUMMARY.md` when done.
</output>
