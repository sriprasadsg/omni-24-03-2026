---
phase: 41-cspm-provider-expansion-oci-alibaba-cloudflare
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/requirements.txt
autonomous: false
requirements: [CSPM-02]
user_setup:
  - service: alibaba-cloud-python-sdk
    why: "Real Alibaba Cloud Config/Security Center V2 posture checks (CSPM-02) require the V2 typed SDK packages, which the package-legitimacy gate flagged SUS pending human verification"
    dashboard_config:
      - task: "Confirm each alibabacloud_* package resolves to the official aliyun GitHub org before install is trusted"
        location: "https://pypi.org/project/<pkg>/ and https://github.com/aliyun/alibabacloud-python-sdk"

must_haves:
  truths:
    - "The 4 SUS-flagged alibabacloud_* V2 SDK packages are human-verified as legitimate before install (CSPM-02)"
    - "oci, cloudflare, and the 4 alibabacloud_* packages are installed in backend/venv and importable"
  artifacts:
    - path: "backend/requirements.txt"
      provides: "Pinned alibabacloud_* V2 SDK dependencies added alongside existing oci/cloudflare pins"
      contains: "alibabacloud_config20200907"
  key_links:
    - from: "backend/requirements.txt"
      to: "backend/venv"
      via: "pip install resolves the V2 Alibaba SDK family and existing oci/cloudflare pins"
      pattern: "alibabacloud_sas20181203"
---

<objective>
Gate and perform installation of the cloud SDK packages required for real (non-simulated) posture ingest: `oci` and `cloudflare` (already pinned but NOT installed in `backend/venv`), and the four NEW Alibaba V2 typed SDK packages that the package-legitimacy audit flagged SUS.

Purpose: CSPM-02 requires the Alibaba Config/Security Center V2 API, which needs `alibabacloud_config20200907`, `alibabacloud_sas20181203`, `alibabacloud_tea_openapi`, `alibabacloud_credentials`. All four came back `[ASSUMED]`/SUS from the package-legitimacy gate (too-new / unknown-downloads signals) though they resolve to Alibaba's official `github.com/aliyun/...` org. Per protocol they are NOT auto-installed — a blocking human verification confirms legitimacy first.

Output: an updated `requirements.txt` and a `backend/venv` with all seven packages installed and importable.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/41-cspm-provider-expansion-oci-alibaba-cloudflare/41-RESEARCH.md
</context>

## Artifacts this phase produces (this plan)

- `backend/requirements.txt`: 4 new pins — `alibabacloud_config20200907`, `alibabacloud_sas20181203`, `alibabacloud_tea_openapi`, `alibabacloud_credentials`
- `backend/venv`: `oci`, `cloudflare`, and the 4 alibabacloud_* packages installed and importable

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Human-verify the 4 SUS Alibaba V2 SDK packages before install</name>
  <action>Blocking human verification gate. Do not install any alibabacloud_* package until a human confirms each resolves to Alibaba's official aliyun org. This gate is never auto-approvable, regardless of auto_advance.</action>
  <what-built>Nothing installed yet. The 4 new Alibaba V2 SDK packages were flagged SUS by the package-legitimacy gate (`alibabacloud_config20200907==4.2.3`, `alibabacloud_sas20181203==9.3.3`, `alibabacloud_tea_openapi==0.4.5`, `alibabacloud_credentials==1.0.10`). This checkpoint requires a human to confirm legitimacy before install. This gate is NOT auto-approvable.</what-built>
  <how-to-verify>
    1. Open each package page and confirm the project is published under Alibaba's official org (source repo `github.com/aliyun/...`):
       - https://pypi.org/project/alibabacloud_config20200907/
       - https://pypi.org/project/alibabacloud_sas20181203/
       - https://pypi.org/project/alibabacloud_tea_openapi/
       - https://pypi.org/project/alibabacloud_credentials/
    2. Cross-check the package index at https://github.com/aliyun/alibabacloud-python-sdk (config/sas service packages) and https://github.com/aliyun/credentials-python.
    3. Confirm the naming matches Alibaba's documented `alibabacloud_<service><api-version>` V2 SDK convention (dozens of sibling packages already on PyPI).
    4. Optionally re-run `pip index versions <pkg>` to confirm the current resolvable version.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with install, or describe any package that does NOT resolve to the aliyun org (which would block CSPM-02).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Pin and install cloud SDK packages into backend/venv</name>
  <files>backend/requirements.txt</files>
  <read_first>
    - backend/requirements.txt (lines around 122-124: existing `oci>=2.181.1`, `aliyun-python-sdk-core-v3>=2.13.33`, `cloudflare>=5.4.0` pins — add the new pins near these)
    - .planning/phases/41-cspm-provider-expansion-oci-alibaba-cloudflare/41-RESEARCH.md (Standard Stack → Installation block, and Assumptions Log A1: re-verify pins at install time rather than trusting them blindly)
  </read_first>
  <action>
    Only proceed if Task 1 was approved. Add four pins to `requirements.txt` next to the existing cloud SDK pins: `alibabacloud_config20200907>=4.2.3`, `alibabacloud_sas20181203>=9.3.3`, `alibabacloud_tea_openapi>=0.4.5`, `alibabacloud_credentials>=1.0.10`. Keep the existing `aliyun-python-sdk-core-v3` pin (it backs the untouched SIEM path — do NOT remove it). Then install into the project venv: `backend/venv/bin/pip install oci>=2.181.1 cloudflare>=5.4.0 alibabacloud_config20200907>=4.2.3 alibabacloud_sas20181203>=9.3.3 alibabacloud_tea_openapi>=0.4.5 alibabacloud_credentials>=1.0.10`. If a pinned version no longer resolves on PyPI (per A1), let pip resolve the nearest compatible version and update the pin in `requirements.txt` to the actually-installed version.
  </action>
  <verify>
    <automated>cd backend && venv/bin/python -c "import oci, cloudflare; import alibabacloud_config20200907, alibabacloud_sas20181203, alibabacloud_tea_openapi, alibabacloud_credentials; print('all imports OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `requirements.txt` contains pins for all four `alibabacloud_*` packages, and still contains the `aliyun-python-sdk-core-v3` pin.
    - `backend/venv/bin/python -c "import oci, cloudflare, alibabacloud_config20200907, alibabacloud_sas20181203, alibabacloud_tea_openapi, alibabacloud_credentials"` exits 0.
    - Any version pin that had to change from the researched value is updated in `requirements.txt` to the installed version.
  </acceptance_criteria>
  <done>All seven cloud SDK packages install cleanly and import in backend/venv; requirements.txt reflects the actual resolved versions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI → build/runtime | Third-party package install introduces supply-chain trust |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-SC | Tampering | npm/pip installs (4 SUS alibabacloud_* packages) | mitigate | Blocking human checkpoint (Task 1) verifies each package resolves to the official aliyun org before install; never auto-approved |
| T-41-03 | Tampering | version pins drifting from verified versions | mitigate | Re-verify at install time (A1); update requirements.txt to the actually-installed version |
</threat_model>

<verification>
- `cd backend && venv/bin/python -c "import oci, cloudflare, alibabacloud_config20200907, alibabacloud_sas20181203, alibabacloud_tea_openapi, alibabacloud_credentials"` exits 0.
- `grep -c alibabacloud backend/requirements.txt` returns at least 4.
</verification>

<success_criteria>
- Human confirmed the four SUS Alibaba V2 packages are legitimate before install.
- All seven cloud SDK packages installed and importable; requirements.txt updated.
</success_criteria>

<output>
Create `.planning/phases/41-cspm-provider-expansion-oci-alibaba-cloudflare/41-02-SUMMARY.md` when done.
</output>
