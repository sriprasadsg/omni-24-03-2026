---
phase: 52-file-integrity-monitoring
plan: 04
type: execute
wave: 3
depends_on: ["52-01", "52-02", "52-03"]
files_modified:
  - agent-install/omni-agent-rs/src/config.rs
  - agent-install/omni-agent-rs/src/lib.rs
autonomous: true
requirements: [FIM-01, FIM-02]
must_haves:
  truths:
    - "config.rs has a fim_paths field with sane defaults (system bins, /etc, key stores; Windows system dirs) and optional override"
    - "agent_loop starts the FIM watcher + runs the restart drift check at startup, with shutdown tied to the existing stop signal"
    - "A background drain posts unposted fim_queue rows to POST /fim-events and marks them posted"
    - "The agent builds linux + windows-gnu; a FIM start failure never stops the heartbeat loop"
  artifacts:
    - path: "agent-install/omni-agent-rs/src/config.rs"
      provides: "fim_paths config field + defaults"
      contains: "fim_paths"
    - path: "agent-install/omni-agent-rs/src/lib.rs"
      provides: "agent_loop starts watcher + drift check + queue drain (graceful)"
      contains: "fim"
  key_links:
    - from: "agent-install/omni-agent-rs/src/lib.rs"
      to: "agent-install/omni-agent-rs/src/capabilities/fim.rs"
      via: "start_watcher(fim_paths, stop) + drain fim_queue -> POST /fim-events"
      pattern: "start_watcher"
    - from: "agent-install/omni-agent-rs/src/lib.rs"
      to: "agent-install/omni-agent-rs/src/capabilities/fim_baseline.rs"
      via: "check_drift_on_start(fim_paths) at startup"
      pattern: "check_drift_on_start"
  prohibitions:
    - "MUST NOT let a FIM start/drain failure stop the heartbeat loop (spawn independently, graceful)"
    - "MUST NOT block agent_loop on the watcher (background task)"
---

<objective>
Wire FIM into the running agent: a `fim_paths` config with defaults, the watcher + restart drift-check started in `agent_loop` (graceful, shutdown-tied), and a background drain that posts queued events to the backend `fim-events` endpoint. Closes FIM end-to-end.

Purpose: FIM-01/02 runtime integration. D-01/D-02/D-05.
Output: config + agent_loop wiring + queue drain.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@.planning/phases/52-file-integrity-monitoring/52-RESEARCH.md
@.planning/phases/52-file-integrity-monitoring/52-CONTEXT.md

# Read-first anchors:
@agent-install/omni-agent-rs/src/lib.rs        # agent_loop + stop signal (where to spawn the watcher/drain)
@agent-install/omni-agent-rs/src/config.rs     # config struct to add fim_paths
@agent-install/omni-agent-rs/src/capabilities/fim.rs          # start_watcher + fim_queue
@agent-install/omni-agent-rs/src/capabilities/fim_baseline.rs # check_drift_on_start
@agent-install/omni-agent-rs/src/instructions.rs             # existing POST-to-backend pattern (bearer auth)
</execution_context>

<artifacts>
- EDIT `agent-install/omni-agent-rs/src/config.rs` — `fim_paths` field + defaults.
- EDIT `agent-install/omni-agent-rs/src/lib.rs` — start watcher + drift check + queue drain in agent_loop.
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: fim_paths config + defaults</name>
  <files>agent-install/omni-agent-rs/src/config.rs</files>
  <action>Add `fim_paths: Vec<String>` to the config struct (serde default) with sensible cross-platform defaults when unset — Linux: `/etc`, `/usr/bin`, `/usr/sbin`, `~/.ssh`; Windows: `C:\Windows\System32\drivers\etc`, key stores; keep the set small/bounded. Allow an operator/backend override via the config file. Ensure load/save round-trips the field.</action>
  <verify>
    <automated>cd agent-install/omni-agent-rs && cargo build 2>&1 | tail -5</automated>
  </verify>
  <done>fim_paths present with bounded defaults; config round-trips.</done>
</task>

<task type="auto">
  <name>Task 2: start watcher + drift check + queue drain in agent_loop</name>
  <files>agent-install/omni-agent-rs/src/lib.rs</files>
  <read_first>agent_loop + the stop signal; instructions.rs for the bearer-auth POST pattern.</read_first>
  <action>At the start of `agent_loop`: run `capabilities::fim_baseline::check_drift_on_start(cfg.fim_paths)` (best-effort), then `tokio::spawn` (or a background thread) the FIM watcher `capabilities::fim::start_watcher(cfg.fim_paths, stop_handle)` tied to the existing `stop_rx`. Add a periodic background drain task: read unposted `fim_queue` rows, POST them as `{changes: [...]}` to `{api_base}/api/agents/{host}/security/fim-events` with bearer auth (mirror the instructions.rs POST), mark rows `posted=1` on success. ALL of this must be spawned independently and wrapped so a failure logs and never stops the heartbeat loop; never block agent_loop on the watcher. Trigger a rebuild (new mtime).</action>
  <verify>
    <automated>cd agent-install/omni-agent-rs && cargo build 2>&1 | tail -6 && cargo check --target x86_64-pc-windows-gnu 2>&1 | tail -6 && cargo test 2>&1 | tail -6</automated>
  </verify>
  <done>Watcher + drift check + drain start in agent_loop (graceful, shutdown-tied); builds linux + windows-gnu; tests green.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-52-08 | DoS | FIM failure stalls the agent | mitigate | Watcher/drain spawned independently, wrapped; heartbeat loop never blocks/fails on FIM |
| T-52-09 | Info Disclosure | queued events posted cross-tenant | mitigate | Bearer-auth POST to the agent's own tenant-scoped endpoint (52-01) |
| T-52-10 | Tampering | windows cross-build regression | mitigate | windows-gnu check in Task 2 |
</threat_model>

<verification>
- `cargo build` (linux) + `cargo check --target x86_64-pc-windows-gnu` clean; `cargo test` green.
- Watcher + drift check + drain start in agent_loop, shutdown-tied, non-blocking; queued events POST to fim-events.

<human-check>Run the agent, modify a watched file (e.g. touch /etc test file), and confirm a FIM change event reaches the backend fim-events (rich fields) and the local fim_queue; restart the agent after an offline change and confirm a drift event is emitted.</human-check>
</verification>

<success_criteria>
FIM runs end-to-end: bounded watched paths from config, an event-driven watcher + restart drift check started in agent_loop (graceful, shutdown-tied), and a background drain posting queued rich events to the backend — without ever blocking or crashing the heartbeat loop.
</success_criteria>

<output>
Create `.planning/phases/52-file-integrity-monitoring/52-04-SUMMARY.md` when done.
</output>
