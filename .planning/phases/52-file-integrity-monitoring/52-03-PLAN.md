---
phase: 52-file-integrity-monitoring
plan: 03
type: execute
wave: 2
depends_on: ["52-02"]
files_modified:
  - agent-install/omni-agent-rs/src/capabilities/fim_baseline.rs
  - agent-install/omni-agent-rs/src/capabilities/mod.rs
autonomous: true
requirements: [FIM-03]
must_haves:
  truths:
    - "At first run the agent computes a baseline snapshot (watched path -> sha256), signs it with an agent-local ed25519 key, and persists it"
    - "On restart the agent verifies the baseline signature before trusting it; an invalid/missing baseline is recomputed and flagged as a reset (fail-closed)"
    - "Drift (added/removed/changed paths vs baseline) is detected on restart and emitted as events into fim_queue"
    - "The baseline private key is agent-generated, persisted locally, and never shipped"
  artifacts:
    - path: "agent-install/omni-agent-rs/src/capabilities/fim_baseline.rs"
      provides: "signed baseline snapshot + restart drift detection"
      contains: "ed25519"
      min_lines: 60
  key_links:
    - from: "agent-install/omni-agent-rs/src/capabilities/fim_baseline.rs"
      to: "fim_queue (local sqlite)"
      via: "drift events enqueued for the remediation engine"
      pattern: "fim_queue"
  prohibitions:
    - "MUST NOT trust an unsigned or signature-failed baseline — recompute + flag reset (fail-closed)"
    - "MUST NOT ship the baseline private key in any installer (agent-generated, local-only)"
    - "MUST NOT panic on a missing/corrupt baseline — recompute gracefully"
---

<objective>
Add signed baseline snapshots + restart drift detection (FIM-03): compute a baseline of watched paths, sign it with an agent-local ed25519 key, and on restart verify + diff to emit drift events into the same `fim_queue`.

Purpose: FIM-03. D-03. Reuses `ed25519-dalek` (Phase 50) + the 52-02 queue.
Output: `capabilities/fim_baseline.rs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@.planning/phases/52-file-integrity-monitoring/52-RESEARCH.md
@.planning/phases/52-file-integrity-monitoring/52-CONTEXT.md

# Read-first anchors:
@agent-install/omni-agent-rs/src/capabilities/fim.rs           # hash_file + fim_queue enqueue to reuse
@agent-install/omni-agent-rs/src/capabilities/feed_bundle.rs   # ed25519 verify pattern (VerifyingKey) to mirror
</execution_context>

<artifacts>
- NEW `agent-install/omni-agent-rs/src/capabilities/fim_baseline.rs` — signed baseline + drift.
- EDIT `agent-install/omni-agent-rs/src/capabilities/mod.rs` — `pub mod fim_baseline;`.
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: agent-local baseline keypair + signed snapshot</name>
  <files>agent-install/omni-agent-rs/src/capabilities/fim_baseline.rs, agent-install/omni-agent-rs/src/capabilities/mod.rs</files>
  <read_first>feed_bundle.rs for the ed25519 verify usage; fim.rs hash_file.</read_first>
  <action>Create `fim_baseline.rs`: `fn local_keypair()` loads or generates+persists an `ed25519_dalek::SigningKey` at a local path (0600, agent data dir); the private key is never transmitted. `pub fn compute_baseline(paths) -> Baseline` walks the watched paths → `entries: [{path, sha256}]` + `{version, created_at}`. `pub fn save_signed(baseline)` serializes the baseline and writes it + a detached ed25519 signature (over the serialized bytes) to local files. Register `pub mod fim_baseline;`.</action>
  <verify>
    <automated>cd agent-install/omni-agent-rs && cargo build 2>&1 | tail -6</automated>
  </verify>
  <done>Baseline computes, signs, persists; keypair local-only.</done>
</task>

<task type="auto">
  <name>Task 2: restart verify + drift detection → queue</name>
  <files>agent-install/omni-agent-rs/src/capabilities/fim_baseline.rs</files>
  <action>`pub fn load_verified() -> Option<Baseline>` reads the baseline + signature and verifies with the local public key; on failure returns None. `pub fn check_drift_on_start(paths)`: if `load_verified()` is None → recompute + `save_signed` + enqueue a `baseline_reset` event (fail-closed). Else diff current path hashes vs baseline entries → for each added/removed/changed path, enqueue a drift event into `fim_queue` (reuse fim's enqueue with `change_type: "drift"` + hash_before from baseline + hash_after current). After reconciliation, re-save the signed baseline. Never panic on missing/corrupt files.</action>
  <verify>
    <automated>cd agent-install/omni-agent-rs && cargo build 2>&1 | tail -5</automated>
  </verify>
  <done>Verify + drift diff + queue emit + fail-closed reset implemented.</done>
</task>

<task type="auto">
  <name>Task 3: unit tests — sign/verify roundtrip, tamper-fail, drift diff</name>
  <files>agent-install/omni-agent-rs/src/capabilities/fim_baseline.rs</files>
  <action>Add `#[cfg(test)] mod tests`: sign→verify roundtrip succeeds; a tampered baseline byte fails verification (→ None → reset path); a drift diff (add/remove/change) against a known baseline yields the expected drift events; missing baseline files → recompute (no panic). Use temp dirs / in-memory where possible.</action>
  <verify>
    <automated>cd agent-install/omni-agent-rs && cargo test fim_baseline 2>&1 | tail -12</automated>
  </verify>
  <done>Sign/verify, tamper-fail, drift-diff, missing-file unit tests pass.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-52-05 | Tampering | attacker edits baseline to hide drift | mitigate | ed25519 signature over the baseline; verify-fail → reset + flag, never silently trust |
| T-52-06 | Info Disclosure | baseline signing key leak | mitigate | Agent-local, 0600, never shipped/transmitted |
| T-52-07 | DoS | corrupt baseline crashes startup | mitigate | Fail-closed recompute; no panic |
</threat_model>

<verification>
- `cargo build` clean; `cargo test fim_baseline` green.
- Sign/verify roundtrip; tamper → reset; drift emits queue events; fail-closed on missing/corrupt.
</verification>

<success_criteria>
The agent maintains an ed25519-signed baseline snapshot, verifies it on restart (fail-closed recompute on tamper), and detects + enqueues drift events — with the signing key agent-local and never shipped.
</success_criteria>

<output>
Create `.planning/phases/52-file-integrity-monitoring/52-03-SUMMARY.md` when done.
</output>
