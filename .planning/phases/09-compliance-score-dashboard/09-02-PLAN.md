---
phase: 09-compliance-score-dashboard
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - components/ComplianceScorePanel.tsx
  - components/Dashboard.tsx
  - services/apiService.ts
  - types.ts
autonomous: true
requirements:
  - SCORE-01
  - SCORE-02
  - SCORE-03

must_haves:
  truths:
    - "ComplianceScorePanel renders on the main Dashboard above ComplianceStatus"
    - "Overall score number is displayed with correct color (green ≥80, amber 50-79, red <50)"
    - "Severity weight legend is accessible via Info icon tooltip (keyboard + mouse)"
    - "Each framework appears as a collapsible row with progress bar and score percentage"
    - "Clicking a framework row expands a drill-down showing passing, failing, partial counts"
    - "Loading skeleton replaces score block and framework rows while fetch is in-flight"
    - "Error state renders when fetchComplianceScore returns null"
    - "Empty state renders when frameworks are empty and overall_score is 0"
    - "No text-[Npx] arbitrary sizes used; text-xs is minimum (WCAG AA, UI-01)"
    - "No explicit Content-Type set on fetch call (T-02-07 decision)"
  artifacts:
    - path: "components/ComplianceScorePanel.tsx"
      provides: "Self-contained compliance score panel with tooltip, accordion, loading/error/empty states"
      max_lines: 250
    - path: "components/Dashboard.tsx"
      provides: "ComplianceScorePanel mounted above ComplianceStatus"
    - path: "services/apiService.ts"
      provides: "fetchComplianceScore() function"
    - path: "types.ts"
      provides: "FrameworkScore and ComplianceScorePayload interfaces"
  key_links:
    - from: "components/ComplianceScorePanel.tsx"
      to: "services/apiService.ts"
      via: "fetchComplianceScore() called in useEffect on mount"
    - from: "services/apiService.ts"
      to: "backend GET /api/compliance/score"
      via: "authFetch(`${API_BASE}/compliance/score`)"
    - from: "components/Dashboard.tsx"
      to: "components/ComplianceScorePanel.tsx"
      via: "import + render above ComplianceStatus in mt-8 space-y-6 wrapper"
---

<objective>
Deliver the frontend for Phase 9: ComplianceScorePanel.tsx component with overall score gauge,
per-framework accordion drill-down, severity weight tooltip, and all three display states
(loading skeleton / error / empty). Mount it in Dashboard.tsx above the existing ComplianceStatus.

Purpose: SCORE-01 requires the score visible on the main dashboard; SCORE-02 requires the
severity weight legend tooltip; SCORE-03 requires the per-framework breakdown that can be
expanded. The frontend fetches from the Wave-1 backend endpoint and renders the live DB-backed
score — not the static framework.progress field used by ComplianceStatus.tsx.

Output:
- components/ComplianceScorePanel.tsx (new, ≤250 lines)
- components/Dashboard.tsx (3 lines added: import + wrapper class + panel mount)
- services/apiService.ts (fetchComplianceScore function + ComplianceScorePayload type export)
- types.ts (FrameworkScore + ComplianceScorePayload interfaces)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-compliance-score-dashboard/09-UI-SPEC.md
@.planning/phases/09-compliance-score-dashboard/09-RESEARCH.md
@components/Dashboard.tsx
@components/ComplianceStatus.tsx
@services/apiService.ts
@types.ts
@.planning/phases/09-compliance-score-dashboard/09-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add FrameworkScore + ComplianceScorePayload to types.ts and fetchComplianceScore to apiService.ts</name>
  <files>types.ts, services/apiService.ts</files>
  <action>
    In types.ts, append the following two interfaces after the last interface definition (do not modify any existing interface):

    FrameworkScore:
    - framework_id: string
    - framework_name: string
    - short_name: string
    - score: number (0.0-100.0, one decimal)
    - passing: number
    - failing: number
    - partial: number
    - total_controls: number

    ComplianceScorePayload:
    - overall_score: number (0.0-100.0)
    - frameworks: FrameworkScore[]
    - computed_at: string (ISO 8601 UTC)
    - tenant_id: string

    In services/apiService.ts, add the import for ComplianceScorePayload to the existing import block from '../types' (add ComplianceScorePayload to the named imports list). Also add it to the re-export block (export type { ... }).

    Then append fetchComplianceScore near other compliance fetch functions (search for "fetchCompliance" to find the right location). The function signature is:
    export const fetchComplianceScore = async (): Promise<ComplianceScorePayload | null>

    Implementation pattern (same try/catch shape as existing compliance functions in apiService.ts):
    - try block: const res = await authFetch(`${API_BASE}/compliance/score`); if (!res.ok) { console.error with HTTP status; return null; } return await res.json() as ComplianceScorePayload;
    - catch block: console.error with message; return null;

    Do NOT set Content-Type header on this fetch (T-02-07 decision — this is a GET, no body, browser sets correct headers automatically).

    Verify authFetch is in scope (it is — already used by all other compliance functions in the same file). Do not re-import it.
  </action>
  <verify>
    <automated>cd /home/user/enterprise-omni-agent-ai-platform && grep -c "fetchComplianceScore" services/apiService.ts && grep -c "FrameworkScore" types.ts && npx tsc --noEmit 2>&1 | grep -E "FrameworkScore|ComplianceScorePayload|fetchComplianceScore" | head -5; echo "Exit: $?"</automated>
  </verify>
  <done>
    - grep -c "fetchComplianceScore" services/apiService.ts returns ≥ 1
    - grep -c "FrameworkScore" types.ts returns ≥ 1
    - types.ts exports FrameworkScore and ComplianceScorePayload interfaces
    - apiService.ts exports fetchComplianceScore returning Promise&lt;ComplianceScorePayload | null&gt;
    - TypeScript compilation reports no errors for these new symbols
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ComplianceScorePanel.tsx and mount in Dashboard.tsx</name>
  <files>components/ComplianceScorePanel.tsx, components/Dashboard.tsx</files>
  <action>
    Create components/ComplianceScorePanel.tsx. Keep it under 250 lines. Follow the UI-SPEC.md layout exactly.

    Imports:
    - React, { useState, useEffect } from 'react'
    - { fetchComplianceScore } from '../services/apiService'
    - { ComplianceScorePayload, FrameworkScore } from '../types'
    - Lucide icons: ShieldCheckIcon, Info, ChevronDown, CheckCircle2, XCircle, Circle, AlertTriangle, Shield — import from './icons' if that barrel exists (it does: ComplianceStatus.tsx imports ShieldCheckIcon from './icons'); for icons not in the barrel, import directly from 'lucide-react'

    State variables:
    - data: ComplianceScorePayload | null = null
    - loading: boolean = true
    - error: boolean = false
    - expandedFramework: string | null = null
    - tooltipVisible: boolean = false

    Data fetch in useEffect (mount only, no deps array needed beyond []): use the cancellation pattern from UI-SPEC.md section 3 (let cancelled = false; fetchComplianceScore().then(...); return () => { cancelled = true; }).

    Inline time helper (no library): relativeTime(iso: string): string — if within 60s return "just now"; if within 3600s return "${Math.floor(diff/60)} min ago"; else return new Date(iso).toLocaleString(). Compute diff = (Date.now() - new Date(iso).getTime()) / 1000.

    Color helper: scoreColor(score: number): string — returns Tailwind class string: score >= 80 → "text-green-600 dark:text-green-400"; 50 <= score < 80 → "text-amber-600 dark:text-amber-400"; else → "text-red-600 dark:text-red-400".

    Bar color helper: barColor(score: number): string — returns Tailwind class: score >= 80 → "bg-green-500"; 50 <= score < 80 → "bg-amber-500"; else → "bg-red-500".

    SeverityWeightTooltip sub-component (inline in same file, above main component):
    - Props: visible: boolean, onMouseEnter: () => void, onMouseLeave: () => void, onFocus: () => void, onBlur: () => void
    - Renders the Info icon trigger with aria-label="Severity weight explanation", role="button", tabIndex={0}
    - Tooltip div: absolute z-10 w-64 p-3 bg-gray-900 dark:bg-gray-700 text-white rounded-lg shadow-xl text-sm; position bottom-full left-1/2 -translate-x-1/2 mb-2; visible only when visible=true
    - Tooltip content: title "Severity-Weighted Score" (text-xs font-semibold mb-2); four severity rows with colored weight badge (Critical bg-red-600, High bg-orange-500, Medium bg-yellow-500 text-gray-900, Low bg-blue-500); formula line "Score = sum of passing weights / sum of all weights x 100." (text-xs text-gray-400 mt-2 border-t border-gray-600 pt-2); exclusion note "Controls with no monitored asset evidence are excluded." (text-xs text-gray-400 mt-1)
    - Wrapper: relative inline-flex items-center
    - The Info icon itself: w-4 h-4 ml-2 text-gray-400 hover:text-gray-600 dark:hover:text-gray-300

    Main panel structure (per UI-SPEC.md section 3 Layout):
    Panel card: bg-white dark:bg-gray-800 rounded-lg border border-gray-200 dark:border-gray-700 (matches ComplianceStatus card surface)

    Panel header (p-4 border-b border-gray-200 dark:border-gray-700):
    - flex items-center
    - ShieldCheckIcon w-5 h-5 text-indigo-500 mr-2 aria-hidden="true"
    - span "Compliance Score" text-lg font-semibold
    - SeverityWeightTooltip (controlled by tooltipVisible state + handlers)

    When loading === true: render panel header normally; replace body with:
    - aria-busy="true" on panel root
    - Score summary skeleton: animate-pulse bg-gray-200 dark:bg-gray-700 rounded-lg h-20 w-full mb-4
    - 3 skeleton rows: animate-pulse h-10 bg-gray-100 dark:bg-gray-700/50 rounded mb-2
    - sr-only span "Loading compliance score"

    When error === true: render error state inside role="alert" container (p-4 rounded-lg bg-amber-50 dark:bg-amber-900/20):
    - AlertTriangle w-5 h-5 text-amber-500 mr-2 aria-hidden="true" + heading "Score unavailable" (text-sm font-semibold text-amber-700 dark:text-amber-400)
    - Body "The compliance score could not be loaded. Refresh the page to try again." (text-sm text-gray-600 dark:text-gray-400 mt-1)

    When data is not null and data.frameworks.length === 0 and data.overall_score === 0: render empty state (flex flex-col items-center justify-center py-8):
    - Shield w-8 h-8 text-gray-300 dark:text-gray-600 aria-hidden="true"
    - Heading "No monitored controls yet" (text-sm font-medium text-gray-500 dark:text-gray-400 mt-2)
    - Body "Upload evidence or configure asset compliance to see your score here." (text-xs text-gray-400 dark:text-gray-500 mt-1)

    When data is not null (normal state):
    Score Summary Block (p-4 bg-gray-50 dark:bg-gray-700/50 rounded-lg mb-4):
    - flex items-center justify-between
    - Left column: overall score number (text-3xl font-bold + scoreColor(data.overall_score)); below it "Severity-weighted score" (text-xs font-medium text-gray-500 dark:text-gray-400)
    - Right column: sum passing/failing/partial across all frameworks; three lines each with inline-block w-2 h-2 rounded-full mr-1 colored dot + "{N} Passing/Failing/Partial" (text-sm)

    Per-framework list (no outer padding, divide-y divide-gray-100 dark:divide-gray-700):
    For each fw in data.frameworks:
    - Row is a button element (not div) for keyboard accessibility
    - aria-expanded={expandedFramework === fw.framework_id}
    - aria-controls={fw.framework_id + "-detail"}
    - aria-label={expandedFramework === fw.framework_id ? "Collapse " + fw.framework_name + " breakdown" : "Expand " + fw.framework_name + " breakdown"}
    - onClick: toggle expandedFramework (set to fw.framework_id if collapsed, null if expanded)
    - Classes: w-full flex items-center p-4 cursor-pointer hover:bg-gray-50 dark:hover:bg-gray-700/50 transition-colors duration-150 border-t border-gray-100 dark:border-gray-700 min-h-[44px]
    - Left: fw.short_name in w-24 text-sm font-medium text-gray-800 dark:text-white text-left
    - Middle: flex-1 mx-4 h-1.5 rounded-full bg-gray-200 dark:bg-gray-700 container; inner div width={fw.score + "%"} with barColor(fw.score) h-full rounded-full
    - Right: fw.score + "%" in w-12 text-right text-sm font-medium + scoreColor(fw.score)
    - ChevronDown w-4 h-4 text-gray-400 ml-2 transition-transform duration-200; rotate-180 when expanded
    - When expandedFramework === fw.framework_id, render drill-down div id={fw.framework_id + "-detail"} below the button:
      - bg-gray-50 dark:bg-gray-700/30 px-4 py-3
      - Three lines: CheckCircle2 w-4 h-4 mr-2 text-green-500 aria-hidden + "{fw.passing} controls passing"; XCircle w-4 h-4 mr-2 text-red-500 + "{fw.failing} controls failing"; Circle w-4 h-4 mr-2 text-amber-500 + "{fw.partial} controls partial"
      - Each line text-sm; count in font-medium

    Footer (p-4 pt-2):
    - "Score last computed: {relativeTime(data.computed_at)}" in text-xs text-gray-400 dark:text-gray-500

    Edit components/Dashboard.tsx:
    - Add import { ComplianceScorePanel } from './ComplianceScorePanel'; to the import block (after existing imports)
    - Change the existing <div className="mt-8"> wrapper around ComplianceStatus to <div className="mt-8 space-y-6">
    - Insert <ComplianceScorePanel /> immediately before <ComplianceStatus frameworks={complianceFrameworks} />

    Do not modify any other part of Dashboard.tsx.
  </action>
  <verify>
    <automated>cd /home/user/enterprise-omni-agent-ai-platform && npx tsc --noEmit 2>&1 | grep -E "ComplianceScorePanel|FrameworkScore|ComplianceScorePayload" | head -10; echo "TypeScript exit: $?"</automated>
  </verify>
  <done>
    - ComplianceScorePanel.tsx exists, under 250 lines, TypeScript-clean
    - Dashboard.tsx imports and mounts ComplianceScorePanel above ComplianceStatus
    - No text-[Npx] arbitrary sizes in ComplianceScorePanel.tsx (grep confirms)
    - Framework rows use button elements with aria-expanded and aria-controls
    - Error state has role="alert"
    - Panel root has aria-busy="true" during loading
    - All copywriting matches UI-SPEC.md copywriting contract exactly
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ComplianceScorePanel → GET /api/compliance/score | Uses authFetch (JWT Bearer token injected automatically); unauthenticated calls return 401 handled by authFetch |
| API response → React state | data typed as ComplianceScorePayload; all numeric fields from API are rendered as-is (no eval, no dangerouslySetInnerHTML) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-FE-01 | Information Disclosure | ComplianceScorePanel renders tenant data | accept | Data comes from authenticated API; the panel renders what the JWT-authenticated user is entitled to see; no additional client-side gating needed |
| T-09-FE-02 | Spoofing | authFetch token injection | mitigate | authFetch is the project-standard helper and handles Authorization header; no explicit Content-Type set (T-02-07) |
| T-09-FE-03 | Tampering | score data rendered in DOM | accept | Numeric values from API rendered via standard JSX expression (not innerHTML); no XSS surface |
| T-09-SC | Tampering | npm/pip installs | accept | No new packages; all icons from lucide-react (already in project); no install tasks |
</threat_model>

<verification>
Full frontend verification after Wave 2:

```bash
cd /home/user/enterprise-omni-agent-ai-platform

# TypeScript must be clean for all new/edited files
npx tsc --noEmit 2>&1 | head -20

# No arbitrary text sizes in new component
grep -c "text-\[" components/ComplianceScorePanel.tsx  # must be 0

# Button elements used for framework rows (not div)
grep -c "<button" components/ComplianceScorePanel.tsx  # must be >= 1

# aria attributes present
grep -c "aria-expanded" components/ComplianceScorePanel.tsx   # must be >= 1
grep -c "aria-controls" components/ComplianceScorePanel.tsx   # must be >= 1
grep -c 'role="alert"' components/ComplianceScorePanel.tsx    # must be >= 1

# Dashboard mounts panel
grep -c "ComplianceScorePanel" components/Dashboard.tsx  # must be >= 2 (import + mount)
grep -c "space-y-6" components/Dashboard.tsx             # must be >= 1

# File sizes
wc -l components/ComplianceScorePanel.tsx   # must be <= 250
wc -l components/Dashboard.tsx              # must be <= 60

# Backend tests still green (Wave 1 must not regress)
cd backend && source venv/bin/activate && python3 -m pytest tests/test_compliance_score.py -x -q
```
</verification>

<success_criteria>
- ComplianceScorePanel.tsx renders overall score with correct color thresholds (≥80 green, 50-79 amber, <50 red)
- Severity weight tooltip appears on Info icon hover/focus with all four severity rows and formula line
- Framework rows are collapsible buttons with aria-expanded; click toggles drill-down with passing/failing/partial counts
- Loading skeleton renders three animated rows while fetch is in-flight
- Error state (role="alert") shown when API returns null
- Empty state shown when overall_score === 0 and no frameworks
- Computed-at footer shows relative time
- TypeScript compilation is clean
- No arbitrary text size classes (text-[Npx]) in ComplianceScorePanel.tsx
- Dashboard.tsx mounts ComplianceScorePanel above ComplianceStatus in space-y-6 wrapper
- fetchComplianceScore exported from apiService.ts; returns null on HTTP error or network failure
- SCORE-01, SCORE-02, SCORE-03 visually satisfied
</success_criteria>

<nyquist_validation>
## Requirement → Visual Coverage

| Req ID | Behavior | Component / Element | Verified By |
|--------|----------|--------------------|-------------|
| SCORE-01 | Compliance score visible on main dashboard | ComplianceScorePanel mounted in Dashboard.tsx | grep Dashboard.tsx for ComplianceScorePanel |
| SCORE-01 | Score is % controls passing (live, not static) | fetchComplianceScore() calls backend endpoint; does NOT read framework.progress | grep ComplianceScorePanel.tsx — must NOT contain "framework.progress" |
| SCORE-02 | Severity weighting visible in tooltip | SeverityWeightTooltip with Critical×4, High×3, Medium×2, Low×1 rows | grep ComplianceScorePanel.tsx for tooltip content strings |
| SCORE-02 | Score number colored by threshold | scoreColor() helper applied to text-3xl number | grep for scoreColor function definition and usage |
| SCORE-03 | Per-framework breakdown visible | data.frameworks.map() renders rows with short_name + progress bar + score% | grep ComplianceScorePanel.tsx for framework_id accordion |
| SCORE-03 | Framework rows can be expanded | expandedFramework state + button onClick toggle + drill-down div | grep ComplianceScorePanel.tsx for aria-expanded |
| UI-01 | No text-[Npx] arbitrary sizes | Minimum text-xs throughout | grep -c "text-\[" components/ComplianceScorePanel.tsx == 0 |
</nyquist_validation>

<output>
Create `.planning/phases/09-compliance-score-dashboard/09-02-SUMMARY.md` when done.
</output>
