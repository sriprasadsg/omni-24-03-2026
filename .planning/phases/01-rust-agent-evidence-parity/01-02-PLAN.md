---
phase: 01-rust-agent-evidence-parity
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - backend/tests/test_rust_heartbeat_parity.py
autonomous: true
requirements:
  - RUST-01
  - RUST-02
  - RUST-03

must_haves:
  truths:
    - "A simulated Rust agent heartbeat POST to /api/agents/{id}/heartbeat results in asset_compliance records in the database"
    - "Each resulting asset_compliance record contains agent_type equal to 'rust' at the document level and inside at least one evidence[] entry"
    - "All 12 Rust compliance check names produce at least one asset_compliance record each (mapping parity verified)"
    - "The simulation test script exits with code 0 when run against a live backend"
  artifacts:
    - path: "backend/tests/test_rust_heartbeat_parity.py"
      provides: "End-to-end simulation test for Rust agent heartbeat compliance evidence flow"
      contains: "agent_type"
  key_links:
    - from: "backend/tests/test_rust_heartbeat_parity.py"
      to: "/api/agents/{agent_id}/heartbeat"
      via: "HTTP POST with Authorization: Bearer header"
      pattern: "requests.post.*heartbeat"
    - from: "backend/tests/test_rust_heartbeat_parity.py"
      to: "db.asset_compliance"
      via: "pymongo direct query for verification"
      pattern: "asset_compliance.find_one"
---

<objective>
Write and run a simulation test that proves the Rust agent heartbeat evidence pipeline works end-to-end after Plan 01's fixes.

The test:
1. Registers a test agent (or reuses one) and retrieves an auth token
2. POSTs a realistic Rust-style heartbeat payload containing all 12 compliance check names and meta.agent_type = "rust" to /api/agents/{id}/heartbeat
3. Queries MongoDB directly to assert: (a) asset_compliance records exist for the test asset, (b) each record contains agent_type: "rust" at the document level, (c) at least one evidence entry inside each record contains agent_type: "rust", and (d) records exist for representative controls across all 12 check mappings

Purpose: Provides living proof that RUST-01 (pipeline wired), RUST-02 (agent_type preserved in DB), and RUST-03 (all 12 check names mapped) are all satisfied. The script is the acceptance test for the phase.

Output: backend/tests/test_rust_heartbeat_parity.py (new file, ~120 lines)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-rust-agent-evidence-parity/01-RESEARCH.md
@.planning/phases/01-rust-agent-evidence-parity/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Rust heartbeat simulation test script</name>
  <files>backend/tests/test_rust_heartbeat_parity.py</files>

  <read_first>
    Read .planning/phases/01-rust-agent-evidence-parity/01-RESEARCH.md sections "How to verify RUST-01", "How to verify RUST-02", and "How to verify RUST-03" — these contain the exact payload structure, MongoDB queries, and expected control IDs to use.

    Also read backend/agent_heartbeat_endpoints.py lines 1–30 to understand how verify_agent_key works and what auth header format the heartbeat endpoint expects.

    Check if backend/tests/ directory exists: `ls backend/tests/ 2>/dev/null || echo "dir missing"`. If missing, create it.
  </read_first>

  <action>
    Create backend/tests/test_rust_heartbeat_parity.py. This is a standalone Python script (not a pytest module) that can be run directly: `python3 backend/tests/test_rust_heartbeat_parity.py`.

    The script must:

    SECTION 1 — Configuration (top of file, all values as module-level constants):
    - BASE_URL: read from env var BACKEND_URL, default "http://localhost:5000"
    - MONGO_URI: read from env var MONGODB_URI or MONGO_URI, default "mongodb://localhost:27017"
    - DB_NAME: read from env var MONGO_DB_NAME, default "omni_platform"
    - TEST_AGENT_ID: "TEST-RUST-PARITY-001" (constant — used for cleanup)
    - TEST_HOSTNAME: "test-rust-parity-host"
    - TEST_ASSET_ID: f"asset-{TEST_HOSTNAME}"

    SECTION 2 — Payload construction:
    Build RUST_HEARTBEAT_PAYLOAD as a dict with:
    - "status": "Online"
    - "platform": "Windows"
    - "version": "2.0.0-rust"
    - "hostname": TEST_HOSTNAME
    - "meta": {
        "agent_type": "rust",
        "compliance_enforcement": {
          "compliance_checks": [
            {"check": "Windows Firewall Profiles", "status": "Pass", "details": "All profiles enabled"},
            {"check": "Windows Defender Antivirus", "status": "Pass", "details": "Enabled and updated"},
            {"check": "BitLocker Encryption", "status": "Warning", "details": "Not enabled on C:"},
            {"check": "User Access Control", "status": "Pass", "details": "UAC enabled"},
            {"check": "Remote Desktop Service", "status": "Pass", "details": "RDP disabled"},
            {"check": "SMBv1 Protocol Disabled", "status": "Pass", "details": "SMBv1 disabled"},
            {"check": "Password Policy (Min Length)", "status": "Pass", "details": "MinLen:10"},
            {"check": "Audit Logging Policy", "status": "Pass", "details": "Audit policies configured"},
            {"check": "Windows Update Service", "status": "Pass", "details": "wuauserv Running"},
            {"check": "PowerShell Script Block Logging", "status": "Warning", "details": "Not configured"},
            {"check": "WinRM Status", "status": "Pass", "details": "WinRM not running (expected)"},
            {"check": "Secure Boot", "status": "Pass", "details": "Secure Boot enabled"}
          ]
        }
      }

    SECTION 3 — Helper: get_agent_token(db):
    Query MongoDB collection `agents` for a document where id == TEST_AGENT_ID.
    If found and it has an `api_key` field, return that value.
    Otherwise, return None.
    This handles the case where a prior test run already registered the agent.

    SECTION 4 — Helper: register_test_agent(db):
    POST to {BASE_URL}/api/agents/register with JSON body:
    {"agent_id": TEST_AGENT_ID, "hostname": TEST_HOSTNAME, "platform": "Windows", "version": "2.0.0-rust", "agent_type": "rust", "tenantId": "test-tenant"}
    Do not fail if registration returns 409 (already registered). Extract and return the api_key from the response JSON. If registration fails and no existing key is found, raise RuntimeError with clear message.

    SECTION 5 — Helper: cleanup(db):
    Delete from asset_compliance where assetId == TEST_ASSET_ID.
    Delete from agents where id == TEST_AGENT_ID.
    Delete from assets where id == TEST_ASSET_ID.
    Called at the start of main() to ensure a clean state before the test run.

    SECTION 6 — main() function:
    Step 1: Connect to MongoDB with pymongo.MongoClient(MONGO_URI), db = client[DB_NAME]
    Step 2: cleanup(db) — remove any prior test data
    Step 3: Get or register agent token. Call get_agent_token(db) first; if None, call register_test_agent(db).
    Step 4: POST the heartbeat. Use requests.post with:
      url: f"{BASE_URL}/api/agents/{TEST_AGENT_ID}/heartbeat"
      headers: {"Authorization": f"Bearer {token}", "Content-Type": "application/json"}
      json: RUST_HEARTBEAT_PAYLOAD
      timeout: 10
    Assert response.status_code == 200 and response.json().get("success") == True.
    Print f"[RUST-01] Heartbeat POST returned {response.status_code}: PASS"

    Step 5: Wait 1 second (time.sleep(1)) to allow async processing to complete.

    Step 6: RUST-02 verification — query db.asset_compliance.find({"assetId": TEST_ASSET_ID}):
    - Assert at least one document exists
    - For each document, assert doc.get("agent_type") == "rust" (document-level)
    - For each document, assert any(e.get("agent_type") == "rust" for e in doc.get("evidence", []))
    - Print count of records found and "[RUST-02] agent_type: rust present in all records: PASS"

    Step 7: RUST-03 verification — check that records exist for a representative set of control IDs:
    CHECK_TO_CONTROL = {
      "Windows Firewall Profiles": "A.8.22",
      "Windows Defender Antivirus": "A.8.7",
      "BitLocker Encryption": "A.8.1",
      "User Access Control": "A.5.15",
      "Password Policy (Min Length)": "CC6.1",
      "Audit Logging Policy": "CC9.2",
      "Windows Update Service": "CC7.3",
      "PowerShell Script Block Logging": "DE.CM-1",
      "Secure Boot": "CC7.2",
    }
    For each (check_name, control_id) in CHECK_TO_CONTROL.items():
      record = db.asset_compliance.find_one({"assetId": TEST_ASSET_ID, "controlId": control_id})
      Print FOUND or MISSING for each.
      Assert record is not None (fail loudly if any mapping is missing).
    Print "[RUST-03] All representative control mappings present: PASS"

    Step 8: Print final summary:
      "=== RUST AGENT EVIDENCE PARITY: ALL CHECKS PASSED ==="
      "RUST-01: heartbeat endpoint accepted payload and called process_automated_evidence"
      "RUST-02: agent_type=rust preserved in asset_compliance documents and evidence records"
      "RUST-03: all 9 verified control IDs found in asset_compliance"

    Step 9: cleanup(db) — remove test data so the collection stays clean.

    If __name__ == "__main__": call main(). Use sys.exit(0) on success. On any assertion error or exception, print the error with traceback and sys.exit(1).

    Imports required at top: import os, sys, time, requests, pymongo, traceback

    File must stay under 200 lines total.
  </action>

  <verify>
    <automated>cd /home/user/enterprise-omni-agent-ai-platform && python3 -c "
import ast
src = open('backend/tests/test_rust_heartbeat_parity.py').read()
ast.parse(src)
print('syntax ok')
assert 'TEST-RUST-PARITY-001' in src
assert 'agent_type' in src
assert 'RUST-01' in src
assert 'RUST-02' in src
assert 'RUST-03' in src
assert 'asset_compliance' in src
assert 'Windows Firewall Profiles' in src
assert 'Secure Boot' in src
lines = src.splitlines()
assert len(lines) <= 200, f'File is {len(lines)} lines — exceeds 200 line limit'
print(f'Structure check: PASS ({len(lines)} lines)')
"
    </automated>
  </verify>

  <acceptance_criteria>
    - `python3 -c "import ast; ast.parse(open('backend/tests/test_rust_heartbeat_parity.py').read()); print('ok')"` prints "ok"
    - File contains all 12 Rust check names from the payload (verified by grep for "Secure Boot" and "WinRM Status")
    - File contains "agent_type" assertion logic
    - File references "asset_compliance" collection
    - File is 200 lines or fewer (CLAUDE.md: keep files under 500 lines; target is lean at 200)
    - `grep -c 'RUST-0' backend/tests/test_rust_heartbeat_parity.py` returns 3 or more (all three requirement IDs present)
  </acceptance_criteria>

  <done>backend/tests/test_rust_heartbeat_parity.py exists, parses without errors, contains all 12 check names, asserts agent_type and asset_compliance records, references RUST-01/02/03, and is under 200 lines.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plan 01 fixed the two backend gaps (import + agent_type passthrough + evidence_record fields).
    Plan 02 Task 1 created backend/tests/test_rust_heartbeat_parity.py.

    This checkpoint runs the simulation test against the live backend and verifies the full pipeline end-to-end.
  </what-built>

  <how-to-verify>
    1. Ensure the backend is running: `curl -s http://localhost:5000/health | python3 -m json.tool`
       Expected: JSON response with `"status": "healthy"` or similar. If backend is not running, start it first.

    2. Run the simulation test:
       `cd /home/user/enterprise-omni-agent-ai-platform && python3 backend/tests/test_rust_heartbeat_parity.py`

    Expected output (exact text):
       [RUST-01] Heartbeat POST returned 200: PASS
       [RUST-02] agent_type: rust present in all records: PASS
       [RUST-03] All representative control mappings present: PASS
       === RUST AGENT EVIDENCE PARITY: ALL CHECKS PASSED ===

    3. If the backend is not running locally, verify by static analysis only:
       `python3 -c "import ast; ast.parse(open('backend/tests/test_rust_heartbeat_parity.py').read()); print('syntax ok')"`
       and confirm the grep checks in Task 1 acceptance criteria all pass.

    4. Optionally: open the application in the browser, navigate to Compliance Frameworks, select SOC 2, and confirm "System Check: Windows Firewall Profiles" appears for the test asset (if backend ran successfully).
  </how-to-verify>

  <resume-signal>Type "approved" if the test passed (or backend is not running and static checks passed). Describe any failures and Claude will diagnose.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test script→backend | Test makes HTTP POST to local backend using a test agent token |
| test script→MongoDB | Direct pymongo connection for assertion queries (read + cleanup only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | test_rust_heartbeat_parity.py | accept | Script is a test utility in backend/tests/, not a production endpoint. It reads from env vars for connection strings — no hardcoded credentials committed. Test agent and test data are cleaned up before and after the run. |
| T-02-02 | Tampering | TEST-RUST-PARITY-001 agent record | accept | Test uses a clearly namespaced agent ID and cleans up. Risk of collision with real agent data is negligible — test_rust_host hostname is unique to the test. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed. Script uses stdlib (os, sys, time, traceback) plus requests and pymongo which are already in the project's dependency set. |
</threat_model>

<verification>
Overall phase verification after both plans complete:

```bash
# Syntax check both modified backend files
python3 -c "import ast; ast.parse(open('backend/compliance_evidence_processor.py').read()); print('compliance_evidence_processor.py ok')"
python3 -c "import ast; ast.parse(open('backend/agent_heartbeat_endpoints.py').read()); print('agent_heartbeat_endpoints.py ok')"

# Confirm agent_type in processor (expect 3)
grep -c 'agent_type' backend/compliance_evidence_processor.py

# Confirm import fix
grep 'from compliance_evidence_processor import process_automated_evidence' backend/agent_heartbeat_endpoints.py

# Confirm test file created
ls -la backend/tests/test_rust_heartbeat_parity.py

# Run simulation test (requires live backend + MongoDB)
python3 backend/tests/test_rust_heartbeat_parity.py
```

RUST-01 satisfied when: heartbeat endpoint imports directly from compliance_evidence_processor and the POST returns 200.
RUST-02 satisfied when: asset_compliance records contain agent_type: "rust" at document level and inside evidence[] entries.
RUST-03 satisfied when: all 9 representative control IDs appear in asset_compliance for the test asset.
</verification>

<success_criteria>
- Simulation test exits with code 0 (all assertions pass)
- backend/tests/test_rust_heartbeat_parity.py passes syntax check
- Both backend/*.py files from Plan 01 pass syntax check
- No pre-existing Python agent callers are broken (agent_type defaults to None)
- Frontend compliance frameworks view shows evidence for test asset after simulation run (human-verify checkpoint)
</success_criteria>

## Artifacts this phase produces

| Artifact | Type | Description |
|----------|------|-------------|
| `backend/tests/test_rust_heartbeat_parity.py` | New file | Simulation test: POST Rust heartbeat → verify DB records contain agent_type: rust → verify all 12 check mappings |

<output>
Create `.planning/phases/01-rust-agent-evidence-parity/01-02-SUMMARY.md` when done
</output>
