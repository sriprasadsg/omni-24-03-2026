---
phase: 54-integration-operator-ui
plan: 04
type: execute
wave: 3
depends_on: ["54-02", "54-03"]
files_modified:
  - App.tsx
  - components/Sidebar.tsx
autonomous: true
requirements: [INT-01]
must_haves:
  truths:
    - "A 'Native Security' sidebar entry is reachable and opens NativeSecurityConsole, gated by manage:active_response"
    - "App.tsx lazy-imports NativeSecurityConsole and renders it in an ErrorBoundary on the nativeSecurity case"
    - "Users without manage:active_response do not see the entry and cannot reach the view"
  artifacts:
    - path: "App.tsx"
      provides: "lazy import + render switch case for nativeSecurity"
      contains: "NativeSecurityConsole"
    - path: "components/Sidebar.tsx"
      provides: "Native Security nav entry gated by manage:active_response"
      contains: "nativeSecurity"
  key_links:
    - from: "App.tsx"
      to: "components/NativeSecurityConsole.tsx"
      via: "lazy import + switch case (permission map entry landed in 54-02)"
      pattern: "NativeSecurityConsole"
    - from: "components/Sidebar.tsx"
      to: "App.tsx"
      via: "nav entry view:'nativeSecurity' permission:'manage:active_response'"
      pattern: "nativeSecurity"
  prohibitions:
    - "MUST NOT gate on anything other than manage:active_response"
    - "MUST NOT duplicate the permission-map entry (added in 54-02)"
---

<objective>
Register the console into navigation (clone of 49-05): App.tsx lazy import + render switch case + a `manage:active_response`-gated Sidebar entry. The permission-map entry landed in 54-02.

Purpose: INT-01 reachability. D-02.
Output: nav registration.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@.planning/phases/54-integration-operator-ui/54-RESEARCH.md
@.planning/phases/54-integration-operator-ui/54-CONTEXT.md

# Read-first anchors:
@App.tsx                   # fleetGeoMap lazy import (~166) + switch case (~1916) — clone
@components/Sidebar.tsx    # fleetGeoMap / active-response nav entry (~417) — clone; ShieldZapIcon/ShieldAlertIcon already imported
</execution_context>

<artifacts>
- EDIT `App.tsx` — lazy import + `case 'nativeSecurity'` render.
- EDIT `components/Sidebar.tsx` — `nativeSecurity` nav entry, `manage:active_response`.
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: App.tsx lazy import + switch case</name>
  <files>App.tsx</files>
  <read_first>the fleetGeoMap lazy import + switch case to clone.</read_first>
  <action>Add `const NativeSecurityConsole = lazy(() => import('./components/NativeSecurityConsole').then(m => ({ default: m.NativeSecurityConsole })));` near the other lazy imports; add `case 'nativeSecurity': return <ErrorBoundary name="NativeSecurityConsole"><NativeSecurityConsole /></ErrorBoundary>;` in the render switch. (Permission-map entry already added in 54-02.)</action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "(^|/)App.tsx|NativeSecurity" | grep -vE "github-mcp-server|servers/src|version 1" || echo "no errors"; npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>App.tsx lazy-imports + renders nativeSecurity in an ErrorBoundary; builds.</done>
</task>

<task type="auto">
  <name>Task 2: Sidebar nav entry</name>
  <files>components/Sidebar.tsx</files>
  <read_first>an existing active-response / fleetGeoMap nav entry + the icons import.</read_first>
  <action>Add a nav entry `{ view: 'nativeSecurity', label: 'Native Security', icon: <ShieldZapIcon size={20} />, permission: 'manage:active_response' }` next to the security-ops entries (ensure the icon is imported; ShieldZapIcon/ShieldAlertIcon already appear in the icons import). Permission MUST be `manage:active_response`.</action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "Sidebar" | grep -vE "github-mcp-server|servers/src|version 1" || echo "no errors"; npm run build 2>&1 | tail -3; ls dist-new/assets/ | grep -i NativeSecurity || echo "check chunk in build output"</automated>
    <human-check>As a manage:active_response operator, confirm the "Native Security" sidebar entry appears and opens the console. As a user without it, confirm the entry is hidden and the view is unreachable.</human-check>
  </verify>
  <done>Sidebar entry present, gated by manage:active_response, opens the console; app builds; own lazy chunk emitted.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-54-07 | EoP | non-ops user reaching the console | mitigate | Nav + view gated by manage:active_response; endpoints independently gate (54-01/53-04) |
| T-54-SC | Tampering | package installs | accept | No new deps |
</threat_model>

<verification>
- `npx tsc --noEmit` clean for App.tsx/Sidebar.tsx; `npm run build` succeeds; a `NativeSecurityConsole-*.js` lazy chunk is emitted.
- Console reachable for manage:active_response holders, hidden/unreachable otherwise.
</verification>

<success_criteria>
The Native Security Console is registered into navigation: a `manage:active_response`-gated "Native Security" sidebar entry opens `NativeSecurityConsole` in an ErrorBoundary; users without the permission never see or reach it — completing INT-01.
</success_criteria>

<output>
Create `.planning/phases/54-integration-operator-ui/54-04-SUMMARY.md` when done.
</output>
