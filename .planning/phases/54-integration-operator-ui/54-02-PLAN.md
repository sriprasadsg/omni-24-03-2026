---
phase: 54-integration-operator-ui
plan: 02
type: execute
wave: 1
depends_on: []
requirements: [INT-01, INT-02, INT-03]
files_modified:
  - types.ts
  - App.tsx
  - services/apiService.ts
autonomous: true
must_haves:
  truths:
    - "types.ts AppView includes a new 'nativeSecurity' member and the response types for findings/queue/audit/fim-status"
    - "viewPermissionMap has nativeSecurity: 'manage:active_response' (Record is exhaustive — must be present to typecheck)"
    - "apiService exports fetchFindings (aggregated feed), fetchRemediationQueue, triggerScan, fetchRemediationAudit, approveRemediation, denyRemediation, fetchFimStatus, fetchSecuritySummary via authFetch"
    - "apiService exports remediation-playbook CRUD clients (fetchRemediationPlaybooks/create/update/delete) targeting the 53-01 remediation_playbook_endpoints (the deterministic store), NOT the LLM playbook endpoints"
  artifacts:
    - path: "types.ts"
      provides: "nativeSecurity AppView + native-security response types"
      contains: "nativeSecurity"
    - path: "services/apiService.ts"
      provides: "native-security api clients"
      contains: "triggerScan"
    - path: "App.tsx"
      provides: "viewPermissionMap nativeSecurity entry"
      contains: "nativeSecurity"
  key_links:
    - from: "services/apiService.ts"
      to: "backend/security_ops_endpoints.py"
      via: "authFetch(`${API_BASE}/security-ops/...`) + /api/remediation/... (53-04)"
      pattern: "security-ops"
  prohibitions:
    - "MUST NOT gate nativeSecurity on anything other than manage:active_response"
    - "MUST NOT hand-roll fetch — reuse authFetch + the standard error shape"
---

<objective>
Establish the frontend contract for the console: the `nativeSecurity` AppView + its permission-map entry, response types, and the apiService clients for findings/queue/trigger/audit/approve-deny/fim-status. 54-03 builds against this.

Purpose: contract-first for INT-01/02/03. D-02/D-05.
Output: types + permission entry + api clients.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@.planning/phases/54-integration-operator-ui/54-RESEARCH.md
@.planning/phases/54-integration-operator-ui/54-CONTEXT.md

# Read-first anchors:
@types.ts                     # AppView union + viewPermission types
@App.tsx                      # viewPermissionMap (~line 236) — exhaustive Record<AppView,Permission>
@services/apiService.ts       # authFetch + fetchFleetGeo error-shape to clone; existing remediation/vuln clients if any
</execution_context>

<artifacts>
- EDIT `types.ts` — `nativeSecurity` AppView + response types (`SecurityFinding` {source, severity, hostname, target, verdict_or_detail, ts}, RemediationQueueItem, RemediationAuditEntry, FimStatus, SecuritySummary, RemediationPlaybook).
- EDIT `App.tsx` — `nativeSecurity: 'manage:active_response'` in viewPermissionMap.
- EDIT `services/apiService.ts` — the native-security clients.
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: nativeSecurity AppView + types + permission entry</name>
  <files>types.ts, App.tsx</files>
  <read_first>types.ts AppView union (near the fleetGeoMap member); App.tsx viewPermissionMap (~236).</read_first>
  <action>Add `| 'nativeSecurity'` to the AppView union. Add response interfaces for the console: `ScanFinding`, `RemediationQueueItem`, `RemediationAuditEntry`, `FimStatus`, `SecuritySummary` (co-locate in apiService.ts if that matches the fleet-DTO precedent, else types.ts). Add `nativeSecurity: 'manage:active_response'` to `viewPermissionMap` (exhaustive Record — required for tsc).</action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "(^|/)App.tsx|(^|/)types.ts|nativeSecurity" | grep -vE "github-mcp-server|servers/src|version 1" || echo "no errors in touched files"</automated>
  </verify>
  <done>nativeSecurity AppView + types + permission entry compile.</done>
</task>

<task type="auto">
  <name>Task 2: apiService native-security clients</name>
  <files>services/apiService.ts</files>
  <read_first>fetchFleetGeo for the authFetch + error-shape pattern.</read_first>
  <action>Add `fetchFindings(params)` → `GET /security-ops/findings` (the aggregated feed); `fetchRemediationQueue()` → `GET /security-ops/remediation-queue`; `triggerScan(agentId, type, target)` → `POST /security-ops/trigger-scan`; `fetchFimStatus()` → `GET /security-ops/fim-status`; `fetchSecuritySummary()` → `GET /security-ops/summary`; `fetchRemediationAudit(params)` → `GET /api/remediation/audit` (53-04); `approveRemediation(id)` / `denyRemediation(id)` → `POST /api/remediation/{id}/approve|deny` (53-04); and the remediation-playbook CRUD — `fetchRemediationPlaybooks()`/`createRemediationPlaybook(body)`/`updateRemediationPlaybook(id, body)`/`deleteRemediationPlaybook(id)` → the 53-01 `remediation_playbook_endpoints` routes (the deterministic store, NOT the LLM playbook endpoints). All via `authFetch` with the standard error handling.</action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "apiService" | grep -vE "github-mcp-server|servers/src|version 1" || echo "no errors"; npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>All clients typecheck + build; hit the right endpoints via authFetch.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-54-04 | Spoofing | unauthenticated calls | mitigate | authFetch session token on every client |
| T-54-SC | Tampering | package installs | accept | No new deps |
</threat_model>

<verification>
- `npx tsc --noEmit` clean for touched files; `npm run build` succeeds.
- `nativeSecurity` in AppView + permission map (manage:active_response); all clients present.
</verification>

<success_criteria>
The console's frontend contract exists: `nativeSecurity` AppView gated on manage:active_response, response types, and authFetch clients for findings/queue/trigger/audit/approve-deny/fim-status/summary.
</success_criteria>

<output>
Create `.planning/phases/54-integration-operator-ui/54-02-SUMMARY.md` when done.
</output>
