---
phase: 54-integration-operator-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/security_ops_endpoints.py
  - backend/router_registry.py
  - backend/tests/test_security_ops_endpoints.py
autonomous: true
requirements: [INT-03]
must_haves:
  truths:
    - "GET /api/security-ops/findings returns ONE normalized, paginated feed aggregating scan verdicts (security_scan_results) + native vuln findings (vulnerabilities) + FIM events (fim_events), each item {source, severity, hostname, target, verdict_or_detail, ts}, tenant-scoped"
    - "GET /api/security-ops/remediation-queue lists pending/in-flight remediations, tenant-scoped"
    - "POST /api/security-ops/trigger-scan dispatches a scan command to an agent via the existing agent_instructions queue"
    - "GET /api/security-ops/fim-status + /summary return per-agent FIM status and a tenant rollup; all gated manage:active_response, wrapped db"
  artifacts:
    - path: "backend/security_ops_endpoints.py"
      provides: "findings/remediation-queue/trigger-scan/fim-status/summary ops endpoints"
      contains: "/trigger-scan"
      min_lines: 70
    - path: "backend/tests/test_security_ops_endpoints.py"
      provides: "tests: list scoping, trigger dispatch inserts agent_instruction, permission gate"
      contains: "trigger-scan"
      min_lines: 55
  key_links:
    - from: "backend/router_registry.py"
      to: "backend/security_ops_endpoints.py"
      via: "_load(app, 'security_ops_endpoints', 'router')"
      pattern: "security_ops_endpoints"
    - from: "backend/security_ops_endpoints.py"
      to: "agent_instructions (queue)"
      via: "trigger-scan inserts a scan instruction (reuse Phase-53 dispatch shape)"
      pattern: "agent_instructions"
  prohibitions:
    - "MUST NOT duplicate existing endpoints (scan POST 50-04, vuln GET 51, fim-events 52, approve/deny+audit 53-04, playbook CRUD)"
    - "MUST NOT build a second dispatch channel — reuse agent_instructions"
    - "MUST NOT access db._db; MUST gate on manage:active_response"
---

<objective>
Add the thin ops endpoints that complete INT-03 on top of the 50-53 surfaces: list scan findings, list the remediation queue, trigger an on-demand scan (via the existing dispatch queue), and report FIM status + a summary.

Purpose: INT-03. D-03.
Output: `security_ops_endpoints.py` + registry + tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@.planning/phases/54-integration-operator-ui/54-RESEARCH.md
@.planning/phases/54-integration-operator-ui/54-CONTEXT.md

# Read-first anchors:
@backend/agent_fleet_geo_endpoints.py       # router/tenant/wrapped-db pattern + registry _load
@backend/autonomous_remediation_service.py  # execute_plan agent_instructions insert shape (reuse for trigger-scan)
@backend/agent_security_scan_endpoints.py   # security_scan_results collection (findings source)
</execution_context>

<artifacts>
- NEW `backend/security_ops_endpoints.py` — the ops endpoints.
- EDIT `backend/router_registry.py` — register the router.
- NEW `backend/tests/test_security_ops_endpoints.py` — scoping/dispatch/permission tests.
</artifacts>

<tasks>

<task type="tdd">
  <name>Task 1: failing tests for the ops endpoints</name>
  <files>backend/tests/test_security_ops_endpoints.py</files>
  <action>Tests: (a) `GET /findings` returns ONE normalized feed merging mock `security_scan_results` + `vulnerabilities` + `fim_events` for the caller tenant — each item carrying `source` (scan|vuln|fim), `severity`, `hostname`, `target`, `ts` — paginated (limit/offset or cursor), sorted newest-first, and excluding other tenants; (b) `GET /remediation-queue` returns pending/in-flight remediations for the tenant; (c) `POST /trigger-scan {agent_id, type:"file", target}` inserts an agent_instructions doc `{type:"scan_file", agent_id, payload:{path:target}}` (assert the insert); (d) a caller lacking manage:active_response is 403; (e) `GET /fim-status` + `/summary` return the expected shapes. Mock get_database + the permission dep per existing patterns.</action>
  <verify>
    <automated>backend/venv/bin/python -m pytest backend/tests/test_security_ops_endpoints.py -q 2>&1 | tail -10</automated>
  </verify>
  <done>Tests exist and FAIL.</done>
</task>

<task type="auto">
  <name>Task 2: implement the ops endpoints + register</name>
  <files>backend/security_ops_endpoints.py, backend/router_registry.py</files>
  <read_first>execute_plan's agent_instructions insert; the fleet-geo router/permission/tenant pattern.</read_first>
  <action>Create `security_ops_endpoints.py`, `APIRouter(prefix="/api/security-ops", tags=["Security Ops"])`, all routes gated by the `manage:active_response` permission dep. `GET /findings` — query `security_scan_results` + `vulnerabilities` + `fim_events` for the tenant, normalize each into a common item `{source, severity, hostname, target, verdict_or_detail, ts}`, merge, sort newest-first, and paginate (bounded page size); the UI consumes this single feed (no client-side federation); `GET /remediation-queue` (tenant remediations in pending_approval/dispatched-unverified states); `POST /trigger-scan` (`{agent_id, type: file|vuln|fim, target?}` → map to a scan command `scan_file|vuln-scan|fim`, insert `{type, agent_id, payload, status:"pending", triggered_by:"operator"}` into `agent_instructions` after an agent-connected check, return `{queued:true}`); `GET /fim-status` (per-agent FIM summary from `agent.meta.capabilities` + recent fim_events counts); `GET /summary` (findings-by-verdict + open-remediation + drift rollup). Wrapped db, never db._db. Register `_load(app, "security_ops_endpoints", "router")`.</action>
  <verify>
    <automated>backend/venv/bin/python -m pytest backend/tests/test_security_ops_endpoints.py -q 2>&1 | tail -8</automated>
  </verify>
  <done>Endpoints implemented + registered; tests pass; no duplicated endpoints; reuses agent_instructions.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-54-01 | EoP | non-ops user triggers scans / views findings | mitigate | All routes gated manage:active_response |
| T-54-02 | Info Disclosure | cross-tenant findings/queue | mitigate | Tenant-scoped queries + wrapped db |
| T-54-03 | Spoofing | trigger-scan to a foreign agent | mitigate | Agent must belong to the caller tenant; connectivity check |
</threat_model>

<verification>
- `pytest test_security_ops_endpoints.py` green (findings/queue scoping, trigger dispatch insert, 403 gate, fim-status/summary).
- No duplicate endpoints; trigger reuses agent_instructions.
</verification>

<success_criteria>
`/api/security-ops` exposes findings, remediation-queue, trigger-scan (via the existing dispatch), fim-status, and summary — tenant-scoped and gated on manage:active_response — completing INT-03 alongside the 50-53 endpoints.
</success_criteria>

<output>
Create `.planning/phases/54-integration-operator-ui/54-01-SUMMARY.md` when done.
</output>
