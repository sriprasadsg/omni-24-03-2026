---
phase: 54-integration-operator-ui
plan: 03
type: execute
wave: 2
depends_on: ["54-01", "54-02"]
files_modified:
  - components/NativeSecurityConsole.tsx
  - components/nativeSecurity/FindingsTab.tsx
  - components/nativeSecurity/RemediationQueueTab.tsx
  - components/nativeSecurity/AuditTab.tsx
autonomous: true
requirements: [INT-01, INT-02]
must_haves:
  truths:
    - "NativeSecurityConsole renders a tabbed page: Findings, Remediation Queue, Playbooks, Audit"
    - "Findings tab shows scan verdicts + vuln + FIM with a 'Scan now' trigger; Remediation Queue shows pending/in-flight with Approve/Deny; Audit shows the immutable trail; Playbooks embeds the existing PlaybookManager"
    - "Approve/Deny call the 53-04 endpoints; trigger-scan calls 54-01; all with loading/empty/error states"
    - "The console file stays under 500 lines (tabs extracted into child components)"
  artifacts:
    - path: "components/NativeSecurityConsole.tsx"
      provides: "tabbed operator console shell (named export NativeSecurityConsole)"
      contains: "NativeSecurityConsole"
      min_lines: 60
    - path: "components/nativeSecurity/RemediationQueueTab.tsx"
      provides: "pending/in-flight queue with Approve/Deny"
      contains: "approveRemediation"
  key_links:
    - from: "components/NativeSecurityConsole.tsx"
      to: "services/apiService.ts"
      via: "fetchScanFindings/fetchRemediationQueue/triggerScan/fetchRemediationAudit/approve-deny"
      pattern: "fetchRemediationQueue"
    - from: "components/NativeSecurityConsole.tsx"
      to: "components/PlaybookManager.tsx"
      via: "Playbooks tab embeds the existing PlaybookManager"
      pattern: "PlaybookManager"
  prohibitions:
    - "MUST NOT rebuild PlaybookManager — embed the existing component"
    - "MUST NOT bypass the server approval gate — the UI triggers, the server enforces"
    - "MUST NOT exceed 500 lines in NativeSecurityConsole.tsx (extract tabs)"
---

<objective>
Build the unified operator console: a tabbed `NativeSecurityConsole` (Findings / Remediation Queue / Playbooks / Audit) with the trigger-scan and approve/deny controls, reusing existing components and the 54-02 clients.

Purpose: INT-01/02. D-01/D-04/D-05.
Output: console + tab child components.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@.planning/phases/54-integration-operator-ui/54-RESEARCH.md
@.planning/phases/54-integration-operator-ui/54-CONTEXT.md

# Read-first anchors:
@components/FleetObservabilityDashboard.tsx  # dashboard shell + loading/empty/error to mirror
@components/PlaybookManager.tsx              # embed in the Playbooks tab
@components/RemediationDashboard.tsx         # existing queue/approve UI patterns to mirror
@services/apiService.ts                      # 54-02 clients
</execution_context>

<artifacts>
- NEW `components/NativeSecurityConsole.tsx` — tabbed shell (named export).
- NEW `components/nativeSecurity/FindingsTab.tsx` — scan/vuln/FIM feed + Scan-now.
- NEW `components/nativeSecurity/RemediationQueueTab.tsx` — pending/in-flight + Approve/Deny.
- NEW `components/nativeSecurity/AuditTab.tsx` — immutable audit trail (paginated).
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: console shell + tab routing + Findings/Audit tabs</name>
  <files>components/NativeSecurityConsole.tsx, components/nativeSecurity/FindingsTab.tsx, components/nativeSecurity/AuditTab.tsx</files>
  <read_first>FleetObservabilityDashboard for shell + states; apiService clients.</read_first>
  <action>Create `NativeSecurityConsole` (named export) with a tab bar (Findings / Remediation Queue / Playbooks / Audit) rendering the active child. `FindingsTab`: on mount `fetchScanFindings` (+ optionally vuln/fim summaries), render a paginated, filterable table (verdict/severity, hostname, target, ts); a "Scan now" control that calls `triggerScan(agentId, type, target)` and shows a queued toast + refresh. `AuditTab`: `fetchRemediationAudit` paginated, read-only (finding/playbook/steps/verification/override). Loading/empty/error states cloned from FleetObservabilityDashboard. Keep the shell lean.</action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "NativeSecurityConsole|FindingsTab|AuditTab" | grep -vE "github-mcp-server|servers/src" || echo "no errors"; npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>Console shell + Findings + Audit tabs typecheck + build with states.</done>
</task>

<task type="auto">
  <name>Task 2: Remediation Queue tab (approve/deny) + Playbooks embed</name>
  <files>components/nativeSecurity/RemediationQueueTab.tsx, components/NativeSecurityConsole.tsx</files>
  <read_first>RemediationDashboard for the queue/approve UI; PlaybookManager export shape.</read_first>
  <action>`RemediationQueueTab`: `fetchRemediationQueue`, render pending_approval + in-flight remediations (finding, playbook, status, destructive flag); Approve/Deny buttons call `approveRemediation(id)`/`denyRemediation(id)` with optimistic refresh + error toast. In the console, wire the Playbooks tab to render the existing `<PlaybookManager />` (import it; do not rebuild). Ensure `NativeSecurityConsole.tsx` stays < 500 lines (tabs are separate files).</action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "RemediationQueueTab|NativeSecurityConsole" | grep -vE "github-mcp-server|servers/src" || echo "no errors"; npm run build 2>&1 | tail -3; wc -l components/NativeSecurityConsole.tsx</automated>
    <human-check>Open the console: Findings shows scan/vuln/FIM data with a working Scan-now; Remediation Queue shows a pending destructive remediation and Approve dispatches it (Deny cancels); Playbooks shows the library; Audit shows the immutable trail. Confirm the server still enforces approval (a denied item does not execute).</human-check>
  </verify>
  <done>Queue tab approve/deny works; Playbooks embeds PlaybookManager; console < 500 lines; builds.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-54-05 | EoP | UI approves a remediation the caller shouldn't | mitigate | Approve/deny endpoints are permission-gated server-side (53-04); UI gate is convenience |
| T-54-06 | Tampering | UI bypasses the approval gate | mitigate | Server enforces the gate; UI only triggers/displays (D-05) |
| T-54-SC | Tampering | package installs | accept | No new deps; reuses existing components |
</threat_model>

<verification>
- `npx tsc --noEmit` clean for touched files; `npm run build` succeeds; `NativeSecurityConsole.tsx` < 500 lines.
- Tabs render with data + states; approve/deny + trigger call the right endpoints; PlaybookManager reused.
</verification>

<success_criteria>
A tabbed Native Security Console surfaces scan/vuln/FIM findings (with on-demand scan), the remediation queue with approve/deny, the playbook library, and the immutable audit — reusing existing components and enforcing the server-side approval gate.
</success_criteria>

<output>
Create `.planning/phases/54-integration-operator-ui/54-03-SUMMARY.md` when done.
</output>
