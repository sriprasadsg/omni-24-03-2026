phase: 15-evidence-review-workflow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/evidence_review_service.py
  - backend/evidence_review_endpoints.py
  - backend/tests/test_evidence_review.py
  - backend/router_registry.py
  - components/EvidenceReviewPanel.tsx
autonomous: true
requirements:
  - REV-01
  - REV-02
  - REV-03
must_haves:
  truths:
    - "POST /api/evidence/{evidence_id}/review creates a review record with status 'pending', reviewer (from auth token), and optional comment (REV-01)"
    - "PATCH /api/evidence/{evidence_id}/review/{review_id} accepts status of 'approved', 'rejected', or 'changes_requested' with a required comment for non-approved decisions (REV-01)"
    - "GET /api/evidence/{evidence_id}/reviews returns all review records sorted by created_at descending (REV-02)"
    - "When evidence is approved, its status field is updated to 'approved' in the control_evidence collection (REV-02)"
    - "When evidence is rejected or changes_requested, its status is updated to 'rejected' or 'needs_revision' respectively (REV-02)"
    - "GET /api/evidence/pending-review returns all evidence records with status 'pending_review' for the tenant (REV-03)"
    - "POST /api/evidence/{evidence_id}/submit-for-review sets evidence status to 'pending_review' (REV-03)"
    - "All 8 unit tests in test_evidence_review.py pass green"
    - "EvidenceReviewPanel.tsx renders review thread, submit-for-review button, approve/reject/request-changes buttons (role-gated), and comment input"
    - "router_registry.py registers evidence_review_endpoints"
  artifacts:
    - path: "backend/evidence_review_service.py"
      provides: "EvidenceReviewService: create_review, update_review_decision, get_reviews, get_pending_evidence, submit_for_review"
    - path: "backend/evidence_review_endpoints.py"
      provides: "router at /api/evidence with review sub-routes"
    - path: "backend/tests/test_evidence_review.py"
      provides: "8-test TDD suite"
    - path: "components/EvidenceReviewPanel.tsx"
      provides: "Review thread, status badges, action buttons, comment textarea"
  key_links:
    - from: "backend/evidence_review_endpoints.py:update_review_decision"
      to: "backend/evidence_review_service.py:update_review_decision"
      via: "await evidence_review_service.update_review_decision(review_id, decision, comment, db)"
    - from: "backend/evidence_review_service.py:update_review_decision"
      to: "control_evidence collection"
      via: "db.control_evidence.update_one({'id': evidence_id}, {'$set': {'status': mapped_status}})"

<objective>
Implement a per-evidence approval/review thread workflow. This closes the gap versus Comp AI and OpenLane Core which both support evidence review with comments before it counts toward compliance score.

**Review lifecycle:**
```
Uploaded → [submit for review] → pending_review
  → [reviewer approves]     → approved   (counts toward score)
  → [reviewer rejects]      → rejected
  → [reviewer requests edits] → needs_revision → [uploader resubmits] → pending_review
```

**MongoDB schema (evidence_reviews collection):**
```json
{
  "id": "rev-{uuid}",
  "tenantId": "...",
  "evidenceId": "...",
  "reviewer": "...",
  "status": "pending|approved|rejected|changes_requested",
  "comment": "...",
  "created_at": "...",
  "updated_at": "..."
}
```

**TDD order:**
1. Write 8 tests RED:
   - test_submit_for_review_sets_status
   - test_create_review_record
   - test_approve_evidence_updates_status
   - test_reject_requires_comment
   - test_changes_requested_sets_needs_revision
   - test_get_reviews_sorted_by_date
   - test_get_pending_returns_only_pending_review
   - test_tenant_isolation

2. Implement evidence_review_service.py (GREEN)
3. Implement evidence_review_endpoints.py (router at /api/evidence-reviews)
4. Register in router_registry.py
5. Build EvidenceReviewPanel.tsx:
   - Shows review history as a thread (comment + status + reviewer + date)
   - "Submit for Review" button (shown to uploaders when status is draft/needs_revision)
   - "Approve" / "Reject" / "Request Changes" buttons (shown to reviewers only — role: admin or compliance_reviewer)
   - Comment textarea required for Reject/Request Changes

**Role gating:** Only users with role `admin`, `platform-admin`, or `compliance_reviewer` can make review decisions. Any user can submit for review.

Output: 5 files, 8 passing tests, full review lifecycle working end-to-end
</objective>
