---
context: default
phase: null
task: 14
total_tasks: 16
status: in_progress
last_updated: 2026-08-25T01:05:40Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: **do not run the live agent test again blind.** 4 runs this session/last, all inconclusive on the real question (why doesn't the queued instruction get delivered). Get real observability first — redirect the backend's `uvicorn` log to a file (currently goes to a pts terminal, not capturable), or add a temporary `log::info!` line inside `instructions.rs::poll()` and rebuild. Do that BEFORE running the agent a 5th time.
- [ ] CONSTRAINT: **do not conclude a bug from a route decorator's signature alone — read the function body.** A prior check-in this session claimed "CONFIRMED real production bug" based on a route's param name, without reading what the function actually does. Had to retract it (commit `fb3eb1e4f`). The handler was already correct.
- [ ] CONSTRAINT: **background Bash commands intermittently get killed** in this environment. Foreground `timeout`-bounded calls work reliably.

**Do not proceed until acknowledged.**

<current_state>
Phase 66's live-agent test: registration and connectivity are proven solid (real heartbeats, real telemetry). The actual `scan_file` instruction delivery is still unexplained after 4 test runs and 2 ruled-out hypotheses. Full history, including a retracted false claim, is in `.planning/phases/66-full-yara-rule-engine-for-native-scan/66-LIVE-TEST-RUNBOOK.md` — read bottom-to-top, most recent section is authoritative.
</current_state>

<completed_work>
See `.planning/HANDOFF.json`'s `completed_tasks`. Full detail in the runbook, not duplicated here.
</completed_work>

<remaining_work>

**Task 14 (the actual next step) — get observability, then retry:**
1. Restart the backend with its log going to a file instead of a pts
   terminal (`ps aux | grep uvicorn` to find the current run command;
   restart with `> backend.log 2>&1` or similar), OR add a temporary
   `log::info!("polling instructions from {url}")` / result line inside
   `agent-install/omni-agent-rs/src/instructions.rs::poll()` and rebuild
   (`cargo build --release` in `agent-install/omni-agent-rs/`).
2. Re-run the agent once (foreground, `timeout`-bounded — background
   commands get killed unpredictably in this environment).
3. Read whichever log now has real visibility. Confirm: does the request
   reach the server at all? If yes, what does the handler actually return?
4. Only then form a new hypothesis.

Reusable state: `config.yaml` (real credentials, no re-registration
needed), test instruction `6a8ce75f12e698ced33d2477` (currently `status:
"pending"`, correct `agent_id`), `/tmp/eicar_test.txt`.

**Task 15 — deepen SOAR audit:** `.planning/audits/SOAR-AUDIT-RUNBOOK.md`.

**Task 16 — broader "what else is broken":** only SOAR checked so far.
</remaining_work>

<decisions_made>
- Retracted a wrong bug claim transparently rather than leave it standing.
- Used DB-status-flip as an observability proxy when the real log wasn't reachable in time — worked for a yes/no signal, but the signal came back "no" both times without explaining why.
</decisions_made>

<blockers>
- Backend's live log isn't in a tailable file (goes to a pts terminal). Not a hard blocker — restart with output redirected, or instrument the Rust side instead. Neither attempted yet.
</blockers>

## Required Reading (in order)
1. This file.
2. `.planning/phases/66-full-yara-rule-engine-for-native-scan/66-LIVE-TEST-RUNBOOK.md` — bottom-to-top.

## Infrastructure State
- Backend on `:5000`, healthy, logs to a pts terminal (not a file).
- No `omni-agent` process running.
- `agent-install/omni-agent-rs/target/release/config.yaml` has real working credentials.
- Test instruction `6a8ce75f12e698ced33d2477` in Mongo, `status: "pending"`.

<next_action>
Start with: get real log visibility (restart backend with output to a file, or instrument `instructions.rs::poll()`) before running the agent again.
</next_action>
