---
context: default
phase: null
task: 13
total_tasks: 15
status: in_progress
last_updated: 2026-08-25T00:57:35Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: **stale-doc trap** — planning docs (`*-VERIFICATION.md` etc.) in this repo repeatedly claim a status that doesn't match reality, in both directions. Never trust a doc's claim — re-derive from source + a real run before acting. Bit phases 32, 64, 71, 66 in an earlier session.
- [ ] CONSTRAINT: **phase-number collisions** — mostly resolved as of an earlier session (see `dual-roadmap-track-collision` memory), but re-check with `find .planning -iname "NN-*" -type d` before touching any phase numbered 50-67.
- [ ] CONSTRAINT: **background Bash commands intermittently get killed** in this environment (happened repeatedly across sessions — pytest runs, and this session's `timeout 90 ./omni-agent` came back SIGKILL'd, exit 137, not the SIGTERM `timeout` normally sends). Foreground calls with a `timeout` param have worked reliably. Prefer foreground for anything long-running here.
- [ ] CONSTRAINT: **`agentic_mode_enabled` in the Rust agent's config.yaml is dead code** — declared in `config.rs`, never read anywhere else. Do not assume it gates anything without re-checking if the source changes.

**Do not proceed until acknowledged.**

<current_state>
Continuing a long cross-phase audit-fix session. Most of the original scope (phases 32/50-65/64/71/66-docs) is done and committed from earlier in this thread. Currently mid-way through Phase 66's live-agent test (task 13) and a SOAR feature audit (task 14) — both were explicitly pushed into despite repeated context-critical warnings, each step confirmed with the user first. Session is pausing again now at another context-critical point.
</current_state>

<completed_work>

This session specifically (task 11-12, phase 66 live-agent test):
- Found tenant `platform-admin`'s real `registrationKey` directly in local Mongo (`omni_platform.tenants`).
- Wrote `agent-install/omni-agent-rs/target/release/config.yaml` pointing at `http://localhost:5000` with that key.
- Ran the agent binary — it self-registered for real (`POST /api/agents/register`), config.yaml now has a real `agent_id`/`agent_token`, confirmed real heartbeats with live telemetry posted to the backend.
- Queued a test `scan_file` instruction directly into Mongo's `agent_instructions` collection. Ran the agent through 2 full heartbeat cycles (~90s) — instruction stayed `pending`, never picked up.
- Investigated why: `agentic_mode_enabled` (leading suspect) is dead code — grep confirms it's declared once, never read. The real poll loop (`lib.rs:121`, `instructions::poll()`) runs unconditionally every cycle. But `poll()` is an HTTP GET to a backend endpoint (`instructions.rs:5,14`), NOT a direct Mongo query — so the test instruction, inserted straight into Mongo, likely never matched whatever that endpoint's real query filter is (leading guess: `tenant_id`, which was empty string on both the agent config and the test instruction doc).

Full detail with exact grep results and line numbers:
`.planning/phases/66-full-yara-rule-engine-for-native-scan/66-LIVE-TEST-RUNBOOK.md`
(3 progress sections appended this session, most recent at the bottom).

Prior session's work (phases 32/50-65/64/71, SOAR first pass) — see git log or the earlier `.continue-here.md` content now superseded by this one; not re-summarized here to save space, nothing about it changed this session.
</completed_work>

<remaining_work>

**Task 13 — finish the phase 66 live-agent test:**
1. Read the rest of `agent-install/omni-agent-rs/src/instructions.rs::poll()` — specifically how `url` is constructed from `cfg.api_base_url` (only saw `.get(&url)` at line 14, not the URL-building line before it).
2. Grep `backend/*.py` for the matching route, read its actual query filter.
3. Fix the mismatch — either set the correct `tenant_id` on a fresh test instruction, or (better) trigger it through whatever real backend flow inserts `agent_instructions` correctly (mirror `backend/compliance_scans_endpoints.py`'s pattern) instead of a raw Mongo insert.
4. Re-run the agent (foreground, `timeout` bounded), confirm the instruction gets picked up and a real scan_file verdict comes back.
5. Repeat for the cache-invalidation-on-feed-update test (same runbook, "Step 4").
6. Update `66-VERIFICATION.md` with actual results; consider a fresh `gsd-verifier` pass if both pass clean, rather than hand-editing status.

Agent's `config.yaml` already has working credentials — no need to re-register.

**Task 14 — deepen SOAR audit:** see `.planning/audits/SOAR-AUDIT-RUNBOOK.md`. First pass (core engine + security connector) was clean, no fabrication found. Not done: messaging/cloud connectors, live execution test.

**Task 15 — broader "what else is broken" question:** only SOAR has been checked. Method that worked (grep for mock/simulated/hardcoded-fake-response signals in "make client" functions, check what tests actually mock) is reusable for another feature area if continuing this line of work.
</remaining_work>

<decisions_made>

- Read tenant registrationKey directly from local Mongo rather than hunting for an admin UI/endpoint flow — fastest reliable path in a local dev environment.
- Ran the agent in short, bounded foreground `timeout` windows rather than backgrounding it, given this environment's pattern of killing backgrounded long-running processes.
- Chose to keep pushing into the live-agent test across multiple context-critical warnings only because the user explicitly confirmed each time — did not push through unprompted.
</decisions_made>

<blockers>
None blocking — the poll()-endpoint-vs-raw-Mongo-insert mismatch is a concrete, findable next step, not a dead end.
</blockers>

## Required Reading (in order)

1. This file, fully.
2. `.planning/phases/66-full-yara-rule-engine-for-native-scan/66-LIVE-TEST-RUNBOOK.md` — full detail on the live-agent test, read bottom-to-top for the latest state first.
3. `.planning/audits/SOAR-AUDIT-RUNBOOK.md` — if resuming task 14.

## Infrastructure State

- Backend (`uvicorn app:socket_app`) on `:5000`, healthy.
- No `omni-agent` process left running (confirmed via `ps aux` before this pause).
- `agent-install/omni-agent-rs/target/release/config.yaml` has real, working registered credentials for tenant `platform-admin` — reusable, no re-registration needed.
- One test instruction sits in Mongo `omni_platform.agent_instructions` (`_id: 6a8ce75f12e698ced33d2477`, `status: "pending"`) — harmless leftover, fine to reuse or delete.
- `/tmp/eicar_test.txt` exists (EICAR test string) — reusable for the scan_file test.

<context>
Fresh context window each resume, but this thread has now paused/resumed twice for the same underlying work. The pattern that's working: check in with the user via AskUserQuestion before each risky/uncertain next step when context is already low, rather than silently pushing forward or silently stopping. Keep doing that.
</context>

<next_action>
Start with: read `agent-install/omni-agent-rs/src/instructions.rs` in full (not just the grep excerpt) to see how `poll()` builds its target URL from `cfg.api_base_url`.
</next_action>
