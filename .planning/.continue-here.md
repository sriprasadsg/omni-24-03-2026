---
context: default
phase: null
task: 14
total_tasks: 16
status: in_progress
last_updated: 2026-08-25T01:02:02Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: **do not conclude a bug from a route decorator's signature alone — read the function body.** This session claimed "CONFIRMED real production bug" in `deployment_result_endpoints.py`'s instruction-poll endpoint based on its param name (`agent_id`) not matching what the Rust client sends (`hostname`). That claim was wrong and had to be retracted — the handler already resolves either value via a `$or` query. Committed the wrong claim to git twice before catching it. **Always read the full function body before asserting root cause, especially under time/context pressure — pressure is exactly when this mistake happens.**
- [ ] CONSTRAINT: **stale-doc trap** (from earlier sessions) — planning docs in this repo repeatedly claim a status that doesn't match reality, both directions. Never trust a doc's claim — re-derive from source + a real run.
- [ ] CONSTRAINT: **background Bash commands intermittently get killed** in this environment. Foreground `timeout`-bounded calls work reliably. Use those for anything long-running.

**Do not proceed until acknowledged.**

<current_state>
Phase 66's live-agent test found real progress (agent registers, connects, heartbeats for real) but the actual `scan_file` instruction was never delivered/picked up, and the reason is genuinely still unknown after multiple ruled-out hypotheses. Full blow-by-blow, including the retracted false claim and why it was wrong, is in `.planning/phases/66-full-yara-rule-engine-for-native-scan/66-LIVE-TEST-RUNBOOK.md` — read it bottom-to-top, the most recent section (the correction) is authoritative over the two sections above it.
</current_state>

<completed_work>
See `.planning/HANDOFF.json`'s `completed_tasks` for the compact version. Full detail, including exact grep results, code snippets, and the retraction, is in the runbook — not duplicated here to save space.
</completed_work>

<remaining_work>

**Task 14 — find the real root cause (this is the actual next step):**
1. Re-run the live agent test (`agent-install/omni-agent-rs/target/release/config.yaml` already has real, working credentials — no re-registration needed) with the backend's `uvicorn` request log visible.
2. Watch specifically for `GET /api/agents/security-test-vm/instructions` appearing in that log during the poll window. This one observation splits the remaining hypotheses cleanly: if it never appears, the problem is client-side (auth, networking, or the poll loop not firing as expected); if it appears but returns nothing useful, the problem is server-side (agent lookup, instruction query, or the delivered/pending status logic).
3. Only then form a new hypothesis — don't guess further without that observation first.
4. Full candidate list (4 items, none confirmed) is in the runbook's final section.

**Task 15 — deepen SOAR audit:** `.planning/audits/SOAR-AUDIT-RUNBOOK.md`. First pass clean, messaging/cloud connectors and live execution not yet covered.

**Task 16 — broader "what else is broken":** only SOAR checked so far.
</remaining_work>

<decisions_made>
- Corrected a wrong "confirmed bug" claim transparently (retraction commit `fb3eb1e4f`) rather than leaving it standing or quietly working around it.
</decisions_made>

<blockers>
None — task 14 has a clear, cheap next observation to make (check the request log), not a dead end.
</blockers>

## Required Reading (in order)

1. This file.
2. `.planning/phases/66-full-yara-rule-engine-for-native-scan/66-LIVE-TEST-RUNBOOK.md` — read bottom-to-top, the final "CORRECTION" section is the current authoritative state.

## Infrastructure State

- Backend on `:5000`, healthy.
- No `omni-agent` process left running.
- `agent-install/omni-agent-rs/target/release/config.yaml` has real, working registered credentials (tenant `platform-admin`, hostname `security-test-vm`) — reusable.
- Test instruction still sits in Mongo `omni_platform.agent_instructions` (`_id: 6a8ce75f12e698ced33d2477`, `status: "pending"`) — reusable for the next attempt, or delete and recreate fresh (its `agent_id` field is correct: `agent-310ee2eca93f47d2ae60efdd16ac70c4`).
- `/tmp/eicar_test.txt` exists, reusable.

<next_action>
Start with: run the agent (foreground, `timeout`-bounded) while watching the backend's uvicorn log (likely need to redirect/tail its output — check how it's currently logging, e.g. `journalctl`, a log file, or attach to the running process's stdout if possible) for a hit on `GET /api/agents/security-test-vm/instructions`.
</next_action>
