---
context: default
phase: null
task: 13
total_tasks: 15
status: in_progress
last_updated: 2026-08-25T00:46:22Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: **stale-doc trap** — this session repeatedly found that `*-VERIFICATION.md`/`*-REVIEW.md`/planning docs claim a status that doesn't match the actual codebase (both directions: claiming "gaps_found" when already fixed, and claiming resolved when a later section of the SAME file still lists it open). **Never trust a doc's status field or prose claim — always independently re-derive from source + a real test run before acting on it.** This bit phases 32, 64, 71, and 66 specifically.
- [ ] CONSTRAINT: **phase-number collisions** — phases 62-67 and formerly 50-65 have/had multiple competing directories under this repo's `.planning/` tree. Before touching ANY phase numbered 50-67, run `find .planning -iname "NN-*" -type d` first to check for collisions. See `dual-roadmap-track-collision` memory (in `/root/.claude/projects/-home-user-enterprise-omni-agent-ai-platform/memory/`) for full history — mostly resolved as of this session, but re-check if anything looks off.
- [ ] CONSTRAINT: **background Bash commands intermittently get killed** in this environment (happened twice this session running the full pytest suite via `run_in_background: true`). Foreground calls with a long `timeout` param worked reliably both times. Prefer foreground for long-running verification commands here.

**Do not proceed until acknowledged.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Trusting VERIFICATION.md status fields | Docs go stale in both directions after fixes land or after a later doc-debt cleanup session | blocking | Always spawn a fresh `gsd-verifier` (or independently re-derive from source) before acting on a doc's claimed status |
| `/gsd-execute-phase` no-ops on phases with an existing non-`missing` VERIFICATION.md | Its own step logic only auto-resumes at the verify gate when status is exactly `missing`; `gaps_found`/`unknown`/etc. all report "No matching incomplete plans" and exit | advisory | To force a fresh verification, spawn `gsd-verifier` directly with `Agent()`, mirroring the prompt pattern in `execute-phase.md`'s `verify_phase_goal` step — don't go through `/gsd-execute-phase` |

**Severity values:** `blocking` — resuming agent must pass understanding check before proceeding. `advisory` — important context, does not gate resumption.

<current_state>
No single active phase — this was a long, cross-cutting session that ran `/gsd-audit-fix`, found real bugs, and kept fixing what it found across phases 32/50-65/64/71/66, then started (and ran low on budget during) two follow-on asks: finishing phase 66's live-agent verification, and auditing SOAR for the same fabrication pattern found in phase 32.
</current_state>

<completed_work>

Full list with commit hashes in `.planning/HANDOFF.json`'s `completed_tasks`. Headline items:

- **Real bug found and fixed**: `poll_oci_cloud_guard_problems`/`poll_alibaba_sas_alerts`/`poll_cloudflare_zero_trust_events` were fabricating hardcoded fake SIEM findings in production, flagged critical in `32-REVIEW.md` a month earlier and never fixed until this session. Now fail safe, and Test Connection/Discover for those 3 providers wired to the real, already-existing CSPM polling functions instead.
- **Phase 71 gap closed**: `Asset.purchase_order_id` was write-only (validated by backend, never displayed). Now shown in `FinancePanel.tsx`.
- **Phases 32, 71, 64 independently re-verified** by fresh `gsd-verifier` passes (not trusted from old docs) — all now genuinely `passed`.
- **Systemic routing bug fixed**: phases 50-61 (except two genuinely-dead phase-64/65 drafts) were archived into `.planning/milestones/*-phases/`, unreachable by any `gsd_run` phase-number query. Moved back to `.planning/phases/` after individually checking each for collisions. The two dead drafts got archived as `_superseded-*` with `SUPERSEDED.md` files, following this repo's own existing convention.
- **Phase 66 doc contradictions fixed**: multiple items in `66-VERIFICATION.md` claimed to be open while a later section of the *same file* already recorded them resolved (same-day doc-debt cleanup that never got reconciled against the earlier draft).
</completed_work>

<remaining_work>

**Task 13 — Phase 66 live-agent test** (see `.planning/phases/66-full-yara-rule-engine-for-native-scan/66-LIVE-TEST-RUNBOOK.md` for full detail):
- Backend is healthy at `http://localhost:5000`; Rust agent binary already built at `agent-install/omni-agent-rs/target/release/omni-agent`.
- Registration flow confirmed: `POST /api/agents/register`, handled by `backend/agent_registry_endpoints.py`, payload shape in `backend/trigger_register.py`.
- **Not found yet**: what `registrationKey` is actually valid for this tenant. Read `agent_registry_endpoints.py`'s handler next.
- After that: get a signed feed bundle cached (`backend/agent_security_feed_service.py`), find the real instruction-dispatch mechanism (likely `agent_instructions`/`instructions` Mongo collection — `soar_engine.py` was seen writing playbook actions to `mongodb.db.instructions` this session, may be the same queue), dispatch `scan_file`, and test cache invalidation on feed update. Full expected JSON shapes are in the runbook.

**Task 14 — Deepen SOAR audit** (see `.planning/audits/SOAR-AUDIT-RUNBOOK.md`):
- First pass done, clean: no fabrication markers in 1341 lines across 6 core files; connectors make real `aiohttp` calls; tests mock at the transport layer correctly (not hiding a fake business-logic result); playbook actions write to the real agent instruction queue.
- Not done: `soar_connectors_messaging.py`/`soar_connectors_cloud.py` got lighter treatment than `soar_connectors_security.py`; no live execution test; not every method read.

**Task 15 — the user's original question was broader**: "are there any features not working, like SOAR and other features?" Only SOAR got checked. If continuing this line of work, the method that worked (grep for `mock`/`simulated`/`TODO`/`fabricat`/hardcoded fake response literals in "make client" functions, then check what the tests actually mock) is reusable — pick another feature area and repeat it.
</remaining_work>

<decisions_made>

- Fixed the SIEM fabrication by making the functions fail-safe (return 0) rather than writing new, unverifiable real SDK integration code — `32-REVIEW.md` itself sanctioned this path, and it matches an existing `aws_guardduty`/`aws_securityhub` precedent already in the codebase.
- Then, separately, wired those same providers' Test Connection/Discover buttons to the CSPM-domain polling functions that already existed and were already real/tested — best of both: no fabrication, and real data where a real implementation already existed.
- Chose to move archived phases back into `.planning/phases/` (restoring `gsd_run` resolution) rather than leave them archived and route around the tooling gap — matches how every other historical phase in this repo is organized.
</decisions_made>

<blockers>
- Phase 66 live-agent test: no known-valid `registrationKey` for this tenant yet. Not a hard blocker — just the next concrete step (read `agent_registry_endpoints.py`).
</blockers>

## Required Reading (in order)

1. This file, fully.
2. `.planning/phases/66-full-yara-rule-engine-for-native-scan/66-LIVE-TEST-RUNBOOK.md` — if resuming task 13.
3. `.planning/audits/SOAR-AUDIT-RUNBOOK.md` — if resuming task 14 (already partially executed; treat its "expected output" section as still pending, not the starting point).
4. `dual-roadmap-track-collision` memory (`/root/.claude/projects/-home-user-enterprise-omni-agent-ai-platform/memory/dual-roadmap-track-collision.md`) — full history of the phase-numbering issue this session mostly resolved.

## Infrastructure State

- Backend (`uvicorn app:socket_app`) running on `:5000`, healthy.
- Legacy Python agent process (`agent/venv/bin/python agent.py`) running — NOT the Rust agent; the Rust binary at `agent-install/omni-agent-rs/target/release/omni-agent` is built but not currently running as a live process.
- Vite dev server running (`node_modules/.bin/vite`).
- No background jobs from this session left running.

<context>
This session had access to peer Claude sessions (via SendMessage/teammate-message) that ran `gsd-verifier` passes in parallel in the background — that pattern worked well for getting independent, non-self-trusting re-verification of fixes. Worth reusing for task 13/14 if similar independent verification is needed.
</context>

<next_action>
Start with: read `backend/agent_registry_endpoints.py`'s register handler to find what `registrationKey` value is valid for this tenant, then continue `66-LIVE-TEST-RUNBOOK.md` Step 1.
</next_action>
