---
context: default
phase: null
task: 16
total_tasks: 19
status: in_progress
last_updated: 2026-08-25T01:12:01Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: **do not conclude a bug from a route decorator's signature alone — read the function body.** Cost a retraction earlier in this investigation (commit `fb3eb1e4f`). The eventual real bug (duplicate agents docs) was found properly, by tracing through actual code and a real DB query — keep doing it that way.
- [ ] CONSTRAINT: **background Bash commands, and even some foreground long-running ones, intermittently get SIGKILL'd** in this environment. Not fully explained. Foreground `timeout`-bounded calls mostly worked but were killed early at least twice this session too. Budget extra margin for this.
- [ ] CONSTRAINT: **check what depends on current behavior before "fixing" it.** The duplicate-agents-doc bug's fix isn't obvious — upserting on hostname at registration is the root-cause fix, but something might rely on multiple docs per hostname existing (audit trail? device history?). Read `agent_registry_endpoints.py`'s full register handler before choosing an approach.

**Do not proceed until acknowledged.**

<current_state>
Phase 66's live-agent test investigation is DONE — it found a real, confirmed, previously-unknown bug: `deployment_result_endpoints.py`'s `get_agent_instructions` resolves an agent's hostname to its canonical `id` via an unsorted `find_one({"$or":[...]})`; if a hostname has multiple `agents` documents (happens when an agent gets re-registered without the old doc being cleaned up), this can silently resolve to the wrong, stale `id`, and the agent then polls for instructions under an `agent_id` nothing was ever queued against. Confirmed via direct evidence this session (two real documents found for hostname `security-test-vm`), not guessed.

What's NOT done: deciding and implementing the actual fix, then re-running the live test to prove it end-to-end.
</current_state>

<completed_work>
Full blow-by-blow (10 progress check-ins across 3 resume cycles) is in
`.planning/phases/66-full-yara-rule-engine-for-native-scan/66-LIVE-TEST-RUNBOOK.md`.
Read bottom-to-top — the "CONFIRMED ROOT CAUSE" section at the very bottom
is the current, authoritative state; everything above it is the trail that
led there (including one retracted wrong claim, clearly marked).
</completed_work>

<remaining_work>

**Task 16 (the actual next step) — decide the fix:**
1. Read `backend/agent_registry_endpoints.py`'s `/register` handler in
   full (only partially read this session — saw the `registration_key`
   validation and `db.tenants.find_one` lookup, not the part that writes
   the `agents` document).
2. Check: does it currently `insert_one` unconditionally, or is there
   already an upsert/dedup check that isn't working? If it's a plain
   insert, that's the root cause of the duplication itself.
3. Decide: (a) patch `get_agent_instructions`'s query to sort by recency
   (smaller, safer, treats the symptom), or (b) make registration upsert
   on hostname (fixes root cause, larger blast radius — check nothing
   relies on historical agent docs first, e.g. is there an "agent history"
   or "device history" feature that expects multiple docs per hostname?).
4. Implement the chosen fix, with a test.

**Task 17 — re-run the live test** once the fix lands, this time expecting
success. Also still need the cache-invalidation-on-feed-update test
(runbook's original "Step 4", never attempted — this whole detour started
before reaching it).

**Task 18 — deepen SOAR audit:** `.planning/audits/SOAR-AUDIT-RUNBOOK.md`.

**Task 19 — broader "what else is broken":** only SOAR checked so far.
</remaining_work>

<decisions_made>
- Prioritized finding phase 66's real root cause over moving to SOAR/broader audit, since a live-agent test surfacing an actual undiscovered bug is more valuable than continuing a checklist.
- Did NOT decide the fix approach this session (ran out of budget right after confirming the cause) — deliberately left as an open decision rather than guessed under pressure.
</decisions_made>

<blockers>
None — task 16 has a clear next read (`agent_registry_endpoints.py`'s register handler in full) before any decision.
</blockers>

## Required Reading (in order)
1. This file.
2. `.planning/phases/66-full-yara-rule-engine-for-native-scan/66-LIVE-TEST-RUNBOOK.md` — bottom-to-top, "CONFIRMED ROOT CAUSE" section is authoritative.
3. `backend/agent_registry_endpoints.py` — the register handler, in full (not yet read past the registrationKey validation).

## Infrastructure State
- Backend on `:5000`, healthy.
- No `omni-agent` process running.
- `agent-install/omni-agent-rs/target/release/config.yaml` has real credentials for `agent-310ee2eca93f47d2ae60efdd16ac70c4`.
- Confirmed duplicate: Mongo `omni_platform.agents` has 2 docs for `hostname: "security-test-vm"` — `agent-1ba27cf216b94d71b4f18a2791dc9b8b` (pre-existing/stale) and `agent-310ee2eca93f47d2ae60efdd16ac70c4` (this session's). Don't delete either without checking the fix decision first — the stale one is useful evidence for reproducing/testing the fix.
- Test instruction `6a8ce75f12e698ced33d2477` in `agent_instructions`, `status: "pending"`, correct `agent_id`. Reusable once the fix lands.

<next_action>
Start with: read `backend/agent_registry_endpoints.py`'s `/register` handler in full to see exactly how it writes the `agents` document, before deciding between a sort-by-recency patch and an upsert-on-hostname fix.
</next_action>
