---
milestone: "3.2"
audited: 2026-07-21T19:00:00Z
status: gaps_found
scores:
  requirements: 9/10
  phases: 4/5
  integration: not_run
  flows: not_run
gaps:
  requirements:
    - id: "RUST-01"
      status: "partial"
      phase: "40"
      claimed_by_plans: ["40-01-PLAN.md"]
      completed_by_plans: ["40-01-SUMMARY.md"]
      verification_status: "gaps_found"
      evidence: "40-VERIFICATION.md: Cargo.toml adds the native-tls feature literal but leaves reqwest's default-features ON, so default-tls=[\"rustls\"] still compiles the full rustls stack (rustls, rustls-webpki, aws-lc-rs, rustls-platform-verifier, tokio-rustls) into the binary alongside native-tls. Confirmed via `strings backend/static/omni-agent-2.1.3-windows.exe | grep rustls` — rustls/aws-lc-rs source paths and TLS-handshake code are physically present. No call site (`src/lib.rs:40`, `agent_update.rs:34,67`) ever calls `.use_native_tls()`; the runtime backend choice is an undocumented reqwest-internal fallback (`tls::TlsBackend::default()`), not the explicit, documented decision the success criterion required. REQUIREMENTS.md was checked off [x] prematurely at commit 9643e37."
  integration: []
  flows: []
tech_debt:
  - phase: 40-rust-agent-modernization-session-reliability
    items:
      - "Mechanism B of the intermittent-401 bug (multi-tab/cloned-tab sessionStorage race) explicitly deferred per D-05 — Mechanism A (missing revoked_tokens.jti unique index) fixed and verified"
  - phase: 41-cspm-provider-expansion-oci-alibaba-cloudflare
    items:
      - "Verified via /gsd-verify-work UAT (not the standard gsd-verifier VERIFICATION.md path) — 41-UAT.md status: complete, 7/7 tests, 1 issue found and fixed inline during verification (OCI credential form was missing oci_region/oci_compartment_id, silently keeping every OCI scan simulated forever even with valid credentials)"
      - "Genuinely non-simulated CSPM scan data for OCI/Alibaba/Cloudflare requires live cloud credentials this environment doesn't have — inherent live-account limitation, not a code defect"
  - phase: 43-remediation-to-ticketing-bridge
    items:
      - "atlassian-python-api SDK remains commented out in requirements.txt — confirmed unnecessary (ticketing_service.py uses raw httpx, not the SDK), left commented per research finding"
  - phase: 44-remediation-sla-escalation
    items: []
nyquist:
  compliant_phases: [41, 42, 43, 44]
  partial_phases: []
  missing_phases: [40]
  overall: "mostly compliant — phase 40 predates the Nyquist/VALIDATION.md convention adopted from phase 41 onward"
---

# Milestone v3.2 Audit — Agent Modernization & Remediation Ops

**Status:** gaps_found (1 partially-satisfied requirement forces this per the FAIL gate)
**Phases:** 40-44, all 5 executed and phase-level-verified (4 passed, 1 gaps_found)

## Requirements Coverage

| Req ID | Phase | Description | Status |
|--------|-------|--------------|--------|
| RUST-01 | 40 | Rust agent modernized deps + explicit TLS decision + 2.1.3 rebuild | ⚠️ **PARTIAL** — TLS-backend sub-clause not met (see gap above) |
| SESS-01 | 40 | Root-cause + fix intermittent 401 | ✓ Satisfied |
| CSPM-01/02/03 | 41 | Real OCI/Alibaba/Cloudflare CSPM checks | ✓ Satisfied (UAT, 1 bug found+fixed during verification) |
| CMT-01 | 42 | Comment threads on compliance controls | ✓ Satisfied |
| REM-01/02 | 43 | Jira/ServiceNow ticketing bridge + close-loop | ✓ Satisfied |
| SLA-01/02 | 44 | Remediation SLA + immutable escalation history | ✓ Satisfied |

Note: `.planning/REQUIREMENTS.md`'s traceability table (lines 358-367) still shows all 10 v3.2 requirements as "Planned" — this is a known, pre-existing sync gap in the tooling (the checkbox list above it and each phase's VERIFICATION.md/SUMMARY.md are the authoritative sources; the table itself isn't auto-updated by `phase.complete`, consistent with the same staleness already noted and left as out-of-scope during phases 41/42/43 this session).

No orphaned requirements — every REQ-ID in the traceability table maps to exactly one of the 5 phases, and every phase's plans declare the correct ID in frontmatter.

## Phase Verification Summary

| Phase | Status | Score |
|-------|--------|-------|
| 40 | gaps_found | 3/4 truths |
| 41 | complete (UAT path) | 7/7 tests, 1 gap found+fixed live |
| 42 | passed | 4/4 must-haves |
| 43 | passed | 12/12 must-haves |
| 44 | passed | 4/4 must-haves |

## Cross-Phase Integration & E2E Flows

**Not run this session** — the integration-checker subagent was skipped given this session's context budget was already constrained (74%+ used) at this point, and the milestone's overall status is already forced to `gaps_found` by Phase 40's confirmed gap regardless of integration findings. Substantial cross-phase integration was, however, directly exercised live during this session's own execution work:
- Phase 41 → compliance score/dashboard (CSPM findings feed the existing coverage denominator, confirmed via live scan + `/api/cloud-checks/results`)
- Phase 42 → notification system (confirmed live: `db.notifications` write + `GET /api/notifications` bell-icon read)
- Phase 43 → remediation workflow + notification system (confirmed live: ticket creation, 502/422/404 paths, re-scan dispatch reuse)
- Phase 44 → Phase 43's `compliance_remediation_tasks` schema (confirmed live: SLA fields coexist with `ticket_provider`/`ticket_ref`/`ticket_url`, no field collision)

Recommend running `/gsd-audit-milestone 3.2` again (or just the integration-checker step) in a fresh session for full E2E flow coverage before treating this milestone as fully closed, especially given Phase 40's gap still needs resolving first anyway.

## Recommendation

Close the RUST-01 gap before completing the milestone: set `default-features = false` on the `reqwest` dependency in `agent-install/omni-agent-rs/Cargo.toml`, keep only the features actually needed (`native-tls` plus whatever else `default` was providing — check `json`, `stream` usage), rebuild the 2.1.3 Windows executable, re-run `strings ... | grep -i rustls` to confirm it's now absent, and re-verify. This also shrinks the shipped binary and removes dead compiled crypto code from the supply-chain surface.
