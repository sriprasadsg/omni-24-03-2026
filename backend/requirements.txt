# ─────────────────────────────────────────────────────────────────────────────
# Enterprise Omni-Agent AI Platform — Backend Requirements
# Fully audited: 2026-05-25
# Recommended Python: 3.12.x – 3.13.x
# ─────────────────────────────────────────────────────────────────────────────

# ── Build prerequisites (install these first on any Python version) ───────────
# pip install --upgrade pip setuptools wheel
setuptools>=70.0.0
wheel>=0.43.0

# ── Core Web Framework ──────────────────────────────────────────────────────
fastapi>=0.110.0,<1.0.0
uvicorn[standard]>=0.28.0
python-multipart>=0.0.9        # File upload parsing (multipart/form-data)
starlette>=0.37.0              # FastAPI dependency
urllib3>=2.6.3                  # CVE-2025-50181/50182/66418/66471/CVE-2026-21441 fixed in 2.6.3

# ── Authentication & Security ────────────────────────────────────────────────
PyJWT>=2.8.0,<3.0.0            # JWT token creation and verification
bcrypt>=4.0.0,<5.0.0           # Password hashing
cryptography>=46.0.0            # CVE-2026-34073/39892/26007 fixed in 46+; Fernet encryption, TLS helpers
pyotp>=2.9.0                   # TOTP/HOTP for MFA (RFC 6238)
qrcode[pil]>=7.4.2             # QR code generation for MFA enrollment
Pillow>=12.2.0                  # CVE-2026-42308/09/10/11/40192/25990 fixed in 12.2.0; required by qrcode[pil]
python-barcode==0.16.1          # Code128 1D barcode generation for ITAM asset-tag labels (ITAM-CAT-05); pinned exactly (not >=) — automated legitimacy seam graded SUS, human-approved 2026-08-05, a floating bound would let a later release skip review
authlib>=1.3.0,<2.0.0          # OIDC / OAuth2 / SAML SSO flows
defusedxml>=0.7.1,<1.0.0       # Safe XML/SAML parsing (prevents XXE)

# ── Database ─────────────────────────────────────────────────────────────────
motor>=3.3.0,<4.0.0            # Async MongoDB driver (used throughout)
pymongo>=4.6.0,<5.0.0         # Sync fallback + types for motor
redis>=5.0.0,<6.0.0            # Caching, rate-limiting, sessions

# ── Data Validation & Settings ───────────────────────────────────────────────
pydantic[email]>=2.5.0,<3.0.0  # Data validation + email validation
pydantic-settings>=2.2.0,<3.0.0 # Settings management from env files
python-dotenv>=1.0.0           # .env file loading
email-validator>=2.1.0         # Used by pydantic email validation

# ── HTTP & Async Clients ─────────────────────────────────────────────────────
requests>=2.28.0               # Sync HTTP (LLM engine, webhook calls)
httpx>=0.27.0,<1.0.0           # Async HTTP client
aiohttp>=3.9.0,<4.0.0          # Async HTTP sessions
click>=8.1.0,<9.0.0            # omni-cli.py CLI framework

# ── WebSocket & Real-time ─────────────────────────────────────────────────────
python-socketio>=5.11.0        # Socket.IO server (real-time dashboard)
websockets>=12.0               # WebSocket protocol support
simple-websocket>=1.0.0        # Simple WS, required by python-socketio

# ── Task Scheduling ──────────────────────────────────────────────────────────
APScheduler==3.10.4            # Cron-style scheduler (FinOps, patch jobs)
celery>=5.3.0,<6.0.0           # Distributed task queue
kombu>=5.3.0,<6.0.0            # Messaging library (Celery backend)

# ── AI & Machine Learning ────────────────────────────────────────────────────
# Heavy ML packages (torch etc.) are in requirements-ml.txt
# Install with: pip install -r requirements-ml.txt
scikit-learn>=1.3.0,<2.0.0     # ML utilities (UEBA, anomaly detection)
numpy>=1.24.0,<3.0.0           # Numerical operations
pandas>=2.1.0,<3.0.0           # Data analysis and manipulation
joblib>=1.3.0,<2.0.0           # Model persistence and parallel jobs
chromadb>=0.5.0,<2.0.0         # Vector DB for RAG / knowledge base
google-genai>=0.3.0,<2.0.0     # Gemini AI / Google GenAI SDK (new SDK)
anthropic>=0.96.0,<1.0.0       # Anthropic Claude SDK (range required by langchain-anthropic 1.4.8)
arize-phoenix>=4.0.0,<5.0.0
openinference-instrumentation-anthropic>=0.1.0,<2.0.0
opentelemetry-exporter-otlp-proto-http>=1.24.0,<2.0.0

# ── LangChain / LangGraph Orchestration (Phase 39) ───────────────────────────
langchain==1.3.14
langchain-core==1.4.9
langgraph==1.2.9
langchain-openai==1.3.5
langchain-anthropic==1.4.8
langchain-ollama==1.1.0
langgraph-checkpoint-sqlite==3.1.0
openinference-instrumentation-langchain==0.1.67

# ── ReBAC Client ──────────────────────────────────────────────────────────────
openfga-sdk>=0.6.0,<1.0.0          # OpenFGA client for Zanzibar-style ReBAC

# ── Payment Gateways ─────────────────────────────────────────────────────────
stripe>=9.0.0,<12.0.0          # Stripe payment processing
paypalrestsdk>=1.13.1          # PayPal REST SDK
razorpay>=1.4.1                # Razorpay (India) payments
squareup>=27.0.0               # Square payment processing

# ── Reports & Documents ──────────────────────────────────────────────────────
reportlab>=4.0.0               # PDF invoice generation
openpyxl>=3.1.0                # Excel report generation
jinja2>=3.1.0                  # HTML templating

# ── Cloud & Infrastructure ───────────────────────────────────────────────────
boto3>=1.34.0,<2.0.0           # AWS SDK (S3, CloudWatch, CloudTrail)
azure-mgmt-resource>=23.0.1    # Azure Resource Manager
azure-mgmt-network>=25.0.0     # Azure Networking
azure-mgmt-compute>=30.0.0     # Azure Compute
google-cloud-compute>=1.15.0   # GCP Compute Engine
google-cloud-storage>=2.14.0   # GCP Cloud Storage

# ── Network & Security Analysis ──────────────────────────────────────────────
python-nmap>=0.7.1             # Nmap network scanning wrapper
psutil>=5.9.0                  # System metrics: CPU, RAM, Disk, Network
pefile>=2023.2.7               # PE header parsing (binary analysis)
pywin32>=306; sys_platform=="win32"  # Windows ETW/EventLog (EDR)
# scapy>=2.5.0                 # Packet capture (uncomment if needed, requires Npcap on Windows)
# yara-python>=4.5.4           # YARA scanning — C extension, needs build tools on Python 3.14:
#                              #   Windows: pip install yara-python --no-binary yara-python
#                              #   Linux:   apt install libyara-dev && pip install yara-python

# ── Rate Limiting ─────────────────────────────────────────────────────────────
slowapi>=0.1.9                 # FastAPI rate limiter

# ── Ticketing & Integrations ─────────────────────────────────────────────────
# atlassian-python-api>=3.41.0  # Optional: Jira client
# pdpyras>=4.5.1                # Optional: PagerDuty client

# ── Cloud Security Integrations ───────────────────────────────────────────────
google-cloud-securitycenter>=1.23.0   # GCP Security Command Center ingest
azure-monitor-query>=1.2.0            # Microsoft Sentinel log query via Log Analytics
oci>=2.181.1
aliyun-python-sdk-core-v3>=2.13.33
alibabacloud_config20200907>=4.2.3
alibabacloud_sas20181203>=9.3.3
alibabacloud_tea_openapi>=0.4.5
alibabacloud_credentials>=1.0.10
cloudflare>=5.4.0
msal>=1.37.0

# ── GraphQL ──────────────────────────────────────────────────────────────────
strawberry-graphql[fastapi]>=0.323.2  # <0.323.2 breaks on pydantic>=2.13 (is_new_type import)

# ── WebAuthn / Passkeys ──────────────────────────────────────────────────────
webauthn>=2.5.0,<3.0.0    # 3.x requires cryptography>=49, conflicts with alibabacloud-tea-openapi (<49); 2.5.x uses same public API

# ── GeoIP (agent public-IP geolocation) ──────────────────────────────────────
maxminddb>=2.5.0    # Pure-Python reader for a MaxMind GeoLite2-City .mmdb (DB file supplied out-of-band; lookups degrade gracefully when absent)

# ── Code Review Graph ─────────────────────────────────────────────────────────
mcp
# code-review-graph>=2.3.0           # Optional: code knowledge graph (install separately if needed)
