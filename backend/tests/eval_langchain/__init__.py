"""Eval harness package for Phase 39 (LangChain AI Integration).

Tests under this package are marked `eval` (code-based and judged eval-harness
tests) and, where they spend tokens or need a live model, also marked `llm`.
See root `pytest.ini` for marker registration and `.planning/phases/
39-langchain-ai-integration/39-AI-SPEC.md` Section 5 for the evaluation
strategy these tests implement.
"""
